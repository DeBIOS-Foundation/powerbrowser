---
phase: 10-sign-off-closeout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [.planning/phases/10-sign-off-closeout/10-UAT.md, .planning/phases/10-sign-off-closeout/10-01-SUMMARY.md]
autonomous: true
requirements: [MIG-01, MIG-02, GUI-01, GUI-03, GUI-04, GEN-01, GEN-02, GEN-03, GEN-05, TEL-03, DOC-01]
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Every record-close box names its requirement, disposition, exact prior evidence with tracked archive pointers, and what was re-run on the current tree"
    - "The 16-vs-15 count reconciliation is recorded with three pointer lines and the enumeration declared source of truth, never silently renumbered"
    - "Five human UAT sheets exist as staged runbooks with exact commands, prior-evidence citations, and staged-awaiting-signature marks — no signature filled in by the executor"
    - "verify-platform.sh --quick is green on the final tree with no sibling driver created"
  artifacts:
    - path: ".planning/phases/10-sign-off-closeout/10-UAT.md"
      provides: "Five staged human UAT runbook sheets"
      contains: "coverage_id"
    - path: ".planning/phases/10-sign-off-closeout/10-01-SUMMARY.md"
      provides: "Record-close boxes plus count reconciliation"
      contains: "STAGED"
  key_links:
    - from: ".planning/phases/10-sign-off-closeout/10-UAT.md"
      to: ".planning/milestones/v1.0-phases/04-theia-surface-branding-extensions-telemetry/04-UAT.md"
      via: "drill steps reused verbatim in expected/result/evidence/steps shape, never a new schema"
      pattern: "coverage_id"
    - from: ".planning/phases/10-sign-off-closeout/10-01-SUMMARY.md"
      to: ".planning/milestones/v1.1-phases/08-installer-hardening-canonical-rename/08-VERIFICATION.md"
      via: "record-close boxes cite tracked archive truths plus re-run row labels"
      pattern: "VERIFIED"
    - from: ".planning/phases/10-sign-off-closeout/10-UAT.md"
      to: ".planning/WINDOWS.md"
      via: "GUI-01/GUI-03 sheets reuse ledger items 15/16 wording plus 01-UAT tests 2/6 as prior evidence"
      pattern: "GUI-01"
  prohibitions:
    - "No human signature filled in by the executor; staged sheets stay result pending with source human"
    - "No re-proving what 08/09 proved live; credit halves cite, delta halves re-run"
    - "No new check scripts and no sibling drivers; every assertion rides an existing registry row"
  assumptions:
    - "The 2026-09-01 01-UAT human passes for GUI-01/GUI-03 remain valid prior evidence; the task confirms via git log that no GUI-01/GUI-03 surface changed since"
    - "The v1.0 outcomes table enumerates exactly 15 Open rows and the three Complete (static) rows were closed live by v1.1, so the reconciliation is a label-error record, not an investigation"
---

<objective>
Establish the Phase 10 evidence chain end-to-end on one record-close box, then expand to all record-close boxes, the count reconciliation, and the five staged human UAT runbooks.

Purpose: Prove the closeout routing works (archive cite plus live gate re-run plus written box) before replicating it across every requirement that closes on record, and stage the human-only halves with exact runbooks per the nonstop standing instruction.
Output: Record-close boxes with cited evidence, the 16-vs-15 reconciliation paragraph, five staged UAT sheets, and a green --quick baseline.
</objective>

<execution_context>
<purpose>
Execute a phase prompt (PLAN.md) and create the outcome summary (SUMMARY.md).
</purpose>

<required_reading>
Read STATE.md before any operation to load project context.
Read config.json for planning behavior settings.
</required_reading>
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-sign-off-closeout/10-CONTEXT.md
@.planning/phases/10-sign-off-closeout/10-RESEARCH.md
</context>

<tasks>

<task type="tracer">
  <name>End-to-end record-close slice — GUI-04 landability box</name>
  <files>.planning/phases/10-sign-off-closeout/10-01-SUMMARY.md</files>
  <read_first>scripts/verify-registry-shape.mjs exported-shape assertion plus set-equality comparison plus self-test twin, scripts/verify-platform.sh gui04-registry-shape row region, 08-VERIFICATION.md truths-table plus staged-not-gap idiom, 09-VERIFICATION.md hard-rule compliance section</read_first>
  <reversibility rating="reversible">Prose evidence record only; a wrong citation is fixed by amending the box, no shipped surface changes.</reversibility>
  <action>Wire the full record-close chain for GUI-04 and no other requirement: re-run the gui04-registry-shape row plus its self-test twin live on the current tree, read the TabUriRegistry sources the gate derives from, locate the GUI-04 prior evidence in the tracked v1.0/v1.1 archives, then write one finished record-close box into the 10-01-SUMMARY.md working draft naming the requirement, the record disposition, the exact prior evidence with tracked archive pointers plus row identifiers plus dates, and the live re-run outcome observed this session. Finish with the commit gate green.</action>
  <acceptance_criteria>One complete GUI-04 box exists in the SUMMARY draft with archive pointers plus row ids plus dates plus the live re-run outcome; both gui04 rows pass live; the commit gate is green</acceptance_criteria>
  <verify>
    <automated>scripts/verify-platform.sh --only gui04-registry-shape && scripts/verify-platform.sh --only gui04-registry-shape-self-test && scripts/verify-platform.sh --quick</automated>
    <fails_when>Any of the three commands exits nonzero, or the shape row names a surplus or shortfall member, or the self-test twin fails to go red naming its planted drift, or --quick reports red</fails_when>
  </verify>
  <done>The single record-close path works end-to-end and is committed</done>
</task>

<task type="auto">
  <name>Remaining record-close boxes plus count reconciliation</name>
  <files>.planning/phases/10-sign-off-closeout/10-01-SUMMARY.md</files>
  <read_first>v1.0 outcomes table Open rows plus the three Complete (static) rows, STATE.md snapshot line carrying the 16 label with its 15-item parenthetical, REQUIREMENTS.md credit-policy line declaring enumeration source of truth, 08-VERIFICATION.md plus 09-VERIFICATION.md score lines with plan-sanctioned staged items, inventory/brand-tokens.json classification plus configuration.toml canonical display name</read_first>
  <reversibility rating="reversible">Prose boxes and one reconciliation paragraph; any correction is an amendment, never a migration.</reversibility>
  <action>Replicate the tracer box pattern for MIG-01 (migration proof from re-fetch history plus UAT tests 14-18 and 29-37), MIG-02 (token-inventory audit over the committed classification with a live scan-brand-residue re-run), the GEN-01 release-variant credit half to BLD-01 with emitter-delta re-proof via generate-check plus generated-byte-identity, the GEN-03 host-build credit half to PKG-01 with installer-schema delta re-proof, and the TEL-03 derivation half via allowlist-schema plus allowlist-doc-consistency rows, each box citing tracked archive records plus row identifiers plus dates and the delta re-run outcome on the current tree. Then write the count-reconciliation paragraph citing the three pointer lines: the v1.0 outcomes table enumerates exactly 15 Open rows, the closeout line plus the STATE.md snapshot say 16 while the snapshot parenthetical itself enumerates only 15 IDs, and the enumeration is source of truth per the credit-policy line with the three Complete (static) rows closed live by v1.1 and never re-counted. Confirm the GUI-01/GUI-03 2026-09-01 prior evidence is still valid by checking the change history of the window-command source, the boundary browser-window rows, and the customize-bridge sources since that date.</action>
  <acceptance_criteria>Five further boxes (MIG-01, MIG-02, GEN-01 credit, GEN-03 credit, TEL-03 derivation) each carry archive pointers plus row ids plus dates plus live delta outcomes; the reconciliation paragraph cites all three pointer lines and declares the enumeration source of truth; the GUI-01/GUI-03 prior-evidence freshness check is recorded</acceptance_criteria>
  <verify>
    <automated>scripts/verify-platform.sh --only scan-brand-residue && scripts/verify-platform.sh --only branding-preflight && scripts/verify-platform.sh --only generate-check && scripts/verify-platform.sh --only generated-byte-identity && scripts/verify-platform.sh --only installer-schema && scripts/verify-platform.sh --only allowlist-schema && scripts/verify-platform.sh --only allowlist-doc-consistency && scripts/verify-platform.sh --quick</automated>
    <fails_when>Any cited row exits nonzero on the current tree, or --quick reports red, or a credit-half box lacks its delta re-run outcome line</fails_when>
  </verify>
  <done>All record-close boxes plus the reconciliation are written and committed</done>
</task>

<task type="auto">
  <name>Five staged human UAT runbook sheets</name>
  <files>.planning/phases/10-sign-off-closeout/10-UAT.md</files>
  <read_first>04-UAT.md GEN-05 drill plus EXT-01 drill plus TEL-02 drill steps in expected/result/evidence/steps shape, 01-UAT.md GUI-01 test plus GUI-03 test with 2026-09-01 human notes, WINDOWS.md ledger items 15/16 reason fields, 03-UAT.md pixel scenario shape, 06-UAT.md prerequisite-test-expected-why-human quad, docs/REBRANDING.md walkthrough plus verify-rebranding-docs gate contract</read_first>
  <reversibility rating="reversible">Prose evidence record only; correction by amendment, no shipped surface.</reversibility>
  <action>Create 10-UAT.md carrying exactly five sheets (GUI-01 five steps from the ledger-15 wording plus 01-UAT test 2, GUI-03 three steps from ledger-16 plus 01-UAT test 6, GEN-02 pixel look from the 03-UAT scenario verbatim, GEN-05 render from the 04-UAT drill verbatim, DOC-01 stranger carry-test defining the fresh-clone plus edit plus build plus checklist protocol), each sheet using the existing expected/result/evidence/steps shape with result pending, source human, a coverage id naming its requirement, exact commands, the prior-evidence citation inside the sheet, and a staged-awaiting-signature mark. Re-run each sheet's automatable-half gate on the current tree (verify-customize-inert, verify-dev-flag-off, gui01-command-registered, icon-ihdr, theia-branding, theia-endpoints, verify-rebranding-docs) and record the outcomes beside the staged perceptual halves without merging them into a pass.</action>
  <acceptance_criteria>10-UAT.md holds five sheets each with result pending plus source human plus coverage id plus exact steps plus prior-evidence citation plus staged mark; no sheet carries an executor-filled signature; every automatable-half gate outcome is recorded beside its staged half</acceptance_criteria>
  <verify>
    <automated>scripts/verify-platform.sh --only verify-customize-inert && scripts/verify-platform.sh --only verify-dev-flag-off && scripts/verify-platform.sh --only gui01-command-registered && scripts/verify-platform.sh --only icon-ihdr && scripts/verify-platform.sh --only theia-branding && scripts/verify-platform.sh --only theia-endpoints && scripts/verify-platform.sh --only verify-rebranding-docs && test "$(grep -c 'coverage_id' .planning/phases/10-sign-off-closeout/10-UAT.md)" = "5" && test "$(grep -c 'result: pending' .planning/phases/10-sign-off-closeout/10-UAT.md)" = "5" && scripts/verify-platform.sh --quick</automated>
    <fails_when>Any automatable-half gate exits nonzero, or the sheet count or pending count differs from five, or any sheet lacks its staged-awaiting-signature mark, or --quick reports red</fails_when>
  </verify>
  <done>Five staged runbooks exist with exact commands and evidence slots, signatures deferred to the operator</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| archive→box | Prior live evidence crosses from the v1.0/v1.1 archives into Phase 10 sign-off prose |
| runbook→human | Staged sheets cross from the executor to the operator who will perform and sign them |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-01 | Tampering | record-close boxes (evidence) | medium | mitigate | Every box cites tracked archive pointers plus row ids plus dates plus a live re-run on the current tree; the registry empty-set guard keeps a green-by-absence box impossible |
| T-10-02 | Repudiation | staged UAT signatures | high | mitigate | Sheets stay result pending with source human and staged-awaiting-signature marks; the 2026-09-01 passes are cited as prior evidence inside the sheet, never as a signature |
| T-10-03 | Tampering | count reconciliation | low | mitigate | Three-pointer paragraph with the enumeration declared source of truth; the three Complete (static) rows are named so they cannot be re-counted as a 16th item |
| T-10-SC | Tampering | npm/pip/cargo installs | low | accept | No package is installed by this plan; all work rides the pinned tree toolchain |
</threat_model>

<verification>
gui04 rows plus self-test twin, seven record-close delta rows, seven automatable-half rows, sheet-shape counts, and the commit gate green.
</verification>

<success_criteria>
Six record-close boxes cite prior live evidence with live delta re-runs green, the reconciliation explains the 16 label against its own 15-item enumeration, five UAT sheets are staged with exact runbooks and deferred signatures, and --quick is green with no sibling driver.
</success_criteria>

<artifacts_this_phase>
This plan produces: six record-close boxes with cited evidence; the 16-vs-15 reconciliation paragraph; five staged human UAT runbook sheets.
</artifacts_this_phase>

<output>
Create `.planning/phases/10-sign-off-closeout/10-01-SUMMARY.md` when done
</output>
