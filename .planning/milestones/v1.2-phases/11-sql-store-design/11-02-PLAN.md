---
phase: 11-sql-store-design
plan: 02
type: execute
wave: 2
depends_on: [11-01]
files_modified: [.planning/phases/11-sql-store-design/schema/SCHEMA.md, .planning/phases/11-sql-store-design/schema/MIGRATIONS.md, .planning/phases/11-sql-store-design/schema/SIGN-OFF.md, .planning/phases/11-sql-store-design/fixtures/tabs-v1.sqlite, .planning/phases/11-sql-store-design/fixtures/exercise-migrations.mjs]
autonomous: true
requirements: [SQL-03]
must_haves:
  truths:
    - "Schema doc exists with URI primary key, schema version from day one, fixed filename as platform content, and the private-exclusion rule with a pinned detection symbol"
    - "Forward-only migrations are exercised against fixture databases with a run log showing passing drives plus non-vacuity controls"
    - "Recorded reviewer approval for the schema and migration plan exists with a per-item checklist"
    - "Phase 12 gates are specified as future registry rows with self-test obligations, not implemented here"
  artifacts:
    - path: ".planning/phases/11-sql-store-design/schema/SCHEMA.md"
      provides: "Tabs DDL on URI TEXT primary key with YAGNI column set and platform-content notes"
      min_lines: 50
    - path: ".planning/phases/11-sql-store-design/schema/MIGRATIONS.md"
      provides: "Forward-only chain procedure, quarantine procedure, fixture inventory, exercise log"
      min_lines: 60
    - path: ".planning/phases/11-sql-store-design/fixtures/tabs-v1.sqlite"
      provides: "Committed fixture database at the initial schema version"
    - path: ".planning/phases/11-sql-store-design/fixtures/exercise-migrations.mjs"
      provides: "Throwaway migration-exercise script, test scaffolding only"
      min_lines: 40
    - path: ".planning/phases/11-sql-store-design/schema/SIGN-OFF.md"
      provides: "Recorded approval with checklist covering SQL-03"
      min_lines: 25
  key_links:
    - from: ".planning/phases/11-sql-store-design/schema/SCHEMA.md"
      to: "theia/extensions/tab-uris/src/browser/tab-uri-registry.ts"
      via: "primary-key semantics cite the opaque canonical form the registry emits"
      pattern: "parseName"
    - from: ".planning/phases/11-sql-store-design/schema/MIGRATIONS.md"
      to: ".planning/phases/11-sql-store-design/fixtures/tabs-v1.sqlite"
      via: "exercise log records drives run against copies of the committed fixture"
      pattern: "tabs-v1\\.sqlite"
    - from: ".planning/phases/11-sql-store-design/schema/MIGRATIONS.md"
      to: "sessionstore"
      via: "quarantine procedure rebuilds live rows from the restore authority"
      pattern: "sessionstore"
---

<objective>
Write the SQL-03 schema plus migration plan, exercise the forward-only chain against fixture databases, and record its review approval.

Purpose: SQL-03 requires a reviewed schema and migration plan with the chain proven against fixture databases; this plan produces the signed record plus the exercise evidence Phase 12 re-runs.
Output: schema/SCHEMA.md, schema/MIGRATIONS.md with an exercise log, fixtures plus throwaway exercise script, schema/SIGN-OFF.md with a recorded approval, static gates green.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-sql-store-design/11-RESEARCH.md
@.planning/phases/11-sql-store-design/11-PATTERNS.md
@.planning/phases/11-sql-store-design/authority/AUTHORITY.md
@docs/URI-SCHEMES.md
@theia/extensions/tab-uris/src/browser/tab-uri-registry.ts
@scripts/verify-downstream-fixture.mjs
@.planning/research/duckdb-vs-sqlite/03-INTEGRATION.md
</context>

<tasks>

<task type="auto">
  <name>Write the tabs schema document</name>
  <reversibility rating="one-way">Primary-key shape, filename, and initial-version header form an on-disk contract later phases read; changing them after the writer ships requires a migration.</reversibility>
  <read_first>
    - .planning/phases/11-sql-store-design/authority/AUTHORITY.md
    - docs/URI-SCHEMES.md (canonical opaque form, coverage-boundary voice)
    - theia/extensions/tab-uris/src/browser/tab-uri-registry.ts (frozen-contract header, parseName opaque-form semantics)
    - .planning/phases/11-sql-store-design/11-RESEARCH.md (Pattern 2, Pattern 5, Open Questions, Pitfall 5, Pitfall 6)
    - .planning/phases/11-sql-store-design/11-PATTERNS.md (schema/SCHEMA.md section)
  </read_first>
  <files>.planning/phases/11-sql-store-design/schema/SCHEMA.md</files>
  <action>Write SCHEMA.md with a header stating the primary key is the canonical opaque URI string in scheme-colon-path form that chrome binds as a parameter and never parses, citing the registry emission semantics. Define the column set as exactly four columns with types and constraints: uri TEXT PRIMARY KEY, url TEXT NOT NULL, title TEXT NOT NULL DEFAULT empty string, last_active INTEGER NOT NULL, plus one index on last_active, with each column naming its Phase 12 consumer for join lookup, read projection, or ordering with bounded closed-retention pruning. Record window-identifier and pinned-flag columns as explicitly excluded with the no-Phase-12-consumer rationale, and record no private column at all with the exclusion-total rationale and the writer-side filter placed before upsert. Pin the private-window detection symbol by searching the local pinned checkout for the private-browsing utility and recording its file-colon-line; when no single symbol resolves, record the searched paths plus the deferral procedure to the Phase 12 catalogue row instead of inventing a name. Record the initial schema version as version 1 set at creation through the user-version pragma, the forward-only numbered migration rule with one transaction per migration plus pre-checks and never downgrades, the fixed filename as platform content with profile-directory-relative open and write-ahead-logging mode pinned while remaining pragmas stay at engine defaults with a soak-on-defaults note, a coverage-boundary section excluding full-text search indexing from the initial version with the cited prior-research rationale, a query-API placement paragraph locating reads beside the registry in the tab-uris extension and never as new public members on the frozen class, and a Phase 12 registry-row specification paragraph listing the second-writer scan, integrity soak, restart roundtrip, and emitter-exercising absence test as specified-not-implemented rows each carrying a self-test obligation. Keep every statement consistent with AUTHORITY.md and stage the file.</action>
  <verify>
    <automated>git add .planning/phases/11-sql-store-design/schema/SCHEMA.md && test -f .planning/phases/11-sql-store-design/schema/SCHEMA.md && scripts/verify-platform.sh --quick</automated>
    <fails_when>quick gate exits non-zero or SCHEMA.md is absent after staging</fails_when>
  </verify>
  <acceptance_criteria>
    - SCHEMA.md states the opaque-form primary key, the four-column set with the last_active index, and version 1 from day one
    - SCHEMA.md records the private-exclusion rule with either a pinned file-colon-line symbol or searched-paths plus deferral procedure
    - SCHEMA.md states the fixed filename as platform content and specifies the four Phase 12 gates without implementing them
    - Quick gate exits 0 with the file staged
  </acceptance_criteria>
  <done>Schema document exists, staged, gate-green, and consistent with the signed authority table</done>
</task>

<task type="auto">
  <name>Write the migration plan, build fixtures, and exercise the chain</name>
  <reversibility rating="costly">Chain procedure plus fixture set bind the exercise evidence; changing the procedure requires rebuilding fixtures and re-running the log.</reversibility>
  <read_first>
    - .planning/phases/11-sql-store-design/schema/SCHEMA.md
    - .planning/research/duckdb-vs-sqlite/03-INTEGRATION.md (migration mechanics)
    - scripts/verify-downstream-fixture.mjs (mktemp-copy discipline, derive-expectations-at-run-time)
    - .planning/phases/11-sql-store-design/11-RESEARCH.md (Pattern 3, Pattern 4, Pitfall 5 non-vacuity note)
  </read_first>
  <files>.planning/phases/11-sql-store-design/schema/MIGRATIONS.md, .planning/phases/11-sql-store-design/fixtures/tabs-v1.sqlite, .planning/phases/11-sql-store-design/fixtures/exercise-migrations.mjs</files>
  <action>Write MIGRATIONS.md stating the forward-only chain procedure with version 1 set at creation, numbered idempotent migrations applied in order each inside one transaction with pre-checks and never downgrades, citing the in-tree wrapper primitives as source pins from the research record. State the corruption procedure as a startup cheap-check tripwire keyed on the exact single-ok result, full integrity check on schedule or suspicion, and on any other result copy then quarantine to a corrupt-suffixed name via the backup primitive, remove dependent sidecar state, rebuild live rows from the restore authority plus registry, continue degraded, and never delete. Build the committed fixture database at the initial schema version with two to three public-tab rows covering opaque URI edge shapes and a quote-plus-unicode title proving bound parameters, with no private rows. Write the throwaway exercise script using only node standard-library imports plus the engine, probing standard-library database support first with a flagged-invocation fallback and an ephemeral registry install confined to the stage directory only when standard-library support is absent, never touching repo manifests. The script copies fixtures to a mktemp stage outside the repo at a space-free path and mutates only the copies, derives expectations at run time covering final version, single-ok integrity result, row preservation, and index presence, and runs a fresh-create drive advancing an unset version to version 1, an idempotent re-run over the version-1 copy, and two non-vacuity controls showing the tripwire firing on a deliberately tampered copy through the quarantine path without deletion and a stale-version copy advancing through the chain. Run the script, paste its full log into the exercise-log section of MIGRATIONS.md, confirm the committed fixtures are byte-identical afterwards, state the script is test scaffolding and explicitly not a registry row with promotion left to Phase 12, and stage all three files.</action>
  <verify>
    <automated>node .planning/phases/11-sql-store-design/fixtures/exercise-migrations.mjs && git add .planning/phases/11-sql-store-design/schema/MIGRATIONS.md .planning/phases/11-sql-store-design/fixtures/tabs-v1.sqlite .planning/phases/11-sql-store-design/fixtures/exercise-migrations.mjs && scripts/verify-platform.sh --quick</automated>
    <fails_when>exercise script exits non-zero, any drive in its output reports FAIL, the fixture file hash differs from the committed copy after the run, or the quick gate exits non-zero</fails_when>
  </verify>
  <acceptance_criteria>
    - Exercise log in MIGRATIONS.md shows passing fresh-create, idempotent re-run, and row-preservation drives
    - Exercise log shows both non-vacuity controls with the tampered-copy drive firing the quarantine path without deletion
    - Committed fixture file is byte-identical before and after the run and the quick gate exits 0
  </acceptance_criteria>
  <done>Migration chain is proven against fixture copies with the evidence logged in MIGRATIONS.md</done>
</task>

<task type="auto">
  <name>Reviewer pass and recorded approval for schema and migration plan</name>
  <reversibility rating="reversible">Approval record edit; re-review rewrites one file.</reversibility>
  <read_first>
    - .planning/phases/11-sql-store-design/schema/SCHEMA.md
    - .planning/phases/11-sql-store-design/schema/MIGRATIONS.md
    - brand/HUMAN-REVIEW.md (sign-off record section)
    - .planning/phases/11-sql-store-design/authority/AUTHORITY.md (consistency cross-check)
  </read_first>
  <files>.planning/phases/11-sql-store-design/schema/SIGN-OFF.md, .planning/phases/11-sql-store-design/schema/SCHEMA.md, .planning/phases/11-sql-store-design/schema/MIGRATIONS.md</files>
  <action>Review the schema and migration documents against the checklist: primary-key form matches the registry emission semantics with zero frozen-class change; every column names a Phase 12 consumer; private exclusion is total with a pinned symbol or a recorded deferral; version from day one with forward-only rule; filename fixed as platform content; exercise log shows passing drives plus firing controls with fixtures untouched; Phase 12 gates specified not implemented; consistency with the signed authority table; no brand token; engine exclusivity; no GUI surface. Fix defects in the two documents directly. Write SIGN-OFF.md following the sign-off ritual shape with STATUS recorded approval carrying reviewer name, date, per-item checklist results, reviewed-files rows for both documents plus the fixture and script, and an evidence-basis section recording assumed-item dispositions. Stage all files and run the quick gate.</action>
  <verify>
    <automated>git add .planning/phases/11-sql-store-design/schema/SCHEMA.md .planning/phases/11-sql-store-design/schema/MIGRATIONS.md .planning/phases/11-sql-store-design/schema/SIGN-OFF.md && scripts/verify-platform.sh --quick && grep -q 'RECORDED' .planning/phases/11-sql-store-design/schema/SIGN-OFF.md</automated>
    <fails_when>quick gate exits non-zero, or no recorded-approval marker is present in schema SIGN-OFF.md</fails_when>
    <human-check>Reviewer reads SCHEMA.md and MIGRATIONS.md end to end and confirms the schema, quarantine path, and exercise evidence satisfy SQL-03</human-check>
  </verify>
  <acceptance_criteria>
    - schema SIGN-OFF.md carries a recorded approval with name, date, per-item checklist results, and reviewed-files rows for both documents
    - Every checklist item passes including the exercise-log and authority-consistency items
    - Quick gate exits 0 on the final tree
  </acceptance_criteria>
  <done>SQL-03 schema and migration plan are reviewed and signed with the record on disk</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chrome-side writer to tabs table | Untrusted URI, URL, and title strings cross into SQL through bound parameters |
| fixture exercise script to staged copies | Throwaway script touches mktemp copies only; committed fixtures and the live profile are never written |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-07 | Tampering | tab upsert statements (Phase 12) | high | mitigate | Bound parameters on every statement specified; fixture title with quote plus unicode proves binding in the exercise |
| T-11-08 | Tampering | migration chain atomicity | high | mitigate | One-transaction-per-migration rule with pre-checks specified; idempotent re-run drive proves it |
| T-11-09 | Information disclosure | private-tab rows | high | mitigate | No private column specified; writer-side filter with pinned symbol; absence test specified for Phase 12 |
| T-11-10 | Denial of service | corrupt-file recovery | medium | mitigate | Quarantine-not-delete procedure with rebuild from the restore authority specified and control-fired |
| T-11-11 | Tampering | foreign-key validation gap | low | accept | Integrity check does not find foreign-key errors per vendor docs; schema carries no foreign keys so there is nothing to miss, recorded here explicitly |
| T-11-12 | Tampering | fixture script writing live state | medium | mitigate | Script confined to mktemp copies with byte-identity check on fixtures after the run |

No package installs occur in this plan; the exercise uses standard-library database support with an ephemeral stage-confined fallback only.
</threat_model>

<verification>
scripts/verify-platform.sh --quick green on the final tree with all schema files staged; exercise script exits 0 with passing drives plus firing controls; both schema documents plus fixture and script named in schema SIGN-OFF.md with a recorded approval.
</verification>

<success_criteria>
SQL-03 schema plus migration plan exists with URI primary key, version from day one, forward-only chain exercised against fixture copies with a complete log, quarantine path stated, private exclusion pinned, filename fixed as platform content, and a recorded checklist approval with green static gates.
</success_criteria>

<output>
Leave the validation-strategy file untouched for the verifier; the 11-03 gate plan consumes both SIGN-OFF.md files plus the exercise log as its evidence.
</output>
