---
phase: 03-firefox-branding-emitter-and-icon-pipeline
plan: 04
subsystem: build
tags: [gecko, mozconfigure, generator, patch-stack, branding, tier-3, node]

# Dependency graph
requires:
  - phase: 02-configuration-manifest-and-generator-core
    provides: scripts/generate.mjs pipeline with frozen TARGETS, byte-identity gate, --check/--self-test conventions
  - phase: 03-firefox-branding-emitter-and-icon-pipeline/03-01
    provides: spike wiring (overlay symlink, --with-branding), locale emitters, agreement gates
  - phase: 03-firefox-branding-emitter-and-icon-pipeline/03-02
    provides: five-size PNG rasters plus ICO/ICNS writers the built artifact carries
  - phase: 03-firefox-branding-emitter-and-icon-pipeline/03-03
    provides: NSIS/MSIX/plist/tile installer emitters the propagation proof moves
provides:
  - emitIdentityConfigure plus generated/identity.configure TARGETS row (46 targets)
  - Regenerated patches/010-powerbrowser-identity.patch with the include hook and no brand values
  - upstream/identity.configure overlay created by fetch-upstream.sh
  - Repointed preflight check 5 with a strip-hook planted fault
  - Zebra-excursion propagation procedure reusable by Phase 6 verification
  - Tier-3 Linux build proof on the de-configured tree (six surfaces green)
affects: [phase-5 hook-only patches, phase-6 verification, phase-7 downstream]

# Actuals (#2632) — pairs with the plan's `estimate` to calibrate future estimates.
# Same estimateTokens scale (chars/4 over the realized diff), never a harness token count.
actuals:
  tokens: 5681
  tasks: 3
  commits: 2

# Tech tracking
tech-stack:
  added: []
  patterns: [generated-configure-fragment carrier, topsrcdir-internal file-symlink overlay, added-lines-only patch assertion]

key-files:
  created:
    - generated/identity.configure
  modified:
    - scripts/generate.mjs
    - patches/010-powerbrowser-identity.patch
    - scripts/fetch-upstream.sh
    - scripts/verify-branding-preflight.mjs
    - inventory/brand-tokens.json
    - docs/BUILD.md

key-decisions:
  - "Include route, not mozconfig exports: project_flag() pins possible_origins to (implied,) so environment-origin exports are rejected by configure -- the 03-01 spike conclusion was wrong, proven red live"
  - "Overlay is a file symlink upstream/identity.configure (git-excluded), not a second branding-dir symlink -- one hook, one fragment, no new tracked tree"
  - "Preflight asserts the hook plus the fragment (not generated bytes on fresh clones inconsistently): absent fragment fails, matching generate-check's honest red"
  - "Proof-only tasks 2-3 ride without commits (03-01 precedent): an empty commit would violate the prohibition, so outcomes live here"
  - "Visible launch not attempted: display :0 exists but the agent cannot judge visuals and will not seize the user's display; headless live-launch green is the surrogate"

patterns-established:
  - "Generated moz.configure fragment carrier: project_flag values reach the build as imply_option lines in a generated fragment pulled in by a patch include() hook, never as mozconfig exports"
  - "Added-lines-only patch assertion: a de-configuration patch's own `-` removal lines must not trip the no-hard-code gate -- only `+` lines are brand values"
  - "Excursion proof shape: manifest edit, regenerate, assert the moved set equals the value-carrying set with frozen comments enumerated, prove --check stale, restore, prove PASS plus clean status"

requirements-completed: [GEN-01, GEN-02, GEN-03]

# Coverage metadata (#1602) — one entry per shipped deliverable. Drives DETERMINISTIC UAT routing in verify-work.
coverage:
  - id: D1
    description: "Identity values flow from generated/identity.configure through the patch include hook; autoconf.mk vendor/display/branding identical pre/post forced configure"
    requirement: "GEN-01"
    verification:
      - kind: other
        ref: "nix develop .#firefox --command bash -c 'cd upstream && MOZCONFIG=../.mozconfig ./mach configure' exit 0 + grep autoconf.mk 3 lines identical (DeBIOS / Power Browser Dev / branding-generated/dev)"
        status: pass
      - kind: other
        ref: "bash scripts/check-patch-surface.sh PASS + bash scripts/apply-patches.sh --self-test PASS (non-vacuity fires both directions)"
        status: pass
      - kind: other
        ref: "grep -c 'MOZ_APP_VENDOR\", \"DeBIOS\"' patches/010-powerbrowser-identity.patch exits 1 (no vendor hard-code)"
        status: pass
    human_judgment: false
  - id: D2
    description: "One display_name edit moves all 14 value-carrying Gecko surfaces with no other file edited; restored tree check-clean, identity-clean, zero tracked drift"
    requirement: "GEN-01"
    verification:
      - kind: other
        ref: "Zebra excursion: 14/14 value surfaces carry Zebra Browser, zero Power Browser Dev remnants, 4 frozen comment lines enumerated, --check stale names exactly those 14"
        status: pass
      - kind: other
        ref: "node scripts/generate.mjs --check PASS + node scripts/verify-generated-identity.mjs PASS + git status tracked drift empty after restore"
        status: pass
    human_judgment: false
  - id: D3
    description: "Tier-3 Linux build exits 0 on the de-configured tree; all six identity surfaces green on the built artifact with the positive control discriminating; desktop Icon resolves to the 03-02 raster"
    requirement: "GEN-01"
    verification:
      - kind: other
        ref: "nix develop .#firefox --command bash -c 'cd upstream && MOZCONFIG=../.mozconfig ./mach build' exit 0 (144s wall, warm cache)"
        status: pass
      - kind: integration
        ref: "node scripts/verify-branding-identity.mjs PASS all six surfaces + --positive-control runtime-identity discriminates"
        status: pass
    human_judgment: false
  - id: D4
    description: "Window, launcher and desktop entry show the downstream mark with the display name plus suffix at correct density"
    verification: []
    human_judgment: true
    rationale: "No visual judge exists in this runtime: display :0 is present but a visible launch would seize the user's display and the agent could not judge pixels anyway. Headless live-launch (runtime-identity surface) is green as the automated surrogate; perceptual sign-off belongs to human UAT."

# Metrics
duration: 30min
completed: 2026-09-04
status: complete
---

# Phase 03 Plan 04: Identity Carrier plus Phase Close Summary

**Generated identity.configure carrier de-configures patch 010 through an include hook, one manifest edit proven to move all 14 Gecko surfaces, tier-3 Linux build green on all six identity surfaces.**

## Performance

- **Duration:** 30 min
- **Started:** 2026-09-04T04:48:57Z
- **Completed:** 2026-09-04T05:18:53Z
- **Tasks:** 3 (1 committed, 2 proof-only with no diff)
- **Files modified:** 6 tracked (1 created gitignored fragment)

## Accomplishments
- `emitIdentityConfigure` writes `generated/identity.configure` (vendor imply from `product.vendor_machine`, UA_NAME Firefox literal); 46th frozen TARGETS row with no tracked comparand
- Patch 010 regenerated from the tree: vendor/UA hard-codes replaced by `include("../identity.configure")`; MOZ_APP_ID, healthreport/normandy False byte-identical; surface check green, non-vacuity firing
- Forced `./mach configure` resolves `MOZ_APP_VENDOR=DeBIOS`, `MOZ_APP_DISPLAYNAME=Power Browser Dev`, `MOZ_BRANDING_DIRECTORY=powerbrowser/branding-generated/dev` — identical pre/post switch
- Zebra Browser excursion moves exactly the 14 value-carrying surfaces; restore is check-clean, identity-clean, drift-free
- Full `./mach build` exits 0; all six branding-identity surfaces green on `objdir/dist/bin/powerbrowser` with the positive control discriminating; desktop Icon resolves to the 03-02 `default128.png` raster

## Task Commits

1. **Task 1: Identity carrier plus patch 010 de-configuration** - `072cb9d` (feat)
2. **Task 2: Single-edit propagation proof across every surface** - no commit (proof-only: manifest edited and restored in-task, generated/ is gitignored, zero tracked diff; precedent 03-01 task 1)
3. **Task 3: Tier-3 Linux build plus built-artifact branding proof** - no commit (proof-only: objdir/ is gitignored, zero tracked diff)

**Plan metadata:** task 2/3 surface lists and outcomes recorded here per the plan's own "record in the commit message" instruction, relocated to the plan's committed output artifact since empty commits are prohibited.

## Files Created/Modified
- `scripts/generate.mjs` - `emitIdentityConfigure` plus TARGETS row (46 targets); count prose 45 to 46
- `generated/identity.configure` - Created (gitignored): banner plus the two imply_option lines
- `patches/010-powerbrowser-identity.patch` - Regenerated: `-vendor Mozilla`/`+DeBIOS`/`+UA_NAME` becomes `-vendor Mozilla`/`+include` hook (`@@ -3,15 +3,15 @@`)
- `scripts/fetch-upstream.sh` - `ensure_branding_overlay` also creates `upstream/identity.configure` symlink plus its git-exclude entry
- `scripts/verify-branding-preflight.mjs` - Check 5 asserts hook plus fragment (added-lines-only gate); fixture carries the fragment; seventh strip-hook plant
- `inventory/brand-tokens.json` - Three rows moved: brand-product-name 3 to 4, UA_NAME 1 to 4 (patch site to generator sites), APP_ID 1 to 2
- `docs/BUILD.md` - Generate-before-configure sentence now names both symlinks

## Decisions Made
- Include route over mozconfig exports: `project_flag()` pins `possible_origins` to `("implied",)` (`upstream/build/moz.configure/util.configure:522`), so the 03-01 spike's export conclusion fails live with "can not be set by environment. Values are accepted from: implied". The plan's own include branch was taken instead.
- File symlink overlay (`upstream/identity.configure`, git-excluded) rather than routing through the branding overlay: one hook, one fragment, nothing new in the tracked tree.
- Preflight reads the fragment from disk with absent-means-fail, matching generate-check's honest red (no new fresh-clone greenness broken: generate-check is already red until generate runs).
- Proof-only tasks commit nothing (03-01 "spike answers in the wiring commit" precedent generalized): an empty commit would violate the destructive-git prohibition family; the SUMMARY is the committed record.
- No visible launch: display `:0` exists, but seizing the user's display for pixels the agent cannot judge trades a real disruption for zero evidence. The headless live launch inside the runtime-identity surface is the launch evidence.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Took the plan's include branch after the spike's export branch failed live**
- **Found during:** Task 1 (forced `./mach configure` with mozconfig exports)
- **Issue:** Configure died with `InvalidOptionError: MOZ_APP_VENDOR=DeBIOS can not be set by environment. Values are accepted from: implied`. `project_flag()` hard-pins `possible_origins=("implied",)`; the 03-01 spike read `env=` and assumed environment accepted. The prior executor attempt never reconfigured, so it never met the error.
- **Fix:** Reverted the export lines and `.mozconfig` touch; added `emitIdentityConfigure` plus TARGETS row; added the overlay symlink plus exclude to `fetch-upstream.sh`; replaced the vendor/UA hard-codes with the include hook via regen-from-tree. The defective prior-attempt patch (over-deleted MOZ_APP_ID context, hash/content mismatch) and the stale emitted-comment change were both discarded, not adopted.
- **Files modified:** scripts/generate.mjs, patches/010-powerbrowser-identity.patch, scripts/fetch-upstream.sh, docs/BUILD.md
- **Verification:** Forced configure exit 0 with the three autoconf.mk lines identical pre/post; all static gates green
- **Committed in:** 072cb9d

**2. [Rule 3 - Blocking] Repointed preflight check 5 and moved three inventory rows**
- **Found during:** Task 1 (`verify-platform.sh --quick` red after the carrier landed)
- **Issue:** Check 5 asserted the patch sets `MOZ_APP_VENDOR` to DeBIOS (the exact line de-configuration removes); three held-back rows counted the old sites.
- **Fix:** Check 5 asserts the hook plus the fragment with an added-lines-only hard-code gate (the patch's own `-` removal must not trip it); fixture carries the fragment; seventh strip-hook plant proves red. Rows moved with COUNT MOVED provenance per the 01-08 precedent.
- **Files modified:** scripts/verify-branding-preflight.mjs, inventory/brand-tokens.json
- **Verification:** scan-brand-residue PASS (120 files), preflight PASS, preflight --self-test PASS (7 plants), full --quick PASS
- **Committed in:** 072cb9d

**3. [Rule 1 - Bug] Corrected the task 2 surface list where the plan over-claims**
- **Found during:** Task 2 (pre-excursion snapshot)
- **Issue:** The plan asserts Zebra reaches "generated .mozconfig" (carries no display_name by design) and "all eight installer fragments" (the two VisualElements manifests carry tile color only), and that old strings appear "in none of them" (two frozen comments name Power Browser by design: the D-78 contrast line and the aboutDialog.css tree-location note).
- **Fix:** Asserted the derived truth instead: the 14 value-carrying files move (configure.sh 2, ftl 2, properties 2, nsi 2, Appx 2, plist 2, desktop 2); `.mozconfig` and VisualElements 2 correctly unchanged; the only remaining old strings are the 4 frozen comment lines, enumerated. Zero `Power Browser Dev` remnants anywhere.
- **Files modified:** None (proof-only)
- **Verification:** --check stale names exactly those 14; post-restore --check PASS, identity PASS, zero tracked drift
- **Committed in:** N/A (recorded here)

---

**Total deviations:** 3 auto-fixed (2 bugs, 1 blocking)
**Impact on plan:** The include branch and the gate follow-ups are what the plan's threat mitigations (T-03-11, T-03-12) actually require on this tree; the task 2 correction narrows an over-broad assertion to its derived, still-complete form. No scope creep: every touched file is named in the plan's files_modified or entailed by its verify steps.

## Issues Encountered
- Full `verify-platform.sh` (not --quick): 2 FAILs, both pre-existing and unrelated — `branding-variant-divergence` and `verify-branding-identity-release` read `objdir-release/dist`, and the release tree was never built (`objdir-release/` absent; documented since 03-01). This plan builds the dev variant only per its action text ("schedule once, do not iterate"). --quick (the commit gate) is fully green including all new rows.
- The prior executor attempt's uncommitted work was adopted in evidence only: its patch file (MOZ_APP_ID context over-deleted with a hash that matched a different tree state) and its emitted-comment change (which would have broken byte-identity against the tracked ftl files) were both reverted, and its `product.ua_name`-via-`assertEmittable` line shape was kept in the new emitter per the plan's "both values through assertEmittable" instruction.

## User Setup Required
None - no external service configuration required.

## Requirement Trace
GEN-01 closes here: the branding directory (03-01), the identity carrier plus patch de-configuration (this plan), and the Linux build proof (this plan) converge — every must-have truth in the plan header is demonstrated above. GEN-02 closes here: the 03-02 rasters ship in the built application (desktop Icon resolves to `default128.png`; all five sizes installed under `dist/.../icons/default/` via the branding overlay). GEN-03 closes here for its Linux-verified half: the full build is green with all six installer fragments (nsi, Appx, plist per variant) present and schema-gated, Windows/macOS packaging hosts remaining out of scope per the requirement text.

## Human Checks (end-of-phase mode, recorded as lines)
- Launch attempt: `DISPLAY=:0` present with an X socket, but no visible launch was attempted — the agent cannot judge pixels, and opening a window would seize the user's display for zero evidence. Headless launch evidence: the runtime-identity surface launches the built binary with a throwaway profile and reads its identity sentinel live (PASS, name/powerbrowser vendor/DeBIOS version/153.1.0esr).
- Artifact evidence: `objdir/dist/bin/powerbrowser` (2.4 MB, rebuilt this session), `application.ini` Name/Vendor exact, `--version` prints `DeBIOS powerbrowser 153.1.0esr`, desktop `Name=Power Browser Dev`, installed `default{16,32,48,64,128}.png` resolve through the overlay to the 03-02 rasters.
- Pending human judgment (coverage D4): window, launcher and desktop entry show the downstream mark at correct density with the suffixed display name. Type approved or describe the visual defect.

## Next Phase Readiness
- Phase 3 is complete pending verification: all four plans have SUMMARies on disk; GEN-01/02/03 close here. Suggested next: `/gsd-verify-work 03` then Phase 4 (Theia surface) planning.
- Watch items carried forward (not new): the release tree was never built (both full-suite FAILs key off it); the emitted ftl/css comments still name the old machine-vendor site in prose (`patches/010`) though the value moved — left byte-identical deliberately, correct when the locale emitter is next touched; the `product.ua_name` guard path names a manifest key that does not exist (plan-directed; harmless constant, revisit if UA_NAME ever becomes a real key).

---
*Phase: 03-firefox-branding-emitter-and-icon-pipeline*
*Completed: 2026-09-04*

## Self-Check: PASSED
- All created files exist: generated/identity.configure FOUND (gitignored, by design untracked)
- All modified files exist: scripts/generate.mjs, patches/010-powerbrowser-identity.patch, scripts/fetch-upstream.sh, scripts/verify-branding-preflight.mjs, inventory/brand-tokens.json, docs/BUILD.md FOUND
- Commits exist: 072cb9d FOUND (task 1); tasks 2-3 intentionally commit-free (zero tracked diff; empty commits prohibited)
- Plan verify blocks re-run at close: task-1 block (self-test, generate-self-test row, surface check, vendor grep exit 1) PASS; autoconf.mk 3-line grep PASS; task-2 block (--check, identity gate, drift grep exit 1) PASS; task-3 block (identity verifier six green plus positive control) PASS
