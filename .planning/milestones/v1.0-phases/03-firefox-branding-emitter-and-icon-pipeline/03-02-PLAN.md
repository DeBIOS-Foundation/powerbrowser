---
phase: 03-firefox-branding-emitter-and-icon-pipeline
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified: [scripts/generate.mjs, scripts/verify-icon-ihdr.mjs, scripts/verify-platform.sh]
autonomous: true
requirements: [GEN-02]
estimate:
  tokens: 30000
  raw_tokens: 60000
  tasks: 3
  confidence: high
must_haves:
  truths:
    - "Running the generator rasterizes default16/32/48/64/128 PNGs per variant from brand/mark.svg at exactly those pixel dimensions"
    - "The generator writes real firefox.ico and firefox.icns files wrapping the rasterized PNGs with structurally valid container headers"
    - "A non-square source logo or a missing source asset fails generation with a plain-words message instead of emitting distorted icons"
  artifacts:
    - path: "scripts/generate.mjs"
      provides: "Icon raster step, ICO writer, ICNS writer, icon TARGETS rows"
      contains: "rasterizeIcons"
    - path: "scripts/verify-icon-ihdr.mjs"
      provides: "PNG IHDR plus ICO/ICNS magic-and-length registry check with self-test"
      min_lines: 100
  key_links:
    - from: "scripts/generate.mjs"
      to: "brand/mark.svg"
      via: "Fixed source asset path — no manifest key selects it; squareness pre-check parses its viewBox"
      pattern: "mark\\.svg"
    - from: "scripts/verify-icon-ihdr.mjs"
      to: "generated/branding/dev/default128.png"
      via: "IHDR dimension assertion reading bytes 16..24 as big-endian uint32"
      pattern: "readUInt32BE"
  prohibitions:
    - requirement_id: "GEN-02"
      category: "transparency"
      status: "resolved"
      verification: "test"
      resolution: null
      reason: null
      statement: "MUST NOT silently distort a downstream logo to fit the square icon set — a non-square source is a hard failure naming the asset and the squareness rule, never a stretch, letterbox, or quiet substitution of the placeholder mark"
---

<objective>
Take the icon pipeline from checked-in Phase 1 rasters to generated output: five exact-size PNGs per variant rasterized from the single source mark, plus real Windows ICO and macOS ICNS containers written with zero new dependencies.

Purpose: GEN-02 makes brand/mark.svg the only icon input a downstream touches; the built application shows the downstream icon because the packaging step reads generated files, not hand-maintained ones.
Output: Raster step plus ICO/ICNS writers in scripts/generate.mjs, icon TARGETS rows, scripts/verify-icon-ihdr.mjs, registry rows.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-firefox-branding-emitter-and-icon-pipeline/03-RESEARCH.md
@.planning/phases/03-firefox-branding-emitter-and-icon-pipeline/03-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Rasterize five exact PNG sizes per variant from brand/mark.svg</name>
  <files>scripts/generate.mjs</files>
  <read_first>scripts/generate.mjs (spawnSync import line 58 and child pattern lines 1269-1285, writeTargets lines 878-908 for where icon bytes join the pending list, variantPath helper lines 610-612), brand/mark.svg (line 34 viewBox declaration), powerbrowser/branding/dev/default128.png (binary — read its first 24 bytes with node to confirm the IHDR reader offsets before writing the checker)</read_first>
  <reversibility rating="reversible">New step plus frozen-table rows; removal restores the prior table exactly.</reversibility>
  <action>Add a rasterizeIcons step that runs inside the emit pass before any write so a raster failure leaves the tree untouched: first validate the source by parsing the viewBox attribute out of brand/mark.svg and requiring width equal to height, hard-failing with a plain-words message naming brand/mark.svg and the squareness rule when they differ or when the file is absent or zero bytes; then invoke the system inkscape once per size in the fixed ascending order 16, 32, 48, 64, 128 with an argument array of exactly brand/mark.svg, --export-filename pointing at a mkdtemp scratch path, -w N, -h N (no shell, no joined strings, sizes from a frozen array), read each output back into memory, and hand the bytes to the pending write list under the generated paths branding/dev/defaultN.png and branding/release/defaultN.png. Dev and release rasters come from the same source so their bytes are identical and they are still written as ten separate files (separate, never merged or deduplicated). A source already at a target size takes the same path with no passthrough shortcut. Add TARGETS rows for all ten PNGs with their powerbrowser/branding tracked comparands, on the 01-03 precedent that inkscape 1.4.4 on this host produced those exact bytes deterministically. Extend the generate.mjs self-test with cases for a non-square fixture viewBox, a missing source, and a planted single-byte drift in one PNG, each required to go red naming the asset and the rule.</action>
  <verify>
    <automated>cd /home/chris/coding/Power-Browser && rm -rf generated && node scripts/generate.mjs && for s in 16 32 48 64 128; do node -e "const b=require('node:fs').readFileSync('generated/branding/dev/default${s}.png');if(b.readUInt32BE(16)!==${s}||b.readUInt32BE(20)!==${s}){console.error('bad IHDR ${s}');process.exit(1)}"; done && node scripts/generate.mjs --self-test && node scripts/verify-generated-identity.mjs</automated>
    <fails_when>non-zero exit from any command, any IHDR width or height not equal to its size, any --self-test line not ok, or the identity gate reporting a drift</fails_when>
  </verify>
  <acceptance_criteria>Ten PNGs emitted with IHDR dimensions exactly N by N; byte-identity green against the ten tracked rasters; all three new self-test cases observed red naming asset and rule in a scratch copy before landing.</acceptance_criteria>
  <done>Icon rasters generated from the single source; squareness and presence guards live; self-test extended.</done>
</task>

<task type="auto">
  <name>Pure-Node firefox.ico and firefox.icns writers</name>
  <files>scripts/generate.mjs</files>
  <read_first>scripts/generate.mjs (Buffer usage around lines 1026-1031 and 1217 for the in-tree byte-ops idiom, GENERATED_BANNER lines 602-607)</read_first>
  <reversibility rating="reversible">Two new writer functions plus rows; the container choice binds nothing else.</reversibility>
  <action>Add emitFirefoxIco taking the three smallest generated PNG buffers (16, 32, 48) and returning a complete ICO file: the 6-byte header with reserved byte 0, image type 1, and count 3, followed by three 16-byte directory entries each carrying width, height, zero color count, zero reserved, two planes, 32 bits per pixel, the PNG payload length, and the running payload offset, followed by the three PNG payloads byte-identical. Add emitFirefoxIcns taking the 128, 32 and 16 PNG buffers and returning a complete ICNS file: the 8-byte icns magic plus total file length as big-endian uint32, followed by one chunk per size with the four-character type code taken from the Apple TN0617 icon-family table cited in a code comment, each chunk carrying its own big-endian total length plus the PNG payload. Wire both writers per variant so generated/branding/dev/firefox.ico, generated/branding/dev/firefox.icns and the release mirrors are produced from the just-rasterized buffers inside the same emit pass. Add TARGETS rows with string-literal generated paths and no tracked comparand (no hand-written originals exist). macOS-host rendering stays explicitly deferred to v2 PKG-01 per GEN-03; what this task guarantees is structural validity (magic, counts, lengths, payload identity), asserted by the next task's checker. Extend the self-test with a truncated-payload case and a wrong-magic case, each required to go red naming the file.</action>
  <verify>
    <automated>cd /home/chris/coding/Power-Browser && node scripts/generate.mjs && node -e "const f=require('node:fs');for(const v of ['dev','release']){const i=f.readFileSync('generated/branding/'+v+'/firefox.ico');if(i.readUInt16LE(0)!==0||i.readUInt16LE(2)!==1||i.readUInt16LE(4)!==3)process.exit(1);const c=f.readFileSync('generated/branding/'+v+'/firefox.icns');if(c.subarray(0,4).toString()!=='icns'||c.readUInt32BE(4)!==c.length)process.exit(1)}" && node scripts/generate.mjs --self-test</automated>
    <fails_when>non-zero exit, ICO header fields differing from 0/1/3, ICNS magic not equal to icns, ICNS length field not equal to the file length, or any --self-test line not ok</fails_when>
  </verify>
  <acceptance_criteria>Four container files emitted; header probe passes for both variants; payload bytes equal the corresponding generated PNGs; new self-test cases observed red before landing.</acceptance_criteria>
  <done>Real ICO and ICNS files generated with zero new packages; structural validity machine-checked.</done>
</task>

<task type="auto">
  <name>Icon registry check plus rows</name>
  <files>scripts/verify-icon-ihdr.mjs, scripts/verify-platform.sh</files>
  <read_first>scripts/verify-platform.sh (registry rows lines 3631-3672 for row shape and NEW-comment convention), scripts/verify-branding-preflight.mjs (skeleton lines 42-65, reporter lines 72-86, self-test control-green-first shape lines 797-802)</read_first>
  <reversibility rating="reversible">New script plus two registry rows; both revert in one commit.</reversibility>
  <action>Create scripts/verify-icon-ihdr.mjs on the preflight skeleton (REPO_ROOT derivation, NAME constant, --self-test-only argument loop exiting 2 on unknown flags, accumulating reporter, absent inputs as failures): for each variant read the five defaultN.png files and assert PNG signature plus IHDR width and height both equal to N via readUInt32BE at bytes 16 and 20; assert firefox.ico opens with reserved 0, type 1, and a count equal to its directory entries with each entry length matching its payload slice; assert firefox.icns opens with icns magic and a total length equal to the file size with each chunk length consistent while walking the chunk chain; treat any empty derived set as its own failure. Ship --self-test that mirrors the generated icon set into mkdtemp, asserts the unmutated control green first, then plants one mutation per case (wrong-height PNG, truncated ICO, bad ICNS magic) requiring red naming file plus both values. Append icon-ihdr and icon-ihdr-self-test rows to the CHECKS array with a NEW (03-02) provenance comment; both honestly --quick (byte reads off disk only).</action>
  <verify>
    <automated>cd /home/chris/coding/Power-Browser && node scripts/verify-icon-ihdr.mjs && node scripts/verify-icon-ihdr.mjs --self-test && scripts/verify-platform.sh --only icon-ihdr && scripts/verify-platform.sh --only icon-ihdr-self-test && scripts/verify-platform.sh --quick</automated>
    <fails_when>non-zero exit from any of the five invocations, or any --only summary line not naming its own label verbatim</fails_when>
  </verify>
  <acceptance_criteria>Icon check green on the real tree and red on each planted fault; both rows pass singly and inside --quick.</acceptance_criteria>
  <done>Icon output gated in the single driver; GEN-02 emission fully checked short of the human visual.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| brand/mark.svg → spawned inkscape | Asset bytes cross into a child process invocation |
| generated PNG bytes → ICO/ICNS containers | Raster output crosses into binary containers shipped to foreign hosts |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-04 | Tampering / Elevation | inkscape spawnSync invocation | high | mitigate | Fixed argv array, no shell, sizes from a frozen numeric array, source path a literal; hostile filenames cannot reach the command line because no manifest value selects the asset path |
| T-03-05 | Tampering | PNG dimension drift (wrong density) | medium | mitigate | IHDR-exact assertion per file in the new registry check; non-vacuous via planted wrong-height fault |
| T-03-06 | Tampering | Silent logo distortion for non-square downstream art | medium | mitigate | Squareness pre-check hard-fails with the plain-words message; prohibition P-GEN-02 recorded above |
| T-03-SC | Tampering | npm/pip/cargo installs | high | mitigate | Zero new packages; ICO/ICNS are pure-Node writers; inkscape is the pinned-nixpkgs system binary per the RESEARCH legitimacy audit |
</threat_model>

<verification>
node scripts/generate.mjs --self-test plus scripts/verify-platform.sh --quick after every task commit; full scripts/verify-platform.sh at plan end. The rendered-pixel visual ("shows the downstream icon in launcher, window, desktop entry") is a human-check carried by plan 03-04's build task under end-of-phase mode.
</verification>

<success_criteria>
Ten PNGs IHDR-exact; ICO/ICNS structurally valid; squareness and presence guards live with hostile self-test cases; both registry rows green inside --quick.
</success_criteria>

<output>
Create `.planning/phases/03-firefox-branding-emitter-and-icon-pipeline/03-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces

New symbols this plan creates (the plan-review-convergence source-grounding pass must exclude these from drift verification):

- Generator step rasterizeIcons plus emitFirefoxIco and emitFirefoxIcns in scripts/generate.mjs
- Frozen TARGETS rows: branding/dev/default{16,32,48,64,128}.png and branding/release/default{16,32,48,64,128}.png with tracked comparands; branding/dev/firefox.ico, branding/dev/firefox.icns and release mirrors with no tracked comparand
- New file scripts/verify-icon-ihdr.mjs exporting the IHDR plus container check and its --self-test
- Registry labels icon-ihdr and icon-ihdr-self-test in scripts/verify-platform.sh
- Explicit non-artifact: inkscape deterministic-output equivalence between the Phase 1 rasters and fresh rasters under 1.4.4, assumed and then proven by the byte-identity gate going green in task 1

## Flagged assumptions (no silent drops)

1. GEN-02 adjacency probe (unresolved edge): dev and release rasters from one source are byte-identical yet written as ten separate files — they merge nowhere, share no cache entry, and neither variant reads the other's bytes. A source already at a target size still passes through inkscape with no passthrough.
2. GEN-02 empty probe (unresolved edge): a missing, zero-byte, or otherwise unreadable brand/mark.svg is a hard generation failure naming the asset, never an empty icon set and never a quiet reuse of the checked-in PNGs.
3. GEN-02 ordering probe (unresolved edge): raster order is the fixed ascending sequence 16, 32, 48, 64, 128 and self-test plus --check output order is stable across runs; no manifest value reorders it.
