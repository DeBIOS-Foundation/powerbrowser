---
phase: 03-firefox-branding-emitter-and-icon-pipeline
plan: 03
type: execute
wave: 3
depends_on: [03-01, 03-02]
files_modified: [scripts/generate.mjs, scripts/lib/config-schema.json, configuration.toml, scripts/verify-installer-schema.mjs, scripts/verify-platform.sh]
autonomous: true
requirements: [GEN-03]
estimate:
  tokens: 32500
  raw_tokens: 65000
  tasks: 3
  confidence: high
must_haves:
  truths:
    - "The generator emits NSIS branding defines, MSIX manifest fields, macOS Info.plist fields, and the Windows tile manifest per variant from configuration.toml values"
    - "Hostile brand values (quotes, NSIS variable syntax, XML metacharacters) are rejected at generate time with the offending key named"
    - "Windows and macOS outputs are labelled and checked as schema-complete only, never as build-verified"
  artifacts:
    - path: "scripts/lib/config-schema.json"
      provides: "installer.support_url and installer.tile_color key entries with regex guards"
      contains: "installer.support_url"
    - path: "scripts/generate.mjs"
      provides: "NSIS, AppxManifest, Info.plist, VisualElementsManifest emitters plus NSIS and XML sink guards"
      contains: "emitBrandingNsi"
    - path: "scripts/verify-installer-schema.mjs"
      provides: "Installer fragment presence and schema registry check with self-test"
      min_lines: 100
  key_links:
    - from: "scripts/generate.mjs"
      to: "scripts/lib/config-schema.json"
      via: "New installer keys flow through the single schema table — unknown-key rejection, masker, validator unchanged"
      pattern: "installer\\."
    - from: "scripts/verify-installer-schema.mjs"
      to: "generated/branding/dev/branding.nsi"
      via: "Presence plus !define-line schema assertion off the emitted fragment"
      pattern: "BrandFullName"
  prohibitions:
    - requirement_id: "GEN-03"
      category: "transparency"
      status: "resolved"
      verification: "judgment"
      resolution: null
      reason: null
      statement: "MUST NOT present Windows or macOS installer outputs as build-verified in any success copy, check label, or summary prose — GEN-03 requires schema-complete only for those hosts, with builds deferred to v2 PKG-01, and claiming more misleads the release decision"
---

<objective>
Emit every cross-platform installer branding field from the manifest: NSIS defines, MSIX manifest fragment, macOS bundle fragment, and tile manifest per variant — each guarded for its sink, each checked for schema completeness.

Purpose: GEN-03 makes Power Browser install anywhere Firefox installs; v1 generates all three hosts' branding and build-verifies Linux, with the honest schema-complete split for Windows and macOS that Mozilla's own generator precedent allows.
Output: Two new schema keys, four emitter functions plus two sink guards, eight TARGETS rows, scripts/verify-installer-schema.mjs, registry rows.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-firefox-branding-emitter-and-icon-pipeline/03-RESEARCH.md
@.planning/phases/03-firefox-branding-emitter-and-icon-pipeline/03-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Installer schema keys plus own-manifest values</name>
  <files>scripts/lib/config-schema.json, configuration.toml</files>
  <read_first>scripts/lib/config-schema.json (key-entry shape lines 26-32, the required-means-masked rule lines 268-270 of generate.mjs via the file $comment), configuration.toml (all 67 lines, section shape and inline required-ness notes)</read_first>
  <reversibility rating="reversible">Two additive optional keys; removal restores the prior table exactly.</reversibility>
  <action>Add exactly two keys to the single schema table, both optional so Power Browser's own build exercises the fallback paths a partial downstream manifest will take: installer.support_url as a string with a regex admitting only https URLs with no spaces or control characters plus regex_help in plain words plus the regex_example https://example.org/support, and installer.tile_color as a string with the regex caret-hash-six-hex-digits plus help and example. Installer CompanyName reuses product.vendor_display and introduces no second vendor string; the NSIS Channel stays the literal unofficial with no key; MSIX publisher and display derive from identity.distribution_id plus the display name with no new keys. Append an [installer] section to configuration.toml setting support_url to https://powerbrowser.org (the project's own homepage host, no new invented URL) and leaving tile_color unset so the omission path is the one this tree builds with. No pipeline change: the keys flow through parse, reject-unknown, mask, merge and validate untouched.</action>
  <verify>
    <automated>cd /home/chris/coding/Power-Browser && node scripts/generate.mjs --self-test && node -e "const s=require('./scripts/lib/config-schema.json');for(const k of ['installer.support_url','installer.tile_color']){if(!s.keys[k]||!s.keys[k].regex)process.exit(1)}" && git add -A scripts/lib/config-schema.json configuration.toml && node scripts/scan-brand-residue.mjs</automated>
    <fails_when>non-zero exit from --self-test, either key missing its regex, or the residue scan going red</fails_when>
  </verify>
  <acceptance_criteria>Both keys present with regex, help, and example; unknown-key rejection still fires on a misspelt installer key in a scratch manifest; Power Browser's own manifest validates with tile_color omitted.</acceptance_criteria>
  <done>Installer keys live in the one schema table; own manifest carries the section.</done>
</task>

<task type="auto">
  <name>Four installer emitters plus NSIS and XML sink guards</name>
  <files>scripts/generate.mjs</files>
  <read_first>scripts/generate.mjs (emitConfigureSh lines 671-685 for emitter shape, UNEMITTABLE plus assertEmittable lines 556-584 for the guard shape to extend, TARGETS lines 830-861 for row shape), .planning/phases/03-firefox-branding-emitter-and-icon-pipeline/03-RESEARCH.md (NSIS !define block plus AppxManifest, Info.plist, VisualElementsManifest field excerpts in Code Examples)</read_first>
  <reversibility rating="reversible">New emitters, guards, and rows; the prior table and guard are untouched in behavior.</reversibility>
  <action>Add two sink guards beside assertEmittable following its report-through-report shape and plain-words copy: an NSIS guard rejecting double quotes, dollar-brace sequences, carriage returns, line feeds and NUL, and an XML guard rejecting ampersand, angle brackets, double quotes and control characters; both report naming the TOML dotted path and ending with the re-run command, and neither escapes (rejection only, per CFG-03). Add four emitters each taking config plus variant: emitBrandingNsi producing the six !define lines with BrandFullNameInternal and BrandFullName carrying display_name plus suffix, CompanyName carrying product.vendor_display, URLInfoAbout and HelpLink carrying installer.support_url falling back to product.homepage and hard-failing naming both keys when both are unset, and Channel as the literal unofficial; emitAppxManifestFields producing a fragment with DisplayName and Description from display_name plus suffix and Identity Name from distribution_id plus app_basename; emitInfoPlistFields producing a fragment with CFBundleName from the display name and CFBundleIdentifier as the derived distribution_id-plus-sanitized-name form carrying no new key, plus the firefox.icns and document.icns file names as literals; emitVisualElementsManifest producing the full tile file with BackgroundColor from installer.tile_color when set and the whole BackgroundColor line omitted when unset (valid per the Windows schema, falling back to the app tile default — no invented color). Every interpolated manifest value passes the sink guard matching its file format. Add eight TARGETS rows with string-literal paths only (branding/dev/branding.nsi, branding/dev/firefox.VisualElementsManifest.xml, installer/dev/AppxManifest-fields.xml, installer/dev/Info-plist-fields.xml plus release mirrors) and no tracked comparand, since no hand-written originals exist. Extend the self-test with hostile cases for a display_name carrying a double quote, a support_url carrying dollar-brace text, and a vendor_display carrying an ampersand, each required to go red naming the key.</action>
  <verify>
    <automated>cd /home/chris/coding/Power-Browser && node scripts/generate.mjs && grep -c '!define BrandFullName ' generated/branding/dev/branding.nsi && grep -c 'BackgroundColor' generated/branding/dev/firefox.VisualElementsManifest.xml; test $? -eq 1 && node scripts/generate.mjs --self-test && node scripts/generate.mjs --check</automated>
    <fails_when>non-zero exit from generate, --self-test, or --check; the BrandFullName count differing from 1; or the BackgroundColor grep finding the omitted line (grep must exit 1 on our own manifest)</fails_when>
  </verify>
  <acceptance_criteria>Eight fragments emitted with manifest-derived values in place; tile-color omission honored on our own tree; all three hostile self-test cases observed red naming their key before landing.</acceptance_criteria>
  <done>Cross-platform installer branding generated and sink-guarded; schema-complete split honored.</done>
</task>

<task type="auto">
  <name>Installer schema registry check plus rows</name>
  <files>scripts/verify-installer-schema.mjs, scripts/verify-platform.sh</files>
  <read_first>scripts/verify-platform.sh (registry rows lines 3631-3672 for row shape and NEW-comment convention), scripts/verify-branding-preflight.mjs (skeleton lines 42-65, reporter lines 72-86, three-direction set comparison lines 649-727)</read_first>
  <reversibility rating="reversible">New script plus two registry rows; both revert in one commit.</reversibility>
  <action>Create scripts/verify-installer-schema.mjs on the preflight skeleton: derive the emitted installer fragment set under generated/ at check time and compare as set equality against the TARGETS-derived expectation in both directions; assert each branding.nsi carries all six !define names with non-empty values; assert each AppxManifest fragment is well-formed XML carrying DisplayName, Description and Identity Name; assert each Info-plist fragment carries CFBundleName and CFBundleIdentifier; assert the tile manifest is well-formed XML and that BackgroundColor presence matches the manifest's tile_color set-or-unset state read from configuration.toml at check time; treat an empty derived set as its own failure; never assert build-verification of Windows or macOS output anywhere in copy or labels. Ship --self-test mirroring the fragments into mkdtemp with the unmutated control green first, then one mutation per case (dropped !define, malformed XML, tile-color state mismatch) requiring red naming file plus both values. Append installer-schema and installer-schema-self-test rows to the CHECKS array with a NEW (03-03) provenance comment; both honestly --quick.</action>
  <verify>
    <automated>cd /home/chris/coding/Power-Browser && node scripts/verify-installer-schema.mjs && node scripts/verify-installer-schema.mjs --self-test && scripts/verify-platform.sh --only installer-schema && scripts/verify-platform.sh --only installer-schema-self-test && scripts/verify-platform.sh --quick</automated>
    <fails_when>non-zero exit from any of the five invocations, or any --only summary line not naming its own label verbatim</fails_when>
  </verify>
  <acceptance_criteria>Installer fragments schema-green on the real tree and red on each planted fault; both rows pass singly and inside --quick; no row label or message claims a Windows or macOS build.</acceptance_criteria>
  <done>Installer output gated as schema-complete in the single driver; GEN-03 Windows/macOS half closed honestly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| manifest → NSIS defines | Brand strings cross into a Windows installer script language with ${} expansion and quote semantics |
| manifest → MSIX / plist / tile XML | Brand strings cross into XML parsed by foreign packagers |
| new TOML keys → merge | New key shapes cross the unknown-key rejection ordering |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-07 | Tampering | NSIS sink (double quote, dollar-brace) | high | mitigate | Schema regex primary plus new NSIS sink guard at every interpolation; hostile self-test cases observed red |
| T-03-08 | Tampering | XML sinks (ampersand, angle brackets) | high | mitigate | Schema regex primary plus new XML sink guard; malformed-XML self-test and registry well-formedness assertion |
| T-03-09 | Tampering | Prototype pollution via new installer keys | high | mitigate | Keys flow through the existing unknown-key rejection before merge; ordering unchanged; no new key list anywhere |
| T-03-10 | Spoofing | Windows .ico companions beyond firefox.ico | medium | mitigate | Out of scope recorded: browser/app/moz.build companion DEFINES (document, newtab and siblings) are PKG-01 inputs for v2, noted in the plan summary, not silently dropped |
| T-03-SC | Tampering | npm/pip/cargo installs | high | mitigate | Zero new packages; no system tool beyond the inkscape already audited |
</threat_model>

<verification>
node scripts/generate.mjs --self-test plus scripts/verify-platform.sh --quick after every task commit; full scripts/verify-platform.sh at plan end. Linux build-verification of installer output rides plan 03-04's tier-3 build.
</verification>

<success_criteria>
Two schema keys live with regex guards; eight fragments emitted with omission semantics honored; hostile values rejected naming their key; both registry rows green inside --quick with no build-verified claim for foreign hosts.
</success_criteria>

<output>
Create `.planning/phases/03-firefox-branding-emitter-and-icon-pipeline/03-03-SUMMARY.md` when done
</output>

## Artifacts this phase produces

New symbols this plan creates (the plan-review-convergence source-grounding pass must exclude these from drift verification):

- Schema keys installer.support_url and installer.tile_color in scripts/lib/config-schema.json
- [installer] section in configuration.toml (support_url set, tile_color unset)
- Emitter functions in scripts/generate.mjs: emitBrandingNsi, emitAppxManifestFields, emitInfoPlistFields, emitVisualElementsManifest, plus NSIS and XML sink-guard functions beside assertEmittable
- Frozen TARGETS rows: branding/dev/branding.nsi, branding/dev/firefox.VisualElementsManifest.xml, installer/dev/AppxManifest-fields.xml, installer/dev/Info-plist-fields.xml plus release mirrors, all without tracked comparands
- New file scripts/verify-installer-schema.mjs exporting the fragment schema check and its --self-test
- Registry labels installer-schema and installer-schema-self-test in scripts/verify-platform.sh

## Flagged assumptions (no silent drops)

1. GEN-03 unclassified probe (manual review): Windows NSIS/MSIX outputs and macOS DMG/icns outputs are schema-complete only — their byte-level correctness against the real Windows shell and macOS Finder is unverified until packaging hosts land under v2 PKG-01 (RESEARCH assumption A1 carries this explicitly, with the png2icns fallback if the pure-Node ICNS writer is rejected there). Nothing in this plan builds, signs, or installs on those hosts.
2. Companion .ico coverage (RESEARCH open question 3): browser/app/moz.build names firefox, document, newwindow, newtab, pbmode and document_pdf ICO defines while this tree ships only firefox.ico; whether a Windows build fails without the companions or falls back is unknown and out of v1 scope (Linux-verified only). Recorded here as a named PKG-01 input so v2 does not rediscover it.
3. Tile color default: installer.tile_color has no color default anywhere — when unset the BackgroundColor line is omitted and Windows falls back to its app-tile default. No hex value is invented for Power Browser; our own tree builds through the omission path.
