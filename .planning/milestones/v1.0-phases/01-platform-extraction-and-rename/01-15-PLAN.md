---
phase: 01-platform-extraction-and-rename
plan: 15
type: execute
wave: 2
depends_on: ["01-14"]
gap_closure: true
autonomous: true
requirements: [MIG-01, MIG-02, MIG-03, MIG-04, GUI-01, GUI-03, GUI-04, SEC-01]
files_modified:
  - scripts/scan-brand-residue.mjs
  - scripts/rebase-upstream.sh
  - .github/workflows/rebase-upstream.yml
  - docs/BUILD.md
  - .planning/phases/01-platform-extraction-and-rename/deferred-items.md

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "scripts/scan-brand-residue.mjs has an `--extra-root <dir>` mode that walks the filesystem under a caller-supplied root OUTSIDE the git index, applying the same `scope.exclude` prefix filter and `binary_extensions` suffix filter the tracked-tree path applies, and scanning those files with the same inventory rows and the same boundary matcher."
    - "scripts/rebase-upstream.sh passes $UPSTREAM_DIR to the post-replay scan invocation, so the invocation whose stated purpose is re-checking the tree the rebase just rewrote can actually read that tree. The --dry-run step listing names the same argument, so a dry run does not describe a command the real path no longer runs."
    - "A --self-test row plants a brand token in a scratch directory passed as `--extra-root` and requires the scan to exit non-zero naming that path."
    - "A --self-test control row passes the same CLI a scratch root containing only a token-free file and requires exit 0, so the red above is attributable to the plant and not to the tracked-tree scan that runs alongside it."
    - "An `--extra-root` pointing at a directory that does not exist fails loudly naming the path, and never silently skips. A skip-when-absent mode would be green by construction — the exact failure class CR-B is."
    - "An `--extra-root` whose walk yields zero files fails, on the same terms the tracked-tree path already fails an empty file set: an empty scan set is not a clean tree, it is a scan that ran over nothing."
    - "The walk never follows a symbolic link and never descends a `.git` directory. `upstream/powerbrowser` is a real symlink back into this repo's own `powerbrowser/` directory, so following links would rescan the repo's tree through a second path and could recurse."
    - "The extra-root pass reports renameable-class occurrences AND unclaimed residue-probe hits, each named as `<root>/<relative path>:<line>`, so a case variant the boundary matcher misses under the rebased tree is caught by the same independent detector condition 4 applies to the tracked tree."
    - "The extra-root pass does NOT run the ground-truth reconciliation arithmetic. D-17's census counts this repo's own migrating tree; applying it to a foreign checkout would make the extra-root pass permanently red for a reason that has nothing to do with residual brand strings."
    - "`upstream/` is a 1.1 GB git-ignored clone that may not exist on a fresh checkout, so no verify-platform.sh row scans the live `upstream/`. The mode reaches `--quick` only through its three hermetic self-test rows, which use mktemp scratch roots and need no clone; the live invocation is rebase-upstream.sh, which runs it only after fetch-upstream.sh has materialised the tree."
    - ".github/workflows/rebase-upstream.yml is explicitly decided and NOT changed functionally: its step-1 scan is a deliberate pre-rebase check of the tracked tree with no upstream tree present, and the post-replay upstream-aware scan reaches CI through the `bash scripts/rebase-upstream.sh` step it already runs. A comment records that decision in the workflow so the question is not silently reopened."
    - "scripts/verify-platform.sh --quick exits 0, with scan-brand-residue PASS and scan-brand-residue-self-test PASS reporting every fixture row including the three new extra-root rows."
    - statement: "A real ESR rebase run of scripts/rebase-upstream.sh against a live 1.1 GB upstream/ checkout completes the post-replay scan within an acceptable wall-clock budget next to the clone and replay it follows."
      verification: backstop
  artifacts:
    - "scripts/scan-brand-residue.mjs — the exported `extraRootFiles(inv, root)` walker, the `--extra-root <dir>` CLI mode, and three new self-test fixture rows"
    - "scripts/rebase-upstream.sh — `--extra-root \"$UPSTREAM_DIR\"` on the post-replay invocation and on the matching --dry-run step listing"
    - ".github/workflows/rebase-upstream.yml — a comment recording the explicit decision that step 1 stays a tracked-tree pre-check and the upstream-aware scan arrives via rebase-upstream.sh"
    - "docs/BUILD.md — the residual-brand scan added to the rebase procedure's fail-loudly list, naming the upstream tree"
    - ".planning/phases/01-platform-extraction-and-rename/deferred-items.md — row 9, recording CR-A and CR-B closure and the deliberate NOT-dones"
  key_links:
    - "rebase-upstream.sh's post-replay invocation -> `--extra-root $UPSTREAM_DIR` -> extraRootFiles() -> scan(inv, {root, files}) -> the actual rewritten upstream tree (the link 01-VERIFICATION.md records as NOT WIRED)"
    - "inventory/brand-tokens.json's `scope.exclude` and `binary_extensions` -> extraRootFiles()'s filters (one filter definition, two file-set sources — the extra root is not a second, weaker ruleset)"
    - "the three new self-test fixture rows -> the existing scan-brand-residue-self-test registry row in verify-platform.sh (the rows reach --quick through a row that already exists)"
    - "the `sourcerer` token literal in the self-test fixtures -> scripts/scan-brand-residue.mjs's presence in the inventory's `scope.exclude` list (the one thing that lets a fault be planted without the plant tripping the gate over the repo's own tracked tree)"
  prohibitions:
    - statement: "MUST NOT write to `upstream/`. The extra-root mode reads only. `git -C upstream diff` staying empty is the invariant, and Gecko is never modified outside the patch stack."
    - statement: "MUST NOT make `--extra-root` skip silently when its root is absent, empty, or unreadable. A silent skip reproduces CR-B in a new shape: an invocation that cannot go red for its stated cause."
    - statement: "MUST NOT spell a brand token from inventory/brand-tokens.json in any file this plan touches other than scripts/scan-brand-residue.mjs, which the inventory's `scope.exclude` already covers. rebase-upstream.sh's existing comment says so in its own words and that reasoning still binds."
    - statement: "MUST NOT create a sibling verification driver. scripts/verify-platform.sh is the one registry; verify-phase-0{2,3,4,5}.sh were deleted and must not come back."
    - statement: "MUST NOT hand-keep an expectation list of directories to skip inside the walker. `.git` and symlinks are structural exclusions with stated mechanical reasons; any content-based exclusion belongs in inventory/brand-tokens.json's `scope.exclude`, which is the derived source both file-set paths already read."
    - statement: "MUST NOT register a verify-platform.sh row that scans the live `upstream/`. A row that skips when the 1.1 GB clone is absent is green by construction on a fresh checkout — deferred-items row 4 records this repo hitting exactly that with apply-patches-self-test."
    - statement: "MUST NOT introduce a generator, template, or derive-at-build-time helper for any branding value. Phase 2's acceptance test is byte-identity against what Phase 1 wrote by hand."
---

<objective>
Close gap **CR-B** from `01-VERIFICATION.md` (`new_gaps_this_pass`, surfaced as CR-02 in
`01-REVIEW.md`), and record the disposition of both this phase's remaining gaps in the deferred-item
ledger.

`scan-brand-residue.mjs`'s `scopeFiles()` builds its file set from `git ls-files` with no parameter
for a filesystem root outside the git index. `.gitignore:20` is `upstream/`.
`scripts/rebase-upstream.sh:108` invokes the scan with no arguments after the patch replay. So that
invocation scans exactly the same tracked files the CI workflow's own step 1 already scanned — none
of them under `upstream/`, which is precisely the tree a rebase rewrites and the tree that specific
invocation exists to re-check. `01-REVIEW.md` reproduced it: 109 tracked files scanned, 0 of them
under `upstream/`. CLAUDE.md's stated guarantee — "an upstream rebase that reintroduces a brand
token fails there rather than in a release" — does not hold for anything under `upstream/`.

**Scope fence.** This does NOT affect the scan's ordinary function over the repo's own tracked tree;
that was re-confirmed this pass by `scan-brand-residue: PASS` and its self-test in `--quick`. The
gap is scoped specifically to the rebase-triggered re-scan of the untracked upstream checkout.

Purpose: the rebase is the one routine operation that pulls in content nobody in this repo wrote,
which is why `rebase-upstream.sh`'s own comment gives that as the reason the gate runs there. A gate
placed at exactly the right call site that cannot see the tree at that call site is the same failure
class as an absence assertion over an emitter you wrote yourself — green by construction, unable to
go red for its stated cause.

Output: an `--extra-root <dir>` mode that walks a caller-supplied root outside the git index under
the same filters; `rebase-upstream.sh` passing `$UPSTREAM_DIR` on both the real and the dry-run
paths; three hermetic self-test rows (control, plant, missing-root) proving the mode can go red and
does not go red for free; the `.github/workflows/rebase-upstream.yml` question decided in writing;
and ledger row 9 recording both closures and the deliberate NOT-dones.

**Requirement mapping for this plan:** CR-B maps to **MIG-01 through MIG-04** — the rename and
residual-brand gate that guards the migration. `01-VERIFICATION.md` scores MIG-01 and MIG-03 as
satisfied in their present state with a regression-prevention gap noted, and names CR-B as
specifically about the "cannot regress via rebase" half of MIG-03. The remaining IDs on the
`requirements` field are carried per the phase contract; SEC-01 is closed by plan 01-14 and GUI-01 /
GUI-03 / GUI-04 are unaffected by this plan.
</objective>

<planning_dispositions>
Decisions this plan makes explicitly rather than leaving open:

- **`.github/workflows/rebase-upstream.yml`: DECIDED — no functional change, one comment added.**
  The workflow's step 1 (`node scripts/scan-brand-residue.mjs`) is deliberately a **pre**-rebase
  scan of the tracked tree, run before any clone exists; its own comment already says it "needs no
  build, no Nix and no upstream tree, so it costs seconds and fails fast on a tree that was already
  dirty — which keeps a pre-existing residue from being misread as something the replay introduced."
  Passing `--extra-root` there would be pointing it at a directory that does not yet exist, which
  Task 1 makes a hard failure. The post-replay, upstream-aware scan reaches CI through the
  `bash scripts/rebase-upstream.sh --tag "$REBASE_TAG"` step the workflow already runs, so fixing
  the shell script fixes the workflow. Task 3 records that reasoning as a comment in the YAML so a
  future reader does not reopen a settled question and add a second, broken invocation.
- **Tier: the mode itself is exercised in `--quick`; the live `upstream/` is never scanned by a
  registry row.** The three new self-test rows use `mktemp -d` scratch roots, run in seconds, and
  need no clone — they ride the existing `scan-brand-residue-self-test` row, which is already in
  `--quick`. No row scans the real `upstream/`: it is a 1.1 GB git-ignored clone absent on a fresh
  checkout, and a row that skips when it is absent would be green by construction. Deferred-items
  row 4 records this repo hitting exactly that with `apply-patches-self-test`. The live invocation
  is `rebase-upstream.sh`, which runs it only after `fetch-upstream.sh` has materialised the tree.
- **No new `verify-platform.sh` registry row.** `scan-brand-residue` and
  `scan-brand-residue-self-test` already invoke this script and its `--self-test`. The new mode and
  its three fixture rows reach `--quick` through rows that already exist.
- **The extra-root pass runs offences and probes, not reconciliation.** `reconcile()` /
  `groundTruth()` implement D-17's census of this repo's own migrating tree. Applied to a foreign
  Gecko checkout the arithmetic cannot close, so the pass would be permanently red for a reason
  unrelated to residual brand strings. Offences (`offensesOf`) and unclaimed residue probes are both
  meaningful over any tree and both run.
- **Spec-less probe fallback: SKIPPED, recorded.** Phase 01 has no plain `01-SPEC.md` (only
  `01-UI-SPEC.md`), so the default-ON probe fallback would normally fire over the phase's eight
  requirement IDs. It is skipped for this gap-closure run: this is a replan of an already-executed
  phase, and `01-VERIFICATION.md`'s two `missing[]` lists are the authoritative, verifier-authored
  predicate source for the work in scope. Probing MIG-01…MIG-04 / GUI-01 / GUI-03 / GUI-04 / SEC-01
  would re-derive edges for functionality already delivered and verified across plans 01-01…01-13,
  which is noise against this plan's scope rather than coverage of it. `must_haves.truths` above is
  derived directly from CR-B's two `missing[]` predicates.
- **`## UI Considerations` lift:** `01-UI-SPEC.md:300-340` carries the section, but no row bears on
  this plan — nothing here paints. Plan 01-14 handles the one row that does.
- **Wave 2, depends_on 01-14, and why.** Two honest reasons, neither manufactured: ledger row 9
  records the disposition of BOTH gaps including 01-14's tier decision, so it cannot be written
  first; and both plans require `scripts/verify-platform.sh --quick` green as an acceptance
  criterion, which two parallel executors would each observe against the other's half-landed tree.
- **Reversibility: reversible.** New CLI mode plus argument wiring; `git revert` restores the prior
  behaviour with no downstream migration and no state to unwind. No `checkpoint:decision` earned.
- **API coverage:** no external API, SDK, or service is integrated — this plan modifies one local
  static Node script, one shell wrapper, one CI workflow comment, and two documents. No `COVERAGE.md`
  matrix is produced.
- **Assumption delta:** the file-set source generalises from one (`git ls-files`) to two
  (`git ls-files` plus a caller-supplied root). This is `add-alongside`, deliberately: the tracked
  tree remains the primary and unconditional scan set, and the extra root is an additional pass on
  top of it, never a replacement. Promotion to a generalised "roots" model is not warranted for a
  second case that is optional, caller-supplied, and never scanned alone. What would force a later
  promote: a third root, or a caller wanting the extra root scanned *instead of* the tracked tree.
- **Schema push:** no ORM-relevant files in scope. Not applicable.
- **Package legitimacy:** no npm/pip/cargo install task in scope. No legitimacy checkpoint is
  earned, and none may be silently skipped later on that basis.
</planning_dispositions>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Give the scan a filesystem root outside the git index, and wire the rebase's post-replay invocation to it</name>

  <read_first>
    - `scripts/scan-brand-residue.mjs` — the file being modified. Read it in full, and specifically:
      the header comment block (lines 1-100) which states the usage line you must extend;
      `scopeFiles(inv, {chainFiles})` at lines 255-269, whose `exclude` / `binary` filter expressions
      are the ones the new walker must reuse rather than restate; `scan(inv, {chain, root, files,
      rows})` at line 289, which ALREADY takes `root` and `files` — the extra-root pass is a second
      call to this existing function, not a second scanner; the empty-file-set guard inside `scan()`
      and its counterpart in `main()`; `offensesOf()`; the `unclaimedProbes` accumulation inside
      `scan()`; and `main()` at lines 805-889 with its single-exit-source discipline and its
      `gateFailures()` decision function.
    - `scripts/rebase-upstream.sh` — the second file being modified. Read the whole script. The
      `--dry-run` step listing (the block of `echo` lines, step `4b`) and the real post-replay
      invocation near line 108 are two separate places that name the same command and must move
      together. Read the long comment above the invocation: it explains why the gate runs at this
      call site and why the old brand token is deliberately not spelled in that file.
    - `inventory/brand-tokens.json` — read `scope.exclude`, `scope.binary_extensions`, and
      `scope.residue_probes`. `scope.exclude` currently holds four repo-relative entries, none of
      which exists under a Gecko checkout, so applying them verbatim to extra-root-relative paths is
      inert there rather than harmful — confirm that before relying on it. Also confirm
      `scripts/scan-brand-residue.mjs` is in that list, because that is what lets Task 2 plant a
      fault using a real token literal without the plant tripping the gate over the repo's own tree.
    - `.gitignore` — line 20 is `upstream/`. This is the mechanical reason `git ls-files` cannot
      reach the tree the rebase rewrites.
    - `CLAUDE.md` — hard rule 2 (never modify Gecko outside the patch stack; `git -C upstream diff`
      staying empty is the invariant), the residual-brand-scan section (the `git ls-files` staging
      trap and the inventory-is-the-only-place-to-spell-a-token rule), and the Verification section's
      one-driver-one-registry rule.
    - `.planning/phases/01-platform-extraction-and-rename/01-VERIFICATION.md` — the CR-B `gaps` entry
      and its `missing[]` list, which is the acceptance source this task is scored against.
    - `.planning/phases/01-platform-extraction-and-rename/deferred-items.md` row 4 — the precedent
      for how this repo has already handled a check that depends on the 1.1 GB `upstream/` clone.
  </read_first>

  <files>scripts/scan-brand-residue.mjs, scripts/rebase-upstream.sh</files>

  <behavior>
    Written as the failing expectations BEFORE the mode exists. Each becomes a self-test row in
    Task 2 or an acceptance criterion below.

    - Given a scratch directory containing one file whose text contains a `brand-identifier`-class
      token, `node scripts/scan-brand-residue.mjs --extra-root <dir>` exits non-zero and its output
      names `<dir>` and the file's relative path and the line number.
    - Given a scratch directory containing one file with no token, the same command exits 0.
    - Given a path that does not exist, the same command exits non-zero and its output names that
      path. It does not skip and it does not pass.
    - Given a scratch directory containing no files at all, the same command exits non-zero stating
      the extra-root file set is empty.
    - Given a scratch directory containing a symlink to another directory holding a planted token,
      the walk does not descend it — the linked tree contributes no files.
    - `node scripts/scan-brand-residue.mjs` with no arguments behaves exactly as it does today:
      the tracked-tree scan and its verdict are unchanged in both file set and exit code.
    - `bash scripts/rebase-upstream.sh --tag <any tag> --dry-run` prints a step-4b line that names
      `--extra-root` and the upstream directory.
  </behavior>

  <action>
Add an `--extra-root <dir>` mode to `scripts/scan-brand-residue.mjs` and wire
`scripts/rebase-upstream.sh`'s post-replay invocation to it.

**1. Export a walker, `extraRootFiles(inv, root)`, beside `scopeFiles`.** It returns a sorted array
of paths RELATIVE to `root`, so its return value drops straight into the existing
`scan(inv, { root, files })` call — that function already takes both parameters and needs no change.
Walk recursively with `readdirSync(dir, { withFileTypes: true })` and:

  - **Never follow a symbolic link.** Skip any entry for which `dirent.isSymbolicLink()` is true,
    directory or file. `upstream/powerbrowser` is a real symlink back into this repo's own
    `powerbrowser/` directory (`rebase-upstream.sh` asserts exactly that resolution), so following
    links would rescan this repo's tree through a second path and could recurse. State that reason
    in a comment beside the skip — it is a specific fact about this tree, not a generic precaution.
  - **Never descend a directory named `.git`.** A rebased upstream checkout carries a multi-gigabyte
    object store whose packfiles are not source.
  - Collect only `dirent.isFile()` entries.
  - Apply the SAME two filters `scopeFiles` applies: the `inv.scope.exclude` prefix test and the
    `inv.scope.binary_extensions` lowercased-suffix test. Extract them into one shared predicate
    used by both `scopeFiles` and `extraRootFiles` rather than writing the expressions twice —
    the extra root must not become a second, weaker ruleset. Note in a comment that `scope.exclude`
    entries are repo-relative and are therefore inert against a foreign checkout's relative paths;
    that is acceptable and intended, and any content-based exclusion a real rebase turns out to
    need belongs in the inventory, never as a literal in this walker.
  - If `root` does not exist or is not a directory, throw an `Error` whose message names the path.
    Do not return an empty array — a silent skip is CR-B in a new shape.

**2. Add the CLI mode to `main()`.** Parse `--extra-root <dir>` the same way `--scope-chain` and
`--report` are parsed, rejecting a missing value with the same `return 2` shape those use. After the
existing tracked-tree scan has produced its `gateFailures()` reasons and BEFORE the single exit
decision:

  - Resolve the root and call `extraRootFiles`. Catch the missing-root `Error` and turn it into a
    `scan-brand-residue: FAIL -- ...` line naming the path, joining the gate reasons.
  - If the walk returns zero files, add a gate reason on the same terms `main()` already uses for
    the tracked tree: an empty scan set is not a clean tree, it is a scan that ran over nothing.
  - Otherwise call `scan(inv, { root: <resolved root>, files: <walked paths> })` and take both
    `offensesOf(...)` and `.unclaimedProbes` from the result. Print each offence and each unclaimed
    probe as `<root>/<relative path>:<line>: <token or probe>` so an operator can open the file
    directly, and add a gate reason if either list is non-empty. Unclaimed probes matter here for
    the same reason reconciliation condition 4 matters over the tracked tree: they are the
    independent detector that catches a case variant the boundary matcher missed.
  - Do NOT call `reconcile()` or `groundTruth()` on the extra-root result, and say why in a comment:
    D-17's arithmetic is a census of this repo's own migrating tree and cannot close over a foreign
    checkout, so reusing it would make this pass permanently red for a reason that has nothing to do
    with residual brand strings.
  - Keep `main()`'s single-exit-source discipline: the extra-root reasons join the same
    `gateFailures` list and the same `return 1`, and the success line is extended to name the
    extra-root file count and the root, so a green run states what it actually scanned.

Extend the header comment's usage block to list `--extra-root <dir>`, and add a short paragraph
there stating what the mode is for: a rebase replays patches onto a tree that `git ls-files` cannot
see because `.gitignore` excludes it, so without this the post-replay invocation re-scans the same
tracked files the pre-replay one already scanned. Do not spell any brand token in that paragraph
beyond what the file already spells.

**3. Wire `scripts/rebase-upstream.sh`.** Two sites, both of which name the same command and must
move together:

  - The real post-replay invocation (near line 108): pass `--extra-root "$UPSTREAM_DIR"`. Extend
    the failure message so it names both halves of what was scanned — the tracked tree and
    `$UPSTREAM_DIR` — because an operator reading a red line needs to know which tree carries the
    residue.
  - The `--dry-run` step listing's `4b.` line: add the same argument and extend its trailing comment
    so a dry run describes the command the real path runs.

  Add two or three lines to the existing comment block above the invocation explaining the CR-B
  correction: `git ls-files` cannot see `upstream/`, so before this the post-replay scan was a no-op
  relative to the tree the replay had just rewritten. Keep that comment's existing rule about not
  spelling the old brand token in this file — it is in the scanned scope and naming the literal there
  would make the file fail the very gate it invokes.

Do not write to `upstream/` from any of this. The mode reads only.
  </action>

  <verify>
    <automated>node scripts/scan-brand-residue.mjs && bash scripts/rebase-upstream.sh --tag FIREFOX_153_1_0esr_RELEASE --dry-run | grep -q -- '--extra-root' && scripts/verify-platform.sh --only scan-brand-residue</automated>
  </verify>

  <acceptance_criteria>
    - `node scripts/scan-brand-residue.mjs` (no arguments) exits 0 and its PASS line reports
      **109 scanned file(s)** — the count measured at plan time, plus one for each new file this plan
      stages (this plan adds no new files, so 109 is the expected number). The tracked-tree path is
      behaviourally untouched.
    - `node scripts/scan-brand-residue.mjs --extra-root "$(mktemp -d)"` exits non-zero, and the output
      states the extra-root file set is empty. Record the exact output line in the SUMMARY.
    - With a scratch root containing one file whose content carries a `brand-identifier`-class token
      from `inventory/brand-tokens.json`: the command exits non-zero and the output contains the
      scratch root path, the file's relative path, and a line number.
    - With that same scratch root containing instead one file of token-free text: the command exits 0
      and its PASS line names the extra-root file count and the root path.
    - `node scripts/scan-brand-residue.mjs --extra-root /nonexistent/path/for/this/check` exits
      non-zero and the output contains `/nonexistent/path/for/this/check`. It must not print a PASS
      line and must not exit 0.
    - `node scripts/scan-brand-residue.mjs --extra-root` (flag with no value) exits 2 with a message
      requiring a directory argument, matching how `--scope-chain` and `--report` reject a missing
      value.
    - Symlink containment: create scratch roots A and B, plant a token in a file under B, place a
      symlink at `A/link` pointing to B, then confirm `--extra-root A` exits 0 — the linked tree
      contributed nothing.
    - `grep -c 'extraRootFiles' scripts/scan-brand-residue.mjs` returns at least 3 (the export, the
      `main()` call, and the Task 2 self-test usage once that lands).
    - `grep -n 'reconcile\|groundTruth' scripts/scan-brand-residue.mjs` shows neither is called on the
      extra-root result, and a comment beside the extra-root block states why.
    - `bash scripts/rebase-upstream.sh --tag FIREFOX_153_1_0esr_RELEASE --dry-run` exits 0 and its
      step-4b line contains both `--extra-root` and the upstream directory path.
    - `grep -c -- '--extra-root' scripts/rebase-upstream.sh` returns **at least 2**, and both
      command-bearing sites carry it: the line invoking `node "$REPO_ROOT/scripts/scan-brand-residue.mjs"`
      and the `echo` line beginning `  4b.` each contain `--extra-root "$UPSTREAM_DIR"`. (At least,
      not exactly, because the explanatory comment added above the invocation may name the flag too.)
    - `git -C upstream diff --stat` produces no output if `upstream/` exists locally; if it does not
      exist, record that in the SUMMARY rather than reporting the check as passed.
    - `node scripts/scan-brand-residue.mjs` exits 0 with both changed files staged. Stage before
      trusting the green — the scan iterates `git ls-files` and an unstaged file is invisible to it.
    - `grep -rn 'scan-brand-residue' scripts/verify-platform.sh` returns exactly the two pre-existing
      rows. No third row was added and no sibling driver was created.
  </acceptance_criteria>

  <done>
    `--extra-root <dir>` walks a filesystem root outside the git index under the same inventory
    filters, reports offences and unclaimed probes named by absolute path and line, fails loudly on a
    missing or empty root, never follows a symlink, and `rebase-upstream.sh` passes `$UPSTREAM_DIR`
    to it on both the real and the dry-run paths.
  </done>

  <reversibility rating="reversible">An additive CLI mode plus two argument sites in a shell wrapper; `git revert` restores the prior behaviour with no state to unwind.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Three self-test rows that make the new mode go red, and one control proving the red is plant-caused</name>

  <read_first>
    - `scripts/scan-brand-residue.mjs` as edited by Task 1 — the file being modified. Read
      `selfTest()` (lines ~654-800) in full before adding rows. Study `SELF_TEST_INVENTORY` (the
      fixture inventory, deliberately never the real one), the `mkdtempSync` scratch-root pattern
      every existing fixture uses, and fixture 7's clean-control discipline: it runs the SAME
      unplanted fixture first and requires it green, so that the red below is proven caused by the
      plant and not by the fixture. That is the pattern rows 1 and 2 below must follow.
    - `inventory/brand-tokens.json` — confirm `scripts/scan-brand-residue.mjs` appears in
      `scope.exclude`. That is the ONLY reason a real token literal may be written into the fixtures
      you are about to add; the same literal in any other file would fail the permanent gate.
    - `scripts/verify-shell-error-copy.mjs`'s `runSelfTest()` — the precedent in this repo for a
      self-test that spawns the real CLI as a subprocess (`execFileSync(process.execPath, [SELF,
      ...])`) rather than calling internals, which is what "passed as `--extra-root`" requires.
    - `CLAUDE.md`'s Verification section — every new check has a `--self-test` that plants faults and
      requires each one to go red naming the drift.
    - `.planning/phases/01-platform-extraction-and-rename/01-VERIFICATION.md` — CR-B's second
      `missing[]` predicate, which this task is scored against verbatim.
  </read_first>

  <files>scripts/scan-brand-residue.mjs</files>

  <behavior>
    Three rows, added to `selfTest()`, each spawning the real CLI so the argument parsing, the
    walker, the filters, the scan, the reporting and the exit code are all exercised end to end.

    - Control: a scratch root holding one token-free file, passed as `--extra-root`, exits 0.
      Without this row a red in the next one could be caused by the tracked-tree scan running
      alongside it rather than by the plant.
    - Plant: a scratch root holding one file containing a real inventory token, passed as
      `--extra-root`, exits non-zero and the combined output names both the scratch root and the
      planted file's relative path.
    - Missing root: a path under the scratch directory that was never created, passed as
      `--extra-root`, exits non-zero and the output names that path. The mode must not skip.
  </behavior>

  <action>
Add three rows to `selfTest()` in `scripts/scan-brand-residue.mjs`, following the file's existing
conventions exactly: allocate scratch roots with `mkdtempSync` under `tmpdir()` (never inside the
repo — a planted file inside the repo would be invisible to `git ls-files` until staged and is
exactly the trap CLAUDE.md warns about), print one `--self-test PASS -- ...` or
`--self-test FAIL -- ...` line per row, set `overall = 1` on failure, and clean up in the existing
`finally` block.

Unlike the existing fixtures, these three spawn the REAL CLI rather than calling `scan()` directly,
because `01-VERIFICATION.md`'s `missing[]` names a token "passed as `--extra-root`" — the argument
parsing and the exit-code path are part of what must be proven. Use the same
`execFileSync(process.execPath, [<this script's own path>, '--extra-root', <dir>], { encoding:
'utf8', stdio: 'pipe' })` shape `verify-shell-error-copy.mjs`'s `runSelfTest()` uses, capturing
`err.status`, `err.stdout` and `err.stderr` from the throw so a non-zero exit is data rather than a
crash. Derive this script's own path from `import.meta.url`; the file already computes `REPO_ROOT`
that way. Note in a comment that each of these three rows also runs the ordinary tracked-tree scan
as a side effect, which is precisely why the control row exists.

Row labels, printed verbatim, so a future reader can cite them:

  1. `--extra-root over a clean scratch root is a PASS control` — write one token-free file (for
     example the text `nothing to see here`) into a fresh scratch root, run the CLI against it, and
     require exit 0. If this row is not green, rows 2 and 3 prove nothing and should say so in their
     own failure text.
  2. `--extra-root names a brand token planted outside the git index` — write one file containing a
     real `brand-identifier`-class token from `inventory/brand-tokens.json` (the existing fixtures
     already use the `chrome://<token>/content/x` form; reuse it) into a second fresh scratch root,
     run the CLI, and require BOTH a non-zero exit AND combined stdout+stderr containing the scratch
     root path AND the planted file's relative name. Exit code alone is not enough — a red that does
     not name the path would leave an operator unable to find the residue.
  3. `--extra-root on a nonexistent directory fails loudly rather than skipping` — join a name that
     was never created onto the scratch directory, run the CLI, and require a non-zero exit AND the
     output naming that path. Add a one-line comment stating what this row defends: a skip-when-absent
     mode would be green by construction, which is the exact failure class CR-B is.

Spelling a real inventory token inside `scripts/scan-brand-residue.mjs` is safe and is the
established practice in this file — `inventory/brand-tokens.json`'s `scope.exclude` lists this
script for precisely that reason. Do not write that literal into any other file this plan touches.
  </action>

  <verify>
    <automated>node scripts/scan-brand-residue.mjs --self-test && scripts/verify-platform.sh --only scan-brand-residue-self-test</automated>
  </verify>

  <acceptance_criteria>
    - `node scripts/scan-brand-residue.mjs --self-test` exits 0 and its output contains all three
      labels verbatim, each on a `--self-test PASS` line:
      `--extra-root over a clean scratch root is a PASS control`,
      `--extra-root names a brand token planted outside the git index`,
      `--extra-root on a nonexistent directory fails loudly rather than skipping`.
    - Every pre-existing self-test fixture row still prints its own PASS line. No prior row was
      removed, renamed, or weakened.
    - **Discrimination control (run explicitly and recorded verbatim in the SUMMARY).** Copy the
      pre-Task-1 `scripts/scan-brand-residue.mjs` from git to a scratch path, then run row 2's
      scenario by hand against it: `node <scratch>/pre-fix.mjs --extra-root <planted dir>` must exit
      **2** with an unknown-argument or missing-flag error — proving `--extra-root` did not exist
      and the tree could not have been checked. Then run the same scenario against the fixed script
      and confirm exit **1** naming the planted path. A row that behaves identically under both is
      not evidence.
    - Row 2's planted file lives under `tmpdir()`, not under the repo. Confirm with
      `git status --porcelain` producing no untracked entry for it after the self-test run.
    - `node scripts/scan-brand-residue.mjs` (the plain tracked-tree gate) exits 0 after this change
      with the file staged — the token literals added to the fixtures do not trip the gate, because
      `scripts/scan-brand-residue.mjs` is in `scope.exclude`.
    - `scripts/verify-platform.sh --quick` exits 0, and its summary table lists `scan-brand-residue`
      and `scan-brand-residue-self-test` as PASS.
    - `grep -rn 'scan-brand-residue' scripts/verify-platform.sh` still returns exactly the two
      pre-existing rows.
  </acceptance_criteria>

  <done>
    The `--extra-root` mode has been observed red on a token planted outside the git index and on a
    missing root, green on a clean control, and every row names the path it found. The rows run in
    `--quick` through the registry row that already exists, with no clone required.
  </done>

  <reversibility rating="reversible">Additional fixture rows inside an existing self-test function.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Record the CI-workflow decision, the rebase procedure's new failure point, and ledger row 9</name>

  <read_first>
    - `.github/workflows/rebase-upstream.yml` — the file being modified. Read the `Scan for residual
      brand strings` step and the comment block above it, which already explains why it runs BEFORE
      the rebase and needs no upstream tree, and the `Rebase onto requested tag` step that invokes
      `bash scripts/rebase-upstream.sh`. Those two facts together are the whole decision; the comment
      you add records them rather than introducing anything new.
    - `docs/BUILD.md` lines 464-495 — the `## Rebase procedure and desktop install (D-74, D-77)`
      section. Its "It fails loudly, by name, at any of:" list enumerates every failure point of
      `rebase-upstream.sh` and currently does not mention the residual-brand scan at all. Read the
      whole list before adding to it so the new clause matches its voice and its level of detail.
    - `.planning/phases/01-platform-extraction-and-rename/deferred-items.md` — the file being
      appended to. Read rows 4, 7 and 8 in full. Row 4 is the `upstream/`-absent precedent this
      plan's tier decision follows. Row 8 is the closed predecessor of this work and is the format
      model: it names what closed the item, names the check that asserts it, and carries an explicit
      **Deliberately NOT done** paragraph. Row 9 follows that shape.
    - `.planning/phases/01-platform-extraction-and-rename/01-VERIFICATION.md` — both `gaps` entries,
      so row 9 records what actually closed rather than a paraphrase.
    - `scripts/rebase-upstream.sh` and `scripts/scan-brand-residue.mjs` as edited by Tasks 1 and 2 —
      row 9 and the BUILD.md clause must describe what shipped, not what was planned.
    - `CLAUDE.md`'s residual-brand-scan section — `docs/BUILD.md` and the workflow YAML are both in
      the scan's scope, so no brand token may be spelled in either.
  </read_first>

  <files>.github/workflows/rebase-upstream.yml, docs/BUILD.md, .planning/phases/01-platform-extraction-and-rename/deferred-items.md</files>

  <action>
Three documentation changes, each closing a question this plan is required to decide in writing
rather than leave open.

**1. `.github/workflows/rebase-upstream.yml` — record the decision, change no behaviour.** Extend
the comment block above the `Scan for residual brand strings` step with two or three sentences
stating: that this step is deliberately the PRE-rebase, tracked-tree-only scan and deliberately does
NOT pass `--extra-root`, because at this point in the job no upstream tree has been cloned and
`--extra-root` on an absent directory is a hard failure by design; and that the post-replay,
upstream-aware scan reaches this workflow through the `Rebase onto requested tag` step below, which
runs `scripts/rebase-upstream.sh`, which passes `--extra-root "$UPSTREAM_DIR"` itself. Name plan
01-15 and CR-B so the provenance is traceable. Do not add a second scan step, do not change the
existing `run:` line, and do not spell any brand token in the comment.

**2. `docs/BUILD.md` — add the scan to the rebase procedure's fail-loudly list.** The
`## Rebase procedure and desktop install (D-74, D-77)` section's "It fails loudly, by name, at any
of:" enumeration currently omits the residual-brand scan entirely. Add one clause in that list's
existing voice covering both halves of what the scan now checks: the repo's own tracked tree, and —
via `--extra-root` — the rebased `upstream/` checkout that `.gitignore` keeps out of `git ls-files`,
which is the tree the replay just rewrote. Keep it to one clause; the list is a reference, not a
narrative. Do not spell any brand token — refer to it as the residual-brand scan, which is what the
rest of that document already does.

**3. `.planning/phases/01-platform-extraction-and-rename/deferred-items.md` — append row 9.** One
new table row, appended after row 8. Do not rewrite, renumber, or reword any existing row. Follow
row 8's shape:

  - **`#`** — `9`.
  - **Found in** — `01-14 / 01-15 (found by 01-REVIEW.md CR-01/CR-02, independently re-confirmed by
    01-VERIFICATION.md as CR-A/CR-B)`.
  - **Item** — state both gaps in one paragraph each, as they were: CR-A, that
    `verify-shell-error-copy.mjs`'s rule (4) admitted any identifier's `.message` including a caught
    exception's, so the gate could not fire on the shape it existed to catch even though the shipped
    tree was clean on its merits; and CR-B, that the rebase's post-replay scan read `git ls-files`
    only while `.gitignore` excludes `upstream/`, so the invocation whose purpose was re-checking the
    replayed tree scanned 109 tracked files and none of them under `upstream/`. Then a
    **Deliberately NOT done** paragraph naming, with reasons, each of: no `verify-platform.sh` row
    scans the live `upstream/`, because that is a 1.1 GB git-ignored clone absent on a fresh checkout
    and a skip-when-absent row is green by construction — precisely what row 4 above records this
    repo already hitting; no new registry rows were added at all, because the four existing rows
    (`scan-brand-residue`, `scan-brand-residue-self-test`, `shell-error-copy-no-internals`,
    `shell-error-copy-no-internals-self-test`) already invoke both checkers and their self-tests;
    `.github/workflows/rebase-upstream.yml` gained no functional change, because its step-1 scan is
    deliberately pre-rebase with no upstream tree present and the upstream-aware scan reaches CI via
    the `rebase-upstream.sh` step it already runs; and rule (4)'s `catch`-parameter derivation is
    file-scoped rather than block-scoped, an accepted ceiling that upgrades only if a real false
    positive appears.
  - **Route to** — the residual: a real ESR rebase run against a live `upstream/` checkout is the
    first time the `--extra-root` walk executes over a multi-gigabyte Gecko tree, and its wall-clock
    cost is unmeasured. State that it is expected to be acceptable next to the 1.1 GB clone and patch
    replay it follows, that no measurement exists yet, and that any content-based exclusion the real
    run turns out to need belongs in `inventory/brand-tokens.json`'s `scope.exclude` rather than in
    the walker.

The ledger row must not spell any brand token; `deferred-items.md` lives under `.planning/`, which
the inventory excludes, but the row is describing today's Power Browser tooling and naming the old
product there would be wrong on the merits.
  </action>

  <verify>
    <automated>node scripts/scan-brand-residue.mjs && grep -c -- '--extra-root' .github/workflows/rebase-upstream.yml && grep -c '^| 9 |' .planning/phases/01-platform-extraction-and-rename/deferred-items.md</automated>
  </verify>

  <acceptance_criteria>
    - `.github/workflows/rebase-upstream.yml`'s comment above the `Scan for residual brand strings`
      step names plan 01-15, names CR-B, states that the step deliberately does not pass
      `--extra-root` because no upstream tree exists at that point, and states that the upstream-aware
      scan arrives via the `rebase-upstream.sh` step.
    - `grep -c 'run: node scripts/scan-brand-residue.mjs' .github/workflows/rebase-upstream.yml`
      returns 1 — exactly one scan step, unchanged, no second invocation added.
    - `docs/BUILD.md`'s "It fails loudly, by name, at any of:" list contains a clause naming the
      residual-brand scan and covering both the tracked tree and the rebased upstream checkout.
    - `grep -c '^| 9 |' .planning/phases/01-platform-extraction-and-rename/deferred-items.md`
      returns 1.
    - `grep -c '^| [0-9]' .planning/phases/01-platform-extraction-and-rename/deferred-items.md`
      returns 9 — eight pre-existing rows plus the new one, none removed.
    - `git diff --stat .planning/phases/01-platform-extraction-and-rename/deferred-items.md` shows
      insertions only; no existing row was rewritten or renumbered.
    - Row 9's **Deliberately NOT done** paragraph names all four items listed in the action: the
      absent live-`upstream/` registry row, the zero new registry rows, the unchanged CI workflow,
      and the file-scoped catch-parameter derivation.
    - `node scripts/scan-brand-residue.mjs` exits 0 with all three changed files staged.
    - `scripts/verify-platform.sh --quick` exits 0.
  </acceptance_criteria>

  <done>
    The `.github/workflows/rebase-upstream.yml` question is settled in writing inside the workflow
    itself, `docs/BUILD.md` names the scan among the rebase's failure points for both trees, and
    ledger row 9 records both closures and every deliberate NOT-done with its reason.
  </done>

  <reversibility rating="reversible">Documentation and comments only; no executable behaviour changes in this task.</reversibility>
</task>

</tasks>

<artifacts_this_phase_produces>
Every symbol, flag, label and path this plan creates. `.planning/intel/API-SURFACE.md` currently
reports `symbolCount: 0` and `stale: true`, so nothing below may be assumed already catalogued
there.

**New exported function in `scripts/scan-brand-residue.mjs`:**
- `extraRootFiles(inv, root)` — walks a filesystem root outside the git index and returns sorted
  root-relative file paths under the inventory's `scope.exclude` / `binary_extensions` filters,
  never following a symlink and never descending `.git`. Throws an `Error` naming the path when the
  root is absent or is not a directory.
- One shared filter predicate extracted from the existing `scopeFiles` body and reused by both
  `scopeFiles` and `extraRootFiles`, so the extra root is not a second, weaker ruleset.

**New CLI flag:**
- `--extra-root <dir>` on `scripts/scan-brand-residue.mjs`. Additive: with no arguments the script
  behaves exactly as before.

**New `--self-test` fixture row labels (printed verbatim by `--self-test`):**
- `--extra-root over a clean scratch root is a PASS control`
- `--extra-root names a brand token planted outside the git index`
- `--extra-root on a nonexistent directory fails loudly rather than skipping`

**New `scripts/verify-platform.sh` registry rows:** none. The pre-existing rows `scan-brand-residue`
and `scan-brand-residue-self-test` carry this work; see `<planning_dispositions>` for why no row
scans the live `upstream/`.

**New invocation sites:**
- `scripts/rebase-upstream.sh` real post-replay invocation — `--extra-root "$UPSTREAM_DIR"`.
- `scripts/rebase-upstream.sh` `--dry-run` step-4b listing — the same argument.

**New file paths:** none. All five modified files already exist.

**New documentation anchors:**
- `.github/workflows/rebase-upstream.yml` — a comment recording the CR-B decision for step 1.
- `docs/BUILD.md` — a clause in the rebase procedure's fail-loudly list naming the residual-brand
  scan over both trees.
- `.planning/phases/01-platform-extraction-and-rename/deferred-items.md` — row 9.
</artifacts_this_phase_produces>

<threat_model>
OWASP ASVS level 1; blocking threshold `high`. Scope is one local static Node script, one local
shell wrapper, one CI workflow comment, and two documents. New in this plan relative to 01-14: the
script now reads a caller-supplied filesystem tree it does not own.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator CLI / `rebase-upstream.sh` → `--extra-root <dir>` | An arbitrary filesystem path becomes a recursive read root. This is the new boundary this plan introduces. |
| `upstream/` (unowned third-party Gecko checkout) → `scan-brand-residue.mjs` | Content nobody in this repo wrote is read and matched. `rebase-upstream.sh`'s own comment names this as the reason the gate runs at that call site. Read-only in both directions; `git -C upstream diff` staying empty is the invariant. |
| GitHub Actions runner → `rebase-upstream.sh` → the scan | The CI path runs the same script with the same argument on a hosted runner against a freshly cloned tree. |
| `inventory/brand-tokens.json` → both file-set paths | The single source of tokens, exclusions and probes. One inventory, two file sets. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-15-01 | Tampering | the post-replay gate in `rebase-upstream.sh` | high | mitigate | This plan's entire subject. A rebase that reintroduces a brand token into `upstream/` currently ships undetected because the gate cannot read that tree. Mitigated by `--extra-root "$UPSTREAM_DIR"`, by the hard failure on an absent or empty root, and by three self-test rows that prove the mode can go red. Rated high because CLAUDE.md states this guarantee as holding today and it does not. |
| T-01-15-02 | Tampering | `extraRootFiles` symlink handling | medium | mitigate | `upstream/powerbrowser` is a real symlink into this repo's `powerbrowser/`. A link-following walk would rescan this repo through a second path, could recurse, and could be induced by a malicious upstream tree to read outside the intended root. Mitigated by skipping every `dirent.isSymbolicLink()` entry, asserted by the symlink-containment acceptance criterion in Task 1. |
| T-01-15-03 | Denial of Service | recursive walk over a rebased Gecko tree | medium | mitigate | An unbounded walk over a multi-gigabyte checkout, or a symlink cycle, could hang the rebase. Mitigated by not following symlinks (removes the cycle class) and skipping `.git` (removes the object store). Residual wall-clock cost over a real tree is unmeasured and is carried as the `backstop` truth and ledger row 9's route-to. |
| T-01-15-04 | Information Disclosure | `--extra-root` pointed at an unintended tree | low | accept | Output is `path:line:token` for inventory-token matches only — never file contents. A developer pointing it at `$HOME` learns which of their own files contain a token they supplied. Local dev tool, no privilege boundary, no elevation. Accepted. |
| T-01-15-05 | Repudiation | the new self-test rows as evidence | medium | mitigate | A row that goes red for an unrelated reason looks like proof and is none. Mitigated by the clean-control row (a token-free scratch root must exit 0, so the red is attributable to the plant and not to the tracked-tree scan running alongside) and by Task 2's discrimination control against the pre-fix script, which must exit 2 on an unknown argument. |
| T-01-15-06 | Tampering | reuse of `reconcile()` / `groundTruth()` on the extra-root result | low | mitigate | Applying D-17's census arithmetic to a foreign checkout would make the pass permanently red for a reason unrelated to residual brand strings, and a permanently-red gate gets disabled. Mitigated by deliberately not calling them on the extra-root result, with the reason stated in a comment and asserted by a Task 1 acceptance criterion. |
| T-01-15-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package-manager install task exists in this plan. All five files are edited in place with the repo's existing Node and Bash. No `[ASSUMED]`/`[SUS]` package is introduced, so no legitimacy checkpoint is earned — and none may later be skipped on the basis of this row. |

`T-01-15-01` is rated `high`, which meets the configured `security_block_on = high` threshold; its
disposition is `mitigate` and the mitigation is this plan's Task 1, so nothing is left unmitigated
at or above the threshold.
</threat_model>

<verification>
Run from the repo root; `node`, `bash` and `scripts/verify-platform.sh` all work outside a Nix dev
shell. None of these require `upstream/` to exist.

1. `node scripts/scan-brand-residue.mjs` — exits 0, tracked-tree file count unchanged.
2. `node scripts/scan-brand-residue.mjs --self-test` — exits 0, all pre-existing rows plus the three
   new `--extra-root` rows PASS.
3. The five ad-hoc `--extra-root` scenarios from Task 1's acceptance criteria (empty root, planted
   token, clean file, missing path, missing flag value) — each produces the stated exit code and
   output.
4. The symlink-containment scenario from Task 1's acceptance criteria.
5. The discrimination control from Task 2's acceptance criteria — pre-fix script exits 2 on
   `--extra-root`, fixed script exits 1 naming the planted path.
6. `bash scripts/rebase-upstream.sh --tag FIREFOX_153_1_0esr_RELEASE --dry-run` — exits 0, step-4b
   names `--extra-root` and the upstream directory. The real path is not run: it removes and
   re-clones a 1.1 GB tree, and `rebase-upstream.sh`'s own header records that it is exercised
   locally via `--dry-run` only.
7. `scripts/verify-platform.sh --quick` — exits 0 with no regression in any other row.
8. `git -C upstream diff --stat` — empty if `upstream/` exists locally; if it does not, say so in
   the SUMMARY rather than reporting the check as passed.
</verification>

<success_criteria>
- CR-B's first `missing[]` predicate is satisfied: `--extra-root <dir>` walks a filesystem root
  outside the git index under the same `scope.exclude` / `binary_extensions` filters, and
  `rebase-upstream.sh` passes `$UPSTREAM_DIR` to the post-replay invocation.
- CR-B's second `missing[]` predicate is satisfied: a `--self-test` row plants a brand token in a
  scratch directory passed as `--extra-root` and requires the scan to name that path.
- The mode fails loudly on an absent root, on an empty file set, and on a missing flag value. It
  never skips.
- The clean control and the pre-fix discrimination control both hold, so the red is proven
  plant-caused and proven impossible before this plan.
- The tracked-tree path is behaviourally unchanged: same file count, same verdict.
- `scripts/verify-platform.sh --quick` exits 0. No new registry row, no sibling driver, no live-
  `upstream/` dependency in any registered check.
- The `.github/workflows/rebase-upstream.yml` question is decided in writing inside the workflow.
- `docs/BUILD.md` names the scan among the rebase procedure's failure points.
- Ledger row 9 records both CR-A and CR-B closure and every deliberate NOT-done with its reason,
  with rows 1-8 untouched.
</success_criteria>

<output>
Create `.planning/phases/01-platform-extraction-and-rename/01-15-SUMMARY.md` when done. Record
verbatim: the three new self-test row outputs, the five ad-hoc `--extra-root` scenario outputs, the
symlink-containment result, and both halves of the discrimination control. A SUMMARY that reports
only the green run reproduces exactly the failure mode CR-B named — a gate that has never been seen
red.
</output>
