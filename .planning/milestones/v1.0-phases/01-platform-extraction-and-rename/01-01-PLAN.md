---
phase: 01-platform-extraction-and-rename
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .mozconfig
  - .gitignore
  - flake.nix
  - flake.lock
  - toolchain-baseline.txt
  - LICENSE
  - .github/workflows/rebase-upstream.yml
  - patches/
  - sourcerer/
  - theia/
  - scripts/
  - docs/
  - inventory/brand-tokens.json
  - scripts/scan-brand-residue.mjs
  - scripts/rename-brand.mjs
  - .planning/phases/01-platform-extraction-and-rename/01-SCAN-RED-REPORT.md
autonomous: true
requirements: [MIG-01, MIG-02, MIG-03]

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The platform tree exists in this repo as a single import commit whose message names the exact sourcerer SHA bce68bb468e4dc160da8c9e238e030a400b806f8, and neither upstream/ nor any objdir was copied"
    - "inventory/brand-tokens.json classifies every brand occurrence in the migrating scope into exactly one of brand-identifier / brand-display / identity / frozen / coincidental, and no occurrence is unclassified"
    - "scripts/scan-brand-residue.mjs --reconcile exits non-zero on the pre-rename tree and its report names every occurrence by path:line:token"
    - "The reconciled red-scan report is committed in the phase directory as evidence, with observed counts equal to the inventory's expected counts and no occurrence found that the inventory does not account for"
    - "scripts/rename-brand.mjs refuses to run and names the offending rows while any inventoried occurrence is unclassified"
    - "The chrome://sourcerer/ coupled chain renames end-to-end through the script: the chrome package name, the six jar.mn lines, the components.conf esModule and constructor, the moz.build wiring, the patch-020 reference, and the check-internals-boundary.sh allowlist glob all move together in one pass"
    - "A token match adjacent to other token characters is not replaced: the rename script's --self-test proves a planted resourcerer fixture is left untouched while a planted sourcererPrivilegedJs fixture becomes powerbrowserPrivilegedJs"
    - "An empty input is never a silent pass: scan-brand-residue.mjs FAILS with a distinct empty-scan-set message when its scope resolves to zero files, and rename-brand.mjs FAILS when the inventory carries zero token rows"
    - "Both scripts read files as UTF-8 and match on JavaScript string code units with no Unicode normalization; binary paths are excluded from content scanning by an explicit extension list, and positions are reported as path:line, never as byte offsets"
    - "Replacement order is deterministic and stable: rows apply longest-token-first then UPPER_SNAKE, TitleCase, lowercase; rerunning rename-brand.mjs on an already-renamed tree produces a zero-byte diff, and scan report rows are sorted by path then line so two runs are byte-identical"
  artifacts:
    - path: "inventory/brand-tokens.json"
      provides: "The single machine-readable token-classification source of truth read by both the rename script and the residual scan"
      contains: "\"class\""
    - path: "scripts/scan-brand-residue.mjs"
      provides: "Static grep-class residual-brand scan with reconciliation, per-site reporting, non-vacuity assertion, and --self-test"
      contains: "--self-test"
    - path: "scripts/rename-brand.mjs"
      provides: "Rerunnable inventory-driven rename executor with per-(token,case,class) passes and gate-on-unclassified"
      contains: "--self-test"
    - path: ".planning/phases/01-platform-extraction-and-rename/01-SCAN-RED-REPORT.md"
      provides: "Committed reconciled evidence that the scan was demonstrably red before any replacement ran"
  key_links:
    - from: "scripts/scan-brand-residue.mjs"
      to: "inventory/brand-tokens.json"
      via: "reads the inventory as its only token source — no token literal is hardcoded in the scanner"
      pattern: "inventory/brand-tokens\\.json"
    - from: "scripts/rename-brand.mjs"
      to: "inventory/brand-tokens.json"
      via: "reads the same inventory as its only replacement plan — one source of truth, no drift between what is scanned and what is renamed"
      pattern: "inventory/brand-tokens\\.json"
  prohibitions:
    - statement: "MUST NOT import Sourcerer-branded git history, Sourcerer's logo/ assets, or docs/PRODUCT-REQUIREMENTS.md into this public platform repo, and MUST NOT retain sourcerer's git remote on any Power Browser commit"
      category: transparency
      status: resolved
      verification: judgment
    - statement: "MUST NOT rewrite, renumber, or delete the historical Sourcerer decision-ID and plan-file citations carried in migrated comments — they are provenance, not brand, and only brand tokens naming the product may change in a comment"
      category: transparency
      status: resolved
      verification: judgment
    - statement: "MUST NOT let a check pass by agreeing with a value the same rename pass rewrote — the residual scan and every branding verifier MUST derive their expected values from a source the rename script does not write"
      category: transparency
      status: resolved
      verification: judgment
---

## Phase Goal

**Goal (verbatim from ROADMAP.md):** The Power Browser platform tree exists in this repo, builds,
boots, and works as an actual web browser under fixed platform identifiers — with no generator
involved.

> ⚠ **MVP-mode note.** This Goal line is not in `As a / I want to / so that` user-story form. Per
> the MVP framing rule no story was invented here. Run `/gsd mvp-phase 1` to convert the ROADMAP
> Goal into a user story if the story framing is wanted; it is not a blocker for execution.

<objective>
Land the sourcerer platform tree in this repo as a fresh snapshot (D-01, D-03, D-04), build the
token-classification inventory (MIG-02, D-15), and prove the rename machinery works end-to-end on
one coupled chain before it is pointed at all 755 occurrences (D-05, D-16, D-17).

Purpose: two binding roadmap ordering constraints live here — the inventory precedes the rename,
and the residual scan is written and **proven demonstrably red** before any replacement runs. The
tracer exists because the rename architecture (inventory-driven per-class passes, coupled-format
grouping, boundary matching, reconciliation) is the thing 755 occurrences depend on; proving it on
six coupled sites costs one commit, discovering it is wrong after the full rename costs the phase.

Output: the imported tree, `inventory/brand-tokens.json`, `scripts/scan-brand-residue.mjs`,
`scripts/rename-brand.mjs`, and a committed reconciled red-scan report.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-platform-extraction-and-rename/01-CONTEXT.md
@.planning/phases/01-platform-extraction-and-rename/01-RESEARCH.md
@.planning/phases/01-platform-extraction-and-rename/01-PATTERNS.md
@.planning/phases/01-platform-extraction-and-rename/SKELETON.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Import the platform tree and prove the rename machinery on the chrome:// coupled chain</name>
  <precondition>/home/chris/coding/sourcerer exists, is a git repo whose HEAD is bce68bb468e4dc160da8c9e238e030a400b806f8, and `git -C /home/chris/coding/sourcerer status --porcelain` prints nothing</precondition>
  <reversibility rating="one-way">D-01 chose a fresh snapshot import over a history import; once later phases build on the snapshot, retrofitting filter-repo history rewrites every subsequent commit. The developer already chose this in CONTEXT.md, so no checkpoint re-asks it.</reversibility>
  <files>sourcerer/, theia/, scripts/, docs/, patches/, .mozconfig, .gitignore, flake.nix, flake.lock, toolchain-baseline.txt, LICENSE, .github/workflows/rebase-upstream.yml, inventory/brand-tokens.json, scripts/scan-brand-residue.mjs, scripts/rename-brand.mjs</files>

  <read_first>
    - /home/chris/coding/sourcerer/.gitignore — confirms objdir/, objdir-release/, .mozbuild/, upstream/, node_modules/, theia/**/lib/, .theia/, plugins/, .env* are already ignored
    - /home/chris/coding/sourcerer/sourcerer/endpoint-allowlist.json — the committed-data-file analog: `$comment` header naming consumers and the fail-closed rule, and a per-row `reason` citing file:line evidence
    - /home/chris/coding/sourcerer/scripts/check-internals-boundary.sh — the named in-tree scan idiom being ported: non-vacuity assertion on an empty scan set, one `path:line:token` row per offense, `--self-test` planting a fixture in `mktemp -d` and asserting the guard both rejects it AND names it, argument dispatch at the bottom
    - /home/chris/coding/sourcerer/scripts/verify-branding.mjs (lines 1-35) — the Node ESM CLI header convention: document why the naive approach was rejected and name the pinning risk
    - /home/chris/coding/sourcerer/sourcerer/shell/jar.mn — the six chrome-package registration lines of the tracer chain
    - /home/chris/coding/sourcerer/sourcerer/shell/components.conf — four token classes in nine lines (frozen cid, identity contract_ids, brand esModule + constructor, frozen `a-` prefix + brand suffix)
    - /home/chris/coding/sourcerer/sourcerer/shell/moz.build — JAR_MANIFESTS, the DEFINES entry consumed by `#filter substitution`, JS_PREFERENCE_PP_FILES, XPCOM_MANIFESTS
    - /home/chris/coding/sourcerer/patches/020-sourcerer-shell.patch — read in full (29 lines); it carries the sixth coupled reference to the chrome package
    - .planning/phases/01-platform-extraction-and-rename/01-RESEARCH.md — sections "Token Inventory Ground Truth", "Coupled Reference Formats", "Common Pitfalls" 1/2/3/4/7/8
    - .planning/phases/01-platform-extraction-and-rename/01-PATTERNS.md — sections for `inventory/brand-tokens.json`, `scripts/scan-brand-residue.mjs`, `scripts/rename-brand.mjs`
  </read_first>

  <action>
Three moves, in this order, each its own commit.

**(a) Snapshot import (D-01, D-03, D-04).** Materialise the sourcerer working tree at
`bce68bb468e4dc160da8c9e238e030a400b806f8` into this repo using `git -C /home/chris/coding/sourcerer archive <sha>` piped into `tar -x` here — an archive of the tracked tree only, so no ignored path can ride along. Then delete, before staging: `docs/research/`, `docs/PRODUCT-REQUIREMENTS.md`, `logo/`, `CLAUDE.md`, `.claude/`, and `.planning/` (this repo has its own). Everything else migrates, including `flake.nix`, `flake.lock`, `toolchain-baseline.txt`, `docs/BUILD.md`, `docs/CUSTOMIZE.md`, `docs/URI-SCHEMES.md`, `LICENSE`, `.github/workflows/rebase-upstream.yml`, all four `theia/extensions/*`, and all of `scripts/` including `verify-phase-02.sh` through `verify-phase-05.sh` (they are consolidated later under D-21, so their assertions must be present now). Confirm `upstream/`, `objdir/`, `objdir-release/`, `.mozbuild/`, and every `node_modules/` are absent from the working tree, not merely ignored. Verify `git remote -v` names no sourcerer remote. Commit as a single commit whose subject is `chore: import platform tree from sourcerer@bce68bb468e4dc160da8c9e238e030a400b806f8` and whose body records the provenance, the D-03 exclusions, and that no history was imported.

**(b) Seed `inventory/brand-tokens.json` with the tracer chain only.** JSON, not TOML — Node reads it with zero dependencies and both consumers are Node. Top level: a `$comment` string stating that this file is the single machine-readable source of truth for the rename, naming its two consumers (`scripts/rename-brand.mjs` and `scripts/scan-brand-residue.mjs`) and the fail-closed rule that an occurrence absent from this file is a FAILURE, never silently ignored; a `scope` object with `include` describing the `git ls-files` derivation and an `exclude` array of path prefixes; and a `tokens` array. Each token row carries: `token`, `case_form` (one of `lower`, `title`, `upper`), `class` (one of `brand-identifier`, `brand-display`, `identity`, `frozen`, `coincidental`), `target`, `expected_count`, `expected_files` (array of repo-relative paths), `couples_with` (array of paths whose sites must move in the same pass), `reason` (a sentence citing file:line evidence, mandatory on every `frozen` and `coincidental` row), and an optional `site_overrides` array of `{file, line, class, target, reason}` for the ambiguous TitleCase form. Seed it with exactly the sites of the `chrome://sourcerer/` chain: the chrome package name in `sourcerer/shell/jar.mn`, its six registration lines, the `esModule` and `constructor` keys in `sourcerer/shell/components.conf`, the `moz.build` wiring, the reference inside `patches/020-sourcerer-shell.patch`, and the allowlist glob in `scripts/check-internals-boundary.sh`. Also seed the four frozen rows this chain proves the classifier can hold back: the `cid` UUID, the `a-` category prefix, and the two `%content/branding/` and `%locale/branding/` slot names in the branding `jar.mn` files (Gecko's own slot, never renamed).

**(c) Write both scripts and run the chain red→green.** `scripts/scan-brand-residue.mjs` ports the `check-internals-boundary.sh` idiom into Node: scope from `execFileSync('git', ['ls-files'])` minus the inventory's `exclude` prefixes minus an explicit binary-extension list (`.png`, `.ico`, `.icns`, `.jpg`, `.gif`, `.woff`, `.woff2`, `.zip`); files read as UTF-8 with no Unicode normalization; matching at token boundary per case form, where "boundary" means the adjacent characters are not `[A-Za-z0-9_]` on either side after accounting for the camelCase and UPPER_SNAKE joins the inventory declares; one report row per offense as `path:line:token`; rows sorted by `path` then `line`; a distinct FAIL when the scanned file set is empty; a `--reconcile` mode asserting all four reconciliation conditions (every inventoried token with a nonzero `expected_count` was found; observed count equals expected count; every observed file appears in that token's `expected_files`; no occurrence was found that the inventory does not account for); a `--report <path>` mode writing the markdown evidence file; and a `--self-test` planting fixtures in `mktemp -d` and asserting the scanner both rejects them and names their paths. `scripts/rename-brand.mjs` reads the same inventory and applies one pass per `(token, case_form, class)` tuple in a deterministic order — longest `token` first, then `case_form` upper, title, lower — honouring `site_overrides` per file:line, skipping every `frozen` and `coincidental` row, and skipping the hard-excluded hand-write set (`sourcerer/sourcerer.desktop`, `sourcerer/sourcerer-release.desktop`, both `brand.ftl`, both `brand.properties`, both `configure.sh`, and the `applicationName` key in `theia/applications/browser/package.json`). It refuses to run, naming the offending rows, while any inventoried occurrence has no `class`. It carries a `--self-test` that plants a `resourcerer` fixture and a `sourcererPrivilegedJs` fixture and asserts the first is untouched and the second becomes `powerbrowserPrivilegedJs`. Both scripts use `REPO_ROOT` derived from the script's own location, matching the in-tree convention. Then: run the reconcile scan and confirm it is red on the chain; run the rename; confirm the scan is green on the chain; confirm all six coupled sites moved together; run the rename a second time and confirm a zero-byte diff.

Do not restructure any migrated file while doing this — a diff that both moves and reshapes is unreviewable.
  </action>

  <verify>
    <automated>test -f flake.lock &amp;&amp; test -f .mozconfig &amp;&amp; test ! -e upstream &amp;&amp; test ! -e objdir &amp;&amp; test ! -e .mozbuild &amp;&amp; git log -1 --format=%s | grep -q 'bce68bb468e4dc160da8c9e238e030a400b806f8'</automated>
    <automated>node scripts/scan-brand-residue.mjs --self-test &amp;&amp; node scripts/rename-brand.mjs --self-test</automated>
    <automated>node scripts/scan-brand-residue.mjs --scope-chain chrome-package; test $? -eq 0</automated>
    <automated>node scripts/rename-brand.mjs --scope-chain chrome-package &amp;&amp; git diff --quiet</automated>
  </verify>

  <acceptance_criteria>
    - `git log -1 --format=%B` on the import commit contains the literal `bce68bb468e4dc160da8c9e238e030a400b806f8`
    - `git ls-files | wc -l` reports at least 110 tracked files, and `git ls-files` lists no path under `docs/research/`, `logo/`, or `.claude/`, and no `docs/PRODUCT-REQUIREMENTS.md`
    - `test ! -e upstream && test ! -e objdir && test ! -e objdir-release && test ! -e .mozbuild` exits 0
    - `git remote -v` output contains no `Deocracy` and no `Sourcerer`
    - `inventory/brand-tokens.json` parses as JSON, its `$comment` names both `scripts/rename-brand.mjs` and `scripts/scan-brand-residue.mjs`, and every row in `tokens[]` has a non-empty `class` drawn from the five-value set
    - Every row whose `class` is `frozen` or `coincidental` has a non-empty `reason`
    - `node scripts/scan-brand-residue.mjs --self-test` exits 0 and its output states the planted violation was rejected AND names the planted path
    - `node scripts/rename-brand.mjs --self-test` exits 0 and its output states the `resourcerer` fixture was left untouched and the camelCase fixture was renamed
    - Before the rename pass, `node scripts/scan-brand-residue.mjs --scope-chain chrome-package --reconcile` exits non-zero and prints one `path:line:token` row for each of the six coupled sites
    - After the rename pass, the same command exits 0
    - `grep -c 'content/powerbrowser/' powerbrowser/shell/jar.mn` reports 5, and `grep -q 'PowerBrowserAPI.sys.mjs' powerbrowser/shell/jar.mn` exits 0
    - `grep -q 'chrome://powerbrowser/content/PowerBrowserAPI.sys.mjs' powerbrowser/shell/components.conf` exits 0
    - Running `node scripts/rename-brand.mjs --scope-chain chrome-package` a second time leaves `git diff` empty
  </acceptance_criteria>

  <done>The platform tree is in this repo under one provenance-recording commit with no copied upstream or objdir; the inventory, the residual scan, and the rename script exist with self-tests; and the `chrome://` coupled chain has gone red → renamed → green through the script with all six coupled formats moving together and a rerun producing no diff.</done>
</task>

<task type="auto">
  <name>Task 2: Classify all 755 occurrences and commit the reconciled red-scan evidence</name>
  <files>inventory/brand-tokens.json, .planning/phases/01-platform-extraction-and-rename/01-SCAN-RED-REPORT.md</files>

  <read_first>
    - inventory/brand-tokens.json — the seeded chain rows written in Task 1; extend, do not restructure
    - .planning/phases/01-platform-extraction-and-rename/01-RESEARCH.md — "Token Inventory Ground Truth" (the per-bucket occurrence table, the five-replacement-targets table, the complete `SOURCERER_*` constant census with its cross-tier column, and the file/directory rename list), plus "Coupled Reference Formats" §1-§6 and Pitfalls 1, 2, 4, 8
    - /home/chris/coding/sourcerer/sourcerer/branding/dev/locales/en-US/brand.ftl and .../release/locales/en-US/brand.ftl — the display-literal sites that must be classified `brand-display` so the script never touches them
    - /home/chris/coding/sourcerer/sourcerer/sourcerer.desktop — the three-tokens-on-one-line hazard driving the `coincidental` repo-root row
    - /home/chris/coding/sourcerer/scripts/verify-branding-identity.mjs — line 347's regex embedding two brand tokens, and line 338's hardcoded vendor expectation
    - /home/chris/coding/sourcerer/scripts/verify-dev-flag-off.mjs — line 68's string containing a symbol key
    - /home/chris/coding/sourcerer/scripts/check-internals-boundary.sh — lines 31, 119, 150, 151, 172: the chrome-package glob and the hardcoded basename and catalogue path
    - /home/chris/coding/sourcerer/scripts/apply-patches.sh — `run_self_test()` hardcodes a patch filename
  </read_first>

  <action>
Extend `inventory/brand-tokens.json` from the tracer chain to the complete migrating scope. Seed the
token set explicitly — it is a set, not a search for one string — with `sourcerer`, `Sourcerer`,
`SOURCERER`, `Deocracy`, `deocracy`, `sourcerer.dev`, and the repo-root path
`/home/chris/coding/sourcerer`. Scanning for three case forms of one word misses the vendor entirely.

Targets, per D-09 / D-10 / D-12: `sourcerer` → `powerbrowser`; `Sourcerer` as an identifier prefix →
`PowerBrowser`; `Sourcerer` as a display literal → `Power Browser` (two words, with the space);
`SOURCERER` → `POWERBROWSER`; `Deocracy` → `DeBIOS` in the machine/path-forming sites and
`DeBIOS Foundation` in the display sites; `deocracy` → `debios` (so `--with-distribution-id` becomes
`org.debios`); `sourcerer.dev` → `powerbrowser.org` (the foundation does not own a `.dev` domain, so
the XPCOM contract id target is `@powerbrowser.org/single-instance-clh;1`); and the repo-root path →
`/home/chris/coding/Power-Browser`, classified `coincidental` so the scan reports it and the script
leaves it alone.

The TitleCase form is the phase's central hazard: 291 occurrences, lexically identical, roughly forty
of them display strings. Classify TitleCase **per site**, not per token. Every one of the six display
surfaces — the four terms in each `brand.ftl`, the three terms in each `brand.properties`, the
`MOZ_APP_DISPLAYNAME` assignment in each `configure.sh`, `Name=` in each `.desktop`, and
`applicationName` in `theia/applications/browser/package.json` — gets a `brand-display` row carrying
its exact hand-written target, so the rename script is forbidden from touching it and the hand-write
task in plan 01-03 owns it instead.

Frozen rows, each with its `reason` (D-08): the historical Sourcerer decision-ID citations and
plan-file references in migrated comments, `-brand-product-name = Firefox` and its D-78 rationale
comment, `MOZ_APP_UA_NAME` and `MOZ_APP_ID`, the XPCOM `cid` UUID, the `a-` category prefix (suffix is
brand, prefix is not), and the `%content/branding/` and `%locale/branding/` slot names in the branding
`jar.mn` files. Every Gecko pref key under `branding/*/pref/firefox-branding.js` is frozen — only
comment text naming the product renames.

Fill `couples_with` for every coupled-format row so the script groups them into one pass. The known
set is: the six `jar.mn` registration lines with the files they name and every `chrome://` string; the
`components.conf` `esModule` and `constructor` keys with `jar.mn` and the exported class name; the
`moz.build` DEFINES entry with the `@...@` substitution token in the preprocessed pref file; the
`verify-branding-identity.mjs` regex with both of its emitters in the shell; the
`verify-dev-flag-off.mjs` symbol-key string with the privileged-JS module; the three
`check-internals-boundary.sh` hardcoded references; and the `apply-patches.sh` self-test's patch
filename.

Every cross-tier constant in the `SOURCERER_*` census — the ones whose chrome side and Node side must
land in one commit — gets a `stage` field naming the contract group it belongs to, so plan 01-02 can
stage by contract instead of by tier. At minimum: the backend-ready stdout sentinel, the auth token
env var, the supervised env var, the app-identity sentinel and its verifier regex, the token-disable
test bypass, and the token cookie name.

Then run `node scripts/scan-brand-residue.mjs --reconcile --report .planning/phases/01-platform-extraction-and-rename/01-SCAN-RED-REPORT.md` over the whole tree and iterate the
inventory until all four reconciliation conditions hold and the totals match research's measured
ground truth of 755 occurrences across 69 migrating files in the three raw case forms plus the vendor
token. A nonzero exit alone is not evidence — an under-scanning scanner also exits nonzero. Commit
the report as the phase's evidence that the scan was demonstrably red before any replacement ran.
  </action>

  <verify>
    <automated>node -e 'const i=require("./inventory/brand-tokens.json");const bad=i.tokens.filter(t=>!["brand-identifier","brand-display","identity","frozen","coincidental"].includes(t.class));if(bad.length){console.error(bad);process.exit(1)}if(i.tokens.length===0)process.exit(1);console.log(i.tokens.length+" classified rows")'</automated>
    <automated>node scripts/scan-brand-residue.mjs --reconcile --report /tmp/pb-red.md; test $? -ne 0 &amp;&amp; grep -qE '^[^:]+:[0-9]+:' /tmp/pb-red.md</automated>
    <automated>test -s .planning/phases/01-platform-extraction-and-rename/01-SCAN-RED-REPORT.md</automated>
  </verify>

  <acceptance_criteria>
    - Every row in `inventory/brand-tokens.json` `tokens[]` has a `class` from the five-value set, a `target`, an `expected_count`, and a non-empty `expected_files` array
    - The inventory contains at least one row for each of `sourcerer`, `Sourcerer`, `SOURCERER`, `Deocracy`, `deocracy`, `sourcerer.dev`, and `/home/chris/coding/sourcerer`
    - The row for `/home/chris/coding/sourcerer` has `class` `coincidental` and `target` `/home/chris/coding/Power-Browser`
    - At least twelve rows have `class` `brand-display`, covering both `brand.ftl` files, both `brand.properties` files, both `configure.sh` files, both `.desktop` files, and `theia/applications/browser/package.json`
    - The `Deocracy` rows distinguish two targets: `DeBIOS` for the machine/path-forming sites and `DeBIOS Foundation` for the display sites
    - At least one row for each of the six coupled reference formats has a non-empty `couples_with` array
    - Every row whose `class` is `frozen` has a non-empty `reason`
    - `node scripts/scan-brand-residue.mjs --reconcile` on the pre-rename tree exits non-zero, and its report asserts all four reconciliation conditions by name
    - The report's summed observed occurrences for `sourcerer`, `Sourcerer`, and `SOURCERER` across the migrating scope total 755, matching research's measured ground truth; any deviation is reconciled in the inventory rather than absorbed
    - `.planning/phases/01-platform-extraction-and-rename/01-SCAN-RED-REPORT.md` is committed and contains at least one `path:line:token` row per inventoried brand or identity token
  </acceptance_criteria>

  <done>The inventory classifies every brand occurrence in the migrating scope with no unclassified rows, the reconciled scan is demonstrably red with all four reconciliation conditions asserted, and the report is committed in the phase directory as evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external tree → this repo | The sourcerer working tree is read-only input crossing into a repo that will be published. Anything that rides along (history, secrets, downstream-only assets) crosses with it. |
| inventory data → rename executor | `inventory/brand-tokens.json` is executable intent: whatever it says gets written across 69 files. A wrong or incomplete row is a silent mass edit. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Information Disclosure | snapshot import | high | mitigate | Import via `git archive <sha>` of the tracked tree only, so no ignored path, no `.env`, no `upstream/.git`, and no history can ride along. Assert `git remote -v` names no sourcerer remote before the first push. |
| T-01-02 | Tampering | `scripts/rename-brand.mjs` | high | mitigate | Refuse to run while any inventoried occurrence is unclassified; skip every `frozen` and `coincidental` row; hard-exclude the hand-write set; require a zero-byte diff on rerun so a second pass cannot compound a wrong replacement. |
| T-01-03 | Repudiation | provenance | medium | mitigate | The import commit message carries the exact sourcerer SHA; it is the only link back once history is not imported. The reconciled red-scan report is committed as dated evidence. |
| T-01-04 | Tampering | `scripts/scan-brand-residue.mjs` | high | mitigate | Non-vacuity assertion — an empty scanned file set FAILS with its own distinct message. Reconciliation condition 4 (no occurrence found that the inventory does not account for) is what catches an under-scanning scanner; a nonzero exit alone is explicitly insufficient. |
| T-01-05 | Spoofing | contract-id namespace | medium | mitigate | The XPCOM contract id is namespaced by a DNS name. Target `@powerbrowser.org/single-instance-clh;1` (a domain the foundation establishes per D-12), never a `.dev` domain nobody owns. Classified `identity`, not `brand-identifier`. |
| T-01-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package-manager install occurs in this plan. The one net-new dependency in this phase (`@theia/mini-browser`, verdict `SUS`) is gated by a blocking human checkpoint in plan 01-06 before its install. |
</threat_model>

## Flagged Assumptions (unresolved edges — surfaced, not dropped)

The deterministic edge probe returned `category: "unclassified"` for these rows. They are carried
forward as explicit assumptions rather than auto-resolved with a backstop:

| Requirement | Probe | Planner assumption |
|---|---|---|
| MIG-01 | unclassified — review manually | Assumed: "migrated with `upstream/` re-fetched via script and no objdirs copied" is fully covered by the artifact assertions above (`test ! -e upstream`, `test ! -e objdir`) plus plan 01-04's live re-fetch. No edge category applies to a one-shot import. If a reviewer sees an uncovered edge here, it is unplanned. |
| MIG-02 | unclassified — review manually | Assumed: "a committed inventory exists before any rename is executed" is an ordering property, not a data-shape edge, and is enforced by the wave graph (this plan is wave 1; the rename is wave 2) plus the rename script's gate-on-unclassified. |

## Artifacts this phase produces

Symbols and paths **created** by this plan (new to the repo; the source-grounding pass should not
flag them as drift):

- `inventory/brand-tokens.json` — new committed data file. Keys: `$comment`, `scope.include`, `scope.exclude[]`, `tokens[]`; per-row keys `token`, `case_form`, `class`, `target`, `expected_count`, `expected_files[]`, `couples_with[]`, `reason`, `stage`, `site_overrides[]` with `{file,line,class,target,reason}`
- Inventory `class` enum values: `brand-identifier`, `brand-display`, `identity`, `frozen`, `coincidental`
- Inventory `case_form` enum values: `lower`, `title`, `upper`
- `scripts/scan-brand-residue.mjs` — new Node ESM CLI. Flags: `--reconcile`, `--report <path>`, `--scope-chain <name>`, `--self-test`
- `scripts/rename-brand.mjs` — new Node ESM CLI. Flags: `--scope-chain <name>`, `--self-test`
- `.planning/phases/01-platform-extraction-and-rename/01-SCAN-RED-REPORT.md` — new committed evidence artifact
- The entire migrated platform tree, first appearing in this repo at this plan: `sourcerer/` (renamed to `powerbrowser/` in plan 01-02), `theia/`, `scripts/`, `patches/`, `docs/`, `.mozconfig`, `flake.nix`, `flake.lock`, `toolchain-baseline.txt`, `LICENSE`, `.github/workflows/rebase-upstream.yml`

<verification>
- `node scripts/scan-brand-residue.mjs --self-test` and `node scripts/rename-brand.mjs --self-test` both exit 0 and both name their planted fixtures
- `node scripts/scan-brand-residue.mjs --reconcile` exits non-zero on the pre-rename tree with all four reconciliation conditions asserted by name
- `test ! -e upstream && test ! -e objdir && test ! -e objdir-release && test ! -e .mozbuild`
- `git log --oneline` shows the import as one commit, and its message contains the sourcerer SHA
</verification>

<success_criteria>
- The platform tree is present in this repo with `upstream/` and every objdir absent, imported under a single provenance-recording commit (MIG-01)
- `inventory/brand-tokens.json` exists, is committed, and classifies every brand occurrence in the migrating scope with zero unclassified rows (MIG-02)
- The residual scan exists, is committed, and is demonstrably red by reconciled counts with the report committed as evidence (MIG-02, roadmap ordering constraint)
- The rename script exists, refuses to run on an unclassified inventory, and has proven the full coupled-format machinery on the `chrome://` chain (MIG-03)
</success_criteria>

<output>
Create `.planning/phases/01-platform-extraction-and-rename/01-01-SUMMARY.md` when done
</output>
