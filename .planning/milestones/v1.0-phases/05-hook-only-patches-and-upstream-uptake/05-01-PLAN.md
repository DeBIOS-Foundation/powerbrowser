---
phase: 05-hook-only-patches-and-upstream-uptake
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - patches/010-powerbrowser-identity.patch
  - scripts/generate.mjs
  - scripts/lib/config-schema.json
  - scripts/check-patch-surface.sh
  - scripts/verify-platform.sh
autonomous: true
requirements:
  - MIG-05
must_haves:
  truths:
    - "No patches/*.patch added (+) line carries a downstream-varying brand or config value; every value lives under generated/"
    - "Both patches still apply non-vacuously with the 3-way blob-hash chain intact"
    - "check-patch-surface.sh rejects a planted brand value in a patch and passes the shipped stack"
  artifacts:
    - path: "patches/010-powerbrowser-identity.patch"
      provides: "Hook-only identity patch (include hook, no value literals)"
    - path: "scripts/check-patch-surface.sh"
      provides: "Patch-surface guard extended with brand-value mode plus --self-test"
    - path: "scripts/generate.mjs"
      provides: "identity.configure emitter carrying the relocated fixed fragment lines"
  key_links:
    - from: "patches/010-powerbrowser-identity.patch"
      to: "generated/identity.configure"
      via: "include hook resolves through upstream/identity.configure symlink created by fetch-upstream.sh ensure_branding_overlay"
      pattern: "include\(.*identity\.configure"
    - from: "scripts/generate.mjs"
      to: "generated/identity.configure"
      via: "emitter writes the fragment from fixed platform content"
      pattern: "identity\.configure"
---

<objective>
Strip every remaining value literal out of the patch stack so each patch is hooks-only, and give the patch-surface guard a brand-value mode that can go red.

Purpose: MIG-05 — the stack must carry no brand values before pins can be single-sourced (plans 05-02/05-03 build on the hook-only form).
Output: Regenerated hook-only 010 patch, extended emitter, extended guard with self-test, new registry rows.
</objective>

<execution_context>
<purpose>Execute this PLAN.md exactly as written and produce its SUMMARY.md.</purpose>
<required_reading>
Read .planning/STATE.md before any operation to load project context.
Read .planning/config.json for planning behavior settings.
</required_reading>
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/05-hook-only-patches-and-upstream-uptake/05-CONTEXT.md
@scripts/apply-patches.sh
@scripts/fetch-upstream.sh
@scripts/check-patch-surface.sh
</context>

<tasks>

<task type="auto">
  <name>Audit the stack and relocate 010's value literals into generated/</name>
  <files>patches/010-powerbrowser-identity.patch, scripts/generate.mjs, scripts/lib/config-schema.json</files>
  <action>Read both patches and classify every added (+) line as hook (include/DIRS) versus value literal. Known value literals in 010: the two hardcoded imply_option False lines for MOZ_SERVICES_HEALTHREPORT and MOZ_NORMANDY, and the MOZ_APP_ID GUID line. 020 is already hook-only per its own header comment — prove that by audit, do not regenerate it. Move every value literal into the generated/identity.configure emitter in scripts/generate.mjs as FIXED platform content (not new manifest keys: the manifest covers identity, branding, telemetry, extensions, URLs, pins per REQUIREMENTS.md Out of Scope, and these two build flags plus the frozen app ID are platform policy, not downstream-varying brand). Record that fixed-vs-configured ruling in the emitter comment and in the SUMMARY. Then REGENERATE 010 from a patched tree per the hard rule (edit the upstream working-tree file, re-derive with git diff so index blob hashes are recomputed). NEVER hand-edit a patch hunk body and NEVER hand-edit anything under upstream/ as a source change — the upstream tree is touched only transiently as the regeneration fixture and must be restored to the fully-applied state afterwards (verify with fetch-upstream.sh classifier). Stage any new file before trusting scan results (git ls-files invisibility trap).</action>
  <verify><automated>bash scripts/apply-patches.sh --self-test && git -C upstream diff --stat | grep -q . || bash scripts/fetch-upstream.sh</automated></verify>
  <done>010 contains only the include hook plus comments; identity.configure emitter carries the relocated lines; upstream/ tree is back in fully-applied state</done>
</task>

<task type="auto">
  <name>Give check-patch-surface.sh a brand-value mode with self-test</name>
  <files>scripts/check-patch-surface.sh</files>
  <action>Extend scripts/check-patch-surface.sh (add-alongside the compiled-suffix scan, same script, one registry) with a brand-value scan: derive the downstream-varying value set from configuration.toml at check time via scripts/lib/toml.cjs (required [identity]/[product]/[legal] display strings — vendor display, display name, binary/basename strings), boundary-match them against added (+) lines of patches/*.patch only (never context lines, never the +++ header paths; fixed identifiers such as the powerbrowser tree name and the frozen app GUID are NOT in the set). Add --self-test rows: a planted patch carrying the display name in a +line goes red naming the patch and value; the shipped hook-only stack goes green (clean control proving the red is plant-caused). Follow the file's existing self-test style (mktemp fixture, never the real patches/).</action>
  <verify><automated>bash scripts/check-patch-surface.sh && bash scripts/check-patch-surface.sh --self-test</automated></verify>
  <done>Brand-value mode rejects planted offender by name, passes shipped stack, self-test green</done>
</task>

<task type="auto">
  <name>Register rows and run the static gate set</name>
  <files>scripts/verify-platform.sh</files>
  <action>Append registry rows for the new brand-value mode and its self-test next to the existing check-patch-surface rows (same comment style, no sibling driver). Then run the full static set that proves MIG-05 without network or build: check-patch-surface plus self-test, apply-patches --self-test (needs upstream/ present — it is; if absent, record as open verification, do NOT clone as part of this task), fetch-upstream --self-test, scan-brand-residue, generate --check, generated-byte-identity. Before running, read scripts/verify-generated-identity.mjs's target list: if identity.configure is not among its comparands, extend that coverage in the same pass so the relocated lines are byte-identity gated, with a planted-fault self-test row.</action>
  <verify><automated>scripts/verify-platform.sh --only check-patch-surface && scripts/verify-platform.sh --only check-patch-surface-self-test && scripts/verify-platform.sh --only generated-byte-identity</automated></verify>
  <done>New rows registered and green; non-vacuity assertion still fires (proven by apply self-test); byte-identity covers the relocated fragment</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| patch-stack → upstream tree | Regenerated patch content crosses into the Gecko build |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Tampering | patches/010 regeneration | high | mitigate | Regenerate from patched tree (blob hashes recomputed); non-vacuity assertion in apply-patches.sh rejects silent no-op by patch name |
| T-05-02 | Tampering | check-patch-surface brand mode | medium | mitigate | Expectations derived from configuration.toml at check time via vendored parser; planted-fault self-test proves it goes red |
</threat_model>

<verification>
Static only — no fetch, no clone, no build in this plan:
1. `bash scripts/check-patch-surface.sh && bash scripts/check-patch-surface.sh --self-test` exits 0
2. `bash scripts/apply-patches.sh --self-test` exits 0 (proves non-vacuity assertion still fires)
3. Added-line review via `grep -E '^[+]' patches/*.patch` shows no added value lines outside hook syntax (manual grep-proof, recorded in SUMMARY)
4. `scripts/verify-platform.sh --quick` exits 0
</verification>

<success_criteria>
- 010 is include-hook plus comments only; 020 proven hook-only by audit
- Brand-value mode with green self-test registered in verify-platform.sh
- MIG-05 statically proven; live rebase proof belongs to plan 05-04
</success_criteria>

<output>
Create `.planning/phases/05-hook-only-patches-and-upstream-uptake/05-01-SUMMARY.md` when done
</output>
