---
phase: 07-sourcerer-as-downstream
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified: [.planning/phases/07-sourcerer-as-downstream/fixtures/sourcerer-equivalent/configuration.toml, .planning/phases/07-sourcerer-as-downstream/fixtures/sourcerer-equivalent/brand/mark.svg, scripts/verify-downstream-fixture.mjs]
autonomous: true
requirements: [CFG-05, VER-03, DOC-02]
user_setup: []

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A synthetic Sourcerer-equivalent external config (fully separate branding, own pins) generates a fully branded tree with zero platform-repo edits"
    - "The generated tree carries the fixture's brand on every emitted surface and none of the platform's required-slot values except the fixed forever-identifiers"
    - "Power Browser still generates and verifies identically with the downstream absent"
  artifacts:
    - path: ".planning/phases/07-sourcerer-as-downstream/fixtures/sourcerer-equivalent/configuration.toml"
      provides: "Synthetic downstream manifest with every required key distinct from the platform's"
    - path: ".planning/phases/07-sourcerer-as-downstream/fixtures/sourcerer-equivalent/brand/mark.svg"
      provides: "Original square synthetic artwork satisfying the single-line svg runtime-channel contract"
    - path: "scripts/verify-downstream-fixture.mjs"
      provides: "Fixture harness: stage-external, generate, assert emitted bytes, restore default"
      min_lines: 120
  key_links:
    - from: "scripts/verify-downstream-fixture.mjs"
      to: "scripts/generate.mjs"
      via: "drives PB_CONFIG_DIR per-command and derives expectations through the imported resolveConfig merge path"
      pattern: "resolveConfig"
    - from: "scripts/verify-downstream-fixture.mjs"
      to: "scripts/verify-branding-identity.mjs"
      via: "mirrors its brand-full-name read contract (trimmed selection and matching) for the generate-level agreement proof"
      pattern: "brand-full-name"
---

<objective>
Prove the milestone acceptance core with a synthetic downstream: a Sourcerer-equivalent external config yields a fully branded product from an untouched platform tree.

Purpose: DOC-02 requires Sourcerer reproduced as a pure downstream, but no Sourcerer mark may enter this tree (residual-brand scan gate; inventory/brand-tokens.json is the only file allowed to name the originating product). The synthetic fixture stands in with fully separate branding, and the harness built here becomes the proof instrument plans 07-03 and 07-04 drive.
Output: Committed fixture sources (scan-excluded under .planning/), the fixture harness with --self-test, and the neutrality re-proof.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@configuration.toml
@scripts/generate.mjs
@.planning/phases/07-sourcerer-as-downstream/07-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Commit the synthetic Sourcerer-equivalent fixture sources</name>
  <files>.planning/phases/07-sourcerer-as-downstream/fixtures/sourcerer-equivalent/configuration.toml, .planning/phases/07-sourcerer-as-downstream/fixtures/sourcerer-equivalent/brand/mark.svg</files>
  <action>Write the fixture as the two rebrand inputs mirroring the in-tree shape: configuration.toml plus brand/mark.svg under fixtures/sourcerer-equivalent/. Read scripts/lib/config-schema.json first and shape every value to its regex (identity fields to the basename pattern, URLs to the URL pattern, ESR tag to the shape the tag-literal sweep accepts — a FIREFOX_-plus-digit tag distinct from the platform's, e.g. a different ESR point release — theia_release matching the platform's since the generator emits no fragment for it). Every required key must differ from the platform's: synthetic vendor pair (spaceless machine form plus multi-word display form, keeping the two-vendor split), display_name with an interior space, distinct app_basename/binary_name/remoting_name/distribution_id, distinct legal triple, distinct support_url/telemetry endpoint host. Omit cosmetic keys deliberately to exercise the default-echo path. Artwork: an original geometric mark (not the platform's power-glyph construction), square viewBox, the svg element on one line starting with the literal svg open tag (the runtime-channel contract readMarkSvgElement enforces). No real-world product name, vendor, URL, or trademark string anywhere — the residual scan must stay green. Commit location under .planning/ keeps both scanners' scope clean (.planning/ is excluded from scan-brand-residue and verify-manifest-literals by their committed tables).</action>
  <verify><automated>node scripts/scan-brand-residue.mjs && node scripts/verify-manifest-literals.mjs && node scripts/verify-manifest-literals.mjs --self-test</automated></verify>
  <done>Fixture committed with fully distinct required values and original square artwork; both tree scans green with the new files in scope-of-exclusion</done>
</task>

<task type="auto">
  <name>Build the downstream-fixture harness with its self-test</name>
  <files>scripts/verify-downstream-fixture.mjs</files>
  <action>Write scripts/verify-downstream-fixture.mjs as an honestly --quick static-plus-generate gate (reads files and runs the generator only; no build, no browser, no display). Contract: --source <committed-fixture-dir> [--expect-fail <substring>] [--fixtures-root <dir>] plus --self-test; a --fixtures-root/--all mode driving every committed fixture is required by plan 07-04, so include it now. For each fixture the harness copies the source to a mkdtemp external dir (proving path-independence — never generate from inside the repo), snapshots the platform generated/ tree hashes, then runs env PB_CONFIG_DIR=<stage> node scripts/generate.mjs in a child process. EXPECTED-PASS path: exit must be 0; derive the fixture expectation through the imported resolveConfig (platform manifest as defaults, staged configuration.toml as downstream — the same call main() makes) and assert exact-equality bytes on the emitted dev and release configure.sh display lines, both variants' brand.ftl full-name terms and brand.properties brandFullName keys (each file agreeing with the other and with the base-plus-suffix derivation — mirror the trimmed-selection read contract, noting the WR-01 agreement rule), the Theia frontend-config applicationName, and the legal-notice own-notice line; assert none of the platform's required-slot values (both variant full names, vendor_display, vendor_machine, trademark_notice, derived from a default resolveConfig) occurs anywhere under generated/ except inside the fixed forever-identifiers (powerbrowser/ tree paths, @powerbrowser scope, chrome package URI, Firefox UA/product compatibility terms, unofficial channel, application ID — enumerate the fixed set from docs/REBRANDING.md step 5 rather than inventing it); every other platform value present must be covered by a default-applied echo line captured from the fixture run (presence if and only if echoed). EXPECTED-FAIL path (needed by plan 07-03): exit must be non-zero with stderr carrying <substring>, the failure must obey the plain-words copy shape (names the setting or asset, ends with a next step, no stack), and the generated/ snapshot must be byte-identical before and after. Always restore with a clean-env default generate plus --check fresh. --self-test drives scratch fixtures through the harness's own assertion functions (never the committed fixtures): green control first, then a platform-value-swapped emitted file going red naming file and value, an ftl/properties drift going red, and a missing-mark.svg stage going red. Non-vacuity: missing source dir, empty fixture set, and zero assertions each fail distinctly.</action>
  <verify><automated>node scripts/verify-downstream-fixture.mjs --self-test && node scripts/verify-downstream-fixture.mjs --source .planning/phases/07-sourcerer-as-downstream/fixtures/sourcerer-equivalent && node scripts/generate.mjs --check && git status --porcelain</automated></verify>
  <done>Harness self-test green; synthetic fixture generates fully branded output with zero platform edits; default tree restored fresh</done>
</task>

<task type="auto">
  <name>Re-prove platform neutrality with the downstream absent</name>
  <files>scripts/verify-downstream-fixture.mjs</files>
  <action>No fixture edits in this task. With PB_CONFIG_DIR unset and the fixture present only under .planning/, re-run the default gates that define neutrality: node scripts/generate.mjs, --check fresh, generated-byte-identity plus its self-test, verify-manifest-literals plus self-test, verify-branding-identity --self-test (derivation still manifest-driven after the 07-01 mechanism change), the full scripts/verify-platform.sh --quick, and the residual scan; confirm git status shows only the two intended new paths (fixture sources, harness) plus the plan files. Log the per-surface fixture values the harness asserted (display names per variant, vendor pair, trademark notice, pins) briefly in the SUMMARY as the DOC-02 evidence baseline plan 07-04 formalizes.</action>
  <verify><automated>node scripts/verify-generated-identity.mjs && node scripts/verify-branding-identity.mjs --self-test && scripts/verify-platform.sh --quick && node scripts/scan-brand-residue.mjs && git status --porcelain</automated></verify>
  <done>Platform generates and verifies identically with the downstream absent; only intended files changed</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture→harness | Fixture values are test data but flow through the same derivation assertions as a stranger's config; a harness that agrees with itself proves nothing |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-02 | Tampering | harness expectation source | low | mitigate | Expectations derive at check time from resolveConfig over the staged manifest, never kept in the harness; self-test plants prove the assertions go red on drift |
| T-07-03 | Information Disclosure | synthetic trademark strings | low | accept | Fixture carries invented marks only; rationale: nothing real to protect, and the scan-excluded location keeps even synthetic marks out of the shipped tree |
</threat_model>

<verification>
Harness self-test green; sourcerer-equivalent fixture fully branded at generate level with required-slot platform values absent (fixed identifiers intact); default-mode byte-identity, both --self-tests, full --quick, and residual scan green; git status shows only intended files.
</verification>

<success_criteria>
- DOC-02 synthetic proof exists: external config plus logo yields the fixture-branded tree, zero platform edits
- CFG-05 exercised beyond the tracer: assets plus pins flow from the external dir
- VER-03 groundwork: harness derives everything at check time and plants prove it discriminates
</success_criteria>

<output>
Create `.planning/phases/07-sourcerer-as-downstream/07-02-SUMMARY.md` when done
</output>
