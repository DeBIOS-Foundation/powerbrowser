---
phase: 07-sourcerer-as-downstream
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [scripts/generate.mjs, scripts/verify-platform.sh]
autonomous: true
requirements: [CFG-05]
user_setup: []

estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Pointing PB_CONFIG_DIR at an external directory builds generated output from that config with zero edits inside the platform repo"
    - "Unset PB_CONFIG_DIR reproduces the exact platform output as before (byte-identity gate still green)"
    - "A missing, empty, or relative PB_CONFIG_DIR fails with a clear plain-words error or resolves predictably — never silently generates from the wrong tree"
  artifacts:
    - path: "scripts/generate.mjs"
      provides: "PB_CONFIG_DIR external-config resolution (manifest + brand asset root)"
      contains: "PB_CONFIG_DIR"
    - path: "scripts/verify-platform.sh"
      provides: "Driver sanitizes PB_CONFIG_DIR so CI can never silently generate from a stray export"
      contains: "PB_CONFIG_DIR"
  key_links:
    - from: "scripts/generate.mjs"
      to: "configuration.toml"
      via: "main() derives downstreamPath from PB_CONFIG_DIR, resolved through the existing resolveConfig merge path"
      pattern: "resolveConfig"
---

<objective>
Land the CFG-05 mechanism: PB_CONFIG_DIR external-config resolution in the generator, as the thinnest end-to-end slice the rest of the phase expands from.

Purpose: Every later plan in this phase (synthetic fixture, adversarial fixtures, layers proof) drives this variable. Without it, resolveConfig's downstream path is reachable only from --self-test fixtures (generate.mjs comment at loadLayer), and both brand-asset reads (MARK_SVG_ABS, readMarkSvgElement) are hard-wired to the platform tree.
Output: PB_CONFIG_DIR support in scripts/generate.mjs plus driver sanitization, proven by a /tmp external manifest end to end.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@configuration.toml
@scripts/generate.mjs
</context>

<tasks>

<task type="tracer">
  <name>End-to-end PB_CONFIG_DIR manifest resolution — one external config, one emitted byte</name>
  <files>scripts/generate.mjs</files>
  <action>Implement the thinnest external-config slice in scripts/generate.mjs main() only: read process.env.PB_CONFIG_DIR once (empty or whitespace-only counts as unset), resolve a relative value against process.cwd(), and pass join(resolvedDir, 'configuration.toml') as the downstreamPath argument to the existing exported resolveConfig(defaultsPath, downstreamPath) merge path — no new merge code, no asset changes in this task. A resolved dir without a readable configuration.toml is a hard failure through report() in the file's existing plain-words shape (name the variable, state the problem, end with the re-run next step, no stack trace, no machine path of this checkout). Then prove it: stage a complete valid manifest under /tmp (copy the shipped configuration.toml, change only identity.display_name, identity.app_basename, identity.binary_name, identity.remoting_name, product.vendor_machine, product.vendor_display, legal.trademark_notice to synthetic Acme values that appear nowhere else in the tree), run env PB_CONFIG_DIR=<stage> node scripts/generate.mjs, and assert the emitted generated dev configure.sh carries the fixture display line exactly while the platform tree shows no modifications (git status --porcelain shows nothing but ignored generated/). Restore with plain node scripts/generate.mjs and confirm node scripts/generate.mjs --check reports fresh. Per D-05 the staged manifest may omit cosmetic keys to exercise the default-echo path, but must state every required key.</action>
  <verify><automated>env PB_CONFIG_DIR=/tmp/pb07t node scripts/generate.mjs && grep -F 'MOZ_APP_DISPLAYNAME="Acme Browser Dev"' generated/dev-configure-check-path && node scripts/generate.mjs && node scripts/generate.mjs --check && node scripts/verify-generated-identity.mjs</automated></verify>
  <done>The single external-manifest path works end to end and the default path is restored fresh with byte-identity green</done>
</task>

<task type="auto">
  <name>Redirect the brand asset root and harden the variable contract</name>
  <files>scripts/generate.mjs, scripts/verify-platform.sh</files>
  <action>Expand the tracer to the second external input. Today both artwork reads are platform-fixed: MARK_SVG_ABS (iconSourceFailures, the inkscape argv in iconPngBytes) and readMarkSvgElement (Theia runtime channel). Introduce one asset-root source of truth defaulting to REPO_ROOT and switching to the resolved PB_CONFIG_DIR dir when set, so every artwork read and the inkscape invocation flow through it; failure copy keeps naming the relative brand/mark.svg form (the fixed asset a reader restores) and never the absolute external path, per the file's copy rule against host paths. Harden the variable: relative values resolve against process.cwd(); a path that does not exist or holds no configuration.toml fails naming PB_CONFIG_DIR with a next step. Harden the driver: unset PB_CONFIG_DIR near the top of scripts/verify-platform.sh with a one-line comment (a stray export must never silently rebrand CI; fixture rows pass the variable per-command with an env prefix instead). Confirm the two manifest-layer verifiers need no change and document why in the SUMMARY: verify-manifest-literals.mjs stays default-mode because its allowlist-freshness rule would report every platform comparand row stale under a fixture manifest (the entry's slot value no longer occurs in its file), so --quick is the default-config gate and per-fixture proof belongs to the harness built in plan 07-02.</action>
  <verify><automated>node scripts/generate.mjs --self-test && node scripts/generate.mjs --check && node scripts/verify-generated-identity.mjs && scripts/verify-platform.sh --quick && node scripts/scan-brand-residue.mjs && git status --porcelain</automated></verify>
  <done>Artwork resolves from the external dir when set and the platform tree otherwise; driver sanitized; full --quick and residual scan green</done>
</task>

<task type="auto">
  <name>Pin the mechanism with self-test cases and close the plan</name>
  <files>scripts/generate.mjs</files>
  <action>Extend the generate --self-test case table (same shape as the existing downstream-path fixture cases that drive resolveConfig(MANIFEST_PATH, fixturePath)) with external-dir resolution cases: a valid external manifest staged under mkdtemp resolves to its own display values through the same derivation the tracer used; a PB_CONFIG_DIR pointing at a dir with no configuration.toml fails naming the variable; every new failure line passes the file's existing internalsLeaked no-internals predicate (no stack frames, no node: specifiers, no checkout path). Do not restate the merge or validation logic in the cases — drive the shipped functions. Re-run the whole gate set from the previous task to close.</action>
  <verify><automated>node scripts/generate.mjs --self-test && scripts/verify-platform.sh --quick && node scripts/scan-brand-residue.mjs</automated></verify>
  <done>New self-test cases green alongside all existing ones; --quick and residual scan green; platform tree clean</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shell-env→generator | PB_CONFIG_DIR is a new input edge; values flow only into path joins and the existing schema/sink guards, never into shell strings |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Tampering | PB_CONFIG_DIR resolution | low | mitigate | External dir is the invoking reader's own choice, not attacker input; manifest values still pass unknown-rejection, mask, validation, and the shell/NSIS/XML sink guards; driver unsets the variable so CI cannot inherit it |
</threat_model>

<verification>
External manifest in /tmp generates fixture-branded output with zero platform edits; default regenerate restores byte-identical output; --self-test, --check, byte-identity, full --quick, and residual scan all green; git status clean.
</verification>

<success_criteria>
- CFG-05 mechanism exists: PB_CONFIG_DIR selects manifest + brand assets, generated/ stays in the platform tree
- Default path behavior is byte-identical to pre-plan (no drift)
- No Sourcerer or synthetic-fixture mark committed anywhere in the tree
</success_criteria>

<output>
Create `.planning/phases/07-sourcerer-as-downstream/07-01-SUMMARY.md` when done
</output>
