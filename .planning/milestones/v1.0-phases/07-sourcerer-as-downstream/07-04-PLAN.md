---
phase: 07-sourcerer-as-downstream
plan: 04
type: execute
wave: 4
depends_on: [07-03]
files_modified: [scripts/verify-platform.sh, docs/REBRANDING.md, scripts/verify-rebranding-docs.mjs, .planning/phases/07-sourcerer-as-downstream/fixtures/README.md]
autonomous: true
requirements: [VER-03, DOC-02]
user_setup: []

estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Both verification layers demonstrably pass for every fixture — nothing keyed to the platform's or any fixture's own values"
    - "docs/REBRANDING.md carries a stranger through an external-config rebrand (its fixture run is the guide's first carry-through test)"
    - "Tier-3 per-fixture build drills exist as exact runnable commands, staged unexecuted per the static-only scope"
  artifacts:
    - path: "scripts/verify-platform.sh"
      provides: "Registered downstream-fixture rows plus mandatory self-test rows"
      contains: "verify-downstream-fixture"
    - path: "docs/REBRANDING.md"
      provides: "External-config rebrand section proven by the fixture carry-through"
    - path: ".planning/phases/07-sourcerer-as-downstream/fixtures/README.md"
      provides: "Per-fixture tier-3 drill commands, staged unexecuted"
  key_links:
    - from: "scripts/verify-platform.sh"
      to: "scripts/verify-downstream-fixture.mjs"
      via: "registry rows drive the harness --all mode plus its self-test beside the other static rows"
      pattern: "verify-downstream-fixture"
    - from: "docs/REBRANDING.md"
      to: "scripts/verify-rebranding-docs.mjs"
      via: "coverage gate still derives every field from the generator schema and every walkthrough command as code spans"
      pattern: "verify-rebranding-docs"
---

<objective>
Formalize the both-layers-pass proof, carry-test the rebranding guide against a fixture, and stage the tier-3 drills — the milestone acceptance close-out.

Purpose: ROADMAP criterion 4 (both layers pass for every fixture) plus DOC-02's guide half: the fixture run doubles as docs/REBRANDING.md's first real carry-through test. Static plus generate-level checks only — fixture builds are verified at generate/verify level, and live-build drills are staged as exact commands, unexecuted (tier-3 builds are out of scope for this phase).
Output: Registry rows, guide updates with a green coverage gate, fixture drill README, and the acceptance mapping.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/REBRANDING.md
@scripts/verify-platform.sh
@scripts/verify-rebranding-docs.mjs
@.planning/phases/06-two-layer-verification-and-rebranding-docs/06-01-SUMMARY.md
@.planning/phases/06-two-layer-verification-and-rebranding-docs/06-02-SUMMARY.md
@.planning/phases/07-sourcerer-as-downstream/07-02-SUMMARY.md
@.planning/phases/07-sourcerer-as-downstream/07-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Register the both-layers proof and its fixture rows</name>
  <files>scripts/verify-platform.sh</files>
  <action>Append the downstream-fixture check row plus its mandatory self-test row beside the other static rows in scripts/verify-platform.sh, driving node scripts/verify-downstream-fixture.mjs in --all mode (all EXPECTED-PASS fixtures generate-plus-verify, both EXPECTED-FAIL fixtures fail naming their rules) with per-command env prefixes (the driver sanitizes PB_CONFIG_DIR per plan 07-01, so no row may rely on inherited environment). The both-layers argument this row completes: static layer — verify-manifest-literals.mjs green in default mode after every fixture run (no fixture literal leaked into hand-written files) plus the harness's required-slot-absence and presence-iff-echoed assertions over each fixture's generated/ tree; runtime layer at generate level — per-fixture brand-full-name agreement (ftl versus properties versus manifest-derived expectation) asserted by the harness for both variants, plus verify-branding-identity.mjs --self-test green proving derivation is still manifest-driven after the 07-01 mechanism change. Prove both new rows via --only, then the full --quick set. Full six-surface live proof stays tier-3 and is staged, not run, in the next task's README.</action>
  <verify><automated>scripts/verify-platform.sh --only verify-downstream-fixtures && scripts/verify-platform.sh --only verify-downstream-fixtures-self-test && scripts/verify-platform.sh --quick</automated></verify>
  <done>Fixture rows registered and green via --only; full --quick green; layers argument recorded in the SUMMARY</done>
</task>

<task type="auto">
  <name>Carry-test REBRANDING.md against a scratch external dir and document PB_CONFIG_DIR</name>
  <files>docs/REBRANDING.md, scripts/verify-rebranding-docs.mjs</files>
  <action>Follow the guide's own walkthrough steps 2, 4, and 8 verbatim against a scratch external dir (fresh mkdtemp holding a copy of the sourcerer-equivalent fixture): generate first, edit configuration.toml plus brand/mark.svg only, re-run generate plus --check, then the static gates — logging every friction, missing prerequisite, or wrong path as a genuine gap. Then close the gaps in place: add the external-config section the guide currently lacks (PB_CONFIG_DIR contract — dir layout configuration.toml plus brand/mark.svg, generated/ stays in the platform tree, upstream pins flow into upstream-pins.env, fixed forever-identifiers restated, profile-migration note applying across the vendor/basename substitution), keeping every existing walkthrough command code span intact. Read scripts/verify-rebranding-docs.mjs first: it derives fields from the generator schema and requires walkthrough commands as code spans — extend its command expectations only if it hardcodes a closed list the new section must join, otherwise leave the checker untouched. No internal identifier in user-facing prose per the UI-SPEC rule; the residual scan must stay green with the edited guide in scope.</action>
  <verify><automated>node scripts/verify-rebranding-docs.mjs && node scripts/verify-rebranding-docs.mjs --self-test && scripts/verify-platform.sh --only verify-rebranding-docs && node scripts/scan-brand-residue.mjs</automated></verify>
  <done>Guide proven step by step against a real external dir; PB_CONFIG_DIR documented; coverage gate plus self-test green; scan green</done>
</task>

<task type="auto">
  <name>Stage the tier-3 per-fixture drills and map the acceptance criteria</name>
  <files>.planning/phases/07-sourcerer-as-downstream/fixtures/README.md</files>
  <action>Write fixtures/README.md (scan-excluded under .planning/) with one UNEXECUTED drill block per EXPECTED-PASS fixture (sourcerer-equivalent, spaced-name, late-sort-name): exact copy-paste commands — stage fixture to a space-free path, env PB_CONFIG_DIR=<stage> node scripts/generate.mjs, scripts/fetch-upstream.sh, nix develop .#firefox plus the MOZCONFIG mach build per docs/BUILD.md tiers, then node scripts/verify-branding-identity.mjs six-surface run for the dev variant plus the release-variant rows once a release objdir exists, then clean-env default regenerate and --check — each block labeled UNEXECUTED with the standing reason (tier-3 cost, roughly an hour per build; static-plus-generate proof is this phase's scope). Close with the milestone acceptance mapping: the four ROADMAP Phase 7 success criteria each naming its evidence (harness rows, matrix from 07-03, registry rows, guide section). Final neutrality pass: full --quick, residual scan, git status showing only intended files.</action>
  <verify><automated>scripts/verify-platform.sh --quick && node scripts/scan-brand-residue.mjs && git status --porcelain && test -f .planning/phases/07-sourcerer-as-downstream/fixtures/README.md</automated></verify>
  <done>Drills staged as exact commands; acceptance criteria mapped to evidence; tree neutral and clean</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| guide→stranger | Every downstream inherits the new external-config section; a wrong command or omitted obligation ships in all of them |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-06 | Tampering | guide command accuracy for the external path | low | mitigate | Carry-through task executes the documented steps against a scratch dir instead of reviewing prose; coverage gate pins field and command completeness going forward |
| T-07-07 | Repudiation | staged-but-unexecuted tier-3 drills claimed as proof | low | mitigate | README labels every drill UNEXECUTED with the cost reason; SUMMARY maps acceptance criteria to generate-level evidence only, never to builds that did not run |
</threat_model>

<verification>
Fixture registry rows plus self-test green via --only and inside full --quick; rebranding coverage gate plus self-test green after the carry-through; residual scan green; drill README present with UNEXECUTED tier-3 blocks; git status shows only intended files.
</verification>

<success_criteria>
- VER-03 closed: both layers pass per fixture through registered rows, nothing keyed to any one config's values
- DOC-02 closed: synthetic downstream proof plus a carry-tested external-config guide
- No tier-3 build executed or claimed; drills staged exactly for the build phase
</success_criteria>

<output>
Create `.planning/phases/07-sourcerer-as-downstream/07-04-SUMMARY.md` when done
</output>
