---
phase: 07-sourcerer-as-downstream
plan: 03
type: execute
wave: 3
depends_on: [07-02]
files_modified: [.planning/phases/07-sourcerer-as-downstream/fixtures/spaced-name/configuration.toml, .planning/phases/07-sourcerer-as-downstream/fixtures/spaced-name/brand/mark.svg, .planning/phases/07-sourcerer-as-downstream/fixtures/late-sort-name/configuration.toml, .planning/phases/07-sourcerer-as-downstream/fixtures/late-sort-name/brand/mark.svg, .planning/phases/07-sourcerer-as-downstream/fixtures/non-square-logo/configuration.toml, .planning/phases/07-sourcerer-as-downstream/fixtures/non-square-logo/brand/mark.svg, .planning/phases/07-sourcerer-as-downstream/fixtures/missing-required-key/configuration.toml, .planning/phases/07-sourcerer-as-downstream/fixtures/missing-required-key/brand/mark.svg]
autonomous: true
requirements: [VER-03]
user_setup: []

estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A spaced display name and a post-m-browser sort name each generate and verify correctly through the harness"
    - "A non-square logo fails loudly naming the square-artwork rule with the tree untouched"
    - "A missing required key fails loudly naming the key with the tree untouched — never a silent wrong build"
  artifacts:
    - path: ".planning/phases/07-sourcerer-as-downstream/fixtures/spaced-name/configuration.toml"
      provides: "Multi-word display-name adversarial manifest"
    - path: ".planning/phases/07-sourcerer-as-downstream/fixtures/late-sort-name/configuration.toml"
      provides: "Post-m-browser sort-name adversarial manifest"
    - path: ".planning/phases/07-sourcerer-as-downstream/fixtures/non-square-logo/brand/mark.svg"
      provides: "Non-square artwork plant for the intended-failure proof"
    - path: ".planning/phases/07-sourcerer-as-downstream/fixtures/missing-required-key/configuration.toml"
      provides: "Required-key-omission plant for the intended-failure proof"
  key_links:
    - from: ".planning/phases/07-sourcerer-as-downstream/fixtures/spaced-name/configuration.toml"
      to: "scripts/verify-downstream-fixture.mjs"
      via: "driven as --source EXPECTED-PASS; emitted shell, desktop, locale, and installer bytes asserted exact"
      pattern: "verify-downstream-fixture"
    - from: ".planning/phases/07-sourcerer-as-downstream/fixtures/missing-required-key/configuration.toml"
      to: "scripts/generate.mjs"
      via: "driven as --expect-fail naming the omitted dotted key through the required-setting check"
      pattern: "is not set"
---

<objective>
Close VER-03's adversarial half: four hostile fixtures that prove the platform is not accidentally built around one friendly config.

Purpose: ROADMAP success criterion 3 names the four classes (spaced display name, post-m-browser sort name, non-square logo, missing required key). Each must either generate-plus-verify correctly or fail with the intended clear error — a silent wrong build anywhere is the milestone failing.
Output: Four committed fixture dirs plus harness runs proving pass-or-intended-fail for each, with the never-silently-wrong matrix recorded.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@scripts/generate.mjs
@scripts/lib/config-schema.json
@.planning/phases/07-sourcerer-as-downstream/07-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Land the two EXPECTED-PASS adversarial fixtures</name>
  <files>.planning/phases/07-sourcerer-as-downstream/fixtures/spaced-name/configuration.toml, .planning/phases/07-sourcerer-as-downstream/fixtures/spaced-name/brand/mark.svg, .planning/phases/07-sourcerer-as-downstream/fixtures/late-sort-name/configuration.toml, .planning/phases/07-sourcerer-as-downstream/fixtures/late-sort-name/brand/mark.svg</files>
  <action>Write two manifests cloned from the 07-02 sourcerer-equivalent fixture with surgical diffs, each with its own original square mark.svg copy. Spaced-name: display_name carrying two interior spaces across three words (e.g. a Cedar Falls Browser-class invention — verify against scripts/lib/config-schema.json first; if the display pattern constrains spacing, use the hardest allowed form and record the constraint), vendor_machine kept spaceless to prove the two-vendor split holds the profile path safe. Late-sort-name: display_name, app_basename, binary_name, and remoting_name all sorting after m-browser (a Zebra Browser-class invention), proving no alphabetical-position assumption anywhere in derivation, emission, or assertion. Drive both through node scripts/verify-downstream-fixture.mjs --source as EXPECTED-PASS: the harness asserts the spaced bytes survive exactly into the double-quoted shell assignment, the desktop Name key, both locale files per variant, and the installer defines, and that the late-sort values do the same with full agreement. All invented values must appear nowhere else in the tree; residual scan stays green.</action>
  <verify><automated>node scripts/verify-downstream-fixture.mjs --source .planning/phases/07-sourcerer-as-downstream/fixtures/spaced-name && node scripts/verify-downstream-fixture.mjs --source .planning/phases/07-sourcerer-as-downstream/fixtures/late-sort-name && node scripts/scan-brand-residue.mjs && git status --porcelain</automated></verify>
  <done>Both hostile-but-valid fixtures generate fully branded output with exact bytes on every asserted surface and zero platform edits</done>
</task>

<task type="auto">
  <name>Land the two EXPECTED-FAIL adversarial fixtures</name>
  <files>.planning/phases/07-sourcerer-as-downstream/fixtures/non-square-logo/configuration.toml, .planning/phases/07-sourcerer-as-downstream/fixtures/non-square-logo/brand/mark.svg, .planning/phases/07-sourcerer-as-downstream/fixtures/missing-required-key/configuration.toml, .planning/phases/07-sourcerer-as-downstream/fixtures/missing-required-key/brand/mark.svg</files>
  <action>Write two plants. Non-square-logo: a valid manifest (clone of the sourcerer-equivalent) with brand/mark.svg carrying viewBox 0 0 128 64 — drive with --expect-fail naming the square-artwork rule, asserting the generate refuses (no stretch, no letterbox, no substitution) and the generated/ snapshot is untouched. Missing-required-key: a manifest identical to the sourcerer-equivalent except identity.display_name removed entirely — drive with --expect-fail naming identity.display_name, asserting the required-setting check fires (masked-defaults behavior: no silent inheritance of the platform's name) with the tree untouched and the message in the plain-words shape. Both failure texts must carry no internals (no stack, no checkout path). Reuse the 07-02 harness unchanged — if it cannot express either assertion, that is a harness gap: fix it under this task and re-run its --self-test, recording the fix in the SUMMARY.</action>
  <verify><automated>node scripts/verify-downstream-fixture.mjs --source .planning/phases/07-sourcerer-as-downstream/fixtures/non-square-logo --expect-fail square && node scripts/verify-downstream-fixture.mjs --source .planning/phases/07-sourcerer-as-downstream/fixtures/missing-required-key --expect-fail identity.display_name && node scripts/generate.mjs --check && git status --porcelain</automated></verify>
  <done>Both hostile fixtures fail loudly with the intended clear error and provably untouched output; default tree fresh</done>
</task>

<task type="auto">
  <name>Record the never-silently-wrong matrix and re-prove neutrality</name>
  <files>.planning/phases/07-sourcerer-as-downstream/fixtures/missing-required-key/brand/mark.svg</files>
  <action>Assemble the VER-03 evidence matrix covering all five fixtures (sourcerer-equivalent from 07-02 plus the four here): per fixture, the intended outcome (pass with exact-bytes proof, or fail naming the exact rule), the harness command that proves it, and the observed result — no other file edits except genuine gaps found. Then re-prove neutrality in default mode: clean-env generate, --check fresh, generated-byte-identity, manifest-literals, branding-identity --self-test, full --quick, residual scan, and git status showing only intended fixture and plan files. If any fixture outcome differs from intent (e.g. a spaced value the schema rejects, making the intended-pass fixture an intended-fail one), do not force the intent: record the actual rule, adjust the fixture to the hardest in-contract hostile value, and re-run.</action>
  <verify><automated>scripts/verify-platform.sh --quick && node scripts/scan-brand-residue.mjs && node scripts/verify-downstream-fixture.mjs --self-test && git status --porcelain</automated></verify>
  <done>Five-row matrix complete with every row at its intended outcome; platform neutral with the downstreams absent</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| hostile-manifest→generator | Adversarial values probe every sink guard (shell, desktop, locale, installer XML/NSIS); rejection must precede any write |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-04 | Spoofing | spaced/sorted display values in emitted surfaces | medium | mitigate | Exact-equality harness assertions per surface plus the schema regexes and sink guards; a value that survives must be the manifest's own bytes |
| T-07-05 | Tampering | non-square artwork stretching into icons | low | mitigate | Squareness rule fails the run before any write; harness snapshot proves the tree untouched |
</threat_model>

<verification>
All four adversarial fixtures at intended outcomes through the unchanged-or-minimally-fixed harness; never-silently-wrong matrix recorded; default-mode full --quick, both self-tests, and residual scan green; git status shows only intended files.
</verification>

<success_criteria>
- VER-03 adversarial classes all covered: spaced, late-sort, non-square, missing-key
- Every fail fixture names its rule in plain words with output untouched
- No fixture value leaks into the platform tree or the residual scan
</success_criteria>

<output>
Create `.planning/phases/07-sourcerer-as-downstream/07-03-SUMMARY.md` when done
</output>
