---
phase: 06-two-layer-verification-and-rebranding-docs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [scripts/verify-manifest-literals.mjs, scripts/verify-platform.sh]
autonomous: true
requirements: [VER-01]
user_setup: []

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A hardcoded Power Browser display value outside the manifest fails the static gate naming file and value"
    - "A stale allowlist entry fails the static gate naming the entry"
    - "Both layers plus generate --check have a CI path (wired in 06-06)"
  artifacts:
    - path: "scripts/verify-manifest-literals.mjs"
      provides: "VER-01 static literal scan, manifest-derived, boundary-matched, stale-allowlist failure"
      min_lines: 150
    - path: "scripts/verify-platform.sh"
      provides: "Two new registry rows for the check and its self-test"
      contains: "verify-manifest-literals"
  key_links:
    - from: "scripts/verify-manifest-literals.mjs"
      to: "scripts/generate.mjs"
      via: "imports resolveConfig to derive expected literals from configuration.toml at check time"
      pattern: "resolveConfig"
    - from: "scripts/verify-manifest-literals.mjs"
      to: "scripts/scan-brand-residue.mjs"
      via: "reuses exported boundary matcher instead of reimplementing it"
      pattern: "isBoundaryMatch|findMatches"
---

<objective>
Build the VER-01 static layer: a manifest-derived, boundary-matched brand-literal scan with a committed allowlist that fails on stale entries, registered in the platform verifier.

Purpose: VER-01 success criterion 1 — the static scan fails the build on configured brand values hardcoded outside the manifest and on stale allowlist entries. The existing scan-brand-residue covers the originating product's pre-rename tokens; check-patch-surface --brand-values covers patches/ only. Nothing covers Power Browser's own configured display values leaking into hand-written files. This plan closes that gap without duplicating either existing check.

Output: scripts/verify-manifest-literals.mjs with --self-test, plus two registry rows.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@configuration.toml
</context>

<tasks>

<task type="auto">
  <name>Write verify-manifest-literals.mjs with self-test</name>
  <files>scripts/verify-manifest-literals.mjs</files>
  <action>Write a new static checker per D-03 VER-01 following the in-tree idiom of verify-theia-endpoints.mjs (derive expectations at check time, never keep them; planted-fault --self-test with green control first). Derive the literal set at check time from configuration.toml through scripts/generate.mjs resolveConfig: per-variant full display names (identity.display_name plus each variant name_suffix), product.vendor_display, legal.trademark_notice. Do NOT include fixed internal identifiers (app_basename, binary_name, remoting_name, vendor_machine) — those are platform-fixed per REQUIREMENTS Out of Scope and occur legitimately everywhere. Match with the boundary rule reused by import from scan-brand-residue.mjs (isBoundaryMatch/findMatches); a second local copy of that rule is a defect. Scope is derived from git ls-files at check time minus a committed exclusion map in the script header (generated/ as derived output, upstream/ and objdir*/ as foreign/build trees, inventory/brand-tokens.json as the one file allowed to name the originating product, .planning/ as historical record, configuration.toml itself as the value source, docs/REBRANDING.md as the documenting surface with reason, LICENSE copyright-holder line with reason). Every exclusion carries a one-line reason or the check fails its own startup assertion. Outside the exclusions, any boundary-matched literal occurrence is a failure naming file, line, and value. Committed allowlist: a literal-to-files table in the same file; each entry must still match at least one in-scope occurrence of its literal, else the entry is stale and fails naming the entry (covers removed literals, rewritten files, deleted files). Non-vacuity: an empty derived literal set, an empty scope, or an empty allowlist table each fail with a distinct message. The --self-test runs hermetic mkdtemp fixtures through the checker's own functions (no real-tree mutation): planted display literal in a scratch root goes red naming file and value with the unplanted control green first; planted identifier-form text (e.g. the space-less identifier followed by a letter) stays green proving the boundary rule; one stale allowlist entry of each kind (literal gone from manifest set, file rewritten clean, file deleted) goes red naming the entry. No assertion on the absence of a log line anywhere; every verdict is a count or set comparison with the compared values named.</action>
  <verify><automated>node scripts/verify-manifest-literals.mjs && node scripts/verify-manifest-literals.mjs --self-test</automated></verify>
  <done>Checker exits 0 on the shipped tree; self-test plants a display literal, a boundary control, and three stale-entry kinds, each going red with the control green first</done>
</task>

<task type="auto">
  <name>Register both rows and prove the gate end to end</name>
  <files>scripts/verify-platform.sh</files>
  <action>Append one check row and its mandatory self-test row to the CHECKS registry in scripts/verify-platform.sh beside the other static branding rows (scan-brand-residue, branding-preflight), following the established label convention verify-manifest-literals and verify-manifest-literals-self-test; the check is honestly --quick (reads text files only, no build, no browser, no display, no network). Then prove the registry path end to end via --only for both labels, and run the full --quick set to confirm no existing row regressed.</action>
  <verify><automated>scripts/verify-platform.sh --only verify-manifest-literals && scripts/verify-platform.sh --only verify-manifest-literals-self-test && scripts/verify-platform.sh --quick</automated></verify>
  <done>Both --only invocations pass and the full --quick table is green with the two new rows present</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| manifest→checker | configuration.toml values are trusted input; the checker must not eval or interpolate them into shell |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Tampering | verify-manifest-literals literal interpolation | medium | mitigate | Treat manifest values as opaque strings; escape before any RegExp construction following verify-branding-preflight.mjs escapeForRegExp precedent |
</threat_model>

<verification>
scripts/verify-platform.sh --only verify-manifest-literals passes; --self-test passes with all planted faults observed red; full --quick passes.
</verification>

<success_criteria>
- Hardcoded configured display value outside the manifest fails the gate naming file, line, and value (VER-01 half 1)
- Stale allowlist entry fails the gate naming the entry (VER-01 half 2)
- Check derives literals from configuration.toml at check time; no literal kept in the checker
- Both registry rows present and green; --quick fully green
</success_criteria>

<output>
Create `.planning/phases/06-two-layer-verification-and-rebranding-docs/06-01-SUMMARY.md` when done
</output>
