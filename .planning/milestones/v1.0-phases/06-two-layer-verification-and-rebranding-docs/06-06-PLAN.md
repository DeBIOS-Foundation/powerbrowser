---
phase: 06-two-layer-verification-and-rebranding-docs
plan: 06
type: execute
wave: 6
depends_on: [06-05]
files_modified: [.github/workflows/verify.yml]
autonomous: true
requirements: [VER-01, VER-02, DOC-01]
user_setup: []

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Both verification layers plus generate --check run in CI on every push and pass on Power Browser's own tree"
    - "The full --quick table is green locally with all Phase 6 rows present"
    - "Tier-3 exclusions from CI are named with reasons, not silently skipped"
  artifacts:
    - path: ".github/workflows/verify.yml"
      provides: "Push/PR CI running generator, freshness, and both static layers"
  key_links:
    - from: ".github/workflows/verify.yml"
      to: "scripts/verify-platform.sh"
      via: "CI invokes the --quick table, the same gate developers run locally"
      pattern: "verify-platform.sh --quick"
    - from: ".github/workflows/verify.yml"
      to: "scripts/generate.mjs"
      via: "CI runs generate then --check in the 02-06 order before any check reads generated output"
      pattern: "generate.mjs --check"
---

<objective>
Wire both verification layers plus generate --check into CI and close the phase with a full local gate run.

Purpose: Phase success criterion 4 — both layers plus generate --check run in CI and pass on Power Browser's own build. Today the only workflow is rebase-upstream.yml (workflow_dispatch-only, with generate/--check/byte-identity steps scoped to the rebase job per 02-06). Nothing runs the verification table on push. This plan adds a push/PR workflow for everything CI can honestly run, and records what it cannot.

Output: .github/workflows/verify.yml green on its first dispatchable state, full local --quick green, phase-close record.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-two-layer-verification-and-rebranding-docs/06-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Add the verify workflow for the static layers</name>
  <files>.github/workflows/verify.yml</files>
  <action>Add .github/workflows/verify.yml on push and pull_request following the rebase-upstream.yml precedents (contents: read, persist-credentials false, no Nix, no tier-3 build): checkout, then node scripts/generate.mjs, then node scripts/generate.mjs --check in the load-bearing 02-06 order (freshness before readers), then scripts/verify-platform.sh --quick as the whole static table including the Phase 6 rows (manifest-literals, trademark-surface, rebranding-docs, preflight, scan-brand-residue, theia-branding, theia-endpoints, telemetry, extension-pins, upstream-pins). Runtime-behind-build rows (the six-surface live drive beyond what --quick covers, verify-endpoints layers 2-3) stay out of this workflow with a comment naming each exclusion and its reason (needs the tier-3 binary / display / network observation CI cannot honestly provide); their local drill commands are recorded in the SUMMARY. Validate the YAML shape with node (parse only, no act run) and confirm the step order reads generate, --check, --quick.</action>
  <verify><automated>node -e "require('fs').readFileSync('.github/workflows/verify.yml','utf8')" && grep -n "generate.mjs --check" .github/workflows/verify.yml && grep -n "verify-platform.sh --quick" .github/workflows/verify.yml</automated></verify>
  <done>Workflow file present with generate, --check, --quick in order; exclusions commented with reasons; YAML parses</done>
</task>

<task type="auto">
  <name>Run the full phase gate locally and record close-out</name>
  <files>.github/workflows/verify.yml</files>
  <action>No file edits except genuine defects found. Run the complete local gate in order: node scripts/generate.mjs --check, scripts/verify-platform.sh --quick, node scripts/verify-branding-identity.mjs --variant dev (live against objdir/dist/bin/powerbrowser), and every Phase 6 --self-test (manifest-literals, trademark-surface, rebranding-docs, branding-preflight, scan-brand-residue). Record the full table, the live six-surface result, and the staged release-variant and CI-runtime drill commands with prerequisites in the SUMMARY. Then update the ROADMAP Phase 6 plan list to the six plans with their objectives (scoped Edit of the Phase 6 section only) so the phase-close state is visible; leave requirement-status flips to verify-work.</action>
  <verify><automated>scripts/verify-platform.sh --quick && node scripts/generate.mjs --check && node scripts/verify-branding-identity.mjs --variant dev</automated></verify>
  <done>Full local gate green; SUMMARY carries the table, live run, and staged drills; ROADMAP Phase 6 plan list reflects the six plans</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI→tree | The workflow clones and runs repo scripts; it builds nothing and pushes nothing |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-06 | Elevation | CI token scope | low | mitigate | Mirror the WR-09 precedent: contents read plus persist-credentials false, stated in the workflow |
</threat_model>

<verification>
Local gate fully green; workflow file ordered and parsing; drills staged with prerequisites.
</verification>

<success_criteria>
- CI runs generate, --check, and the --quick table on push/PR (criterion 4, static half)
- Full --quick plus live dev-variant run green locally on Power Browser's own build
- Runtime-behind-build exclusions named with reasons; release and CI-runtime drills staged exactly
</success_criteria>

<output>
Create `.planning/phases/06-two-layer-verification-and-rebranding-docs/06-06-SUMMARY.md` when done
</output>
