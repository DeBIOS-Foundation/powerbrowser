---
phase: 06-two-layer-verification-and-rebranding-docs
plan: 04
type: execute
wave: 4
depends_on: [06-03]
files_modified: [scripts/generate.mjs, theia/extensions/branding/src/browser/powerbrowser-about-dialog.tsx, theia/extensions/branding/src/browser/powerbrowser-branding-config.ts, theia/applications/browser/package.json, scripts/verify-branding-preflight.mjs]
autonomous: true
requirements: [VER-02]
user_setup: []

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The About dialog renders the downstream trademark notice, the Mozilla non-association statement, and the Eclipse attribution from the runtime channel"
    - "Removing any legal notice from the channel fails the static gate naming the notice"
    - "The Theia branding fragment and tracked package block still agree after the shape change"
  artifacts:
    - path: "scripts/generate.mjs"
      provides: "legalNotices emission in the Theia branding fragment"
      contains: "legalNotices"
    - path: "theia/extensions/branding/src/browser/powerbrowser-about-dialog.tsx"
      provides: "Channel-read rendering of legal notices"
      contains: "legalNotices"
    - path: "scripts/verify-branding-preflight.mjs"
      provides: "Legal-notice channel pin with planted-fault self-test"
      contains: "legalNotices"
  key_links:
    - from: "theia/extensions/branding/src/browser/powerbrowser-about-dialog.tsx"
      to: "theia/extensions/branding/src/browser/powerbrowser-branding-config.ts"
      via: "reads legalNotices through the shared readBrandingConfig reader, never as literals in render"
      pattern: "readBrandingConfig"
    - from: "scripts/verify-branding-preflight.mjs"
      to: "scripts/generate.mjs"
      via: "derives expected legalNotices from the manifest through the emitter, never kept in the checker"
      pattern: "emitTheiaBranding"
---

<objective>
Surface the 06-03 trademark findings in the product: the About dialog renders legal notices from the runtime branding channel, pinned by the preflight.

Purpose: A trademark notice that lives only in configuration.toml satisfies no policy — Mozilla requires an equally prominent non-association statement in words, Eclipse requires a trademark attribution sentence, and the downstream's own notice must appear in the product. Today legal.trademark_notice is never emitted, about_text is unset, and nothing renders these strings. This plan wires manifest → fragment → channel → dialog → gate, all derived, with removal going red.

Output: Extended emitter and fragment shape, channel reader, dialog rendering, preflight pin with self-test, regenerated tracked block.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@configuration.toml
@.planning/phases/06-two-layer-verification-and-rebranding-docs/06-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Emit legalNotices and render them from the channel</name>
  <files>scripts/generate.mjs, theia/extensions/branding/src/browser/powerbrowser-branding-config.ts, theia/extensions/branding/src/browser/powerbrowser-about-dialog.tsx, theia/applications/browser/package.json</files>
  <action>Extend emitTheiaBranding in scripts/generate.mjs with a legalNotices array derived from the manifest at generate time: the downstream's own legal.trademark_notice verbatim, plus a Mozilla non-association sentence interpolating the manifest display identity (same display-name-plus-suffix composition the emitter already uses) with the fixed tail not officially associated with Mozilla or its products, plus the Eclipse attribution sentence naming Theia as a trademark of Eclipse Foundation AISBL with the manifest repo URL domain reused never restated. Extend the branding-config reader to parse legalNotices as an array of non-empty strings (unknown shape falls back to undefined, never throws), and render the notices in powerbrowser-about-dialog.tsx exclusively through readBrandingConfig with no notice literal in render code, following the existing aboutText channel pattern and the section 11 link-half rule. Regenerate via node scripts/generate.mjs so generated/theia-branding.json and the tracked theia/applications/browser/package.json powerbrowserBranding block carry the new key, and confirm node scripts/verify-theia-branding.mjs stays green (it derives from the emitter, so agreement must hold without checker edits). Never edit generated/ by hand; the tracked package.json block is updated only through the generator's own flow.</action>
  <verify><automated>node scripts/generate.mjs && node scripts/generate.mjs --check && node scripts/verify-theia-branding.mjs && node scripts/verify-theia-branding.mjs --self-test</automated></verify>
  <done>Fragment and tracked block carry legalNotices with all three sentences; theia-branding check and self-test green; dialog renders notices only through the channel reader</done>
</task>

<task type="auto">
  <name>Pin the legal-notice channel in the preflight</name>
  <files>scripts/verify-branding-preflight.mjs</files>
  <action>Extend the section 11 about-dialog pin in scripts/verify-branding-preflight.mjs with a legal-notice triple mirroring the section 10 pattern: the read (dialog resolves legalNotices through readBrandingConfig — stopping is a per-rebrand edit again), the fallback (the expected notices derived from the manifest through emitTheiaBranding appear as code literals, never in render), and the render rule (no line paints a notice string as render text outside the channel mapping). Derive the expected notices from configuration.toml through the emitter at check time, never kept in the checker. Extend --self-test with two planted faults (channel read removed, one notice dropped from the fragment), each going red naming the notice with the unplanted control green first.</action>
  <verify><automated>node scripts/verify-branding-preflight.mjs && node scripts/verify-branding-preflight.mjs --self-test</automated></verify>
  <done>Preflight green; both planted legal-notice faults go red naming the notice</done>
</task>

<task type="auto">
  <name>Prove the full static table with the new shape</name>
  <files>scripts/verify-branding-preflight.mjs</files>
  <action>No file edits unless a defect surfaces. Run the affected registry rows plus the whole --quick set to confirm the fragment shape change ripples nowhere else (manifest-literals from 06-01 must still agree with the tree, trademark-surface from 06-03 must still agree, generated-byte-identity and generate-check must still hold since the thirty-three byte-identical surfaces are untouched by the fragment addition).</action>
  <verify><automated>scripts/verify-platform.sh --quick</automated></verify>
  <done>Full --quick table green including the extended preflight rows and all Phase 6 rows so far</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| manifest→dialog | Legal strings render in chrome UI; emitter escapes via JSON encoding, dialog renders as text nodes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-04 | Spoofing | legal-notice rendering | low | mitigate | Notices render as text through the channel reader; the render rule rejects literal markup strings, and the existing emittable-assertion on manifest texts stays in force |
</threat_model>

<verification>
Fragment, tracked block, dialog, and preflight agree; theia-branding and preflight self-tests green; full --quick green.
</verification>

<success_criteria>
- About dialog renders all three legal notices from the runtime channel for Power Browser's own manifest (VER-02 displayed-identity correctness)
- Dropping the channel read or any notice goes red naming it
- No byte-identical generator surface changed; generate --check and byte-identity still green
</success_criteria>

<output>
Create `.planning/phases/06-two-layer-verification-and-rebranding-docs/06-04-SUMMARY.md` when done
</output>
