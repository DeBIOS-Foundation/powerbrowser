---
phase: 02-configuration-manifest-and-generator-core
plan: 09
type: execute
wave: 3
depends_on: ["02-08"]
files_modified:
  - .planning/phases/02-configuration-manifest-and-generator-core/02-04-SUMMARY.md
  - .planning/phases/02-configuration-manifest-and-generator-core/02-06-SUMMARY.md
  - .planning/phases/02-configuration-manifest-and-generator-core/02-VERIFICATION.md
autonomous: true
gap_closure: true
requirements: [CFG-01, CFG-02, CFG-03, CFG-04, GEN-04]

estimate:
  tokens: 20000
  raw_tokens: 40000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "No summary in this phase directory claims the emitted bytes are independent of where the repository lives, because they were not"
    - "02-VERIFICATION.md no longer reports `status: passed` with `No gaps found` while two major gaps were open against the same phase"
    - "The phase record names G-02-11 and G-02-12, the plan that closed them, and the design that was ratified to close them"
    - "Every correction is an ADDITIVE, dated, attributed note beside the original claim, not a silent rewrite of history"
    - statement: "The requirement traceability table in 02-VERIFICATION.md still resolves all five phase requirement IDs after the correction"
      verification: backstop
  artifacts:
    - ".planning/phases/02-configuration-manifest-and-generator-core/02-VERIFICATION.md"
    - ".planning/phases/02-configuration-manifest-and-generator-core/02-04-SUMMARY.md"
  key_links:
    - "02-VERIFICATION.md `## Gaps Summary` -> 02-UAT.md's two recorded gaps -> 02-08-SUMMARY.md's closure evidence"
    - "02-04-SUMMARY.md's desktop-entry paragraph -> the ratified design in 02-DESIGN-G-02-11.md"
  prohibitions:
    - statement: "Must NOT rewrite an original claim in place. Corrections are additive blockquotes carrying a date and the source that found them, matching the two corrections already present in this directory."
    - statement: "Must NOT mark a requirement incomplete that the evidence still supports. G-02-11 and G-02-12 were defects in the GATES, not in CFG-01..04; downgrading a requirement on that basis would misreport the phase in the other direction."
    - statement: "Must NOT edit any file under scripts/, powerbrowser/, inventory/ or .github/. This plan corrects the record; 02-08 corrected the code."
    - statement: "Must NOT delete or renumber plans 02-01 through 02-06 or their summaries. They are executed history."
---

<objective>
Make the phase record honest: correct the one documentation claim that is still wrong, and correct
the verification report that says this phase had no gaps while two major ones were open against it.

Purpose: `02-VERIFICATION.md` carries `status: passed` and "No gaps found" and was written on
2026-09-01. The UAT that found G-02-11 and G-02-12 ran on 2026-09-02. A phase record that reports a
clean verification over a period in which two major gaps were open is the same class of defect as a
gate that can only go green — it is a green statement that discriminated nothing. Correcting it is
part of closing the gaps, not a tidy-up after them.

Output: three corrected `.planning/` files, each carrying additive dated notes in the form this
directory already uses, and a verification report whose status matches the evidence.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-configuration-manifest-and-generator-core/02-UAT.md
@.planning/phases/02-configuration-manifest-and-generator-core/02-VERIFICATION.md
@.planning/phases/02-configuration-manifest-and-generator-core/02-DESIGN-G-02-11.md
</context>

<correction_form>
Two corrections of this exact shape already exist in this directory and are the form to match —
`02-04-SUMMARY.md:112` and `02-06-SUMMARY.md:81`. An additive blockquote placed immediately after
the claim it corrects, opening with a bolded `Corrected <date> (<what found it>).`, stating what the
original said, what is true, and where the reader can verify it. The original sentence stays. A
summary rewritten in place stops being a record of what was believed at the time, which is the only
thing a summary is for.
</correction_form>

<tasks>

<task type="auto">
  <name>Task 1: Correct the one documentation claim still standing — repo location versus working directory</name>
  <files>.planning/phases/02-configuration-manifest-and-generator-core/02-04-SUMMARY.md, .planning/phases/02-configuration-manifest-and-generator-core/02-06-SUMMARY.md</files>
  <read_first>
    - .planning/phases/02-configuration-manifest-and-generator-core/02-UAT.md `## Documentation Drift` — the two items listed there, and G-02-11's `root_cause`, whose last sentence names this exact claim.
    - .planning/phases/02-configuration-manifest-and-generator-core/02-04-SUMMARY.md lines 84-96 — the desktop-entry emitter paragraph, whose closing sentence is the claim.
    - .planning/phases/02-configuration-manifest-and-generator-core/02-04-SUMMARY.md lines 106-120 — the `--check` state table and the correction already added there on 2026-09-02, which is the form to match.
    - .planning/phases/02-configuration-manifest-and-generator-core/02-06-SUMMARY.md lines 78-92 and 168-175 — the nine-versus-twelve correction already added, and the run table that still reports 9 `ok` lines.
    - .planning/phases/02-configuration-manifest-and-generator-core/02-DESIGN-G-02-11.md — the ratified design, which determines what the corrected claim should now say.
    - .planning/phases/02-configuration-manifest-and-generator-core/02-08-SUMMARY.md — what plan 02-08 actually shipped.
  </read_first>
  <action>
    In `02-04-SUMMARY.md`, immediately after the desktop-entry emitter paragraph whose closing
    sentence asserts the emitted bytes are independent of where the generator was invoked from, add a
    correction blockquote in the form this plan's `correction_form` section describes. It must say:
    the claim is true of the working directory and false of the repository's location; the emitted
    absolute paths were built from the resolved repository root, so relocating the checkout changed
    the emitted bytes; that this is gap G-02-11 in `02-UAT.md`; and what the ratified design in
    `02-DESIGN-G-02-11.md` did about it, naming the ratified option token. Date it and attribute it
    to the Phase 2 UAT, test 11.

    Also check the two corrections already present — `02-04-SUMMARY.md`'s `--check` absent-case note
    and `02-06-SUMMARY.md`'s nine-versus-twelve note. Confirm each still states what ships today. If
    `02-08` changed a count either note asserts, extend that note rather than adding a third; if
    neither changed, record in the plan summary that both were re-checked and left, so a later reader
    does not read the absence of a diff as an oversight.

    In `02-06-SUMMARY.md`, the "What was run" table still reports the generator's self-test as
    exactly 9 `ok` lines. The existing correction note explains the drift for the fault table above
    it but does not reach that table row. Add a parenthetical to that row pointing at the correction
    note, so the two do not disagree on the same page.

    Change no other sentence in either file. These are executed history.
  </action>
  <verify>
    <automated>grep -c 'Corrected 2026-09' .planning/phases/02-configuration-manifest-and-generator-core/02-04-SUMMARY.md</automated>
    <fails_when>the printed count is less than 2 — the pre-existing absent-case correction plus the new repo-location correction; a count of 1 means the new note was never added</fails_when>
    <automated>grep -q 'G-02-11' .planning/phases/02-configuration-manifest-and-generator-core/02-04-SUMMARY.md</automated>
    <fails_when>non-zero exit — the correction does not name the gap it comes from, so a reader cannot trace it back to the UAT</fails_when>
    <automated>bash -c 'R=$(sed -n "s/^ratified: *//p" .planning/phases/02-configuration-manifest-and-generator-core/02-DESIGN-G-02-11.md); grep -q "$R" .planning/phases/02-configuration-manifest-and-generator-core/02-04-SUMMARY.md'</automated>
    <fails_when>non-zero exit — the correction does not name the ratified option token, so it records that something changed without recording what</fails_when>
    <automated>bash -c 'git diff --numstat -- .planning/phases/02-configuration-manifest-and-generator-core/02-04-SUMMARY.md .planning/phases/02-configuration-manifest-and-generator-core/02-06-SUMMARY.md | awk "{ if (\$2 > 2) exit 1 } END { exit 0 }"'</automated>
    <fails_when>exit 1 — more than two lines were deleted from a summary, which means a claim was rewritten in place instead of corrected additively</fails_when>
  </verify>
  <acceptance_criteria>
    - `02-04-SUMMARY.md` contains at least two blockquote corrections dated 2026-09, and the new one names `G-02-11`, names the ratified option token from `02-DESIGN-G-02-11.md`, and distinguishes the working directory from the repository location in words.
    - The original sentence asserting invocation-directory independence is still present, unmodified — corrections are additive: `git diff` for that file shows added lines only, with at most 2 deletions across both summaries combined.
    - `02-06-SUMMARY.md`'s "What was run" row for the generator self-test carries a pointer to the count correction above it, so the page does not state two different counts without connecting them.
    - `git status --porcelain` after this task lists only files under `.planning/phases/02-configuration-manifest-and-generator-core/`.
    - `scripts/verify-platform.sh --quick` exits 0, unchanged — this task edits no file any registry row reads.
  </acceptance_criteria>
  <done>No summary in this phase directory still asserts that the emitted bytes are independent of where the repository lives, and each corrected claim carries a dated note naming the gap and the ratified design.</done>
</task>

<task type="auto">
  <name>Task 2: Make 02-VERIFICATION.md tell the truth about the two gaps it reported as absent</name>
  <files>.planning/phases/02-configuration-manifest-and-generator-core/02-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/02-configuration-manifest-and-generator-core/02-VERIFICATION.md — the whole file: frontmatter lines 1-8, the observable-truths table, the requirement traceability table at lines 96-105, and `## Gaps Summary` at lines 138-146.
    - .planning/phases/02-configuration-manifest-and-generator-core/02-UAT.md — both gap records in full, including their `severity`, `root_cause` and `missing` lists.
    - .planning/phases/02-configuration-manifest-and-generator-core/02-DESIGN-G-02-11.md — the ratified design and the three `foreign_checkout_*_exit:` values.
    - .planning/phases/02-configuration-manifest-and-generator-core/02-08-SUMMARY.md — the closure evidence: which branch was executed and the observed relocated-checkout exit codes.
    - .planning/REQUIREMENTS.md lines 39-80 and 265-275 — the five phase requirement IDs and the traceability table that marks each Complete, so the correction does not contradict a table it does not own.
  </read_first>
  <action>
    Correct `02-VERIFICATION.md` in four places, additively, in the form this plan's
    `correction_form` section describes.

    1. Frontmatter. `status: passed` and `score: 5/5 must-haves verified` describe a verification run
       that did not look at the foreign-checkout case. Change `status` to `passed-with-corrections`
       and add two keys beside it: `gaps_found_after_verification: [G-02-11, G-02-12]` and
       `gaps_closed_by: [02-08]`. Keep `verified: 2026-09-01` — the date the run happened does not
       move — and add `corrected:` carrying today's date. Do not restate `score` as anything other
       than what that run measured; a score edited after the fact records nothing.

    2. Truth 1's evidence cell. It says a fresh clone was simulated by removing the generated
       directory and that the gate still passed. That simulation exercised the absent-output case and
       not the relocated-checkout case, which is why it could not see G-02-11. Add one sentence to
       that cell naming the distinction and pointing at 02-UAT.md test 11, which ran a genuine clone
       at a different path and found the gate red.

    3. `## Gaps Summary`. Replace nothing; append a subsection `### Gaps found after this report`
       recording both gap IDs, their one-line truths, their severity, the plan that closed them, and
       the ratified design token. State plainly that this report said "No gaps found" while both were
       open, and why the run could not see them: every check it ran, it ran at one checkout path.

    4. `## Anti-Patterns Found`. It reports none, having scanned for placeholder markers. Add a
       sentence recording the anti-pattern the UAT did find and that a marker scan cannot: an
       expectation hand-kept as an absolute literal, which made one gate a tautology with respect to
       location. Name the CLAUDE.md rule it violated — derive from the tree and compare.

    Leave the requirement traceability table alone. G-02-11 and G-02-12 were defects in the GATES
    over CFG-01..04 and GEN-04, not in the requirements themselves; the manifest still expresses the
    whole brand, identity keys still hard-fail, the basename regex still rejects, cosmetic defaults
    still echo, and generated output is still untracked. Marking a requirement incomplete on this
    evidence would misreport the phase in the opposite direction. Say so in the appended subsection so
    the reader knows the table was considered and deliberately left.

    Finally, add one line to `.planning/phases/02-configuration-manifest-and-generator-core/deferred-items.md`
    recording that no Phase 2 deferral remains open after this gap closure, or naming the ones that do.
  </action>
  <verify>
    <automated>grep -q 'passed-with-corrections' .planning/phases/02-configuration-manifest-and-generator-core/02-VERIFICATION.md</automated>
    <fails_when>non-zero exit — the frontmatter still reports an unqualified pass over a period in which two major gaps were open</fails_when>
    <automated>bash -c 'grep -q "G-02-11" .planning/phases/02-configuration-manifest-and-generator-core/02-VERIFICATION.md && grep -q "G-02-12" .planning/phases/02-configuration-manifest-and-generator-core/02-VERIFICATION.md'</automated>
    <fails_when>non-zero exit — the report does not name both gaps, so the phase record still cannot be traced from the verification to the UAT that contradicted it</fails_when>
    <automated>grep -c 'Gaps found after this report' .planning/phases/02-configuration-manifest-and-generator-core/02-VERIFICATION.md</automated>
    <fails_when>the printed count is 0 — the appended subsection is absent; a count above 1 means it was added twice</fails_when>
    <automated>bash -c 'for R in CFG-01 CFG-02 CFG-03 CFG-04 GEN-04; do grep -q "$R" .planning/phases/02-configuration-manifest-and-generator-core/02-VERIFICATION.md || exit 1; done'</automated>
    <fails_when>exit 1 — a phase requirement ID vanished from the traceability table, which the correction was forbidden to touch</fails_when>
    <automated>scripts/verify-platform.sh --quick</automated>
    <fails_when>non-zero exit, or any row printed with FAIL — this task edits only planning documents and must leave the commit gate exactly as it found it</fails_when>
  </verify>
  <acceptance_criteria>
    - `02-VERIFICATION.md` frontmatter carries `status: passed-with-corrections`, a `corrected:` date, `gaps_found_after_verification` naming both gap IDs, and `gaps_closed_by` naming plan 02-08.
    - `verified: 2026-09-01T00:00:00Z` is unchanged — the date the original run happened is a fact, not a claim to correct.
    - A `### Gaps found after this report` subsection exists exactly once and names both gap IDs, both severities, the closing plan, and the ratified option token.
    - Truth 1's evidence cell distinguishes the absent-output simulation from a relocated checkout and points at 02-UAT.md test 11.
    - `## Anti-Patterns Found` names the hand-kept absolute expectation and the CLAUDE.md rule it violated.
    - All five of `CFG-01`, `CFG-02`, `CFG-03`, `CFG-04`, `GEN-04` still appear in the file, and the traceability table's status column is byte-unchanged.
    - `deferred-items.md` gained exactly one line and lost none.
    - `git status --porcelain` lists only files under `.planning/phases/02-configuration-manifest-and-generator-core/`, and `scripts/verify-platform.sh --quick` exits 0.
  </acceptance_criteria>
  <done>The phase's verification report names the two gaps that were open while it reported none, says why its own method could not see them, and points at the plan and the design that closed them.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| phase record -> a future planner or auditor | every later phase reads these documents as settled fact; a wrong one propagates silently |
| UAT gap text -> the corrected report | prior agent output ingested as data, not as instructions |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-23 | Repudiation | 02-VERIFICATION.md `status` | high | mitigate | The status becomes `passed-with-corrections` with both gap IDs and the closing plan named in frontmatter, so no later reader can quote a clean pass over a period that was not clean. |
| T-02-24 | Tampering | executed summaries 02-01..02-06 | medium | mitigate | Corrections are additive blockquotes; the verify asserts at most 2 deleted lines across both summaries, so a rewrite-in-place goes red instead of landing quietly. |
| T-02-25 | Spoofing | requirement traceability table | medium | mitigate | The table is explicitly out of scope for this plan and a verify asserts all five requirement IDs survive, so a correction cannot silently downgrade a requirement the evidence still supports. |
| T-02-26 | Information disclosure | corrected planning prose | low | accept | These files live under `.planning/`, outside the residual-brand scan's scope and outside any shipped artifact. Naming a checkout path in them discloses nothing new. |
| T-02-23-SC | Tampering | package-manager installs | low | accept | This plan installs no package and runs no package manager; the legitimacy gate has nothing to gate. |
</threat_model>

<verification>
- `scripts/verify-platform.sh --quick` exits 0, unchanged by this plan — it edits no file any registry row reads.
- `git status --porcelain` lists only paths under `.planning/phases/02-configuration-manifest-and-generator-core/`.
- `git diff --numstat` shows additions dominating: at most 2 deleted lines across the two summaries, and none in the requirement traceability table.
- Both gap IDs, the closing plan number and the ratified option token are greppable in `02-VERIFICATION.md`.
</verification>

<success_criteria>
- No summary in this phase directory asserts the emitted bytes are independent of where the repository lives.
- `02-VERIFICATION.md` reports `passed-with-corrections`, names G-02-11 and G-02-12, and explains why a single-checkout verification run could not see either.
- Every correction is additive, dated, and attributed, in the form the two pre-existing corrections in this directory already use.
- The five phase requirement IDs still resolve, and none was downgraded on evidence that concerns the gates rather than the requirements.
</success_criteria>

## Artifacts this plan produces

| Kind | Name | Where |
|---|---|---|
| frontmatter key | `status: passed-with-corrections` | 02-VERIFICATION.md |
| frontmatter key | `corrected:` | 02-VERIFICATION.md |
| frontmatter key | `gaps_found_after_verification:` | 02-VERIFICATION.md |
| frontmatter key | `gaps_closed_by:` | 02-VERIFICATION.md |
| new subsection | `### Gaps found after this report` | 02-VERIFICATION.md `## Gaps Summary` |
| correction note | repo location versus working directory | 02-04-SUMMARY.md, beside the desktop-entry emitter paragraph |
| correction pointer | self-test count in the run table | 02-06-SUMMARY.md |
| appended line | Phase 2 deferral status after gap closure | deferred-items.md |

No new script, no new registry row, no new self-test case, no code change of any kind.

## Planner assumptions — spec-less edge probe

All 8 fallback edges are enumerated with their dispositions in `02-08-PLAN.md`'s section of the same
name, which is the single place they are recorded for this gap-closure set. None is dropped; this
plan adds no new edge because it produces no runtime behaviour.

<output>
Create `.planning/phases/02-configuration-manifest-and-generator-core/02-09-SUMMARY.md` when done.
Record in it whether the two pre-existing corrections needed extending or were re-checked and left.
</output>


