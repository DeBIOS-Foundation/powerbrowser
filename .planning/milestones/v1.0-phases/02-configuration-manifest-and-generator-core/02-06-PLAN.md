---
phase: 02-configuration-manifest-and-generator-core
plan: 06
type: execute
wave: 5
depends_on: [02-05]
files_modified:
  - scripts/generate.mjs
  - scripts/verify-platform.sh
  - .github/workflows/rebase-upstream.yml
  - powerbrowser/branding/dev/configure.sh
  - powerbrowser/branding/release/configure.sh
autonomous: true
requirements: [CFG-02, CFG-03, GEN-04]

estimate:
  tokens: 40000
  raw_tokens: 80000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "The generator's --self-test plants a missing required key, an invalid basename, an unknown key, a whitespace-only identity value, a stale generated file, a partial identity table, and a short downstream array, and requires each to go red naming the drift (D-14, CLAUDE.md verification rules)"
    - "An invalid basename is rejected with a message naming the key, quoting the offending value, and stating the character set, the length bounds, and the leading-letter rule in words — and no file is written under generated/ on that run (CFG-03)"
    - "All four generator rows are registered in scripts/verify-platform.sh's --quick array: the freshness check, the byte-identity check, and both self-tests (D-15)"
    - "The rebase workflow runs generate, then generate --check, then the byte-identity check, in that order — the middle step then proves idempotence rather than failing on a git-ignored directory that a fresh clone never has (D-15, Pitfall 1)"
    - "Both hand-written branding configure.sh files and the emitter that reproduces them now carry a header declaring the file generated from configuration.toml, and the byte-identity check is green against the new bytes (D-03 step 2)"
    - "The comment rewrite happened only after the byte-identity check was green against the original bytes — proof before the target moves (D-03)"
  artifacts:
    - path: "scripts/generate.mjs"
      provides: "The completed --self-test case table covering every D-14 fault plus the two pitfall cases, and the rewritten configure.sh header"
      min_lines: 500
    - path: "scripts/verify-platform.sh"
      provides: "Four appended --quick registry rows"
      contains: "generate-check"
    - path: ".github/workflows/rebase-upstream.yml"
      provides: "Three appended run steps in the generate, check, byte-identity order"
      contains: "scripts/generate.mjs"
    - path: "powerbrowser/branding/dev/configure.sh"
      provides: "The dev display-name define under a generated-from-manifest header"
      contains: "configuration.toml"
    - path: "powerbrowser/branding/release/configure.sh"
      provides: "The release display-name define under the same header"
      contains: "configuration.toml"
  key_links:
    - from: "scripts/verify-platform.sh"
      to: "scripts/generate.mjs"
      via: "two appended rows in the --quick CHECKS array running the generator with --check and with --self-test"
      pattern: "generate-check"
    - from: ".github/workflows/rebase-upstream.yml"
      to: "scripts/generate.mjs and scripts/verify-generated-identity.mjs"
      via: "three run steps ordered generate, then check, then byte-identity"
      pattern: "generate\\.mjs"
    - from: "the configure.sh emitter in scripts/generate.mjs"
      to: "powerbrowser/branding/{dev,release}/configure.sh"
      via: "byte-identity: the emitter's header lines and the tracked files' header lines are changed in one commit and compared"
      pattern: "MOZ_APP_DISPLAYNAME"
  prohibitions:
    - requirement_id: CFG-03
      category: safety
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT edit either hand-written branding file to make a check green. Those five hand-written files are the independent comparand this whole phase's acceptance test rests on; the only permitted edit to them in this phase is the header rewrite in this plan, made deliberately and only after the check was green against the original bytes"
---

<objective>
**As a** downstream brand author, **I want to** the generator's own failure modes proved by planted
faults, every gate registered where CI and my commit hook both reach it, and the hand-written files
labelled as generated, **so that** nothing in this pipeline is trusted on a claim rather than on a
red I have actually seen.

This closes the phase. It completes the fault table, wires all four rows into the one registry and
into the rebase workflow, and then — only then — moves the target: the two hand-written
`configure.sh` files stop describing themselves as hand-written.

Purpose: D-03's ordering is the point. The byte-identity proof is established against the original
bytes first; only after it is green does the comment change, and the check must stay green against
the new bytes. A comment rewritten before the proof would leave nothing independent to compare
against.

Output: a complete self-test, four registry rows, three CI steps, and two files that now say what
they are.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/01-platform-extraction-and-rename/01-UI-SPEC.md
@.planning/phases/02-configuration-manifest-and-generator-core/02-CONTEXT.md
@.planning/phases/02-configuration-manifest-and-generator-core/02-RESEARCH.md
@.planning/phases/02-configuration-manifest-and-generator-core/02-PATTERNS.md
@.planning/phases/02-configuration-manifest-and-generator-core/02-05-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Complete the generator's fault table</name>
  <files>scripts/generate.mjs</files>
  <read_first>
    - scripts/generate.mjs (the `--self-test` harness and its three existing cases from plan 02-03; the validator and its failure messages; the `--check` mode from plan 02-04)
    - scripts/verify-registry-shape.mjs (lines 179-249 — the case-table shape being extended)
    - .planning/phases/01-platform-extraction-and-rename/01-UI-SPEC.md (the no-internals copy rule the generator's failure lines must satisfy)
    - .planning/phases/02-configuration-manifest-and-generator-core/02-RESEARCH.md (§Code Examples E-3 and E-6; §Common Pitfalls 2 and 6)
  </read_first>
  <behavior>
    - Removing a required identity key from a fixture drives the check red naming that key.
    - Setting a basename to a two-word value drives it red naming the key AND naming the length
      bounds in words, and leaves `generated/` untouched.
    - Misspelling a section header drives it red naming the misspelled header, not naming a
      correctly-spelled key as missing.
    - Setting a required key to a whitespace-only string drives it red naming that key.
    - Altering one byte in a file under `generated/` drives `--check` red naming that file's path.
    - Deleting `generated/` drives `--check` red with the absent-directory message and without
      listing five paths.
    - A syntactically malformed fixture drives it red naming the file and the line, with no parser
      diagnostic text in the output.
    - Every case's failure output is free of stack traces, host paths, and internal variable names.
  </behavior>
  <action>
    Extend the existing `--self-test` case table from three cases to nine. The harness, the
    clean-baseline guard, the mutation-landed guard, the per-case `ok` line, and the counted PASS
    line are already in place from plan 02-03; this task adds cases, not machinery.

    New cases, each a name, a fixture, and an `expect` string the failure output must contain:

      - `missing required key` — a fixture with a required identity key's line deleted. Expect the
        failure to name that dotted key path. This is CFG-02's core.
      - `invalid basename` — a fixture whose binary name is a two-word value containing a space.
        Expect the failure to name the dotted key path AND to contain the phrase stating the length
        bounds, because the pattern's quantifier is not self-explanatory to a stranger. Assert
        additionally that the run wrote no file under `generated/` — CFG-03's rule is that the
        value is rejected, never transformed into a passing one, and a rejection that still emitted
        output would be a half-rejection.
      - `unknown key` — a fixture whose identity section header is misspelled. Expect the failure to
        name the misspelled header. Assert the output does NOT contain the phrase the
        missing-required-key failure uses, because a misspelled table that survives to the merge
        produces a missing-key error naming a key that is spelled correctly, which sends the author
        looking in the wrong place. This case is what pins the unknown-key check's position ahead of
        the merge.
      - `stale generated output` — generate into a temporary tree, alter one byte of one file, run
        the freshness comparison. Expect the failure to name that file's path.
      - `absent generated directory` — run the freshness comparison against a directory that does
        not exist. Expect the distinct absent-directory message and assert the output does NOT name
        all five target paths.
      - `malformed manifest` — a fixture that is not valid TOML. Expect the failure to name the file
        and the line number. Assert the output contains no parenthesised parser vocabulary, no caret
        character on a line of its own, and no absolute filesystem path outside the manifest's own
        name — this is the case that keeps the parse-failure copy inside the no-internals rule, and
        it is the case most likely to be quietly broken by a future refactor that reaches for the
        caught error's own message.

    Add one cross-cutting assertion applied to every case's output, not a tenth case: no failure line
    contains a stack-trace frame marker, a `node:` module specifier, or the repo's absolute path.
    Implement it as a predicate run over each case's collected output inside the existing loop.

    Do not restructure the harness, do not introduce a test framework, and do not add a third CLI
    flag. This repo's convention is per-script fault planting registered as rows in one driver.
  </action>
  <verify>
    <automated>cd /home/chris/coding/Power-Browser &amp;&amp; node scripts/generate.mjs --self-test &amp;&amp; test "$(node scripts/generate.mjs --self-test | grep -c '^  ok ')" -eq 9 &amp;&amp; node scripts/generate.mjs &amp;&amp; node scripts/generate.mjs --check &amp;&amp; node scripts/verify-generated-identity.mjs &amp;&amp; node scripts/verify-generated-identity.mjs --self-test &amp;&amp; scripts/verify-platform.sh --quick</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/generate.mjs --self-test` exits 0 and prints exactly nine `ok` lines plus a PASS line naming the count 9.
    - The `invalid basename` case's recorded failure output contains the dotted key path and the phrase `2 to 32 characters`, and the case asserts no file was written.
    - The `unknown key` case's recorded failure output names the misspelled header and does not contain the missing-key phrase.
    - The `absent generated directory` case's recorded output does not contain the string `.mozconfig`.
    - `grep -c 'e.message' scripts/generate.mjs` returns 0.
    - Making a case's fixture identical to the clean manifest makes `--self-test` report that case as vacuous rather than passing.
    - Introducing an unrelated failure into the repo manifest makes `--self-test` bail with the already-red-baseline message.
    - `node scripts/generate.mjs --check` and `node scripts/verify-generated-identity.mjs` both still exit 0.
    - `scripts/verify-platform.sh --quick` is fully green.
  </acceptance_criteria>
  <done>Every failure mode CFG-02, CFG-03, D-10, D-12, and GEN-04 promise has been seen to go red naming the drift, and the parse-failure copy is pinned inside the no-internals rule by a case rather than by a reviewer's memory.</done>
</task>

<task type="auto">
  <name>Task 2: Register all four rows and add the three CI steps in the order that makes --check honest</name>
  <files>scripts/verify-platform.sh, .github/workflows/rebase-upstream.yml</files>
  <read_first>
    - scripts/verify-platform.sh (the `--quick` CHECKS array head, the two rows plan 02-05 appended, and the dispatcher's constraint that a command string must be a bare shell function name or a `bash <path>` / `node <path>` invocation)
    - .github/workflows/rebase-upstream.yml (the "Scan for residual brand strings" step at lines 68-69 — the analog for a plain `run:` gate step, and the only gate the workflow currently has)
    - .planning/phases/02-configuration-manifest-and-generator-core/02-RESEARCH.md (§Code Examples E-5; §Common Pitfalls 1, point 3; §Validation Architecture, Wave 0 Gaps)
  </read_first>
  <action>
    Registry. Append two more rows to the `--quick` array beside the pair plan 02-05 added, in the
    same `label|command` form with a repo-root-absolute node invocation:

      - label `generate-check`, running `scripts/generate.mjs --check`
      - label `generate-self-test`, running `scripts/generate.mjs --self-test`

    Four generator rows total. All belong in `--quick`: no build, no browser, no display, no network.
    Give the new pair an adjacent comment in the house style stating what each asserts and why the
    self-test rides alongside. Note in that comment that `generate-check` asserts IDEMPOTENCE — that
    a second run produces the same bytes as the first — because on a tree where the generator has
    already run, that is the only thing it can honestly assert. Labels are the surface `--only`
    resolves against; do not rename them later.

    CI. The rebase workflow currently has no verify driver invocation at all; its only gate is the
    residual-brand scan step. So this is three NEW steps, not an extension of an existing one. Append
    them after the residue-scan step, each a `name:` and a `run:` in the file's existing style, in
    exactly this order:

      1. generate — run `node scripts/generate.mjs`
      2. check freshness — run `node scripts/generate.mjs --check`
      3. byte identity — run `node scripts/verify-generated-identity.mjs`

    The order is load-bearing and deserves a comment in the workflow saying why. `generated/` is
    git-ignored, so a fresh clone — which every workflow run is — has no such directory. Running the
    freshness check first would report all five targets as stale on a tree with no defect. Running it
    after a generate makes it assert that two consecutive runs produce identical bytes, which is a
    real property worth gating and is the only thing it can truthfully assert in CI. The byte-identity
    step is last and is independent of both: it emits into its own temporary directory and compares
    against the tracked files, so it would pass even if the first two steps had not run.

    No `./mach build` step. Nothing this phase produces reaches the compiler — every build consumer
    still reads the hand-written files. A build step here would cost forty-plus minutes and gate
    nothing new.

    Do not create a sibling driver script and do not add a `verify-phase-*` file. One driver, one
    registry.
  </action>
  <verify>
    <automated>cd /home/chris/coding/Power-Browser &amp;&amp; scripts/verify-platform.sh --only generate-check &amp;&amp; scripts/verify-platform.sh --only generate-self-test &amp;&amp; scripts/verify-platform.sh --only generated-byte-identity &amp;&amp; scripts/verify-platform.sh --only generated-byte-identity-self-test &amp;&amp; test "$(grep -c 'node \$REPO_ROOT/scripts/\(generate\|verify-generated-identity\)\.mjs' scripts/verify-platform.sh)" -eq 4 &amp;&amp; python3 -c "import sys,re;t=open('.github/workflows/rebase-upstream.yml').read();i=[t.index(s) for s in ['node scripts/generate.mjs','node scripts/generate.mjs --check','node scripts/verify-generated-identity.mjs']];sys.exit(0 if i==sorted(i) else 1)" &amp;&amp; test -z "$(ls scripts/verify-phase-*.sh 2>/dev/null)" &amp;&amp; scripts/verify-platform.sh --quick</automated>
  </verify>
  <acceptance_criteria>
    - All four `--only` invocations above exit 0 and each summary names its label verbatim.
    - `scripts/verify-platform.sh` contains exactly four rows invoking the two new scripts, all in the `node $REPO_ROOT/scripts/...` form.
    - Each new row pair carries an adjacent comment naming what it asserts and why it is quick.
    - `.github/workflows/rebase-upstream.yml` contains three new `run:` steps and they appear in the order generate, then check, then byte identity.
    - The workflow carries a comment stating why the freshness check runs after the generate rather than before it.
    - The workflow contains no `mach build` invocation.
    - `ls scripts/verify-phase-*.sh` finds nothing — no sibling driver was created.
    - `scripts/verify-platform.sh --quick` is fully green.
  </acceptance_criteria>
  <done>Every gate this phase built is reachable by `--only`, runs in the commit gate, and runs in CI in the order that lets each one assert something true.</done>
</task>

<task type="auto">
  <name>Task 3: Move the target — the hand-written files declare themselves generated</name>
  <files>scripts/generate.mjs, powerbrowser/branding/dev/configure.sh, powerbrowser/branding/release/configure.sh</files>
  <precondition>`scripts/verify-platform.sh --only generated-byte-identity` exits 0 against the ORIGINAL bytes before this task changes anything — D-03 requires the proof to be established before the comparand is edited.</precondition>
  <read_first>
    - powerbrowser/branding/dev/configure.sh (all 8 lines, current bytes)
    - powerbrowser/branding/release/configure.sh (all 8 lines, current bytes)
    - scripts/generate.mjs (the configure.sh emitter's literal header lines)
    - scripts/verify-branding-preflight.mjs (lines 186-200 — confirm the only thing it reads from configure.sh is the display-name define, so a comment change cannot disturb it)
    - .planning/phases/02-configuration-manifest-and-generator-core/02-CONTEXT.md (D-03, and the Integration Points note that only the comment changes)
  </read_first>
  <action>
    <!-- planner-discipline-allow: hand-written (plan 01-03) -->
    <!-- planner-discipline-allow: A COMPILED define -->
    This is D-03 step 2, and it runs only now, because the byte-identity check had to be green
    against the original bytes first. Establish that green explicitly before editing: run the
    byte-identity row and confirm exit 0. If it is red, stop — the comparand is not editable yet.

    Then make one commit that changes three things together, so no intermediate state has the emitter
    and the tracked files disagreeing:

      1. In `scripts/generate.mjs`, replace the three literal comment lines the configure.sh emitter
         currently reproduces — the block naming the plan that hand-wrote the define and the rebuild
         cost — with a three-line header that says the file is generated from `configuration.toml` by
         `scripts/generate.mjs` and must not be edited, that the way to change it is to edit
         `configuration.toml` and re-run the generator, and that
         `scripts/verify-platform.sh --only generated-byte-identity` goes red if this file and the
         generator disagree. Keep it to three comment lines so the file stays eight lines. Keep the
         Mozilla Public License boilerplate on lines 1 to 3 and the blank line 4 exactly as they are:
         they are a source-file licence notice, not a rebrand input, and not part of this rewrite.

      2. Apply the identical three-line replacement to `powerbrowser/branding/dev/configure.sh`.

      3. Apply it to `powerbrowser/branding/release/configure.sh`.

    Both tracked files carry the same comment block, so both get the same replacement; the only line
    that differs between them is the display-name define, which this task does not touch.

    Then re-run the byte-identity check. It must be green against the NEW bytes. If it is red, the
    emitter's replacement and the tracked files' replacement disagree — fix the disagreement, do not
    revert the tracked files, and do not adjust the check.

    Make no other edit to either tracked file. The display-name define, the licence header, and the
    blank line are untouched, so `verify-branding-preflight.mjs` — which reads only the define — and
    the inventory's brand-display expectations both keep passing unchanged.

    Stage everything new and changed across the whole phase before running the residue scan. Five
    files created in this phase are new to the tree, and that scan derives its file set from
    `git ls-files`, so an unstaged file is invisible to it and a green scan over the new generator
    source would prove nothing.
  </action>
  <verify>
    <automated>cd /home/chris/coding/Power-Browser &amp;&amp; node scripts/generate.mjs &amp;&amp; node scripts/verify-generated-identity.mjs &amp;&amp; node scripts/verify-generated-identity.mjs --self-test &amp;&amp; cmp generated/branding/dev/configure.sh powerbrowser/branding/dev/configure.sh &amp;&amp; cmp generated/branding/release/configure.sh powerbrowser/branding/release/configure.sh &amp;&amp; test "$(wc -l &lt; powerbrowser/branding/dev/configure.sh)" -eq 8 &amp;&amp; grep -q 'MOZ_APP_DISPLAYNAME="Power Browser Dev"' powerbrowser/branding/dev/configure.sh &amp;&amp; grep -q 'MOZ_APP_DISPLAYNAME="Power Browser"' powerbrowser/branding/release/configure.sh &amp;&amp; node scripts/verify-branding-preflight.mjs &amp;&amp; node scripts/verify-branding-preflight.mjs --self-test &amp;&amp; git add -A &amp;&amp; node scripts/scan-brand-residue.mjs &amp;&amp; scripts/verify-platform.sh --quick</automated>
    <human-check>
      Read `powerbrowser/branding/dev/configure.sh` and confirm the new three-line header reads as
      instructions a stranger could follow: it names the file to edit instead, names the command to
      re-run, and names the check that catches a disagreement. Confirm it carries no internal
      identifier a downstream author has no use for. Then run `node scripts/generate.mjs` on a
      manifest with a deliberately wrong binary name and confirm the failure message reads like
      documentation for `configuration.toml` rather than like a parser: it names the key, quotes what
      you wrote, states the rule in words, and offers a value that would work.
    </human-check>
  </verify>
  <acceptance_criteria>
    - `scripts/verify-platform.sh --only generated-byte-identity` exited 0 BEFORE this task's edits, and the summary records that it was run and its result.
    - Both `cmp` comparisons of the generated configure.sh files against the tracked ones exit 0 after the rewrite.
    - Both tracked files are still 8 lines and their first four lines are byte-unchanged.
    - `grep -c 'configuration.toml' powerbrowser/branding/dev/configure.sh` returns at least 1, and the same for the release file.
    - `grep -c 'hand-written (plan 01-03)' powerbrowser/branding/dev/configure.sh` returns 0, and the same for the release file.
    - The display-name define line is unchanged in both files.
    - `node scripts/verify-branding-preflight.mjs` and its `--self-test` both exit 0.
    - `node scripts/scan-brand-residue.mjs` exits 0 with every new file staged.
    - `scripts/verify-platform.sh --quick` is fully green.
  </acceptance_criteria>
  <done>The two files Phase 1 wrote by hand now say they are generated, the generator agrees with them byte for byte, and the proof that made the move safe was established before the move rather than after.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planted fault → gate verdict | The self-test is the only evidence any of these gates can go red. If it is vacuous, every green above it is unsupported. |
| CI step ordering → CI verdict | A step run in the wrong order reports a defect that is not there, which trains readers to ignore the workflow. |
| hand-written comparand → acceptance test | Editing the comparand is how a byte-identity test is silently made to agree with anything. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-18 | Spoofing | the generator's `--self-test` as a vacuous instrument | high | mitigate | Nine planted faults, each required to go red naming the specific drift; a clean-baseline guard; a mutation-landed guard per case; a cross-cutting predicate asserting no case's output carries a stack frame, a node module specifier, or the repo's absolute path. The self-test has its own registry row so it runs in the commit gate. |
| T-02-03 | Information Disclosure | parse-failure and validation copy | low | mitigate | The `malformed manifest` case asserts the output contains no parser vocabulary, no standalone caret, and no absolute filesystem path; the cross-cutting predicate applies the same rule to every case. This moves the no-internals rule from a reviewer's memory to a check. |
| T-02-19 | Tampering | editing the hand-written comparand to make a check green | high | mitigate | Task 3 requires the byte-identity row to be green against the ORIGINAL bytes before any edit, and requires the emitter and both tracked files to change in one commit. The failure banner in `verify-generated-identity.mjs` states that the emitter is what changes, not the comparand. An acceptance criterion pins the display-name define and the first four lines as unchanged. |
| T-02-20 | Repudiation | a CI gate red for a reason that is not a defect | medium | mitigate | The workflow steps run generate, then check, then byte-identity, with a comment stating why. The freshness check then asserts idempotence rather than reporting five phantom stale paths on a fresh clone. |
</threat_model>

<flagged_assumptions>
1. **Nothing installs the desktop files.** Research verified by absence that no script in this repo
   copies the `.desktop` files into a user applications directory. If the operator installed them by
   hand, the installed copy is stale relative to `generated/` — but D-01 keeps every consumer on the
   hand-written copies this phase, so nothing changes for them until Phase 3 or 5 repoints consumers.
   Recorded as an accepted residual rather than mitigated.

2. **The rewritten header names a registry label.** The new comment cites
   `generated-byte-identity`, which makes that label a citation surface in a shipped file as well as
   in the registry. Renaming it later would require editing two tracked branding files. Accepted:
   naming the check that catches a disagreement is what makes the header actionable, and the label
   was already declared non-renameable in plan 02-05.
</flagged_assumptions>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Created by this plan:

- Six additional `--self-test` cases in `scripts/generate.mjs`, named `missing required key`, `invalid basename`, `unknown key`, `stale generated output`, `absent generated directory`, `malformed manifest` — bringing the table to nine — plus a cross-cutting no-internals predicate applied to every case's output.
- Registry labels `generate-check` and `generate-self-test` in `scripts/verify-platform.sh`'s `--quick` array.
- Three `run:` steps in `.github/workflows/rebase-upstream.yml`, ordered generate, check, byte identity.
- A three-line generated-from-manifest header, present identically in the configure.sh emitter and in both tracked branding `configure.sh` files.

Phase-wide registry labels after this plan: `generate-check`, `generate-self-test`,
`generated-byte-identity`, `generated-byte-identity-self-test`.
</artifacts_this_phase_produces>

<verification>
- `node scripts/generate.mjs --self-test` exits 0 with nine `ok` lines.
- All four `scripts/verify-platform.sh --only <label>` invocations exit 0.
- Both `configure.sh` comparisons against the tracked files exit 0 after the header rewrite.
- `node scripts/verify-branding-preflight.mjs` and its `--self-test` both exit 0 — the preflight is
  untouched by this phase apart from the mark path in plan 02-02.
- `node scripts/scan-brand-residue.mjs` exits 0 with every file this phase created staged.
- `scripts/verify-platform.sh --quick` is fully green.
- No `./mach build` is run anywhere in this phase.
</verification>

<success_criteria>
1. Every failure mode the phase promises has been observed going red naming its drift.
2. All four gates are registered in the one registry and run in CI in an order where each asserts
   something true.
3. The two hand-written branding files declare themselves generated, and the generator still
   reproduces them byte for byte.
4. The proof preceded the move.
</success_criteria>

<output>
Create `.planning/phases/02-configuration-manifest-and-generator-core/02-06-SUMMARY.md` when done.
</output>
