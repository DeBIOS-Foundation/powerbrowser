# Milestone v1.0: PowerBrowser

**Status:** ✅ SHIPPED 2026-09-04 (override closeout — see Known Gaps)
**Phases:** 1–7
**Total Plans:** 52 (52 executed, SUMMARY.md for every plan)
**Stats:** 326 commits (68 `feat(`), 352 files changed, +93,852/−34, ~33.5k LOC (JS/TS/py/sh, excl. `upstream/`), 2026-08-22 → 2026-09-04
**Closeout type:** override — no milestone audit file; phase verifications `human_needed` (all static checks green, human/tier-3 drills staged); 16/31 requirements unchecked (deferred verification batch, not missing code)
**Tag:** v1.0

## Overview

Power Browser starts as a copy of the sourcerer platform tree and ends as a
platform a stranger can rebrand by editing one file. The journey has a
deliberate shape: first the tree is extracted and renamed to fixed platform
identifiers with **no generator at all** — branding values hand-written — so
that the rename either works or doesn't, with nothing else to blame. Then the
`configuration.toml` schema and generator arrive, and their acceptance test is
free: generated output must be byte-identical to the files Phase 1 wrote by
hand. From there the two emitter halves (Firefox branding + icons, Theia
branding + extensions + telemetry) fill in every remaining surface, the patch
stack is de-configured into hook-only form, verification is rebuilt to read
expectations from the manifest instead of constants, and finally Sourcerer is
reproduced as a pure downstream — the milestone's real acceptance test, gated
by adversarial fixtures so the platform can't have been accidentally built
around one friendly config.

Final state at close: all 52 plans executed; `verify-platform.sh --quick`
95 PASS, 0 FAIL; 19/19 launch-lifecycle checks green on the dev binary
(WINDOWS #11 + #19, run 2026-09-04); trademark human ritual signed
2026-09-04 (Chris, CONFIRMED, `brand/HUMAN-REVIEW.md`). Remaining open items
are human-eyes/heavy-machine drills (icon pixel look, welcome/about render
drill, release build, per-fixture tier-3 builds, live ESR rebase, Theia
re-pin) plus code deviations WINDOWS #13/#14 — carried to v2 (see
`.planning/NEXT-MILESTONE-INPUTS.md`).

## Phases

### Phase 1: Platform Extraction and Rename

**Goal**: The Power Browser platform tree exists in this repo, builds, boots, and works as an actual web browser under fixed platform identifiers — with no generator involved
**Mode:** mvp
**Depends on**: Nothing (first phase)
**Requirements**: MIG-01, MIG-02, MIG-03, MIG-04, GUI-01, GUI-03, GUI-04, SEC-01
**Plans**: 21/21 executed (20 executed + 01-21 closing the About-dialog suppression gate gap)

Key outcomes: token-classification inventory + red-scan evidence; full rename
(`powerbrowser/` tree, `@powerbrowser/*`, `PowerBrowserAPI.sys.mjs`,
`chrome://powerbrowser/`); hand-written branding literals + placeholder power-glyph
mark; walking-skeleton Gecko build (MIG-04 closed on artifact evidence,
~47m tier-3 build); GUI-01 browser-window toggle (land-as-spiked);
GUI-04 registry-shape assertion (GUI-02 deferred to v2 at D-22 gate);
user-facing error copy + supervisor hardening across gap-closure waves 8–21
(state-keying, terminal handler, retry/recovery-probe classification,
checker self-test hardening, About-dialog debranding).

### Phase 2: Configuration Manifest and Generator Core

**Goal**: A downstream author can express their entire brand in `configuration.toml` + `brand/`, and one generator turns it into the cheap build surfaces
**Mode:** mvp
**Depends on**: Phase 1
**Requirements**: CFG-01, CFG-02, CFG-03, CFG-04, GEN-04
**Plans**: 9/9 executed (6 + 3 gap-closure for G-02-11/G-02-12)
**Status in tooling:** complete (2026-09-04) — the only phase marked complete

Key outcomes: vendored TOML parser; parse→schema→validate→emit→write
pipeline; required-key hard-fail + binary-name validation; defaults layer
that is itself a `configuration.toml` with sorted echo; five build surfaces
from one frozen target table; `generate --check` (fresh/stale/absent/
leftover); byte-identity gate proving generated output equals Phase 1
hand-written files; full `--self-test` + CI wiring. CFG-01 marked complete.

### Phase 3: Firefox Branding Emitter and Icon Pipeline

**Goal**: Every Gecko-side branding surface — branding directory, icon set, installer fields — is materialized from the manifest
**Mode:** mvp
**Depends on**: Phase 2
**Requirements**: GEN-01, GEN-02, GEN-03
**Plans**: 4/4 executed (03-01 tracer + branding dir, 03-02 icons, 03-03
installer schema, 03-04 identity carrier + patch 010 de-configuration +
tier-3 proof; ROADMAP's "3/4" checkbox was stale — 03-03 SUMMARY exists)

Key outcomes: complete branding dir per variant via overlay symlink +
agreement checker; five exact PNG rasters per variant + pure-Node ICO/ICNS
writers + IHDR registry check; 8 installer fragments (NSIS/MSIX/plist/tile)
schema-complete with sink guards; identity carrier as generated
`identity.configure` include hook; single-edit propagation proof; tier-3
dev-variant build green with six identity surfaces passing. Windows/macOS
host builds remain v2 PKG-01 scope per the requirement text.

### Phase 4: Theia Surface — Branding, Extensions, Telemetry

**Goal**: Everything on the Theia side — product branding, declared extensions, telemetry delivery — is driven by `configuration.toml` with no TypeScript recompile for a rebrand
**Mode:** mvp
**Depends on**: Phase 2 (parallelizable with Phase 3)
**Requirements**: GEN-05, EXT-01, TEL-01, TEL-02, TEL-03
**Plans**: 4/4 executed

Key outcomes: Theia branding via generated frontend-config runtime channel
(logo as SVG text); extension declarations (Open VSX/URL + pin, `--packed`
download, strict empty-means-no-block, loud failures); telemetry sender
(batched, bounded retry, level-honoring, fail-closed) with `off` default;
endpoint allowlist derived from the manifest; Mozilla telemetry/crash prefs
blanked per manifest. Live render + delivery drills staged for human.

### Phase 5: Hook-Only Patches and Upstream Uptake

**Goal**: The patch stack carries no brand values, and adopting an upstream release means editing one pin
**Mode:** mvp
**Depends on**: Phase 3
**Requirements**: MIG-05, CFG-06, UPD-01, UPD-02
**Plans**: 4/4 executed

Key outcomes: patches carry hooks only (MOZ_APP_ID GUID retained as stock
context); ESR + Theia pins schema-required in `configuration.toml` and
consumed by fetch/build scripts; tag-literal sweep; rebase/conflict tooling
with drift classes (conflict / silent-adoption / unaccounted-dirt /
surface-regression) each red-capable. Live rebase + tier-3 build staged
unexecuted (needs a next ESR tag).

### Phase 6: Two-Layer Verification and Rebranding Docs

**Goal**: The build proves its own branding correctness for any downstream, and a stranger has a document that carries them through a full rebrand
**Mode:** mvp
**Depends on**: Phase 4, Phase 5
**Requirements**: VER-01, VER-02, DOC-01
**Plans**: 6/6 executed

Key outcomes: VER-01 manifest-derived brand-literal scan (committed
slot-keyed allowlist, stale-entry fails); VER-02 six-surface runtime check
re-sourced to manifest expectations; trademark gate with self-test +
signed `brand/HUMAN-REVIEW.md` (Chris, 2026-09-04, CONFIRMED); legal
notices on the runtime channel; `docs/REBRANDING.md` stranger walkthrough
with schema-derived coverage gate; CI wiring. Release + CI-runtime drills
staged; human trademark ritual closed at milestone close.

### Phase 7: Sourcerer as Downstream

**Goal**: A stranger's config, living in their own repo, produces their fully branded browser from an untouched platform tree
**Mode:** mvp
**Depends on**: Phase 6
**Requirements**: CFG-05, VER-03, DOC-02
**Plans**: 4/4 executed

Key outcomes: `PB_CONFIG_DIR` external-config resolution; synthetic
Sourcerer-equivalent downstream proven end-to-end (emitted shell, desktop,
locale, installer bytes exact); four adversarial fixtures (spaced name,
late-sort name, non-square logo, missing key) each building/verifying or
failing with the intended error; both-layers-pass proof formalized;
REBRANDING.md carry-tested against a fixture; tier-3 per-fixture drills staged.

## Milestone Summary

**Key Decisions** (see PROJECT.md Key Decisions table for the full log):
- Phase 1 ships with no generator — byte-identity acceptance for Phase 2
- `configuration.toml` (TOML) as the single rebrand file; identity/legal keys hard-fail, cosmetic keys default with echo
- Internal identifiers fixed forever (`chrome://powerbrowser/`, `@powerbrowser/*`, pref branches, API names)
- Telemetry via Theia's real enum (off/crash/error/all, default off) + downstream endpoint; Power Browser implements the destination
- Extensions declared with id + source + pin; fail loudly
- Product name from v2: canonical display **PowerBrowser** (accepted variants Powerbrowser/powerbrowser); v1 shipped spaced "Power Browser" (recorded in NEXT-MILESTONE-INPUTS.md §1)

**Issues Resolved:**
- Supervisor start-path defect class (state-keying, terminal handler, retry/probe classification) closed across waves 9–13 with registered red-capable scenarios
- Checker green-by-construction class closed (brand scan reconciliation, error-copy rule enumeration, extra-root coincidental filtering, About-dialog link coverage per variant)
- Fresh-clone incoherence narrowed; byte-identity gate compares tracked files, never gitignored output

**Issues Deferred (carried to v2):**
- Heavy drills: release build + release-variant rows (WINDOWS #10), per-fixture tier-3 builds, live ESR rebase, Theia re-pin proof
- Code deviations: WINDOWS #13 (registerWindowActor boundary hole), #14 (BiDi double-window)
- Human-eyes: icon pixel look (03), welcome/about render drill (04)

**Technical Debt Incurred:**
- `scripts/generate.mjs` single-file growth (all emitters + self-tests in one file)
- WR-04 (NSIS sink guard misses bare `$VAR`), WR-07 (installer verifier reads live manifest) — queued for Phase 08
- 03-03 ROADMAP checkbox left stale (plan executed, box unchecked)

**Decimal Phases:** none.

---

*Archived from `.planning/ROADMAP.md` at v1.0 close. For current project status, see `.planning/ROADMAP.md`. Next-milestone inputs: `.planning/NEXT-MILESTONE-INPUTS.md`.*
