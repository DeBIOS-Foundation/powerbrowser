# Requirements Archive — v1.0 PowerBrowser (shipped 2026-09-04, override closeout)

Final checkbox states as shipped. Checked = satisfied with evidence cited.
Unchecked = code complete and statically green where stated, but the
human/tier-3 verification half is staged, not run — carried to v2 (see Known
Gaps). Nothing below was cut from scope except GUI-02 (deferred to v2 at the
D-22 gate, 2026-08-30).

## Final outcomes by requirement

| Requirement | Phase | Final | Outcome |
|-------------|-------|-------|---------|
| MIG-01 | 1 | Open | Tree migrated; `upstream/` re-fetch via script proven, no copied objdirs — formal sign-off staged with tier-3 drills |
| MIG-02 | 1 | Open | Token inventory committed and reconciled; carried as process evidence, box left for audit pass |
| MIG-03 | 1 | Complete | Fixed platform identifiers everywhere |
| MIG-04 | 1 | Complete | Tier-3 Linux build + boot + six identity surfaces green (01-04) |
| GUI-01 | 1 | Open | Toggle landed + automated window checks green (incl. 2026-09-04 batch); manual UAT passed 2026-09-01, formal box staged |
| GUI-02 | v2 | Deferred | mini-browser rejected; `<xul:browser>` direction recorded |
| GUI-03 | 1 | Open | Customize bridge shipped; automatable halves green; perceptual UAT passed 2026-09-01, formal box staged |
| GUI-04 | 1 | Open | Registry-shape assertion green; landability statement staged for audit |
| SEC-01 | 1 | Complete | Satisfied by `theia/extensions/token-gate` |
| CFG-01 | 2 | Complete | — |
| CFG-02 | 2 | Complete | — |
| CFG-03 | 2 | Complete | — |
| CFG-04 | 2 | Complete | — |
| GEN-04 | 2 | Complete | — |
| GEN-01 | 3 | Open | Emitter + carrier + tier-3 dev build green; release-variant half staged |
| GEN-02 | 3 | Open | Rasters exact + shipped in build; visual pixel sign-off staged (human) |
| GEN-03 | 3 | Open | 8 fragments schema-complete + Linux build-verified; Windows/macOS host builds are v2 PKG-01 by requirement text |
| GEN-05 | 4 | Open | Runtime-channel branding green statically; live render drill staged (human) |
| EXT-01 | 4 | Open | Declaration + pin gates green statically; bundle+load drill staged |
| TEL-01 | 4 | Open | Schema + generated off-by-default green; live drill staged |
| TEL-02 | 4 | Open | Sender unit suite 5/5 + gates green; live delivery drill staged |
| TEL-03 | 4 | Open | Allowlist derivation + Mozilla pref repointing green; installed-binary layer staged |
| MIG-05 | 5 | Complete | Hook-only patches, hash chain intact |
| CFG-06 | 5 | Complete | Pins declared once, consumed by scripts |
| UPD-01 | 5 | Complete (static) | Tooling + drift classes red-capable; live rebase drill staged (needs next ESR tag) |
| UPD-02 | 5 | Complete (static) | Re-pin procedure documented; live proof staged (needs nix Theia shell) |
| VER-01 | 6 | Open | Scan + stale-entry failure green; full-fleet proof staged with fixtures |
| VER-02 | 6 | Complete | Six surfaces by exact equality from manifest expectations |
| DOC-01 | 6 | Open | REBRANDING.md + coverage gate green; stranger carry-test recorded, formal box staged |
| CFG-05 | 7 | Complete | PB_CONFIG_DIR external-config builds proven |
| VER-03 | 7 | Complete (static) | Adversarial fixtures green via harness; tier-3 per-fixture builds staged |
| DOC-02 | 7 | Complete | Sourcerer-equivalent downstream proven from untouched tree |

## Known Gaps (override closeout)

1. No milestone audit file (`v1.0-MILESTONE-AUDIT.md` never created).
2. Phase verifications all `human_needed`, none `passed`.
3. Human-eyes: 03 icon pixel look; 04 welcome/about render drill (trademark ritual closed 2026-09-04).
4. Heavy-machine: release `objdir-release` build + release-variant rows (WINDOWS #10); tier-3 per-fixture builds; live ESR rebase; Theia re-pin proof.
5. Code deviations: WINDOWS #13 (registerWindowActor boundary hole), #14 (BiDi double-window).
6. 03-03 ROADMAP checkbox stale (executed, box unchecked) — cosmetic.

## v1 Requirements (body as shipped, checkboxes frozen)

### Migration & Debrand (MIG)

- [ ] **MIG-01**: Platform code is migrated from the sourcerer tree into this
      repo with `upstream/` re-fetched via script (never copied) and no
      objdirs copied

- [ ] **MIG-02**: A committed token-classification inventory (brand /
      identity / frozen / coincidental) exists before any rename is executed

- [x] **MIG-03**: All internal identifiers are renamed to fixed platform
      names — `powerbrowser/` tree, `@powerbrowser/*` extension scope,
      `PowerBrowserAPI.sys.mjs`, `chrome://powerbrowser/` — and these never
      vary per downstream

- [x] **MIG-04**: The renamed tree builds and boots on Linux under Power
      Browser branding (proven by the existing smoke tests)

- [x] **MIG-05**: Brand values are removed from `patches/*.patch` (hook-only
      patches that `include()` generated files), preserving the 3-way-merge
      hash chain

### Configuration Manifest (CFG)

- [x] **CFG-01**: A downstream author can define their entire brand in
      `configuration.toml` + a `brand/` assets folder and touch no other file
- [x] **CFG-02**: Identity fields (vendor, app basename, binary name,
      remoting name) and legal fields are required — generation hard-fails
      with a clear message when any is unset
- [x] **CFG-03**: Binary/basename fields are validated
      (`^[a-z][a-z0-9-]{1,31}$`) so no invalid `MOZ_APP_NAME` can be produced
- [x] **CFG-04**: Cosmetic fields fall back to Power Browser defaults with a
      visible echo at generate time; the defaults are themselves a
      `configuration.toml` (one merge code path)
- [x] **CFG-05**: A downstream can point the build at an external config dir
      (`PB_CONFIG_DIR`) so distributions like Sourcerer live in their own repo
- [x] **CFG-06**: Upstream pins (Firefox ESR tag, Theia release) are declared
      in `configuration.toml` and consumed by the fetch/build scripts

### Brand Generator (GEN)

- [ ] **GEN-01**: A single generator script materializes the complete Firefox
      branding directory (brand.ftl / brand.properties / brand.dtd emitted
      atomically and cross-checked), `.mozconfig`, desktop files, and
      `generated/identity.configure` from `configuration.toml`
- [ ] **GEN-02**: The generator derives all required Linux icon sizes
      (16/32/48/64/128) from the source SVG/PNG in `brand/`
- [ ] **GEN-03**: The generator emits installer branding for Linux, Windows
      (NSIS/MSIX fields), and macOS (DMG/.icns fields) from
      `configuration.toml` so Power Browser installs anywhere Firefox
      installs; Linux output is build-verified in v1, Windows/macOS outputs
      are generated and schema-complete with builds verified when those
      packaging hosts land
- [x] **GEN-04**: Generated output is never committed (single gitignored
      `generated/` root) and `generate --check` verifies freshness in CI
- [ ] **GEN-05**: Theia-side branding (welcome tab, about dialog, product
      name, logo, default theme) is applied via frontend config keys — no
      TypeScript recompile for a rebrand

### Browser GUI (GUI)

- [ ] **GUI-01**: The user can toggle away from Theia to proper web-browser
      UI and back
- [ ] **GUI-03**: The GUI customization bridge (runtime CSS layer +
      dev-flagged privileged JS) ships as a platform feature
- [ ] **GUI-04**: Nothing in v1 welds Theia to full-window presentation; the
      future unified tab strip remains landable without rework

### Security (SEC)

- [x] **SEC-01**: The Theia backend is unreachable without a per-launch
      credential, and fails closed rather than degrading to open
      (satisfied by `theia/extensions/token-gate`)

### Telemetry (TEL)

- [ ] **TEL-01**: `configuration.toml` declares telemetry level
      (off/crash/error/all, default off) and the downstream's own endpoint
- [ ] **TEL-02**: Full telemetry pipeline (batched, retrying, level-honoring;
      nothing sent when off)
- [ ] **TEL-03**: Telemetry/URL hosts flow into the generated endpoint
      allowlist; Mozilla telemetry/crash endpoints repointed or disabled per
      the manifest

### Extensions (EXT)

- [ ] **EXT-01**: A downstream can declare Theia extensions in
      `configuration.toml` with source (Open VSX id or direct URL) and pin;
      they are downloaded and bundled into the sidecar at build time

### Verification (VER)

- [ ] **VER-01**: Static brand-literal scan with committed scope list and
      boundary-matched tokens; stale allowlist entry also fails
- [x] **VER-02**: Runtime verification checks the six branding surfaces by
      exact equality, reading expectations from `configuration.toml`
- [x] **VER-03**: Adversarial fixture configs build and verify correctly

### Upstream Uptake (UPD)

- [x] **UPD-01**: Firefox ESR point release adopted by bumping the pin +
      re-running fetch + patch apply (live drill staged)
- [x] **UPD-02**: Theia release adopted by re-pinning; core never forked or
      patched (live proof staged)

### Documentation & Downstream Proof (DOC)

- [ ] **DOC-01**: `docs/REBRANDING.md` walks a stranger through a complete
      rebrand with every field documented
- [x] **DOC-02**: Sourcerer reproduced as a pure downstream with zero
      platform-file edits

## v2 Requirements (deferred, carried forward)

- **GUI-02**: In-Theia web tabs (`<xul:browser>`-backed direction)
- **GUI-05**: Unified tab strip (chrome-owned tab model, mirror/proxy bridge)
- **PKG-01**: Windows and macOS installer builds verified on real packaging hosts
- **EXT-02**: npm and local-path extension source kinds
- **TEL-04**: Crash-report pipeline beyond endpoint repointing
- **NAME-01** (new): canonical display form **PowerBrowser** applied across
  all generated surfaces with gates re-pinned (see NEXT-MILESTONE-INPUTS.md §1)

## Out of Scope (unchanged)

- Databasise and the curated addon set — downstream compositions
- Forking/patching Theia core or modifying Gecko outside the patch stack
- Per-downstream internal identifiers — fixed forever
- Moving every conceivable Firefox pref into `configuration.toml`
