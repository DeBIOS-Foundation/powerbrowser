---
phase: 09-extensions-crash-pipeline
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [scripts/crash-collector.mjs, scripts/verify-crash-collector.mjs, theia/extensions/telemetry/test/telemetry-sender.test.mjs, docs/CRASH-POLICY.md, scripts/verify-platform.sh]
autonomous: true
requirements: [TEL-04]
estimate:
  tokens: 27500
  raw_tokens: 55000
  tasks: 3
  confidence: high
must_haves:
  truths:
    - "A well-formed multipart crash submit returns a CrashID response and a matching local store record"
    - "A malformed submit is rejected with a named discard reason and no store write"
    - "An over-throttle submit is soft-rejected while staying on the success status code"
    - "Error events at the crash level ride the ping sender while minidump bytes never enter that sender"
  artifacts:
    - path: "scripts/crash-collector.mjs"
      provides: "stdlib-only Antenna-protocol crash collector"
      exports: ["parseMultipart", "buildCrashIdResponse", "buildDiscardResponse"]
    - path: "scripts/verify-crash-collector.mjs"
      provides: "collector contract gate with self-test"
      contains: "upload_file_minidump"
    - path: "docs/CRASH-POLICY.md"
      provides: "written PII retention throttle policy"
      contains: "retention"
    - path: "theia/extensions/telemetry/test/telemetry-sender.test.mjs"
      provides: "ping versus report separation cases"
      contains: "crash"
  key_links:
    - from: "scripts/verify-crash-collector.mjs"
      to: "scripts/crash-collector.mjs"
      via: "contract gate imports the exported pure functions, never a kept copy"
      pattern: "parseMultipart"
    - from: "theia/extensions/telemetry/test/telemetry-sender.test.mjs"
      to: "theia/extensions/telemetry/src/browser/telemetry-sender.ts"
      via: "separation cases assert the level gate admits crash error events only"
      pattern: "levelAllowsEvent"
    - from: "scripts/verify-platform.sh"
      to: "scripts/verify-crash-collector.mjs"
      via: "registry CHECKS pair rows drive the gate and its self-test"
      pattern: "crash-collector"
  prohibitions:
    - "No re-enabling the native crash reporter and no new Gecko patch surface for crashes"
    - "No unbounded minidump retention and no unlisted annotation fields stored"
    - "No Socorro self-host and no archived mini-breakpad-server adoption"
  assumptions:
    - "The about:crashes surface half is proven through the collector round-trip (submit, CrashID response, local record) per the locked acceptance shape, never through a native crash, since the reporter stays compiled out"
    - "The collector binds loopback by default; any network exposure needs token auth and is out of scope for this plan"
---

<objective>
Ship the minimal Antenna-protocol crash collector as zero-dependency downstream-ops tooling with its contract gate, the written PII/retention/throttle policy, and the crash-ping versus crash-report separation cases.

Purpose: Give downstreams their own crash endpoint under a reviewable policy while the native reporter stays compiled out, per locked decision TEL-04.
Output: Collector script, contract gate with self-test, policy doc, separation tests, registry rows, loopback round-trip proof.
</objective>

<execution_context>
<purpose>
Execute a phase prompt (PLAN.md) and create the outcome summary (SUMMARY.md).
</purpose>

<required_reading>
Read STATE.md before any operation to load project context.
Read config.json for planning behavior settings.
</required_reading>
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-extensions-crash-pipeline/09-RESEARCH.md
@.planning/phases/09-extensions-crash-pipeline/09-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Collector with pure contract surface and bounded ingestion</name>
  <files>scripts/crash-collector.mjs</files>
  <read_first>scripts/verify-mar-update-hop.mjs reporter plus single-source emission idiom, theia/extensions/telemetry/src/browser/telemetry-sender.ts zero-dependency header, scripts/verify-theia-endpoints.mjs CLI plus root constants region</read_first>
  <action>Create the collector importing only node stdlib modules so the contract suite runs under plain node with no install, exporting pure functions for multipart parsing and for the CrashID and Discarded response builders with the minidump part name fixed to the Antenna contract value, wrapping them in a thin loopback-bound HTTP layer that caps part count, part size, and total body with an oversized-body rejection, rejects non-multipart content with a named malformed reason, scopes all writes to its store dir, and answers throttled submits with the soft-reject body on the success status. Keep user-facing strings in the product voice with plain language and a real next step, identifiers to diagnostics only, and stage the new file before trusting any residue scan.</action>
  <acceptance_criteria>Module imports under plain node; the three contract functions are exported and callable; a well-formed synthetic part set yields a CrashID body; a missing minidump part yields a named malformed reason; loopback bind is the default with no network exposure flag on</acceptance_criteria>
  <verify>
    <automated>node --check scripts/crash-collector.mjs && node -e "import('./scripts/crash-collector.mjs').then(m => { for (const f of ['parseMultipart','buildCrashIdResponse','buildDiscardResponse']) if (typeof m[f] !== 'function') throw new Error('missing export ' + f); console.log('CONTRACT_EXPORTS_OK'); })"</automated>
    <fails_when>node --check exits nonzero, or any named export is not a function, or the smoke line is absent from output</fails_when>
  </verify>
  <done>Collector module exists with an importable pure contract surface and bounded loopback ingestion</done>
</task>

<task type="auto">
  <name>Contract gate plus written PII retention throttle policy</name>
  <files>scripts/verify-crash-collector.mjs, docs/CRASH-POLICY.md</files>
  <read_first>scripts/verify-mar-update-hop.mjs assertion plumbing region, scripts/verify-extension-pins.mjs self-test control-green-first idiom, scripts/verify-theia-endpoints.mjs arg parsing plus PASS FAIL line shape</read_first>
  <action>Create the contract gate in the shared assertion-plumbing shape with the standard CLI root constants and NAME-prefixed result lines, testing the exported functions in process with byte fixtures in a temp dir and no sockets: well-formed input answers the CrashID body, each malformed class answers its named discard reason on the rejection status, over-cap input answers the oversized rejection, and a tripped throttle rule answers the soft-reject body on the success status, with control-green-first and every plant asserting red naming its rule. Write the policy doc first as the reviewable source the code enforces, stating the annotation allowlist, the bounded retention window with deletion, and the throttle rules modeled on the Antenna throttler, plus the ping-versus-report routing that sends error events to the telemetry endpoint and minidumps to this collector.</action>
  <acceptance_criteria>Gate self-test passes with one plant per contract direction each red naming its rule; policy doc states an annotation allowlist, a retention window with a deletion step, and throttle rules; no plant passes vacuously on empty input</acceptance_criteria>
  <verify>
    <automated>node scripts/verify-crash-collector.mjs --self-test</automated>
    <fails_when>Self-test exits nonzero, or any plant goes green when it must go red, or the control is red before planting</fails_when>
  </verify>
  <done>Contract behavior is pinned by a discriminating gate and the enforced policy exists as a reviewable doc</done>
</task>

<task type="auto">
  <name>Ping report separation cases plus registry rows plus loopback round-trip</name>
  <files>theia/extensions/telemetry/test/telemetry-sender.test.mjs, scripts/verify-platform.sh</files>
  <read_first>theia/extensions/telemetry/test/telemetry-sender.test.mjs full suite plus stub discrimination idiom, theia/extensions/telemetry/src/browser/telemetry-sender.ts level gate function, scripts/verify-platform.sh extension-pins row pair plus honesty-note comment convention</read_first>
  <action>Extend the existing plain-node suite without adding a second sender class: error events at the crash level are admitted to the ping endpoint, usage events at the crash level stay dropped, unknown levels fail closed to the off behavior, and minidump bytes have no path into the sender with the report path asserted as the collector contract. Append the registry CHECKS pair for the new gate with the per-pair comment naming what it asserts differently from the rows above plus the honesty note, then prove the loop by starting the collector on loopback, posting one synthetic multipart submit, and asserting the CrashID response plus the matching local store record file.</action>
  <acceptance_criteria>Suite passes unmodified-behavior cases plus the four new separation cases; the stub-discrimination run still goes red on the off-sends-nothing assertion; registry carries the collector gate pair rows; one loopback round-trip shows a CrashID response and a store record file with the matching id</acceptance_criteria>
  <verify>
    <automated>node theia/extensions/telemetry/test/telemetry-sender.test.mjs && node scripts/verify-telemetry.mjs --self-test && node scripts/verify-crash-collector.mjs --self-test && scripts/verify-platform.sh --quick</automated>
    <fails_when>Any suite or self-test exits nonzero, or the stub run stays green, or --quick reports a red row</fails_when>
  </verify>
  <done>Ping and report paths are separated by test, registered in the single driver, and proven over loopback</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→collector | Multipart crash submits cross from untrusted submitters into the store |
| collector→disk | Parsed minidumps and annotations land in the store dir |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-04 | Denial of service | collector HTTP layer (crash-collector.mjs) | high | mitigate | Part-count, part-size, and total-body caps with oversized rejection plus throttle rules answering soft-reject |
| T-09-05 | Information disclosure | stored crash records | high | mitigate | Written annotation allowlist, bounded retention with deletion, loopback bind by default; no full-dump retention |
| T-09-06 | Tampering | malformed multipart input | medium | mitigate | Strict parser rejecting non-multipart and part-name violations with named discard reasons and no store write |
| T-09-SC | Tampering | npm installs | low | accept | Collector is stdlib-only by design; no package is installed, so no legitimacy gate fires |
</threat_model>

<verification>
Contract self-test plus telemetry suite plus stub discrimination plus the commit gate, with one loopback submit-to-record round-trip.
</verification>

<success_criteria>
Collector answers the Antenna contract over loopback, malformed and throttled inputs get their documented answers, the policy doc states allowlist retention and throttle, pings and reports stay separated, and the native reporter path is untouched.
</success_criteria>

<artifacts_this_phase>
This plan produces: scripts/crash-collector.mjs collector; scripts/verify-crash-collector.mjs contract gate; docs/CRASH-POLICY.md written policy; telemetry separation test cases; verify-platform.sh collector registry rows; loopback round-trip evidence.
</artifacts_this_phase>

<output>
Create `.planning/phases/09-extensions-crash-pipeline/09-02-SUMMARY.md` when done
</output>
