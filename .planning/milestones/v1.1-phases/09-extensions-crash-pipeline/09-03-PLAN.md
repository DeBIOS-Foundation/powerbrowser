---
phase: 09-extensions-crash-pipeline
plan: 03
type: execute
wave: 2
depends_on: ["09-01"]
files_modified: [scripts/generate.mjs, scripts/lib/config-schema.json, scripts/verify-webextensions.mjs, powerbrowser/distribution/policies.json, scripts/verify-platform.sh]
autonomous: true
requirements: [EXT-03]
estimate:
  tokens: 25000
  raw_tokens: 50000
  tasks: 3
  confidence: high
must_haves:
  truths:
    - "A manifest webextensions table emits an ExtensionSettings fragment matching the tracked policies.json key"
    - "A drifted ExtensionSettings key fails the agreement gate naming the add-on id"
    - "An install_url origin missing from allowlist coverage fails naming the host"
    - "The tracked policies.json carries only declared add-ons with the three existing keys byte-identical"
  artifacts:
    - path: "scripts/generate.mjs"
      provides: "ExtensionSettings emitter plus fragment row"
      contains: "emitWebExtensionSettings"
    - path: "scripts/verify-webextensions.mjs"
      provides: "webextensions agreement gate with self-test"
      contains: "ExtensionSettings"
    - path: "powerbrowser/distribution/policies.json"
      provides: "tracked ExtensionSettings consumer key"
      contains: "ExtensionSettings"
  key_links:
    - from: "scripts/generate.mjs"
      to: "generated/webextensions-settings.json"
      via: "new TARGETS fragment row, no tracked comparand"
      pattern: "webextensions-settings"
    - from: "generated/webextensions-settings.json"
      to: "powerbrowser/distribution/policies.json"
      via: "surgical copy setting only the ExtensionSettings key"
      pattern: "ExtensionSettings"
    - from: "scripts/verify-webextensions.mjs"
      to: "scripts/generate.mjs"
      via: "gate derives through the generator emitter, never a kept copy"
      pattern: "emitWebExtensionSettings"
  prohibitions:
    - "No extension binaries or XPIs vendored into the platform tree; mechanism only, zero bundled add-ons"
    - "No distribution extensions sideload directory; the policy engine path is the only install route"
    - "No AMO publication requirement for downstream XPIs; self-hosted install_url is the sanctioned path"
  assumptions:
    - "The ESR unsigned-install mechanism is confirmed against the pinned ESR tag during execution; if the exemption does not hold at this tag, the gate header records AMO-unlisted-signed as the alternative posture instead of stalling"
    - "Allowlist-origin coverage is proven through fixture manifests in the gate self-test; no synthetic hosts land in the tracked allowlist, which stays free of fixture data"
    - "The root manifest ships zero webextensions entries per the curated-list bar, so the tracked key rests at the empty state and every non-empty proof rides fixtures"
---

<objective>
Ship the WebExtensions declaration sibling: a manifest webextensions table emitted as ExtensionSettings into the tracked policies.json with an agreement gate, closing the STATE.md pending todo mechanism half.

Purpose: Give downstreams the sanctioned force and normal install path with update semantics from the policy engine, without vendoring any add-on into the tree.
Output: Schema keys, emitter plus fragment row, surgical copy-over, agreement gate with self-test, registry rows, ESR signing confirmation.
</objective>

<execution_context>
<purpose>
Execute a phase prompt (PLAN.md) and create the outcome summary (SUMMARY.md).
</purpose>

<required_reading>
Read STATE.md before any operation to load project context.
Read config.json for planning behavior settings.
</required_reading>
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-extensions-crash-pipeline/09-RESEARCH.md
@.planning/phases/09-extensions-crash-pipeline/09-PATTERNS.md
@.planning/phases/09-extensions-crash-pipeline/09-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Webextensions schema keys plus emitter plus surgical copy-over</name>
  <files>scripts/lib/config-schema.json, scripts/generate.mjs, powerbrowser/distribution/policies.json</files>
  <reversibility rating="costly">Tracked policies.json is read by the Gecko policy engine and the MAR-hop gate; changing the ExtensionSettings key shape later touches both consumers at once.</reversibility>
  <read_first>scripts/lib/config-schema.json telemetry level enum precedent without schema regex, scripts/generate.mjs emitTheiaPlugins plus TARGETS frozen-row shape plus mozillaEndpointPrefs splice idiom, powerbrowser/distribution/policies.json current three keys plus MAR-hop gate policy read</read_first>
  <action>Add the webextensions table keys in the established four-field shape with the add-on id, an installation mode validated by the validator rather than a schema regex following the telemetry level precedent, and an install URL accepting the two sanctioned schemes, then add the emitter that builds the ExtensionSettings object keyed by add-on id with an empty table emitting the empty object, register the derived-only fragment row with no tracked comparand following the theiaPlugins pattern, and extend the copy-over so it sets only the ExtensionSettings key leaving the three existing keys byte-identical. Reject unknown installation modes and malformed URLs with failures naming the add-on id, pass every emitted value through the sink guard, and never emit the curated list or the welcome-widget discovery sketch, both out of scope.</action>
  <acceptance_criteria>Schema carries the three webextensions keys; unknown mode and malformed URL fixtures fail naming the add-on id; fragment for a two-entry fixture manifest matches the tracked key exactly; the three pre-existing policy keys are byte-identical after copy-over; empty table yields the empty object on both sides</acceptance_criteria>
  <verify>
    <automated>node scripts/generate.mjs --self-test && node scripts/generate.mjs --check</automated>
    <fails_when>Either command exits nonzero, or --check reports stale or leftover paths, or the tracked key differs from the derived fragment</fails_when>
  </verify>
  <done>Manifest webextensions declarations flow into the tracked ExtensionSettings key with siblings preserved</done>
</task>

<task type="auto">
  <name>Agreement gate with both-direction plants and allowlist leg</name>
  <files>scripts/verify-webextensions.mjs</files>
  <read_first>scripts/verify-theia-endpoints.mjs full gate derive plus fresh-clone SKIP plus self-test shape, scripts/verify-telemetry.mjs fragment-absent SKIP plus block-equality failure wording, scripts/verify-extension-pins.mjs entry-naming hash-mismatch message</read_first>
  <action>Create the agreement gate importing the new emitter so expectations derive at check time through the generator, with the absent-fragment SKIP that never fails on a fresh clone while the tracked-key equality half always runs, an allowlist leg that requires every install_url origin derived from the manifest to carry coverage and fails naming the host, and a stale-key check that fails naming the add-on id when the tracked key outlives its manifest entry. Prove discrimination with control-green-first and one plant per direction using synthetic Acme fixtures only, each asserting red naming its defect, and stage the new file before trusting any residue scan.</action>
  <acceptance_criteria>Unmutated control is green; drifted tracked side goes red naming the add-on id; drifted fragment side goes red naming the add-on id; removed-entry stale key goes red naming the id; uncovered fixture origin goes red naming the host; full self-test exits zero</acceptance_criteria>
  <verify>
    <automated>node scripts/verify-webextensions.mjs --self-test</automated>
    <fails_when>Self-test exits nonzero, or any plant goes green when it must go red, or a failure message omits the add-on id or host</fails_when>
  </verify>
  <done>Tracked key, derived fragment, and origin coverage agree with discriminating proof</done>
</task>

<task type="auto">
  <name>Registry rows plus ESR signing confirmation plus full EXT-03 proof</name>
  <files>scripts/verify-platform.sh</files>
  <read_first>scripts/verify-platform.sh theia-endpoints row pair plus per-pair comment convention, upstream preference defaults for the signature requirement at the pinned tag, powerbrowser/endpoint-allowlist.json AMO deny entry</read_first>
  <action>Append the registry CHECKS pair for the new gate with the per-pair comment naming what it asserts differently from the rows above plus the honesty note, then confirm the self-hosted install mechanism against the pinned ESR tag by locating the signature-requirement exemption in the upstream tree and recording the confirmed mechanism string in the gate header, falling back to recording the unlisted-signed alternative posture there if the exemption does not hold at this tag. Finish with the full proof: generator check, both self-tests, the new registry rows, the endpoint coverage rows, and the commit gate, leaving the tracked allowlist free of fixture hosts and the root manifest free of bundled entries per the mechanism-only scope.</action>
  <acceptance_criteria>Registry carries the webextensions gate pair rows; gate header names the confirmed signing mechanism string for the pinned tag; generator check plus both self-tests plus the new rows plus the endpoint rows plus --quick are all green; tracked allowlist holds no fixture hosts</acceptance_criteria>
  <verify>
    <automated>node scripts/generate.mjs --check && node scripts/verify-webextensions.mjs --self-test && scripts/verify-platform.sh --only webextensions-self-test && scripts/verify-platform.sh --only theia-endpoints && scripts/verify-platform.sh --quick</automated>
    <fails_when>Any command exits nonzero, or the new registry label is unknown to the driver, or any row reports red</fails_when>
  </verify>
  <done>EXT-03 rides the single driver with the install mechanism confirmed and the pending-todo mechanism half closed</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| manifest→policies | Declared add-on ids, modes, and URLs flow into enforced browser policy |
| policy→network | install_url origins become fetchable update sources at runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-07 | Elevation of privilege | ExtensionSettings emission (generate.mjs) | high | mitigate | Explicit installation_mode per declared id with deny-by-default posture documented; only declared ids land, proven by the stale-key plant |
| T-09-08 | Tampering | install_url values (validator plus allowlist leg) | high | mitigate | Scheme-restricted URLs through the sink guard plus origin-coverage requirement failing naming the host |
| T-09-09 | Elevation of privilege | unsigned XPI sideload outside policy | medium | mitigate | Sideload directory rejected by locked decision; ESR signing posture confirmed and recorded rather than silent |
| T-09-SC | Tampering | npm installs | low | accept | No package is installed by this plan; nothing to legitimacy-gate |
</threat_model>

<verification>
Generator check plus webextensions self-test plus endpoint rows plus the commit gate, with the tracked key equal to the derived fragment.
</verification>

<success_criteria>
Declared WebExtensions land as ExtensionSettings through the one-fragment path, drift fails naming the add-on, origins carry coverage, the install mechanism is confirmed at the pinned tag, and the tree holds zero bundled add-ons.
</success_criteria>

<artifacts_this_phase>
This plan produces: webextensions schema keys; ExtensionSettings emitter plus fragment row; surgical copy-over into policies.json; verify-webextensions.mjs agreement gate; registry rows; recorded ESR signing mechanism.
</artifacts_this_phase>

<output>
Create `.planning/phases/09-extensions-crash-pipeline/09-03-SUMMARY.md` when done
</output>
