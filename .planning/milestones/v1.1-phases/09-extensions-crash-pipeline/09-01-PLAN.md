---
phase: 09-extensions-crash-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [scripts/generate.mjs, scripts/lib/config-schema.json, scripts/verify-extension-pins.mjs]
autonomous: true
requirements: [EXT-02]
estimate:
  tokens: 25000
  raw_tokens: 50000
  tasks: 3
  confidence: high
must_haves:
  truths:
    - "A manifest declaring one synthetic npm entry and one synthetic local-path entry generates a theiaPlugins fragment carrying pinned URLs for both"
    - "A floated npm tarball URL fails the pin gate naming the entry id"
    - "An absent local-path directory fails the pin gate naming the entry id"
    - "A theiaPlugins URL carrying the target placeholder reaches the fragment byte-verbatim, never expanded to the generator host platform"
  artifacts:
    - path: "scripts/lib/config-schema.json"
      provides: "npm and local-path extension key shapes"
      contains: "extensions[].integrity"
    - path: "scripts/generate.mjs"
      provides: "npm/local-path validation plus resolver branches"
      contains: "local-path"
    - path: "scripts/verify-extension-pins.mjs"
      provides: "per-kind archive handling plus npm/local-path self-test plants"
      contains: "local-path"
  key_links:
    - from: "scripts/lib/config-schema.json"
      to: "scripts/generate.mjs"
      via: "validator reads SCHEMA_KEYS, never its own key copy"
      pattern: "SCHEMA_KEYS"
    - from: "scripts/generate.mjs"
      to: "generated/theia-plugins.json"
      via: "emitTheiaPlugins resolver dispatch, one URL per entry"
      pattern: "emitTheiaPlugins"
    - from: "generated/theia-plugins.json"
      to: "scripts/verify-extension-pins.mjs"
      via: "fragment equality plus per-kind hash proof"
      pattern: "fragment equality"
  prohibitions:
    - "No npm entry may resolve latest or a version range; exact pins only"
    - "No generate-time expansion of the downloader target placeholder into a platform literal"
    - "No local-path pin over a raw directory listing; packed bytes only"
  assumptions:
    - "A4 npm-pack byte-stability is MEDIUM-low risk: the verifier packs in one controlled step, and per-machine drift is caught as a loud pin failure, never silent adoption"
    - "Live registry download is out of scope for this tracer; network proof rides plan 09-04 over the same resolver code"
---

<objective>
Static EXT-02 tracer: one synthetic npm entry and one synthetic local-path entry through schema, validation, resolution, fragment emission, and the pin gate, with zero network and zero new registry rows.

Purpose: Prove the EXT-01 chain extension points carry the two new source kinds end to end before the live collector and heavy fixture builds land.
Output: Extended schema keys, validator and resolver branches, per-kind pin handling with self-test plants, all gates green.
</objective>

<execution_context>
<purpose>
Execute a phase prompt (PLAN.md) and create the outcome summary (SUMMARY.md).
</purpose>

<required_reading>
Read STATE.md before any operation to load project context.
Read config.json for planning behavior settings.
</required_reading>
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-extensions-crash-pipeline/09-RESEARCH.md
@.planning/phases/09-extensions-crash-pipeline/09-PATTERNS.md
</context>

<tasks>

<task type="tracer">
  <name>End-to-end synthetic npm plus local-path slice through schema, resolver, and pin gate</name>
  <files>scripts/lib/config-schema.json, scripts/generate.mjs, scripts/verify-extension-pins.mjs</files>
  <read_first>scripts/lib/config-schema.json extensions block, scripts/generate.mjs EXTENSION_SOURCES plus validateExtensionElements plus emitTheiaPlugins, scripts/verify-extension-pins.mjs archiveSuffix plus step structure plus self-test fixture idiom</read_first>
  <action>Extend the frozen source allowlist to the four kinds and update the trailing later-phase hint at the unknown-source failure since the later phase is now. Add conditional-pin branches so npm demands an exact version that rejects latest and range characters while local-path demands a path whose absence fails naming the entry id, with every failure naming the entry through the existing label helper and every new manifest value passing the sink guard. Add resolver branches in the same dispatch position as the existing URL forms so npm emits the registry tarball verbatim and local-path emits its packed-archive reference, extending the entry-shape guard alongside the allowlist so the two cannot disagree, and extend the archive-suffix resolver plus the float guard so npm pins compare tarball-URL equality against the recorded URL while local-path re-packs in one controlled step and hashes those bytes. Carry one synthetic Acme npm entry and one synthetic Acme local-path entry through the generator self-test fixture block and the pin-gate self-test plants, keeping the one-entry-per-line fragment shape the copy-over parses. Per D-02 of the locked pre-decisions the exact-pin plus integrity-digest plus fail-loud shape is non-negotiable.</action>
  <acceptance_criteria>EXTENSION_SOURCES frozen array holds all four kinds; an npm entry stating latest fails naming its id; a local-path entry with a missing path fails naming its id; the emitted fragment carries a literal downloader placeholder for a target-specific entry with no platform literal from the generator host; node scripts/generate.mjs --self-test passes; node scripts/verify-extension-pins.mjs --self-test passes with control-green-first and each new plant red naming its defect</acceptance_criteria>
  <verify>
    <automated>node scripts/generate.mjs --self-test && node scripts/verify-extension-pins.mjs --self-test && scripts/verify-platform.sh --quick</automated>
    <fails_when>Any of the three commands exits nonzero, or the pin-gate self-test output lacks its planted-fault PASS line, or --quick reports a red row</fails_when>
  </verify>
  <done>The synthetic two-kind slice generates, resolves, and pin-verifies end to end with mismatch plants failing loud, and the commit gate stays green</done>
</task>

<task type="auto">
  <name>Schema key shapes for npm integrity and local-path plus webextensions-ready conditional logic</name>
  <files>scripts/lib/config-schema.json, scripts/generate.mjs</files>
  <read_first>scripts/lib/config-schema.json extensions id source version url sha256 key block, scripts/generate.mjs conditional-pin validation region plus per-key shape loop plus schema header comment forbidding a second key list</read_first>
  <action>Bring the schema block to full coverage of the two kinds using the same four-field key shape as the existing keys: an integrity key in SRI form for npm, a relative-path key in the branding-dir style for local-path, keeping id source and sha256 untouched and keeping conditional-required logic in the validator rather than required flags. Mirror each new key with a per-key shape check in the validator loop and a conditional-pin branch that states exactness for npm and presence for local-path, so schema and validator agree through the single key list.</action>
  <acceptance_criteria>Schema block carries integrity and path keys in the four-field shape; validator conditional branches reference exactly the same key names with no second list; malformed integrity and absolute-path fixtures each fail naming the entry id</acceptance_criteria>
  <verify>
    <automated>node scripts/generate.mjs --self-test && node scripts/generate.mjs --check</automated>
    <fails_when>Either command exits nonzero, or --check reports stale or leftover paths on the unmutated tree</fails_when>
  </verify>
  <done>Schema and validator accept exactly the well-formed new-kind shapes and reject malformed ones naming the entry</done>
</task>

<task type="auto">
  <name>Pin-gate plants for npm float and local-path absence with byte-identity held</name>
  <files>scripts/verify-extension-pins.mjs</files>
  <read_first>scripts/verify-extension-pins.mjs full gate steps one through three plus float-guard region plus self-test writeFixture reset idiom</read_first>
  <action>Extend the gate in place preserving the three-step structure and the kind-agnostic fragment and block equality halves, adding the npm tarball-URL equality check against the recorded URL and the local-path absent-or-unpackable failure naming the entry id, then add one self-test plant per direction starting each plant block from the fixture reset with synthetic Acme data only. Keep the existing direct-URL precedent comment mirrored for hash-only kinds and leave the registry rows untouched since the existing extension-pins pair already drives this gate.</action>
  <acceptance_criteria>Self-test shows control green on the unmutated tree first, then the npm-float plant red naming the entry id and the local-absence plant red naming the entry id, and the full self-test exits zero</acceptance_criteria>
  <verify>
    <automated>node scripts/verify-extension-pins.mjs --self-test && scripts/verify-platform.sh --only extension-pins-self-test</automated>
    <fails_when>Self-test exits nonzero, or any plant goes green when it must go red, or the control is red before planting</fails_when>
  </verify>
  <done>Per-kind pin failures discriminate with entry-naming errors and the registered rows stay green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| registry→generator | npm registry metadata and tarballs cross into pinned manifest state |
| manifest→build | Pinned URLs and digests flow into downloaded plugin bytes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-01 | Tampering | npm-kind resolver (generate.mjs) | high | mitigate | Exact version pin plus tarball-URL equality against the recorded URL plus human review at pin time; hash alone is not the control |
| T-09-02 | Tampering | pin gate (verify-extension-pins.mjs) | high | mitigate | sha256 over packed bytes at verify time catches registry republication under a stable version |
| T-09-03 | Tampering | npm version field (validator) | medium | mitigate | latest and range characters rejected at validation; unpinned resolution cannot reach the fragment |
| T-09-SC | Tampering | npm installs | low | accept | No registry dependency is installed by this plan; resolution shells out to the host CLI, so there is no new supply-chain surface to gate |
</threat_model>

<verification>
node scripts/generate.mjs --self-test plus pin-gate self-test plus the commit gate, all green on the tracer slice.
</verification>

<success_criteria>
Synthetic npm and local-path entries generate pinned fragment lines, floated and absent inputs fail loud naming the entry, the placeholder passes through verbatim, and no live network was touched.
</success_criteria>

<artifacts_this_phase>
This plan produces: extended config-schema.json npm and local-path key shapes; generate.mjs validator plus resolver branches for the two kinds; verify-extension-pins.mjs per-kind handling with two new self-test plants; synthetic Acme fixture entries proving the slice.
</artifacts_this_phase>

<output>
Create `.planning/phases/09-extensions-crash-pipeline/09-01-SUMMARY.md` when done
</output>
