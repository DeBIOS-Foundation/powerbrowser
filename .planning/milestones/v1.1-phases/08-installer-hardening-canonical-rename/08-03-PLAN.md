---
phase: 08-installer-hardening-canonical-rename
plan: "03"
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - scripts/check-internals-boundary.sh
  - scripts/lib/firefox-bidi.mjs
  - scripts/verify-platform.sh
  - .planning/WINDOWS.md
autonomous: true
requirements: [SEC-02, SHELL-01]
estimate:
  tokens: 25000
  raw_tokens: 50000
  tasks: 2
  confidence: high
must_haves:
  truths:
    - "A registerWindowActor call outside the boundary file fails the guard naming file and pattern"
    - "BiDi evaluate and screenshot run against the URL-bearing context, not the shell context"
    - "WINDOWS ledger entries 13 and 14 are closed with their gates green"
  artifacts:
    - path: "scripts/check-internals-boundary.sh"
      provides: "registerWindowActor in forbidden patterns plus self-test plant"
      contains: "registerWindowActor"
    - path: "scripts/lib/firefox-bidi.mjs"
      provides: "URL-matched context selection with logged fallback"
      contains: "topLevelContexts"
    - path: ".planning/WINDOWS.md"
      provides: "Ledger entries 13 and 14 marked closed with gate evidence"
      contains: "CLOSED"
  key_links:
    - from: "scripts/check-internals-boundary.sh"
      to: "powerbrowser/INTERNAL-APIS.md"
      via: "any boundary-file occurrence needs a catalogue row; zero WindowActor occurrences today"
      pattern: "WindowActor"
    - from: "scripts/lib/firefox-bidi.mjs"
      to: "scripts/verify-platform.sh"
      via: "app-check callers launch with no URL or assert the matched context"
      pattern: "_run_app_check_mjs"
  prohibitions:
    - statement: "The boundary fix must not add a chrome-scheme exemption for actor registration; the entry is unconditional"
      verification: backstop
    - statement: "The BiDi fix must not kill the shell context or change launch semantics; it only changes which context evaluate targets"
      verification: backstop
    - statement: "Neither fix may widen into GUI work; no chrome UI, no new user-facing surface"
      verification: backstop
  flagged_assumptions:
    - "SEC-02/boundary: probe asks about exact min/max thresholds — the guard is a string-pattern boundary with no numeric threshold; a one-line pattern addition is the whole fix, assumed sufficient with the plant as proof"
    - "SEC-02/precision: probe asks about precision loss or rounding contracts — no numeric computation is involved; assumed not applicable, recorded here rather than dropped silently"
    - "SHELL-01/unclassified: edge probe unclassified, reviewed manually — two-context selection assert plus single-context positive control are the coverage; additional BiDi edge shapes beyond URL match plus empty-URL fallback are accepted as harness coverage"
---

<objective>
Close WINDOWS 13 (registerWindowActor boundary hole, SEC-02) and 14 (BiDi double-window context mis-resolution, SHELL-01) with green self-test gates, keeping the internals-boundary single-touchpoint invariant.

Purpose: Hardening fixes land alongside the rename with pre-written in-tree fix directions, no GUI scope.
Output: Guard pattern plus plant, URL-matched context selection, closed ledger entries.
</objective>

<execution_context>
<purpose>Execute this PLAN.md, commit per task, create SUMMARY.md.</purpose>
<required_reading>
Read .planning/STATE.md before any operation. SEC-02 must keep the single-touchpoint invariant: one boundary file, catalogue consistency.
Commits: one per task as fix, then a docs close-out with SUMMARY.md.
</required_reading>
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08-installer-hardening-canonical-rename/08-RESEARCH.md
@.planning/phases/08-installer-hardening-canonical-rename/08-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Close 13: forbid registerWindowActor outside the boundary with a self-test plant</name>
  <read_first>scripts/check-internals-boundary.sh lines 36-56 and 237-255; scripts/verify-gui01-window.mjs lines 170-185; powerbrowser/INTERNAL-APIS.md catalogue shape</read_first>
  <files>scripts/check-internals-boundary.sh, .planning/WINDOWS.md</files>
  <action>Add ChromeUtils.registerWindowActor to the forbidden-patterns list in scripts/check-internals-boundary.sh as an unconditional entry with no chrome-scheme exemption, mirroring the existing Services entry shape, and add a self-test plant fixture carrying that call which must be rejected with the file and pattern named. Confirm zero WindowActor occurrences in the boundary file and catalogue so no catalogue row is owed, and flip ledger entry 13 to closed with the gate evidence cited.</action>
  <acceptance_criteria>The pattern list contains the registerWindowActor entry unconditionally; the self-test plant is rejected naming file and pattern; boundary file and catalogue gain no new touchpoint; ledger 13 reads closed with the passing gate named; --self-test and --catalogue modes both pass.</acceptance_criteria>
  <verify>
    <automated>bash scripts/check-internals-boundary.sh --self-test && scripts/verify-platform.sh --only internals-boundary</automated>
    <fails_when>The self-test exits non-zero, or the --only row reports anything but PASS</fails_when>
  </verify>
  <done>Actor registration outside the single boundary file fails loud with plant proof</done>
</task>

<task type="auto">
  <name>Fix 14: select the BiDi context by URL and align the app-check callers</name>
  <read_first>scripts/lib/firefox-bidi.mjs lines 282-289, 350-390; scripts/verify-platform.sh lines 248-265</read_first>
  <files>scripts/lib/firefox-bidi.mjs, scripts/verify-platform.sh, .planning/WINDOWS.md</files>
  <action>Replace the first-context pin in the BiDi helper with selection by requested URL through the already-exported topLevelContexts helper, falling back to the first context with a logged note only when no URL was passed, keeping process hygiene and launch semantics untouched. Update the app-check caller functions that boot a dev app they do not read to launch with an empty URL or assert against the matched context, and add a two-context session assertion proving the URL-bearing context is selected with a single-context positive control. Flip ledger entry 14 to closed with the gate evidence cited.</action>
  <acceptance_criteria>Evaluate and screenshot target the URL-matched context in a two-context session; single-context sessions still resolve; no caller depends on the shell context being first; ledger 14 reads closed; module keeps zero dependencies beyond Node built-ins.</acceptance_criteria>
  <verify>
    <automated>node scripts/lib/firefox-bidi.mjs --self-test && scripts/verify-platform.sh --only gui01-window && scripts/verify-platform.sh --quick</automated>
    <fails_when>The BiDi self-test exits non-zero, the gui01-window row reports anything but PASS, or --quick exits non-zero</fails_when>
  </verify>
  <done>URL-bearing context selected deterministically with two-context proof and ledger closed</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chrome-privileged JS to boundary file | Actor registration crosses the privilege boundary |
| BiDi client to browsing contexts | Test driver crosses into live browser contexts |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-13 | Elevation of privilege | registerWindowActor guard | high | mitigate | Unconditional forbidden pattern plus self-test plant proving rejection, this plan; catalogue consistency keeps the single touchpoint |
| T-08-14 | Tampering | firefox-bidi context selection | medium | mitigate | URL-matched selection removes shell-context confusion; logged fallback keeps behavior explicit, this plan |
| T-08-SC | Tampering | npm/pip/cargo installs | high | mitigate | No registry installs in this plan |
</threat_model>

<artifacts_produced>
Symbols created by this plan: one forbidden-pattern entry for ChromeUtils.registerWindowActor; one self-test plant case riding the existing --self-test flag; URL-matched context selection inside the BiDi helper using the existing topLevelContexts export; one two-context session assertion; ledger 13 and 14 status flips to closed. No new files, CLI flags, verify rows, or config keys.
</artifacts_produced>

<verification>
Boundary self-test plus targeted rows plus --quick, green with both ledger entries closed.
</verification>

<success_criteria>
Guard rejects actor registration outside the boundary file; BiDi targets the requested page; single-touchpoint invariant holds with zero new touchpoints.
</success_criteria>

<output>
Create `.planning/phases/08-installer-hardening-canonical-rename/08-03-SUMMARY.md` when done
</output>
