---
phase: 08-installer-hardening-canonical-rename
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/generate.mjs
  - scripts/verify-installer-schema.mjs
  - configuration.toml
  - inventory/brand-tokens.json
  - powerbrowser/identity.configure.comparand
  - powerbrowser/branding/dev/configure.sh
  - powerbrowser/branding/release/configure.sh
autonomous: true
requirements: [PKG-03, NAME-01]
estimate:
  tokens: 35000
  raw_tokens: 70000
  tasks: 2
  confidence: high
must_haves:
  truths:
    - "A bare dollar-variable in an installer-bound manifest value fails generation naming the key"
    - "The installer verifier checks a fixture manifest root, never the live tree"
    - "configuration.toml carries identity.display_name PowerBrowser and generated branding matches it"
    - "generate --check and the byte-identity gate are green after the rename slice"
  artifacts:
    - path: "scripts/generate.mjs"
      provides: "WR-04 bare-dollar rejection in assertNsisEmittable plus self-test plant"
      contains: "NSIS_UNEMITTABLE"
    - path: "scripts/verify-installer-schema.mjs"
      provides: "WR-07 readTileColor rooted at fixture dir plus divergent-manifest plant"
      contains: "readTileColor"
    - path: "configuration.toml"
      provides: "Canonical display_name value"
      contains: 'display_name = "PowerBrowser"'
    - path: "inventory/brand-tokens.json"
      provides: "Re-pinned hand-authored display expectations"
      contains: "PowerBrowser"
  key_links:
    - from: "scripts/generate.mjs"
      to: "powerbrowser/branding/dev/configure.sh"
      via: "emitter regenerates tracked branding through TARGETS, proved by byte-identity"
      pattern: "emitBrandingNsi|byte-identity"
    - from: "inventory/brand-tokens.json"
      to: "scripts/verify-branding-preflight.mjs"
      via: "preflight reads expected display values from the inventory block"
      pattern: "brand_display_expectations"
  prohibitions:
    - statement: "The rename slice must not touch app_basename, binary_name, remoting_name, distribution_id, MOZ_APP_REMOTINGNAME, or StartupWMClass"
      verification: backstop
    - statement: "The WR-04 guard must not become an escaper that rewrites values; rejection-only per CFG-03"
      verification: backstop
    - statement: "No upstream/ edit and no hand-edited patch hunk in this plan"
      verification: backstop
  flagged_assumptions:
    - "PKG-03/unclassified: edge probe unclassified, reviewed manually — WR-04/WR-07 plants are the discriminating gate; any residual installer-schema edge not covered by the two plants is accepted as fixture-twin coverage"
---

<objective>
Land the WR-04 + WR-07 pre-fixes and the NAME-01 rename slice through the byte-identity gate, per CONTEXT locked pre-decisions (WR pre-fixes first; NAME-01 folded in first because every installer surface embeds the display name).

Purpose: Falsify the static pipeline before any host-dependent work burns tier-3 build minutes.
Output: Stricter generator guard, rooted verifier, canonical display value, green byte-identity.
</objective>

<execution_context>
<purpose>Execute this PLAN.md, commit per task, create SUMMARY.md.</purpose>
<required_reading>
Read .planning/STATE.md before any operation. Honor CONTEXT.md locked pre-decisions.
Commits: one per task as feat/fix/test, then a docs close-out with SUMMARY.md.
</required_reading>
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08-installer-hardening-canonical-rename/08-RESEARCH.md
@.planning/phases/08-installer-hardening-canonical-rename/08-PATTERNS.md
@.planning/phases/08-installer-hardening-canonical-rename/08-VALIDATION.md
</context>

<tasks>

<task type="tracer">
  <name>Tracer: WR-04 bare-dollar guard plus WR-07 fixture-root thread with self-test twins</name>
  <read_first>scripts/generate.mjs lines 861-914 and 3904-3926; scripts/verify-installer-schema.mjs lines 266-292, 351, 370-386, 443; 08-PATTERNS.md WR-04/WR-07 sections</read_first>
  <files>scripts/generate.mjs, scripts/verify-installer-schema.mjs</files>
  <action>Extend the NSIS sink guard in scripts/generate.mjs so a dollar sign not followed by a second dollar sign is rejected, covering bare dollar-name and dollar-brace in one class, and update the guard message rule sentence to state the dollar rule. Mirror the existing hostile-support-url harness with a new plant whose fixture support_url carries a bare dollar-name path that must exit non-zero naming the key. Thread the fixture root parameter through readTileColor in scripts/verify-installer-schema.mjs so it reads the manifest under the passed root at all three call sites, and add a divergent-manifest plant where a fixture manifest with tile_color set faces tile XML without BackgroundColor and vice versa, each required to go red naming the file and state, copying a fixture manifest into the mirror dir first.</action>
  <acceptance_criteria>A hostile support_url with a bare dollar-name exits non-zero naming installer.support_url; the brace form still rejected; a value with doubled dollars for a literal dollar still emits; readTileColor takes a root argument and no call site reads the repo-root manifest; both divergent-manifest polarities go red naming file and state; control-green-first holds for both self-tests.</acceptance_criteria>
  <verify>
    <automated>node scripts/generate.mjs --self-test && node scripts/verify-installer-schema.mjs --self-test && scripts/verify-platform.sh --quick</automated>
    <fails_when>Any of the three commands exits non-zero, or the output lacks the PASS line for installer-schema-self-test</fails_when>
  </verify>
  <done>Tracer committed: both pre-fix guards discriminate on synthetic fixtures and the quick gate is green</done>
</task>

<task type="auto">
  <name>NAME-01 slice: canonical display value plus inventory re-pin plus regenerated comparands</name>
  <reversibility rating="costly">Undo re-touches every display gate, comparand, and fixture re-pinned to the canonical form.</reversibility>
  <read_first>configuration.toml lines 30-45; inventory/brand-tokens.json lines 73-110; docs/BUILD.md BRAND-06 section; 08-PATTERNS.md NAME-01 pair section</read_first>
  <files>configuration.toml, inventory/brand-tokens.json, powerbrowser/identity.configure.comparand, powerbrowser/branding/dev/configure.sh, powerbrowser/branding/release/configure.sh</files>
  <action>Change identity.display_name in configuration.toml to the canonical spaceless form PowerBrowser, leaving app_basename, binary_name, remoting_name, distribution_id, and every remoting or window-class pin frozen. Rewrite the hand-authored brand_display_expectations variants block in inventory/brand-tokens.json so dev brand_full_name becomes PowerBrowser Dev and release becomes PowerBrowser with the short and app display names following the same base move, keeping the Dev suffix spaced. Decide the identifier_form tension explicitly: after this change the canonical display equals the identifier form, so re-scope that absence-assertion rather than deleting it silently, and record the scoping in the inventory comment. Regenerate through scripts/generate.mjs and update the tracked comparand plus any tracked branding files the emitter rewrites in the same commit, then prove generate --check and the byte-identity comparison green.</action>
  <acceptance_criteria>configuration.toml line 38 reads exactly display_name equals PowerBrowser in quotes; no diff touches app_basename, binary_name, remoting_name, distribution_id, MOZ_APP_REMOTINGNAME, or StartupWMClass; inventory dev and release full names read PowerBrowser Dev and PowerBrowser; generate --check reports fresh for zero paths; residual-brand scan exits 0 with new files staged.</acceptance_criteria>
  <verify>
    <automated>node scripts/generate.mjs && node scripts/generate.mjs --check && node scripts/scan-brand-residue.mjs && scripts/verify-platform.sh --quick</automated>
    <fails_when>generate --check reports any stale, absent, or leftover path, or the residue scan exits non-zero, or --quick exits non-zero</fails_when>
  </verify>
  <done>Canonical value fanned out through the emitter with byte-identity green and identity pins frozen</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| manifest to NSIS define | Author-controlled strings cross into installer compiler input |
| verifier to fixture root | Test double crosses into gate input |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-01a | Tampering | emitBrandingNsi six defines | high | mitigate | WR-04 rejection-only guard at emission plus schema gate asserting non-empty defines, this plan |
| T-08-01b | Tampering | verify-installer-schema tile check | medium | mitigate | WR-07 root threading so the gate checks the fixture it claims to check, this plan |
| T-08-SC | Tampering | npm/pip/cargo installs | high | mitigate | No registry installs in this plan; Package Legitimacy Audit records none |
</threat_model>

<artifacts_produced>
Symbols created by this plan: none new as named exports. Modified guard regex NSIS_UNEMITTABLE in scripts/generate.mjs; modified signature readTileColor(root) in scripts/verify-installer-schema.mjs; two new self-test plant cases riding existing --self-test flags; config value identity.display_name equals PowerBrowser; rewritten brand_display_expectations variant values; regenerated tracked files powerbrowser/identity.configure.comparand and both configure.sh variants.
</artifacts_produced>

<verification>
Tracer self-tests plus generate --check plus residue scan plus --quick, all green on the renamed tree.
</verification>

<success_criteria>
Both pre-fix guards discriminate before binaries exist; canonical value propagated with byte-identity green; frozen identity fields byte-identical to pre-plan.
</success_criteria>

<output>
Create `.planning/phases/08-installer-hardening-canonical-rename/08-01-SUMMARY.md` when done
</output>
