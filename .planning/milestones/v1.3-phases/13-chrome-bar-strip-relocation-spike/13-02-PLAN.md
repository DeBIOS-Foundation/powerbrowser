---
phase: 13-chrome-bar-strip-relocation-spike
plan: 02
type: execute
wave: 2
depends_on: [13-01]
files_modified: [theia/extensions/chrome-bar/package.json, theia/extensions/chrome-bar/tsconfig.json, theia/applications/browser/package.json, theia/package.json, theia/extensions/tab-uris/src/node/tab-query-service.ts, theia/extensions/chrome-bar/src/browser/chrome-bar-suggestion-service.ts, theia/extensions/chrome-bar/src/node/chrome-bar-backend-module.ts, theia/extensions/chrome-bar/src/node/chrome-bar-suggestion-service-impl.ts, theia/extensions/chrome-bar/src/browser/chrome-bar-frontend-module.ts, scripts/verify-chrome-bar-suggestions.mjs, scripts/verify-strip-spike-verdict.mjs, scripts/verify-platform.sh]
autonomous: true
requirements: [GUI-06]
must_haves:
  truths:
    - "The chrome-bar extension composes into the sidecar and builds with no new packages"
    - "Typing a prefix returns at most 8 recency-ordered suggestions with wildcard input escaped"
    - "Suggestions arrive over the existing authenticated channel through the first TabQueryService consumer"
    - "The spike verdict and the suggestion search each run as a verify-platform.sh registry row with a self-test"
    - "Theia core stays unpatched and no new Firefox-internal touchpoint exists"
  artifacts:
    - path: "theia/extensions/chrome-bar/package.json"
      provides: "New extension skeleton composed into the sidecar"
      contains: "@powerbrowser/chrome-bar"
    - path: "theia/extensions/tab-uris/src/node/tab-query-service.ts"
      provides: "Prefix search beside the existing point reads"
      contains: "searchByPrefix"
    - path: "scripts/verify-chrome-bar-suggestions.mjs"
      provides: "Suggestion search gate with self-test"
      min_lines: 60
    - path: "scripts/verify-strip-spike-verdict.mjs"
      provides: "Spike verdict gate with self-test"
      min_lines: 60
    - path: "scripts/verify-platform.sh"
      provides: "Four new registry rows for suggestions plus verdict with honesty comments"
      contains: "chrome-bar-suggestions"
  key_links:
    - from: "theia/extensions/chrome-bar/src/node/chrome-bar-suggestion-service-impl.ts"
      to: "theia/extensions/tab-uris/src/node/tab-query-service.ts"
      via: "backend impl delegates to the prefix search over the readonly handle"
      pattern: "searchByPrefix"
    - from: "scripts/verify-platform.sh"
      to: "scripts/verify-chrome-bar-suggestions.mjs"
      via: "registry row invokes the search gate; self-test row invokes it with --self-test"
      pattern: "chrome-bar-suggestions"
    - from: "scripts/verify-platform.sh"
      to: "scripts/verify-strip-spike-verdict.mjs"
      via: "registry row invokes the verdict gate; self-test row invokes it with --self-test"
      pattern: "strip-spike-verdict"
---

<objective>
Ship the chrome-bar foundation: extension skeleton composed and building, prefix search with its first JSON-RPC consumer, and two of the three new verify rows (suggestions plus spike verdict) green in the one registry.

Purpose: this is the contracts-and-data slice. The skeleton plus the search service plus its RPC path are the interfaces plan 13-03 builds the widget against, and the two rows whose sources exist after this plan go green now so plan 13-03 adds only the commands row.
Output: building extension, prefix search over the readonly handle, RPC proxy pair, two gated scripts with four registry rows, quick green.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-chrome-bar-strip-relocation-spike/13-CONTEXT.md
@.planning/phases/13-chrome-bar-strip-relocation-spike/13-RESEARCH.md
@.planning/phases/13-chrome-bar-strip-relocation-spike/13-PATTERNS.md
@.planning/phases/13-chrome-bar-strip-relocation-spike/13-UI-SPEC.md
@.planning/phases/13-chrome-bar-strip-relocation-spike/13-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Extension skeleton plus sidecar composition, building clean</name>
  <reversibility rating="costly">The extension id, composition entries, and build-list line are consumed by every later chrome-bar file; renaming touches each, but no on-disk format changes.</reversibility>
  <read_first>
    - .planning/phases/13-chrome-bar-strip-relocation-spike/13-PATTERNS.md (section 1: package.json shape, tsconfig verbatim copy, two composition edits, D-50 static-bind rule)
    - .planning/phases/13-chrome-bar-strip-relocation-spike/13-RESEARCH.md (Standard Stack install block: composition only, no registry lookup)
    - theia/extensions/telemetry/package.json (minimal skeleton base to copy)
    - theia/extensions/tab-uris/tsconfig.json (tsconfig to copy verbatim)
    - theia/extensions/customize/package.json line 7 (cross-extension dep precedent shape)
    - theia/applications/browser/package.json lines 37-42 (composition block to extend)
    - theia/package.json line 69 (hand-kept build list to append)
  </read_first>
  <files>theia/extensions/chrome-bar/package.json, theia/extensions/chrome-bar/tsconfig.json, theia/applications/browser/package.json, theia/package.json</files>
  <action>Create the new extension directory with the telemetry package shape under the powerbrowser scope at version 0.1.0, depending only on the pinned Theia scope modules the bar needs plus the tab-uris workspace pin where tab identity is read, declaring both the frontend and the backend module entries since this extension serves suggestions from the backend, and carrying the standard files list plus clean and build scripts. Copy the tab-uris tsconfig verbatim. Compose with exactly two edits: the sidecar dependency block beside the existing powerbrowser entries in alphabetical order, and the hand-kept build list appended with the new extension build. Fetch no registry package and invent no version: every version string is already pinned in-tree. Bind nothing lazily. Stage all four files before any scan, then compile the extension inside the Nix theia shell only, never with host yarn. Commit as feat(13-02): chrome-bar extension skeleton composed and building. Finish with the extension compiling and the quick gate green.</action>
  <verify>
    <automated>git add theia/extensions/chrome-bar/package.json theia/extensions/chrome-bar/tsconfig.json theia/applications/browser/package.json theia/package.json && nix develop .#theia --command bash -c "cd theia/extensions/chrome-bar && yarn build" && scripts/verify-platform.sh --quick</automated>
    <fails_when>extension build fails, any dependency version is invented rather than pinned in-tree, or the quick gate exits non-zero</fails_when>
  </verify>
  <acceptance_criteria>
    - Extension builds clean inside the Nix theia shell with frontend plus backend module entries
    - Both composition edits present with no app-file edit and no other tree change
    - No dependency outside the in-tree pins
    - Quick gate exits 0 with Theia core diff clean
  </acceptance_criteria>
  <done>New extension exists, is composed, and compiles without touching core</done>
</task>

<task type="auto">
  <name>Prefix search plus first JSON-RPC consumer over the existing channel</name>
  <reversibility rating="costly">The search signature, RPC path string, and row projection are consumed by the plan 13-03 widget; reshaping them touches widget and script together, but no stored format changes.</reversibility>
  <read_first>
    - .planning/phases/13-chrome-bar-strip-relocation-spike/13-RESEARCH.md (Pattern 3 new-query spec with bound prefix plus escape clause and limit cap, recency-ordering contract, never-throw convention, RPC token plus binding shapes, Pitfalls 2-3)
    - theia/extensions/tab-uris/src/node/tab-query-service.ts (existing point reads and recency listing whose conventions the new method mirrors; STAGED header note to revise once the consumer exists)
    - theia/extensions/tab-uris/src/browser/browser-tab-uri.ts (beside-registry key rule, unchanged and unextended)
    - theia/extensions/tab-uris/src/node/tab-query-backend-module.ts (ContainerModule shape to mirror, not edit)
    - theia/extensions/token-gate/src/node/token-gate-backend-module.ts (12-line backend module shape to copy)
  </read_first>
  <files>theia/extensions/tab-uris/src/node/tab-query-service.ts, theia/extensions/chrome-bar/src/browser/chrome-bar-suggestion-service.ts, theia/extensions/chrome-bar/src/node/chrome-bar-backend-module.ts, theia/extensions/chrome-bar/src/node/chrome-bar-suggestion-service-impl.ts, theia/extensions/chrome-bar/src/browser/chrome-bar-frontend-module.ts</files>
  <action>Add a body-only prefix-search method to the query service that filters address and title columns with bound parameters, neutralises pattern metacharacters before wrapping the pattern, carries the explicit escape clause, orders by recency like the existing listing, caps rows at the caller-supplied limit, and resolves empty on any failure exactly like the three existing methods, keeping the existing row projection of four fields and adding no public member to the registry module beside it. Revise the service header note so it states the first consumer now exists. Create the frontend service token carrying the RPC path constant and the search contract, the backend module binding the connection handler to the JSON-RPC handler at the path, the backend implementation delegating straight to the new method, and the frontend module binding the interface to the websocket proxy in singleton scope plus the backend service binding, all statically at module load. Open no profile database anywhere new, keep the single readonly handle discipline with its engine-property assert, and project address and title fields only. Touch no Gecko file and add no catalogue row because no internal is touched. Stage all five files before any scan, compile the touched extensions in the Nix theia shell, and commit as feat(13-02): prefix search with first RPC consumer. Finish with both extensions compiling, the reader carrying zero modifying statements, and the quick gate green.</action>
  <verify>
    <automated>git add theia/extensions/tab-uris/src/node/tab-query-service.ts theia/extensions/chrome-bar/src/browser/chrome-bar-suggestion-service.ts theia/extensions/chrome-bar/src/node/chrome-bar-backend-module.ts theia/extensions/chrome-bar/src/node/chrome-bar-suggestion-service-impl.ts theia/extensions/chrome-bar/src/browser/chrome-bar-frontend-module.ts && nix develop .#theia --command bash -c "cd theia/extensions/tab-uris && yarn build" && nix develop .#theia --command bash -c "cd theia/extensions/chrome-bar && yarn build" && test $(grep -v '^#' theia/extensions/tab-uris/src/node/tab-query-service.ts | grep -c -E 'INSERT|UPDATE |DELETE|DROP|ALTER') -eq 0 && test $(grep -v '^#' theia/extensions/chrome-bar/src/node/chrome-bar-suggestion-service-impl.ts | grep -c -E 'INSERT|UPDATE |DELETE|DROP|ALTER') -eq 0 && scripts/verify-platform.sh --quick</automated>
    <fails_when>either extension fails to build, any modifying statement appears in the reader sources, or the quick gate exits non-zero</fails_when>
  </verify>
  <acceptance_criteria>
    - Prefix search filters with bound parameters, escape clause, recency order, caller limit, and empty-on-failure
    - RPC proxy pair rides the existing channel with static binds and the service no longer reads as staged
    - Reader sources carry zero modifying statements and open no profile database
    - Quick gate exits 0
  </acceptance_criteria>
  <done>Suggestions are servable over RPC from the readonly handle with the first consumer landed</done>
</task>

<task type="auto">
  <name>Suggestions plus verdict gates as registry rows with self-tests</name>
  <reversibility rating="reversible">Test scaffolding only; changing a gate touches one script plus its two registry rows.</reversibility>
  <read_first>
    - .planning/phases/13-chrome-bar-strip-relocation-spike/13-RESEARCH.md (Validation Architecture test map rows for search and verdict, Wave 0 gaps with plant lists, A4-A5 assumptions)
    - .planning/phases/13-chrome-bar-strip-relocation-spike/13-PATTERNS.md (section 5: EXPECTED const, derived actuals, both-directions diff, non-vacuity guards, plant-landed self-test, honest quick, fix-naming failures, CHECKS shape beside the gui04 pair)
    - scripts/verify-registry-shape.mjs lines 55-80 and 119-166 and 179-250 (derive-and-compare plus both-directions self-test skeleton to imitate)
    - .planning/phases/13-chrome-bar-strip-relocation-spike/13-SPIKE-STRIP-RELOCATION.md (the verdict artifact this gate parses)
    - scripts/verify-platform.sh lines 3681-3689 (gui04 row shape plus honesty comment to copy)
  </read_first>
  <files>scripts/verify-chrome-bar-suggestions.mjs, scripts/verify-strip-spike-verdict.mjs, scripts/verify-platform.sh</files>
  <action>Write the suggestions gate honestly quick with a static half and a live half sharing one file. Derive the search statement text from the query-service source at check time and compare projection columns, escape clause, recency ordering, and bound limit as set equality against one EXPECTED const so an addition and a removal each go red naming the drift, with an empty derivation failing as a broken instrument. Prove semantics against a scratch fixture database outside the repo using the engine already vendored in the theia tree: prefix matching, metacharacter input returning literal matches only, the UI cap honoured, and recency ordering newest first. Cover the activation backstop with a held-out wired check that committing a suggestion routes through the existing opener path, printing STAGED with the exact rerun command and exiting cleanly when the binary or display is absent rather than passing silently. Give it a self-test asserting the unmodified tree green first, then a planted unescaped-wildcard fixture and a planted limit breach each going red naming the drift with the plant-landed assertion. Write the verdict gate honestly quick: parse the spike record for a one-line GREEN or RED verdict with a cause on RED plus the summary table routing to a Variant, assert the core-diff check exits clean, assert the upstream diff empty, and assert version control shows no new file under theia or scripts from the spike plan. Its self-test asserts green first, then a malformed verdict and a GREEN-with-dirty-core plant each going red naming the drift. Append four rows to the single CHECKS registry beside the gui04 pair with one honesty comment each, creating no sibling driver: the two entry points plus their two self-tests. Stage the three files before any scan and commit as feat(13-02): suggestions and verdict gates with self-tests. Finish with both scripts green, both self-tests green, single-gate sampling green per new row, and the quick gate green.</action>
  <verify>
    <automated>git add scripts/verify-chrome-bar-suggestions.mjs scripts/verify-strip-spike-verdict.mjs scripts/verify-platform.sh && node --check scripts/verify-chrome-bar-suggestions.mjs && node --check scripts/verify-strip-spike-verdict.mjs && node scripts/verify-chrome-bar-suggestions.mjs && node scripts/verify-chrome-bar-suggestions.mjs --self-test && node scripts/verify-strip-spike-verdict.mjs && node scripts/verify-strip-spike-verdict.mjs --self-test && scripts/verify-platform.sh --only gui06-chrome-bar-suggestions && scripts/verify-platform.sh --only gui06-chrome-bar-suggestions-self-test && scripts/verify-platform.sh --only gui07-strip-spike-verdict && scripts/verify-platform.sh --only gui07-strip-spike-verdict-self-test && scripts/verify-platform.sh --quick</automated>
    <fails_when>either script exits non-zero on the unmodified tree, any self-test plant fails to go red naming the drift, any new row fails through the single-gate runner, or the quick gate exits non-zero</fails_when>
  </verify>
  <acceptance_criteria>
    - Suggestions gate derives the search shape from source with set equality plus fixture semantics plus the activation backstop with honest staging
    - Verdict gate parses the spike record and asserts core-diff clean, upstream diff empty, and no shipped-tree drift
    - Self-tests prove green first with plant-landed assertions on every fault
    - Exactly four new rows exist with honesty comments and no sibling driver is created
  </acceptance_criteria>
  <done>Search semantics and the spike verdict are enforced mechanically with proven checks</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Suggestion input to store query | Typed text reaches a SQL pattern match over tab rows. |
| Suggestion rows to the widget | Row fields render in the plan 13-03 dropdown. |
| Backend to dedicated database file | New code holds the readonly handle beside the existing one. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-02-01 | Information disclosure | wildcard dumping via metacharacter input | high | mitigate | Metacharacters neutralised before wrapping, explicit escape clause, caller limit cap, fixture-proven by the new gate. |
| T-13-02-02 | Information disclosure | secret reflected into suggestion rows | medium | mitigate | Projection stays four address and title fields; never add a column without review; writer stores no secrets. |
| T-13-02-03 | Tampering | second writer on the store | high | mitigate | Readonly flag at open plus engine-property assert; zero modifying statements enforced by grep gate; chrome writer stays sole. |
| T-13-02-04 | Elevation of privilege | new privileged channel | high | mitigate | No new channel: RPC rides the existing authenticated transport, no chrome addition, internals catalogue untouched. |
| T-13-02-SC | Tampering | npm/pip/cargo installs | medium | mitigate | No package-manager install occurs in this plan; composition reuses in-tree pins only. |
</threat_model>

<verification>
Both extensions compile in the Nix theia shell with zero modifying statements in the reader, both new scripts plus self-tests green, four new rows green through the single-gate runner, quick green, core diff clean.
</verification>

<success_criteria>
The extension skeleton builds composed, prefix search serves over RPC from the readonly handle, and the suggestions plus verdict gates run through the one driver with self-tests.
</success_criteria>

<output>
Plan 13-03 consumes the skeleton, the RPC proxy, the search method, and the two green rows: it adds the widget, the commands, the styling, and the remaining commands row.
</output>
