---
phase: 13-chrome-bar-strip-relocation-spike
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [.planning/phases/13-chrome-bar-strip-relocation-spike/13-SPIKE-STRIP-RELOCATION.md]
autonomous: true
requirements: [GUI-07]
must_haves:
  truths:
    - "A live tab strip moves Theia shell areas with zero Theia-core modifications, or the blocking cause is recorded"
    - "Tab identity survives the move: same registry URIs, selection intact, no duplicate widgets"
    - "The spike verdict is recorded as GREEN with the Variant-B handoff or RED with the Variant-A fallback and its cause"
    - "No spike scaffolding remains in the shipped tree: probe harness lived under /tmp and is removed"
    - statement: "Zero, one, and many tabs keep strip geometry sane in the relocated region, matching top-region behaviour"
      verification: backstop
    - statement: "Many-tab overflow in the relocated region scrolls rather than clipping or wrapping"
      verification: backstop
  artifacts:
    - path: ".planning/phases/13-chrome-bar-strip-relocation-spike/13-SPIKE-STRIP-RELOCATION.md"
      provides: "Strip-relocation spike record imitating 01-SPIKE-GUI-01.md with the GREEN/RED verdict"
      min_lines: 100
      contains: "Variant"
  key_links:
    - from: ".planning/phases/13-chrome-bar-strip-relocation-spike/13-SPIKE-STRIP-RELOCATION.md"
      to: "scripts/diff-theia-core.sh"
      via: "spike cites the zero-core-modification proof as its green criterion"
      pattern: "diff-theia-core"
    - from: ".planning/phases/13-chrome-bar-strip-relocation-spike/13-SPIKE-STRIP-RELOCATION.md"
      to: "theia/extensions/tab-uris/src/browser/tab-uri-registry.ts"
      via: "identity oracle for the move is the registry uriOf set before and after"
      pattern: "uriOf"
---

<objective>
Prove or disprove strip relocation (GUI-07 entry criterion): move a live tab strip between Theia shell areas using only public shell APIs, and record the GREEN/RED verdict that fixes Phase 14 on Variant B or the Variant-A fallback.

Purpose: sketch 002 winner B is PENDING this verdict by committed direction, not preference. The spike decides. A GREEN verdict unlocks Variant-B strip work in Phase 14; a RED verdict records the blocking cause, the strip stays top per Variant A, and modes still ship.
Output: the spike record with observations, proofs, verdict, and ratification; no shipped-tree change.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-chrome-bar-strip-relocation-spike/13-CONTEXT.md
@.planning/phases/13-chrome-bar-strip-relocation-spike/13-RESEARCH.md
@.planning/phases/13-chrome-bar-strip-relocation-spike/13-PATTERNS.md
@.planning/phases/13-chrome-bar-strip-relocation-spike/13-UI-SPEC.md
@.planning/milestones/v1.0-phases/01-platform-extraction-and-rename/01-SPIKE-GUI-01.md
</context>

<tasks>

<task type="auto">
  <name>Live probe: move main-area widgets to another area via public shell API</name>
  <reversibility rating="reversible">Throwaway probe under /tmp plus a planning record; nothing shipped, undo is deletion.</reversibility>
  <read_first>
    - .planning/phases/13-chrome-bar-strip-relocation-spike/13-RESEARCH.md (Architecture Patterns P1, Code Examples top-area registration, Pitfalls 5-7, spike MEDIUM confidence note)
    - .planning/phases/13-chrome-bar-strip-relocation-spike/13-PATTERNS.md (section 7 spike structure plus spike-specific constraints)
    - .planning/milestones/v1.0-phases/01-platform-extraction-and-rename/01-SPIKE-GUI-01.md lines 1-120 (header block, what-changed, build, instrumentation-note discipline)
    - theia/node_modules/@theia/core/lib/browser/shell/application-shell.d.ts (Area union, addWidget WidgetOptions, getAreaFor, getTabBarFor, allTabBars)
    - theia/extensions/tab-uris/src/browser/tab-uri-registry.ts (uriOf and parseName identity oracle)
  </read_first>
  <files>.planning/phases/13-chrome-bar-strip-relocation-spike/13-SPIKE-STRIP-RELOCATION.md</files>
  <action>Build the Theia sidecar once inside the Nix theia shell only, reusing an existing built app when one is present rather than rebuilding, and record the command, exit code, and timing in the spike record. Drive the live probe from a scratch harness kept strictly under /tmp/phase13-spike, never under the repo, following the BiDi frontend-walk precedent of scripts/verify-gui01-command.mjs without copying that file into the tree. Move two to three live main-area widgets to the bottom area by re-adding the same widget instances with an area option only, creating no widget, rebinding nothing, and touching no panel internals. For each move capture the registry URI set before and after as set equality, the current selection before and after, the resolved area read back after the move rather than the requested one, and the widget-id lookup proving no duplicate. A resolver-forced placement back into main is a RED data point with its cause, never a silent pass. Any launch uses a temp profile outside the repo and outside any home directory. Record each attempt as a numbered Observation section with the command that produced it and verbatim evidence, including a discarded-instrument note when an instrument cannot go red. Commit as docs(13-01): strip-relocation live probe observations. Stage the record file before any scan and finish with the quick gate green.</action>
  <verify>
    <automated>git add .planning/phases/13-chrome-bar-strip-relocation-spike/13-SPIKE-STRIP-RELOCATION.md && test -s .planning/phases/13-chrome-bar-strip-relocation-spike/13-SPIKE-STRIP-RELOCATION.md && grep -qc 'Observation' .planning/phases/13-chrome-bar-strip-relocation-spike/13-SPIKE-STRIP-RELOCATION.md && test ! -e theia/extensions/chrome-bar && scripts/verify-platform.sh --quick</automated>
    <fails_when>record missing or has no numbered observations, any probe file exists under the repo, or the quick gate exits non-zero</fails_when>
  </verify>
  <acceptance_criteria>
    - Spike record exists with numbered observations carrying commands plus verbatim evidence
    - Every move asserts same-instance re-adding with URI set equality, selection intact, resolved-area readback, and no duplicate id
    - Probe harness lives only under /tmp with nothing added under theia or scripts
    - Quick gate exits 0
  </acceptance_criteria>
  <done>Live main-to-bottom moves are observed and recorded with identity evidence, all scaffolding outside the shipped tree</done>
</task>

<task type="auto">
  <name>Edge observations: resolver veto, move-back, geometry sanity, persistence boundary</name>
  <reversibility rating="reversible">Planning record only; undo is deletion.</reversibility>
  <read_first>
    - .planning/phases/13-chrome-bar-strip-relocation-spike/13-SPIKE-STRIP-RELOCATION.md (Task 1 observations to extend, not rewrite)
    - .planning/phases/13-chrome-bar-strip-relocation-spike/13-RESEARCH.md (Pitfalls 6-7: resolver override, layout-restorer persistence split)
    - .planning/phases/13-chrome-bar-strip-relocation-spike/13-UI-SPEC.md (Interaction Contract strip verdict plus E6 backstop rows)
  </read_first>
  <files>.planning/phases/13-chrome-bar-strip-relocation-spike/13-SPIKE-STRIP-RELOCATION.md</files>
  <action>Extend the record with the edge observations that decide GREEN versus RED: the requested-versus-resolved area comparison proving no silent veto, the move-back to main proving the relocation is not a one-way trip, selection-versus-activation nuance if the two disagree, zero/one/many tab geometry sanity in the relocated region stated as held-out visual checks, many-tab overflow behaviour stated the same way, and any ordering or focus constraints found. Assert the live-move-only boundary explicitly: persistence across reload is Phase 14 scope and no observation asserts post-reload geometry. Record the tabs invariant in every outcome including the fallback: no tab closed, no window moved on, nothing detached. Commit as docs(13-01): strip-relocation edge observations and invariant. Stage before any scan and finish with the quick gate green.</action>
  <verify>
    <automated>git add .planning/phases/13-chrome-bar-strip-relocation-spike/13-SPIKE-STRIP-RELOCATION.md && grep -qc 'backstop' .planning/phases/13-chrome-bar-strip-relocation-spike/13-SPIKE-STRIP-RELOCATION.md && grep -qc 'tabs invariant' .planning/phases/13-chrome-bar-strip-relocation-spike/13-SPIKE-STRIP-RELOCATION.md && scripts/verify-platform.sh --quick</automated>
    <fails_when>edge observations absent, the invariant statement missing, or the quick gate exits non-zero</fails_when>
  </verify>
  <acceptance_criteria>
    - Requested-versus-resolved area comparison recorded with veto-as-RED-data discipline
    - Move-back, geometry sanity, and overflow recorded with the two held-out checks marked as backstop
    - Live-move-only boundary and the tabs invariant stated explicitly
    - Quick gate exits 0
  </acceptance_criteria>
  <done>Edge behaviour is recorded with the evidence that separates a green move from a red one</done>
</task>

<task type="auto">
  <name>Verdict record imitating 01-SPIKE structure with proofs and ratification</name>
  <reversibility rating="reversible">A recorded decision plus cited proofs; re-running the probe revises it.</reversibility>
  <read_first>
    - .planning/phases/13-chrome-bar-strip-relocation-spike/13-SPIKE-STRIP-RELOCATION.md (full draft to complete)
    - .planning/milestones/v1.0-phases/01-platform-extraction-and-rename/01-SPIKE-GUI-01.md lines 380-487 (summary table, artefacts, correction, ratification discipline)
    - .planning/phases/13-chrome-bar-strip-relocation-spike/13-PATTERNS.md (section 7 structure table rows: summary, artefacts, ratification)
  </read_first>
  <files>.planning/phases/13-chrome-bar-strip-relocation-spike/13-SPIKE-STRIP-RELOCATION.md</files>
  <action>Complete the record section for section after the analog: title plus header block with plan and task, GUI-07 decision reference, run host and date, Theia pin 1.74.1, app under test, and a one-line verdict of GREEN or RED; what-changed enumerating probe edits as scaffolding; build command with exit and timing; instrumentation note naming any discarded instrument with its cause; the observations already recorded left intact with any misreading corrected in place and the reasoning left visible; constraints found; a summary table with one row per spike question where GREEN names strip-moves-areas plus core-diff-clean plus identity-preserved leading to Variant B and RED names the blocking cause leading to the Variant-A fallback with modes still shipping; an artefacts section stating the harness was scratch and removed; and a ratification table answering land-as-spiked versus fallback, the constraints binding Phase 14, whether the fallback was taken, and that the gui07 verdict registry row in plan 13-02 guards it. Prove zero core modification by pasting the core-diff check output, prove no Gecko touch by asserting the upstream diff empty, prove the shipped tree clean by showing version control knows no new file under theia or scripts from this plan, and remove the /tmp harness. Inconclusive probe conditions are RED with the harness or environment named as the blocking cause, never an unwritten verdict. Commit as docs(13-01): strip-relocation verdict and ratification. Stage before any scan and finish with the core-diff check, the upstream-diff assert, and the quick gate all green.</action>
  <verify>
    <automated>git add .planning/phases/13-chrome-bar-strip-relocation-spike/13-SPIKE-STRIP-RELOCATION.md && grep -qE 'Verdict.*(GREEN|RED)' .planning/phases/13-chrome-bar-strip-relocation-spike/13-SPIKE-STRIP-RELOCATION.md && grep -qc 'Variant' .planning/phases/13-chrome-bar-strip-relocation-spike/13-SPIKE-STRIP-RELOCATION.md && test ! -e /tmp/phase13-spike && bash scripts/diff-theia-core.sh --quick && git -C upstream diff --quiet && git status --porcelain theia/ scripts/ | grep -v '^??' | test $(wc -l) -eq 0 && scripts/verify-platform.sh --quick</automated>
    <fails_when>no GREEN/RED verdict line, no Variant routing, the scratch harness still exists, the core-diff check fails, the upstream diff is non-empty, tracked files under theia or scripts changed, or the quick gate exits non-zero</fails_when>
  </verify>
  <acceptance_criteria>
    - Record carries a one-line GREEN or RED verdict with Variant-B handoff or Variant-A fallback plus cause
    - Core-diff proof, empty upstream diff, and clean shipped-tree evidence are pasted or asserted
    - Scratch harness removed with no new file under theia or scripts
    - Quick gate exits 0
  </acceptance_criteria>
  <done>Strip-relocation feasibility is decided and recorded with proofs, and Phase 14 knows which variant to build</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Probe harness to live profile | The live probe drives a real sidecar that reads the developer's tab store. |
| Spike record to Phase 14 | The verdict routes an entire phase of strip work down one of two variants. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-01-01 | Tampering | probe against live profile | high | mitigate | Temp profile outside the repo and outside any home directory; no run touches the developer profile. |
| T-13-01-02 | Denial of Service | vacuous verdict | medium | mitigate | Verdict requires observed evidence lines per observation; inconclusive conditions go RED with the cause named, never unwritten. |
| T-13-01-03 | Tampering | committed scaffolding | medium | mitigate | Harness lives under /tmp only; closeout asserts removal and version control shows no new file under theia or scripts. |
| T-13-01-04 | Spoofing | resolver veto mistaken for success | medium | mitigate | Resolved area read back after every move; a forced placement is RED data, not a pass. |
| T-13-01-SC | Tampering | npm/pip/cargo installs | low | mitigate | No package-manager install occurs in this plan; the one build reuses the pinned tree. |
</threat_model>

<verification>
Spike record exists with numbered observations plus a GREEN/RED verdict routing to a Variant, core-diff check green, upstream diff empty, no new file under theia or scripts, quick green.
</verification>

<success_criteria>
Strip-relocation feasibility is proven or disproven against a live strip with identity evidence, the verdict is recorded with the fallback decided, and the shipped tree carries no probe scaffolding.
</success_criteria>

<output>
Plan 13-02 consumes the verdict: its verdict registry row parses this record, and the chrome-bar build proceeds identically under either verdict since the bar is Variant A.
</output>
