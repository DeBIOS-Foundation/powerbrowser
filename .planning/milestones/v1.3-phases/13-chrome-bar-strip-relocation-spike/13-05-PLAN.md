---
phase: 13-chrome-bar-strip-relocation-spike
plan: 05
type: execute
wave: 2
depends_on: ["13-04"]
files_modified: [.planning/milestones/v1.3-phases/13-chrome-bar-strip-relocation-spike/13-UI-SPEC.md, theia/extensions/chrome-bar/src/browser/chrome-bar-widget.tsx, scripts/verify-chrome-bar-placement.mjs, scripts/verify-platform.sh]
autonomous: true
requirements: [GUI-06]
gap_closure: true
gap_ids: [G-13-3]
must_haves:
  truths:
    - "The UI contract records bar-above-strip as the ratified Variant-A order with the below-strip slot ruled out by the never-fork-core rule"
    - "Bar placement is asserted mechanically: the gate derives the contribution area and the contracted order and compares them"
    - "The placement gate ships with a green-first self-test proving each plant goes red naming its drift"
    - "The new rows run through the single driver with honesty comments and the quick gate is green"
  artifacts:
    - path: ".planning/milestones/v1.3-phases/13-chrome-bar-strip-relocation-spike/13-UI-SPEC.md"
      provides: "Ratified bar-above-strip Variant-A order"
      contains: "chrome bar"
    - path: "scripts/verify-chrome-bar-placement.mjs"
      provides: "DOM-order gate with self-test"
      min_lines: 60
    - path: "scripts/verify-platform.sh"
      provides: "Placement entry plus self-test rows beside the chrome-bar pair"
      contains: "chrome-bar-placement"
  key_links:
    - from: "scripts/verify-chrome-bar-placement.mjs"
      to: "theia/extensions/chrome-bar/src/browser/chrome-bar-widget.tsx"
      via: "gate derives the contribution area from the addWidget call at check time"
      pattern: "addWidget"
    - from: "scripts/verify-chrome-bar-placement.mjs"
      to: ".planning/milestones/v1.3-phases/13-chrome-bar-strip-relocation-spike/13-UI-SPEC.md"
      via: "gate derives the ratified order from the contract's order line at check time"
      pattern: "tab strip"
    - from: "scripts/verify-platform.sh"
      to: "scripts/verify-chrome-bar-placement.mjs"
      via: "registry rows invoke the placement gate; self-test row invokes it with --self-test"
      pattern: "chrome-bar-placement"
---

<objective>
Close G-13-3 facet 2 on the ratify path: bar-above-strip becomes the Variant-A contract, asserted by a mechanical gate.

Purpose: the bar lives in the top panel above the main dock by public shell API, which structurally cannot satisfy the sketch mock order of tab strip above chrome bar; the in-dock slot needs dock surgery, which is a Theia-core touch forbidden by the hard rule. The honest fix is ratification: update the contract to the as-built order with the reason recorded, and add the DOM-order gate that was missing so placement can never drift silently again.
Output: ratified UI-SPEC order, placement gate with self-test, two registry rows, quick green.
</objective>

<context>
@.planning/milestones/v1.3-phases/13-chrome-bar-strip-relocation-spike/13-UAT.md
@.planning/debug/chrome-bar-suggestions-and-placement.md
@.planning/milestones/v1.3-phases/13-chrome-bar-strip-relocation-spike/13-UI-SPEC.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Ratify bar-above-strip in the UI contract plus widget note</name>
  <read_first>
    - .planning/milestones/v1.3-phases/13-chrome-bar-strip-relocation-spike/13-UI-SPEC.md (Bar placement and order section lines 171-178: mock order plus the planner-call sentence this task supersedes)
    - .planning/debug/chrome-bar-suggestions-and-placement.md (Evidence row on facet 2: createLayout stacking, area-top mapping, no public in-dock slot; Resolution facet2)
    - theia/extensions/chrome-bar/src/browser/chrome-bar-widget.tsx (header note lines 33-35 already stating above-the-main-dock; contribution onStart area lines 468-476, untouched by this task)
  </read_first>
  <files>.planning/milestones/v1.3-phases/13-chrome-bar-strip-relocation-spike/13-UI-SPEC.md, theia/extensions/chrome-bar/src/browser/chrome-bar-widget.tsx</files>
  <action>Rewrite the contract's Bar placement and order section to record bar-above-strip as the ratified Variant-A order: the order line becomes menubar, then chrome bar, then tab strip, then workarea, then status bar, using the same arrow separators the section already uses so the placement gate in the next task parses the identical line. Replace the planner-call sentence that permitted either side of the toolbar with the ratification reason in one paragraph: the bar is contributed to the top shell area, which the shell stacks above the main dock owning the tab strip, and the below-strip slot would need dock surgery, which is a Theia-core touch forbidden by the never-fork-core hard rule, so below-strip stays out of Variant A. Keep the exactly-one-bar sentence, the status-bar chip sentence, and every other section of the file byte-identical. Extend the widget header note with one sentence citing the ratified order and the Variant-A routing, changing no code. Stage both files before any scan and commit as fix(13-05): ratify bar-above-strip as the Variant-A contract. Finish with the quick gate green.</action>
  <verify>
    <automated>git add .planning/milestones/v1.3-phases/13-chrome-bar-strip-relocation-spike/13-UI-SPEC.md theia/extensions/chrome-bar/src/browser/chrome-bar-widget.tsx && grep -qc 'chrome bar → tab strip' .planning/milestones/v1.3-phases/13-chrome-bar-strip-relocation-spike/13-UI-SPEC.md && scripts/verify-platform.sh --quick</automated>
    <fails_when>the ratified order line is absent or misspelled, any other UI-SPEC section changed, or the quick gate exits non-zero</fails_when>
  </verify>
  <acceptance_criteria>
    - Order line reads menubar, chrome bar, tab strip, workarea, status bar with the file's existing separators
    - Planner-call sentence replaced by the ratification reason naming the core-touch prohibition
    - Widget header cites the ratified order; no widget code changed
  </acceptance_criteria>
  <done>The contract describes the as-built order with the reason recorded, not wished otherwise</done>
</task>

<task type="auto">
  <name>DOM-order placement gate as registry rows with self-test</name>
  <read_first>
    - scripts/verify-chrome-bar-commands.mjs lines 1-60 and 164-224 (gate header idiom plus green-first plant-landed self-test skeleton to imitate)
    - scripts/verify-registry-shape.mjs lines 55-80 and 119-166 (derive-and-compare plus both-directions self-test skeleton to imitate)
    - theia/extensions/chrome-bar/src/browser/chrome-bar-widget.tsx (contribution onStart addWidget area value this gate derives)
    - .planning/milestones/v1.3-phases/13-chrome-bar-strip-relocation-spike/13-UI-SPEC.md (ratified order line from the previous task this gate derives)
    - scripts/verify-platform.sh lines 3691-3711 (chrome-bar row shape plus honesty comments to copy)
  </read_first>
  <files>scripts/verify-chrome-bar-placement.mjs, scripts/verify-platform.sh</files>
  <action>Write the placement gate honestly quick following the imitated skeletons. Derive two facts at check time and compare them: the contribution area from the addWidget call in the widget contribution source, failing as a broken instrument when no area derives, and the ordered region names from the contract's ratified order line, failing as a broken instrument when the line or any expected region name is missing. Pass only when the derived area is the top area and the derived order places the chrome bar immediately above the tab strip; any other area, any flipped or separated order, or any extra bar-adjacency claim fails naming the drift on both sides. Give it a self-test asserting the unmodified tree green first, then three plants each going red naming its drift with the plant-landed assertion: the area re-spelled to the main area, the contract order line flipped to strip-above-bar, and the contract order line deleted. Append two rows beside the chrome-bar pair with one honesty comment each, creating no sibling driver: the entry point plus its self-test. Stage both files before any scan and commit as fix(13-05): placement gate with self-test. Finish with the script green, its self-test green, single-gate sampling green on both new rows plus the neighbouring chrome-bar rows, and the quick gate green.</action>
  <verify>
    <automated>git add scripts/verify-chrome-bar-placement.mjs scripts/verify-platform.sh && node --check scripts/verify-chrome-bar-placement.mjs && node scripts/verify-chrome-bar-placement.mjs && node scripts/verify-chrome-bar-placement.mjs --self-test && scripts/verify-platform.sh --only gui06-chrome-bar-placement && scripts/verify-platform.sh --only gui06-chrome-bar-placement-self-test && scripts/verify-platform.sh --only gui06-chrome-bar-commands && scripts/verify-platform.sh --only gui06-chrome-bar-suggestions && scripts/verify-platform.sh --quick</automated>
    <fails_when>the gate exits non-zero unmodified, any self-test plant fails to go red naming the drift, any sampled row fails, or the quick gate exits non-zero</fails_when>
  </verify>
  <acceptance_criteria>
    - Gate derives the contribution area and the contracted order with set and order equality plus distinct empty-derivation failures
    - Self-test proves green-first with three plant-landed assertions
    - Exactly two new rows exist with honesty comments and no sibling driver is created
  </acceptance_criteria>
  <done>Placement is enforced mechanically with a proven check instead of resting on a mock</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Contract order to rendered order | The documented bar position must match the shell contribution. |
| Gate derivation to tree sources | The check reads the widget and the contract, never a copy. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-05-01 | Tampering | placement drift back below the strip or into the dock | medium | mitigate | The gate derives area plus contracted order at check time; area flip, order flip, and order deletion each go red by planted proof. |
| T-13-05-02 | Denial of Service | gate passing vacuously on an unparsable tree | low | mitigate | Empty derivation on either side fails as a broken instrument, never passes as clean. |
| T-13-05-SC | Tampering | npm/pip/cargo installs | low | mitigate | No package-manager install occurs in this plan; no new dependency. |
</threat_model>

<verification>
UI-SPEC carries the ratified bar-above-strip order with the core-touch reason, widget note cites it with no code change, placement gate plus self-test green, both new rows plus neighbouring chrome-bar rows green through the single-gate runner, quick green, Theia core diff clean.
</verification>

<success_criteria>
The wrong-location facet is closed by ratification with the reason on record, and a mechanical gate holds the placement from here on.
</success_criteria>

<output>
Create `.planning/milestones/v1.3-phases/13-chrome-bar-strip-relocation-spike/13-05-SUMMARY.md` when done
</output>
