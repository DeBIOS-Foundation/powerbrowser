---
phase: 14-modes-windows-setups
plan: 02
type: execute
wave: 2
depends_on: [14-01]
files_modified: [theia/extensions/modes/src/browser/mode-service.ts, theia/extensions/modes/src/browser/modes-commands.ts, theia/extensions/modes/src/browser/organising-placeholder-widget.ts, theia/extensions/modes/src/browser/mode-descriptors.ts, theia/extensions/modes/src/browser/modes-frontend-module.ts, theia/extensions/modes/src/browser/modes.css, theia/extensions/chrome-bar/src/browser/chrome-bar-widget.tsx, scripts/verify-mode-switch-tabs-invariant.mjs, scripts/verify-shell-error-copy.mjs, scripts/verify-platform.sh]
autonomous: true
requirements: [GUI-07]
must_haves:
  truths:
    - "User saves the current panel layout as a custom mode and picks it from the toggle menu beside shipped defaults"
    - "Unreadable custom-mode data falls back to shipped Browsing with the contracted notice, never a blank shell"
    - "Organising shows the contracted placeholder slot with a working Back to Browsing control and no canvas surface"
    - "Mode switches slide panels within budget with tabs never unmounting and the chip re-asserting the count"
    - "Every new user-facing string follows the contracted copy with no internal identifier"
  artifacts:
    - path: "theia/extensions/modes/src/browser/mode-service.ts"
      provides: "Custom modes from user-storage JSON with total parsing and fallback"
      contains: "modes.json"
    - path: "theia/extensions/modes/src/browser/modes-commands.ts"
      provides: "Mode command ids with contracted labels"
      contains: "powerbrowser.modes.save-as-mode"
    - path: "theia/extensions/modes/src/browser/organising-placeholder-widget.ts"
      provides: "Organising main-area slot with contracted copy"
      contains: "Back to Browsing"
    - path: "theia/extensions/modes/src/browser/modes.css"
      provides: "Single style layer on locked tokens under the user layer"
      min_lines: 40
    - path: "scripts/verify-mode-switch-tabs-invariant.mjs"
      provides: "Switch-path allowlist gate plus mode-command registry with self-test"
      min_lines: 60
    - path: "scripts/verify-platform.sh"
      provides: "Invariant-gate entry plus self-test rows beside the toggle pair"
      contains: "mode-switch-tabs-invariant"
  key_links:
    - from: "theia/extensions/modes/src/browser/mode-service.ts"
      to: "theia/extensions/modes/src/browser/mode-descriptors.ts"
      via: "customs register as descriptors beside shipped literals without mutating them"
      pattern: "registerPerspective"
    - from: "theia/extensions/modes/src/browser/modes-commands.ts"
      to: "theia/extensions/modes/src/browser/mode-service.ts"
      via: "commands invoke the service through the registry by imported id consts"
      pattern: "MODES.*COMMAND_ID"
    - from: "theia/extensions/chrome-bar/src/browser/chrome-bar-widget.tsx"
      to: "theia/extensions/modes/src/browser/mode-service.ts"
      via: "toggle menu lists customs from the service with shipped rows untouched"
      pattern: "onDidChangePerspective"
    - from: "scripts/verify-platform.sh"
      to: "scripts/verify-mode-switch-tabs-invariant.mjs"
      via: "registry row invokes the invariant gate; self-test row invokes it with --self-test"
      pattern: "mode-switch-tabs-invariant"
---

<objective>
Ship full modes (GUI-07): custom-mode save and fallback, the organising placeholder slot, custom rows on the toggle, the modes style layer, and the switch-path invariant gate with copy coverage green.

Purpose: this is the modes expansion on the plan 14-01 tracer. Shipped descriptors and the bridged toggle already switch; this plan adds user-authored modes as data, the contracted organising slot that Phase 15 later replaces with the canvas, and mechanical proof that no switch path can close, move, or detach a tab.
Output: mode service with user-storage customs, mode commands, placeholder widget, toggle custom rows, style layer, invariant gate with self-test, copy coverage, quick green.

Schema call (resolves RESEARCH Open Question 2, modes half): user-storage modes.json holds an object with a numeric version plus a customs array whose rows carry a name, side-panel visibility flags, and a shell layout snapshot; validation is hand-rolled total parsing that drops unknown fields and treats a corrupt file as shipped Browsing with the contracted fallback notice.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-modes-windows-setups/14-CONTEXT.md
@.planning/phases/14-modes-windows-setups/14-RESEARCH.md
@.planning/phases/14-modes-windows-setups/14-PATTERNS.md
@.planning/phases/14-modes-windows-setups/14-UI-SPEC.md
@.planning/phases/14-modes-windows-setups/14-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Custom modes service with fallback plus mode commands</name>
  <reversibility rating="costly">The user-storage schema and command ids are read by later plans and gates; renaming touches each consumer, but migration is a local file rewrite.</reversibility>
  <read_first>
    - theia/extensions/customize/src/browser/customize-css-contribution.ts lines 55-145 (never-create default, catch fallback, keep-last-good, file-change reload with debounce, boot-chain deadlock note)
    - .planning/phases/14-modes-windows-setups/14-RESEARCH.md (Pattern 2 user-storage idiom, Pitfalls 2 and 3 on onStart deadlock and custom-id re-application, V5 validation rules)
    - .planning/phases/14-modes-windows-setups/14-PATTERNS.md (section 3 write path with name cap and contracted errors, never-create absence-is-default)
    - theia/extensions/chrome-bar/src/browser/chrome-bar-commands.ts (whole file: exported id consts, contracted labels, labelless command precedent to copy)
    - theia/extensions/modes/src/browser/modes-frontend-module.ts (plan 14-01 binds to extend in the same static voice)
    - .planning/phases/14-modes-windows-setups/14-UI-SPEC.md (Copywriting Contract mode rows, mode-switch rules, typography roles for the notice)
  </read_first>
  <files>theia/extensions/modes/src/browser/mode-service.ts, theia/extensions/modes/src/browser/modes-commands.ts, theia/extensions/modes/src/browser/modes-frontend-module.ts</files>
  <action>Build custom modes as data beside the shipped literals: resolve the customs file through user storage beside the customize precedent, load it fire-and-forget after startup with never-create absence meaning shipped defaults, total-parse each row dropping unknown fields, register valid customs as descriptors, and on corrupt data fall back to shipped Browsing with the contracted fallback notice flashed on the status bar, never a blank shell and never a throw during startup. Hot-reload through the file-change event filtered to the customs URI with the in-tree debounce spec, re-applying the persisted active id after registration so a custom mode survives relaunch. Save the current panel visibility plus layout snapshot under a typed name with the contracted cap cutting pasted overflow before commit, the contracted empty-name and duplicate-name errors writing nothing partial, and the contracted saved confirmation on success, with a custom never altering a shipped default in place. Register two commands with scope-prefixed exported id consts: an activate command carrying no palette label that the toggle invokes by const import, and a save command carrying the contracted save-mode label verbatim. Bind the service and commands statically at module load beside the plan 14-01 binds. Stage the three files before any scan, compile in the Nix theia shell, and commit as feat(14-02): custom modes with fallback and mode commands. Finish building with the quick gate green.</action>
  <verify>
    <automated>git add theia/extensions/modes/src/browser/mode-service.ts theia/extensions/modes/src/browser/modes-commands.ts theia/extensions/modes/src/browser/modes-frontend-module.ts && nix develop .#theia --command bash -c "cd theia/extensions/modes && yarn build" && grep -qc 'powerbrowser.modes.save-as-mode' theia/extensions/modes/src/browser/modes-commands.ts && grep -qc 'Save as Mode' theia/extensions/modes/src/browser/modes-commands.ts && scripts/verify-platform.sh --quick</automated>
    <fails_when>extension fails to build, either command id missing or the save label reworded, or the quick gate exits non-zero</fails_when>
  </verify>
  <acceptance_criteria>
    - Customs load after startup with absence, corruption, and reload handled per the contracted semantics
    - Save path enforces the cap with contracted empty, duplicate, and confirmation copy and no partial writes
    - Two exported command ids with the contracted label on the save command and none on the activate command
    - All binds static at module load with the extension building clean
  </acceptance_criteria>
  <done>Modes are user-authored data over shipped defaults with fallback that never blanks the shell</done>
</task>

<task type="auto">
  <name>Organising placeholder plus toggle custom rows and style layer</name>
  <reversibility rating="reversible">Placeholder widget and stylesheet behind stable contribution points; the Phase-15 canvas replaces the slot without touching mode machinery.</reversibility>
  <read_first>
    - .planning/phases/14-modes-windows-setups/14-UI-SPEC.md (organising placeholder slot section, toggle custom-name ellipsis rules, Spacing through Motion sections, accent reservation with the fourth selected-row use)
    - .planning/phases/14-modes-windows-setups/14-PATTERNS.md (section 1 toggle custom-row idiom with active marker plus weight, section 2 open-through-contribution routing never hardcoding areas, style-layer mechanics below the user layer)
    - .claude/skills/sketch-findings-Power-Browser/sources/themes/default.css (locked token values, the only token source)
    - theia/extensions/chrome-bar/src/browser/chrome-bar-widget.tsx (Task 14-01 bridge plus segment hooks to extend with menu rows)
    - theia/extensions/modes/src/browser/mode-descriptors.ts (organising descriptor to hook show and hide)
    - theia/extensions/chrome-bar/src/browser/chrome-bar.css lines 1-20 (single-layer mechanics to mirror)
  </read_first>
  <files>theia/extensions/modes/src/browser/organising-placeholder-widget.ts, theia/extensions/modes/src/browser/mode-descriptors.ts, theia/extensions/chrome-bar/src/browser/chrome-bar-widget.tsx, theia/extensions/modes/src/browser/modes.css</files>
  <action>Render the organising slot as one centred static panel at the contracted max width with the contracted heading, body, and a single stock button that selects Browsing, painting synchronously with the switch carrying no spinner, skeleton, canvas, tree, drag surface, or failure state. Hook the organising descriptor to show the panel through the contribution open path on activation and hide it on deactivation, leaving every other descriptor untouched. Append custom modes as menu rows after the shipped segments with names truncated by ellipsis at row width and full names in tooltips, the active row carrying the single chosen selected-row marker plus the reserved weight, the menu scrolling internally past its cap without shifting bar layout, and shipped rows never mutated. Express all of it as one style layer on locked sketch tokens with the three contracted text sizes, delegated typefaces with system fallbacks and no literal stack, the contracted focus outline on every control, the sketch motion baseline with panel slides inside budget and instant under reduced motion, hairline borders, and cascade position below the user layer, with accent confined to the four reserved uses and no second hue. Stage all four files before any scan, compile both touched extensions in the Nix theia shell, and commit as feat(14-02): organising placeholder, custom toggle rows, and modes styling. Finish building with the quick gate green.</action>
  <verify>
    <automated>git add theia/extensions/modes/src/browser/organising-placeholder-widget.ts theia/extensions/modes/src/browser/mode-descriptors.ts theia/extensions/chrome-bar/src/browser/chrome-bar-widget.tsx theia/extensions/modes/src/browser/modes.css && nix develop .#theia --command bash -c "cd theia/extensions/modes && yarn build" && nix develop .#theia --command bash -c "cd theia/extensions/chrome-bar && yarn build" && test $(grep -v '^#' theia/extensions/modes/src/browser/modes.css | grep -c 'font-family') -eq 0 && grep -qc 'Organising arrives next' theia/extensions/modes/src/browser/organising-placeholder-widget.ts && scripts/verify-platform.sh --quick</automated>
    <fails_when>either extension fails to build, the stylesheet sets any literal typeface stack, any contracted placeholder string missing or reworded, or the quick gate exits non-zero</fails_when>
  </verify>
  <acceptance_criteria>
    - Placeholder paints synchronously with contracted copy and one working stock button, nothing canvas-shaped
    - Custom rows append with ellipsis, tooltips, single active marker, and internally scrolling menu
    - One layer on locked tokens with contracted geometry, type roles, focus, motion, and accent reservation below the user layer
    - Both touched extensions build clean
  </acceptance_criteria>
  <done>Organising is a contracted slot and customs read as native toggle entries</done>
</task>

<task type="auto">
  <name>Switch-path invariant gate plus copy coverage as registry rows</name>
  <reversibility rating="reversible">One gate script plus test scaffolding; tightening the allowlist edits one EXPECTED const.</reversibility>
  <read_first>
    - .planning/phases/14-modes-windows-setups/14-PATTERNS.md (section 5 derive-and-compare idiom with non-vacuity and self-test plants, section 1 call-site const discipline)
    - theia/extensions/modes/src/browser/mode-service.ts (Task 1 switch path and save path to derive calls from)
    - theia/extensions/chrome-bar/src/browser/chrome-bar-widget.tsx (Task 14-01 bridge plus Task 2 menu rows to derive calls from)
    - theia/extensions/modes/src/browser/modes-commands.ts (the mode command ids and labels the gate derives)
    - scripts/verify-shell-error-copy.mjs (derivation harness to extend over the modes sources)
    - scripts/verify-platform.sh lines 3723-3724 (gui07 verdict row shape plus honesty comment to sit beside)
  </read_first>
  <files>scripts/verify-mode-switch-tabs-invariant.mjs, scripts/verify-shell-error-copy.mjs, scripts/verify-platform.sh</files>
  <action>Write the invariant gate honestly quick: derive the set of shell-mutating calls on every mode-switch path from the service and widget sources at check time and compare as set equality against one EXPECTED allowlist covering stock perspective switching, side-panel expand and collapse, contribution-routed placeholder open and close, and chip re-assertion — red on any addition and any removal with a distinct failure on empty derivation — and derive the two mode command ids from the commands source with set equality plus the contracted save label verbatim and the labelless activate command, requiring call sites to import the exported consts. Give it a self-test asserting green first, then a planted switch-path close call, a planted command-id removal, and a planted label drift each going red naming the drift with the plant-landed assertion. Extend the copy gate derivation to cover the new modes sources so every new user-facing string is checked for internal identifiers, keeping its existing self-test green. Append two rows beside the toggle pair with honesty comments, creating no sibling driver. Stage the three files before any scan and commit as feat(14-02): switch invariant gate and copy coverage. Finish with both scripts green, both self-tests green, single-gate sampling green across the new rows plus the guard rows, and the quick gate green.</action>
  <verify>
    <automated>git add scripts/verify-mode-switch-tabs-invariant.mjs scripts/verify-shell-error-copy.mjs scripts/verify-platform.sh && node --check scripts/verify-mode-switch-tabs-invariant.mjs && node scripts/verify-mode-switch-tabs-invariant.mjs && node scripts/verify-mode-switch-tabs-invariant.mjs --self-test && node scripts/verify-shell-error-copy.mjs --self-test && scripts/verify-platform.sh --only gui07-mode-switch-tabs-invariant && scripts/verify-platform.sh --only gui07-mode-switch-tabs-invariant-self-test && scripts/verify-platform.sh --only gui07-mode-toggle-commands && scripts/verify-platform.sh --only shell-error-copy-no-internals && scripts/verify-platform.sh --only gui04-registry-shape && scripts/verify-platform.sh --quick</automated>
    <fails_when>the gate exits non-zero unmodified, any self-test plant fails to go red naming the drift, the extended copy self-test regresses, any sampled row fails, or the quick gate exits non-zero</fails_when>
  </verify>
  <acceptance_criteria>
    - Switch-path calls match the EXPECTED allowlist exactly with green-first self-test on three plants
    - Mode command ids derived with set equality plus verbatim save label and labelless activate
    - Copy gate covers the modes sources with its self-test still green
    - New rows plus guard rows green through the single-gate runner with quick green
  </acceptance_criteria>
  <done>No switch path can close, move, or detach a tab without the gate going red by name</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User-authored JSON to the shell | Customs file content becomes registered descriptors and layouts. |
| Save dialog text to storage | Typed mode names become persisted rows and menu entries. |
| Placeholder button to mode state | One stock control switches back to Browsing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-02-01 | Tampering | malformed customs file crashing startup or blanking the shell | high | mitigate | Never-throw load with catch fallback, total parsing dropping unknown fields, corrupt data falling back to shipped Browsing with the contracted notice. |
| T-14-02-02 | Tampering | mode name as code or markup | medium | mitigate | Names render as text with ellipsis plus tooltip, capped before commit, with contracted empty and duplicate errors writing nothing partial. |
| T-14-02-03 | Information disclosure | internal identifier in new user-facing copy | medium | mitigate | Contracted strings verbatim from the UI-SPEC; extended copy gate stays green; diagnostics carry what copy omits. |
| T-14-02-04 | Denial of Service | switch path unmounting tabs | high | mitigate | Switch-path allowlist gate derives calls at check time and goes red naming any addition; chip re-asserts the count on every switch. |
| T-14-02-SC | Tampering | npm/pip/cargo installs | medium | mitigate | No new registry package; persistence reuses the file service and the pinned debounce spec. |
</threat_model>

<verification>
Customs save, reload, and fall back per contract, placeholder paints synchronously with contracted copy, toggle lists customs without mutating shipped rows, stylesheet carries no literal typeface stack, invariant gate plus self-test green, copy gate plus self-test green, sampled rows plus guard rows green, quick green, core diff clean.
</verification>

<success_criteria>
The user customises modes as data with a safe fallback, organising holds a contracted slot, and the tabs invariant is enforced mechanically on every switch path.
</success_criteria>

<output>
Modes complete per contract: customs, placeholder, toggle rows, style layer, invariant and copy gates; plan 14-03 adds dependents and setups on this foundation.
</output>
