---
phase: 14-modes-windows-setups
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [.planning/phases/14-modes-windows-setups/14-PROBE-DEPENDENT-WINDOWS.md, theia/extensions/modes/package.json, theia/extensions/modes/tsconfig.json, theia/extensions/modes/src/browser/mode-descriptors.ts, theia/extensions/modes/src/browser/modes-frontend-module.ts, theia/package.json, theia/applications/browser/package.json, theia/extensions/chrome-bar/src/browser/chrome-bar-widget.tsx, scripts/verify-mode-toggle-commands.mjs, scripts/verify-platform.sh]
autonomous: true
requirements: [GUI-07, GUI-09]
must_haves:
  truths:
    - "User switches Coding / Browsing / Organising from the chrome-bar toggle and panels reshape with the strip staying top in every mode"
    - "Every tab persists across every mode switch with the status-bar chip re-asserting the count"
    - "A live probe record proves whether dependent windows open through the stock secondary-window path or need the chrome-side fallback"
    - "Shipped mode defaults are enforced mechanically against the toggle"
  artifacts:
    - path: ".planning/phases/14-modes-windows-setups/14-PROBE-DEPENDENT-WINDOWS.md"
      provides: "GREEN/RED routing verdict for dependent-window implementation"
      contains: "Verdict:"
    - path: "theia/extensions/modes/src/browser/mode-descriptors.ts"
      provides: "Three shipped PerspectiveDescriptors with RED-bounded shell map"
      contains: "browsing"
    - path: "theia/extensions/modes/src/browser/modes-frontend-module.ts"
      provides: "Static module-load binds for the modes extension"
      contains: "FrontendApplicationContribution"
    - path: "theia/extensions/chrome-bar/src/browser/chrome-bar-widget.tsx"
      provides: "Toggle bridged to PerspectiveService with chip re-assert"
      contains: "switchPerspective"
    - path: "scripts/verify-mode-toggle-commands.mjs"
      provides: "Shipped-mode defaults gate with self-test"
      min_lines: 60
    - path: "scripts/verify-platform.sh"
      provides: "Toggle-gate entry plus self-test rows beside the gui07 verdict pair"
      contains: "mode-toggle-commands"
  key_links:
    - from: "theia/extensions/chrome-bar/src/browser/chrome-bar-widget.tsx"
      to: "theia/extensions/modes/src/browser/mode-descriptors.ts"
      via: "toggle segments and descriptors agree on ids, order, and labels through the gate, never imports"
      pattern: "MODES"
    - from: "theia/extensions/modes/src/browser/modes-frontend-module.ts"
      to: "theia/extensions/modes/src/browser/mode-descriptors.ts"
      via: "contribution registers shipped descriptors synchronously in onStart and returns immediately"
      pattern: "registerPerspective"
    - from: "scripts/verify-platform.sh"
      to: "scripts/verify-mode-toggle-commands.mjs"
      via: "registry row invokes the toggle gate; self-test row invokes it with --self-test"
      pattern: "mode-toggle-commands"
---

<objective>
Prove the load-bearing unknown first, then land the modes tracer: a BiDi window-routing probe with a GREEN/RED record, a new @powerbrowser/modes extension holding the three shipped PerspectiveDescriptors, the chrome-bar toggle bridged to stock switching, and the defaults gate green.

Purpose: every later task in this phase builds on two facts established here — which opener hosts dependents (probe verdict) and that one mode switch works end-to-end with tabs invariant (tracer). Nothing in this plan relocates the strip, moves widgets between areas, or touches Gecko.
Output: probe record, modes extension skeleton with shipped descriptors, bridged toggle, toggle gate with self-test, quick green.

Extension-host call (resolves RESEARCH Open Question 1): NEW extension @powerbrowser/modes, not chrome-bar growth. Rationale: this phase adds six-plus new sources plus its own CSS layer and command namespaces, which would triple chrome-bar past the single-concern threshold; the copy-verbatim skeleton precedent exists (customize package.json, tab-uris tsconfig, hand-appended build:extensions); the toggle bridge needs no cross-extension import because both sides inject stock PerspectiveService from @theia/core.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-modes-windows-setups/14-CONTEXT.md
@.planning/phases/14-modes-windows-setups/14-RESEARCH.md
@.planning/phases/14-modes-windows-setups/14-PATTERNS.md
@.planning/phases/14-modes-windows-setups/14-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>BiDi window-routing probe with GREEN/RED record</name>
  <reversibility rating="reversible">Untracked scratch removed at closeout; the record is a decision artifact, not shipped code.</reversibility>
  <read_first>
    - .planning/phases/13-chrome-bar-strip-relocation-spike/13-SPIKE-STRIP-RELOCATION.md (whole file: header block, what-changed, build, instrumentation note, observations, constraints, summary table, artefacts, ratification, proofs — imitate this structure)
    - .planning/phases/14-modes-windows-setups/14-RESEARCH.md (Pitfall 1, Assumptions A1-A5, Environment Availability, Code Examples extract section)
    - scripts/lib/firefox-bidi.mjs (withFirefoxPage temp-profile/finally pattern from the 13-01 precedent)
    - theia/applications/browser/lib/frontend/secondary-window.html (static read for assumption A3)
  </read_first>
  <files>.planning/phases/14-modes-windows-setups/14-PROBE-DEPENDENT-WINDOWS.md</files>
  <action>Run the load-bearing probe in one BiDi session reusing the 13-01 harness: build the sidecar in the Nix theia shell only if the lib tree is stale, launch the existing powerbrowser binary on a temp profile removed in a finally, then extract a live widget through SecondaryWindowHandler.moveWidgetToSecondaryWindow and observe whether the dependent loads the secondary frame or falls through to stock chrome, move and resize core plus secondary windows and read back screenX and outerWidth, run the two-by-two close matrix of closing in core versus closing the dependent across dirty and clean widgets, and record the restore-log order relative to contributions. Keep all harness scratch under an untracked tmp-prefixed directory that is removed at closeout, and stage no probe file under theia or scripts. Write the probe record imitating the 13-SPIKE section structure with a one-line GREEN or RED verdict plus the implementation routing it selects, the three shipped-tree proofs of a clean core diff with the instrument output pasted, an empty upstream diff, and a clean shipped tree, and the verbatim observed evidence behind every observation. A GREEN verdict routes plan 14-03 to the stock secondary-window path; a RED verdict routes it to the chrome-side opener fallback through the existing boundary file with a catalogue amendment and no new patch. Commit as docs(14-01): record dependent-window routing probe. Finish with the quick gate green.</action>
  <verify>
    <automated>git add .planning/phases/14-modes-windows-setups/14-PROBE-DEPENDENT-WINDOWS.md && grep -q '^Verdict: \(GREEN\|RED\)' .planning/phases/14-modes-windows-setups/14-PROBE-DEPENDENT-WINDOWS.md && git -C upstream diff --quiet && test -z "$(git status --porcelain theia/ scripts/ | head -5)" && scripts/verify-platform.sh --quick</automated>
    <fails_when>record missing or verdict line malformed, upstream diff non-empty, any shipped-tree file staged by the probe, or the quick gate exits non-zero</fails_when>
  </verify>
  <acceptance_criteria>
    - Record follows the 13-SPIKE section structure with a one-line GREEN or RED verdict plus routing
    - Observations cover popup survival, geometry roundtrip, the close matrix, and restore-log order with verbatim evidence
    - Scratch removed with nothing staged under theia or scripts
    - Upstream diff empty with the core-diff proof pasted in the record
  </acceptance_criteria>
  <done>Dependent-window implementation order is decided by live evidence, not assumption</done>
</task>

<task type="auto">
  <name>Modes extension skeleton with shipped descriptors</name>
  <reversibility rating="costly">Extension name, descriptor ids, and composition entries are addressed by later plans and gates; renaming touches each consumer, but no stored format changes.</reversibility>
  <read_first>
    - theia/extensions/customize/package.json (whole file: dep spec shape to copy verbatim — private, version 0.1.0, license MIT, theiaExtensions frontend entry, files lib plus src, clean/build scripts)
    - theia/extensions/tab-uris/tsconfig.json (whole file: compiler options to copy verbatim)
    - .planning/phases/14-modes-windows-setups/14-RESEARCH.md (Pattern 1 descriptor shape and registration calls, Ordering paragraph on synchronous shipped registration, Code Examples register block)
    - .planning/phases/14-modes-windows-setups/14-PATTERNS.md (sections 1 composition with static binds at module load, 2 expand/collapse as the only sanctioned move, 3 user-storage naming)
    - .planning/phases/14-modes-windows-setups/14-UI-SPEC.md (per-mode shell map table, strip-never-moves binding, shipped mode names and order)
    - theia/package.json line 69 and theia/applications/browser/package.json chrome-bar dependency line (hand-append composition points)
  </read_first>
  <files>theia/extensions/modes/package.json, theia/extensions/modes/tsconfig.json, theia/extensions/modes/src/browser/mode-descriptors.ts, theia/extensions/modes/src/browser/modes-frontend-module.ts, theia/package.json, theia/applications/browser/package.json</files>
  <action>Create the modes extension by copying the customize package shape with the modes name and depending only on already-pinned in-tree specs — theia core, filesystem, and userstorage at the pinned minor, the tab-uris workspace version, and the debounce pin at its exact in-tree spec — adding no new registry package, and copy the tab-uris tsconfig verbatim. Write the shipped descriptors as static literals with contracted ids in contracted order: browsing and organising collapse all three side areas while coding leaves Explorer visible, the strip named in no descriptor, custom modes never mutating a shipped default, and organising placeholder show/hide left to plan 14-02 hooks. Write the frontend module with static binds at module load beside each other, registering the three shipped descriptors synchronously in onStart and returning immediately with user-data loads deferred to later plans. Append the build entries and the application dependency by hand in the existing voice. Stage all six files before any scan, compile in the Nix theia shell, and commit as feat(14-01): modes extension skeleton with shipped descriptors. Finish building with the quick gate green.</action>
  <verify>
    <automated>git add theia/extensions/modes/package.json theia/extensions/modes/tsconfig.json theia/extensions/modes/src/browser/mode-descriptors.ts theia/extensions/modes/src/browser/modes-frontend-module.ts theia/package.json theia/applications/browser/package.json && nix develop .#theia --command bash -c "cd theia/extensions/modes && yarn build" && grep -qc "registerPerspective" theia/extensions/modes/src/browser/modes-frontend-module.ts && scripts/verify-platform.sh --quick</automated>
    <fails_when>extension fails to build, registration call absent, any new registry package introduced, or the quick gate exits non-zero</fails_when>
  </verify>
  <acceptance_criteria>
    - Extension builds with only pinned in-tree dependency specs and the verbatim tsconfig shape
    - Three shipped descriptors match the contracted shell map with the strip named nowhere
    - Shipped registration is synchronous in onStart with no awaited user-data read
    - Composition entries appended by hand in the existing voice
  </acceptance_criteria>
  <done>Modes live as stock descriptors behind a skeleton later plans extend</done>
</task>

<task type="auto">
  <name>Toggle bridge to stock switching plus defaults gate</name>
  <reversibility rating="reversible">Toggle internals behind stable descriptor ids and segment hooks; reworking the bridge touches one widget method.</reversibility>
  <read_first>
    - theia/extensions/chrome-bar/src/browser/chrome-bar-widget.tsx lines 55-80 and 216-260 (MODES literal, selectMode, countTabs, publishTabCount, status element id to preserve)
    - .planning/phases/14-modes-windows-setups/14-PATTERNS.md (section 1 toggle extension rules with custom rows appending never mutating, chip re-assert shape, plus section 5 gate idiom with one EXPECTED const, derivation, set equality both directions, non-vacuity, self-test plants)
    - .planning/phases/14-modes-windows-setups/14-UI-SPEC.md (toggle order, fixed segment width with ellipsis, active segment accent ink plus reserved weight, chip invariant, motion timing)
    - theia/extensions/modes/src/browser/mode-descriptors.ts (Task 2 shipped literals the gate derives)
    - scripts/verify-platform.sh lines 3723-3724 (gui07 verdict row shape plus honesty comment to sit beside)
  </read_first>
  <files>theia/extensions/chrome-bar/src/browser/chrome-bar-widget.tsx, scripts/verify-mode-toggle-commands.mjs, scripts/verify-platform.sh</files>
  <action>Bridge the toggle to stock switching without touching its shipped surface: inject the stock perspective service into the widget, route segment selection through the perspective switch for the matching shipped id, subscribe to perspective change to set the active segment and re-assert the chip, keep the MODES literal with contracted order, the fixed segment widths, the before/after tab-count mismatch log that never throws, and the switch still landing on mismatch. Write the defaults gate honestly quick: derive registered descriptor ids, labels, order, and the placement map from the descriptors source plus the widget MODES literal at check time, compare as set equality against one EXPECTED const covering the three shipped modes, fail distinctly on empty derivation, and assert through a scoped negated search that no manifest flag for modes exists in the configuration schema, the generator, or the modes sources. Give it a self-test asserting green first, then a planted id removal, a label drift, a reorder, and a placement drift each going red naming the drift with the plant-landed assertion. Append two rows beside the gui07 verdict pair with honesty comments, creating no sibling driver. Stage the three files before any scan and commit as feat(14-01): toggle bridge and shipped-mode defaults gate. Finish with the script green, its self-test green, single-gate sampling green across the new rows plus the guard rows, and the quick gate green.</action>
  <verify>
    <automated>git add theia/extensions/chrome-bar/src/browser/chrome-bar-widget.tsx scripts/verify-mode-toggle-commands.mjs scripts/verify-platform.sh && nix develop .#theia --command bash -c "cd theia/extensions/chrome-bar && yarn build" && node --check scripts/verify-mode-toggle-commands.mjs && node scripts/verify-mode-toggle-commands.mjs && node scripts/verify-mode-toggle-commands.mjs --self-test && scripts/verify-platform.sh --only gui07-mode-toggle-commands && scripts/verify-platform.sh --only gui07-mode-toggle-commands-self-test && scripts/verify-platform.sh --only gui07-strip-spike-verdict && scripts/verify-platform.sh --only gui04-registry-shape && scripts/verify-platform.sh --only internals-catalogue && scripts/verify-platform.sh --quick</automated>
    <fails_when>either extension fails to build, the gate exits non-zero unmodified, any self-test plant fails to go red naming the drift, any sampled row fails, or the quick gate exits non-zero</fails_when>
  </verify>
  <acceptance_criteria>
    - Segment selection switches stock perspectives with the shipped literal, order, widths, and chip assertion intact
    - Gate derives ids, labels, order, and placements with set equality plus green-first self-test on four plants
    - Scoped search proves modes ship as data with no manifest flag
    - New rows plus guard rows green through the single-gate runner with quick green
  </acceptance_criteria>
  <done>One mode switch works end-to-end with tabs invariant and defaults enforced mechanically</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Probe harness to the running shell | Scratch BiDi session drives a temp-profile build; nothing persists. |
| Toggle gesture to shell layout | Segment selection reshapes panels through stock switching. |
| Descriptor literals to the gate | Shipped defaults are asserted, never trusted by inspection. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-01-01 | Denial of Service | mode switch losing tabs | high | mitigate | Switch routes through stock saved-layout restore; before/after count mismatch is logged never thrown and the switch still lands; chip re-asserts on every change. |
| T-14-01-02 | Tampering | unreviewed mode or placement entering the shell | medium | mitigate | Defaults gate compares derived ids, order, labels, and placements as set equality, red on addition and removal, with non-vacuity and four planted faults. |
| T-14-01-03 | Elevation of privilege | modes as manifest flags widening the config attack surface | low | mitigate | Modes ship as descriptors plus user data only; scoped search proves no manifest flag exists in schema, generator, or modes sources. |
| T-14-01-04 | Tampering | probe scratch polluting the shipped tree | low | mitigate | Scratch lives under an untracked tmp-prefixed directory removed at closeout; record asserts no staged file under theia or scripts. |
| T-14-01-SC | Tampering | npm/pip/cargo installs | medium | mitigate | No new registry package; the modes package depends only on pinned in-tree specs including the exact debounce spec. |
</threat_model>

<verification>
Probe record carries a well-formed verdict with live evidence and clean-tree proofs, modes extension builds with shipped descriptors registered synchronously, toggle switches stock perspectives with the chip asserting the invariant, toggle gate plus self-test green, sampled rows plus guard rows green, quick green, core diff clean.
</verification>

<success_criteria>
The phase tracer is proven: popup routing is decided by evidence and one mode switch works end-to-end with tabs invariant and defaults under mechanical enforcement.
</success_criteria>

<output>
Phase 14 entry slice: probe verdict routing dependent-window work, modes extension with shipped descriptors, bridged toggle, defaults gate; plans 14-02 and 14-03 expand from this proven slice.
</output>
