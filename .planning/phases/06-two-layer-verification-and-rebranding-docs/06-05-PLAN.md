---
phase: 06-two-layer-verification-and-rebranding-docs
plan: 05
type: execute
wave: 5
depends_on: [06-04]
files_modified: [docs/REBRANDING.md, scripts/verify-rebranding-docs.mjs, scripts/verify-platform.sh]
autonomous: true
requirements: [DOC-01]
user_setup: []

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A stranger can go from clone to branded build following docs/REBRANDING.md alone"
    - "Every configuration.toml field is documented; an undocumented field fails the gate"
    - "Downstream trademark, profile-migration, and fixed-identifier obligations are stated in the guide"
  artifacts:
    - path: "docs/REBRANDING.md"
      provides: "Stranger walkthrough plus every-field reference"
      min_lines: 200
    - path: "scripts/verify-rebranding-docs.mjs"
      provides: "Manifest-schema vs guide coverage gate"
      min_lines: 80
    - path: "scripts/verify-platform.sh"
      provides: "Two new registry rows for the doc-coverage check and its self-test"
      contains: "verify-rebranding-docs"
  key_links:
    - from: "scripts/verify-rebranding-docs.mjs"
      to: "scripts/generate.mjs"
      via: "derives the documented-field list from the generator's known-setting schema at check time"
      pattern: "generate.mjs"
    - from: "docs/REBRANDING.md"
      to: "configuration.toml"
      via: "every dotted field path in the manifest appears in the guide's reference table"
      pattern: "configuration.toml"
---

<objective>
Write docs/REBRANDING.md and pin its completeness with a derived doc-coverage gate.

Purpose: DOC-01 — a stranger goes from clone to branded build with no prior knowledge of the tree, and every configuration.toml field is documented. Pitfall research names the exact failure modes this guide must pre-empt: reference-not-walkthrough (Pitfall FEATURES), silent-default surprise (Pitfall 5), profile stranding on rebrand (Pitfall 22), fixed-vs-configurable confusion (ARCHITECTURE pattern 4), and unstated trademark obligations (Pitfall 14).

Output: docs/REBRANDING.md, scripts/verify-rebranding-docs.mjs with --self-test, two registry rows.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@configuration.toml
@.planning/phases/06-two-layer-verification-and-rebranding-docs/06-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Write docs/REBRANDING.md as walkthrough plus reference</name>
  <files>docs/REBRANDING.md</files>
  <action>Write docs/REBRANDING.md in two halves. Walkthrough half: numbered clone-to-branded-build path with exact commands a first-time reader pastes in order — prerequisites (repo path with no space, nix develop .#firefox vs .#theia shells and which tree each serves), scripts/fetch-upstream.sh, editing configuration.toml, dropping brand/mark.svg (square viewBox, dual-fill rule), node scripts/generate.mjs plus generate --check, the tier-3 build with its real cost pointer at docs/BUILD.md, then scripts/verify-platform.sh --quick and the six-surface identity check. Reference half: a table of every configuration.toml dotted field grouped by section stating required vs defaulted, the default and its generate-time echo, and what surfaces each field reaches; the vendor_machine vs vendor_display split and why collapsing them breaks the profile path; fixed-forever internal identifiers (powerbrowser/ tree, @powerbrowser scope, chrome URI) that a downstream must not expect to change; build-time-only branding with no hot rebrand; downstream obligations — own unique identity, Mozilla non-association statement, Eclipse Theia attribution, no use of Power Browser marks, profile old-to-new path mapping with copy command. Name the product as Power Browser throughout; no internal identifier in user-facing prose per the UI-SPEC rule. Verify prose factually against the tree (every command and path must exist) rather than from memory.</action>
  <verify><automated>test -f docs/REBRANDING.md && node scripts/scan-brand-residue.mjs</automated></verify>
  <done>Guide complete in both halves; residual scan still green with the new prose file in scope</done>
</task>

<task type="auto">
  <name>Land the doc-coverage gate and register it</name>
  <files>scripts/verify-rebranding-docs.mjs, scripts/verify-platform.sh</files>
  <action>Write scripts/verify-rebranding-docs.mjs as an honestly --quick static gate: derive the full dotted field list at check time from scripts/generate.mjs's known-setting schema (the same table that rejects unknown settings — a second hand-kept list is the defect), plus the variant and extensions entry shapes, and require each dotted path to appear in docs/REBRANDING.md as inline code or heading, failing with the missing paths named; require the walkthrough's load-bearing commands (fetch-upstream, generate.mjs, generate --check, verify-platform.sh --quick) to appear as code spans, failing with the missing command named. Non-vacuity: a missing guide, an empty derived field set, or zero matched fields each fail distinctly. The --self-test runs against scratch copies: one field's mentions removed goes red naming the field, one command removed goes red naming it, unmutated copy green first. Then append the check row plus its mandatory self-test row beside the other static rows and prove both via --only, followed by the full --quick set.</action>
  <verify><automated>node scripts/verify-rebranding-docs.mjs && node scripts/verify-rebranding-docs.mjs --self-test && scripts/verify-platform.sh --only verify-rebranding-docs && scripts/verify-platform.sh --quick</automated></verify>
  <done>Coverage gate green on the shipped guide; planted field and command removals go red naming each; registry rows present; --quick fully green</done>
</task>

<task type="auto">
  <name>Stranger-read the guide against a fresh clone mental model</name>
  <files>docs/REBRANDING.md</files>
  <action>No file edits except genuine gaps found. Re-read docs/REBRANDING.md end to end as a reader who has never seen this tree and confirm every command resolves to a real script, every referenced path exists, every configuration.toml field named in the guide matches the manifest's actual keys, and no step assumes knowledge from .planning/ or prior phases. Log each confirmation briefly in the SUMMARY; fix real gaps in place and re-run the coverage gate.</action>
  <verify><automated>node scripts/verify-rebranding-docs.mjs</automated></verify>
  <done>Guide verified step by step against the tree; coverage gate still green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| guide→downstream | Every downstream inherits the guide's claims; a wrong command or omitted obligation ships in all of them |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-05 | Tampering | guide command accuracy | low | mitigate | Stranger-read task verifies every command and path against the tree; coverage gate pins field completeness going forward |
</threat_model>

<verification>
Guide complete and stranger-read; coverage gate plus self-test green; both --only rows green; full --quick green; residual scan green.
</verification>

<success_criteria>
- docs/REBRANDING.md walks clone to branded build with exact commands and documents every field (DOC-01)
- Undocumented field or missing walkthrough command fails the gate naming it
- Trademark, profile-migration, and fixed-identifier obligations stated
</success_criteria>

<output>
Create `.planning/phases/06-two-layer-verification-and-rebranding-docs/06-05-SUMMARY.md` when done
</output>
