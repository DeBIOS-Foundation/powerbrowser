---
phase: 06-two-layer-verification-and-rebranding-docs
plan: 03
type: execute
wave: 3
depends_on: [06-02]
files_modified: [brand/HUMAN-REVIEW.md, scripts/verify-trademark-surface.mjs, scripts/verify-platform.sh]
autonomous: false
requirements: [VER-01]
user_setup: []

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every file in brand/ has a named human review with reviewer and date on record"
    - "A new file added to brand/ fails the static gate until the ritual is re-recorded"
    - "Trademark-asset and display-field violations fail the static gate naming the file"
  artifacts:
    - path: "brand/HUMAN-REVIEW.md"
      provides: "Durable named human review record for every file in brand/"
    - path: "scripts/verify-trademark-surface.mjs"
      provides: "Mechanical trademark gate with review-agreement check"
      min_lines: 120
    - path: "scripts/verify-platform.sh"
      provides: "Two new registry rows for the check and its self-test"
      contains: "verify-trademark-surface"
  key_links:
    - from: "scripts/verify-trademark-surface.mjs"
      to: "brand/HUMAN-REVIEW.md"
      via: "derives brand/ file list at check time and requires set equality with the reviewed list"
      pattern: "HUMAN-REVIEW"
---

<objective>
Close the ROADMAP research note: re-check the LOW-confidence trademark findings against primary policy text, record the named human review of every file in brand/, and land a mechanical trademark gate.

Purpose: The milestone gates downstream shipment on marks the platform got right. Pitfall 14's Mozilla and Eclipse findings were web-sourced and explicitly flagged for primary-source re-verification before any gate depends on them. That re-check was done at plan time (findings below — no network needed at execution); this plan records the ritual, then automates the mechanical half.

Output: brand/HUMAN-REVIEW.md signed via checkpoint, scripts/verify-trademark-surface.mjs with --self-test, two registry rows.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@configuration.toml
</context>

<tasks>

<task type="auto">
  <name>Draft the brand review record from mechanical facts</name>
  <files>brand/HUMAN-REVIEW.md</files>
  <action>Derive the file list under brand/ at task time (currently brand/mark.svg only — confirm by listing, do not assume) and draft brand/HUMAN-REVIEW.md containing: the review procedure (open every file in brand/, not judged by filename; confirm no third-party marks, logos, or wordmarks of Mozilla, Eclipse, or any other owner; confirm mark.svg is an original square dual-fill mark), a per-file table with reviewer and date left as TO-BE-SIGNED placeholders, and the primary-source basis recorded verbatim — Mozilla Trademark Guidelines Open Source Project Guidelines at https://www.mozilla.org/en-US/foundation/trademarks/policy/ (modified code must carry its own unique identity, must not use Mozilla marks in user-facing name or branding, sanctioned framing is words-only based-on-Mozilla-technology plus an equally prominent not-officially-associated statement; confirmed 2026-09-04) and Eclipse Foundation Trademark Usage Policy v1.1.1 effective 2026-03-18 (project names such as Theia are Eclipse Project Trademarks, must not be incorporated into the product name, first and most prominent reference per page reads Eclipse Theia, attribution sentence of the form marks are trademarks of Eclipse Foundation AISBL is required; confirmed 2026-09-04). Confirmations to record: Power Browser incorporates neither mark in its name, binary, or identifiers. Leave reviewer/date for the checkpoint; do everything else now.</action>
  <verify><automated>test -f brand/HUMAN-REVIEW.md && grep -c "TO-BE-SIGNED" brand/HUMAN-REVIEW.md</automated></verify>
  <done>Review record drafted with derived file list, procedure, primary-source basis with URLs and dates, and unsigned placeholders</done>
</task>

<task type="checkpoint:human-action">
  <action>Perform the brand/ human review ritual and sign the record</action>
  <instructions>Everything mechanical is prepared in brand/HUMAN-REVIEW.md. The one thing requiring a human: open every file listed under brand/ (currently brand/mark.svg), confirm by inspection that it carries no third-party mark, and reply with your name and today's date plus either CONFIRMED or a finding. The executor will write the name and date into the record, replacing the placeholders.</instructions>
  <verification>Executor re-runs the mechanical file listing and requires set equality with the signed list before resolving the checkpoint</verification>
  <resume-signal>Type your name, the date, and CONFIRMED (or describe the finding)</resume-signal>
</task>

<task type="auto">
  <name>Land the mechanical trademark gate and register it</name>
  <files>scripts/verify-trademark-surface.mjs, scripts/verify-platform.sh</files>
  <action>Write scripts/verify-trademark-surface.mjs as an honestly --quick static gate deriving everything at check time: trademark-asset walk over git ls-files requiring the set of tracked files matching trademark-asset shapes (mozilla logo basenames such as moz.png, firefox logo containers such as Assets.car, installer logo payloads such as msix asset files, icns files outside the generator's gitignored output) to equal the empty set, failing with the offending path; display-field scan over the emitted branding surfaces (per-variant brand.ftl and brand.properties, the generated theia branding fragment, desktop entry Name lines) requiring no Firefox or Mozilla token by boundary match, failing with file and line; legal-keys presence requiring non-empty license, copyright_holder, and trademark_notice in configuration.toml; review-agreement requiring the brand/ directory listing at check time to equal the signed list parsed from brand/HUMAN-REVIEW.md, so a newly added brand file fails naming it until the ritual is re-recorded. The --self-test plants four faults (asset basename, display-field token, emptied legal key, unlisted brand file), each going red naming the drift with the unplanted control green first. Then append the check row plus its mandatory self-test row to the CHECKS registry beside the other static branding rows and prove both via --only, followed by the full --quick set.</action>
  <verify><automated>node scripts/verify-trademark-surface.mjs && node scripts/verify-trademark-surface.mjs --self-test && scripts/verify-platform.sh --only verify-trademark-surface && scripts/verify-platform.sh --quick</automated></verify>
  <done>Gate green on the shipped tree; all four planted faults go red naming the drift; registry rows present; --quick fully green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| brand/→downstream | Every file in brand/ ships in every downstream; an unreviewed addition is inherited by all of them |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-03 | Repudiation | brand/ review provenance | medium | mitigate | Named reviewer plus date committed in brand/HUMAN-REVIEW.md; review-agreement check fails on any unlisted brand/ file |
</threat_model>

<verification>
Signed review record committed; mechanical gate plus self-test green; both --only rows green; --quick fully green.
</verification>

<success_criteria>
- Primary-text re-check recorded with URLs, versions, and dates; LOW-confidence flag lifted for the two cited findings
- Every brand/ file carries reviewer and date; new brand/ files fail the gate until re-reviewed
- Asset, display-field, and legal-key assertions all red-capable via planted faults
</success_criteria>

<output>
Create `.planning/phases/06-two-layer-verification-and-rebranding-docs/06-03-SUMMARY.md` when done
</output>
