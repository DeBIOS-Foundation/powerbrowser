---
phase: 12-sql-store-build
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [powerbrowser/shell/PowerBrowserAPI.sys.mjs, powerbrowser/INTERNAL-APIS.md, theia/extensions/tab-uris/src/browser/browser-tab-uri.ts, scripts/verify-sql-store-roundtrip.mjs]
autonomous: true
requirements: [SQL-01, SQL-05]
must_haves:
  truths:
    - "Every tab is a SQL row written only by the chrome-side writer into its own tabs.sqlite in the profile dir, with sessionstore still authoritative for restore"
    - "Registry URIs are the join key: browser-tab row keys use the beside-registry emission and the frozen TabUriRegistry shape is untouched"
    - "A URI-to-row-to-restart-to-reopen roundtrip passes on a temp DB with the restored set equal to sessionstore"
  artifacts:
    - path: "powerbrowser/shell/PowerBrowserAPI.sys.mjs"
      provides: "Writer wrappers behind the sole boundary (open/write/remove/read/prune, triggers, quarantine)"
      contains: "tabs.sqlite"
    - path: "powerbrowser/INTERNAL-APIS.md"
      provides: "Catalogue row per new writer touchpoint with exact file:line"
      contains: "PowerBrowserAPI.sys.mjs"
    - path: "theia/extensions/tab-uris/src/browser/browser-tab-uri.ts"
      provides: "Beside-registry browser-tab key emission (frozen registry untouched)"
      min_lines: 15
    - path: "scripts/verify-sql-store-roundtrip.mjs"
      provides: "Temp-DB roundtrip proof with --self-test, DDL derived from writer source"
      min_lines: 60
  key_links:
    - from: "powerbrowser/shell/PowerBrowserAPI.sys.mjs"
      to: "powerbrowser/INTERNAL-APIS.md"
      via: "each new touchpoint line gains one catalogue row with exact file:line"
      pattern: "PowerBrowserAPI\\.sys\\.mjs:[0-9]+"
    - from: "scripts/verify-sql-store-roundtrip.mjs"
      to: "powerbrowser/shell/PowerBrowserAPI.sys.mjs"
      via: "roundtrip script extracts the DDL from writer-source markers, never a copy"
      pattern: "PowerBrowserAPI\\.sys\\.mjs"
    - from: "powerbrowser/shell/PowerBrowserAPI.sys.mjs"
      to: "theia/extensions/tab-uris/src/browser/browser-tab-uri.ts"
      via: "chrome key expression and beside-registry function implement the same one-line rule, roundtrip asserts agreement naming the URI"
      pattern: "browser-tab-uri"
---

<objective>
Ship the chrome-side writer tracer: Sqlite.sys.mjs behind PowerBrowserAPI.sys.mjs persisting every tab as a row in its own tabs.sqlite, with triggers, the beside-registry key rule, all seven Phase 11 fixes, catalogue rows, and a temp-DB roundtrip proof.

Purpose: SQL-01 requires the single chrome-side writer behind the sole boundary with sessionstore authoritative and registry URIs as join key; this plan proves that slice live before read paths build on it. It also scaffolds SQL-05 gate 3 (roundtrip) as a working script plan 12-03 promotes to a registry row.
Output: Writer wrappers + triggers in PowerBrowserAPI.sys.mjs, new INTERNAL-APIS.md rows, beside-registry key module, roundtrip proof script, static gates green.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-sql-store-build/12-CONTEXT.md
@.planning/phases/12-sql-store-build/12-RESEARCH.md
@.planning/phases/12-sql-store-build/12-PATTERNS.md
@.planning/phases/11-sql-store-design/authority/AUTHORITY.md
@.planning/phases/11-sql-store-design/schema/SCHEMA.md
@.planning/phases/11-sql-store-design/schema/MIGRATIONS.md
@powerbrowser/INTERNAL-APIS.md
</context>

<tasks>

<task type="tracer">
  <name>End-to-end writer slice — open, DDL, upsert, private filter, quarantine, catalogue rows</name>
  <reversibility rating="one-way">On-disk format (DDL columns, tabs.sqlite filename, user_version chain head) binds every later phase; changing it after rows exist requires a numbered migration, and the filename is fixed platform content.</reversibility>
  <read_first>
    - .planning/phases/11-sql-store-design/schema/SCHEMA.md (DDL verbatim, CHECKs, WAL pre-BEGIN, private exclusion, filename as platform content)
    - .planning/phases/11-sql-store-design/schema/MIGRATIONS.md (forward-only procedure, quarantine N=max+1 allocator, sidecar cleanup, downgrade refusal, readonly tripwire)
    - .planning/phases/11-sql-store-design/authority/AUTHORITY.md (rows 1, 2, 5, 6: single writer, sessionstore authoritative, own file, no credential in SQL)
    - .planning/phases/12-sql-store-build/12-RESEARCH.md (Pattern 1 lazy-import shape, Code Examples open/version-guard/write-through/tripwire skeletons, Phase 11 Fixes table items 1-7, Pitfalls 2 and 4)
    - .planning/phases/12-sql-store-build/12-PATTERNS.md (boundary wrapper conventions, never-throw reads plus loud-write errors, catalogue file:line discipline)
    - powerbrowser/shell/PowerBrowserAPI.sys.mjs lines 14-24 and 36-42 (lazy-getter block and thin-wrapper shape to extend)
    - powerbrowser/INTERNAL-APIS.md Touchpoints table plus Deliberately-not-touched section (row shape; SessionStore row amendment if SessionStore.sys.mjs is touched)
  </read_first>
  <files>powerbrowser/shell/PowerBrowserAPI.sys.mjs, powerbrowser/INTERNAL-APIS.md</files>
  <action>Extend the sole boundary file only, keeping all writer logic inside it: no new sys.mjs helper module and no jar.mn change, because the boundary guard scans powerbrowser/shell minus PowerBrowserAPI.sys.mjs, so a helper could not legally touch Sqlite directly and every wrapper must live here anyway. Append Sqlite, PrivateBrowsingUtils, and SessionStore to the existing lazy-getter block in the same shape, touching zero forbidden-pattern configuration. Add thin named wrappers on the frozen PowerBrowserAPI object covering open, write, remove, point/list reads, and bounded closed-retention prune, each with a D-comment header naming its decision. Carry the v1 DDL from SCHEMA.md verbatim as one delimited string constant wrapped in marker comments the roundtrip script extracts, with the two CHECK constraints intact and no private, window, pinned, or credential-shaped column. Implement the open path as relative-path openConnection against the profile directory resolving to tabs.sqlite, then WAL pin outside any transaction with a journal_mode read-back assert, then version guard via getSchemaVersion and setSchemaVersion: zero runs DDL plus index plus version bump inside exactly one executeTransaction, head runs forward migrations each in one transaction with tableExists and indexExists pre-checks, newer-than-head refuses loudly leaving the file untouched. Parameterize every statement with bound parameters, never interpolating URI, URL, or title strings. Run the PrivateBrowsingUtils private-window check before every upsert so private tabs never reach SQL, with no private column anywhere. Key the startup tripwire on exact single-ok quick_check with unopenable counting as tripped, and implement quarantine as backupToFile to tabs.sqlite.corrupt-N with N equal to max existing suffix plus one starting at one, then sidecar removal of wal, shm, and journal state, then rebuild from parsed sessionstore state plus registry keys inside exactly one transaction, continuing degraded and never deleting the corrupt copy. Keep reads on the never-throw convention and let constraint violations and downgrade refusal throw naming the method and cause. Add one INTERNAL-APIS.md catalogue row per new touchpoint line with the exact file-colon-line verified by grep after the edit, and amend the Deliberately-not-touched SessionStore row in the same commit since the writer now touches SessionStore.sys.mjs. Author no window, strip, toolbar, address bar, menu, or user-facing string. Stage both files before any scan and finish with the catalogue check and the quick gate green.</action>
  <verify>
    <automated>git add powerbrowser/shell/PowerBrowserAPI.sys.mjs powerbrowser/INTERNAL-APIS.md && node --check powerbrowser/shell/PowerBrowserAPI.sys.mjs && scripts/check-internals-boundary.sh --catalogue && scripts/verify-platform.sh --quick</automated>
    <fails_when>node parse fails, the catalogue check names an uncatalogued file:line or a wrong line number, or the quick gate exits non-zero on any row including residue over the staged files and the untouched registry-shape gate</fails_when>
  </verify>
  <acceptance_criteria>
    - PowerBrowserAPI.sys.mjs carries the three new lazy imports plus open, write, remove, read, and prune wrappers with the verbatim v1 DDL constant inside marker comments
    - WAL pin executes outside the DDL transaction, downgrade refuses without rewriting, private check precedes upsert, every statement uses bound parameters
    - INTERNAL-APIS.md gains one row per new touchpoint with exact file:line and the SessionStore amendment, catalogue check exits 0
    - Quick gate exits 0 with registry-shape green and no patch file touched
  </acceptance_criteria>
  <done>The single chrome-side writer exists behind the sole boundary with all seven Phase 11 fixes implemented and catalogued, gates green</done>
</task>

<task type="auto">
  <name>Triggers plus beside-registry key rule plus temp-DB roundtrip proof</name>
  <reversibility rating="one-way">The browser-tab key rule determines every row key ever written; changing it after rows exist forks identity and needs a key-migration, so the one-line rule plus its gate drift-detector ship together.</reversibility>
  <read_first>
    - .planning/phases/12-sql-store-build/12-RESEARCH.md (Pattern 2 trigger shapes, Open Question 1 recommendation: stock-window listeners plus sessionstore sweep, beside-registry emission, no new Theia-to-chrome channel; sessionstore getBrowserState returns a JSON string)
    - .planning/phases/11-sql-store-design/schema/SCHEMA.md (opaque TEXT join key, chrome binds never parses; query placement beside the registry)
    - theia/extensions/tab-uris/src/browser/existing-scheme-coverage.ts (reuse the webview scheme spelling for the key rule if one exists there)
    - theia/extensions/tab-uris/src/browser/tab-uri-registry.ts lines 82-96 (emission semantics: authority read directly, never serialized toString)
    - scripts/verify-registry-shape.mjs lines 119-166 and 179-250 (derive-and-compare with named surplus and missing plus both-directions self-test skeleton and non-vacuity guard)
  </read_first>
  <files>powerbrowser/shell/PowerBrowserAPI.sys.mjs, powerbrowser/INTERNAL-APIS.md, theia/extensions/tab-uris/src/browser/browser-tab-uri.ts, scripts/verify-sql-store-roundtrip.mjs</files>
  <action>Wire the two chrome-observable triggers inside the boundary file from the previous task: per-stock-window tab listeners over the TabOpen, TabClose, TabSelect, and TabAttrModified family enumerated via the window service, each calling the write or remove wrapper, plus a sessionstore-state-write-complete observer running a bounded reconciliation sweep that parses the JSON-string browser state and upserts missing rows and prunes closed ones. Create no Theia-to-chrome channel and register no actor. Fix the browser-tab key rule as one line: the webview scheme prefix from existing-scheme-coverage plus the tab URL spec, bound opaquely and never parsed chrome-side. Place that same one-line rule in a new pure string function in a new beside-registry module under tab-uris browser sources with zero imports from the registry file and zero new public members on TabUriRegistry, leaving both frozen files byte-identical. Write the roundtrip proof script to derive rather than duplicate: it extracts the DDL from the writer-source marker comments at check time and fails distinctly when the markers are absent, then drives a standard-library node sqlite handle against a mktemp stage outside the repo at a space-free path, proving create with version one and WAL pinned, bound-parameter upsert of a quote-plus-unicode title row, close plus reopen with the restored row set equal by exact set equality, integrity result exactly single-ok, rejection of an empty URI and a negative last_active, and the quarantine second-incident allocator advancing the corrupt suffix without overwriting the first copy, with checkpoint-then-clean sidecar removal after handles close and the committed tree untouched. Give the script a self-test that asserts the unmodified path green first, then plants a missing row and an extra row each going red naming the URI, and asserts each plant actually landed before trusting the result. Catalogue the new trigger touchpoint lines in INTERNAL-APIS.md with exact file:line in the same commit. Stage all four files and finish with the roundtrip script green, its self-test green, and the quick gate green.</action>
  <verify>
    <automated>git add powerbrowser/shell/PowerBrowserAPI.sys.mjs powerbrowser/INTERNAL-APIS.md theia/extensions/tab-uris/src/browser/browser-tab-uri.ts scripts/verify-sql-store-roundtrip.mjs && node --check scripts/verify-sql-store-roundtrip.mjs && node scripts/verify-sql-store-roundtrip.mjs && node scripts/verify-sql-store-roundtrip.mjs --self-test && scripts/verify-platform.sh --quick && scripts/verify-platform.sh --only gui04-registry-shape</automated>
    <fails_when>roundtrip or its self-test exits non-zero or fails to name the planted URI, the registry-shape row reports any surplus or missing member, or the quick gate exits non-zero</fails_when>
  </verify>
  <acceptance_criteria>
    - Boundary file attaches stock-window tab listeners and the sessionstore sweep observer with no new channel and no actor registration
    - Beside-registry key module exists with the one-line rule while tab-uri-registry.ts and view-factory-table.ts are byte-identical to before this plan
    - Roundtrip script derives DDL from writer markers, proves URI-to-row-to-restart-to-reopen with set equality and single-ok integrity on a temp DB outside the repo
    - Self-test goes red naming the planted URI in both directions and the quick gate exits 0
  </acceptance_criteria>
  <done>Live triggers feed the writer under the beside-registry key rule and the roundtrip is proven on a temp DB with the frozen registry untouched</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tab content to SQL through the single writer | Untrusted URI, URL, and title strings cross into tabs.sqlite only via bound parameters |
| Corrupt database file to writer recovery | A malformed tabs.sqlite crosses the tripwire into quarantine-then-rebuild, never delete |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-01 | Tampering | chrome-side writer exclusivity | high | mitigate | Single-writer invariant implemented here; second-writer negative scan lands in plan 12-03 as enforcement |
| T-12-02 | Tampering | SQL statement construction | high | mitigate | Bound parameters on every statement; quote-plus-unicode title row in the roundtrip proof exercises the CHECK-quoting path |
| T-12-03 | Information disclosure | private-tab persistence | high | mitigate | Private-window check before upsert with no private column; emitter-exercising absence gate lands in plan 12-02 |
| T-12-04 | Denial of service | corrupt-file recovery | medium | mitigate | Quarantine-not-delete with N equal to max suffix plus one, sidecar cleanup, rebuild from sessionstore in one transaction, degraded continue |
| T-12-05 | Tampering | schema downgrade | medium | mitigate | Newer-than-head user_version refuses loudly with the file untouched; roundtrip self-test pins the refusal path via newer-version fixture |
| T-12-06 | Information disclosure | credential reflection into rows | medium | mitigate | No credential-shaped column in the verbatim DDL; write wrappers carry tab fields only per AUTHORITY row 6 |

No package installs occur in this plan; patch 020 is untouched and no XPCOM surface is registered.
</threat_model>

<verification>
Catalogue check green, roundtrip script plus self-test green on a temp DB outside the repo, quick gate green with registry-shape untouched, no patch change, no GUI surface.
</verification>

<success_criteria>
SQL-01 writer slice ships behind the sole boundary with triggers, the beside-registry key rule, all seven Phase 11 fixes, and catalogue rows; SQL-05 roundtrip scaffolding works on a temp DB; static gates green with the frozen registry byte-identical.
</success_criteria>

<output>
Plan 12-02 consumes the writer wrappers and the roundtrip script: read paths query rows this plan writes, and the absence gate drives the private filter this plan implements.
</output>
