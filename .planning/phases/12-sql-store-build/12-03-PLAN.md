---
phase: 12-sql-store-build
plan: 03
type: execute
wave: 3
depends_on: [12-01, 12-02]
files_modified: [scripts/verify-sql-store-second-writer.mjs, scripts/verify-sql-store-soak.mjs, scripts/verify-platform.sh]
autonomous: true
requirements: [SQL-05]
must_haves:
  truths:
    - "The second-writer negative scan finds no profile-DB opens outside powerbrowser/shell apart from the single readonly reader, with a self-test proving both directions"
    - "An interleaved tab-plus-bookmark write soak ends with integrity_check exactly single-ok on fixtures and, when the binary exists, live"
    - "All four store gates run as verify-platform.sh registry rows with self-tests, registry-shape stays green, and the ESR rebase drill passes over the new touchpoints"
  artifacts:
    - path: "scripts/verify-sql-store-second-writer.mjs"
      provides: "Negative scan over this repo's own trees with allowlisted readonly reader"
      min_lines: 60
    - path: "scripts/verify-sql-store-soak.mjs"
      provides: "Split static-fixture and live soak ending in exact single-ok integrity"
      min_lines: 60
    - path: "scripts/verify-platform.sh"
      provides: "Nine new registry rows (five entry points plus four self-tests) with honest tier placement"
      contains: "sql-store-"
  key_links:
    - from: "scripts/verify-platform.sh"
      to: "scripts/verify-sql-store-second-writer.mjs"
      via: "registry row invokes the scan; self-test row invokes it with --self-test"
      pattern: "sql-store-second-writer"
    - from: "scripts/verify-platform.sh"
      to: "scripts/verify-sql-store-roundtrip.mjs"
      via: "registry row promotes the plan 12-01 proof script without duplicating it"
      pattern: "sql-store-roundtrip"
    - from: "scripts/verify-platform.sh"
      to: "scripts/verify-sql-store-absence.mjs"
      via: "registry row promotes the plan 12-02 absence instrument without duplicating it"
      pattern: "sql-store-absence"
---

<objective>
Land the SQL-05 store gates as registry rows with self-tests and run the live ESR rebase drill over the new touchpoints.

Purpose: SQL-05 requires the second-writer scan, the integrity soak, the restart roundtrip, the untouched registry-shape gate, and the rebase drill green with no sibling driver created.
Output: Second-writer scan plus soak scripts, nine registry rows with honest tier placement, rebase drill evidence, quick green at closeout.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-sql-store-build/12-CONTEXT.md
@.planning/phases/12-sql-store-build/12-RESEARCH.md
@.planning/phases/12-sql-store-build/12-PATTERNS.md
@.planning/phases/11-sql-store-design/schema/MIGRATIONS.md
@.planning/phases/12-sql-store-build/12-01-PLAN.md
@.planning/phases/12-sql-store-build/12-02-PLAN.md
@powerbrowser/INTERNAL-APIS.md
</context>

<tasks>

<task type="auto">
  <name>Second-writer scan plus integrity soak with both-directions self-tests</name>
  <reversibility rating="reversible">Test scaffolding only; scan scope and soak fixtures change in one script each.</reversibility>
  <read_first>
    - .planning/phases/12-sql-store-build/12-RESEARCH.md (Pattern 4 gate 1 and gate 2 specs, Pitfall 1 scan scope excluding upstream, Pitfall 8 temp-profile plus sidecar discipline, Open Question 3 static-live split)
    - .planning/phases/12-sql-store-build/12-PATTERNS.md (internals-boundary scan shape with comment skip and empty-set-is-red, registry-shape self-test skeleton with plant-landed assertion, tier-placement rows)
    - scripts/check-internals-boundary.sh lines 78-85 and 92-147 (comment-line skip and bounded tree-walk shape to mirror)
    - .planning/phases/11-sql-store-design/schema/MIGRATIONS.md (fixture inventory, byte-identity discipline, checkpoint-then-clean sidecar note)
  </read_first>
  <files>scripts/verify-sql-store-second-writer.mjs, scripts/verify-sql-store-soak.mjs</files>
  <action>Write the second-writer scan to walk tracked first-party source only via git ls-files filtered to *.sys.mjs, *.mjs, *.js, *.ts, and *.xhtml excluding node_modules/ and lib/, confined to the powerbrowser, theia, scripts, and patches trees, never upstream which legitimately opens its own databases hundreds of times, matching profile-database open shapes and failing naming file-colon-line for every offense unless allowlisted under exactly two rules: inside powerbrowser/shell, or the single backend reader call carrying the readonly flag, where the reader rule is derive-and-compare so every new Database call in theia without the flag fails naming the line. Fail distinctly on an empty scan set so a broken walk cannot report clean, skip comment lines the way the boundary scan does, and keep the originating-product token out of every string so the residue scan stays green. Give the scan a self-test that asserts the unmodified tree green first, then plants an offending open outside the shell and separately strips the readonly flag, each going red naming the planted path with the plant-landed assertion. Write the soak in two halves sharing one file: the static half copies the committed fixture to a mktemp stage outside the repo at a space-free path, mutates the copy only with byte-identity of the committed file asserted before and after, runs interleaved tab upserts including the quote-plus-unicode title plus bookmark-shape writes, and ends asserting the integrity result is exactly single-ok with a tampered copy tripping distinctly; the live half runs the same interleave against a temp profile through the built binary and the chrome reader clone when the binary exists, otherwise printing STAGED with the exact rerun command and exiting cleanly. Remove wal, shm, and journal sidecars after handles close via checkpoint-then-clean, asserting the gitignore already covers the sidecar names rather than adding duplicate entries. Stage both scripts and finish with each script green plus its self-test green.</action>
  <verify>
    <automated>git add scripts/verify-sql-store-second-writer.mjs scripts/verify-sql-store-soak.mjs && node --check scripts/verify-sql-store-second-writer.mjs && node --check scripts/verify-sql-store-soak.mjs && node scripts/verify-sql-store-second-writer.mjs && node scripts/verify-sql-store-second-writer.mjs --self-test && node scripts/verify-sql-store-soak.mjs && node scripts/verify-sql-store-soak.mjs --self-test</automated>
    <fails_when>either script exits non-zero on the unmodified tree, any self-test plant fails to go red naming the planted path, the committed fixture hash changes, or a sidecar file is left behind or newly tracked</fails_when>
  </verify>
  <acceptance_criteria>
    - Scan covers the four own trees excluding upstream, fails naming file:line, honors exactly the shell-dir and readonly-reader allowlist, and fails distinctly on an empty scan set
    - Self-tests prove the unmodified tree green first with plant-landed assertions on the offending-open and stripped-flag faults
    - Soak static half mutates stage copies only with fixture byte-identity intact and ends exactly single-ok; live half runs on the binary or prints STAGED with the exact rerun command
    - No sidecar is committed and the residue scan sees only staged clean files
  </acceptance_criteria>
  <done>Second-writer enforcement and the integrity soak exist as proven instruments that cannot pass vacuously and cannot touch the live profile</done>
</task>

<task type="auto">
  <name>Nine registry rows with honest tier placement and per-gate sampling</name>
  <reversibility rating="reversible">Registry rows are additive configuration; moving a row between tiers edits one placement block.</reversibility>
  <read_first>
    - scripts/verify-platform.sh lines 3660-3668 (gui04 row shape plus honesty comment to copy)
    - scripts/verify-platform.sh lines 4209-4210 (full-tier placement block for binary and display dependent rows)
    - .planning/phases/12-sql-store-build/12-RESEARCH.md (Validation Architecture: quick versus full classification per gate; Rebase-Drill Scope items 1-4)
    - .planning/phases/12-sql-store-build/12-PATTERNS.md (eight-row naming plus tier split: second-writer and soak-static in base, roundtrip and absence live halves in full)
  </read_first>
  <files>scripts/verify-platform.sh</files>
  <action>Append nine rows (five entry points plus four self-tests; the soak static and live halves share one self-test row) to the single registry and nowhere else, creating no sibling driver: the second-writer scan plus its self-test and the soak static entry point plus its self-test join the base quick array beside the gui04 rows with one honesty comment each stating the tier and why, while the soak live entry point, the roundtrip script, its self-test, the absence static entry point, and its self-test register inside the full-tier block with honesty comments naming the binary or display dependency. Promote the plan 12-01 roundtrip script and the plan 12-02 absence script by invocation only, duplicating neither file's logic into the registry. Keep the registry-shape rows untouched and keep patch 020 to its single DIRS line. Stage the registry change and finish with the quick gate green plus a per-gate sampling run of each new quick row via the single-gate runner.</action>
  <verify>
    <automated>git add scripts/verify-platform.sh && scripts/verify-platform.sh --quick && scripts/verify-platform.sh --only sql-store-second-writer && scripts/verify-platform.sh --only sql-store-second-writer-self-test && scripts/verify-platform.sh --only sql-store-soak && scripts/verify-platform.sh --only sql-store-soak-self-test && scripts/verify-platform.sh --only gui04-registry-shape && scripts/verify-platform.sh --only sql-store-roundtrip && scripts/verify-platform.sh --only sql-store-absence && scripts/verify-platform.sh --only sql-store-soak-live</automated>
    <fails_when>quick gate exits non-zero, any new quick row fails through the single-gate runner, any new full-tier row fails to register (STAGED-exit-clean without the binary or display counts as registration proof), or the registry-shape row reports any drift</fails_when>
  </verify>
  <acceptance_criteria>
    - Exactly nine new rows exist: five entry points plus four self-tests with the soak static and live halves sharing one self-test, each row commented with its honest tier and reason
    - Base array holds only tree-reading rows; binary and display dependent rows sit inside the full-tier block
    - Single-gate sampling passes per new quick row and registry-shape stays green
    - No sibling driver file is created
  </acceptance_criteria>
  <done>All four store gates run through the one driver with self-tests and honest tiers, quick green at closeout</done>
</task>

<task type="auto">
  <name>Live ESR rebase drill over the new touchpoints with staged exact commands</name>
  <reversibility rating="reversible">Drill evidence only; a moved upstream pin is re-pinned in the same commit, never silently.</reversibility>
  <read_first>
    - .planning/phases/12-sql-store-build/12-RESEARCH.md (Rebase-Drill Scope: four assertions over patch surface, pinned lines, green gates on the rebased tree, empty upstream diff; dry-run composition until a next tag exists)
    - powerbrowser/INTERNAL-APIS.md (full new-touchpoint list the drill walks)
    - scripts/rebase-upstream.sh (dry-run flags and exact live-command shape to stage)
  </read_first>
  <files>.planning/phases/12-sql-store-build/12-03-SUMMARY.md</files>
  <action>Run the rebase drill over the new touchpoints only: query whether an ESR tag newer than the pinned one exists, and when none exists run the dry-run composition at the pinned tag for an idempotence pass while staging the exact live commands with the resolved TAG in the plan SUMMARY for the first tagged run. Assert all four drill items and record each with evidence: patch 020 still carries only its single DIRS line with the store adding no patch surface; every pinned upstream line cited by the writer, readers, triggers, and research still resolves, re-pinning any moved line in the same commit rather than silently; the quick gate plus the new store quick rows pass on the rebased tree; and the upstream diff is empty with no hand-edit smuggled in during the drill. When a newer tag does exist, run the live drill instead and record the tag plus per-item results. Write the drill log with commands, outputs, and per-item verdicts into the plan SUMMARY file listed above, which the executor creates at plan close per its standard summary step, so this task's deliverable is the recorded evidence rather than a tree change. Keep the tree free of drill artifacts and finish with the quick gate green on the final tree.</action>
  <verify>
    <automated>PINNED=$(grep -E '^FIREFOX_ESR_TAG=' generated/upstream-pins.env | cut -d= -f2); LATEST=$(git ls-remote --tags https://github.com/mozilla-firefox/firefox.git 'refs/tags/FIREFOX_*esr_RELEASE' 2>/dev/null | grep -oE 'FIREFOX_[0-9_]+esr_RELEASE' | sort -uV | tail -1); if [ -n "$LATEST" ] && [ "$(printf '%s\n%s\n' "$PINNED" "$LATEST" | sort -V | tail -1)" != "$PINNED" ]; then TAG=$LATEST; else TAG=$PINNED; fi; scripts/rebase-upstream.sh --tag "$TAG" --dry-run > /tmp/rebase-drill.log 2>&1; rc=$?; tail -5 /tmp/rebase-drill.log; test $rc -eq 0 && git -C upstream diff --quiet && scripts/verify-platform.sh --quick</automated>
    <fails_when>dry-run reports an error, the upstream diff is non-empty, any pinned line fails to resolve without a same-commit re-pin, or the quick gate exits non-zero</fails_when>
  </verify>
  <acceptance_criteria>
    - Drill ran over the new touchpoints with the tag query recorded: live results when a newer tag exists, otherwise dry-run evidence plus the exact staged live commands in the SUMMARY
    - All four drill assertions hold with per-item evidence: patch surface unchanged, pins resolve, gates green on the rebased tree, upstream diff empty
    - No drill artifact remains in the tree and the quick gate exits 0 on the final tree
  </acceptance_criteria>
  <done>The rebase drill is proven over the new touchpoints live or dry-run with exact staged commands, and the tree is clean</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| New code to profile databases | Any open of a profile database outside the shell crosses into second-writer corruption territory |
| Test harness to live profile | Soak and roundtrip drives cross into developer-data corruption unless confined to temp profiles |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-12 | Tampering | second writer in any process | high | mitigate | Negative scan over the four own trees with exactly two allowlist rules; both-directions self-test; upstream excluded by scope with the reason recorded |
| T-12-13 | Tampering | vacuous gates | medium | mitigate | Every new instrument asserts the unmodified tree green first with plant-landed assertions and named-drift failures in both directions |
| T-12-14 | Denial of service | soak or roundtrip against the live profile | high | mitigate | mktemp stage outside the repo at space-free paths, fixture copies only with byte-identity asserts, checkpoint-then-clean sidecars, never a home-directory or repo-root path |
| T-12-15 | Tampering | upstream rebase invalidating pins | medium | mitigate | Live or dry-run drill re-resolving every pinned line with same-commit re-pins, patch-surface assert, green gates on the rebased tree, empty upstream diff |
| T-12-16 | Tampering | hand-kept gate expectations | medium | mitigate | Derive-from-tree set equality in scan, soak, roundtrip, and absence instruments; non-vacuity guards on empty derivations |

No package installs occur in this plan; no GUI surface is added; patch 020 is asserted unchanged, never edited.
</threat_model>

<verification>
Both new scripts plus self-tests green, nine registry rows green through the single-gate runner with quick green at closeout, registry-shape untouched, rebase drill evidenced live or dry-run with staged exact commands.
</verification>

<success_criteria>
SQL-05 gates are green as registry rows with self-tests: second-writer scan clean, soak ending exactly single-ok, roundtrip and absence promoted, registry-shape untouched, and the ESR rebase drill passes over the new touchpoints.
</success_criteria>

<output>
Phase 12 closeout: writer, reads, and gates all land with the drill evidence the verifier consumes; no GUI surface, no manifest flag, no engine beyond SQLite.
</output>
