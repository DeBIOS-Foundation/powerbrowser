---
phase: 12-sql-store-build
plan: 02
type: execute
wave: 2
depends_on: [12-01]
files_modified: [theia/extensions/tab-uris/package.json, theia/extensions/tab-uris/src/node/tab-query-backend-module.ts, theia/extensions/tab-uris/src/node/tab-query-service.ts, powerbrowser/shell/PowerBrowserAPI.sys.mjs, powerbrowser/INTERNAL-APIS.md, scripts/verify-sql-store-absence.mjs]
autonomous: true
requirements: [SQL-04]
must_haves:
  truths:
    - "Bookmarks and history are readable through Places APIs with no raw places writes anywhere in new code"
    - "A sessionstore read projection and a query API on @powerbrowser/tab-uris answer tab reads from the dedicated file opened readonly"
    - "Private tabs are absent from the store under a test that drives real private-window tab events through the real writer with a public-window positive control"
  artifacts:
    - path: "theia/extensions/tab-uris/src/node/tab-query-service.ts"
      provides: "Readonly better-sqlite3 query service with missing-file tolerance"
      min_lines: 40
    - path: "theia/extensions/tab-uris/src/node/tab-query-backend-module.ts"
      provides: "ContainerModule composition beside the frozen frontend surface"
      min_lines: 8
    - path: "powerbrowser/shell/PowerBrowserAPI.sys.mjs"
      provides: "Chrome-side Places reads plus sessionstore projection behind the boundary"
      contains: "getBrowserState"
    - path: "scripts/verify-sql-store-absence.mjs"
      provides: "Emitter-exercising absence instrument with positive control and --self-test"
      min_lines: 60
  key_links:
    - from: "theia/extensions/tab-uris/src/node/tab-query-service.ts"
      to: "powerbrowser/shell/PowerBrowserAPI.sys.mjs"
      via: "backend serves rows the chrome writer wrote; key rule shared with browser-tab-uri.ts"
      pattern: "tabs\\.sqlite"
    - from: "powerbrowser/shell/PowerBrowserAPI.sys.mjs"
      to: "powerbrowser/INTERNAL-APIS.md"
      via: "each new Places and sessionstore touchpoint line gains one catalogue row"
      pattern: "PowerBrowserAPI\\.sys\\.mjs:[0-9]+"
    - from: "scripts/verify-sql-store-absence.mjs"
      to: "powerbrowser/shell/PowerBrowserAPI.sys.mjs"
      via: "absence instrument drives the real writer path and asserts on its rows"
      pattern: "isWindowPrivate|writeTabRow"
---

<objective>
Ship the SQL-04 read paths: Places reads plus sessionstore projection behind the boundary, a readonly query API on @powerbrowser/tab-uris, and the emitter-exercising private-absence instrument.

Purpose: SQL-04 requires bookmarks and history via Places APIs, a sessionstore read projection, a query API beside the frozen registry, and private exclusion proven by a test that exercises the real emitter per the project's absence-assertion rule.
Output: better-sqlite3 install, backend query module plus composition entry, chrome-side read wrappers plus catalogue rows, absence script with positive control, static gates green.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-sql-store-build/12-CONTEXT.md
@.planning/phases/12-sql-store-build/12-RESEARCH.md
@.planning/phases/12-sql-store-build/12-PATTERNS.md
@.planning/phases/11-sql-store-design/authority/AUTHORITY.md
@.planning/phases/11-sql-store-design/schema/SCHEMA.md
@.planning/phases/12-sql-store-build/12-01-PLAN.md
@powerbrowser/INTERNAL-APIS.md
</context>

<tasks>

<task type="auto">
  <name>Readonly query API in @powerbrowser/tab-uris with better-sqlite3 install</name>
  <reversibility rating="costly">Backend module shape and the readonly-handle contract are consumed by every later read surface; reshaping them touches each consumer, but no on-disk format changes.</reversibility>
  <read_first>
    - .planning/phases/12-sql-store-build/12-RESEARCH.md (Pattern 3 reader skeleton: readonly open, missing-file catch with serve-empty plus lazy re-open, official readonly semantics; Pitfall 3; install-inside-Nix-shell rule with Pitfall 9)
    - .planning/phases/12-sql-store-build/12-PATTERNS.md (token-gate ContainerModule analog, backend package.json entry shape, missing-file reader with no in-tree analog)
    - theia/extensions/token-gate/src/node/token-gate-backend-module.ts (12-line ContainerModule shape to copy)
    - theia/extensions/token-gate/package.json lines 9-13 (backend theiaExtensions entry shape)
    - theia/extensions/tab-uris/package.json (current frontend-only entry to extend)
    - theia/extensions/tab-uris/src/browser/browser-tab-uri.ts (key rule the query side must apply identically; created in plan 12-01)
  </read_first>
  <files>theia/extensions/tab-uris/package.json, theia/extensions/tab-uris/src/node/tab-query-backend-module.ts, theia/extensions/tab-uris/src/node/tab-query-service.ts</files>
  <action>Install better-sqlite3 at the exact pinned version inside the Nix theia shell only, never with host yarn, recording the legitimacy verdict of OK with no postinstall from the Phase 12 research in the commit message rather than re-auditing. Add the dependency pin to the tab-uris package.json alongside a second theiaExtensions entry for the new backend module, keeping the existing frontend entry byte-identical so the browser app composes the service at build time with no app-file edit and no core patch. Create the backend module file copying the token-gate ContainerModule shape with one singleton query-service binding, adding the application-contribution binding only if the service registers an HTTP route. Create the query service holding the single readonly handle on the dedicated tabs.sqlite file opened with the readonly flag at open time, catching open failure to serve empty results with lazy re-open on subsequent queries so first launch before the writer creates the file degrades instead of crashing. Serve point reads by URI and recency-ordered listing via prepared statements with bound parameters, asserting the engine readonly property at open, with zero write statements in the new sources. Apply the beside-registry key function for browser-tab lookups rather than re-spelling the rule. Author no GUI surface and touch no Theia core file. Stage the three files before any scan and finish with the extension compiling in the theia shell and the quick gate green.</action>
  <verify>
    <automated>git add theia/extensions/tab-uris/package.json theia/extensions/tab-uris/src/node/tab-query-backend-module.ts theia/extensions/tab-uris/src/node/tab-query-service.ts && nix develop .#theia --command bash -c "cd theia/extensions/tab-uris && yarn build" && test $(grep -v '^#' theia/extensions/tab-uris/src/node/tab-query-service.ts | grep -c -E 'INSERT|UPDATE |DELETE|DROP|ALTER') -eq 0 && scripts/verify-platform.sh --quick</automated>
    <fails_when>extension build fails, any write-statement token appears in the new reader sources, or the quick gate exits non-zero</fails_when>
  </verify>
  <acceptance_criteria>
    - better-sqlite3 resolves at the pinned version under the theia shell with the extension compiling clean
    - Backend module composes via the theiaExtensions entry with the frontend entry unchanged and no app-file edit
    - Reader opens readonly, tolerates a missing file by serving empty with re-open, and carries zero write statements
    - Quick gate exits 0 with Theia core diff clean
  </acceptance_criteria>
  <done>Theia backend serves tab reads from a readonly handle on the dedicated file with first-launch tolerance, composed without touching core</done>
</task>

<task type="auto">
  <name>Chrome-side Places reads plus sessionstore projection behind the boundary</name>
  <reversibility rating="reversible">Thin read wrappers over stable platform APIs; replacing or removing one touches its wrapper and its catalogue row only.</reversibility>
  <read_first>
    - .planning/phases/12-sql-store-build/12-RESEARCH.md (Places fetch and getFolderContents pins with file-colon-line, getBrowserState JSON-string contract, Pattern 3 read surfaces, backend-never-opens-places rule)
    - .planning/phases/11-sql-store-design/authority/AUTHORITY.md (row 4: backend never opens profile databases readonly included; reads stay chrome-side)
    - .planning/phases/12-sql-store-build/12-PATTERNS.md (wrapper conventions, catalogue file:line discipline, SessionStore Deliberately-not-touched context)
    - powerbrowser/shell/PowerBrowserAPI.sys.mjs (writer wrappers from plan 12-01 to extend in the same voice)
  </read_first>
  <files>powerbrowser/shell/PowerBrowserAPI.sys.mjs, powerbrowser/INTERNAL-APIS.md</files>
  <action>Extend the boundary file from plan 12-01 with read-only wrappers only: a history point read via the History fetch API keyed by URL, a bookmark point read via the Bookmarks fetch API, a folder listing via getFolderContents, and a sessionstore read projection that parses the JSON-string browser state and shapes open tabs into the same row projection the store serves with URL and title fields. Open no database file in any new wrapper, read no places file directly, and project tab fields only with no credential, token, or secret carried. Append the Places and sessionstore symbols to the existing lazy-getter block in the same shape with zero forbidden-pattern configuration change. Add one INTERNAL-APIS.md catalogue row per new touchpoint line with the exact file-colon-line verified by grep after the edit. Stage both files before any scan and finish with the catalogue check and the quick gate green.</action>
  <verify>
    <automated>git add powerbrowser/shell/PowerBrowserAPI.sys.mjs powerbrowser/INTERNAL-APIS.md && node --check powerbrowser/shell/PowerBrowserAPI.sys.mjs && scripts/check-internals-boundary.sh --catalogue && test $(grep -c -E 'places\.sqlite' powerbrowser/shell/PowerBrowserAPI.sys.mjs) -eq 0 && scripts/verify-platform.sh --quick</automated>
    <fails_when>node parse fails, the catalogue check names any uncatalogued line, any new code names the places file directly, or the quick gate exits non-zero</fails_when>
  </verify>
  <acceptance_criteria>
    - Boundary file exposes history, bookmark, folder-listing, and sessionstore-projection readers with no direct database file access
    - Projection parses the JSON-string state and shapes tab rows with tab fields only
    - Catalogue rows cover every new touchpoint with exact file:line and the catalogue check exits 0
    - Quick gate exits 0
  </acceptance_criteria>
  <done>All upstream-owned state is readable chrome-side through platform APIs with the backend still touching only the dedicated file</done>
</task>

<task type="auto">
  <name>Emitter-exercising private-absence instrument with positive control</name>
  <reversibility rating="reversible">Test scaffolding only; changing the instrument touches one script and its future registry rows.</reversibility>
  <read_first>
    - .planning/phases/11-sql-store-design/schema/SCHEMA.md (private-tab exclusion rule: filter before upsert, no private column; absence test must exercise the real emitter)
    - .planning/phases/12-sql-store-build/12-RESEARCH.md (Pattern 4 gate 4 spec with positive control, Pitfall 7 vacuous-absence warning, Open Question 2 headless-first with Xvfb fallback)
    - scripts/verify-registry-shape.mjs lines 119-166 and 179-250 (derive-and-compare plus both-directions self-test with plant-landed assertion)
    - powerbrowser/shell/PowerBrowserAPI.sys.mjs (writer private-filter placement from plan 12-01 the static half derives from)
  </read_first>
  <files>scripts/verify-sql-store-absence.mjs</files>
  <action>Write the absence instrument in two halves sharing one file. Build the static half honestly quick: derive the DDL column set from the writer-source markers at check time and compare as set equality against exactly the four v1 columns so an addition and a removal each go red naming the column, and derive that the private-window check call precedes the upsert statement inside the same writer method by line order, failing distinctly when either side is unlocatable. Give it a self-test that asserts the unmodified tree green first, then plants a private-marker column and a removed column, each going red naming the drift with the plant-landed assertion before trusting the result. Build the live half to drive real private-window tab events through the real writer path against a temp profile outside the repo and assert zero private rows, then drive the identical events through a public window asserting rows exist as the positive control proving the emitter ran, spiking headless first and falling back to the Xvfb harness pattern when headless cannot produce private-window tab events, recording the harness choice in the script header. When the built binary or a display is unavailable, the live half prints STAGED with the exact rerun command and exits cleanly rather than faking a pass, and the run records the deferral in its SUMMARY per the standing nonstop rule. Stage the script and finish with its static half green, its self-test green, and the quick gate green.</action>
  <verify>
    <automated>git add scripts/verify-sql-store-absence.mjs && node --check scripts/verify-sql-store-absence.mjs && node scripts/verify-sql-store-absence.mjs && node scripts/verify-sql-store-absence.mjs --self-test && scripts/verify-platform.sh --quick</automated>
    <fails_when>static half exits non-zero on the unmodified tree, any self-test plant fails to go red naming the drift, or the quick gate exits non-zero</fails_when>
  </verify>
  <acceptance_criteria>
    - Static half derives the column set and filter ordering from the writer source with set equality, red on addition and removal by name
    - Self-test proves the unmodified tree green first with plant-landed assertions on every fault
    - Live half drives real private-window events with a public-window positive control, or prints STAGED with the exact rerun command when the binary or display is absent
    - Quick gate exits 0
  </acceptance_criteria>
  <done>Private exclusion is proven by an emitter-exercising instrument that cannot pass vacuously, with the live drive staged exactly when the harness is absent</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Theia backend to dedicated database file | Backend holds one readonly handle on tabs.sqlite only; every other profile database stays unopened |
| Chrome-side readers to upstream-owned state | History, bookmark, and session reads cross through platform APIs, never raw file opens |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-07 | Tampering | backend database opens | high | mitigate | Readonly flag at open plus engine readonly-property assert; zero write statements enforced by grep gate; backend never opens profile databases |
| T-12-08 | Information disclosure | private-tab leakage through reads | high | mitigate | Exclusion enforced at the single writer from plan 12-01; this plan's absence instrument proves it with a positive control |
| T-12-09 | Information disclosure | token or secret reflected into rows | medium | mitigate | Write-path review confirms bound parameters carry tab fields only; no credential-shaped column exists in the verbatim DDL |
| T-12-10 | Denial of service | first-launch missing store file | low | mitigate | Reader catches open failure, serves empty, and lazily re-opens on subsequent queries |
| T-12-11 | Tampering |Places API misuse into raw writes | medium | mitigate | Reads use fetch and getFolderContents only; grep gate asserts the places filename appears nowhere in new chrome code |
| T-12-SC | Tampering | better-sqlite3 supply chain | medium | accept | Registry legitimacy OK with multi-year history and multi-million weekly downloads verified this session, no postinstall script, exact pin recorded; rationale documented in the install commit |

No GUI surface is added; Theia core stays unpatched and no Gecko change lands outside the patch stack.
</threat_model>

<verification>
Extension builds in the theia shell with zero write statements in the reader, catalogue check green over the new read wrappers, absence static half plus self-test green, quick gate green, core diff clean.
</verification>

<success_criteria>
SQL-04 read paths ship with Places reads chrome-side, a sessionstore projection, a readonly beside-registry query API tolerating a missing file, and private absence proven by an emitter-exercising instrument with a positive control.
</success_criteria>

<output>
Plan 12-03 consumes the absence script and the backend reader: it promotes the instruments to registry rows and scans the reader open as the single allowlisted exception.
</output>
