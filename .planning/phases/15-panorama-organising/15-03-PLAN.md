---
phase: 15-panorama-organising
plan: 03
type: execute
wave: 3
depends_on: [15-02]
files_modified:
  - theia/extensions/modes/src/browser/organising-tree.ts
  - theia/extensions/modes/src/browser/organising-widget.ts
  - theia/extensions/modes/src/browser/modes.css
  - theia/extensions/modes/src/browser/modes-frontend-module.ts
  - theia/extensions/modes/src/browser/organising-placeholder-widget.ts
  - scripts/verify-gui08-view-parity.mjs
  - scripts/verify-gui08-close-exactness.mjs
  - scripts/verify-gui08-panorama-copy.mjs
  - scripts/verify-platform.sh
autonomous: true
requirements: [GUI-08]
---

<objective>
Deliver the tree view over the SAME GroupModel (parity by construction),
prove the three held-out backstops with gates (view parity, close
exactness, copy coverage), and retire the Phase-14 placeholder (file, bind
residue, CSS, copy) so the organising slot is canvas/tree only.

Purpose: GUI-08's second half (flip to a tree over identical group data)
plus the phase's gate closeout and placeholder removal.
Output: tree view, three new verify rows (parity, close exactness, copy),
placeholder deleted.
</objective>

<context>
@.planning/phases/15-panorama-organising/15-CONTEXT.md
@.planning/phases/15-panorama-organising/15-UI-SPEC.md
@.planning/phases/15-panorama-organising/15-PATTERNS.md
@.planning/phases/15-panorama-organising/15-01-PLAN.md
@.planning/phases/15-panorama-organising/15-02-PLAN.md
@theia/extensions/modes/src/browser/group-model.ts
@theia/extensions/modes/src/browser/organising-widget.ts
</context>

<tasks>

<task type="auto">
  <name>Tree view over the shared GroupModel</name>
  <reversibility rating="reversible">Second render root over the proven
  model; no store, no schema, no channel touch.</reversibility>
  <files>theia/extensions/modes/src/browser/organising-tree.ts, theia/extensions/modes/src/browser/organising-widget.ts, theia/extensions/modes/src/browser/modes.css</files>
  <action>New organising-tree.ts exporting render functions OWNED by the
  widget (one file-owning widget per Pitfall 5 — the tree holds NO fetch,
  NO cache, NO listGroups call site; a second fetchGroups outside the model
  is the defect this structure prevents): each group a section (header:
  title 14px + tab count exact + close × with Close group tooltip; active
  header carries accent marker + name 600, planner picks border vs
  check-glyph consistent with the mode-toggle item-4 idiom, never both);
  stacked tab rows (title 13px + mono URI caption 12px, 32px min-height,
  ellipsis + title tooltips, textContent only); NO thumbnails in tree (contract,
  not a gap); same affordances as canvas (active marker, double-click
  rename with identical 60-cap/empty-revert/duplicates-allowed rules,
  header × with the IDENTICAL Close Group confirmation, empty-group hint
  Empty group — drag tabs here.); toggle flips visibility ONLY — never
  reloads, never loses selection (active group + New Group draft survive);
  flip never destructive, never confirmed. Zero-groups renders contracted
  tree empty copy (No tab groups + There are no tab groups yet — choose New
  Group to start one.). modes.css: tree classes in the SAME @layer block
  (tree rows surface fill, hover surface-raised, hairlines, focus idiom on
  rows/headers). Any canvas/tree order-or-membership mismatch is a render
  bug in these files, never store divergence.</action>
  <verify><automated>nix develop .#theia --command bash -c "tsc -b
  theia/extensions/modes" AND scripts/verify-platform.sh --quick AND node
  scripts/verify-gui08-view-parity.mjs (written in Task 2 — green when it
  lands); headed flip halves reserved full-suite per nonstop rule</automated></verify>
  <done>Tree renders identical order/membership over one model; flip keeps
  selection; tsc + quick green</done>
  <commit>feat(15-03): tree view over shared GroupModel</commit>
</task>

<task type="auto">
  <name>View-parity + close-exactness gates</name>
  <reversibility rating="reversible">New check files only.</reversibility>
  <files>scripts/verify-gui08-view-parity.mjs, scripts/verify-gui08-close-exactness.mjs, scripts/verify-platform.sh</files>
  <action>verify-gui08-view-parity.mjs: static half derives the single-model
  assertion at check time (exactly ONE listGroups/getGroupTabs import shared
  by both render roots; negated search for a second fetchGroups/listGroups
  call site outside group-model.ts; toggle-flip preserves-selection code
  shape) as set equality; backstop half records membership-equality +
  flip-keeps-selection as flat { statement, verification: backstop } scalars
  (reserved live, never silent pass). verify-gui08-close-exactness.mjs:
  static half derives dialog copy verbatim (Close Group title/body
  singular/plural/buttons) + exact close wiring (closeGroup actor kind →
  stock per-tab close loop → removeGroupRow; cancel path returns before any
  mutation; activity-handoff code shape) as set equality; backstop half
  records confirm-closes-exact-tabs / cancel-no-op / activity-never-on-
  removed-box as backstop scalars. Both: one EXPECTED const, non-vacuity
  guards, --self-test with ≥3 plants each (second fetch site plant,
  dialog-copy drift, wiring removal) red NAMING drift, honestly --quick
  (text reads only). Stage before scanning. Append all four registry rows
  beside the gui09 pair with honesty comments.</action>
  <verify><automated>node scripts/verify-gui08-view-parity.mjs AND node
  scripts/verify-gui08-view-parity.mjs --self-test AND node
  scripts/verify-gui08-close-exactness.mjs AND node
  scripts/verify-gui08-close-exactness.mjs --self-test AND
  scripts/verify-platform.sh --only gui08-view-parity AND
  scripts/verify-platform.sh --only gui08-close-exactness</automated></verify>
  <done>Parity + exactness gates green through the registry; plants red
  naming drift; live halves honestly reserved</done>
  <commit>feat(15-03): gui08 parity and close-exactness gates</commit>
</task>

<task type="auto">
  <name>Panorama copy gate (contracted strings + placeholder absence)</name>
  <reversibility rating="reversible">New check file only.</reversibility>
  <files>scripts/verify-gui08-panorama-copy.mjs, scripts/verify-platform.sh</files>
  <action>Create scripts/verify-gui08-panorama-copy.mjs: derive every
  contracted 15-UI-SPEC string from Theia sources at check time vs EXPECTED
  literals verbatim (New Group, Canvas, Tree, Zoom in/out/reset tooltips +
  readout, Group name placeholder, Untitled group, No tab groups ×2
  headings/bodies, Empty group — drag tabs here., Ungrouped, No ungrouped
  tabs — drag a tab here to ungroup it., both error bars + Retry ×2, Close
  group tooltip, Close Group title/body singular+plural/buttons, Group
  "{name}" closed. flash) compared as set equality (surplus AND missing
  named); NEGATED search asserting ZERO occurrences of retired placeholder
  copy (Organising arrives next, Back to Browsing, The freeform canvas for
  arranging tabs lands in the next update) in shipped sources; no-internals
  shape check on the same strings (no pref key / table / group_id / bounds
  / URI-scheme literal inside user-facing copy); --self-test with ≥3 plants
  (label drift, removed string, reintroduced placeholder line) red NAMING
  drift. If the verify-shell-error-copy.mjs harness fits, EXTEND it instead
  of a new file (planner decides at write time; either way one gate owns
  these strings). Honestly --quick. Append both registry rows.</action>
  <verify><automated>node scripts/verify-gui08-panorama-copy.mjs AND node
  scripts/verify-gui08-panorama-copy.mjs --self-test AND
  scripts/verify-platform.sh --only gui08-panorama-copy AND
  scripts/verify-platform.sh --only gui08-panorama-copy-self-test</automated></verify>
  <done>All contracted copy asserted verbatim; placeholder copy proven
  absent; plants red naming drift</done>
  <commit>feat(15-03): gui08 panorama copy gate</commit>
</task>

<task type="auto">
  <name>Placeholder retirement + phase gate sweep</name>
  <reversibility rating="reversible">Deletions of dead code only; slot
  already owned by the tracer since 15-01.</reversibility>
  <files>theia/extensions/modes/src/browser/organising-placeholder-widget.ts, theia/extensions/modes/src/browser/modes-frontend-module.ts, theia/extensions/modes/src/browser/modes.css</files>
  <action>git rm theia/extensions/modes/src/browser/organising-placeholder-widget.ts;
  remove its import/bind residue from modes-frontend-module.ts (tracer bind
  from 15-01 stays the single owner); remove placeholder CSS classes
  (pb-modes-organising-slot/heading/body/back) from modes.css keeping the
  single @layer shape; grep the tree for surviving placeholder copy (must be
  zero — Task 3 gate proves it). Then full sweep, staged before scanning:
  node scripts/scan-brand-residue.mjs (exit 0), nix develop .#theia
  --command bash -c "tsc -b theia/extensions/modes theia/extensions/tab-uris",
  scripts/verify-platform.sh --quick, plus every touched row's --self-test
  (gui08-persistence-roundtrip, gui08-canvas-geometry, gui08-view-parity,
  gui08-close-exactness, gui08-panorama-copy, internals-boundary,
  internals-catalogue, sql-store-second-writer). No diff-theia-core drift
  (never fork core), git -C upstream diff empty (no Gecko touch).</action>
  <verify><automated>test ! -f theia/extensions/modes/src/browser/organising-placeholder-widget.ts
  AND ! grep -rn "Organising arrives next\|Back to Browsing" theia/
  powerbrowser/ scripts/ AND scripts/verify-platform.sh --quick</automated></verify>
  <done>Placeholder fully retired; copy gate + quick + all touched
  self-tests green; upstream diff empty</done>
  <commit>feat(15-03): retire organising placeholder, close gui08 gates</commit>
</task>

</tasks>

<threat_model>
| Threat | STRIDE | Disposition | Mitigation (this plan) |
|---|---|---|---|
| Tree as second store diverges from canvas | Tampering (integrity) | mitigate | One model, widget-owned render fns; parity gate asserts single fetch site (Tasks 1-2) |
| Close Group closes wrong set | Tampering | mitigate | Exactness gate: static wiring + reserved-live backstop (Task 2) |
| Internal identifier leaks into copy | Info disclosure | mitigate | Copy gate no-internals shape check (Task 3) |
| Dead placeholder code re-activatable | Tampering | mitigate | File removed, bind residue + CSS + copy proven absent (Task 4) |
</threat_model>

<verification>
scripts/verify-platform.sh --quick green; all five gui08 rows +
self-tests green; internals-boundary/catalogue + second-writer green;
tsc -b clean; upstream diff empty. No new npm packages. Piles +
type-anywhere search stay OUT — deferred with no copy and no surface (the
provisional Search-tabs copy in UI-SPEC ships ONLY if search ships cheap;
it does not ship in this phase).
</verification>

<success_criteria>
Tree flips over identical data with selection preserved; parity,
exactness, and copy gates green; placeholder gone (file, bind, CSS, copy);
GUI-08 five-row gate plan complete (5 of 5 landed); phase ready for
deferred verification per the nonstop rule (/gsd-verify-work 15 runs at
roadmap end, backstop live halves then).
</success_criteria>

<output>Create .planning/phases/15-panorama-organising/15-03-SUMMARY.md</output>
