---
phase: 15-panorama-organising
plan: 02
type: execute
wave: 2
depends_on: [15-01]
files_modified:
  - theia/extensions/modes/src/browser/organising-widget.ts
  - theia/extensions/modes/src/browser/modes.css
  - theia/extensions/modes/src/browser/panorama-commands.ts
  - powerbrowser/shell/PowerBrowserAPI.sys.mjs
  - powerbrowser/INTERNAL-APIS.md
  - scripts/verify-gui08-canvas-geometry.mjs
  - scripts/verify-platform.sh
autonomous: true
requirements: [GUI-08]
must_haves:
  truths:
    - "User drags boxes by headers, resizes by the corner handle, and zooms 25-200 percent with bounds and card data untouched"
    - "Dropping a card auto-boxes, joins, or ungroups with optimistic paint and contracted rollback on write failure"
    - "The ungrouped tray stays fixed at 144px with contracted captions and horizontal scroll"
    - "Clicking or pressing Enter on a card opens its tab through the stock opener without moving anything"
    - "Close Group asks the contracted confirmation and closes exactly that group's tabs; cancel changes nothing"
    - "Cards show PNG snapshots where captured and silent title-plus-URI text otherwise, never a glyph or spinner"
    - "Active-group state never filters or hides the tab strip"
  artifacts:
    - path: "theia/extensions/modes/src/browser/organising-widget.ts"
      provides: "Full canvas geometry with auto-box drops and zoom"
      contains: "data-canvas"
    - path: "theia/extensions/modes/src/browser/modes.css"
      provides: "Canvas, box, card, handle, and zoom classes on locked tokens"
      min_lines: 40
    - path: "theia/extensions/modes/src/browser/panorama-commands.ts"
      provides: "Panorama command consts with contracted labels"
      contains: "powerbrowser.panorama"
    - path: "powerbrowser/shell/PowerBrowserAPI.sys.mjs"
      provides: "PageThumbs last-view capture with size cap and private skip"
      contains: "PageThumbs"
    - path: "powerbrowser/INTERNAL-APIS.md"
      provides: "PageThumbs catalogue row"
      contains: "PageThumbs"
    - path: "scripts/verify-gui08-canvas-geometry.mjs"
      provides: "Canvas geometry gate with thumbnail-fallback assertions and self-test"
      min_lines: 60
    - path: "scripts/verify-platform.sh"
      provides: "Canvas-geometry registry rows beside the gui09 pair"
      contains: "gui08-canvas-geometry"
  key_links:
    - from: "theia/extensions/modes/src/browser/organising-widget.ts"
      to: "theia/extensions/modes/src/browser/panorama-commands.ts"
      via: "call sites import command consts, never re-spell ids or labels"
      pattern: "COMMAND_ID"
    - from: "theia/extensions/modes/src/browser/organising-widget.ts"
      to: "theia/extensions/modes/src/browser/group-actor-client.ts"
      via: "drop gestures persist through the actor client debounced 150ms with pointerup commit"
      pattern: "group-actor-client"
    - from: "powerbrowser/shell/PowerBrowserAPI.sys.mjs"
      to: "powerbrowser/INTERNAL-APIS.md"
      via: "PageThumbs touchpoint catalogued in the same commit or the catalogue gate goes red"
      pattern: "PageThumbs"
    - from: "scripts/verify-platform.sh"
      to: "scripts/verify-gui08-canvas-geometry.mjs"
      via: "registry row invokes the geometry gate; self-test row invokes it with --self-test"
      pattern: "gui08-canvas-geometry"
---

<objective>
Complete the canvas surface over the 15-01 write path: free header-drag,
corner resize, auto-box on drop, zoom 25–200%, ungrouped tray, full rename
rules, Close Group (the ONLY destructive action) with contracted
confirmation, and PNG last-view thumbnails with the guaranteed text-first
fallback.

Purpose: the tracer proved the channel; this plan delivers the Panorama
interaction contract (pixel-free boxes, no snap/grid/auto-fit).
Output: full canvas interactions + capture pipeline + one new verify row
(canvas geometry incl. thumbnail fallback).
</objective>

<context>
@.planning/phases/15-panorama-organising/15-CONTEXT.md
@.planning/phases/15-panorama-organising/15-UI-SPEC.md
@.planning/phases/15-panorama-organising/15-PATTERNS.md
@.planning/phases/15-panorama-organising/15-01-PLAN.md
@theia/extensions/modes/src/browser/organising-widget.ts
@theia/extensions/modes/src/browser/group-model.ts
</context>

<tasks>

<task type="auto">
  <name>Canvas geometry: drag, corner resize, zoom, auto-box</name>
  <reversibility rating="reversible">Pure frontend presentation over the
  proven model; geometry consts change without migration.</reversibility>
  <files>theia/extensions/modes/src/browser/organising-widget.ts, theia/extensions/modes/src/browser/modes.css</files>
  <action>In organising-widget.ts (15-01 tracer file — sequential wave, same
  executor lineage): header-drag moves boxes (pointerdown + setPointerCapture
  on the title strip ONLY; card bodies are drag sources, never move
  handles); bottom-right corner handle 16×16 hit area resizes with minimum
  200×144 enforced live; positions are user pixels — NO snap, grid,
  auto-fit, auto-size (a box never resizes on overflow: card area scrolls
  vertically inside); zoom scales ONE inner canvas layer (transform:
  scale(z), range 25–200% fixed steps, 100% reset, ≤150ms transform,
  readout caption; zoom touches transform only — bounds + card data
  untouched, thumbnails scale by CSS never re-captured); drop matrix paints
  optimistically then persists via the 15-01 actor client (debounced 150ms,
  pointerup commit; Esc cancels with nothing moved/persisted; nack/timeout
  → save-error bar + Retry replaying once, reverting paint on second
  failure): card-onto-card auto-draws a box containing both, card-onto-field
  auto-draws around it, card-into-box joins, card-into-tray ungroups; valid
  target shows accent outline during drag (reserved use 7), dragged card
  opacity 0.7 following the pointer (no transition while dragging);
  prefers-reduced-motion renders every change instantly. modes.css: extend
  the SAME @layer powerbrowser-modes block (canvas/box/card/handle/zoom
  classes; card 160px, thumb 160×90 cover, radius-sm 4px, hairline 1px
  var(--color-border); NO font-size/weight literal outside 14/13/12 roles;
  600 only on active-group title; no !important).</action>
  <verify><automated>git add theia/extensions/modes/src/browser/organising-widget.ts theia/extensions/modes/src/browser/modes.css && nix develop .#theia --command bash -c "tsc -b
  theia/extensions/modes" AND scripts/verify-platform.sh --quick AND node
  scripts/verify-gui08-canvas-geometry.mjs (covering order: Task 4 authors
  this script — commit Task 4 first, then re-run this gate to mark Task 1
  green); headed drag halves reserved full-suite per nonstop rule</automated>
  <fails_when>drag, resize, zoom, or any drop gesture misses the contract, the geometry gate fails, or tsc or quick exits non-zero</fails_when></verify>
  <done>All four drop gestures + move + resize + zoom behave per contract
  with optimistic paint and contracted rollback; static halves green</done>
  <commit>feat(15-02): full canvas geometry with auto-box drops</commit>
</task>

<task type="auto">
  <name>Tray, dive, rename rules</name>
  <reversibility rating="reversible">Frontend behavior over proven model;
  no schema touch.</reversibility>
  <files>theia/extensions/modes/src/browser/organising-widget.ts, theia/extensions/modes/src/browser/panorama-commands.ts</files>
  <action>Tray: 144px fixed bottom strip, always rendered (stable ungroup
  target), Ungrouped caption label + contracted empty caption at zero tabs,
  horizontal scroll beyond width (never grows host, never shifts canvas);
  cards share the 160px card contract. Dive: click or Enter on a focused
  card opens its tab through the EXISTING opener path — never a new open
  path; opening never closes/moves/detaches anything; roving tabindex
  within box/tray; cards keyboard-focusable with visible focus idiom.
  Rename (canvas boxes): double-click title → edit mode (placeholder Group
  name); Enter commits, Esc cancels; empty commit reverts to prior title
  (never blank); 60-char cap with pasted overflow cut at cap; duplicates
  allowed (no duplicate-name error — boxes differ by position/content);
  commit persists via renameGroup actor message with the same optimistic +
  rollback discipline. panorama-commands.ts: New Group creates Untitled
  group with title in edit mode (text selected); command consts imported at
  call sites, never re-spelled. Active-group: header/field-background click
  marks active (accent border + title 600, persisted via setActiveGroup);
  opening a tab marks its group active; active NEVER filters/hides the tab
  strip (13-01 RED binding — no strip touch anywhere in this diff).</action>
  <verify><automated>git add theia/extensions/modes/src/browser/organising-widget.ts theia/extensions/modes/src/browser/panorama-commands.ts && nix develop .#theia --command bash -c "tsc -b
  theia/extensions/modes" AND scripts/verify-platform.sh --quick</automated>
  <fails_when>the tray, dive path, or any rename rule misses the contract, or tsc or quick exits non-zero</fails_when></verify>
  <done>Tray always present with contracted captions; dive routes stock
  path; rename rules exact incl. empty-revert and 60-cap</done>
  <commit>feat(15-02): tray, card dive, and rename rules</commit>
</task>

<task type="auto">
  <name>Close Group + PageThumbs last-view capture</name>
  <reversibility rating="reversible">Additive chrome methods + frontend
  dialog wiring; capture degrades to contracted text fallback, never a
  hard dependency.</reversibility>
  <files>theia/extensions/modes/src/browser/organising-widget.ts, powerbrowser/shell/PowerBrowserAPI.sys.mjs, powerbrowser/INTERNAL-APIS.md</files>
  <action>Close Group (ONLY destructive action): header × opens stock
  ConfirmDialog (setups-service precedent: title/msg/ok/cancel + if
  (!confirmed) return) with contracted copy verbatim — title Close Group,
  body Close "{name}"? Its {N} tab(s) will close too. You can't undo this.
  (singular/plural exact; long names WRAP, never truncate), buttons Close
  Group (destructive ink #ff848b) / Cancel. Confirm drives the closeGroup
  actor kind → chrome per-tab stock gBrowser.removeTab loop (same
  enumeration as startTabStoreTriggers; existing TabClose triggers do row
  cleanup) + removeGroupRow; cancel changes nothing; closed-group activity
  passes to next box or none (never a removed box); last-box close lands on
  contracted empty-canvas copy; status-bar flash Group "{name}" closed.
  Thumbnails chrome-side: lazy-getter PageThumbs
  (resource://gre/modules/PageThumbs.sys.mjs — new touchpoint, ONE
  INTERNAL-APIS.md row in the SAME commit or --catalogue goes red);
  captureToCanvas(browser, canvas, { targetWidth: 160,
  preserveAspectRatio: true, backgroundColor: '#2b2a33' }) on TabSelect-leave
  + TabClose only (capture-on-settle, debounced/coalesced per tab — NEVER
  synchronously inside onTabEvent; rapid Ctrl-Tab must not jank); toDataURL
  PNG size-capped at 100KB chrome-side (drop to NULL over cap → contracted
  text fallback); failures silent (no spinner/glyph); PrivateBrowsingUtils
  skip at every capture/write site (v1 writeTabRow precedent — no private
  column exists to select on). Theia renders title + URI block FIRST; img
  fills in only when bytes exist.</action>
  <verify><automated>git add theia/extensions/modes/src/browser/organising-widget.ts powerbrowser/shell/PowerBrowserAPI.sys.mjs powerbrowser/INTERNAL-APIS.md && bash scripts/check-internals-boundary.sh AND bash
  scripts/check-internals-boundary.sh --catalogue AND
  scripts/verify-platform.sh --quick AND nix develop .#theia --command
  bash -c "tsc -b theia/extensions/modes"</automated>
  <fails_when>the confirmation copy or wiring drifts, capture misses the size cap or private skip, or any boundary gate exits non-zero</fails_when></verify>
  <done>Close Group exact + contracted; capture seeds 160px PNGs with cap,
  skip-private, and silent fallback; boundary catalogue green</done>
  <commit>feat(15-02): Close Group confirmation and last-view capture</commit>
</task>

<task type="auto">
  <name>Canvas-geometry gate (incl. thumbnail fallback) + registry rows</name>
  <reversibility rating="reversible">New check files only.</reversibility>
  <files>scripts/verify-gui08-canvas-geometry.mjs, scripts/verify-platform.sh</files>
  <action>Create scripts/verify-gui08-canvas-geometry.mjs deriving at check
  time (never a second hand-kept list): canvas hooks (data-canvas/data-g/
  data-u) + zoom range consts (25/200/100) + min-size consts (200×144) +
  tray height (144) + card geometry (160/160×90) from widget + CSS sources;
  thumbnail-fallback assertions (no broken-image markup pattern, no
  per-card spinner pattern, text-block-first ordering); command ids derived
  from panorama-commands.ts as set equality against one EXPECTED const +
  contracted labels verbatim (New Group, Close Group, Close group tooltip)
  + labelless toggle-only commands asserted labelless + call-site
  const-import discipline (widget imports consts); set-equality diff both
  directions; non-vacuity guards; --self-test with ≥3 plants (removed hook,
  zoom-range drift, label drift) each red NAMING drift. Backstop halves
  (live drag/auto-box/close-matrix) recorded as flat { statement,
  verification: backstop } scalars — no explicit evidence → reserved live,
  never silent pass. Stage before scanning. Append both registry rows beside
  the gui09 pair with honesty comment.</action>
  <verify><automated>git add scripts/verify-gui08-canvas-geometry.mjs scripts/verify-platform.sh && node scripts/verify-gui08-canvas-geometry.mjs AND node
  scripts/verify-gui08-canvas-geometry.mjs --self-test AND
  scripts/verify-platform.sh --only gui08-canvas-geometry AND
  scripts/verify-platform.sh --only
  gui08-canvas-geometry-self-test; this script covers the Task-1 forward
  reference — re-run the Task-1 gate after landing</automated>
  <fails_when>the gate or its self-test exits non-zero, any plant fails to go red naming the drift, or backstop halves read as silent passes</fails_when></verify>
  <done>Geometry + fallback + command-registry gate green; plants red naming
  drift; backstops honestly reserved</done>
  <commit>feat(15-02): gui08 canvas-geometry gate with self-test</commit>
</task>

</tasks>

<threat_model>
| Threat | STRIDE | Disposition | Mitigation (this plan) |
|---|---|---|---|
| Oversized thumbnail blows store/layout | DoS | mitigate | 160px target + 100KB chrome-side cap, drop-to-NULL (Task 3) |
| Capture jank on TabSelect hot path | DoS | mitigate | Settle-debounce, never sync in handler (Task 3) |
| Private tab thumbnailed/persisted | Info disclosure | mitigate | isWindowPrivate skip at every capture/write site (Task 3) |
| Close Group closes wrong tabs | Tampering | mitigate | Stock per-tab loop + existing TabClose cleanup; exactness gate lands 15-03 (Task 3 + 15-03) |
| Active-group filters strip | Spoofing (scope) | mitigate | RED binding asserted — no strip-mutating call on any path (Tasks 1-2) |
</threat_model>

<verification>
scripts/verify-platform.sh --quick green; internals-boundary +
--catalogue green (PageThumbs row); gui08-canvas-geometry + --self-test
green; tsc -b clean. No new npm packages. Piles + type-anywhere search
stay OUT (no surface, no copy, no placeholder).
Residual: RESEARCH A2 capture spike skipped — 160px settle-debounced
capture assumed; the silent text-fallback (title plus URI block alone, never
a glyph or spinner) is the guaranteed milestone surface and is
non-negotiable: if capture janks or fails, the milestone ships text-only
with capture seeded later and no spec change.
</verification>

<success_criteria>
Canvas matches the Panorama interaction contract (free drag, corner
resize, auto-box, zoom, tray, renames, confirmed Close Group, PNG cards
with text fallback); gates 2 of 5 landed.
</success_criteria>

<output>Create .planning/phases/15-panorama-organising/15-02-SUMMARY.md</output>
