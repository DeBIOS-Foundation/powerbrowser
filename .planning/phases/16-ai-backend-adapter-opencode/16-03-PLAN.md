---
phase: 16-ai-backend-adapter-opencode
plan: 03
type: execute
wave: 3
depends_on: [16-02]
files_modified: [theia/extensions/backend-opencode/src/node/opencode-mcp-contribution.ts, theia/extensions/backend-opencode/src/node/backend-opencode-backend-module.ts, theia/extensions/backend-opencode/src/node/opencode-acp-supervisor.ts, theia/extensions/backend-opencode/opencode.mcp.json, docs/ai-opencode-adapter.md, configuration.toml, scripts/generate.mjs, scripts/verify-opencode-bridge.mjs, scripts/verify-platform.sh]
autonomous: true
requirements: [AI-04, AI-05]
must_haves:
  truths:
    - "An MCP workspace-context read from opencode returns live sidecar state (R5)"
    - "Concurrent read/write never crashes; reads are point-in-time with no snapshot promise (R5 backstop)"
    - "Backend toggles on/off via configuration.toml; missing key means off (R4)"
    - "#-variables and skills resolve inside @OpenCode chats; CLI-owned tools doc names every CLI-owned tool (R6)"
  artifacts:
    - path: "theia/extensions/backend-opencode/src/node/opencode-mcp-contribution.ts"
      provides: "Minimal read-only /mcp endpoint contribution (slice 0)"
      contains: "/mcp"
    - path: "theia/extensions/backend-opencode/opencode.mcp.json"
      provides: "Checked-in bridge config with env-interpolated token ref only"
      contains: "{env:"
    - path: "docs/ai-opencode-adapter.md"
      provides: "Per-adapter ownership doc: shared versus CLI-owned"
      contains: "CLI-owned"
    - path: "configuration.toml"
      provides: "Backend selection key"
      contains: "[ai]"
    - path: "scripts/verify-opencode-bridge.mjs"
      provides: "Bridge plus selection gate with self-test and R5 backstop holdout"
      min_lines: 80
    - path: "scripts/verify-platform.sh"
      provides: "Bridge registry rows"
      contains: "opencode-bridge"
  key_links:
    - from: "theia/extensions/backend-opencode/opencode.mcp.json"
      to: "theia/extensions/backend-opencode/src/node/opencode-mcp-contribution.ts"
      via: "checked-in config points at the contributed endpoint; per-spawn value injected, never committed"
      pattern: "{env:"
    - from: "scripts/generate.mjs"
      to: "configuration.toml"
      via: "generator derives the backend-selection fragment; hand edits to output stay forbidden"
      pattern: "ai"
    - from: "scripts/verify-platform.sh"
      to: "scripts/verify-opencode-bridge.mjs"
      via: "registry row invokes the bridge gate; self-test row invokes it with --self-test"
      pattern: "opencode-bridge"
---

<objective>
Ship the slice-0 bridge plus selection wiring per D-07: minimal read-only /mcp endpoint, per-spawn token injection via opencode env interpolation, configuration.toml backend selection through the generator, and the per-adapter ownership doc covering R6. Research refuted the prebuilt-endpoint premise, so this plan builds the endpoint read-only minimal.

Purpose: opencode reads live workspace context through one configured server while the sidecar keeps its token posture and selection stays a manifest edit. Nothing in this plan changes the tracer or preset behavior.
Output: /mcp contribution, bridge config, selection key plus generator support, ownership doc, bridge gate green, quick green.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-ai-backend-adapter-opencode/16-SPEC.md
@.planning/phases/16-ai-backend-adapter-opencode/16-CONTEXT.md
@.planning/phases/16-ai-backend-adapter-opencode/16-RESEARCH.md
@.planning/phases/16-ai-backend-adapter-opencode/16-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Minimal read-only /mcp endpoint plus per-spawn bridge auth</name>
  <reversibility rating="costly">The /mcp route plus tool names are consumed by checked-in bridge config and live backends; renaming after ship breaks configured servers — one-way only after ship, costly while pre-ship.</reversibility>
  <read_first>
    - .planning/phases/16-ai-backend-adapter-opencode/16-RESEARCH.md (Q3 auth verdict plus endpoint verdict, scrub hazard, remote-plus-headers interpolation shape)
    - .planning/phases/16-ai-backend-adapter-opencode/16-PATTERNS.md (Backend contribution initialize-configure-onStart, fail-closed exit 78, no new transport unless needed)
    - theia/extensions/token-gate/src/node/powerbrowser-env.ts (module-load capture-and-scrub, stdin-token path)
    - theia/extensions/backend-opencode/src/node/opencode-acp-supervisor.ts (Task-01 spawn site to extend with per-child injection)
  </read_first>
  <files>theia/extensions/backend-opencode/src/node/opencode-mcp-contribution.ts, theia/extensions/backend-opencode/src/node/backend-opencode-backend-module.ts, theia/extensions/backend-opencode/opencode.mcp.json, theia/extensions/backend-opencode/src/node/opencode-acp-supervisor.ts</files>
  <action>Mount a minimal StreamableHTTP contribution on the existing gated channel serving workspace-context reads only (file read plus workspace listing, no writes, no diagnostics beyond what the read needs) so the early middleware gates it for free with no new auth surface, failing closed with exit 78 before listen when the handshake is missing. Serve reads through that channel so unauthenticated GET plus POST are refused while per-spawn token holders get live state; the Task-3 gate proves this live. Bridge per D-07: at opencode-spawn time write a private per-child config (or per-session server entry) whose only secret reference is the interpolation form pointing at a per-spawn variable, with the checked-in opencode.mcp.json carrying URL plus reference and no value; scrub the supervised marker plus any re-added secrets from the child env at the same spawn site. Keep the tool surface read-only minimal to bound the new surface. Read secrets only through the captured backend env module. Stage all four files before any scan, build the extension in the Nix theia shell, commit as feat(16-03): minimal MCP endpoint with per-spawn bridge auth. Finish with a live backend read returning current sidecar state.</action>
  <verify>
    <automated>git add theia/extensions/backend-opencode/src/node/opencode-mcp-contribution.ts theia/extensions/backend-opencode/src/node/backend-opencode-backend-module.ts theia/extensions/backend-opencode/opencode.mcp.json theia/extensions/backend-opencode/src/node/opencode-acp-supervisor.ts && nix develop .#theia --command bash -c "cd theia/extensions/backend-opencode && yarn build" && grep -q '/mcp' theia/extensions/backend-opencode/src/node/opencode-mcp-contribution.ts && grep -q '{env:' theia/extensions/backend-opencode/opencode.mcp.json && test $(grep -v '^\s*//' theia/extensions/backend-opencode/src/node/opencode-mcp-contribution.ts | grep -c 'process\.env') -eq 0 && grep -q 'exit(78)' theia/extensions/backend-opencode/src/node/opencode-mcp-contribution.ts && scripts/diff-theia-core.sh --quick && ! pwd | grep -q ' ' && scripts/verify-platform.sh --quick</automated>
    <fails_when>build fails, route or interpolation reference absent, checked-in secret value present, host-env object read present, fail-closed exit absent, core diff red, or quick exits non-zero</fails_when>
  </verify>
  <acceptance_criteria>
    - Workspace-context read from opencode returns live sidecar state through the gated channel
    - Unauthenticated GET plus POST to /mcp are refused; token holders read live state
    - Checked-in config carries reference only; the value is per-spawn and never committed
    - Child env carries no supervised marker and no secret; contribution fails closed
  </acceptance_criteria>
  <done>Slice-0 bridge reads live state with the token posture intact</done>
</task>

<task type="auto">
  <name>Selection wiring plus per-adapter ownership doc</name>
  <reversibility rating="costly">The manifest key plus generator derivation are read by downstream manifests; renaming after ship breaks them — one-way only after ship, costly while pre-ship.</reversibility>
  <read_first>
    - .planning/phases/16-ai-backend-adapter-opencode/16-PATTERNS.md (configuration.toml wiring: generator owns derivation, missing key means off, plain-if skip)
    - .planning/phases/16-ai-backend-adapter-opencode/16-SPEC.md (R4 acceptance, R6 acceptance, out-of-scope list)
    - scripts/generate.mjs (target table plus derivation shape the new key follows)
    - configuration.toml (current tables; no ai table exists yet)
  </read_first>
  <files>configuration.toml, scripts/generate.mjs, docs/ai-opencode-adapter.md</files>
  <action>Add one backend-selection key under a new ai table defaulting to off with missing key meaning off, wire generator derivation plus the emitted fragment the browser composition consumes through a plain conditional so the binding is skipped entirely when off, and keep generated output hand-edit forbidden. Write docs/ai-opencode-adapter.md in plain user language with no internal identifiers: shared side (chat sessions, context variables, skills pool resolve inside @OpenCode chats), CLI-owned side naming every CLI-owned tool plus model selection staying opencode-side, serve-attach noted as fallback-only per D-02, and bridge setup steps with verification commands. Stage all three files before any scan, run the generator plus the extension build in the Nix theia shell, commit as feat(16-03): backend selection wiring with ownership doc. Finish with the backend off by default and on through one manifest edit plus regenerate.</action>
  <verify>
    <automated>git add configuration.toml scripts/generate.mjs docs/ai-opencode-adapter.md && node --check scripts/generate.mjs && node scripts/generate.mjs && nix develop .#theia --command bash -c "cd theia/extensions/backend-opencode && yarn build" && grep -q '\[ai\]' configuration.toml && grep -q 'CLI-owned' docs/ai-opencode-adapter.md && scripts/diff-theia-core.sh --quick && ! pwd | grep -q ' ' && scripts/verify-platform.sh --quick</automated>
    <fails_when>generator fails, key missing, doc names no CLI-owned tool, composition ignores the key, core diff red, or quick exits non-zero</fails_when>
  </verify>
  <acceptance_criteria>
    - Backend off by default and missing key means off; one manifest edit plus regenerate turns it on
    - Generator owns derivation; no hand-edited generated output
    - Ownership doc resolves shared variables plus skills in-chat and names every CLI-owned tool
  </acceptance_criteria>
  <done>Selection is a manifest edit and ownership is documented</done>
</task>

<task type="auto">
  <name>Bridge gate plus live /mcp auth proof and R5 backstop held-out test</name>
  <reversibility rating="reversible">Test scaffolding only; changing the gate touches one script plus its two registry rows.</reversibility>
  <read_first>
    - .planning/phases/16-ai-backend-adapter-opencode/16-SPEC.md (R5 acceptance, R5 backstop row: concurrent read/write, R4/R6 acceptance lines)
    - .planning/phases/16-ai-backend-adapter-opencode/16-PATTERNS.md (gate idiom, registry rule, no-internal-identifiers user-copy rule)
    - scripts/verify-opencode-presets.mjs (sibling gate shape plus STAGED-holdout convention to mirror)
    - scripts/verify-platform.sh (start_backend/stop_backend boot idiom plus token-cookie positive/negative controls)
  </read_first>
  <files>scripts/verify-opencode-bridge.mjs, scripts/verify-platform.sh</files>
  <action>Write the honestly-quick bridge gate deriving the endpoint route, read-only tool set, interpolation-only checked-in config, selection key default-off, and doc coverage from the tree at check time with set equality against one EXPECTED const, plus fixture proofs for live-read shape, reference-only config, missing-key-off, and variables-plus-skills resolution. Prove bridge auth live and mandatorily with no STAGED in this section: boot theia/applications/browser/lib/backend/main.js on 127.0.0.1 with an OS-assigned port per the start_backend idiom, minting a fresh token and polling for the READY line; run an authenticated GET plus an authenticated POST tool call to /mcp carrying the token cookie and assert 200 with live workspace state in both; repeat GET plus POST with no token and assert both refused fail-closed with no Set-Cookie leak; stop the backend on every path; a backend that never becomes ready fails the gate non-zero. Cover the R5 backstop as an explicit held-out test: concurrent workspace read during an applied write never crashes and returns point-in-time state, printing STAGED with the exact rerun command and exiting cleanly when the live backend host is absent. Assert core diff clean, path space-free, captured-env use, fail-closed exits present, and user copy carrying no internal identifiers. Give a self-test proving green first then a writable-tool plant, a hardcoded-secret plant, a default-on plant, and a missing-doc-row plant each going red naming the drift. Append two registry rows with honesty comments, no sibling driver. Stage before any scan, commit as feat(16-03): bridge plus selection gate. Finish with script, self-test, sampled rows, and quick green.</action>
  <verify>
    <automated>git add scripts/verify-opencode-bridge.mjs scripts/verify-platform.sh && node --check scripts/verify-opencode-bridge.mjs && node scripts/verify-opencode-bridge.mjs && node scripts/verify-opencode-bridge.mjs --self-test && scripts/verify-platform.sh --only ai-opencode-bridge && scripts/verify-platform.sh --only ai-opencode-bridge-self-test && scripts/diff-theia-core.sh --quick && ! pwd | grep -q ' ' && scripts/verify-platform.sh --quick</automated>
    <fails_when>live auth section skipped or green without a live backend, script red on the unmodified tree, any plant fails to go red naming the drift, backstop holdout passes silently without a host, core diff red, or quick exits non-zero</fails_when>
  </verify>
  <acceptance_criteria>
    - Gate derives bridge plus selection shape from source with set equality and fixture proofs
    - Live /mcp auth proven mandatorily: token GET plus POST return live state, anonymous GET plus POST refused with no cookie leak
    - Config reference-only, default-off, and doc coverage proven; R5 backstop held out honestly
    - Self-test green-first with plant-landed asserts on all four faults
    - Exactly two new rows with honesty comments and no sibling driver
  </acceptance_criteria>
  <done>Bridge plus selection are enforced mechanically with the R5 edge held out</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Opencode to /mcp | Configured server reads workspace context over the gated channel. |
| Spawn site to child env | Per-child secret reference resolved at spawn, never committed. |
| Manifest key to composition | One key decides whether the backend binds at all. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-03-01 | Information disclosure | token hardcoded in checked-in config | high | mitigate | Reference-only config plus per-spawn injection per D-07; hardcoded-secret plant in the gate. |
| T-16-03-02 | Tampering | writable bridge tool reaching sidecar state | high | mitigate | Read-only tool set; writable-tool plant goes red naming the drift. |
| T-16-03-03 | Elevation of privilege | backend on without manifest consent | medium | mitigate | Default off, missing key off, plain-conditional skip; default-on plant in the gate. |
| T-16-03-04 | Denial of Service | concurrent read/write crash | medium | mitigate | Point-in-time reads with no snapshot promise; R5 backstop held-out test. |
| T-16-03-SC | Tampering | npm installs | low | accept | No new package in this plan; SDK pin landed in 16-01. |
</threat_model>

<verification>
Extension builds in the Nix theia shell, live MCP read proven, selection toggles through one manifest edit, bridge script plus self-test green, sampled rows plus guard rows green, core diff clean, path space-free, quick green.
</verification>

<success_criteria>
The bridge reads live state with the token posture intact and the backend selects through configuration with ownership documented.
</success_criteria>

<output>
Phase 16 complete: tracer plus presets plus bridge deliver selectable @OpenCode chat with gated review, revertible history, and documented selection.
</output>
