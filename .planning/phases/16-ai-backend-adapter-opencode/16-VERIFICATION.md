# Phase 16: AI backend adapter - OpenCode — Verification

**Phase goal (SPEC.md):** A user of the PowerBrowser Theia sidecar can select `@OpenCode` in chat and complete a code-change task through a staged accept/reject gate, where today no non-native backend exists in the sidecar at all.

**Verified:** 2026-09-06 (goal-backward, from SPEC.md R1–R6 + 12 acceptance lines + 4 prohibitions + 2 backstops)
**Sources read:** `16-SPEC.md`, `16-01-SUMMARY.md`, `16-02-SUMMARY.md`, `16-03-SUMMARY.md`, `deferred-items.md`, plus `16-01/02/03-PLAN.md`, `16-CONTEXT.md` and tree spot-checks (no code modified; no live gates re-run — verification is of the summaries' behavioral proofs, distinguishing live vs compiled-lib-behavioral vs static-fixture).
**Tree spot-checks performed:** extension files present (`src/node/*`, `src/browser/*`, `src/common/*`, `opencode.mcp.json`), all three `scripts/verify-opencode-*.mjs` present (657/862/731 lines), `docs/ai-opencode-adapter.md` present (113 lines), `[ai]` key in `configuration.toml`, `ai.backend` in `config-schema.json` + `generate.mjs`, `STAGED` holdout lines confirmed in both preset and bridge gates, `session/new` live-probe logic confirmed in tracer gate, redaction/outside-root/default-gated/explicit-action asserts confirmed, `{env:}` reference-only template confirmed.

## Per-requirement verdicts

| Req | Title | Verdict | Evidence (behavioral, not fixture-only) | Residual risk |
|-----|-------|---------|------------------------------------------|---------------|
| R1 | Selectable chat agent (`@OpenCode` over ACP stdio, session continuity) | **PASS** | 16-01 D1 `kind: e2e` live probe `node scripts/verify-opencode-tracer.mjs`: 4 prompts on one `sessionId` (exceeds SPEC's 3), new chat mints a new `sessionId`, sent-method log contains no `session/load` cross-cwd. Agent registers id `OpenCode` via ChatAgent token, JSON-RPC `/services/opencode`, supervised `opencode acp` child (workspace-root handoff, die-with-parent, bounded restart, exit 78). Spot-check confirms `session/new` + stable-`sessionId` + `no session/load` asserts in gate source. | Picker UI not visually verified (no screenshots per out-of-scope); session mapping proven at protocol level, not via screenshot. Low risk. |
| R2 | Gated staged review (per-file accept/reject + diff, nothing to disk until accepted) | **PASS** | 16-01 D2 behavioral checks **against the compiled extension lib** (not a reimplementation): ask stages + rejects with no disk write; accept writes exact bytes; reject leaves byte-identical; stale refused; zero-edit empty. Load-bearing fix proven live: `selected/once` double-writes on 1.18.25, so emitter uses stage-from-ask-then-`cancelled` (reply cancelled ends turn, disk untouched — live-probe asserted). 16-02 extends with supersede / emission-order / stale-refuse fixtures against shipped lib + reorder/merge/silent-accept plants. | Accept-exactness proven at engine level + one live cancelled-ask assertion; no live multi-file backend turn with real model edits (live probe used controlled prompts). The double-write finding is version-pinned to 1.18.25 — a binary upgrade could invalidate the stage-from-ask assumption; gate would go red (EXPECTED-set + live probe), which is the correct fail-closed direction. |
| R3 | Auto-accept preset with mandatory history (immediate apply + full diff history + revert) | **PASS** | 16-02 D1+D2: per-session store defaulting to gated, explicit-opt-in-only, zero persistence; auto-accept via agent turn-end apply-all while supervisor stays gated-always (16-01 fix untouched; disagreement defaults to safe/staged). History-before-disk (`historyWriteFailure` blocks apply), append-order = apply-order, working revert with concurrent-edit refusal, D-03 fallback records without writing. Gate asserts default-gated + explicit-action-only + history-order + redaction + fallback + revert against compiled lib, with 5-plant self-test (persisted-preset, merged-supersede, silent-stale-accept, history-order, reorder B,A). 16-01 tracer rows re-pass on extended tree. | Immediacy is **turn-end, not per-edit streaming** (documented open contract in 16-02 summary): a delegated write arriving after turn close stages safely-unapplied under auto-accept. Safe direction, but UX differs from streaming apply. R3 concurrent-revert backstop honestly STAGED (see Backstops). |
| R4 | Extension-only packaging (no core edits, tab-uris pattern, `configuration.toml` selection) | **PASS** | 16-01 D3: `scripts/diff-theia-core.sh --quick` + `verify-platform.sh --quick` green; skeleton per tab-uris, one composition line in browser app + `build:extensions` clause. 16-03 D2: `[ai] backend` (`off`/`opencode`, default and missing = off) via `config-schema.json` enum + `generate.mjs emitAiBackend` + `generated/ai-backend.json` fragment + composed `powerbrowserAiBackend: off` + D-62 plain-conditional skip in frontend module (bind lines byte-identical, tracer/preset substring asserts hold). Gate pins fragment + composed key + `=== 'opencode'` conditional + `resolveConfig`/`emitAiBackend` fixtures. Spot-check confirms `[ai]` key and `ai.backend` wiring in tree. | Token-gate `onStart` null-address race (pre-existing, deterministic exit 78) fixed at the announcer (defer READY to `listening`) — behavior-preserving per summary, all token-gate rows green. Fix touches shared `token-gate` extension; no gate scans that file's source, so future regressions there rely on token-gate's own rows. |
| R5 | MCP bridge slice 0 (minimal endpoint, opencode consumes it for workspace context) | **PASS** | 16-03 D1 `kind: e2e` **mandatory live half** (no STAGED in this section): boot `main.js` on 127.0.0.1 OS-assigned port, fresh token, READY poll; auth GET root = live dir bytes; auth POST `tools/call` = live bytes; anon GET + POST both 403 with no `Set-Cookie` leak; backend stopped on every path; never-ready backend fails non-zero. Plus compositional chain without spending a model: `opencode mcp list` reports connected with the exact checked-in template shape + per-spawn env (Cookie-header interpolation proven live via header-echo server), ACP `session/new` accepts `{type http, name, url, headers[]}` (probed live on 1.18.25). Read-only tool set (`workspace_list`, `workspace_read`) with writable-tool plant. | Opencode-side **model-driven tool round trip NOT spent** — D4 held as human-UAT (see Backstops/residuals). Bridge proven to the HTTP boundary (connected + headers + shape + live endpoint), not via a live model turn calling the tool. R5 concurrent backstop honestly STAGED (see below). Bridge token equals live token under a different name in the same-uid-readable child env — accepted by D-07 design, documented in `opencode-bridge-env.ts`; reviewers must accept this tradeoff consciously. |
| R6 | Shared services boundary (sessions/variables/skills shared; CLI-owned documented) | **PASS (with noted residual)** | Agent consumes framework-resolved `request.message.request.text` + shared `ChangeSetImpl` + `OpencodeService`, declares `readonly variables = []` (no own variables — CLI tools stay opencode-side); gate asserts all three markers + empty-variables ownership check. `docs/ai-opencode-adapter.md` in plain user language states shared sessions/`#`-variables/skills-pool, names all 15 CLI-owned tool ids (spot-checked: `bash edit read write glob grep task todowrite webfetch websearch lsp skill question plan_exit apply_patch`), model-selection opencode-side, serve-attach fallback-only, setup + curl steps; gate checks tool-id set equality + required lines + copy check (no internal identifiers). SPEC's 3 unclassified R1/R4/R6 probe-nudge edges treated as assumptions per edge table — consistent with plan scope. | `#`-variable/skill resolution proven **by construction + static gate, not by a live `#`-mention chat turn**: framework resolves before `invoke`, so reading `request.text` inherits resolution — architecturally sound but no live behavioral turn exercised (no model spent per D4). Recommend one human UAT turn with a `#`-variable + skill selected (pairs naturally with D4). Doc half is strong; resolution half carries this single-turn residual. |

## Acceptance-line matrix (SPEC § Acceptance Criteria, 12 lines)

| # | Line (R) | Status | Proof type | Source |
|---|----------|--------|------------|--------|
| 1 | `@OpenCode` in picker and answers a prompt (R1) | PASS | Live probe round-trip + `OpenCode` agent registration/binding | 16-01 D1 + gate EXPECTED-set |
| 2 | Continuity across 3 prompts; new chat → new backend session (R1) | PASS | Live: 4 prompts one `sessionId`, new `session/new` → different id, no `session/load` | 16-01 D1 (exceeds 3) |
| 3 | Per-file diff; accept exact; reject byte-identical (R2) | PASS | Compiled-lib behavioral + live cancelled-ask no-disk-write | 16-01 D2 + deviation 1 |
| 4 | Zero-edit → no Change Set entry (R2) | PASS | Compiled-lib behavioral (incl. emission-fixture filename isolation fix) | 16-01 D2, 16-02 D2 |
| 5 | Auto-accept no-click + revertible history; gated writes nothing (R3) | PASS | Compiled-lib behavioral (turn-end apply-all, history-before-disk, revert) + default-gated/explicit-action fixtures | 16-02 D1+D2 |
| 6 | Overlap → latest supersedes, old marked, never merged (R2 edge) | PASS | Compiled-lib behavioral + merged-supersede plant red | 16-02 D2 |
| 7 | Multi-file order = emission order (R2 edge) | PASS | A→B emission fixture + B,A reorder plant red | 16-02 D2 |
| 8 | Stale accept refused + conflict msg + proposal kept (R2 edge) | PASS | Compiled-lib behavioral (accept-time live-file compare, D-04) + silent-stale-accept plant red | 16-01 D2, 16-02 D2 |
| 9 | History append order = apply order (R3 edge) | PASS | Compiled-lib behavioral + history-order plant red | 16-02 D2 |
| 10 | No core edits; quick green; toggle via `configuration.toml` (R4) | PASS | `diff-theia-core.sh --quick` + full `--quick` + selection fixtures/pins | 16-01 D3, 16-03 D2 |
| 11 | MCP read returns live sidecar state (R5) | PASS | Mandatory live GET+POST vs live dir bytes + anon 403s + `mcp list` connected | 16-03 D1 |
| 12 | `#`/skills resolve; CLI-owned doc exists (R6) | PASS (residual) | Static-by-construction (framework-resolved text + no own vars) + doc set-equality; no live `#` turn | 16-03 D3 + gate |

**Fixture-vs-behavioral audit:** the word "fixture" in the summaries means *behavioral fixtures executed against the shipped compiled lib* (accept/reject/stale/supersede/order/redaction/fallback/revert/reference-only/missing-key-off), plus EXPECTED-set derivation from the tree at check time with both-directions diff and plant-landed self-tests (4 + 5 + 4 plants). The only pure-static asserts are R6 resolution-by-construction and doc copy checks — flagged above. Live-binary proofs cover: ACP session continuity (mandatory, fail-closed on absent binary), cancelled-ask no-write, `/mcp` auth GET+POST vs anon, `mcp list` connected shape, ACP `session/new` header-entry shape. No summary claims a live model turn; D4 is explicitly held for human UAT. No circular test detected (gates exercise shipped `lib`, stub-override self-tests fault the instrument's dependency, never the assertion).

## Prohibitions (SPEC § Prohibitions, 4/4)

| Prohibition | Req | Verification tier | Verdict | Evidence |
|-------------|-----|-------------------|---------|----------|
| MUST NOT write outside the workspace root | R2, R3 | test | **HOLDS** | `clampStagedPath` + `writeTextFile` both refuse fail-closed; gate fixtures + outside-root-pass plant red (T-16-01-01). 16-02/16-03 preserve; bridge tool surface read-only with writable-tool plant. |
| MUST NOT enable auto-accept except by explicit user action per session | R3 | test | **HOLDS** | Fresh sessions open gated; per-session store, zero persistence; `enableAutoAccept` explicit-action fixture + persisted-preset plant red (T-16-02-01). Default-on plant red in bridge gate (T-16-03-03). |
| MUST NOT route file contents to any provider other than the session's selected backend | R1 | judgment | **HOLDS (judgment)** | Single supervised `opencode acp` stdio child; no OpenAI-compatible `serve` hookup, no secondary network path; spawn env inherited pre-scrubbed + `POWERBROWSER_ENV`-only reads (zero `process.env` in supervisor, bridge-env helper owns forward+scrub); checked-in `opencode.mcp.json` carries URL + `{env:}` refs only (spot-checked). No cross-provider routing surface exists in the staged files. |
| MUST NOT persist credentials/tokens into chat history or Change Sets | R1, R3 | test | **HOLDS** | `redactSecretStrings` over history + Change Set fixtures, staged diff passes through redaction (source assert), redaction-miss plant red (T-16-01-02/T-16-02-04); reference-only config + hardcoded-secret plant red (T-16-03-01); child env carries only `POWERBROWSER_MCP_TOKEN`, supervisor token name + marker scrubbed. |

## Backstops (honestly STAGED — confirmed)

- **R3 revert-under-concurrent-edits (SPEC edge table 🧪 backstop): STAGED, honest.** 16-02 D4 `verification: []`, `human_judgment: true`; gate default prints `STAGED` + exact rerun `node scripts/verify-opencode-presets.mjs --live-backstop` and exits clean (confirmed in gate source ll. 28–31, 550–557, 646). `--live-backstop` runs the real one-hunk refusal + clean-restore scenario and fails naming drift (summary reports PASS on demand). Engine half proven behaviorally; end-to-end held out by plan design. Never silently passes.
- **R5 concurrent read/write (SPEC edge table 🧪 backstop): STAGED by default, honest, with a reporting nuance.** Gate default prints `STAGED` + rerun `node scripts/verify-opencode-bridge.mjs --live-backstop` (confirmed ll. 45–48, 641) and the self-test requires the STAGED line present (l. 669). `--live-backstop` hammers 8 concurrent reads around a mid-flight write, all 200 whole bytes (summary reports PASS on demand). Nuance: 16-03 summary D5 lists `status: pass` with `human_judgment: false` rather than a STAGED holdout entry like 16-02 D4 — the *behavior* is holdout-honest (default STAGED, live driver on demand), but the coverage-metadata label understates the holdout. Recommend relabeling D5 as holdout/STAGED-by-default in a docs-only touch-up; not a correctness gap.
- **D4 opencode-side model-driven round trip (16-03 coverage, not a SPEC backstop row): human-UAT, honestly held.** `verification: []`, `human_judgment: true`; proven to HTTP boundary only, no model turn spent. Correct to hold — pairs with the R6 `#`-variable live-turn residual above.

## Deferred item (out-of-scope confirmation)

- **`scan-brand-residue` red row (deferred-items.md, 2026-09-06): OUT OF PHASE-16 SCOPE — confirmed.** Cause documented as the concurrent phase-15 agent's citation additions (`INTERNAL-APIS.md` 19→21 + one in `PowerBrowserAPI.sys.mjs`, inventory 36→37→39) with zero 16-03 contribution (no staged 16-03 file contains `-PLAN.md`, none in the row's `expected_files`). Owner assigned to phase-15 per precedent 12-02. Status at Task-2 commit: only red rows in `--quick`; **resolved during Task 3** when the phase-15 inventory bump landed — Task-3 full `--quick` green on every row including both scan rows and both bridge rows. No phase-16 action owed; correctly logged, not fixed here. Concurrent-agent transients elsewhere (16-01 `--quick` flakes on `verify-downstream-fixtures`/`verify-upstream-pins`, 16-03 `tab-uris-typecheck` flake, 16-03 deviation 5 index-sweep soft-reset → clean `8711126` via pathspec commits) are documented with green re-runs and hygiene follow-ups — process noise, not product gaps.

## Residual risks (non-blocking)

1. **D4 + R6 live-turn UAT (one human turn):** drive an `@OpenCode` chat with a `#`-variable mention + a skill selected through the configured bridge server; expect the tool call to return live workspace bytes and the turn to stage per-file. Closes both the D4 boundary proof and the R6 resolution residual at once.
2. **Binary-version sensitivity:** stage-from-ask-then-reject and the `session/new {headers[]}` / Cookie-interpolation shapes are pinned to opencode 1.18.25 by live probe. A binary upgrade that changes ask/delegate or MCP-header behavior will (correctly) turn the gates red — treat any such red as a re-probe trigger, not a gate to weaken.
3. **Turn-end (not streaming) auto-accept:** documented contract; late-arriving delegated writes stage safely-unapplied. If UX feedback demands streaming apply, that is a new scope item with its own history-ordering proof.
4. **Shared-index hygiene under concurrent agents:** 16-03 deviation 5 precedent — always `git diff --cached --name-only` before committing; prefer pathspec commits while phases 14/15 share the tree.
5. **D5 coverage label:** relabel 16-03 D5 as STAGED-by-default holdout (docs-only) to match its gate behavior and the 16-02 D4 pattern.

## Verdict

**VERDICT: COMPLETE** — all 6 requirements PASS (R6 with a single live-turn UAT residual that pairs with the explicitly held D4), all 12 SPEC acceptance lines evidenced (11 by live or compiled-lib-behavioral proofs, 1 by construction + static gate with a defined UAT close-out), all 4 prohibitions hold (3 by test incl. plants, 1 by judgment with a single-child/no-secondary-path rationale), both backstops honestly STAGED with passing on-demand live drivers, extension-only boundary intact (`diff-theia-core.sh --quick` green), and the `scan-brand-residue` red row confirmed owned by the phase-15 agent and already resolved via their inventory bump. No code modified by this verification. Recommended next step: the D4/R6 human UAT turn during verify-work, then proceed.
