---
phase: 16-ai-backend-adapter-opencode
plan: 02
type: execute
wave: 2
depends_on: [16-01]
files_modified: [theia/extensions/backend-opencode/src/browser/opencode-preset-contribution.ts, theia/extensions/backend-opencode/src/browser/opencode-preset-commands.ts, theia/extensions/backend-opencode/src/node/opencode-changeset-emitter.ts, theia/extensions/backend-opencode/src/browser/backend-opencode-frontend-module.ts, scripts/verify-opencode-presets.mjs, scripts/verify-platform.sh]
autonomous: true
requirements: [AI-02, AI-03]
must_haves:
  truths:
    - "User toggles gated/auto-accept per session in the chat header; default is gated (R3, D-05)"
    - "Auto-accept writes without a click AND records revertible history; gated writes nothing without accept (R3)"
    - "History append order equals apply order; staging order matches backend emission order (R2/R3 edges)"
    - "Latest overlapping proposal supersedes the old one, marked, never merged (R2 edge)"
    - "Accept of a stale staged hunk after an external edit is refused with a conflict and the proposal kept (R2 edge, D-04)"
  artifacts:
    - path: "theia/extensions/backend-opencode/src/browser/opencode-preset-contribution.ts"
      provides: "Per-session preset toggle, default gated (D-05)"
      contains: "gated"
    - path: "theia/extensions/backend-opencode/src/node/opencode-changeset-emitter.ts"
      provides: "History plus revert with ordering and stale-hunk refuse (D-04/D-06)"
      contains: "revert"
    - path: "scripts/verify-opencode-presets.mjs"
      provides: "Preset plus history gate with self-test and R3 backstop holdout"
      min_lines: 80
    - path: "scripts/verify-platform.sh"
      provides: "Preset registry rows"
      contains: "opencode-presets"
  key_links:
    - from: "theia/extensions/backend-opencode/src/browser/opencode-preset-contribution.ts"
      to: "theia/extensions/backend-opencode/src/node/opencode-changeset-emitter.ts"
      via: "preset selects gated queue versus immediate apply with mandatory history write on both paths"
      pattern: "auto-accept"
    - from: "scripts/verify-platform.sh"
      to: "scripts/verify-opencode-presets.mjs"
      via: "registry row invokes the preset gate; self-test row invokes it with --self-test"
      pattern: "opencode-presets"
---

<objective>
Ship presets plus mandatory history on the proven tracer: per-session gated/auto-accept toggle defaulting to gated per D-05, history plus revert reusing existing Change Set plus session-history surfaces per D-06, accept-time stale-hunk refuse per D-04, and the apply-then-record fallback branch per D-03 kept behind the intercept path.

Purpose: this is the review-UX slice — safe by default, post-hoc review always possible. Nothing in this plan builds the /mcp endpoint or selection wiring (plan 16-03).
Output: preset toggle, ordered history with revert, conflict handling, fallback branch, preset gate green, quick green.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-ai-backend-adapter-opencode/16-SPEC.md
@.planning/phases/16-ai-backend-adapter-opencode/16-CONTEXT.md
@.planning/phases/16-ai-backend-adapter-opencode/16-RESEARCH.md
@.planning/phases/16-ai-backend-adapter-opencode/16-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Per-session preset toggle defaulting to gated</name>
  <reversibility rating="costly">Preset ids, default value, and session-scope contract are read by the emitter and the gate; flipping the default or persisting across sessions changes the safety posture.</reversibility>
  <read_first>
    - .planning/phases/16-ai-backend-adapter-opencode/16-PATTERNS.md (section 4 command-per-action, named COMMAND_ID consts, labelless palette-invisible pattern)
    - .planning/phases/16-ai-backend-adapter-opencode/16-CONTEXT.md (D-05 session scope plus default, D-06 no bespoke widget)
    - theia/extensions/modes/src/browser/modes-commands.ts (command id plus registration shape to mirror)
    - theia/extensions/backend-opencode/src/browser/backend-opencode-frontend-module.ts (Task-01 static binds to extend)
  </read_first>
  <files>theia/extensions/backend-opencode/src/browser/opencode-preset-commands.ts, theia/extensions/backend-opencode/src/browser/opencode-preset-contribution.ts, theia/extensions/backend-opencode/src/browser/backend-opencode-frontend-module.ts</files>
  <action>Add the preset toggle as one command per action with named id consts, surfaced in the chat header area, scoped strictly per session with gated as the default and auto-accept enabled only by an explicit per-session user action per the auto-accept prohibition, never persisted across sessions and never a bespoke history widget. Bind both commands statically at module load beside the Task-01 binds. Stage all three files before any scan, build the extension in the Nix theia shell, commit as feat(16-02): per-session preset toggle default gated. Finish with the toggle switching presets within one session while a fresh session opens gated.</action>
  <verify>
    <automated>git add theia/extensions/backend-opencode/src/browser/opencode-preset-commands.ts theia/extensions/backend-opencode/src/browser/opencode-preset-contribution.ts theia/extensions/backend-opencode/src/browser/backend-opencode-frontend-module.ts && nix develop .#theia --command bash -c "cd theia/extensions/backend-opencode && yarn build" && grep -q 'gated' theia/extensions/backend-opencode/src/browser/opencode-preset-contribution.ts && grep -q 'COMMAND_ID' theia/extensions/backend-opencode/src/browser/opencode-preset-commands.ts && scripts/diff-theia-core.sh --quick && ! pwd | grep -q ' ' && scripts/verify-platform.sh --quick</automated>
    <fails_when>build fails, default is not gated, command id const absent, session scope leaks across sessions, core diff red, or quick exits non-zero</fails_when>
  </verify>
  <acceptance_criteria>
    - Toggle lives in the chat header area with named command ids and static binds
    - Fresh sessions always open gated; auto-accept needs an explicit per-session action
    - No bespoke history widget introduced
  </acceptance_criteria>
  <done>Preset selection is per-session, safe by default, and committed</done>
</task>

<task type="auto">
  <name>Mandatory history plus revert with ordering, supersede, conflict, and fallback</name>
  <reversibility rating="costly">History entry shape plus revert semantics are read by later sessions; reshaping after ship needs a migration — one-way only after ship, costly while pre-ship.</reversibility>
  <read_first>
    - .planning/phases/16-ai-backend-adapter-opencode/16-RESEARCH.md (Q2 edit-application as entry creation, Q1 double-write idempotency note)
    - .planning/phases/16-ai-backend-adapter-opencode/16-PATTERNS.md (section 7 ordering rules, D-04 accept-time check, D-06 surface reuse)
    - .planning/phases/16-ai-backend-adapter-opencode/16-SPEC.md (R3 acceptance, R2/R3 edge rows: supersede, emission order, stale refuse, history order)
    - theia/extensions/backend-opencode/src/node/opencode-changeset-emitter.ts (Task-01 tracer to extend)
  </read_first>
  <files>theia/extensions/backend-opencode/src/node/opencode-changeset-emitter.ts</files>
  <action>Extend the Task-01 emitter so both presets record every applied edit with per-file diff plus working revert on the existing Change Set plus session-history surfaces per D-06: history append order equals apply order, staging order matches backend emission order, overlapping proposals to one hunk resolve latest-supersedes with the old entry marked superseded never merged, zero-edit answers create no entry, and idempotent accept handling tolerates a backend double-write. Put the stale-hunk check at accept time on the Theia side per D-04 by comparing the staged hunk against the live file and refusing with a plain-language conflict while preserving the proposal. Keep the apply-then-record branch per D-03 strictly as fallback behind the intercept path with an immediate history write on the same ordering rules. Name the failure guard historyWriteFailure: when the history write fails the apply is blocked and the failure surfaced, never applied silently. Read secrets only through the captured backend env module. Stage before any scan, build in the Nix theia shell, commit as feat(16-02): mandatory history with revert plus fallback.</action>
  <verify>
    <automated>git add theia/extensions/backend-opencode/src/node/opencode-changeset-emitter.ts && nix develop .#theia --command bash -c "cd theia/extensions/backend-opencode && yarn build" && grep -q 'revert' theia/extensions/backend-opencode/src/node/opencode-changeset-emitter.ts && grep -q 'supersed' theia/extensions/backend-opencode/src/node/opencode-changeset-emitter.ts && grep -q 'historyWriteFailure' theia/extensions/backend-opencode/src/node/opencode-changeset-emitter.ts && test $(grep -v '^\s*//' theia/extensions/backend-opencode/src/node/opencode-changeset-emitter.ts | grep -c 'process\.env') -eq 0 && grep -q 'exit(78)' theia/extensions/backend-opencode/src/node/opencode-acp-supervisor.ts && scripts/diff-theia-core.sh --quick && ! pwd | grep -q ' ' && scripts/verify-platform.sh --quick</automated>
    <fails_when>build fails, revert or supersede handling absent, history-write-failure guard absent, host-env object read present, fail-closed exit lost, core diff red, or quick exits non-zero</fails_when>
  </verify>
  <acceptance_criteria>
    - Auto-accept writes without click with revertible history; gated writes nothing without accept
    - All four ordering plus supersede plus zero-edit rules hold on both the intercept and fallback paths
    - Stale accept refuses with conflict and keeps the proposal; history-write failure blocks the apply
  </acceptance_criteria>
  <done>History plus revert is mandatory, ordered, and conflict-safe on both paths</done>
</task>

<task type="auto">
  <name>Preset gate plus R3 backstop held-out test</name>
  <reversibility rating="reversible">Test scaffolding only; changing the gate touches one script plus its two registry rows.</reversibility>
  <read_first>
    - .planning/phases/16-ai-backend-adapter-opencode/16-SPEC.md (R3 acceptance, R3 backstop row: revert under concurrent user edits, Prohibitions auto-accept plus redaction rows)
    - .planning/phases/16-ai-backend-adapter-opencode/16-PATTERNS.md (Build/Test Commands registry rule, gate idiom EXPECTED plus derivation plus self-test)
    - scripts/verify-opencode-tracer.mjs (Task-01 gate shape to mirror, STAGED-holdout convention)
  </read_first>
  <files>scripts/verify-opencode-presets.mjs, scripts/verify-platform.sh</files>
  <action>Write the honestly-quick preset gate deriving the toggle default, per-session scope, history ordering, supersede marking, and stale-refuse behavior from source at check time with set equality against one EXPECTED const, plus fixture proofs for default-gated assertion, auto-accept-enabled-only-by-explicit-action, history-order-equals-apply-order, and redaction of token-shaped strings. Add an explicit two-file emission-to-staging order fixture: proposals emitted for file A then file B must stage in order A,B. Cover the R3 backstop as an explicit held-out test: revert-after-concurrent-external-edit on one hunk, printing STAGED with the exact rerun command and exiting cleanly when the live session host is absent rather than passing silently. Assert core diff clean, path space-free, captured-env use, and fail-closed exit present. Give a self-test proving green first then a persisted-preset plant, a merged-supersede plant, a silent-stale-accept plant, a history-order plant, and a reorder plant (B,A) each going red naming the drift. Append two registry rows with honesty comments, no sibling driver. Stage before any scan, commit as feat(16-02): preset plus history gate. Finish with script, self-test, sampled rows, and quick green.</action>
  <verify>
    <automated>git add scripts/verify-opencode-presets.mjs scripts/verify-platform.sh && node --check scripts/verify-opencode-presets.mjs && node scripts/verify-opencode-presets.mjs && node scripts/verify-opencode-presets.mjs --self-test && scripts/verify-platform.sh --only ai-opencode-presets && scripts/verify-platform.sh --only ai-opencode-presets-self-test && scripts/diff-theia-core.sh --quick && ! pwd | grep -q ' ' && scripts/verify-platform.sh --quick</automated>
    <fails_when>script red on the unmodified tree, any plant fails to go red naming the drift, backstop holdout passes silently without a host, core diff red, or quick exits non-zero</fails_when>
  </verify>
  <acceptance_criteria>
    - Gate derives preset plus history shape from source with set equality and fixture proofs
    - Two-file emission order equals staging order by fixture, with a reorder plant in self-test
    - Default-preset and explicit-action prohibitions proven; R3 backstop held out honestly
    - Self-test green-first with plant-landed asserts on all five faults
    - Exactly two new rows with honesty comments and no sibling driver
  </acceptance_criteria>
  <done>Presets plus history are enforced mechanically with the R3 edge held out</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Preset gesture to apply path | Toggle selects staged queue versus immediate apply with history. |
| Staged hunk to live file | Accept-time comparison decides write versus conflict refusal. |
| History entry to revert | Stored diff drives the revert; shape changes need migration. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-02-01 | Tampering | auto-accept inherited across sessions | high | mitigate | Default gated, per-session scope only; default-preset assertion test in the new gate. |
| T-16-02-02 | Tampering | stale hunk silently overwriting external edits | high | mitigate | Accept-time live-file comparison per D-04; refuse with conflict, preserve proposal. |
| T-16-02-03 | Tampering | overlapping proposals merged silently | medium | mitigate | Latest supersedes with old marked, never merged; ordering asserted by the gate. |
| T-16-02-04 | Information disclosure | token into history | high | mitigate | Redaction test over history fixtures; captured-env path only. |
| T-16-02-SC | Tampering | npm installs | low | accept | No new package in this plan; tracer SDK pin already landed. |
</threat_model>

<verification>
Extension builds in the Nix theia shell, preset defaults plus history ordering plus conflict handling proven, preset script plus self-test green, sampled rows plus guard rows green, core diff clean, path space-free, quick green.
</verification>

<success_criteria>
Presets are safe by default with mandatory revertible history and conflict-safe accept on both the intercept and fallback paths.
</success_criteria>

<output>
Plan 16-03 adds the MCP bridge plus selection wiring on top of this review slice.
</output>
