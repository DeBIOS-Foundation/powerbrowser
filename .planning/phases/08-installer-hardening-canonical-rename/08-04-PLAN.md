---
phase: 08-installer-hardening-canonical-rename
plan: "04"
type: execute
wave: 3
depends_on: [08-01, 08-02]
files_modified:
  - scripts/generate.mjs
  - .mozconfig
  - powerbrowser/distribution/policies.json
  - scripts/verify-installer-build-proof.mjs
  - scripts/verify-mar-update-hop.mjs
  - scripts/verify-platform.sh
  - docs/BUILD.md
autonomous: true
requirements: [PKG-02, PKG-01, PKG-03]
estimate:
  tokens: 45000
  raw_tokens: 90000
  tasks: 3
  confidence: high
must_haves:
  truths:
    - "The mozconfig no longer disables the updater and the built tree contains an updater binary"
    - "A Linux client performs one real N to N-plus-1 MAR hop from the fork update descriptor with zero Mozilla update hosts contacted"
    - "makensis on Nix compiles the upstream installer script with generated branding on the Linux host"
    - "BUILD.md documents the packaging procedure with attributed timings"
  artifacts:
    - path: ".mozconfig"
      provides: "Regenerated updater-enabled build flags"
      contains: "enable-unverified-updates"
    - path: "powerbrowser/distribution/policies.json"
      provides: "Fork update URL policy without the self-update contradiction"
      contains: "AppUpdateURL"
    - path: "scripts/verify-installer-build-proof.mjs"
      provides: "Build-proof check with synthetic-dist self-test twin"
      contains: "runChecks"
    - path: "scripts/verify-mar-update-hop.mjs"
      provides: "MAR-hop check with mock-MAR self-test twin and zero-Mozilla-host proof"
      contains: "runChecks"
    - path: "docs/BUILD.md"
      provides: "Packaging procedure with attributed timings and key-custody rung"
      contains: "AppUpdateURL"
  key_links:
    - from: "scripts/generate.mjs"
      to: ".mozconfig"
      via: "mozconfig emitter regenerates flags through the byte-identity gate in one commit"
      pattern: "emitMozconfig|byte-identity"
    - from: "powerbrowser/distribution/policies.json"
      to: "scripts/verify-mar-update-hop.mjs"
      via: "hop check reads the effective update URL from policy, never from user-branch prefs"
      pattern: "AppUpdateURL"
    - from: "scripts/verify-platform.sh"
      to: "scripts/verify-installer-build-proof.mjs"
      via: "registry rows appended with self-test twins riding quick"
      pattern: "installer-build-proof"
  prohibitions:
    - statement: "The updater flag flip must never be accompanied by an upstream/ edit; a mozconfig or policy shaped hole means reshape the approach"
      verification: backstop
    - statement: "A same-version MAR loop must never be presented as the exit proof; exit requires two distinct versions and build IDs"
      verification: backstop
    - statement: "The update hop must never contact Mozilla update hosts; a green hop with no fork-server access-log entry is a failure, not a pass"
      verification: backstop
  flagged_assumptions:
    - "PKG-01/unclassified: edge probe unclassified, reviewed manually — Nix NSIS spike is the falsification test; Windows-only plugin incompatibility is the known residual, handled by the recorded fallback"
    - "PKG-02/idempotency: probe asks what rerunning on the same input does — regenerating update.xml from identical MAR bytes is byte-identical and re-serving is harmless; assumed idempotent, asserted by re-running generation twice in the hop check"
    - "PKG-02/concurrency: probe asks about interruption or parallel runs — two builds use separate objdirs and MAR outputs are content-addressed by version; parallel-hop guarantees beyond separate objdirs are out of scope, recorded here"
---

<objective>
Enable the updater, prove one real Linux N to N-plus-1 MAR hop under fork signing, compile the NSIS installer on Nix, and document the packaging procedure, per CONTEXT locked pre-decisions (self-hosted MAR under fork signing; Nix first with agent-VM fallback).

Purpose: Turn schema-complete installer fragments into a proven self-update story on the cheapest host before real-host matrix work.
Output: Enabled updater build, green MAR hop, Nix-built NSIS proof, registry rows with self-test twins, BUILD.md procedure.

Host: Linux Nix dev host for every task in this plan (nix shell nixpkgs nsis for makensis; nix develop .#firefox for builds). Falsification criterion triggering the fallback: if makensis-on-Nix cannot emit a byte-valid installer within this plan because the pinned installer script needs Windows-only NSIS plugins or otherwise fails on Linux, record the exact stderr plus the missing plugin set in the SUMMARY, keep the schema and MAR proofs, and continue the NSIS artifact itself on the plan 08-05 Windows VM. Host selection is not ambiguous: nothing in this plan waits on a VM.
</objective>

<execution_context>
<purpose>Execute this PLAN.md, commit per task, create SUMMARY.md.</purpose>
<required_reading>
Read .planning/STATE.md before any operation. Tier-3 rebuilds cost about 47 to 54 minutes each; run them inside nix develop .#firefox and never gate quick checks on them.
Commits: one per task as feat, then a docs close-out with SUMMARY.md.
</required_reading>
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08-installer-hardening-canonical-rename/08-RESEARCH.md
@.planning/phases/08-installer-hardening-canonical-rename/08-PATTERNS.md
@.planning/phases/08-installer-hardening-canonical-rename/08-01-SUMMARY.md
@.planning/phases/08-installer-hardening-canonical-rename/08-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Flip the updater flag, regenerate mozconfig, rebuild, assert updater present</name>
  <reversibility rating="one-way">Published MARs, served update descriptors, and rebuilt objdirs depend on this flag set; reverting requires full rebuilds and re-serving.</reversibility>
  <read_first>scripts/generate.mjs lines 1150-1190; .mozconfig; scripts/verify-endpoints.sh layer-1 policy expectation; docs/BUILD.md tier-3 section</read_first>
  <files>scripts/generate.mjs, .mozconfig</files>
  <action>Flip the updater literal in the mozconfig emitter from disable-updater to enable-unverified-updates as a literal flip with no new manifest table, regenerate .mozconfig through the byte-identity gate with emitter and tracked comparand changing in one commit, and record the MAR signing-key custody rung as fork key plus HTTPS plus hash-pinned update descriptors in the commit message for BUILD.md to cite. Rebuild the dev tree inside the firefox Nix shell and assert the updater binary exists beside the build output and config.status shows the new flag before any MAR test runs.</action>
  <acceptance_criteria>Byte-identity green on the emitter change; dist updater binary present; config.status carries enable-unverified-updates; no upstream diff; no new Gecko patch; key-custody rung recorded.</acceptance_criteria>
  <verify>
    <automated>node scripts/generate.mjs --check && git -C upstream diff --quiet && test -x objdir/dist/bin/updater && echo UPDATER_PRESENT</automated>
    <fails_when>Any link fails, the updater binary is absent or not executable, or the UPDATER_PRESENT line is missing</fails_when>
  </verify>
  <done>Updater compiled in on the dev tree with the flag change proved through byte-identity</done>
</task>

<task type="auto">
  <name>Point policy at the fork server and add the MAR-hop check with registry rows</name>
  <read_first>powerbrowser/distribution/policies.json; scripts/verify-installer-schema.mjs reporter and entry shape lines 51-90 and 472-485; scripts/verify-platform.sh lines 3908-3918 and 4144-4160; scripts/verify-endpoints.sh resolver-log shape</read_first>
  <files>powerbrowser/distribution/policies.json, scripts/verify-mar-update-hop.mjs, scripts/verify-platform.sh</files>
  <action>Add the AppUpdateURL policy pointing at the fork update descriptor under machine scope in powerbrowser/distribution/policies.json and resolve its contradiction with DisableAppUpdate so a self-updating client is possible, moving the endpoints layer-1 expectation in the same commit. Create scripts/verify-mar-update-hop.mjs copying the installer-schema verifier skeleton with a reporter, a runChecks over a mock MAR plus update descriptor fixture, hash-pinned descriptor generation from MAR bytes with sha512 and size, a two-distinct-version exit assertion that rejects same-version loops as plumbing-only, and a zero-Mozilla-host negative proof reusing the resolver-log sifting shape. Append the full row plus its self-test twin to scripts/verify-platform.sh with the honesty comment earning quick for the twin only.</action>
  <acceptance_criteria>Policy carries AppUpdateURL with no self-update contradiction; mock hop self-test passes on control-green then goes red for same-version descriptors, hash mismatch, and any Mozilla host in the resolver log; registry rows appended with no sibling driver; full row fails rather than skips when its subject is absent.</acceptance_criteria>
  <verify>
    <automated>node scripts/verify-mar-update-hop.mjs --self-test && scripts/verify-platform.sh --only mar-update-hop-self-test</automated>
    <fails_when>The self-test exits non-zero, a planted fault is not rejected naming file and values, or the --only row reports anything but PASS</fails_when>
  </verify>
  <done>Policy and hop gate ready; real two-build hop runs as the task proof with fork-server access log cited</done>
</task>

<task type="auto">
  <name>Prove NSIS on Nix, add the build-proof row, write the packaging procedure</name>
  <read_first>scripts/verify-installer-schema.mjs expectedFragments and set-equality shape; docs/BUILD.md tier-3 and rebase sections; upstream installer defines contract via generated branding.nsi</read_first>
  <files>scripts/verify-installer-build-proof.mjs, scripts/verify-platform.sh, docs/BUILD.md</files>
  <action>Compile the pinned upstream installer script with the generated branding.nsi six defines using makensis from a nix shell on the Linux host as a spike first, reporting the plugin set, and on success create scripts/verify-installer-build-proof.mjs asserting the installer artifact exists from generated inputs with set-equality in both directions plus a synthetic-dist self-test twin, appending both rows to the registry. On spike failure record the exact stderr and missing plugins and route the artifact to the plan 08-05 Windows VM per the plan objective fallback. Append the BUILD.md packaging procedure section covering the NSIS-on-Nix path, the MAR build and serve loop, the key-custody rung, and the VM paths, with every timing attributed to tree plus host plus toolchain and no inherited number presented as fresh.</action>
  <acceptance_criteria>Either a Nix-built installer artifact plus green build-proof rows, or a recorded spike failure with exact stderr routed to the Windows VM; BUILD.md procedure complete with attributed timings; no hand-kept installer expectation where derivation was possible.</acceptance_criteria>
  <verify>
    <automated>node scripts/verify-installer-build-proof.mjs --self-test && scripts/verify-platform.sh --quick</automated>
    <fails_when>The self-test exits non-zero or --quick exits non-zero</fails_when>
  </verify>
  <done>NSIS path proven on Nix or explicitly routed to VM with evidence; procedure documented</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fork server to client | MAR bytes and descriptor cross the network into the updater |
| makensis to installer artifact | Generated defines cross into compiled installer |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-04a | Tampering | MAR update channel | high | mitigate | Fork-key signing plus HTTPS plus hash-pinned descriptors; AppUpdateURL under machine scope; zero-Mozilla-host negative proof, this plan |
| T-08-04b | Spoofing | installer provenance | high | mitigate | Build-proof row derives from TARGETS and generated inputs both directions; prod-cert decision tracked as out-of-phase, test-sign only here |
| T-08-04c | Tampering | NSIS define injection | high | mitigate | WR-04 guard from plan 08-01 enforced at emission; schema gate asserts non-empty defines before compile |
| T-08-SC | Tampering | npm/pip/cargo installs | high | mitigate | No registry installs in this plan; nsis arrives via nix shell as host tooling only |
</threat_model>

<artifacts_produced>
Symbols created by this plan: mozconfig literal enable-unverified-updates; config key AppUpdateURL in distribution/policies.json; new files scripts/verify-mar-update-hop.mjs with runChecks plus descriptor generation helper and mock fixture shape, and scripts/verify-installer-build-proof.mjs with runChecks; registry rows mar-update-hop, mar-update-hop-self-test, installer-build-proof, installer-build-proof-self-test; BUILD.md packaging-procedure section.
</artifacts_produced>

<verification>
Updater binary present; mock hop self-test green with all plants red; build-proof self-test green; --quick green; one real Linux N to N-plus-1 hop logged with fork-server access entry.
</verification>

<success_criteria>
Updater enabled and present; update story proven on Linux with two distinct builds; NSIS proven on Nix or routed with evidence; procedure documented with attributed timings.
</success_criteria>

<output>
Create `.planning/phases/08-installer-hardening-canonical-rename/08-04-SUMMARY.md` when done
</output>
