---
phase: 08-installer-hardening-canonical-rename
plan: "02"
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - scripts/verify-branding-preflight.mjs
  - scripts/verify-branding.mjs
  - scripts/generate.mjs
  - docs/BUILD.md
  - .planning/milestones/v1.0-phases/07-sourcerer-as-downstream/fixtures/canonical-name/configuration.toml
  - .planning/milestones/v1.0-phases/07-sourcerer-as-downstream/fixtures/canonical-name/brand/mark.svg
autonomous: true
requirements: [NAME-01]
estimate:
  tokens: 30000
  raw_tokens: 60000
  tasks: 3
  confidence: high
must_haves:
  truths:
    - "No generated or built surface carries the spaced display form"
    - "The preflight, branding check, and generator self-test assert the canonical form"
    - "A single-token display name emits cleanly through shell quoting, desktop Name, locale files, and NSIS defines"
    - "The identity checker derives the new value with no re-pin, proving single-edit propagation"
  artifacts:
    - path: "scripts/verify-branding-preflight.mjs"
      provides: "Re-pinned plants and messages on the canonical form"
      contains: "PowerBrowser"
    - path: "scripts/verify-branding.mjs"
      provides: "Updated display expectation"
      contains: "PowerBrowser"
    - path: ".planning/milestones/v1.0-phases/07-sourcerer-as-downstream/fixtures/canonical-name/configuration.toml"
      provides: "No-interior-space downstream fixture"
      contains: 'display_name = "PowerBrowser"'
  key_links:
    - from: "inventory/brand-tokens.json"
      to: "scripts/verify-branding-preflight.mjs"
      via: "preflight expected values follow the inventory re-pin from plan 08-01"
      pattern: "brand_display_expectations|brand_full_name"
    - from: "scripts/verify-branding-identity.mjs"
      to: "configuration.toml"
      via: "VARIANTS structural paths derive values via resolveConfig with no re-pin"
      pattern: "resolveConfig"
  prohibitions:
    - statement: "The re-pin must not rewrite the spaced-name mechanism fixture; it tests multi-word spacing as a mechanism, not the platform value"
      verification: backstop
    - statement: "No display surface may split on interior space or trim a spaceless name to empty; the new fixture must catch under-spacing"
      verification: backstop
    - statement: "User-facing error copy re-pinned here must name the product and end with a real on-screen next step, never a pref key or sentinel"
      verification: backstop
  flagged_assumptions:
    - "NAME-01/adjacency: probe asks what merges or collides at exact equality — treated as the Dev-suffix join base plus suffix; PowerBrowser Dev with the spaced suffix is the pinned join, assumed stable"
    - "NAME-01/empty: probe asks about empty or single-element input — an empty display_name is rejected by schema validation before emission; assumed covered, not separately planted"
    - "NAME-01/ordering: probe asks about stable order on equal comparison — the divergence check compares both directions as sets over named keys; key order is assumed insignificant"
---

<objective>
Complete the NAME-01 propagation: re-pin every remaining spaced-form gate, add the canonical-form downstream fixture, and re-run the single-edit propagation proof live, per CONTEXT locked pre-decision.

Purpose: Every installer, branding, and trademark surface asserts PowerBrowser before packaging proofs multiply the work.
Output: Re-pinned gates, new no-space fixture, green propagation proof.
</objective>

<execution_context>
<purpose>Execute this PLAN.md, commit per task, create SUMMARY.md.</purpose>
<required_reading>
Read .planning/STATE.md before any operation. Needs plan 08-01 green: canonical manifest value plus re-pinned inventory.
Commits: one per task as feat/fix, then a docs close-out with SUMMARY.md.
</required_reading>
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08-installer-hardening-canonical-rename/08-RESEARCH.md
@.planning/phases/08-installer-hardening-canonical-rename/08-PATTERNS.md
@.planning/phases/08-installer-hardening-canonical-rename/08-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Re-pin preflight plants and branding expectation to the canonical form</name>
  <read_first>scripts/verify-branding-preflight.mjs plant regions near the brand-full-name and title plants; scripts/verify-branding.mjs near line 204; inventory/brand-tokens.json variants block as re-pinned in 08-01</read_first>
  <files>scripts/verify-branding-preflight.mjs, scripts/verify-branding.mjs</files>
  <action>Re-pin every occurrence of the spaced display form in scripts/verify-branding-preflight.mjs to the canonical values from the 08-01 inventory rewrite, following the file's own plant-then-require-red shape with control-green-first for each touched plant, deriving the mutation target at self-test time rather than pinning line numbers. Update the display-form comment and expectation together in scripts/verify-branding.mjs so the expectation and its rationale move as one. Keep the derivation discipline: surfaces derived from the tree, expected values from the inventory block only.</action>
  <acceptance_criteria>Zero occurrences of the spaced platform display form remain in either file as a platform expectation; every touched plant goes red naming the expected canonical value; the preflight self-test twin passes; verify-branding-identity.mjs is untouched and still derives via resolveConfig.</acceptance_criteria>
  <verify>
    <automated>node scripts/verify-branding-preflight.mjs --self-test && scripts/verify-platform.sh --only branding-preflight</automated>
    <fails_when>The self-test exits non-zero, or the --only row reports anything but PASS</fails_when>
  </verify>
  <done>All display gates assert PowerBrowser with plant coverage proving each one can go red</done>
</task>

<task type="auto">
  <name>Re-pin generator self-test comparands and audit trademark exclusions plus BUILD prose</name>
  <read_first>scripts/generate.mjs near lines 222 and 4530; scripts/verify-trademark-surface.mjs fixture near lines 387-390; docs/BUILD.md BRAND-06 section</read_first>
  <files>scripts/generate.mjs, docs/BUILD.md</files>
  <action>Re-pin the generator error-message example and the brand.properties planted-drift comparand in scripts/generate.mjs to the canonical dev form PowerBrowser Dev, keeping the Dev suffix spaced per the locked default. Read the trademark-surface anchored exclusions and confirm none references the old spaced platform value, leaving its spaced multi-word hostile fixture shape untouched since it tests spacing as a mechanism. Align docs/BUILD.md brand prose with the canonical form, preserving the attributed timing rows untouched since timings are unaffected by the rename.</action>
  <acceptance_criteria>Generator self-test comparands carry the canonical dev form; trademark fixture still uses a spaced multi-word hostile name that is not the platform value; BUILD.md brand prose names the canonical form and every timing row keeps its tree-plus-host-plus-toolchain attribution; generate --self-test passes.</acceptance_criteria>
  <verify>
    <automated>node scripts/generate.mjs --self-test && node scripts/verify-trademark-surface.mjs --self-test</automated>
    <fails_when>Either self-test exits non-zero or reports a planted case not rejected</fails_when>
  </verify>
  <done>Generator, trademark, and docs expectations agree on the canonical form with attributions intact</done>
</task>

<task type="auto">
  <name>Add canonical-form downstream fixture and run the propagation proof</name>
  <read_first>.planning/milestones/v1.0-phases/07-sourcerer-as-downstream/fixtures/spaced-name/configuration.toml; .planning/milestones/v1.0-phases/07-sourcerer-as-downstream/fixtures/README.md</read_first>
  <files>.planning/milestones/v1.0-phases/07-sourcerer-as-downstream/fixtures/canonical-name/configuration.toml, .planning/milestones/v1.0-phases/07-sourcerer-as-downstream/fixtures/canonical-name/brand/mark.svg</files>
  <action>Create a canonical-name downstream fixture copying the spaced-name shape verbatim with invented names and URLs only, inverting the hostility to a single-token display_name with no interior space, and exercise it through the downstream overlay path of generate --check plus the fixture verifier so shell quoting, desktop Name, locale files, and NSIS defines prove the no-space form emits cleanly. Then run the full single-edit propagation proof on the platform tree: generate, --check, byte-identity, preflight, branding-identity both variants where buildable, trademark-surface, and residue scan, plus an identity-field freeze assertion that app_basename, binary_name, remoting_name, and distribution_id are byte-identical to pre-rename.</action>
  <acceptance_criteria>New fixture overlays clean through generate --check; spaced-name fixture untouched; full proof chain green; freeze assertion passes; no new gate asserts on a hand-kept literal where derivation was possible.</acceptance_criteria>
  <verify>
    <automated>node scripts/generate.mjs --check && scripts/verify-platform.sh --quick && node scripts/scan-brand-residue.mjs</automated>
    <fails_when>Any command exits non-zero or --check reports a stale, absent, or leftover path</fails_when>
  </verify>
  <done>NAME-01 closed on static surfaces: fixture proves the no-space form, proof chain green, identity pins frozen</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| inventory to preflight | Hand-authored expectation crosses into gate input |
| fixture to generator | Downstream-shaped manifest crosses into emission |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-02a | Spoofing | display-name surfaces | medium | mitigate | Re-pinned gates plus no-space fixture catch over- and under-spacing; identity pins frozen against profile and remoting collision, this plan |
| T-08-02b | Tampering | inventory expectation rewrite | medium | mitigate | Mechanical-pass discipline: inventory authored by hand, never derived from branding output; preflight asserts both counts and values |
| T-08-SC | Tampering | npm/pip/cargo installs | high | mitigate | No registry installs in this plan |
</threat_model>

<artifacts_produced>
Symbols created by this plan: new fixture directory canonical-name with configuration.toml and brand/mark.svg; re-pinned plant strings in verify-branding-preflight.mjs and verify-branding.mjs; re-pinned comparand strings in scripts/generate.mjs; aligned BRAND-06 prose in docs/BUILD.md. No new CLI flags, verify rows, or config keys.
</artifacts_produced>

<verification>
Preflight row plus both self-test twins plus --quick plus residue scan, green on the canonical tree.
</verification>

<success_criteria>
No spaced-form platform expectation anywhere; no-space emission proven through the overlay path; propagation control untouched and green.
</success_criteria>

<output>
Create `.planning/phases/08-installer-hardening-canonical-rename/08-02-SUMMARY.md` when done
</output>
