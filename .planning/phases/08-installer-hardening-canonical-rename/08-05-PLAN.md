---
phase: 08-installer-hardening-canonical-rename
plan: "05"
type: execute
wave: 4
depends_on: [08-04]
files_modified:
  - docs/BUILD.md
  - .planning/WINDOWS.md
autonomous: true
requirements: [PKG-01, PKG-02, BLD-01, UPD-03]
estimate:
  tokens: 40000
  raw_tokens: 80000
  tasks: 3
  confidence: high
must_haves:
  truths:
    - "MSIX and DMG artifacts are built on named real hosts, never on Linux"
    - "The per-OS install, launch, uninstall, no-residue matrix is green including alongside stock Firefox"
    - "One real N to N-plus-1 hop is proven per OS from the fork server"
    - "The release objdir build passes with release-variant rows green"
    - "The ESR rebase drill passes or is staged with the exact unblock condition"
  artifacts:
    - path: "docs/BUILD.md"
      provides: "Packaging timings per host plus matrix results plus drill outcome"
      contains: "pkg-win11"
    - path: ".planning/WINDOWS.md"
      provides: "Ledger updates closing release-gated and matrix entries"
      contains: "CLOSED"
  key_links:
    - from: "docs/BUILD.md"
      to: "scripts/verify-platform.sh"
      via: "procedure cites the registry rows that gate each proof"
      pattern: "installer-build-proof|mar-update-hop"
    - from: ".planning/WINDOWS.md"
      to: "scripts/verify-platform.sh"
      via: "gate exclusions key on ledger ids, never bare labels"
      pattern: "ledger_entry_is_open"
  prohibitions:
    - statement: "No MSIX or DMG attempt from Linux tooling; the pinned tree's own makeappx and hdiutil requirements are the authority"
      verification: backstop
    - statement: "The matrix must not skip the alongside-stock-Firefox interleaved launch or the no-residue check after uninstall"
      verification: backstop
    - statement: "Production signing must not be procured or faked in this phase; matrix proofs run test-signed with the decision recorded"
      verification: backstop
  flagged_assumptions:
    - "BLD-01/unclassified: edge probe unclassified, reviewed manually — release build plus both release-variant rows green is the coverage; release-only drift beyond titlebar pref and brandFullName is accepted as divergence-check coverage"
    - "UPD-03/unclassified: edge probe unclassified, reviewed manually — drill green over the rebased tree including installer-schema is the coverage; if no newer ESR tag exists the drill is staged-unexecuted with the exact tag-watch command, assumed sufficient per 05-04 precedent"
---

<objective>
Build MSIX and DMG on named real hosts, run the per-OS install matrix with per-OS update hops, pass the release build, and run the live ESR rebase drill, closing PKG-01, PKG-02, BLD-01, and UPD-03.

Purpose: Exit the phase with real installers, a proven update story per OS, a green release tree, and a drilled rebase path.
Output: Host-built artifacts, green matrix, green release rows, drill outcome, documented timings.

Hosts: agent-driven Windows 11 VM named pkg-win11 carrying the Windows SDK with makeappx and signtool plus NSIS 3.12, and agent-driven macOS VM named pkg-macos carrying Xcode command-line tools with hdiutil and SetFile. Linux work stays on the Nix dev host. Falsification criterion: if a named VM is unreachable from the agent substrate, record the exact provisioning error and the unblock command, complete every host-independent item, and mark the affected matrix cells staged-unexecuted rather than green. MSIX and DMG are never attempted on Linux.
</objective>

<execution_context>
<purpose>Execute this PLAN.md, commit per task, create SUMMARY.md.</purpose>
<required_reading>
Read .planning/STATE.md before any operation. Test-signing only; production cert procurement is out of phase per Open Question 1.
Commits: one per task as feat/chore, then a docs close-out with SUMMARY.md.
</required_reading>
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08-installer-hardening-canonical-rename/08-RESEARCH.md
@.planning/phases/08-installer-hardening-canonical-rename/08-PATTERNS.md
@.planning/phases/08-installer-hardening-canonical-rename/08-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Name and reach the packaging hosts with a capability record</name>
  <read_first>08-RESEARCH.md Environment Availability and Open Question 3; docs/BUILD.md toolchain-baseline section</read_first>
  <files>docs/BUILD.md</files>
  <action>Establish the two named packaging hosts pkg-win11 and pkg-macos through the available agent VM substrate, recording for each the OS version, SDK or toolchain versions, and reachability proof, plus the Nix Linux host makensis version from the plan 08-04 spike. Record this as a BUILD.md host-capability section shaped as one row per host with fields host name, OS version, SDK or toolchain versions with numbers, makensis version on the Linux row, and reachability state reading reachable with proof or staged-unexecuted with error plus unblock. If a VM is unreachable, record the exact provisioning error and the operator unblock command in BUILD.md and mark dependent matrix cells staged-unexecuted. No artifact work starts on an unnamed host.</action>
  <acceptance_criteria>BUILD.md names both hosts with versions and reachability state; every later task cites its host by name; unreachable hosts carry the exact error plus unblock command and staged cells, never a silent skip.</acceptance_criteria>
  <verify>
    <automated>grep -c 'pkg-win11' docs/BUILD.md && grep -c 'pkg-macos' docs/BUILD.md && grep -E 'makensis [0-9]+\.[0-9]+' docs/BUILD.md && grep -cE 'reachable|staged-unexecuted' docs/BUILD.md && grep -cE 'Windows 11 [0-9]|SDK [0-9]|Xcode [0-9]|macOS [0-9]' docs/BUILD.md</automated>
    <fails_when>Either host count is zero, no makensis version line, no reachability state, or no numbered OS or SDK version line exists</fails_when>
  </verify>
  <done>Hosts named with capability evidence; fallback branches explicit before artifact work</done>
</task>

<task type="auto">
  <name>Build MSIX and DMG on their hosts and run the per-OS matrix with update hops</name>
  <precondition>Host capability record from task 1 exists; any staged-unexecuted host is excluded from this task with its unblock command cited</precondition>
  <read_first>docs/BUILD.md packaging procedure from plan 08-04; scripts/verify-mar-update-hop.mjs exit assertions; powerbrowser/distribution/policies.json AppUpdateURL</read_first>
  <files>docs/BUILD.md</files>
  <action>On pkg-win11 build the MSIX with makeappx and test-sign with signtool plus the NSIS installer if plan 08-04 routed it here, and on pkg-macos build the DMG through mach repackage on the real host, each from generated branding carrying the canonical name. Run the per-OS install, launch, uninstall, no-residue matrix including an alongside-stock-Firefox interleaved launch proving no profile or remoting collision with frozen remoting and window-class pins, and prove one real N to N-plus-1 hop per reachable OS from the fork descriptor with the fork-server access log and zero Mozilla update hosts. Record artifact hashes, matrix cells, hop versions with distinct build IDs, and per-host timings in BUILD.md.</action>
  <acceptance_criteria>Artifacts exist per reachable host from generated inputs; every matrix cell green or staged with unblock cited; alongside launch shows no collision; per-OS hops cite two distinct versions and build IDs with fork-server log entries; timings attributed to tree plus host plus toolchain.</acceptance_criteria>
  <verify>
    <automated>scripts/verify-platform.sh --only installer-build-proof && scripts/verify-platform.sh --only mar-update-hop && grep -Ei 'sha512|hashValue' docs/BUILD.md && grep -Ei 'buildID' docs/BUILD.md && grep -Ei 'fork-server access|access log' docs/BUILD.md && grep -Ei 'zero-Mozilla-host|zero Mozilla' docs/BUILD.md</automated>
    <fails_when>Either row reports anything but PASS on a reachable host, a matrix cell is marked green without its log evidence recorded in BUILD.md, or BUILD.md lacks any of artifact hashes, buildID pairs, fork-server log entries, or the zero-Mozilla-host statement</fails_when>
  </verify>
  <done>Real installers on real hosts with a green matrix and per-OS proven hops</done>
</task>

<task type="auto">
  <name>Pass the release build with release rows and run the ESR rebase drill</name>
  <read_first>docs/BUILD.md tier-3 release procedure; scripts/verify-platform.sh gate exclusions near ledger 10; scripts/rebase-upstream.sh tag and dry-run flags</read_first>
  <files>docs/BUILD.md, .planning/WINDOWS.md</files>
  <action>Run the release objdir-release build inside the firefox Nix shell with the same updater flag set as dev, then pass verify-branding-identity-release and branding-variant-divergence unchanged and close the ledger 10 exclusions with the fresh attributed timing. Run the live ESR rebase drill against the newest tag newer than the pinned release through the existing rebase and conflict tooling including the residual-brand scan in both trees and installer-schema green over the rebased tree, or if no newer tag exists record the drill staged-unexecuted with the exact tag-watch command per precedent. Update BUILD.md and the ledger with outcomes.</action>
  <acceptance_criteria>Release rows green on the fresh release tree with timing attributed; rebase drill green including installer-schema over the rebased tree, or staged with the tag-watch command when no newer tag exists; ledger reflects the true state with no premature Complete.</acceptance_criteria>
  <verify>
    <automated>scripts/verify-platform.sh --gate</automated>
    <fails_when>The gate exits non-zero for the reachable set, or a staged drill lacks the tag-watch command</fails_when>
  </verify>
  <done>Release tree green and rebase path drilled or explicitly staged with its unblock</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| named host to artifact | Binaries cross from build host into user machines |
| rebased tree to gates | New upstream content crosses into the fork pipeline |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-05a | Spoofing | installer signing identity | high | accept | Matrix proofs run test-signed with expected SmartScreen and Gatekeeper friction documented; production cert procurement is a later decision per Open Question 1, rationale recorded in BUILD.md |
| T-08-05b | Tampering | MAR per-OS hops | high | mitigate | Same fork-key plus hash-pin plus zero-Mozilla-host proof as plan 08-04, repeated per OS with distinct versions |
| T-08-05c | Tampering | rebase drift | medium | mitigate | Drill includes residual-brand scan both trees plus installer-schema green; conflict tooling backstops silent adoption |
| T-08-SC | Tampering | npm/pip/cargo installs | high | mitigate | No registry installs in this plan |
</threat_model>

<artifacts_produced>
Symbols created by this plan: host identities pkg-win11 and pkg-macos with capability records; per-host artifact hashes and timings; matrix cell results; per-OS hop version pairs; release timing row; drill outcome or tag-watch command. No new files, functions, flags, rows, or config keys; BUILD.md and WINDOWS.md extended only.
</artifacts_produced>

<verification>
Registry build-proof and hop rows green per reachable host; gate green for the reachable set; every timing attributed; staged items carry unblock commands.
</verification>

<success_criteria>
Real MSIX and DMG from named hosts; matrix green with alongside proof; per-OS hops proven; release rows green; drill passed or staged with precision.
</success_criteria>

<output>
Create `.planning/phases/08-installer-hardening-canonical-rename/08-05-SUMMARY.md` when done
</output>
