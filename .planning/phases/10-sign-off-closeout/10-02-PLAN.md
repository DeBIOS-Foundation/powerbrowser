---
phase: 10-sign-off-closeout
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified: [.planning/phases/10-sign-off-closeout/10-02-SUMMARY.md]
autonomous: true
requirements: [GUI-01, GUI-03, GEN-01, GEN-03, EXT-01, TEL-01, TEL-02, TEL-03, VER-01]
estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Every live drill opens with environment probes and stages with exact commands plus blocking condition plus operator unblock where unrunnable — never environment-red as product-red"
    - "GEN-01 and GEN-03 deltas are re-proven on the current tree while the 08/09 live halves stay cited, never re-run"
    - "VER-01 fleet proof runs the BLD-02 fixture-tier chain on the current tree with the static manifest-literals layer green"
    - "TEL-01 declaration, TEL-02 delivery, EXT-01 bundle-plus-load, TEL-03 installed-binary layer, and GUI-01 automated rows are green where runnable and honestly staged where not"
  artifacts:
    - path: ".planning/phases/10-sign-off-closeout/10-02-SUMMARY.md"
      provides: "Live-drill evidence record with green logs or staged-with-unblock rows"
      contains: "staged-unexecuted"
  key_links:
    - from: ".planning/phases/10-sign-off-closeout/10-02-SUMMARY.md"
      to: ".planning/phases/10-sign-off-closeout/10-01-SUMMARY.md"
      via: "drills build on the record-close boxes and reuse the staged UAT runbook steps verbatim"
      pattern: "10-01"
    - from: ".planning/phases/10-sign-off-closeout/10-02-SUMMARY.md"
      to: "scripts/verify-platform.sh"
      via: "every drill outcome is a --only row run plus observed result, never prose"
      pattern: "--only"
    - from: "theia/extensions/telemetry/src/browser/telemetry-sender.ts"
      to: "scripts/crash-collector.mjs"
      via: "TEL-02 delivery drill round-trips sender output into the loopback collector"
      pattern: "TELEMETRY_LEVELS"
  prohibitions:
    - "No tier-3 rebuild for completeness; binaries on disk are reused and only a tree delta onto a compiled surface justifies a scheduled rebuild"
    - "No green claim without logs; unreachable or display-bound cells stay staged-unexecuted with exact errors"
    - "No new drill scripts; genuinely new assertions land as registry rows with self-test twins or not at all"
  assumptions:
    - "Built binaries plus the installed sidecar prerequisites are present from the v1.1 proofs; the first task verifies staleness rather than trusting it"
    - "Display and the Theia dev shell are unprobed at plan time, so every live-drill task probes first and stages with unblocks on absence"
---

<objective>
Run the still-staged live drills green against the already-built tree, with probe-first staging discipline everywhere the environment cannot serve the drill.

Purpose: Close the live halves of GEN-01, GEN-03, EXT-01, TEL-01, TEL-02, TEL-03, VER-01, and the GUI-01 automated rows on current-tree evidence, citing — never redoing — the 08/09 live proofs.
Output: Drill evidence with green logs where runnable and staged-with-unblock rows where not, recorded in the plan SUMMARY.
</objective>

<execution_context>
<purpose>
Execute a phase prompt (PLAN.md) and create the outcome summary (SUMMARY.md).
</purpose>

<required_reading>
Read STATE.md before any operation to load project context.
Read config.json for planning behavior settings.
</required_reading>
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-sign-off-closeout/10-CONTEXT.md
@.planning/phases/10-sign-off-closeout/10-RESEARCH.md
@.planning/phases/10-sign-off-closeout/10-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Environment probes plus GEN-01 and GEN-03 delta re-proofs</name>
  <files>.planning/phases/10-sign-off-closeout/10-02-SUMMARY.md</files>
  <read_first>scripts/verify-platform.sh harness-display-available row plus gui01 full-tier rows plus verify-endpoints row, 08-VERIFICATION.md release-rows-green truth plus ledger-10-closed line, scripts/verify-generated-identity.mjs byte-identity contract, scripts/verify-installer-schema.mjs fixture-manifest root contract</read_first>
  <action>Open with the three probes and record each outcome: run the harness-display-available row for headed availability, verify the Theia dev shell answers a trivial command for drill readiness, and diff the change history since the v1.1 build proofs against compiled surfaces (shell sources, patch stack, branding inputs) to rule on binary staleness, scheduling no rebuild unless a delta touches a compiled surface. Then re-prove the GEN-01 emitter delta (generate-check plus generated-byte-identity on the current tree, crediting the release-variant half to the cited BLD-01 record) and the GEN-03 delta (installer-schema on the current tree, crediting the NSIS-on-Nix half to the cited PKG-01 record), writing probe outcomes plus delta evidence into the SUMMARY. Where a probe fails, record the drill as staged-unexecuted with the exact command sequence, the blocking condition observed, and the operator unblock.</action>
  <acceptance_criteria>Three probe outcomes recorded with the staleness ruling explicit; GEN-01 delta rows green on the current tree with BLD-01 credited by pointer; GEN-03 delta row green with PKG-01 credited by pointer; any unrunnable drill staged with commands plus condition plus unblock</acceptance_criteria>
  <verify>
    <automated>scripts/verify-platform.sh --only harness-display-available; scripts/verify-platform.sh --only generate-check && scripts/verify-platform.sh --only generated-byte-identity && scripts/verify-platform.sh --only installer-schema && scripts/verify-platform.sh --quick</automated>
    <fails_when>The generate-check, generated-byte-identity, or installer-schema row exits nonzero, or --quick reports red; the display-availability probe may report either state but its outcome must be recorded verbatim in the SUMMARY rather than treated as a product verdict</fails_when>
  </verify>
  <done>Probes recorded and emitter plus installer deltas re-proven on the current tree</done>
</task>

<task type="auto">
  <name>VER-01 fleet proof plus TEL-03 installed-binary layer</name>
  <files>.planning/phases/10-sign-off-closeout/10-02-SUMMARY.md</files>
  <read_first>scripts/verify-downstream-fixture.mjs all-mode plus fixtures-root argv plus self-test discrimination, scripts/verify-manifest-literals.mjs static-layer contract, 09-VERIFICATION.md BLD-02 tier-3 matrix evidence plus staged-not-gap routing language, scripts/verify-endpoints.sh installed-binary layer contract, powerbrowser/endpoint-allowlist.json allow entries with reason fields</read_first>
  <action>Run the VER-01 chain on the current tree: the verify-manifest-literals static layer plus its self-test, then the verify-downstream-fixtures row driving the BLD-02 fixture tier, recording the observed assertion counts and any cell outcome verbatim in the SUMMARY. Then run the TEL-03 installed-binary layer via the verify-endpoints row against the existing built binary, with the allowlist-schema derivation half already credited from plan 10-01 by pointer. If the fixture tier or the binary layer needs a display, a shell, or a rebuild the probes denied, record it staged-unexecuted with the exact command sequence, the blocking condition, and the operator unblock, and never mark it green without logs. Keep MSIX/DMG cells out of scope entirely: they belong to PKG-01 staged host cells already routed as not-gaps, not to GEN-03 or TEL-03.</action>
  <acceptance_criteria>Manifest-literals layer green with counts recorded; fixture-tier run green with counts recorded or staged with commands plus condition plus unblock; installed-binary layer green or staged with the same discipline; no MSIX/DMG cell pulled into scope</acceptance_criteria>
  <verify>
    <automated>scripts/verify-platform.sh --only verify-manifest-literals && scripts/verify-platform.sh --only verify-downstream-fixtures && scripts/verify-platform.sh --quick</automated>
    <fails_when>The manifest-literals row exits nonzero, or the fixtures row exits nonzero for a product reason (missing-prerequisite reds must be re-recorded as staged with unblocks, never as product verdicts), or --quick reports red</fails_when>
  </verify>
  <done>Fleet proof and allowlist binary layer closed or honestly staged</done>
</task>

<task type="auto">
  <name>TEL-01 plus TEL-02 plus EXT-01 sidecar drills and GUI-01 automated rows</name>
  <files>.planning/phases/10-sign-off-closeout/10-02-SUMMARY.md</files>
  <read_first>theia/extensions/telemetry/src/browser/telemetry-sender.ts level enum plus fail-closed normalization plus queue bounds, theia/extensions/telemetry/test/telemetry-sender.test.mjs plain-node suite, scripts/crash-collector.mjs loopback submit contract, scripts/verify-telemetry.mjs emitter-derived expectations, scripts/verify-extension-pins.mjs fragment-plus-block-plus-hash pipeline, 04-UAT.md EXT-01 drill steps plus TEL-02 drill steps, theia/extensions/tab-uris/src/browser/browser-window-command.ts open affordance with no chrome-side command</read_first>
  <action>Run the sidecar drills where the probes allow: TEL-01 declaration drill (telemetry level plus endpoint declaration flowing to generated prefs on the running sidecar with the telemetry row green), TEL-02 delivery drill (sender suite green plus a loopback collector round-trip showing the submitted record with matching store entry, honoring level gating including silence when off), EXT-01 bundle-plus-load drill following the 04-UAT steps over the 09-04 sidecar path with extension-pins green, and the GUI-01 automated rows against the built binary. Cite the 09-04 sidecar-build proof and the 09 verifier loopback re-proof by pointer rather than rebuilding or re-proving them; attempt no full sidecar rebuild and no Gecko compile inside this task. Anything the probes deny records as staged-unexecuted with exact commands, blocking condition, and operator unblock.</action>
  <acceptance_criteria>TEL-01 declaration evidenced green or staged; TEL-02 suite plus loopback round-trip evidenced or staged; EXT-01 bundle-plus-load evidenced or staged with 09-04 proof cited; GUI-01 automated rows green or staged on display absence; nothing rebuilt for completeness</acceptance_criteria>
  <verify>
    <automated>node theia/extensions/telemetry/test/telemetry-sender.test.mjs && scripts/verify-platform.sh --only telemetry && scripts/verify-platform.sh --only extension-pins && scripts/verify-platform.sh --only crash-collector && scripts/verify-platform.sh --quick</automated>
    <fails_when>The sender suite, telemetry row, extension-pins row, or crash-collector row exits nonzero for a product reason, or --quick reports red; display-bound reds must be re-recorded as staged with unblocks rather than left as failures</fails_when>
  </verify>
  <done>Sidecar drills and automated window rows closed or honestly staged</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| probe→verdict | Environment observations cross into drill verdicts that must stay staged, never product-red |
| drill→record | Live drill outcomes cross into the SUMMARY sign-off record |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-04 | Tampering | drill verdicts under missing display or shell | high | mitigate | Probe-first in every task; environment reds record as staged-unexecuted with commands plus condition plus unblock, never as product verdicts |
| T-10-05 | Denial of service | built-binary state via surprise rebuilds | medium | mitigate | Staleness ruling before any build; cite 08/09 proofs by pointer; a rebuild is a scheduled task with attributed timing or it does not happen |
| T-10-06 | Repudiation | green-without-logs drill claims | high | mitigate | Every green carries its row label plus observed counts plus date; staged cells carry exact errors plus unblocks |
| T-10-SC | Tampering | npm/pip/cargo installs | low | accept | No package is installed by this plan; sidecar drills ride the already-installed tree toolchain |
</threat_model>

<verification>
Display plus shell plus staleness probes, GEN delta rows, VER-01 chain rows, TEL/EXT plus collector rows, sender suite, GUI-01 automated rows, and the commit gate green.
</verification>

<success_criteria>
Live drills run green where runnable with logs cited, everything unrunnable is staged with exact commands plus blocking condition plus operator unblock, no environment red is recorded as a product red, and no rebuild happens without a staleness delta.
</success_criteria>

<artifacts_this_phase>
This plan produces: probe outcomes with staleness ruling; GEN-01/GEN-03 delta re-proofs; VER-01 fleet evidence; TEL-01/TEL-02/EXT-01/TEL-03/GUI-01 drill records.
</artifacts_this_phase>

<output>
Create `.planning/phases/10-sign-off-closeout/10-02-SUMMARY.md` when done
</output>
