---
phase: 10-sign-off-closeout
plan: 03
type: execute
wave: 3
depends_on: ["10-02"]
files_modified: [.planning/phases/10-sign-off-closeout/10-03-SUMMARY.md]
autonomous: true
requirements: [MIG-01, MIG-02, GUI-01, GUI-03, GUI-04, GEN-01, GEN-02, GEN-03, GEN-05, EXT-01, TEL-01, TEL-02, TEL-03, VER-01, DOC-01]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "verify-platform.sh --quick plus every cited --only row is re-run green on the final tree"
    - "No sibling driver exists and no new check lacks its --self-test twin"
    - "The 15-box sign-off record assembles record-close, live-drill, and staged-UAT outcomes with pointers to the 10-01/10-02 evidence"
    - "Deferred human signatures are recorded as the explicit carry list for verify-work, never backdated"
  artifacts:
    - path: ".planning/phases/10-sign-off-closeout/10-03-SUMMARY.md"
      provides: "Gates-green sweep record plus assembled 15-box sign-off"
      contains: "VERIFIED"
  key_links:
    - from: ".planning/phases/10-sign-off-closeout/10-03-SUMMARY.md"
      to: ".planning/phases/10-sign-off-closeout/10-01-SUMMARY.md"
      via: "record-close boxes and reconciliation restated by pointer, not recopied"
      pattern: "10-01"
    - from: ".planning/phases/10-sign-off-closeout/10-03-SUMMARY.md"
      to: ".planning/phases/10-sign-off-closeout/10-02-SUMMARY.md"
      via: "live-drill outcomes rolled up green-or-staged with row labels"
      pattern: "10-02"
    - from: ".planning/phases/10-sign-off-closeout/10-03-SUMMARY.md"
      to: ".planning/phases/10-sign-off-closeout/10-UAT.md"
      via: "deferred signatures listed as the carry list with coverage ids"
      pattern: "coverage_id"
  prohibitions:
    - "No new gates invented in the sweep; genuinely new assertions would be registry rows with self-test twins, and none are assumed"
    - "No silent renumbering of the count; the reconciliation stays a three-pointer explanation"
    - "No executor-filled signatures; the carry list names the operator action per sheet"
  assumptions:
    - "Plans 10-01 and 10-02 executed in order, so their SUMMARYs plus 10-UAT.md exist when this plan reads them"
    - "Staged drills from 10-02 need no re-attempt here; the sweep re-runs static rows and records drill states as reported"
---

<objective>
Sweep every cited gate green on the final tree and assemble the 15-box formal sign-off record with the deferred-signature carry list.

Purpose: Turn the per-plan evidence into one closable record: static gates re-proven at closeout, registry integrity confirmed, each of the 15 requirements dispositioned with pointers, and the human remainder explicitly routed.
Output: Gates-green sweep record, 15-box sign-off assembly, and the verify-work carry list.
</objective>

<execution_context>
<purpose>
Execute a phase prompt (PLAN.md) and create the outcome summary (SUMMARY.md).
</purpose>

<required_reading>
Read STATE.md before any operation to load project context.
Read config.json for planning behavior settings.
</required_reading>
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-sign-off-closeout/10-CONTEXT.md
@.planning/phases/10-sign-off-closeout/10-RESEARCH.md
@.planning/phases/10-sign-off-closeout/10-01-SUMMARY.md
@.planning/phases/10-sign-off-closeout/10-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Gates-green sweep plus registry integrity</name>
  <files>.planning/phases/10-sign-off-closeout/10-03-SUMMARY.md</files>
  <read_first>scripts/verify-platform.sh registry append rule plus --only contract plus empty-set guard, 10-01-SUMMARY.md cited-row inventory, 10-02-SUMMARY.md drill-row inventory, 10-UAT.md automatable-half gate list</read_first>
  <reversibility rating="reversible">Re-run record plus prose assembly; a missed row is added by amendment, no shipped surface changes.</reversibility>
  <action>Re-run scripts/verify-platform.sh --quick plus every --only row cited by plans 10-01 and 10-02 on the final tree (record-close delta rows, automatable-half rows, drill rows, self-test twins) and record each label with its observed outcome and date in the SUMMARY sweep table. Then confirm registry integrity: the working tree holds no new top-level verify driver beside the single registry, every row the phase relies on carries its self-test twin, and the residual-brand scan ran over staged files so no unstaged new file hid from it. If any re-run reds for a product reason, fix within the three-attempt standing budget and re-run; if a red traces to environment absence already staged in 10-02, keep the staged routing and say so by pointer.</action>
  <acceptance_criteria>Sweep table lists --quick plus every cited --only row with outcome plus date; single-registry confirmation recorded with the self-test-twin check; scan ran over staged files; product reds fixed or staged-by-pointer with the attempt count stated</acceptance_criteria>
  <verify>
    <automated>scripts/verify-platform.sh --quick && git status --porcelain -- scripts/ | test "$(git status --porcelain -- scripts/ | grep -c '^??')" = "0" && scripts/verify-platform.sh --only scan-brand-residue-self-test && scripts/verify-platform.sh --only gui04-registry-shape-self-test && scripts/verify-platform.sh --only telemetry-self-test && scripts/verify-platform.sh --only extension-pins-self-test && scripts/verify-platform.sh --only verify-downstream-fixtures-self-test && scripts/verify-platform.sh --only verify-rebranding-docs-self-test && scripts/verify-platform.sh --only icon-ihdr-self-test && scripts/verify-platform.sh --only installer-schema-self-test</automated>
    <fails_when>--quick exits nonzero, or any untracked file sits under scripts/, or any self-test twin exits nonzero or fails to go red naming its planted drift</fails_when>
  </verify>
  <done>Sweep green on the final tree with registry integrity confirmed</done>
</task>

<task type="auto">
  <name>Fifteen-box sign-off assembly plus verify-work carry list</name>
  <files>.planning/phases/10-sign-off-closeout/10-03-SUMMARY.md</files>
  <read_first>10-01-SUMMARY.md record-close boxes plus reconciliation paragraph, 10-02-SUMMARY.md drill outcomes green-or-staged, 10-UAT.md five staged sheets with coverage ids, 08-VERIFICATION.md plus 09-VERIFICATION.md staged-not-gap routing language</read_first>
  <action>Assemble one sign-off box per requirement for all 15 IDs (MIG-01, MIG-02, GUI-01, GUI-03, GUI-04, GEN-01, GEN-02, GEN-03, GEN-05, EXT-01, TEL-01, TEL-02, TEL-03, VER-01, DOC-01), each box stating its disposition (record-close, live-green, live-staged, or human-staged) with pointers to the 10-01/10-02 evidence plus the sweep-table row outcomes, restating the count reconciliation by pointer rather than renumbering. Then write the verify-work carry list: the five deferred human signatures with their coverage ids and runbook locations, plus any 10-02 staged drills with their unblock commands, in the plan-sanctioned staged-not-gap language so the verifier reads them as routed work rather than gaps.</action>
  <acceptance_criteria>Fifteen boxes each with disposition plus evidence pointers plus sweep outcome; reconciliation restated by pointer; carry list names every deferred signature with coverage id plus runbook location plus every staged drill with unblock</acceptance_criteria>
  <verify>
    <automated>test "$(grep -cE 'MIG-0[12]|GUI-0[134]|GEN-0[1235]|EXT-01|TEL-0[123]|VER-01|DOC-01' .planning/phases/10-sign-off-closeout/10-03-SUMMARY.md)" -ge "15" && grep -q 'coverage_id' .planning/phases/10-sign-off-closeout/10-03-SUMMARY.md && grep -qiE 'staged' .planning/phases/10-sign-off-closeout/10-03-SUMMARY.md && scripts/verify-platform.sh --quick</automated>
    <fails_when>Any of the 15 IDs is absent from the SUMMARY, or the carry list lacks coverage ids or staged routing, or --quick reports red at closeout</fails_when>
  </verify>
  <done>Fifteen requirements dispositioned with the human remainder explicitly routed</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sweep→record | Final-tree gate outcomes cross into the closable sign-off record |
| record→verifier | The assembled boxes plus carry list cross into verify-work |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-07 | Tampering | sweep completeness | medium | mitigate | Sweep table enumerates --quick plus every row cited by 10-01/10-02 with outcome plus date; no box claims a row the sweep did not re-run |
| T-10-08 | Tampering | registry integrity at closeout | medium | mitigate | Single-registry confirmation plus self-test-twin sampling plus staged-files scan discipline; any new check would be a registry row with a twin |
| T-10-09 | Repudiation | carry-list completeness | high | mitigate | Every deferred signature appears with coverage id plus runbook location; every staged drill appears with its unblock; staged-not-gap language keeps routed work distinct from gaps |
| T-10-SC | Tampering | npm/pip/cargo installs | low | accept | No package is installed by this plan |
</threat_model>

<verification>
Full sweep table with outcomes plus dates, registry-integrity checks, 15-ID presence plus carry-list shape checks, and the commit gate green.
</verification>

<success_criteria>
The final tree is gates-green, the registry stands alone with twinned checks, all 15 requirements carry dispositioned boxes with evidence pointers, and the deferred human work is an explicit carry list for verify-work.
</success_criteria>

<artifacts_this_phase>
This plan produces: the gates-green sweep table; the 15-box sign-off assembly; the verify-work carry list.
</artifacts_this_phase>

<output>
Create `.planning/phases/10-sign-off-closeout/10-03-SUMMARY.md` when done
</output>
