---
phase: 14.1-in-theia-web-tabs-gui-02
plan: 03
type: execute
wave: 3
depends_on: ["14.1-02"]
files_modified:
  - scripts/verify-web-tab-bridge.mjs
  - scripts/verify-web-tab-live.mjs
  - scripts/lib/firefox-bidi.mjs
  - scripts/verify-platform.sh
  - .planning/GUI-DEFECTS.md
autonomous: false
requirements: [GUI-02]

estimate:
  tokens: 60000
  raw_tokens: 120000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "A --quick gate (gui02-web-tab-bridge) derives the webTab* kind set from PowerBrowserAPI.sys.mjs and from web-tab.ts and compares them set-equal both ways, derives the three wire event names across GroupActorChild.sys.mjs, group-actor-client.ts, browser-window-command.ts and web-tab.ts and compares them set-equal, asserts the open handler's priority literal exceeds the stock http handler's 500, asserts the five load-bearing overlay attributes and the embedder-primary wall are present, and asserts no chrome-bar source reaches the stock-window command or a bare popup while browser-window-command.ts still does; every one of those goes red on a planted fault naming the drift (GUI-02, SC5)"
    - "The full-tier live check (gui02-web-tab-live) drives the built shell: New Tab adds one main-area web tab and a typed address renders a served page as an overlay context aligned to the placeholder within 1 CSS px on all four edges, with zero window.open calls (GUI-02, SC1)"
    - "The web tab survives Coding → Browsing → Organising → Browsing → Coding with second visits, stays attached, its overlay realigns to the placeholder after every hop where it is current and reports hidden while Organising's slot or another tab is current (GUI-02, SC2)"
    - "After navigation the pill value equals the served URL, uriOf(widget).toString(true) equals the overlay's live URL, a second typed address navigates the SAME overlay context (context id unchanged, URL changed) and Back becomes enabled (GUI-02, SC3)"
    - "A row for the served URL is readable through ChromeBarSuggestionService.searchByPrefix after the navigation and is gone after the tab is closed; closing the tab also removes the overlay context (GUI-02, SC4)"
    - "Four registry rows exist in scripts/verify-platform.sh: gui02-web-tab-bridge and its -self-test in the --quick array, gui02-web-tab-live and its -self-test in the full-tier array; no sibling driver was created (GUI-02, SC5)"
    - "scripts/lib/firefox-bidi.mjs exposes evaluateIn(context, expression) beside evaluate, and its own --self-test covers it"
    - "Firefox internals stay behind PowerBrowserAPI.sys.mjs (internals-boundary and internals-catalogue green) and Theia core is untouched (diff-theia-core green) at phase end (GUI-02, SC5)"
    - statement: "Concurrency (GUI-02): a mode switch during an in-flight navigation leaves the overlay loading and tracking; a geometry burst during a panel slide is coalesced per frame; a chrome-side restart mid-session drops orphan overlays via didDestroy and any later request for an unknown tab puts the widget in the lost-view state rather than a blank frame"
      verification: backstop
    - statement: "E6 mode switch (loading): after each hop Browsing→Coding→Organising→Browsing→Coding the overlay rect equals the placeholder rect within 1 CSS px, and is hidden while another tab or the organising slot is current"
      verification: backstop
    - statement: "E6 panel slide (loading): during the ≤200ms side-panel slide the page moves with the placeholder with no misalignment persisting beyond one frame and no white flash"
      verification: backstop
    - statement: "E6 occlusion (error): opening the suggestion dropdown, the command palette, a dialog, or a menu hides the overlay within one frame and the layer is fully visible and hittable; closing restores the page within one frame"
      verification: backstop
    - statement: "E5 overflow: many web tabs overflow the strip with Theia stock scrolling; each hidden tab's overlay is hidden and only the current tab paints"
      verification: backstop
    - statement: "Activation: clicking New Tab adds one main-area tab and a committed address renders a page inside that tab's bounds with no OS window opened — confirmed by the live check AND by Chris in his own window per GUI-DEFECTS.md"
      verification: backstop
  artifacts:
    - path: "scripts/verify-web-tab-bridge.mjs"
      provides: "--quick derive-and-compare bridge gate with --self-test (eight plants)"
      min_lines: 200
    - path: "scripts/verify-web-tab-live.mjs"
      provides: "full-tier BiDi behavioural check v2: New Tab, typed commit, geometry alignment via evaluateIn, mode walk, pill/uriOf/Back, store row present/absent, close; --self-test with six plants"
      contains: "evaluateIn"
    - path: "scripts/lib/firefox-bidi.mjs"
      provides: "evaluateIn(context, expression) callback member and its self-test case"
      contains: "evaluateIn"
    - path: "scripts/verify-platform.sh"
      provides: "four gui02 registry rows"
      contains: "gui02-web-tab-live"
    - path: ".planning/GUI-DEFECTS.md"
      provides: "items 1 and 3 moved to IN TREE with the plan reference; item 4 note that rows now flow from overlays"
      contains: "14.1-03"
  key_links:
    - from: "scripts/verify-platform.sh"
      to: "scripts/verify-web-tab-bridge.mjs"
      via: "registry row gui02-web-tab-bridge|node $REPO_ROOT/scripts/verify-web-tab-bridge.mjs"
      pattern: "gui02-web-tab-bridge\\|node"
    - from: "scripts/verify-platform.sh"
      to: "scripts/verify-web-tab-live.mjs"
      via: "registry row gui02-web-tab-live|node $REPO_ROOT/scripts/verify-web-tab-live.mjs in the full-tier array"
      pattern: "gui02-web-tab-live\\|node"
    - from: "scripts/verify-web-tab-live.mjs"
      to: "scripts/lib/firefox-bidi.mjs"
      via: "withFirefoxPage('', ({ evaluate, evaluateIn, waitFor, topLevelContexts }) => …)"
      pattern: "evaluateIn"
    - from: "scripts/verify-web-tab-bridge.mjs"
      to: "powerbrowser/shell/PowerBrowserAPI.sys.mjs and theia/extensions/tab-uris/src/browser/web-tab.ts"
      via: "regex derivation of case \"webTab…\" labels and kind: 'webTab…' literals compared as sets"
      pattern: "webTab"
---

<objective>
Make success criterion 5 true and prove criteria 1-4 behaviourally: a `--quick` derive-and-compare gate over the bridge contract, a full-tier WebDriver BiDi check that drives the built shell through "+", a typed address, the five-hop mode walk, the pill, the store row and close — each with a `--self-test` whose planted faults go red naming their drift — registered as four rows in the single driver. Then the phase ends where `GUI-DEFECTS.md` says it must: in Chris's own window.

Purpose: every earlier phase that shipped green while the window was broken did so because its gates read text. This plan adds the behavioural half the static gates structurally cannot be, and closes on a human confirmation that no gate can substitute for.

Output: `scripts/verify-web-tab-bridge.mjs`, `scripts/verify-web-tab-live.mjs` v2, `evaluateIn` in `scripts/lib/firefox-bidi.mjs`, four rows in `scripts/verify-platform.sh`, `GUI-DEFECTS.md` status updates, and Chris's confirmation.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14.1-in-theia-web-tabs-gui-02/14.1-RESEARCH.md
@.planning/phases/14.1-in-theia-web-tabs-gui-02/14.1-PATTERNS.md
@.planning/phases/14.1-in-theia-web-tabs-gui-02/14.1-UI-SPEC.md
@.planning/phases/14.1-in-theia-web-tabs-gui-02/14.1-01-SUMMARY.md
@.planning/phases/14.1-in-theia-web-tabs-gui-02/14.1-02-SUMMARY.md
@.planning/GUI-DEFECTS.md
@CLAUDE.md
</context>

<binding_constraints>
- One driver, one registry: new checks are rows appended to `scripts/verify-platform.sh` (`--quick` rows in the `local -a CHECKS=(` array after `gui06-chrome-bar-placement-self-test`; full-tier rows in the `CHECKS+=(` block under `if [ "$QUICK" -eq 0 ]` after `gui07-mode-switch-tabs-live-self-test`), each preceded by a `# NEW (14.1-03):` comment stating why it is honestly `--quick` (or why it is not). Never a sibling driver.
- Derive from the tree and compare: a zero-size derivation is a named FAIL (broken instrument), never a skip; set equality in both directions; each self-test plant must first assert its mutation landed, then require the red to name the drifted token. Never assert on the absence of a log line this phase emits.
- The live check launches the built binary through `withFirefoxPage('')` (empty URL; a URL argument opens a stock window beside the shell) with a fresh `mkdtemp` profile — no startup-cache purge needed — but it serves the LAST Theia app build: rebuild before trusting it. Plants are applied in the page realm, one fresh session per plant.
- Node built-ins plus `scripts/lib/firefox-bidi.mjs` only (D-69); no package import in any script.
- Chris's window is the phase gate (`GUI-DEFECTS.md`): the final task is a `checkpoint:human-verify` with `gate="blocking-human"` — never auto-approved, even with `auto_advance` on.
- Explicit-path commits; `.planning/STATE.md` and `.planning/state.json` are left to GSD tooling. `.planning/GUI-DEFECTS.md` is edited only to move statuses (never to mark anything CONFIRMED — only Chris does that).
</binding_constraints>

<!-- planner-discipline-allow: OPEN_BROWSER_WINDOW_COMMAND_ID -->
<!-- planner-discipline-allow: window.open( -->
<!-- planner-discipline-allow: gui02-web-tab-bridge|node -->
<!-- planner-discipline-allow: gui02-web-tab-bridge-self-test|node -->
<!-- planner-discipline-allow: gui02-web-tab-live|node -->
<!-- planner-discipline-allow: gui02-web-tab-live-self-test|node -->

<tasks>

<task type="auto">
  <name>Task 1: gui02-web-tab-bridge — the --quick derive-and-compare gate</name>
  <files>scripts/verify-web-tab-bridge.mjs, scripts/verify-platform.sh</files>
  <read_first>
    - scripts/verify-registry-shape.mjs — whole file (the skeleton to copy: header, REPO_ROOT, regex derivation, non-vacuity guard, diff(), selfTest with in-memory mutations that assert the anchor landed, main with --self-test, process.exit)
    - .planning/phases/14.1-in-theia-web-tabs-gui-02/14.1-PATTERNS.md — "scripts/verify-web-tab-bridge.mjs" section (derivations listed) and "Derive-and-compare gate with --self-test" shared pattern; "scripts/verify-platform.sh (+4 registry rows)" section
    - scripts/verify-platform.sh lines 3700-3725 (the gui06 rows and their `# NEW` comments) and 4700-4716 (the gui07 full-tier rows)
    - powerbrowser/shell/PowerBrowserAPI.sys.mjs — the eight `case "webTab…"` arms, `webTabOpen`'s attribute list, `groupSenderIsTheia`
    - powerbrowser/shell/GroupActorChild.sys.mjs, theia/extensions/modes/src/browser/group-actor-client.ts, theia/extensions/tab-uris/src/browser/browser-window-command.ts, theia/extensions/tab-uris/src/browser/web-tab.ts — the four spellings of the wire event names
    - theia/extensions/chrome-bar/src/browser/*.ts, *.tsx — the sources the no-stock-window assertion scans
    - .planning/phases/14.1-in-theia-web-tabs-gui-02/14.1-RESEARCH.md — Validation Architecture SC1/SC5 rows; Pattern 1 attribute list; Pattern 2 "Second wall"
  </read_first>
  <action>
    Create `scripts/verify-web-tab-bridge.mjs` (executable header, `--help`, `--self-test`; imports `readFileSync`, `readdirSync`, `join`, `dirname`, `fileURLToPath` only). `readSources()` reads: `powerbrowser/shell/PowerBrowserAPI.sys.mjs`, `powerbrowser/shell/GroupActorChild.sys.mjs`, `theia/extensions/modes/src/browser/group-actor-client.ts`, `theia/extensions/tab-uris/src/browser/browser-window-command.ts`, `theia/extensions/tab-uris/src/browser/web-tab.ts`, `theia/extensions/tab-uris/src/browser/tab-uris-frontend-module.ts`, and every `.ts`/`.tsx` under `theia/extensions/chrome-bar/src/browser/` (via `readdirSync`). `checkBridge(sources)` returns an array of failure strings; every derivation strips `//` line comments and `/* */` blocks before matching so header prose never counts:

    1. KINDS: derive `case "webTab…"` labels from the API file (`/case "(webTab[A-Za-z]+)"/g`) and `kind: 'webTab…'` literals from `web-tab.ts` (`/kind: '(webTab[A-Za-z]+)'/g`); either set empty → FAIL "derived ZERO … proves nothing"; `diff()` both ways → surplus/missing failures naming each kind.
    2. EVENT NAMES: derive quoted `PowerBrowser[A-Za-z]+` literals from `GroupActorChild.sys.mjs` and from `web-tab.ts`; require the two sets equal (expected content is whatever both spell — the comparison is the contract); additionally require `group-actor-client.ts` and `browser-window-command.ts` each to spell `PowerBrowserGroupRequest` and `PowerBrowserGroupResponse` exactly as the child does (derive from the child, compare per file), and the child to spell the state event the API file pushes (`sendAsyncMessage("(PowerBrowser[A-Za-z]+)"` derived from the API).
    3. PRIORITY: derive the numeric literal returned by `canHandle` in `web-tab.ts` (`/canHandle[\s\S]*?\?\s*(\d+)\s*:\s*0/`); absent → FAIL; `<= 500` → FAIL naming the number (stock `HttpOpenHandler` is 500). Also require `tab-uris-frontend-module.ts` to contain `toService(WebTabOpenHandler)`.
    4. OVERLAY CONTRACT: inside the `webTabOpen` method body of the API file (region from `webTabOpen(` to the next method at the same indentation), require each of `type`, `remote`, `remoteType`, `maychangeremoteness`, `manualactiveness` to be set, and require the `appendChild` call to appear AFTER the last of them (index comparison) — the frameloader reads them at connect.
    5. WALL: inside the `groupSenderIsTheia` function body require the tokens `embedderElement` and `primary`.
    6. NO STOCK-WINDOW ESCAPE: for every chrome-bar source, FAIL if the comment-stripped text contains `OPEN_BROWSER_WINDOW_COMMAND_ID` or matches `/window\.open\s*\(/`; POSITIVE CONTROL: FAIL if `browser-window-command.ts` no longer matches `/window\.open\s*\(/` (GUI-01's popup fallback must survive; a check that only bans would pass on "removed everywhere").
    7. CHILD PURITY: FAIL if the comment-stripped child contains `Cu.`, `Services.`, `Ci.`, or `cloneInto` (belt to the boundary scan's braces), and FAIL unless it contains `receiveMessage(`.

    `selfTest()`: baseline must be green first (else FAIL "unmodified tree is already red"); cases, each mutating one source in memory, asserting the mutation changed the text, and requiring the failure list to contain `expect`: (a) API gains `case "webTabPlanted"` → expect `webTabPlanted`; (b) `web-tab.ts` loses `kind: 'webTabClose'` (replace with `kind: 'webTabCloze'`) → expect `webTabClose`; (c) child re-spells `PowerBrowserWebTabState` as `PowerBrowserWebtabState` → expect `PowerBrowserWebTabState`; (d) `chrome-bar-widget.tsx` gains a line `window.open(url, '_blank');` → expect `window.open`; (e) `canHandle` literal becomes `100` → expect `100`; (f) `maychangeremoteness` removed from `webTabOpen` → expect `maychangeremoteness`; (g) `primary` removed from `groupSenderIsTheia` → expect `primary`; (h) `browser-window-command.ts` loses its `window.open(` (positive control) → expect `browser-window-command`. Print `  ok  <name> -> red, naming '<expect>'` per case; exit 1 on any miss.

    `scripts/verify-platform.sh`: append two rows to the `--quick` `CHECKS=(` array after `gui06-chrome-bar-placement-self-test`, preceded by a `# NEW (14.1-03):` comment (text reads only — no build, no browser, no display, no network; the self-test rides alongside because a gate nobody has seen go red is not a check): `"gui02-web-tab-bridge|node $REPO_ROOT/scripts/verify-web-tab-bridge.mjs"` and `"gui02-web-tab-bridge-self-test|node $REPO_ROOT/scripts/verify-web-tab-bridge.mjs --self-test"`. Stage the new script before running the residue scan.
  </action>
  <verify>
    <automated>node scripts/verify-web-tab-bridge.mjs && node scripts/verify-web-tab-bridge.mjs --self-test</automated>
    <fails_when>non-zero exit, a line containing "verify-web-tab-bridge: FAIL", or fewer than eight "  ok " lines in the self-test output</fails_when>
    <automated>scripts/verify-platform.sh --only gui02-web-tab-bridge && scripts/verify-platform.sh --only gui02-web-tab-bridge-self-test && scripts/verify-platform.sh --quick</automated>
    <fails_when>non-zero exit, a line containing ": FAIL", or the --quick summary not listing gui02-web-tab-bridge as PASS</fails_when>
  </verify>
  <done>The bridge gate derives kinds, event names, priority, overlay attributes, the wall, the no-escape rule and child purity from the tree, is green on the working tree, goes red on all eight plants naming the drift, and runs as two `--quick` rows in the single driver.</done>
  <acceptance_criteria>
    - `scripts/verify-web-tab-bridge.mjs` exists; `grep -c "case \\\\\"(webTab" scripts/verify-web-tab-bridge.mjs` prints at least 1 or an equivalent regex deriving `case "webTab` labels is present; `grep -c "kind: '(webTab" scripts/verify-web-tab-bridge.mjs` prints at least 1
    - `grep -c 'derived ZERO' scripts/verify-web-tab-bridge.mjs` prints at least 2 (non-vacuity guards on both kind derivations)
    - `grep -c 'browser-window-command' scripts/verify-web-tab-bridge.mjs` prints at least 2 (the read and the positive control)
    - `node scripts/verify-web-tab-bridge.mjs` exits 0 and prints a line beginning "verify-web-tab-bridge: PASS"
    - `node scripts/verify-web-tab-bridge.mjs --self-test` exits 0 and prints eight lines matching "  ok .* -> red, naming"
    - `grep -c 'gui02-web-tab-bridge|node' scripts/verify-platform.sh` prints 1 and `grep -c 'gui02-web-tab-bridge-self-test|node' scripts/verify-platform.sh` prints 1, and both rows sit inside the `local -a CHECKS=(` quick array (their line numbers are smaller than the line number of the full-tier `CHECKS+=(` block)
    - `grep -c '# NEW (14.1-03)' scripts/verify-platform.sh` prints at least 1 after this task
    - `scripts/verify-platform.sh --quick` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: gui02-web-tab-live v2 — New Tab, typed commit, geometry, mode walk, pill, store row, close; evaluateIn; full-tier rows</name>
  <precondition>objdir/dist/bin/powerbrowser exists and theia/applications/browser/lib/frontend is the build produced after plan 02's chrome-bar rewiring (rebuild first if in doubt — the harness serves the last app build)</precondition>
  <files>scripts/lib/firefox-bidi.mjs, scripts/verify-web-tab-live.mjs, scripts/verify-platform.sh, .planning/GUI-DEFECTS.md</files>
  <read_first>
    - scripts/lib/firefox-bidi.mjs lines 380-445 (evaluate/screenshot/topLevelContexts closures and the callback object) and 451-540 (the --self-test cases; the import guard below them)
    - scripts/verify-web-tab-live.mjs (v1 from plan 01)
    - scripts/verify-mode-switch-tabs-live.mjs lines 208-246 (derive — mode ids from mode-descriptors.ts, activate command id, organising widget id), 290-476 (the walk protocol: idsIn/widgetsIn, settle, per-hop assertions), 480-515 (drive), 593-676 (selfTest)
    - theia/extensions/modes/src/browser/mode-descriptors.ts lines 15-45 (the three shipped ids in source order: coding, browsing, organising) and theia/extensions/modes/src/browser/modes-commands.ts line 15 (MODES_ACTIVATE_COMMAND_ID)
    - theia/extensions/chrome-bar/src/browser/chrome-bar-commands.ts (CHROME_BAR_NEW_TAB_COMMAND_ID literal, CHROME_BAR_INPUT_CLASS literal) and chrome-bar-widget.tsx (the input's onKeyDown Enter handling and `commitText`)
    - theia/extensions/chrome-bar/src/browser/chrome-bar-suggestion-service.ts (DI identifier name `ChromeBarSuggestionService`, `searchByPrefix(prefix, limit)`)
    - .planning/phases/14.1-in-theia-web-tabs-gui-02/14.1-RESEARCH.md — "Live check: geometry assertion with no chrome access" Code Example, Validation Architecture table (SC1-SC4 rows and the planted-fault list), Open Questions 1 and 3, Assumption A4
    - .planning/GUI-DEFECTS.md — status vocabulary and items 1, 3, 4
  </read_first>
  <action>
    `scripts/lib/firefox-bidi.mjs`: beside `evaluate`, add `const evaluateIn = async (targetContext, expression) => { const result = await client.send('script.evaluate', { expression, target: { context: targetContext }, awaitPromise: true }); return result.result.result.value; };` and pass `evaluateIn` in the callback object (document it in the JSDoc above `withFirefoxPage` next to `topLevelContexts`: a caller that opened a SECOND context needs to read inside it, and `evaluate` is pinned to the launch-time context). Add self-test Case 3 after Case 2: on a bare launch, `evaluateIn(seen[0].context, 'location.href')` must return a non-empty string equal to `evaluate('location.href')`, and `evaluateIn('no-such-context', '1')` must reject (a helper that silently evaluates in the wrong context would make every geometry assertion meaningless). Update the final PASS line count.

    `scripts/verify-web-tab-live.mjs` v2 (keep v1's structure; extend `derive()`, the protocol and the plants):
    - `derive()` additionally reads: shipped mode ids from `mode-descriptors.ts` (`/^\s*id:\s*'([^']+)'/gm`, exactly three or FAIL as a broken instrument), the activate command id from `modes-commands.ts`, the organising widget id from `organising-widget.ts`, `CHROME_BAR_NEW_TAB_COMMAND_ID` and `CHROME_BAR_INPUT_CLASS` literals from `chrome-bar-commands.ts`, and the factory id / handler name from v1. Build the walk as `[ids[0], ids[1], ids[2], ids[1], ids[0]]` from the derived source order (Coding → Browsing → Organising → Browsing → Coding — Chris's sequence, derived, not hand-kept).
    - Serve `/a` and `/b` as in v1. Protocol (page realm, one `protocolExpression(derived, plant)`), with `settle()` of 600 ms between steps and every observable recorded in `report.steps`:
      1. Wrap `window.open` with a counter. Record the main-area id set before anything.
      2. NEW TAB: `commands.executeCommand(newTabCommandId)`; await; assert exactly one new main-area title whose owner's factory id is the web-tab factory id, that it is `shell.currentWidget`, that the pill input (`document.querySelector('.' + inputClass)`) has focus and an empty value, and that the tab label is `New Tab`.
      3. TYPED COMMIT: set the input's value to `servedA` through the React-compatible path (native value setter + `input` event), then dispatch a `keydown` with `key: 'Enter'` on it; wait until `widget.url === servedA` (the widget's live url) and `widget.hasPage`.
      4. Record `widgetId`, the placeholder rect in screen space (`{ sx: mozInnerScreenX + r.left, sy: mozInnerScreenY + r.top, w: r.width, h: r.height }`), `uriOf`: `__getByName(container, 'TabUriRegistry').uriOf(widget).toString(true)`, pill value, Back button `disabled` state, and `report.mainIds`.
      5. SECOND COMMIT: commit `servedB` the same way; record the pill value, `uriOf`, and Back's `disabled` after settle.
      6. MODE WALK: for each hop in the derived walk execute the activate command, settle, record `{ hop, attached: widget.isAttached, inMain: mainIds.includes(widget.id), isCurrent: shell.currentWidget === widget, placeholderRect }`; the first hop is `ids[0]` even if already active (the command early-returns; recorded as such).
      7. STORE: `__getByName(container, 'ChromeBarSuggestionService').searchByPrefix(servedOrigin, 8)` polled up to 5 s until a row with `url === servedB` appears; record the rows.
      8. CLOSE: `widget.close()`; settle; poll the same query up to 5 s until no row has `url === servedA` or `servedB`; record `mainIds` after close.
      Return the JSON report with `failures`, `notes` (plant notes start with `PLANT`), `steps`.
    - Outside the realm (the Node side, in `drive()` after the report): use `topLevelContexts()` to find the overlay context by URL after step 3 and after step 5 (poll ≤10 s): assert exactly one context carries `servedA` after step 3 and the SAME context id carries `servedB` after step 5 (navigation in place); read the overlay's screen rect with `evaluateIn(overlayCtx, '({ sx: mozInnerScreenX, sy: mozInnerScreenY, w: innerWidth, h: innerHeight, hidden: document.hidden })')` and assert `|Δ| <= 1` on all four numbers against the placeholder rect recorded in step 4/5; after each mode hop where the report says the web tab is current, re-read and assert alignment again; where the organising slot is current, assert `hidden === true` (Assumption A4 — if this reads false on a build that otherwise aligns, fall back to asserting the frontend's last published `visible` was `false` and note the fallback in the output, never silently pass); after close assert no context carries either served URL. To interleave Node-side reads with page-realm steps, split the protocol into phases evaluated separately (`evaluate` for each phase; the widget is held on `window.__pbWebTabProbe` between phases) rather than one expression.
    - `assertReport`: window.open count 0; factory id equality; pill value === servedA then servedB; `uriOf` === live overlay URL after each commit; Back disabled after the first commit and enabled after the second; every hop attached and in main; store row present after navigation and absent after close; main-area id set after close equals the set before step 2 (no residue).
    - `--self-test` (clean control first, one fresh session per plant, each reporting a `PLANT` note): P1 requests swallowed (from v1) → `context`; P2 handler falls back to a popup (from v1) → `window.open`; P3 geometry never published — replace the widget class's `publish` with a no-op before New Tab → alignment red naming `rect`; P4 state pushes ignored — remove the channel's `PowerBrowserWebTabState` listener (`window.removeEventListener` on the derived event name with the channel's handler, or stub `WebTabWidget.prototype.applyState`) → pill red naming `pill`; P5 store rows unread — stub `ChromeBarSuggestionService.searchByPrefix` to return `[]` → red naming `row`; P6 New Tab rerouted — override the new-tab command handler to call `window.open(EMPTY, '_blank')` → red naming `window.open`. Every case must report applied or fail as a broken instrument.

    `scripts/verify-platform.sh`: append two full-tier rows after `gui07-mode-switch-tabs-live-self-test` inside the `if [ "$QUICK" -eq 0 ]` block with a `# NEW (14.1-03):` comment (needs the built binary and the built app; registered as a plain `node` invocation like gui07, never through `_run_app_check_mjs`; the self-test boots seven sessions): `"gui02-web-tab-live|node $REPO_ROOT/scripts/verify-web-tab-live.mjs"` and `"gui02-web-tab-live-self-test|node $REPO_ROOT/scripts/verify-web-tab-live.mjs --self-test"`.

    `.planning/GUI-DEFECTS.md`: move item 1 and item 3 to `IN TREE` with a note "14.1-03 live check green; awaiting Chris's window", and add to item 4's note that the chrome-side writer now receives rows from in-shell overlays (still `OPEN` until Panorama is seen populated). Do not mark anything CONFIRMED.

    Rebuild the app if any frontend file changed since the last build (`nix develop .#theia --command bash -c 'cd theia && yarn --cwd extensions/tab-uris build && yarn --cwd extensions/chrome-bar build && cd applications/browser && PATH="$PWD/node_modules/.bin:$PWD/../../node_modules/.bin:$PATH" theia build --app-target=browser --mode development'`), then run the verify commands.
  </action>
  <verify>
    <automated>node scripts/lib/firefox-bidi.mjs --self-test</automated>
    <fails_when>non-zero exit or a line containing "firefox-bidi: --self-test FAIL"</fails_when>
    <automated>node scripts/verify-web-tab-live.mjs && node scripts/verify-web-tab-live.mjs --self-test</automated>
    <fails_when>non-zero exit, a line beginning "verify-web-tab-live: FAIL", a line containing "never applied its fault", or fewer than six "  ok " plant lines in the self-test output</fails_when>
    <automated>scripts/verify-platform.sh --only gui02-web-tab-live && scripts/verify-platform.sh --quick</automated>
    <fails_when>non-zero exit or a line containing ": FAIL"</fails_when>
  </verify>
  <done>The live check proves SC1-SC4 in the built shell (New Tab → typed commit → overlay aligned ≤1 CSS px → in-place navigation → five-hop mode walk → row present/absent → close), all six plants go red naming their drift, `evaluateIn` is covered by the helper's own self-test, four rows live in the single driver, and GUI-DEFECTS items 1 and 3 read IN TREE.</done>
  <acceptance_criteria>
    - `grep -c 'const evaluateIn' scripts/lib/firefox-bidi.mjs` prints 1, `grep -c 'evaluateIn,' scripts/lib/firefox-bidi.mjs` prints at least 1 (passed in the callback object), and `node scripts/lib/firefox-bidi.mjs --self-test` exits 0 printing a Case 3 PASS line mentioning evaluateIn
    - `grep -c 'evaluateIn' scripts/verify-web-tab-live.mjs` prints at least 2 and `grep -c 'mozInnerScreenX' scripts/verify-web-tab-live.mjs` prints at least 2 (placeholder side and overlay side)
    - `grep -c "mode-descriptors" scripts/verify-web-tab-live.mjs` prints at least 1 and `grep -c 'searchByPrefix' scripts/verify-web-tab-live.mjs` prints at least 2 (query and plant)
    - `node scripts/verify-web-tab-live.mjs` exits 0 and prints a line beginning "verify-web-tab-live: PASS" that names the number of hops walked and the alignment delta
    - `node scripts/verify-web-tab-live.mjs --self-test` exits 0 and prints six lines matching "  ok .* -> red, naming"
    - `grep -c 'gui02-web-tab-live|node' scripts/verify-platform.sh` prints 1 and `grep -c 'gui02-web-tab-live-self-test|node' scripts/verify-platform.sh` prints 1, both after the line containing `gui07-mode-switch-tabs-live-self-test`
    - `bash -n scripts/verify-platform.sh` exits 0 and `scripts/verify-platform.sh --quick` exits 0
    - `grep -c 'IN TREE' .planning/GUI-DEFECTS.md` is greater than it was before this task, and `grep -c '14.1-03' .planning/GUI-DEFECTS.md` prints at least 2 (items 1 and 3)
    - `git diff --name-only -- .planning/STATE.md .planning/state.json` prints nothing attributable to this task (left to tooling)
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Chris confirms GUI-02 in his own window</name>
  <what-built>In-Theia web tabs (GUI-02): "+" opens a tab inside the shell's own strip; a chrome-owned overlay renders the page aligned to the tab's body; the tab follows every mode switch; the pill shows and navigates the active tab's URL; back/forward/reload act on it; rows flow to the chrome-side tab store; a `--quick` bridge gate and a full-tier live check go red on planted faults. Everything is committed; the Theia app is rebuilt and the bridge/live checks are green on this tree.</what-built>
  <how-to-verify>
    Run from /home/chris/coding/Power-Browser (the cache purge is mandatory — a stale startup cache runs the OLD chrome script and every step below would look "not fixed"):

    1. `rm -rf objdir/tmp/profile-default/startupCache`
    2. `MOZ_PURGE_CACHES=1 ./objdir/dist/bin/powerbrowser -purgecaches`
    3. Click "+" in the chrome bar. Expected: one new tab labelled "New Tab" appears in the shell's tab strip (no separate browser window), its body is the dark surface with the single line "Type an address above to get started.", and the address pill is empty, shows "Search or enter address" and has focus.
    4. Type `example.com` and press Enter. Expected: the Example Domain page renders inside that tab's body, filling it edge to edge; the pill reads `https://example.com/`; the tab's label becomes "Example Domain" with the globe icon (a brief spinner while loading); Reload is enabled; Back and Forward are dimmed.
    5. Click the "More information..." link on the page. Expected: the page navigates inside the same tab, the pill updates to the new URL, Back becomes enabled; click Back → the Example Domain page returns.
    6. Switch modes with the toggle in this exact order: Coding → Browsing → Organising → Browsing → Coding. Expected after each switch: the web tab is still in the strip; in Coding and Browsing the page is visible and exactly fills the tab body even though the side panels changed (no offset, no gap, no page painted over the chrome bar or panels); in Organising the page is hidden behind the organising canvas (the tab remains in the strip); returning to Browsing brings the page back in place.
    7. While the web tab is active, click the pill and type `ex` so the suggestion dropdown opens. Expected: the dropdown is fully visible (the page goes dark under it while it is open) and it lists the example.com row you just visited; press Esc → the page returns.
    8. Press Ctrl+L while the page has focus (click inside the page first). Expected: the address pill gets focus with its text selected.
    9. Press Tab from the chrome bar until the tab body has focus (a 2px blue inset outline), press Enter. Expected: focus moves into the page.
    10. Open the Organising mode and look at the canvas. Expected: the example.com tab appears as a real tab (thumbnail may be blank; the title/URL must be there).
    11. Close the web tab with its close glyph. Expected: the tab disappears, the page disappears, the pill shows the address of whatever tab is now active (or empty), and no browser window was ever opened during any of these steps.
    12. Optional cross-check from the palette: "Open Browser Window" must still open a stock Firefox window (GUI-01 is unchanged).
  </how-to-verify>
  <resume-signal>Type "approved" to close GUI-DEFECTS items 1 and 3 as CONFIRMED (and note whether item 4's canvas showed the tab), or describe exactly which step failed and what you saw instead — the step number and a one-line observation is enough; the next fix is one item at a time.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Gate scripts → repo sources | Text reads only; a gate that derives an empty set must fail, not pass |
| Live check → built binary | Launches the shell headless with `--remote-allow-system-access` on a loopback BiDi port bound to 127.0.0.1 |
| Live check → local HTTP fixture | A `node:http` server on 127.0.0.1 serving two static pages for the session only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14.1-12 | Repudiation | `verify-web-tab-bridge.mjs` | medium | mitigate | Non-vacuity guards on every derivation; set equality both ways; each self-test plant asserts its mutation landed before asserting red; the positive control on `browser-window-command.ts` keeps the no-escape rule from passing on "removed everywhere" |
| T-14.1-13 | Repudiation | `verify-web-tab-live.mjs` | medium | mitigate | One fresh session per plant; a plant that never reports `PLANT` fails as a broken instrument; the geometry assertion is a numeric comparison in screen space, not a presence check; the store assertion polls a reader independent of the writer |
| T-14.1-14 | Information Disclosure | BiDi port / fixture server | low | mitigate | Both bound to 127.0.0.1 on ephemeral ports; the fixture serves fixed static bytes; the profile is `mkdtemp` and removed by the harness |
| T-14.1-15 | Denial of Service | live self-test runtime | low | accept | Seven headless sessions (~3-4 min) run only in the full tier, never `--quick` |
| T-14.1-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs in this plan |
</threat_model>

<verification>
- `scripts/verify-platform.sh --quick` green (adds `gui02-web-tab-bridge` + self-test).
- `scripts/verify-platform.sh --only gui02-web-tab-live` and `--only gui02-web-tab-live-self-test` green after the final rebuild.
- `bash scripts/check-internals-boundary.sh --catalogue`, `node scripts/verify-registry-shape.mjs`, `scripts/diff-theia-core.sh` (inside `nix develop .#theia`) green — SC5's three halves.
- Chris's checkpoint approved; only then may GUI-DEFECTS items 1 and 3 move to CONFIRMED.
</verification>

<success_criteria>
- Every one of the five ROADMAP success criteria has an automated red-on-fault instrument (bridge gate for the static half, live check for the behavioural half) registered in the single driver.
- The live check walks Chris's exact sequence and asserts alignment, persistence, pill/URI, store rows and close.
- Chris has seen "+" open a tab inside the shell, a page render in it, the tab follow every mode switch, and the URL work — in his own window.
</success_criteria>

<artifacts_this_phase_produces>
## Artifacts this phase produces (plan 03 share)

- `scripts/verify-web-tab-bridge.mjs` (NEW): `checkBridge`, `readSources`, `diff`, `selfTest` with eight plants; registry rows `gui02-web-tab-bridge`, `gui02-web-tab-bridge-self-test`
- `scripts/verify-web-tab-live.mjs` (v2): extended `derive()` (mode ids, activate command, organising id, new-tab command id, input class, factory id, handler name), phased protocol, Node-side geometry assertions through `evaluateIn`, six plants; registry rows `gui02-web-tab-live`, `gui02-web-tab-live-self-test`
- `scripts/lib/firefox-bidi.mjs`: `evaluateIn(context, expression)` callback member and self-test Case 3
- `scripts/verify-platform.sh`: four `# NEW (14.1-03)` rows
- `.planning/GUI-DEFECTS.md`: items 1 and 3 → IN TREE; item 4 note
</artifacts_this_phase_produces>

<planner_assumptions>
- E1 favicon (UI-SPEC ⚠ unresolved): no favicon transport this phase; the fixed globe icon is contracted and the checkpoint does not ask Chris to look for a favicon.
- The UI-SPEC's coverage line says 3 backstop rows; its table has 4 (E6 mode switch, E6 panel slide, E6 occlusion, E5 overflow). All four plus the activation backstop are lifted above; the mode-switch backstop is additionally covered by the live check, the other three by Chris's steps 6, 7 and (for overflow) by opening several tabs at his discretion.
- Research Assumption A4 (`document.hidden` follows `docShellIsActive`) is the hidden observable; the fallback is stated in Task 2 and is never a silent pass.
- Known limitations recorded, not verified: `target=_blank` inside a page opens the stock window (GUI-01 path); content `alert()` falls back to a window-modal prompt; no context menu, find or zoom inside a page; Theia keybindings other than Ctrl+L are not delivered while a page has focus.
</planner_assumptions>

<output>
Create `.planning/phases/14.1-in-theia-web-tabs-gui-02/14.1-03-SUMMARY.md` when done, pasting the bridge gate and live check PASS lines, the self-test "ok" lines, the four registry row labels, and Chris's checkpoint outcome verbatim.
</output>
