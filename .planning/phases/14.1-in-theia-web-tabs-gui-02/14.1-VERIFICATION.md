---
phase: 14.1-in-theia-web-tabs-gui-02
verified: 2026-09-07T15:47:49Z
status: human_needed
score: 43/56 must-haves verified
covered_files:
  - ".planning/GUI-DEFECTS.md"
  - ".planning/REQUIREMENTS.md"
  - ".planning/phases/14.1-in-theia-web-tabs-gui-02/14.1-01-PLAN.md"
  - ".planning/phases/14.1-in-theia-web-tabs-gui-02/14.1-01-SUMMARY.md"
  - ".planning/phases/14.1-in-theia-web-tabs-gui-02/14.1-02-PLAN.md"
  - ".planning/phases/14.1-in-theia-web-tabs-gui-02/14.1-02-SUMMARY.md"
  - ".planning/phases/14.1-in-theia-web-tabs-gui-02/14.1-03-PLAN.md"
  - ".planning/phases/14.1-in-theia-web-tabs-gui-02/14.1-03-SUMMARY.md"
  - "docs/URI-SCHEMES.md"
  - "inventory/brand-tokens.json"
  - "powerbrowser/INTERNAL-APIS.md"
  - "powerbrowser/shell/GroupActorChild.sys.mjs"
  - "powerbrowser/shell/PowerBrowserAPI.sys.mjs"
  - "powerbrowser/shell/powerbrowser.js"
  - "scripts/lib/firefox-bidi.mjs"
  - "scripts/verify-chrome-bar-suggestions.mjs"
  - "scripts/verify-platform.sh"
  - "scripts/verify-web-tab-bridge.mjs"
  - "scripts/verify-web-tab-live.mjs"
  - "theia/applications/browser/esbuild.mjs"
  - "theia/extensions/chrome-bar/src/browser/chrome-bar-commands.ts"
  - "theia/extensions/chrome-bar/src/browser/chrome-bar-widget.tsx"
  - "theia/extensions/tab-uris/src/browser/tab-uri-registry.ts"
  - "theia/extensions/tab-uris/src/browser/tab-uris-frontend-module.ts"
  - "theia/extensions/tab-uris/src/browser/web-tab.css"
  - "theia/extensions/tab-uris/src/browser/web-tab.ts"
covered_digest: "v1:sha256:597b2f8f5dfde5d523910c27b6d3fda10ba35febc5e9b96d78b3921bc732cf4d"
behavior_unverified: 9
overrides_applied: 0
behavior_unverified_items:
  - truth: "handleGroupMutation accepts a request only when the sender's top browsing context is embedded by the primary Theia frame; a loopback page rendered inside an overlay is rejected with reason validation"
    test: "From the Browser Toolbox, select a page's frame inside a web-tab overlay and dispatch document.dispatchEvent(new CustomEvent('PowerBrowserGroupRequest', { bubbles: true, detail: { requestId: 'x', msg: { kind: 'webTabClose', tabId: 'wt1' } } }))"
    expected: "The overlay is not closed; the reply (if any) carries reason 'validation'; the Theia frame's own requests keep working"
    why_human: "The wall's presence is asserted by the bridge gate (embedderElement + primary), but no check sends a request from inside an overlay; only a live rejection proves the predicate fires on the sender that matters"
  - truth: "Adjacency: two web tabs opened on the same URL are two distinct widgets with two overlays and share one store row keyed by that URL"
    test: "Click + and commit example.com; click + again and commit example.com again; open Organising"
    expected: "Two strip tabs, each rendering the page when current; the Organising surface / suggestion rows show one row for https://example.com/"
    why_human: "The live check opens one tab; the counter-in-options and one-row-per-URL contract is code-present but never exercised with two tabs"
  - truth: "Ordering: geometry publishes are coalesced to one requestAnimationFrame per widget, byte-identical messages are skipped, chrome applies each rect synchronously in receipt order"
    test: "With a web tab current, drag the side-panel splitter continuously for a few seconds, then stop"
    expected: "The page tracks the placeholder with no lag beyond one frame and lands exactly aligned; no stale rect is applied after the drag ends"
    why_human: "The live check reads alignment after discrete hops; a continuous burst is the only way to observe coalescing and latest-wins ordering"
  - truth: "Idempotency: a second webTabOpen for a held tabId returns already-open with no second overlay; a second webTabClose returns unknown-tab and is a no-op"
    test: "In the Browser Console (chrome context) call PowerBrowserAPI.webTabOpen twice with the same tabId and PowerBrowserAPI.webTabClose twice"
    expected: "Second open returns 'already-open' and one overlay exists; second close returns 'unknown-tab' and nothing throws"
    why_human: "The outcome words are present in the host, but no check drives the duplicate calls"
  - truth: "E1 error: a web tab whose open chrome refused or never acknowledged keeps the label New Tab (or the last URL) and its body renders the lost-view line"
    test: "With a web tab showing a page, close its overlay from the Browser Console (PowerBrowserAPI.webTabClose({ tabId })) without closing the Theia tab, then click Reload"
    expected: "The tab body shows 'Power Browser can't show this page right now. Choose Reload to try again, or close this tab.' on the dark surface; Reload re-opens the page; no internal identifier appears"
    why_human: "The lostView transition is wired (unknown-tab / nack / timeout fold into lostView) but the live check's swallowed-request plant asserts only that the check goes red, not that the widget rendered the lost-view state"
  - truth: "A committed http(s) address opens a NEW web tab through the opener when the active tab is not a web tab"
    test: "Make a non-web tab current (Explorer view or an editor), click the pill, type example.com, Enter"
    expected: "A new web tab appears in the strip with the page; the non-web tab is untouched; no OS window opens"
    why_human: "The live check commits only while a web tab is current; the checkpoint steps 3-12 never commit from a non-web tab"
  - truth: "Idempotency (chrome bar): clicking New Tab twice opens two empty tabs with distinct counters"
    test: "Click + twice on a fresh launch; also watch the strip immediately after launch"
    expected: "Exactly two 'New Tab' tabs appear only after the two clicks; no web tabs exist at startup before any click (GUI-DEFECTS item 24 reported unexplained wt1/wt2 at startup once)"
    why_human: "Not exercised by any check, and item 24 is an unreproduced observation in this exact area"
  - truth: "E3 partial: a state push while the input is focused and edited is not applied to the pill value; a push with a URL but no title updates the pill and leaves the tab label at the URL"
    test: "Open a page that redirects after a delay (or click a slow link), then click the pill and type a few characters before the navigation lands"
    expected: "The typed text stays in the pill while the page changes; after blur or Enter the canonical URL is shown; a title-less page labels its tab with the URL"
    why_human: "The guard at chrome-bar-widget.tsx:437 (activeElement === input && inputValue !== committedAddress) is present but no check pushes state while the pill is being edited"
  - truth: "E5 zero-one-many / E5 overflow: many web tabs overflow the strip with Theia stock scrolling; each hidden tab's overlay is hidden and only the current tab paints; the chip counts every main-area tab"
    test: "Open 15+ web tabs on different pages, scroll the strip, switch between a few"
    expected: "Stock Theia strip overflow; only the current tab's page is visible; the status-bar chip equals the number of main-area tabs"
    why_human: "Backstop truth; no check opens more than one web tab"
coincidental_reliance_items:
  - truth: "After navigation the pill value equals the served URL, uriOf(widget).toString(true) equals the overlay's live URL, a second typed address navigates the SAME overlay context and Back becomes enabled (GUI-02, SC3)"
    reason: fixture-only
    harden: "Remove the synthetic FocusEvent the live check dispatches on the placeholder (scripts/verify-web-tab-live.mjs:420, GUI-DEFECTS item 25). The SUMMARY states the pill assertion no longer depends on it after the strip-selection binding, but that independence is unproven while the accommodation is still in the fixture; removing it is what makes a regression to a focus-keyed binding go red"
human_verification:
  - test: "Embedder wall rejects a request sent from inside an overlay (see behavior_unverified_items #1)"
    expected: "Request refused with reason validation; overlay unaffected"
    why_human: "Presence asserted by gate; rejection never exercised"
  - test: "Two web tabs on the same URL (behavior_unverified_items #2)"
    expected: "Two tabs, two pages, one store row"
    why_human: "Never exercised with two tabs"
  - test: "Continuous splitter drag / panel slide with a web tab current (behavior_unverified_items #3 and plan-03 backstop E6 panel slide)"
    expected: "Page moves with the placeholder; no misalignment persisting beyond one frame; no white flash during the <=200ms slide"
    why_human: "Backstop; only observable frame by frame in a real window"
  - test: "Chrome-side idempotency of webTabOpen/webTabClose (behavior_unverified_items #4)"
    expected: "'already-open' then 'unknown-tab'; one overlay, no throw"
    why_human: "Never driven by a check"
  - test: "Lost-view state after the overlay is dropped, and a mode switch during an in-flight navigation (behavior_unverified_items #5; plan-03 backstop Concurrency)"
    expected: "Lost-view line rendered, Reload recovers; a navigation started just before a mode switch keeps loading and the overlay stays aligned after the hop"
    why_human: "Backstop; chrome restart / orphan drop cannot be scripted"
  - test: "Typed address while a non-web tab is current (behavior_unverified_items #6)"
    expected: "A new web tab opens with the page"
    why_human: "Branch not exercised by the live check or the checkpoint steps"
  - test: "Two New Tab clicks and a startup observation (behavior_unverified_items #7; GUI-DEFECTS item 24)"
    expected: "Exactly two empty tabs after two clicks; none before any click"
    why_human: "Item 24 is an unreproduced anomaly in this path"
  - test: "Pill typing guard against a state push (behavior_unverified_items #8)"
    expected: "Typed text not overwritten mid-edit; canonical URL after commit"
    why_human: "Guard present, never exercised"
  - test: "Occlusion by the command palette (F1), a dialog, and a context menu with a web tab current (plan-03 backstop E6 occlusion; Chris confirmed the suggestion dropdown at checkpoint step 7)"
    expected: "The page hides within one frame, the layer is fully visible and hittable, the page returns when the layer closes"
    why_human: "Only the suggestion dropdown was confirmed; palette/dialog/menu are separate blocking layers"
  - test: "Many web tabs (behavior_unverified_items #9; plan-03 backstop E5 overflow)"
    expected: "Stock overflow, only the current page paints, chip count correct"
    why_human: "No check opens more than one web tab"
---

# Phase 14.1: In-Theia Web Tabs (GUI-02) Verification Report

**Phase Goal:** The chrome-bar "+" opens a web page tab inside the Theia shell's own tab strip. The page is rendered by a chrome-owned `<xul:browser>` overlay kept aligned with a Theia placeholder widget through a two-way bridge (Theia sends geometry, visibility, close; chrome sends URL, title, loading state, back/forward availability); the tab follows every mode switch; the address pill shows and navigates the active tab's URL.
**Verified:** 2026-09-07T15:47:49Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Evidence actually run by the verifier (not taken from SUMMARY.md)

| Command | Result |
| ------- | ------ |
| `node scripts/verify-web-tab-bridge.mjs` | PASS — 8 webTab kinds, event names, priority, overlay attribute order, embedder wall, no-stock-window rule agree across 11 sources |
| `node scripts/verify-web-tab-bridge.mjs --self-test` | PASS — 8 planted faults all went red naming the drift |
| `node scripts/verify-web-tab-live.mjs` (headless, own profile) | PASS — "+" added `powerbrowser.web-tab:wt1` labelled "New Tab", pill focused; two commits navigated one overlay context (id unchanged, URL A then B); 6 alignment reads, max delta 0.48 CSS px; 5 mode hops attached, hidden while Organising current (`visible:false`, `overlay hidden true`); 15 state pushes with url/title/loading/canGoBack/canGoForward; Back disabled then enabled; store rows `[…/b]` after navigation, `[]` after close; overlay context gone after close; `window.open` calls: 0 |
| `node scripts/verify-web-tab-live.mjs --self-test` | PASS — clean control green, six plants red (context, window.open, rect, pill, row, window.open) |
| `node scripts/lib/firefox-bidi.mjs --self-test` | PASS — 4 cases incl. evaluateIn and send |
| `bash scripts/check-internals-boundary.sh` / `--catalogue` | PASS / PASS |
| `scripts/verify-platform.sh --quick` (run once, log saved) | PASS — all checks passed; rows `gui02-web-tab-bridge`, `gui02-web-tab-bridge-self-test`, `internals-boundary`, `internals-catalogue`, `gui04-registry-shape`, `gui06-chrome-bar-suggestions(-self-test)`, `tab-uris-typecheck` all PASS |
| `node scripts/verify-gui01-command.mjs` | PASS — `powerbrowser.open-browser-window` registered and palette-reachable |
| `nix develop .#theia -c bash scripts/diff-theia-core.sh` | PASS — zero changes inside `@theia/*` (fails outside the dev shell only because `yarn` is absent there, as CLAUDE.md documents) |
| `gsd query verify.artifacts` × 3 plans | 8/8, 5/5, 5/5 passed |
| `gsd query verify.key-links` × 3 plans | 5/5, 4/4, 4/4 verified |
| `git log` for cited hashes | 1203209, cd1410e, 1416d3c, af5de26, 9e8f773, a3917c9, b76358f, 6ef14cf, e5f05e3 all exist; every phase file is committed |

Process hygiene: `pgrep -x powerbrowser` before and after every launch returned only Chris's pid 743034; no verifier-launched process was left running.

## Goal Achievement

### Observable Truths — ROADMAP Success Criteria (the contract)

| # | Truth | Status | Evidence |
| --- | ----- | ------ | -------- |
| SC1 | "+" adds a tab to the main-area strip and a page renders inside it; no OS window for "+", typed addresses, or suggestion activation | ✓ VERIFIED | Live check: one main-area widget from factory `powerbrowser.web-tab`, overlay context on the served URL, `window.open` calls 0; bridge gate bans `OPEN_BROWSER_WINDOW_COMMAND_ID`/`window.open(` in every chrome-bar source with a positive control; suggestion rows route through `row.url` → `navigateWebTab` (chrome-bar-widget.tsx:318, :275); Chris's approved checkpoint steps 3–5, 11 |
| SC2 | Tab survives every mode switch incl. second visits; overlay tracks placeholder on resize, tab switch, panel slide, hide, close | ✓ VERIFIED | Live check five-hop walk coding→browsing→organising→browsing→coding: attached/in-main after every hop, realigned (0.00–0.48 px) where current, `visible:false` + overlay hidden while Organising's slot is current, overlay context removed on close; Chris step 6. Panel-slide frame behaviour is a backstop routed to human (below) |
| SC3 | Every tab has a URI via `TabUriRegistry` (web tabs = page URL); pill shows the active address, navigates the active web tab or opens one; navigations report URL/title/loading/back-forward to pill and nav buttons | ✓ VERIFIED | `tab-uri-registry.ts:166-173` `factoryId === WEB_TAB_FACTORY_ID → new URI(widget.url)`; live: pill == uriOf == overlay URL after each commit, 15 pushes carrying all five fields, Back disabled→enabled; binding on `mainPanel.onDidChangeCurrent` (widget.tsx:816) + `onDidChangeState` (:456); registry-shape gate PASS; Chris steps 4–5 (item 21/22 fixes in 1203209). The "opens one when active tab is not a web tab" branch is code-present but unexercised → human item |
| SC4 | Each web tab writes its uri/url/title row on open, navigate, close | ✓ VERIFIED | `writeTabRow({ uri: spec…` ×2 and `removeTabRow` in the progress listener / `webTabClose`; live: rows `["http://127.0.0.1:44595/b"]` after navigation, `[]` after close (searchByPrefix reader independent of the writer) |
| SC5 | Internals behind `PowerBrowserAPI.sys.mjs`, Theia core untouched, `--quick` gate + live check go red on planted faults | ✓ VERIFIED | internals-boundary + catalogue PASS (6 new catalogue rows incl. `createNullPrincipal`); `GroupActorChild.sys.mjs` has zero `Services./Cc[/Ci./ChromeUtils` tokens; diff-theia-core PASS; bridge self-test 8/8 red; live self-test 6/6 red; four registry rows in the single driver, no `scripts/verify-phase-*` sibling. Caveat: item 25 (synthetic focus still in the live fixture) narrows what the pill plant can catch — see coincidental_reliance_items |

### Observable Truths — Plan 01 (overlay bridge, chrome host, widget)

| # | Truth | Status | Evidence |
| --- | ----- | ------ | -------- |
| 1 | Open handler → one main-area tab + chrome overlay at the placeholder rect; `window.open` never called | ✓ VERIFIED | Live check (restates SC1); `web-tab.ts:515-519` getOrCreateWidget → addWidget main → activateWidget; no `window.open` in web-tab.ts |
| 2 | Overlay attrs (type/remote/remoteType/maychangeremoteness/manualactiveness) set before append, permanentKey, `gBrowser.tabs` push, `fixupAndLoadURIString` + null principal, non-http(s) refused | ✓ VERIFIED | Bridge gate asserts all five set and `appendChild` after the last (API:2142-2184); `permanentKey` ×3, `gBrowser.tabs` ×4, `fixupAndLoadURIString` ×4, `createNullPrincipal` ×1, `refused-scheme` ×4 |
| 3 | Pushes carry url/title/loading/canGoBack/canGoForward as primitives via `GroupActorChild.receiveMessage` with zero privileged reach | ✓ VERIFIED | Live: 15 pushes, all five primitive fields; child purity gate; `wrappedJSObject` JSON re-dispatch (item 17 fix) |
| 4 | Row written on top-level non-same-document http(s) location change and title change, removed on close; empty page never writes a row | ✓ VERIFIED | `LOCATION_CHANGE_SAME_DOCUMENT` guard, `pagetitlechanged` ×2, `writeTabRow({ uri:` ×2, `removeTabRow` ×5; live rows present/absent; the empty "+" tab produced no row |
| 5 | visible=false → visibility hidden + `docShellIsActive=false`; visible=true restores | ✓ VERIFIED | API:2322-2324 `visibility: visible ? "visible" : "hidden"`, `docShellIsActive = !!visible`; API:2185 initial false; live: `overlay hidden true` in Organising, visible after the next hop |
| 6 | Embedder-is-primary wall rejects a loopback page inside an overlay | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | Wall present at API:141-149 (`embedderElement…getAttribute("primary") === "true"`), gate plant red; rejection never exercised — human item 1 |
| 7 | Ctrl/Cmd+L inside an overlay page focuses the Theia frame and pushes focusAddress | ✓ VERIFIED | `powerbrowser.js` key `powerbrowser-focus-address-key` → `webTabFocusAddress`; `onDidRequestFocusAddress` → `focusAddressPill()` (widget.tsx:823); Chris checkpoint step 8 approved |
| 8 | Adjacency: two tabs on one URL = two widgets/overlays, one row | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | Counter in widget options, row keyed by URL — never exercised with two tabs — human item 2 |
| 9 | Empty: label "New Tab", visible=false, no row; URL-without-title labels with the URL | ✓ VERIFIED | `web-tab.ts:330-331` label = `pageTitle || (hasPage ? url : 'New Tab')`; visible term includes `hasPage` (:409); live: "+" tab labelled "New Tab", no row; Chris step 3 |
| 10 | Ordering: one rAF per widget, latest wins, identical skipped, chrome applies synchronously in order | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | `requestAnimationFrame` ×1, last-sent comparison present; coalescing not observed under a burst — human item 3 |
| 11 | Idempotency: second open → already-open, second close → unknown-tab, same URL twice → same overlay, one row | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | Outcome words present (`already-open` ×4, `unknown-tab` ×10); live shows A→B on one context but the duplicate-call paths are unexercised — human item 4 |
| 12 | E1 loading: spinning `codicon-loading` (static under reduced motion), label = URL until title, reverts to globe | ✓ VERIFIED | `web-tab.ts:471-472` matchMedia reduced-motion branch; `codicon-globe` ×2; Chris step 4 (spinner then globe) |
| 13 | E1 long-text: stock ellipsis, tooltip, label never blank | ✓ VERIFIED | Label fallback chain at :330 cannot yield empty; `title.caption` set at :331; strip is Theia stock |
| 14 | E1 error: refused/unacknowledged open keeps label, body renders lost-view line, no internal id | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | `LOST_VIEW_COPY` literal (:47), `lostView` ×6; the transition is never observed rendering — human item 5 |
| 15 | E2 empty: contracted line on `--color-bg`, overlay not shown | ✓ VERIFIED | `EMPTY_STATE_COPY` (:46), css `color-bg` ×2; Chris step 3 |
| 16 | E2 loading: no spinner/skeleton in the body | ✓ VERIFIED | Body's only child is the text node (web-tab.ts:281 region); no spinner class in css |
| 17 | E2 error: stock Gecko error page inside overlay / lost-view line; never blank white | ✓ VERIFIED | Overlay is a real `<xul:browser>` (stock error pages are Gecko's); body background is `--color-bg` |
| 18 | E2 populated: overlay within 1 CSS px on every edge | ✓ VERIFIED | Live: max delta 0.48 px over 6 reads in screen space via `evaluateIn` |
| 19 | E2 overflow: page scrolls inside overlay; placeholder never scrolls | ✓ VERIFIED | `web-tab.css:29 overflow: hidden` on the placeholder |
| 20 | E2 long-text: lines wrap in a 480px block | ✓ VERIFIED | `480px` ×2 in web-tab.css |

### Observable Truths — Plan 02 (chrome bar and registry)

| # | Truth | Status | Evidence |
| --- | ----- | ------ | -------- |
| 1 | New Tab adds one empty tab, activates it, focuses the pill; no OS window | ✓ VERIFIED | `chrome-bar-commands.ts` New Tab: `getOpener(new URI(EMPTY_PAGE_URL))` → `handler.open` → `focusAddressPill()`; live: "+" → `wt1`, pill focused true; Chris step 3 |
| 2 | Committed http(s) navigates the active web tab; non-web active → new tab; suggestion via `row.url`; no stock-window/popup reach | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | Three of four clauses proven (live commit in place; `row.url` → `navigateWebTab` :318/:275; gate bans + gui06 gate PASS); the non-web-active branch is unexercised — human item 6 |
| 3 | `uriOf(webTab)` = live page URL; shape unchanged; other tabs keep their schemes | ✓ VERIFIED | registry.ts:166-173; live uriOf == overlay URL; `gui04-registry-shape` PASS |
| 4 | Pill mirrors the active tab on widget change and push; never overwrites a focused edit | ✓ VERIFIED | `addressOf` (web tab: `hasPage ? url : ''`; else registry URI; else `NavigatableWidget.getUri`; else ''); `mainPanel.onDidChangeCurrent` seed + subscribe (:816-817); live pill == canonical; Chris steps 4–5 (items 21/22). Guard clause (:437) is routed to human item 8 |
| 5 | Back/Forward from last push, Reload for page or lost-view; all dimmed (not removed) on non-web tabs; act on the active web tab only | ✓ VERIFIED | `isEnabled` from `canGoBack/canGoForward/canReload` (commands.ts); live Back disabled→enabled; Chris step 4 (Back/Forward dimmed, Reload enabled) and step 11 (non-web active) |
| 6 | GUI-01 palette command unchanged; `verify-gui01-command.mjs` green | ✓ VERIFIED | Ran: PASS; bridge gate positive control requires `window.open(` in browser-window-command.ts |
| 7 | Suggestions gate retargeted; four plants red | ✓ VERIFIED | `gui06-chrome-bar-suggestions` and `-self-test` PASS in `--quick` |
| 8 | Empty: empty commit is a no-op; page-less web tab shows an empty pill with placeholder | ✓ VERIFIED | `commitText` returns on `!raw.trim()` (widget.tsx:342-345); `addressOf` returns '' without a page; "Search or enter address" ×2 |
| 9 | Idempotency: same URL twice → same overlay; New Tab twice → two tabs, distinct counters | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | Not exercised; GUI-DEFECTS item 24 (unexplained wt1/wt2 at startup) sits in this path — human item 7 |
| 10 | E3 empty: placeholder copy; never an internal URL | ✓ VERIFIED | `addressOf` total function; `EMPTY_PAGE_URL` never reaches `inputValue` (grep: only in commands.ts New Tab route) |
| 11 | E3 loading: target address shown, canonical replaces on location change, no spinner in pill | ✓ VERIFIED | Live pushes: `loading:true` with URL, then canonical; pill == canonical after load |
| 12 | E3 error: failure row on refused/unacknowledged; Enter re-commits | ✓ VERIFIED | `commitFailed` flag and carried in-bar failure row (Phase 13 contract, unchanged) |
| 13 | E3 partial: URL-only push updates pill, label = URL; push while editing not applied | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | Guard present at :437; live pushes with `title:""` labelled by URL (web-tab.ts:330) but the mid-edit push is unexercised — human item 8 |
| 14 | E3 long-text: horizontal scroll; lock glyph on https:// | ✓ VERIFIED | Carried; `secure = inputValue.trim().startsWith('https://')` (:668) |
| 15 | E4 error: disabled with tooltips, no layout shift | ✓ VERIFIED | Styled-not-removed with tooltips (widget.tsx:665 region); Chris steps 4, 11 |
| 16 | E5 populated: one current title, only the current overlay paints, chip = main-area count | ✓ VERIFIED | Live: hidden when not current; `countTabs()` → `${tabCount} tabs` (:497-501) |
| 17 | E5 zero-one-many: zero/one bound correctly; many → stock overflow, chip counts all | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | Zero/one covered by live + Chris; "many" never exercised — human item 10 |

### Observable Truths — Plan 03 (gates, live check, confirmation)

| # | Truth | Status | Evidence |
| --- | ----- | ------ | -------- |
| 1 | `gui02-web-tab-bridge` derives kinds/events/priority/attributes/wall/no-escape and goes red on each plant | ✓ VERIFIED | Ran gate and self-test (8/8 red); `canHandle` literal 1000 (web-tab.ts:506) > 500 |
| 2 | Live check: New Tab + typed address → overlay aligned ≤1 px, zero window.open | ✓ VERIFIED | Ran: PASS (0.00 px after both commits) |
| 3 | Five-hop mode walk, realigned where current, hidden in Organising | ✓ VERIFIED | Ran: 5 hops, max 0.48 px, `visible:false` + hidden in Organising |
| 4 | Pill == served URL, uriOf == overlay URL, second address navigates the SAME context, Back enabled | ✓ VERIFIED (coincidental-reliance) | Ran: context id unchanged across A→B, Back disabled→enabled. Fixture still dispatches a synthetic focus (live.mjs:420, item 25) — independence of the pill assertion from it is asserted by the SUMMARY, not proven; see coincidental_reliance_items |
| 5 | Store row readable after navigation, gone after close; overlay context removed | ✓ VERIFIED | Ran: rows `[…/b]` → `[]`; contexts after close exclude the overlay |
| 6 | Four registry rows in `verify-platform.sh`; no sibling driver | ✓ VERIFIED | Lines 3733-3734 (`--quick`, ran in the quick summary) and 4742-4743 (full tier block); `ls scripts/verify-phase-*` → none |
| 7 | `firefox-bidi.mjs` exposes `evaluateIn` with self-test | ✓ VERIFIED | `evaluateIn` ×13; self-test PASS (cases 3–4) |
| 8 | Internals boundary + catalogue green, Theia core untouched at phase end | ✓ VERIFIED | Ran all three: PASS |
| 9 | Backstop — Concurrency (in-flight nav across a mode switch, geometry burst, chrome restart → lost-view) | ⚠️ insufficient_spec | No explicit evidence; presence never qualifies for a backstop — human item 5 (and 3) |
| 10 | Backstop — E6 mode switch alignment ≤1 px, hidden when not current | ✓ VERIFIED | Live walk (explicit observed behaviour) + Chris step 6 |
| 11 | Backstop — E6 panel slide: no misalignment beyond one frame, no white flash | ⚠️ insufficient_spec | Frame-level behaviour not observed by any check — human item 3 |
| 12 | Backstop — E6 occlusion: dropdown, palette, dialog, menu hide the overlay within one frame | ⚠️ insufficient_spec | Dropdown confirmed by Chris (step 7); palette/dialog/menu unconfirmed — human item 9 |
| 13 | Backstop — E5 overflow: many tabs | ⚠️ insufficient_spec | Never exercised — human item 10 |
| 14 | Backstop — Activation confirmed by the live check AND by Chris | ✓ VERIFIED | Live PASS; checkpoint "approved" recorded in 14.1-03-SUMMARY (D4) and GUI-DEFECTS items 1, 3, 21, 22 CONFIRMED 2026-09-07 |

**Score:** 43/56 truths verified (9 present, behavior-unverified; 4 backstop abstentions)

### Prohibitions (judgment tier — non-authoritative LLM-judge verdict, human review recommended)

| Statement | Verdict | Evidence |
| --------- | ------- | -------- |
| A web tab never surfaces an internal identifier in label, tooltip, aria-label, body text, or the pill | holds (judge) | Label/caption/aria-label built only from `pageTitle`, `url`, `'New Tab'` (web-tab.ts:330-332); the two body literals are fixed product copy; `addressOf` maps no-page/no-URI to ''; `EMPTY_PAGE_URL` appears in no rendered path |
| A hidden web tab never keeps painting or running as if visible | holds (judge) | `manualactiveness` set at creation; `docShellIsActive = !!visible` on every geometry message (API:2324); live `overlay hidden true` while Organising is current |
| The chrome bar never sends typed text to a search engine or third-party host | holds (judge) | Zero matches for `duckduckgo|google.com|bing.com|search?q|/search` in chrome-bar sources; `typedAddressTargetOf` three-way mapping intact; gui06 engine-host plant red |
| The pill never displays an internal identifier | holds (judge) | Same `addressOf` derivation; command/factory ids and counters never reach `inputValue`/`committedAddress` |

None of the four is a test-tier prohibition; all four are flagged `unverified-prohibition — human review recommended` per the judgment-tier rule and folded into the human checkpoint that already exists for this phase.

### Required Artifacts

| Artifact | Expected | Status | Details |
| -------- | -------- | ------ | ------- |
| `theia/extensions/tab-uris/src/browser/web-tab.ts` | Widget, channel, open handler, factory id, geometry publisher | ✓ VERIFIED | 522 lines; all 5 exports; wired via frontend module (`toService(WebTabOpenHandler)`, factory id) and imported by chrome-bar and registry |
| `theia/extensions/tab-uris/src/browser/web-tab.css` | `@layer powerbrowser-web-tab` | ✓ VERIFIED | 59 lines; layer, 480px, color-bg, overflow hidden |
| `theia/extensions/tab-uris/src/browser/tab-uris-frontend-module.ts` | Factory + channel + handler bindings | ✓ VERIFIED | Lines 92-103 |
| `powerbrowser/shell/PowerBrowserAPI.sys.mjs` | 11 webTab* host methods, progress listener, wall | ✓ VERIFIED | All 11 methods present once each; 8 `case "webTab…"` arms |
| `powerbrowser/shell/GroupActorChild.sys.mjs` | `receiveMessage` re-dispatch | ✓ VERIFIED | Present; zero privileged tokens |
| `powerbrowser/shell/powerbrowser.js` | reserved accel+L key | ✓ VERIFIED | `powerbrowser-focus-address-key` → `webTabFocusAddress` |
| `powerbrowser/INTERNAL-APIS.md` | rows for new forbidden patterns | ✓ VERIFIED | `--catalogue` PASS |
| `scripts/verify-web-tab-live.mjs` | BiDi live check v2 + self-test | ✓ VERIFIED | 949 lines; ran both |
| `theia/extensions/tab-uris/src/browser/tab-uri-registry.ts` | uriOf web-tab branch | ✓ VERIFIED | :166-173 |
| `theia/extensions/chrome-bar/src/browser/chrome-bar-commands.ts` | current-web-tab slot, flipped predicate, nav executes, New Tab route | ✓ VERIFIED | 3 exports; `chromeBarHasNavigableTab = current !== undefined` |
| `theia/extensions/chrome-bar/src/browser/chrome-bar-widget.tsx` | navigateWebTab, pill binding, nav enablement, focusAddress | ✓ VERIFIED | All present and wired |
| `scripts/verify-chrome-bar-suggestions.mjs` | retargeted gate | ✓ VERIFIED | gui06 rows PASS |
| `docs/URI-SCHEMES.md` | `## http: / https:` section | ✓ VERIFIED | Line 231, before carve-outs at 264 |
| `scripts/verify-web-tab-bridge.mjs` | derive-and-compare gate, 8 plants | ✓ VERIFIED | 379 lines; ran |
| `scripts/lib/firefox-bidi.mjs` | `evaluateIn` | ✓ VERIFIED | ran self-test |
| `scripts/verify-platform.sh` | four gui02 rows | ✓ VERIFIED | 3733-3734, 4742-4743 |
| `.planning/GUI-DEFECTS.md` | items 1/3 moved, 14.1-03 references | ✓ VERIFIED | `14.1-03` ×7; items 1, 3, 21, 22 CONFIRMED; 23–25 OPEN |

### Key Link Verification

All 13 declared key links verified by `gsd query verify.key-links` (5/5, 4/4, 4/4). Highlights checked by hand:

| From | To | Via | Status |
| ---- | -- | --- | ------ |
| web-tab.ts | GroupActorChild.sys.mjs | `PowerBrowserGroupRequest` CustomEvent | WIRED (live check delivered open/navigate/geometry/close) |
| PowerBrowserAPI.sys.mjs | GroupActorChild.sys.mjs | `sendAsyncMessage("PowerBrowserWebTabState"` | WIRED (15 pushes received) |
| GroupActorChild.sys.mjs | web-tab.ts | `PowerBrowserWebTabState` re-dispatch via `wrappedJSObject` | WIRED |
| PowerBrowserAPI.sys.mjs | tabs.sqlite | `writeTabRow({ uri:` / `removeTabRow` | WIRED (row present then absent) |
| chrome-bar-widget.tsx | chrome-bar-commands.ts | `setCurrentWebTab(` from `mainPanel.onDidChangeCurrent` | WIRED |
| chrome-bar-widget.tsx | web-tab.ts | `@powerbrowser/tab-uris/lib/browser/web-tab` import; `navigate`, `onDidChangeState`, `onDidRequestFocusAddress` | WIRED |
| tab-uri-registry.ts | web-tab.ts | `factoryId === WEB_TAB_FACTORY_ID` | WIRED |
| chrome-bar-commands.ts | OpenerService | `getOpener(` + `handler.open` | WIRED |
| verify-platform.sh | both gui02 scripts | registry rows | WIRED (quick rows ran) |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| -------- | ------------- | ------ | ------------------ | ------ |
| chrome-bar-widget.tsx pill | `inputValue` | `addressOf(widget)` ← `WebTabWidget.url` ← chrome state push ← `nsIWebProgressListener.onLocationChange` | Yes (live: canonical served URL) | ✓ FLOWING |
| web-tab.ts strip label | `title.label` | `pageTitle`/`url` ← state push ← `pagetitlechanged` / location change | Yes (live: "Web tab B") | ✓ FLOWING |
| Nav buttons | `canGoBack/canGoForward/canReload` | last state push ← `browser.canGoBack/canGoForward` | Yes (live: Back false → true) | ✓ FLOWING |
| Suggestion rows / Organising | tabs.sqlite rows | `writeTabRow` in the progress listener | Yes (live: row for served URL) | ✓ FLOWING |
| Overlay rect | geometry message | `getBoundingClientRect` of the placeholder, rAF-coalesced | Yes (live: 0.00–0.48 px) | ✓ FLOWING |

### Behavioral Spot-Checks

See the "Evidence actually run" table above — 11 runnable checks, all PASS.

### Probe Execution

No `scripts/*/tests/probe-*.sh` probes are declared or present for this phase; the gates above are the phase's runnable checks.

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
| ----------- | ----------- | ----------- | ------ | -------- |
| GUI-02 | 14.1-01, 14.1-02, 14.1-03 | In-Theia web tabs over a chrome-owned overlay bridge; pill/nav bound; store rows; replaces the stock-window escape | ✓ SATISFIED (human items open) | SC1–SC5 verified above; REQUIREMENTS.md marks GUI-02 complete and maps it only to Phase 14.1 |

No orphaned requirements: REQUIREMENTS.md maps only GUI-02 to Phase 14.1 and every plan declares it.

### Anti-Patterns Found

Debt-marker scan (`TBD|FIXME|XXX|TODO|HACK|not yet implemented|coming soon`) over all 14 phase-modified source files: zero hits. Findings below are open follow-ups the SUMMARYs record and this verification confirms are still open.

| File | Line | Pattern | Severity | Impact |
| ---- | ---- | ------- | -------- | ------ |
| `powerbrowser/shell/powerbrowser.js` | 64 | `window.gBrowser = { tabs: [...] }` has no `getTabForBrowser` (GUI-DEFECTS item 23, confirmed: 0 matches) | ⚠️ Warning | Every overlay creation logs a `TypeError` from browser-custom-element.mjs / LinkHandlerParent / ContextMenuParent; harmless to the page, noisy, and masks real errors in the same log |
| `scripts/verify-web-tab-live.mjs` | 420 | `P.widget.node.dispatchEvent(new FocusEvent('focus'))` synthetic-focus accommodation (item 25, confirmed present) | ⚠️ Warning | Narrows SC5: a regression back to a focus-keyed pill binding may not go red; see coincidental_reliance_items |
| (unreproduced) | — | Two web tabs `wt1`/`wt2` present at startup once (item 24) | ⚠️ Warning | Unexplained tab creation on launch; folded into human item 7 |
| `theia/extensions/modes/src/browser/main-area-exemption.ts` | — | Staged but uncommitted (Phase 14's session); SC2 depends on it | ℹ️ Info | The live check ran against the working tree and passed; if that session's work is discarded, web tabs will not survive mode switches. Not a Phase 14.1 defect |
| `theia/extensions/tab-uris/src/browser/browser-window-command.ts` | — | Modified, uncommitted (other session); the bridge gate's positive control reads it | ℹ️ Info | Gate passed on the working tree; the committed version also carries `window.open(` |

### Human Verification Required

Chris's blocking checkpoint (plan 03 task 3, steps 3–12) was approved on 2026-09-07 after the 1203209 fixes; that approval covers both plans' harvested `<human-check>` items (empty-state line, spinner/globe, Back/Forward dimmed and Reload enabled, link + Back, five-hop walk, suggestion-dropdown occlusion, Ctrl+L, Tab focus ring + Enter, close, GUI-01 palette). The items below are what neither the checkpoint nor any check exercised.

1. **Embedder wall rejects an in-overlay sender** — dispatch a `PowerBrowserGroupRequest` from inside an overlay page's frame; expect refusal with reason validation and an untouched overlay.
2. **Two web tabs on one URL** — two tabs, two pages, one store row.
3. **Continuous panel slide / splitter drag** — page tracks within one frame, no white flash, lands aligned (covers the Ordering truth and backstop E6 panel slide).
4. **Chrome-side idempotency** — `webTabOpen` twice → `already-open`, one overlay; `webTabClose` twice → `unknown-tab`.
5. **Lost-view state and in-flight navigation across a mode switch** — drop an overlay from the Browser Console; expect the lost-view line and Reload recovery; start a navigation, switch mode immediately; expect it to finish loading aligned (backstop Concurrency).
6. **Typed address while a non-web tab is current** — a new web tab opens with the page.
7. **Two New Tab clicks and the startup strip** — exactly two empty tabs after two clicks; none at launch (item 24).
8. **Pill typing guard** — a state push during an edit does not overwrite the typed text.
9. **Occlusion by command palette, dialog, context menu** — overlay hides within one frame and returns on close (backstop E6 occlusion beyond the dropdown Chris confirmed).
10. **Many web tabs** — stock strip overflow, only the current page paints, chip counts all main-area tabs (backstop E5 overflow).

### Gaps Summary

No must-have failed and no artifact is missing, stubbed, or unwired: the phase goal is achieved in the codebase with runnable evidence the verifier produced itself (bridge gate + 8-plant self-test, live check + 6-plant self-test, internals boundary and catalogue, diff-theia-core, `--quick`, GUI-01 command check) plus Chris's approved checkpoint. Status is `human_needed` rather than `passed` because nine truths are present and wired but their runtime behaviour is unexercised by any check (embedder-wall rejection, adjacency, geometry coalescing, chrome-side idempotency, lost-view rendering, the non-web-active commit branch, double New Tab, the pill typing guard, many-tab overflow) and four backstop truths have no explicit evidence. Three follow-ups the SUMMARYs record are confirmed still open (GUI-DEFECTS items 23, 24, 25) and are reported as warnings, not gaps; item 25 is the one that touches SC5's strength and is recorded as a coincidental-reliance advisory on the pill assertion.

---

_Verified: 2026-09-07T15:47:49Z_
_Verifier: Claude (gsd-verifier)_
