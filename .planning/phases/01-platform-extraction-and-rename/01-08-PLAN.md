---
phase: 01-platform-extraction-and-rename
plan: 08
type: execute
wave: 8
depends_on: ["01-07"]
gap_closure: true
files_modified:
  - scripts/scan-brand-residue.mjs
  - inventory/brand-tokens.json
  - scripts/verify-platform.sh
  - .planning/WINDOWS.md
  - theia/extensions/branding/src/browser/powerbrowser-about-dialog.tsx
  - scripts/verify-branding-preflight.mjs
  - scripts/verify-branding.mjs
autonomous: true
requirements: [MIG-03, MIG-04]

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The registered residual-brand gate — the plain, un-flagged `node scripts/scan-brand-residue.mjs` that `verify-platform.sh`, `rebase-upstream.sh`, and the CI workflow all invoke — exits non-zero when an occurrence appears that no inventory row claims, in both the pre-rename and post-rename branches (D-17, D-18)"
    - "The scan's `--self-test` plants an unclaimed form on a post-rename fixture and requires the un-flagged gate path to go red; the same fixture without the plant is required to go green, so the red is caused by the plant and not by the fixture"
    - "Every `expected_count` row in `inventory/brand-tokens.json` reconciles against the tree as it stands, and each of the four rows whose number moved records in its own `reason` field which tree change moved it (D-08's provenance survives the reconciliation)"
    - "The About dialog renders the display form `Power Browser`, with the space, exactly as `inventory/brand-tokens.json`'s `brand_display_expectations` fixes every display value (D-35, Success Criterion 5)"
    - "The pre-build preflight's display-surface set is derived by walking `theia/extensions/branding/src/browser/` at check time rather than from a hand-appended path list, so it goes red both when a leaking surface is added and when the directory it derives from disappears"
    - "`verify-branding.mjs`'s About surface carries the identical display-form, identifier-leak, and stock-identity assertions its welcome sibling carries, through one shared assertion both surfaces call, with the expected literal read from the inventory rather than hand-kept in the checker"
    - "MIG-03 adjacency — the scan's two matchers disagree on purpose at the boundary: a form whose left neighbour is a letter (`resourcerer`) is claimed by no row, an upper-camel continuation (`sourcererPrivilegedJs`) is claimed, and a trailing-lowercase form (`Sourcerers`) that no row claims is reported by condition 4 as an unclaimed probe hit and fails the un-flagged gate"
    - "MIG-03 empty — three distinct empty inputs each FAIL rather than pass: an empty scanned file set, an inventory carrying zero token rows, and a post-rename run in which no frozen or coincidental row lies wholly inside the scanned scope. A clean result over an empty set is reported as unrun, never as clean"
    - "MIG-03 encoding — equality is defined twice on purpose and neither definition applies Unicode normalization: the claim matcher is case-sensitive with a token-boundary rule, and condition 4's independent probe is a `toLowerCase()` substring search with no boundary rule at all. Counting is JavaScript UTF-16 index arithmetic, so `expected_count` is a count of UTF-16 substring matches, not of grapheme clusters"
    - "MIG-03 ordering — output order is specified and stable: occurrences sort by file, then line, then index; unclaimed probes sort by file then line; and where two rows could claim the same span the claim order is longest-token-first, then by case-form rank, so a rerun over an unchanged tree produces byte-identical output"
    - "UI-SPEC E7 — the About dialog keeps Theia's stock `ad-container` layout with the mark at 48x48 carrying the `prefers-color-scheme` dual fill (D-36); the display-literal correction changes the `<h3>` text node and nothing else about that surface"
    - statement: "No display surface outside the derived set renders the identifier form. The preflight's leak scan reaches the inventory-declared variant files, the Theia application manifest, and every source file under the branding extension's browser directory; a display surface authored anywhere else is not reached by it and rests on code review"
      verification: backstop
  prohibitions:
    - statement: "MUST NOT reconcile a failing expected_count by copying the observed number without recording, per row, which tree change legitimately moved it — a blind rebaseline converts a real census into a rubber stamp and destroys exactly the provenance D-08 protects"
      category: transparency
      status: resolved
      verification: judgment
      resolution: null
      reason: null
    - statement: "MUST NOT close or downgrade a WINDOWS.md ledger item whose underlying work is not actually done; a ledger that records a deferral as fixed is worse than no ledger, because it removes the only surface that would have surfaced the deferral again"
      category: transparency
      status: resolved
      verification: judgment
      resolution: null
      reason: null
    - statement: "MUST NOT substitute an automated proxy for either perceptual human verification. A check that cannot observe the perceptual outcome reports a green nobody earned, which is strictly worse than an open ledger item"
      category: safety
      status: resolved
      verification: judgment
      resolution: null
      reason: null
    - statement: "MUST NOT make the gate pass by weakening it — removing or narrowing a residue probe, widening a claim rule, adding a scope exclusion, or deleting an inventory row — instead of reconciling the tree the gate is describing"
      category: values
      status: resolved
      verification: judgment
      resolution: null
      reason: null
  artifacts:
    - path: "scripts/scan-brand-residue.mjs"
      provides: "The residual-brand gate, with condition 4 reachable on both branches and one single-sourced exit-code decision proven red by its own self-test"
      contains: "gateFailures"
    - path: "inventory/brand-tokens.json"
      provides: "The token-classification census, reconciled against the current tree with each moved number justified in its row"
      contains: "expected_count"
    - path: "theia/extensions/branding/src/browser/powerbrowser-about-dialog.tsx"
      provides: "The About dialog, rendering the hand-written display literal"
      contains: "Power Browser"
    - path: "scripts/verify-branding-preflight.mjs"
      provides: "The cheap static branding gate, with its display-surface set derived from the tree instead of hand-appended"
      contains: "branding/src/browser"
    - path: "scripts/verify-branding.mjs"
      provides: "The runtime branding verifier, with one shared display-form assertion applied to both the welcome and about surfaces"
      contains: "assertDisplayForm"
    - path: "scripts/verify-platform.sh"
      provides: "The one driver and one registry, gaining a single new row for the scan's self-test"
      contains: "scan-brand-residue-self-test"
  key_links:
    - from: "scripts/scan-brand-residue.mjs"
      to: "scripts/verify-platform.sh"
      via: "the registry's un-flagged `scan-brand-residue` invocation must be able to fail; its exit code is single-sourced through gateFailures()"
      pattern: "scan-brand-residue"
    - from: "inventory/brand-tokens.json"
      to: "scripts/verify-branding-preflight.mjs"
      via: "brand_display_expectations.identifier_form drives the leak regex, applied over a surface set derived from the tree at check time"
      pattern: "identifier_form"
    - from: "theia/extensions/branding/src/browser/powerbrowser-about-dialog.tsx"
      to: "scripts/verify-branding.mjs"
      via: "the rendered dialog's textContent is read live over BiDi and passed to the shared display-form assertion"
      pattern: "theia-aboutDialog"
---

<objective>
Close the two must-have truths 01-VERIFICATION.md recorded as FAILED: a residual-brand gate that
cannot fail on an unclaimed occurrence in either mode, and an About dialog rendering the space-less
identifier form as display text behind a check that does not discriminate on that surface.

Purpose: both are the same failure shape, which is why they are one plan. A guard was written, wired,
and registered — and then never proven able to go red. The scan's condition 4 returns before it is
reached on the branch the tree actually sits on, and its result is gated behind a flag no registered
caller passes; the branding check's About surface simply carries fewer assertions than its welcome
sibling. In both cases the green is an artefact of the instrument, not a fact about the product. The
About-dialog defect is the identical bug class already found and fixed once in the welcome widget
during 01-07 and never generalized, so this plan fixes it where all surfaces route through rather
than at the one site the report named.

Output: a residual-brand gate whose exit code has one source and whose self-test plants a fault the
un-flagged run must reject (D-17, D-18); a reconciled `expected_count` census with every moved number
justified in its own row (D-08); an About dialog rendering `Power Browser` (D-35); a preflight whose
display-surface set is derived from the tree; and both perceptual walkthroughs carried forward as
human checks rather than replaced by an automated proxy.

Mode note (MVP): ROADMAP.md marks this phase `**Mode:** mvp`, but its `**Goal:**` line is not in
user-story form, so no user story is invented here. The phase is already executed; re-running
`/gsd mvp-phase 01` would rewrite the goal of delivered work. Surfaced, not silently skipped.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/01-platform-extraction-and-rename/01-VERIFICATION.md
@.planning/phases/01-platform-extraction-and-rename/01-REVIEW.md
@.planning/phases/01-platform-extraction-and-rename/01-UI-SPEC.md
@.planning/phases/01-platform-extraction-and-rename/01-07-SUMMARY.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Make the registered residual-brand gate able to fail, prove it red by self-test, and reconcile the census in the same commit</name>
  <files>scripts/scan-brand-residue.mjs, inventory/brand-tokens.json, scripts/verify-platform.sh, .planning/WINDOWS.md</files>

  <precondition>`inventory/brand-tokens.json`'s `scope.exclude` still lists `scripts/scan-brand-residue.mjs`. The self-test fixture spells a brand token as a literal inside that file; if the exclusion were removed, adding the literal would make the file permanently red under its own scan. Assert this before writing any literal, and halt if it is absent.</precondition>

  <reversibility rating="costly">Making `rec.failures` gate unconditionally is a two-line revert and is reversible. Rewriting four `expected_count` rows is costly: a number recorded for the wrong reason looks correct forever afterward, and the gate then enforces the wrong expectation with no way to tell from the exit code. This is why each moved number must carry its justification in the row.</reversibility>

  <read_first>
    - scripts/scan-brand-residue.mjs in full — specifically `scan()` and how `unclaimedProbes` is built from `scope.residue_probes` against the `taken` map; `reconcile()`'s post-rename early return and the four named conditions after it; `main()`'s two exit paths and where `rec.failures` is printed; and the existing `selfTest()` with its five cases and its `SELF_TEST_INVENTORY` fixture
    - inventory/brand-tokens.json — `scope.exclude` (which files the scan does not read and why), `scope.residue_probes`, and the four rows whose counts currently disagree with the tree: `/home/chris/coding/sourcerer`, `MOZ_APP_UA_NAME`, `MOZ_APP_ID`, and the plan-file provenance row
    - scripts/verify-platform.sh, the `run_own_checks()` registry — the quick-set array where `scan-brand-residue` is the first row, and the convention that every self-testable check carries a sibling self-test row
    - .planning/WINDOWS.md — the ledger table header and rows 6 through 9, so the item-6 update matches the shape rows 7, 8, and 9 already use for a resolved item
    - CLAUDE.md, the "Verification" and "Phase-specific rules that outlive their phase" sections — one driver and one registry; derive from the tree and compare; never assert on the absence of a log line
  </read_first>

  <action>
Three code changes, one census reconciliation, one registry row, one ledger update. All of it lands
as a single commit, and the reason is the ordering constraint: `node scripts/scan-brand-residue.mjs`
is registered as the first row of the commit gate, so a commit that lands the gating change without
the census would be a commit whose own gate is knowingly red. Do the work in the order below, observe
the intermediate red, and commit once at the end.

First change, in `reconcile()`. The walk over `result.unclaimedProbes` that pushes condition-4
failures currently sits after the post-rename early return, so it is only ever reached in the red
state. Move that walk above the `if (postRename)` block so it runs on both branches, and keep its
`conditions.push` description alongside it so both branches report that condition 4 was asserted.
Condition 4 is the scanner's only defense against a brand string reappearing in a form no inventory
row matches, and the post-rename branch is the branch this tree permanently sits on — a defense that
is unreachable in the only state it will ever run in is not a defense.

Second change, in `main()`. Extract the exit-code decision into one exported pure function —
`gateFailures(offenses, rec)` returning an array of human-readable reason strings — and have `main()`
return 1 if and only if that array is non-empty. Today the two `return 1` sites are independent and
the reconciliation one is guarded by `wantReconcile`, which is precisely why none of the three
registered call sites can reach it. One function, one exit source, callable from the self-test
without a subprocess.

Third change, also in `main()`: print `rec.failures` whenever they exist, not only under
`--reconcile`. Keep the verbose `rec.conditions` "asserting ..." lines behind the flag — those are
progress narration — but a gate that exits 1 while saying nothing about what failed is unusable.
Note what this widens: on the pre-rename branch the un-flagged run now also enforces conditions 1
through 3 and the ground-truth arithmetic. That is not a regression, because the pre-rename branch is
only reached when renameable offenses exist, in which case the gate already failed.

Fourth, the self-test. Add one case to `selfTest()` that proves both changes together. Build a
POST-RENAME fixture — a temp directory under the system temp root, never a path inside this repo,
containing a file whose content carries a form that no `SELF_TEST_INVENTORY` row claims but that
condition 4's case-insensitive probe still finds. The trailing-plural form of the probe token is the
intended plant: its trailing lowercase character puts it outside the claim matcher's boundary rule
while leaving the probe substring intact. Confirm that against the fixture inventory before relying
on it; if some row does claim it, choose another form that is genuinely unclaimed and say which in
the case's output. Run `scan()` over the fixture, confirm `offensesOf()` is empty so the fixture is
genuinely on the post-rename branch, then require `gateFailures()` to be NON-EMPTY and to name the
planted file and line. Pair it with a clean control: the same fixture without the plant must produce
an empty `gateFailures()`, so the red is caused by the plant rather than by the fixture. This one case
fails if either the condition-4 move or the unconditional gating is missing, which is what makes it a
fault-planting self-test rather than a green-only one.

Two traps govern where that fixture and that literal may live, and both have been hit for real in
this repo. The fixture is written to a temp directory outside the repo, so it is invisible to
`git ls-files` by construction — a fixture inside the tree would be either unstaged and therefore
unseen by the scan, or staged and therefore permanently red. The literal itself may be spelled inside
`scripts/scan-brand-residue.mjs` and nowhere else, because `scope.exclude` already exempts that file
from its own scan; the existing self-test cases already spell probe-token forms in their output
strings for the same reason. Assert the exclusion before writing the literal.

Fifth, the census. With the gating changes applied and before touching the inventory, run the plain
un-flagged scan and record its output verbatim in the SUMMARY — it must now exit 1 naming four
held-back rows. That is the red demonstration and it needs no planted fault, because the drift is
real and pre-existing. Then reconcile each of the four rows by name, never by copying the observed
number. Each row needs its `expected_count` corrected, its `expected_files` corrected where it names
a file that no longer exists, and its `reason` extended with one sentence saying which tree change
moved the number. The four, with what to confirm before writing anything:

The absolute-repo-root row expects four occurrences across the two desktop entries and observes zero,
because both entries were hand-rewritten to this repo's path in plan 01-03. Confirm that by reading
both entries, then set the count to zero. Setting it to zero is load-bearing beyond this row:
`verify-branding-preflight.mjs`'s coincidental-row check only asserts against rows that are both
coincidental and zero, so this correction activates a standing assertion that the old path never
comes back.

The two Gecko-define rows each expect two occurrences across patch 010 and patch 020 and observe one.
Confirm which patch still carries the define and which no longer does before writing; patch 020 was
rewritten during the browser-window work. Correct both the count and the `expected_files` list, and
record in `reason` that the second site is gone rather than merely uncounted — the row exists to
prove those defines stayed pinned, so it must keep naming a real site.

The plan-file provenance row expects twenty-one and observes twenty-eight. Its `expected_files` names
three per-phase verifier scripts that were deleted when the drivers were consolidated, and does not
name the consolidated driver that now carries those citations. Derive the current per-file
distribution across the scanned scope, rewrite `expected_files` to the files that actually carry the
citations, set the count to the derived total, and record in `reason` that the consolidation moved
them. Do not renumber or delete a single citation while doing this — they are provenance under D-08
and the count assertion exists to prove they all survived.

One ordering hazard specific to that last row, and it is the way this task most plausibly fails. The
consolidated driver is inside the scanned scope, so any comment you add to it that cites a plan by
its full filename changes the count again after you have just reconciled it, and the commit gate goes
red at the next task. The existing comment convention in that file cites plans by bare number — keep
to it, in every file this plan touches that lies inside the scanned scope. This plan document itself
is under the planning directory, which `scope.exclude` removes from the scan, so nothing written here
affects any count.

Sixth, the registry. Append exactly one row to `run_own_checks()`'s quick-set array, labelled
`scan-brand-residue-self-test`, invoking the scan with `--self-test`. It belongs beside the existing
`scan-brand-residue` row for the reason every other self-test row in that registry gives: a check
that has never been seen to go red is a check nobody has verified. One row in the one registry — do
not create a sibling driver.

Seventh, the ledger. WINDOWS.md item 6 records that reconciliation no longer closes post-rename and
that the plain run is the gate. Both halves of that entry are now false: the census closes and the
plain run is itself the reconciliation gate. Move the item to resolved with a resolution note saying
so, matching the column shape items 7 through 9 already use, including the resolved timestamp.
  </action>

  <verify>
    <automated>node scripts/scan-brand-residue.mjs --self-test</automated>
    <automated>node scripts/scan-brand-residue.mjs</automated>
    <automated>node scripts/scan-brand-residue.mjs --reconcile</automated>
    <automated>scripts/verify-platform.sh --only scan-brand-residue-self-test</automated>
    <automated>scripts/verify-platform.sh --quick</automated>
  </verify>

  <acceptance_criteria>
    - RED, recorded before the census is touched: with only the `reconcile()` and `main()` changes applied, `node scripts/scan-brand-residue.mjs` with no flags exits 1 and its stderr names all four drifted rows by token. The verbatim output is quoted in the SUMMARY. This demonstration is not committed — the census correction lands in the same commit.
    - RED, by planted fault: `node scripts/scan-brand-residue.mjs --self-test` prints a PASS line for the new post-rename case stating that the planted unclaimed form was rejected by the un-flagged gate path and naming the fixture file and line, and a second line stating that the same fixture without the plant produced no gate failure.
    - The new self-test case fails if the condition-4 walk is returned to its position after the post-rename early return, and fails if `main()`'s reconciliation exit is put back behind `--reconcile`. Confirm both by temporary local reversion during development; neither reversion is committed.
    - `reconcile()` pushes its condition-4 description and its `unclaimedProbes` failures on both the post-rename and pre-rename branches, verifiable by reading the function: the walk sits above the `if (postRename)` block.
    - `main()` has exactly one source for a non-zero return: a non-empty result from `gateFailures()`. No other `return 1` remains in the gate path.
    - `rec.failures` are written to stderr whenever non-empty, with or without `--reconcile`; the `rec.conditions` narration remains flag-gated.
    - GREEN, after the census correction: `node scripts/scan-brand-residue.mjs` exits 0, and `node scripts/scan-brand-residue.mjs --reconcile` exits 0 with zero reconciliation failures on the identical tree — the two modes now agree, which is the specific disagreement 01-VERIFICATION.md reproduced.
    - Each of the four corrected inventory rows carries a `reason` naming the specific tree change that moved its number. A row whose count changed with no added justification fails this criterion regardless of whether the scan is green.
    - The absolute-repo-root row is `coincidental` with `expected_count` 0, and `node scripts/verify-branding-preflight.mjs` exits 0 with that row now active in its coincidental-zero check.
    - The plan-file provenance row's `expected_files` names only files that exist on disk, includes the consolidated verification driver, and excludes the three deleted per-phase drivers; the total count equals the sum actually observed across those files.
    - No file inside the scanned scope gained a plan citation written as a full filename; citations use the bare plan number, and the reconciliation stays green after the registry row is added.
    - `scripts/verify-platform.sh --only scan-brand-residue-self-test` resolves and exits 0; the label exists exactly once in the registry, and no new driver script was created.
    - `scripts/verify-platform.sh --quick` exits 0 with a PASS row for every registered check, including the new one.
    - `.planning/WINDOWS.md` item 6 is no longer `open`; its resolution names both facts that changed — the census now closes, and the plain run now gates on reconciliation.
    - `scope.exclude` still contains the scanner's own path, asserted before any brand-token literal was added to it.
  </acceptance_criteria>

  <done>The plain, un-flagged residual-brand gate goes red on an unclaimed occurrence and is proven to do so by a fault-planting self-test with a clean control; the census reconciles against the real tree with every moved number justified; the self-test is registered as one row in the one registry; and the ledger item that understated the defect is closed.</done>
</task>

<task type="auto">
  <name>Task 2: Render the display form in the About dialog, and derive the surface set that guards it from the tree</name>
  <files>theia/extensions/branding/src/browser/powerbrowser-about-dialog.tsx, scripts/verify-branding-preflight.mjs, scripts/verify-branding.mjs</files>

  <precondition>The Theia workspace's dependencies are installed, because `verify-platform.sh --only verify-branding` lazily starts the live Theia dev app and the branding checks assume the state `scripts/smoke-theia.sh` establishes. If the lazy start reports a missing frontend, run `scripts/smoke-theia.sh` inside `nix develop .#theia` first — `yarn` does not work outside that shell.</precondition>

  <reversibility rating="reversible">Both halves revert cleanly: the display literal is one text node, and the derived surface enumeration replaces a two-line hand-appended push.</reversibility>

  <read_first>
    - theia/extensions/branding/src/browser/powerbrowser-about-dialog.tsx — the whole file; `renderContent()` is short, and the D-35 header comment explains why `render()` is overridden entirely rather than partially
    - theia/extensions/branding/src/browser/powerbrowser-welcome-widget.tsx — the sibling surface that was already fixed in 01-07, and the exported repository-URL constant the About dialog also renders
    - scripts/verify-branding-preflight.mjs — section 6 in full, which builds `displaySurfaces` and applies the leak regex; the 01-07 comment explaining why the welcome widget had to be appended by hand; and `selfTest()`'s `copy` list, which currently carries only one file from the branding extension's browser directory
    - scripts/verify-branding.mjs — `checkWelcome()`'s three assertions and the comment explaining why the inventory is a third source neither the value nor the checker was produced from; `checkAbout()`, which carries only the stock-identity and framework-package assertions; and `main()`'s expected-surface coverage guard
    - inventory/brand-tokens.json, the `brand_display_expectations` block — `identifier_form` and the per-variant short name, which are the two values both checks must read rather than hand-keep
    - .planning/phases/01-platform-extraction-and-rename/01-UI-SPEC.md, the E7 row in `## UI Considerations` and the render-size table — the About dialog's mark is 48x48 inside Theia's stock `ad-container`, and the dual fill is D-36
    - CLAUDE.md, "No generator in Phase 1" — the display value stays a hand-written literal in the JSX; only the checker's expectation is derived
  </read_first>

  <action>
Do the two guard changes first, observe them go red against the real defect, then correct the literal.
The order matters: a guard proven green over a fixed tree has not been proven to discriminate, and
this gap exists precisely because a guard was accepted on that basis. Commit once, at the end — the
red is observed, not committed.

Static half, in `verify-branding-preflight.mjs` section 6. Replace the hand-appended path for the
welcome widget with a derived enumeration: read the branding extension's browser directory at check
time and include every TypeScript and TSX source file it contains. The 01-07 comment beside that push
says exactly why it exists — the widget rendered the identifier form through the entire rename
because its path was not on the list — and the correct fix is not to append a second path but to stop
keeping the list by hand. A derived set goes red when a leaking surface is added and red when the
directory it derives from disappears; an appended path can only ever cover the one file someone
remembered. Guard the walk the way this file guards its other sets: if the directory yields zero
source files, that is a failure, not a clean run. Keep the inventory-declared variant files and the
Theia application manifest as they are — those are specific declared files, not a directory, and the
inventory is their source. Note in the comment that this walk reads the filesystem rather than the
index, so unlike the residual scan it sees a file that has not been staged.

Then extend that script's self-test. Its fixture copy list currently carries one file from that
directory, so the derived walk would have almost nothing to walk and the new code path would be
exercised vacuously. Copy the whole branching browser directory into the fixture, confirm the
unmutated fixture is still green as the existing control requires, then plant the space-less
identifier form in the About dialog's copy as a JSX text node and require rejection naming that file
and its line. This is the same plant-and-require-red shape the existing case uses for the branding
term file.

Runtime half, in `verify-branding.mjs`. The About check carries two assertions where its welcome
sibling carries five, which is the whole reason the check printed a pass over a real user-visible
defect. Extract the shared part into one function both surfaces call — the display form must be
present, the space-less identifier form must be absent, and the stock framework identity must be
absent — and have it read the expected display literal and the identifier form out of
`inventory/brand-tokens.json` rather than carrying them as string constants. The file's own comment
already argues for the inventory as the third source that neither the value nor the checker was
produced from; today the welcome check states that reasoning and then hard-codes the literal anyway.
Apply the shared assertion to both surfaces, keeping the About check's two existing framework-
specific assertions, which have no welcome counterpart. Fix once where both callers route through so
a third surface added later inherits the assertion instead of needing to remember it.

Only then, the defect itself: the About dialog's product-name heading renders the space-less
identifier form. Replace that text node with the display form the inventory fixes for every display
value — the two-word form with the space. Hand-written literal, per the no-generator rule; nothing
about this value is derived at build time. Change nothing else on that surface: the mark stays at
48x48 inside Theia's stock container class with its dual fill, and the version line and repository
link are untouched (D-35, D-36, UI-SPEC E7).

The repository URL rendered in this dialog is all-lowercase, so the case-sensitive identifier-form
assertion does not collide with it. Confirm that when you run the runtime check rather than assuming
it.
  </action>

  <verify>
    <automated>node scripts/verify-branding-preflight.mjs --self-test</automated>
    <automated>node scripts/verify-branding-preflight.mjs</automated>
    <automated>scripts/verify-platform.sh --only branding-preflight</automated>
    <automated>scripts/verify-platform.sh --quick</automated>
    <automated>scripts/verify-platform.sh --only verify-branding</automated>
  </verify>

  <acceptance_criteria>
    - RED against the real defect, recorded before the literal is corrected: with only the derived enumeration applied, `node scripts/verify-branding-preflight.mjs` exits 1 and its failure names `theia/extensions/branding/src/browser/powerbrowser-about-dialog.tsx` with the line number of the product-name heading. The verbatim output is quoted in the SUMMARY.
    - RED at runtime, recorded before the literal is corrected: with only the shared assertion applied, `scripts/verify-platform.sh --only verify-branding` exits 1 and the failure message identifies the about surface rather than the welcome surface.
    - RED by planted fault: `node scripts/verify-branding-preflight.mjs --self-test` reports that the unmutated fixture was green, that the identifier form planted in the About dialog copy was rejected naming that file, and that the pre-existing planted-branding-term and emptied-row cases still pass.
    - GREEN after the correction: `node scripts/verify-branding-preflight.mjs` exits 0 and `scripts/verify-platform.sh --only verify-branding` exits 0 with a PASS line for each of the four expected surfaces.
    - The preflight's display-surface set is produced by reading the branding extension's browser directory at check time. A source file added to that directory is included without editing the script — demonstrate by adding a scratch file containing the identifier form, confirming the check goes red naming it, and deleting it before committing.
    - A zero-file result from that directory walk is reported as a failure with its own message, not as a clean run.
    - `verify-branding.mjs` contains one shared assertion function invoked by both the welcome and about surfaces; neither surface carries its own copy of the display-form, identifier-leak, or stock-identity test.
    - Neither the expected display literal nor the identifier form appears as a string constant in `verify-branding.mjs`; both are read from `inventory/brand-tokens.json` at run time.
    - The About dialog's rendered text contains the two-word display form and the running application's version, and contains neither the stock framework name nor the space-less identifier form — asserted against the live DOM, not against the source file.
    - The About dialog's markup is otherwise unchanged: the same container class, the same 48-pixel mark dimensions, the same version line, the same repository anchor. Confirm by diff — the JSX diff is one text node.
    - `scripts/verify-platform.sh --quick` exits 0, and `node scripts/scan-brand-residue.mjs --reconcile` still exits 0 after this task's edits.
  </acceptance_criteria>

  <done>The About dialog renders the display form, and the two checks that failed to notice that it did not now both go red against that exact defect — one statically from a tree-derived surface set, one at runtime from an assertion shared with the surface where this bug class was first caught.</done>
</task>

<task type="auto">
  <name>Task 3: Re-prove the tree and carry both perceptual walkthroughs forward as human checks</name>
  <files>.planning/WINDOWS.md</files>

  <read_first>
    - .planning/WINDOWS.md — ledger items 15 and 16, the two platform-blocked perceptual verifications, and their recorded reasons
    - .planning/phases/01-platform-extraction-and-rename/01-VERIFICATION.md, the `human_verification` block and the "Human Verification Required" section — the five GUI-01 steps and the three GUI-03 steps, verbatim
    - .planning/phases/01-platform-extraction-and-rename/01-VALIDATION.md, the Manual-Only Verifications table — the two rows and the stated reason each is manual
  </read_first>

  <action>
Re-run the two cheap gates over the whole tree after both fixes have landed, and confirm the two
modes of the residual scan now agree. This is the honest end state for a gap-closure plan whose two
defects are both static: neither required a Gecko rebuild, so do not spend one. The full-suite gate
was already run green at the phase gate in the prior plan and nothing in this plan touches a compiled
path.

Then record the two perceptual walkthroughs as human checks. Both are platform-blocked from
automation on Linux — the remote-debugging protocol cannot see chrome contexts and chrome-context
automation is blocked — and both are already ledger items. Do not write an automated proxy for
either. An automated check that cannot observe the perceptual outcome produces a green nobody earned,
which is worse than an open ledger item because it removes the surface that would have surfaced the
gap again. If a human is present and performs them, update ledger items 15 and 16 with the per-step
outcome; if not, leave both open and say so in the SUMMARY. Either outcome is acceptable; a silently
closed item is not.

Leave the six code-review warnings that were not promoted to gaps untouched. They are real findings
and they are recorded in the review document, but none of them falsifies a must-have truth on the
current tree, and widening a gap-closure plan to absorb them is how a scoped fix becomes an
unreviewable one.
  </action>

  <verify>
    <automated>scripts/verify-platform.sh --quick</automated>
    <automated>node scripts/scan-brand-residue.mjs --reconcile</automated>
    <automated>node scripts/verify-branding-preflight.mjs --self-test</automated>
    <human-check>
GUI-01, five steps, all five required. Launch the application. Open a browser window from the command
palette. Confirm the address bar takes keyboard focus and navigates a typed URL. Confirm an in-window
modal dialog appears rather than being suppressed. Close the browser window and confirm the shell
returns with the application still running. Expected: all five behave exactly as a stock browser
window would, with no sidecar chrome present in the browser window.
    </human-check>
    <human-check>
GUI-03, three steps. With the development flag on, edit the runtime stylesheet and confirm the shell
visibly restyles with no rebuild. Delete the stylesheet and confirm the shell reverts. Expected: the
runtime style layer visibly applies and un-applies. The existing automated checks prove only that an
absent stylesheet is inert and that the privileged binding is absent with the flag off; neither
observes the visible effect, and neither is a substitute for this.
    </human-check>
  </verify>

  <acceptance_criteria>
    - `scripts/verify-platform.sh --quick` exits 0 with a PASS row for every registered check.
    - `node scripts/scan-brand-residue.mjs` and `node scripts/scan-brand-residue.mjs --reconcile` both exit 0 on the same tree — the disagreement 01-VERIFICATION.md reproduced is gone.
    - `node scripts/verify-branding-preflight.mjs --self-test` and `node scripts/scan-brand-residue.mjs --self-test` both exit 0, each having planted at least one fault and required it to be rejected.
    - No new automated check was added that claims to cover either perceptual walkthrough. Verify by inspecting the registry diff: this task adds no registry row.
    - Ledger items 15 and 16 are either updated with a performed per-step outcome or left open, and the SUMMARY states which, with the reason. Neither is closed without a recorded walkthrough.
    - The six code-review warnings outside this plan's scope are untouched; the review document is not edited.
  </acceptance_criteria>

  <done>Both gap fixes are green on the whole tree under the cheap gates, both self-tests plant faults and require rejection, and the two perceptual verifications are carried forward as human checks rather than replaced by an automated proxy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tree content → the residual-brand gate's exit code | A scanner that under-scans, or whose failure path is unreachable, reports clean over a dirty tree. The exit code is the only signal three registered call sites consume. |
| `inventory/brand-tokens.json` → every branding check | The single expected-value source. A wrong value here propagates to the preflight, the runtime branding verifier, and the identity verifier simultaneously, and all three then agree with it. |
| rendered display text → the user | The product name a user reads is the product's identity claim. A wrong name is not a cosmetic defect. |
| self-test fixture → the repo working tree | The fixture deliberately contains a brand token that the permanent gate forbids in the tree. Fixture material escaping into a tracked path makes the gate permanently red. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-01 | Spoofing | the registered residual-brand gate | high | mitigate | The exit code is single-sourced through `gateFailures()`, condition 4 is made reachable on both branches, and a self-test plants an unclaimed form on a post-rename fixture and requires the un-flagged path to go red, with a clean control proving the red is plant-caused. |
| T-08-02 | Tampering | the `expected_count` census | high | mitigate | Each of the four moved numbers is reconciled by name with its justification written into the row's own `reason`; a blind copy of the observed value is prohibited and fails the task's acceptance criteria independently of the exit code. |
| T-08-03 | Spoofing | `verify-branding.mjs`'s About surface | high | mitigate | One shared assertion is applied to both display surfaces, with its expected values read from the inventory rather than hand-kept, so a surface cannot pass by carrying fewer assertions than its sibling. |
| T-08-04 | Tampering | the preflight's display-surface list | medium | mitigate | The list is derived by walking the branding extension's browser directory at check time and fails on an empty walk, so it goes red on an addition and on a removal rather than covering only remembered paths. |
| T-08-05 | Information Disclosure | About dialog display text | low | accept | The dialog renders only the product name, the application version, and a public site URL. No internal identifier reaches it, and the version string is already public in the built artifact. Accepted at ASVS L1. |
| T-08-06 | Tampering | self-test fixture material entering the tracked tree | medium | mitigate | The fixture is created under the system temp root and removed in a `finally` block; the brand-token literal is confined to the scanner file, whose exclusion from its own scan is asserted as a task precondition. |
| T-08-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package-manager install occurs in this plan. No new dependency is added to either workspace; every change is to existing first-party scripts and one TSX source file. The `## Package Legitimacy Audit` gate does not apply. |
</threat_model>

## Edges and UI considerations lifted

| Source | Item | Status | Lift |
|---|---|---|---|
| Edge probe | MIG-03 adjacency | ✅ resolved (explicit) | `truths` — the boundary rule at exact touch: left-adjacent letter unclaimed, upper-camel continuation claimed, trailing-lowercase form reported by condition 4 and failing the un-flagged gate |
| Edge probe | MIG-03 empty | ✅ resolved (explicit) | `truths` — empty file set, empty token set, and zero in-scope held-back rows each fail rather than pass; a clean result over an empty set is reported as unrun |
| Edge probe | MIG-03 encoding | ✅ resolved (explicit) | `truths` — two deliberately different equality definitions, neither normalizing; counts are UTF-16 substring matches, not grapheme clusters |
| Edge probe | MIG-03 ordering | ✅ resolved (explicit) | `truths` — occurrences sorted by file/line/index, probes by file/line, claim order longest-first then by case-form rank; reruns are byte-identical |
| Edge probe | MIG-04 unclassified | ⚠️ unresolved | Flagged assumption below — never auto-resolved with a backstop |
| UI-SPEC | E7 welcome/about, populated | ✅ covered | `truths` — the About dialog keeps the stock container layout, the 48-pixel mark, and the dual fill (D-36); the fix changes one text node |
| — | display surfaces outside the derived set | 🧪 backstop | `truths` backstop marker — surfaces authored outside the enumerated directory and the inventory-declared files are not reached by the leak scan |

## Flagged Assumptions (unresolved edges — surfaced, not dropped)

| Requirement | Probe | Planner assumption |
|---|---|---|
| MIG-04 | unclassified — review manually | Assumed that MIG-04's remaining exposure in this gap-closure run is exactly the one display literal 01-VERIFICATION.md reproduced, and that the build/boot half is closed on the evidence recorded in REQUIREMENTS.md (both smoke scripts green, six identity surfaces green with a discriminating positive control). Not auto-resolved with a backstop, because an `unclassified` probe row has no defensible acceptance criterion to write. If the assumption is wrong, the missed surface is a second display literal somewhere the derived enumeration does not reach — which is the backstop truth this plan already records. |

## Edges out of scope for this plan (closed by the executed plans — surfaced, not dropped)

| Requirement | Probe | Where it is already covered |
|---|---|---|
| MIG-01 | unclassified | Closed by plans 01-01 and 01-04; the fetch and patch-apply self-tests and the built artifact are the evidence. |
| MIG-02 | unclassified | Closed by plan 01-01; the committed pre-rename red-scan report and a fully classified inventory are the evidence. |
| GUI-01 | idempotency — what happens if this runs twice | Covered by the second-launch-focus and no-second-backend checks in the full registry. The perceptual half is carried forward as this plan's first human check. |
| GUI-01 | concurrency — guarantees under interruption or parallel runs | Covered by the four signal-and-recovery checks in the full registry. |
| GUI-03 | unclassified | Covered by the inertness and flag-off verifiers; the visible-restyle half is carried forward as this plan's second human check. |
| GUI-04 | unclassified | Closed by plan 01-06; the registry-shape assertion and its self-test are the evidence. |
| SEC-01 | unclassified | Closed against the existing credential gate, verified directly against the three stated invariants at the phase gate. |

## Explicitly deferred (not planned here, by instruction)

- The six code-review warnings not promoted to gaps: cookie port-scoping, the missing positive control, the error-copy regex escape hatch, the unhandled-rejection paths, the bare-signal throw contract, and the start-address read race. Each is real; none falsifies a must-have truth on the current tree.
- Ledger item 10: the release-variant identity and variant-divergence checks, which need a second full release build.

## Artifacts this phase produces

- `gateFailures(offenses, rec)` — new exported function in `scripts/scan-brand-residue.mjs`; the single source of the gate's non-zero exit
- New `--self-test` case in `scripts/scan-brand-residue.mjs`: a post-rename fixture with a planted unclaimed form plus a clean control, requiring the un-flagged gate path to go red
- `scan-brand-residue-self-test` — one new row in `scripts/verify-platform.sh`'s `run_own_checks()` quick set
- A tree-derived display-surface enumeration in `scripts/verify-branding-preflight.mjs` section 6, replacing the hand-appended welcome-widget path, with a zero-file guard
- Expanded `selfTest()` fixture in `scripts/verify-branding-preflight.mjs`: the branding extension's browser directory copied whole, with the identifier form planted in the About dialog copy
- A shared display-form assertion in `scripts/verify-branding.mjs`, invoked by both `checkWelcome()` and `checkAbout()`, reading its expected values from `inventory/brand-tokens.json`
- Corrected `expected_count`, `expected_files`, and `reason` on four rows of `inventory/brand-tokens.json`
- The corrected display literal in `theia/extensions/branding/src/browser/powerbrowser-about-dialog.tsx`
- `.planning/WINDOWS.md` ledger item 6 moved to resolved

<verification>
- `node scripts/scan-brand-residue.mjs` and `node scripts/scan-brand-residue.mjs --reconcile` both exit 0 on the same tree
- `node scripts/scan-brand-residue.mjs --self-test` exits 0, having planted an unclaimed form on a post-rename fixture and required the un-flagged gate to reject it, with a clean control
- `node scripts/verify-branding-preflight.mjs` and `node scripts/verify-branding-preflight.mjs --self-test` both exit 0
- `scripts/verify-platform.sh --quick` exits 0 with a PASS row for every registered check, including `scan-brand-residue-self-test`
- `scripts/verify-platform.sh --only verify-branding` exits 0 with a PASS line for all four surfaces
- Both red demonstrations are quoted verbatim in the SUMMARY: the un-flagged scan naming four drifted rows, and the preflight naming the About dialog's leaking line
- Both perceptual walkthroughs are recorded as performed with per-step outcomes, or left open with the reason stated
</verification>

<success_criteria>
- The registered residual-brand gate fails on an unclaimed occurrence in its plain, un-flagged mode, and its self-test proves it (MIG-03, Success Criterion 1, D-17, D-18)
- The census reconciles against the real tree, with every moved number justified by name rather than absorbed (MIG-03, D-08)
- The About dialog renders the hand-written display form, and both the static and runtime checks that guard it go red against that exact defect (MIG-04, Success Criterion 5, D-35)
- Every new or changed guard has been observed red — two against pre-existing real defects, two against planted faults with clean controls
- No sibling verification driver was created; the one registry gained exactly one row
- The two platform-blocked perceptual verifications are carried forward as human checks with no automated proxy standing in for them
</success_criteria>

<output>
Create `.planning/phases/01-platform-extraction-and-rename/01-08-SUMMARY.md` when done
</output>
