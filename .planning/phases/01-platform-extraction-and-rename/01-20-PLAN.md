---
phase: 01-platform-extraction-and-rename
plan: 20
type: execute
wave: 20
depends_on: ["01-18", "01-19"]
gap_closure: true
autonomous: true
requirements: [MIG-03, MIG-04]
files_modified:
  - powerbrowser/branding/dev/content/aboutDialog.css
  - powerbrowser/branding/release/content/aboutDialog.css
  - scripts/verify-about-dialog-suppression.mjs
  - scripts/verify-platform.sh
  - .planning/phases/01-platform-extraction-and-rename/deferred-items.md
  - .planning/WINDOWS.md
  - .planning/REQUIREMENTS.md

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The About dialog keeps exactly one visible bottom-row link — the internal `about:license` disclosure — while both `https://www.mozilla.org` bottom-row links stay suppressed, in both branding variants (D-13: the dev/release split lives in display names and the title-bar pref, never in this stylesheet)."
    - "A registered check fails by name when any shipped suppression selector matches the `about:license` element or any of its ancestors, so the current defect cannot be reintroduced silently (MIG-03 regression-reintroduction edge; probe R3)."
    - "A registered check fails by name when any shipped suppression selector matches nothing in the upstream markup, so an ESR rebase that renames a container id turns a silent no-op into a red row (01-REVIEW.md WR-03)."
    - "The checker's `--self-test` is green on an unmutated fixture and red on each of four planted faults (internal-link suppression, stale selector, empty derived selector set, missing `about:license` premise), each red naming the drift (CLAUDE.md verification rule 2)."
    - "Both suppression selector sets and both stylesheets are derived and compared at check time; no expectation list about upstream markup is hand-kept inside the checker (CLAUDE.md verification rule 2; D-16's static-script layer)."
    - "`scripts/verify-platform.sh --quick` stays green with one additional row, and the upstream-reading row is reachable through `--only` in the full set — one driver, one registry, no sibling driver (D-21)."
    - "`node scripts/scan-brand-residue.mjs` exits 0 with the new checker script staged, so the permanent residual gate (D-18) sees the new file rather than skipping it as untracked."
    - statement: "A repackaged binary's About dialog renders `Licensing Information` as the only bottom-row link, and clicking it opens the aggregated open-source licence text; no Terms of Use or Privacy Notice row is present (MIG-04 boot-failure edge; probe R4)."
      verification: backstop
  artifacts:
    - "powerbrowser/branding/dev/content/aboutDialog.css — href-qualified suppression selector plus a comment that names the internal link the row keeps"
    - "powerbrowser/branding/release/content/aboutDialog.css — byte-identical to the dev variant"
    - "scripts/verify-about-dialog-suppression.mjs — derived selector/markup comparison with `--self-test`"
    - "scripts/verify-platform.sh — two new registry rows, no new driver"
    - ".planning/phases/01-platform-extraction-and-rename/deferred-items.md — row 11 corrected to record the defect that existed, not only the risk that might"
  key_links:
    - "shipped `aboutDialog.css` suppression selectors → `upstream/browser/base/content/aboutDialog.xhtml`'s element tree, compared by the checker's selector matcher rather than assumed"
    - "`scripts/verify-about-dialog-suppression.mjs` → `scripts/verify-platform.sh` registry rows `about-dialog-suppression` (full set, reads `upstream/`) and `about-dialog-suppression-self-test` (`--quick`, hermetic)"
    - "`powerbrowser/branding/*/content/jar.mn` → `aboutDialog.css` — the packaging line landed by 01-18 and asserted by `branding-preflight` section 9; unchanged here and must stay green"
  prohibitions:
    - statement: "No branding value in this tree may be generated, templated, or derived at build time — every value stays a hand-written literal, because Phase 2's acceptance test is byte-identity against what Phase 1 wrote by hand."
      status: flagged-unverified
    - statement: "`upstream/` must not be hand-edited. The upstream markup is read as evidence only; the fix lands under `powerbrowser/branding/` and `scripts/`. `git -C upstream diff` stays empty."
      status: flagged-unverified
    - statement: "No custom browser chrome, licence page, or About-dialog replacement may be authored to substitute for the suppressed row (CLAUDE.md rule 5 — the stock chrome stays stock)."
      status: flagged-unverified
    - statement: "The new checker must not hand-keep an expectation list of upstream ids, selectors, or hrefs, and must not become a second verification driver — it is one script reached by rows appended to the single registry."
      status: flagged-unverified
    - statement: "No internal identifier — pref key, sentinel, port, timeout, or raw exception message — may appear in any user-facing string this plan touches."
      status: flagged-unverified
    - statement: "A selector the checker cannot parse, or an upstream file it cannot read, must fail loudly and never be skipped; a skip that keeps a check green is the exact failure class plans 01-14 through 01-17 each closed once."
      status: flagged-unverified
  # Canon security items (input validation at trust boundaries, secret handling,
  # authn/authz) are covered by SEC-01's existing token-gate work and are not
  # minted here as new prohibitions — breadcrumb only.
---

<objective>
Close the single remaining verification gap in phase 01 (01-VERIFICATION.md, Truth 9): the About
dialog's debranding stylesheet suppresses `#bottomBox > hbox` wholesale, which takes the internal
`about:license` disclosure link down with the two `https://www.mozilla.org` links that were the
actual reported defect (UAT G-01-3). Introduced by plan 01-18 (commit `e24f210`), independently
reproduced by the verifier against the shipped CSS and the upstream markup it targets, and named
independently by 01-REVIEW.md as CR-01 with WR-03 as its missing gate.

Purpose: the phase goal says the tree "works as an actual web browser". A browser that ships
MPL-covered and third-party code with no in-UI route to its aggregated licence text does not.
MIG-03 and MIG-04 are the two requirements 01-18's own SUMMARY claims for this file.

Output: an href-qualified suppression selector in both byte-identical branding variants; a
registered, tree-derived check with a planted-fault `--self-test` that cannot be green while the
licence link is suppressed; and a corrected `deferred-items.md` row 11 plus the two
documentation-currency corrections the verification report names.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md

@.planning/phases/01-platform-extraction-and-rename/01-VERIFICATION.md
@.planning/phases/01-platform-extraction-and-rename/01-18-SUMMARY.md
</context>

## Artifacts this phase produces

Every symbol below is created by THIS plan. Nothing here exists in the tree yet, so none of it can
be verified against current sources.

**New file**
- `scripts/verify-about-dialog-suppression.mjs`

**New function names inside that file** (names are the executor's to keep or improve, but the
SUMMARY must record whichever names shipped)
- `parseSuppressionSelectors(css)` — derives the selector list from a stylesheet's `display: none` rules
- `parseMarkup(xhtml)` — derives the element tree (tag, id, classes, href, parent) from the upstream document
- `parseSelector(text)` — compound + combinator parse; throws/fails loudly on anything outside the supported grammar
- `matches(selector, element)` — right-to-left match against the element's ancestor chain
- `runChecks(root)` — the four assertions, rooted at a directory so the self-test can pass a fixture
- `selfTest()` — mktemp fixture, green control, four planted faults

**New registry labels in `scripts/verify-platform.sh`**
- `about-dialog-suppression` (full set — reads `upstream/`)
- `about-dialog-suppression-self-test` (`--quick` — hermetic)

**New CSS selector shipped in both variants**
- `#bottomBox > hbox > .bottom-link[href^="https://www.mozilla.org"]`

## Surfaced assumptions (probe edges not authored into must_haves)

The spec-less edge probe returned 12 applicable edges, all unresolved. Two bear on the surfaces this
plan touches and are authored above (MIG-03 regression-reintroduction → truth 2; MIG-04 boot-failure
→ the backstop truth). The remaining ten are recorded here as explicit planner assumptions, not
dropped:

1. **MIG-01 empty-input** — assumed covered by `fetch-upstream-self-test`; this plan adds no fetch path.
2. **MIG-01 idempotent-rerun** — assumed covered by 01-01/01-02's rerunnable rename script; untouched here.
3. **MIG-02 empty-input** — assumed covered by the inventory's non-vacuity rules; untouched here.
4. **MIG-03 partial-rename** — assumed covered by `scan-brand-residue`; this plan adds one file to its scope and asserts the scan green with it staged.
5. **MIG-04 build-failure** — assumed unaffected: this plan changes no build input. `jar.mn`, `moz.build`, and the patch stack are untouched; only a CSS selector body changes.
6. **GUI-01 toggle-roundtrip** — out of scope; covered by UAT test 2 (live, user-confirmed 2026-09-01).
7. **GUI-03 runtime-apply-revert** — out of scope; covered by UAT test 6.
8. **GUI-04 shape-drift** — out of scope; covered by `gui04-registry-shape`.
9. **SEC-01 missing-credential** — out of scope; covered by the token-gate work.
10. **SEC-01 fail-closed** — out of scope; covered by the token-gate work.

**01-UI-SPEC.md `## UI Considerations`:** no row bears on this surface. E7 covers the *Theia*
about/welcome surfaces, not the Gecko About dialog. Nothing lifted, nothing dropped.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Make the licence link's disappearance a red check, then fix the selector so it goes green</name>

  <files>scripts/verify-about-dialog-suppression.mjs, powerbrowser/branding/dev/content/aboutDialog.css, powerbrowser/branding/release/content/aboutDialog.css</files>

  <precondition>`upstream/browser/base/content/aboutDialog.xhtml` exists — it is the git-ignored clone `scripts/fetch-upstream.sh` materialises. It is present in this checkout; if it is not, run the fetch script before starting. Halt rather than writing a checker that skips when the file is absent.</precondition>

  <reversibility rating="reversible">A CSS selector body in two files under `powerbrowser/branding/`; `git revert` restores the prior state with no migration, no build input, and no schema.</reversibility>

  <read_first>
    - `upstream/browser/base/content/aboutDialog.xhtml` lines 120-146 — the DOM under test. **Read only; never edit** (CLAUDE.md hard rule 2). `#bottomBox` is a `vbox` whose first child is an `hbox pack="center"` holding three `<label is="text-link" class="bottom-link">` elements in order: `about:license` (with `useoriginprincipal="true"`), then the terms link, then the privacy link; `#bottomBox`'s second child is `#trademark`.
    - `powerbrowser/branding/release/content/aboutDialog.css` — the shipped stylesheet, including the comment block at lines 37-50 that currently frames the whole row as outbound.
    - `powerbrowser/branding/dev/content/aboutDialog.css` — byte-identical to the release variant; confirm that before editing either.
    - `scripts/verify-branding-preflight.mjs` — the plumbing to copy: `makeReporter()` and its `ok`/`fail` shape (lines 59-114), section 9's derive-the-set-from-the-directory comment (lines 513-560), and the `selfTest()` harness at lines 617-830 whose contract is "the unmutated fixture must be GREEN first, or a red after the plant proves nothing".
    - `scripts/verify-registry-shape.mjs` — the derive-and-compare-as-set-equality precedent CLAUDE.md names.
    - `.planning/phases/01-platform-extraction-and-rename/01-REVIEW.md` lines 178-198 (WR-03) — the staleness half of the assertion set, proposed independently of the verifier.
    - `.planning/phases/01-platform-extraction-and-rename/01-VERIFICATION.md` lines 18-58 — the gap statement and its three named deliverables.
  </read_first>

  <behavior>
    The checker's four assertions, each with its own planted-fault row:
    - Assertion 1 (non-vacuity): a derived selector set of size zero, or a parsed markup with zero link elements, is a FAIL. A walk that found nothing has proven nothing.
    - Assertion 2 (grammar totality): every derived selector parses under the supported grammar. An unparsed selector is a FAIL naming it, never a skip.
    - Assertion 3 (staleness, WR-03): every derived selector matches at least one element in the upstream markup. A selector matching nothing is a silent no-op after an ESR rename and is a FAIL naming the selector.
    - Assertion 4 (disclosure preservation, the gap itself): at least one element carrying `href="about:license"` exists in the markup — its absence is a FAIL, because the check's premise is gone — and no derived selector matches that element or any element on its ancestor chain. A match is a FAIL naming the selector, the element, and the ancestor it matched through.
    Red-first ordering: the checker is written and observed exiting 1 against the CURRENT unfixed tree before either stylesheet is edited. That observed message is the RED evidence and goes into the SUMMARY verbatim.
  </behavior>

  <action>
    Create `scripts/verify-about-dialog-suppression.mjs`, a standalone static checker in the same
    layer as `scan-brand-residue.mjs` (per D-16: no build, no browser, node stdlib only —
    `node:fs`, `node:path`, `node:os`; no new dependency).

    Derive both sides at check time; hand-keep no expectation list (CLAUDE.md verification rule 2).
    Left side: read both `powerbrowser/branding/{dev,release}/content/aboutDialog.css` relative to
    the `root` argument, strip block comments, and collect the comma-split selector list of every
    rule whose declaration block contains `display: none`. Right side: read
    `upstream/browser/base/content/aboutDialog.xhtml` relative to the same `root` and build an
    element tree — tag name, `id`, class list, `href`, and parent link — by scanning for `<tag ...>`,
    `<tag .../>` and `</tag>`, skipping `<?xml`, `<!--`, `<!DOCTYPE`, and the `#ifdef`/`#endif`
    preprocessor lines. Rooting the reads at `root` rather than at the repo is what lets the
    self-test point the whole check at a fixture.

    Support exactly the selector grammar the shipped stylesheet uses: a compound of optional tag
    name, optional `#id`, zero or more `.class`, and an optional `[href^="value"]` attribute test;
    combinators are descendant (whitespace) and child (`>`); match right-to-left over the ancestor
    chain. Any selector outside that grammar is a named FAIL, not a skip.

    Missing upstream markup is a FAIL naming the path and `scripts/fetch-upstream.sh`, never a skip
    and never a silent green — that green-by-construction shape is the failure class recorded in
    `deferred-items.md` rows 4, 9 and 10.

    Give it `--self-test`, built on `verify-branding-preflight.mjs`'s harness shape: `mkdtempSync`
    fixture; copy the REAL shipped `powerbrowser/branding/{dev,release}/content/aboutDialog.css`
    into it; author a minimal `upstream/browser/base/content/aboutDialog.xhtml` inside the fixture
    that mirrors upstream's three suppressed structures — `#communityDesc`, `#contributeDesc`, and
    `#bottomBox`'s `hbox pack="center"` with three `.bottom-link` children whose hrefs are
    `about:license`, a `https://www.mozilla.org/about/legal/terms/firefox/` value and a
    `https://www.mozilla.org/privacy/firefox/` value. Assert the unmutated control is GREEN first,
    then plant four faults in turn, restoring between each, and require each to go RED naming the
    drift: (a) rewrite the fixture stylesheet's qualified bottom-row selector back to its bare
    container form — this reproduces the pre-fix tree and must name `about:license`; (b) rename
    `bottomBox` in the fixture markup so a shipped selector matches nothing — must name that
    selector; (c) delete the fixture stylesheet's `display: none` rule so the derived set is empty —
    must give the vacuity message; (d) delete the `about:license` element from the fixture markup —
    must fail on the missing premise. `--self-test` must call `runChecks` only with the mktemp
    fixture path, never with the repository root, so it stays hermetic and can live in `--quick`.

    Run `node scripts/verify-about-dialog-suppression.mjs` against the unfixed tree and record the
    exact failure text.

    Only then edit the stylesheets. In `powerbrowser/branding/release/content/aboutDialog.css`,
    replace the bare container entry in the suppression rule's selector list with
    `#bottomBox > hbox > .bottom-link[href^="https://www.mozilla.org"]`, leaving `#communityDesc`
    and `#contributeDesc` as they are. Both upstream outbound bottom links carry that href prefix;
    the licence link carries `about:license` and is therefore untouched, and the row keeps its
    `pack="center"` layout with one visible child. Rewrite the comment block so it describes what
    the rule now does: the row is mixed, not outbound; its first child is the product's only in-UI
    route to the aggregated open-source licence text and it stays visible; only the two
    mozilla.org-destined children are suppressed; `#trademark` is `#bottomBox`'s other child and is
    also untouched. Copy the release file over the dev file so the two stay byte-identical (D-13 —
    the variant split lives in display names and the title-bar pref, not in this stylesheet).

    Stage the new script before trusting any residual-scan result: the scan iterates `git ls-files`
    and an unstaged new file is invisible to it (CLAUDE.md, and `deferred-items.md`'s standing trap).
  </action>

  <verify>
    <automated>node scripts/verify-about-dialog-suppression.mjs && node scripts/verify-about-dialog-suppression.mjs --self-test && cmp powerbrowser/branding/dev/content/aboutDialog.css powerbrowser/branding/release/content/aboutDialog.css && git add scripts/verify-about-dialog-suppression.mjs && node scripts/scan-brand-residue.mjs</automated>
  </verify>

  <acceptance_criteria>
    - Before either stylesheet is edited, `node scripts/verify-about-dialog-suppression.mjs` exits **1** and its message names both `about:license` and the offending selector. The SUMMARY records that message verbatim as the observed RED.
    - After the edit, the same command exits **0**.
    - `node scripts/verify-about-dialog-suppression.mjs --self-test` exits **0** and prints one line per fault row plus the green control — four planted faults, each red, each naming the drift it planted.
    - Source assertion: `--self-test` passes only the `mkdtempSync` fixture path to `runChecks`; the repository root appears nowhere in its call arguments, so the row is hermetic and needs no `upstream/`.
    - Source assertion: neither the checker nor its self-test contains a list of expected upstream ids, selectors, or hrefs used as the assertion's right-hand side; both sides of every comparison are read at check time. The authored fixture markup is a fault-planting harness, not an expectation, and the checker never reads it outside `--self-test`.
    - `node -e` or `grep -c` of the qualified selector in each variant returns 1 for `powerbrowser/branding/dev/content/aboutDialog.css` and 1 for `powerbrowser/branding/release/content/aboutDialog.css` after filtering comment lines (`grep -v '^ *\*' | grep -v '^ */\*'`).
    - `cmp powerbrowser/branding/dev/content/aboutDialog.css powerbrowser/branding/release/content/aboutDialog.css` exits **0**.
    - The stylesheet comment block states that the bottom row is mixed, that the internal licence link stays visible, and why — no sentence in it describes the row as entirely outbound.
    - `git -C upstream diff --stat` is empty: the upstream markup was read, never edited.
    - `git add scripts/verify-about-dialog-suppression.mjs && node scripts/scan-brand-residue.mjs` exits **0** with the new file counted in the scanned set.
  </acceptance_criteria>

  <done>The About dialog's licence disclosure link survives debranding in both variants, both mozilla.org bottom links stay suppressed, and a checker that was observed red on the pre-fix tree is green on the fixed one and red on each of four planted faults.</done>
</task>

<task type="auto">
  <name>Task 2: Register both rows in the one registry, at the tier that honestly describes their prerequisites</name>

  <files>scripts/verify-platform.sh</files>

  <precondition>`upstream/browser/base/content/aboutDialog.xhtml` exists, so `--only about-dialog-suppression` can actually run rather than reporting its own missing prerequisite.</precondition>

  <read_first>
    - `scripts/verify-platform.sh` lines 3487-3520 — the `--quick` block, which is where the other hermetic `*-self-test` rows sit and where the comment explains that `--quick` is a narrowing, never a different set.
    - `scripts/verify-platform.sh` lines 3612-3635 — the RE-TIERED block, whose comment records exactly why `apply-patches-self-test` sits in the full set (it derives its fixture from the git-ignored 1.1 GB clone) and that being re-tiered is not being excused: the row still runs and `--only <label>` still reaches it.
    - `CLAUDE.md` `## Verification` — one driver, one registry; adding a check means appending one row, never creating a sibling driver (D-21).
    - `scripts/verify-about-dialog-suppression.mjs` — the file being registered, as it shipped in Task 1.
  </read_first>

  <action>
    Append exactly two rows to `run_own_checks()`'s registry in `scripts/verify-platform.sh`. No new
    driver, no new function, no sibling script (D-21 — the consolidation that deleted
    `verify-phase-0{2,3,4,5}.sh` is the whole reason this rule exists).

    Row 1, in the `--quick` set next to the other self-test rows:
    `about-dialog-suppression-self-test|node $REPO_ROOT/scripts/verify-about-dialog-suppression.mjs --self-test`.
    It belongs in `--quick` because it is hermetic: its fixture is a `mkdtemp` directory carrying its
    own markup, so it needs no build, no browser, no display, and no `upstream/`.

    Row 2, in the full set inside the RE-TIERED block, immediately after `apply-patches-self-test`
    and covered by a comment in the same voice as the one already there:
    `about-dialog-suppression|node $REPO_ROOT/scripts/verify-about-dialog-suppression.mjs`.
    It reads `upstream/browser/base/content/aboutDialog.xhtml`, which is git-ignored and absent on a
    fresh checkout, so it cannot honestly claim `--quick`'s promises. The comment must say that it is
    re-tiered rather than excused, that it fails loudly rather than skipping when the clone is
    absent, and that `--only about-dialog-suppression` reaches it — mirroring the existing rationale
    for `apply-patches-self-test` rather than inventing a second convention.

    Do not move, re-tier, or reword any existing row. Do not gate either new row behind a
    conditional that could make it green when its prerequisite is missing.
  </action>

  <verify>
    <automated>bash scripts/verify-platform.sh --only about-dialog-suppression-self-test && bash scripts/verify-platform.sh --only about-dialog-suppression && bash scripts/verify-platform.sh --quick</automated>
  </verify>

  <acceptance_criteria>
    - `bash scripts/verify-platform.sh --only about-dialog-suppression-self-test` exits **0**.
    - `bash scripts/verify-platform.sh --only about-dialog-suppression` exits **0**.
    - `bash scripts/verify-platform.sh --quick` exits **0** and reports **25** PASS rows — one more than the 24 recorded in 01-VERIFICATION.md's behavioural spot-check table — with `about-dialog-suppression-self-test` among them.
    - Source assertion: `about-dialog-suppression` appears in the full-set array inside the RE-TIERED block, not in the `--quick` array, and carries a comment naming its `upstream/` dependency and stating that the row fails rather than skips when the clone is absent.
    - Source assertion: `scripts/` gains no new driver shell; the diff to `scripts/verify-platform.sh` is confined to the two registry rows and their comment.
    - `git diff --stat scripts/verify-platform.sh` shows a single file changed with no deletions of existing rows.
  </acceptance_criteria>

  <done>Both rows run under the single registry driver, each reachable through `--only`, with `--quick` still green and one row longer.</done>
</task>

<task type="auto">
  <name>Task 3: Correct the record — deferred-items row 11, and the two documentation-currency items the verifier named</name>

  <files>.planning/phases/01-platform-extraction-and-rename/deferred-items.md, .planning/WINDOWS.md, .planning/REQUIREMENTS.md</files>

  <read_first>
    - `.planning/phases/01-platform-extraction-and-rename/deferred-items.md` row 11 — the row being corrected. Its half (a) currently records the "hidden, not removed" design choice and the future-ESR-rebase risk, but does not record that the shipped selector already removed a legitimate link.
    - `.planning/phases/01-platform-extraction-and-rename/01-VERIFICATION.md` lines 55-58 (the three named deliverables) and lines 165-178 and 236-242 (the MIG-04 checkbox determination and the WINDOWS.md ledger-currency recommendation).
    - `.planning/WINDOWS.md` — the ledger header row and rows 15, 16 and 19, so the status and resolution columns are filled in the shape the table already uses.
    - `.planning/phases/01-platform-extraction-and-rename/01-UAT.md` — tests 2 and 6, the live user-confirmed evidence that resolves ledger rows 15 and 16.
    - `.planning/REQUIREMENTS.md` line 19 (the unticked MIG-04 checklist entry and its inline evidence comment) and line 263 (the traceability table row that already reads Complete).
  </read_first>

  <action>
    Rewrite `deferred-items.md` row 11's half (a) so it records what was true rather than only what
    might become true. It must state: the selector 01-18 shipped suppressed the whole bottom row and
    therefore removed the internal `about:license` disclosure link — the product's only in-UI route
    to its aggregated open-source licence text — alongside the two mozilla.org links that were the
    reported defect; that this was a live defect in the tree, not a latent risk; that
    01-VERIFICATION.md's Truth 9 and 01-REVIEW.md's CR-01 both found it independently; and that plan
    01-20 closed it with the href-qualified selector plus the `about-dialog-suppression` registry
    row, whose staleness assertion now also covers the future-ESR-rebase risk the original row
    recorded. Keep half (b) — the unshipped wordmark — unchanged.

    Add to the same row, as deliberate NOT-dones with their reasons, so neither is silently
    re-litigated:
    (i) `#communityDesc` remains suppressed even though it contains an `about:credits` link.
    That link is Mozilla contributor credits — vendor content this product is correctly debranding —
    not a licence disclosure, so the new check asserts reachability for `about:license` only. If a
    future decision says credits must survive debranding, the fix is a decision recorded here and a
    widened assertion in the checker, never a widened selector.
    (ii) The self-test's upstream markup fixture is authored rather than copied from `upstream/`,
    because `--quick` must not depend on a git-ignored 1.1 GB clone. Fixture drift can only weaken
    the self-test, never the gate — the real row reads the real markup — but it is a real ceiling.
    Route: re-derive the fixture at the first ESR bump.

    Then the two documentation-currency corrections 01-VERIFICATION.md explicitly recommends, and
    nothing beyond them. In `.planning/WINDOWS.md`, move ledger rows 15 and 16 from `open` to
    resolved, citing 01-UAT.md tests 2 and 6 respectively and their 2026-09-01 user confirmation in
    the resolution column, filling the date column in the format the table already uses. Leave
    ledger row 19 open — it still needs a repackaged binary. In `.planning/REQUIREMENTS.md`, tick
    line 19's MIG-04 checklist box to `[x]`, matching line 263's traceability row, which the verifier
    determined is the correct half; leave the inline evidence comment and every other line untouched.
  </action>

  <verify>
    <automated>node scripts/scan-brand-residue.mjs && bash scripts/verify-platform.sh --quick</automated>
  </verify>

  <acceptance_criteria>
    - `deferred-items.md` row 11 states that the shipped selector removed the internal `about:license` link, names 01-VERIFICATION.md Truth 9 and 01-REVIEW.md CR-01 as the independent findings, and names plan 01-20 and the `about-dialog-suppression` row as the closure. No sentence in the row frames the licence-link removal as only a future risk.
    - Row 11 carries both new NOT-done entries — the `about:credits`/`#communityDesc` scope decision and the authored-fixture ceiling — each with its reason and its route.
    - Row 11's half (b) (the unshipped `about-wordmark.svg`) is unchanged, and rows 1-10 and 12 are unchanged: `git diff .planning/phases/01-platform-extraction-and-rename/deferred-items.md` touches only row 11's line.
    - `.planning/WINDOWS.md` rows 15 and 16 no longer read `open` in the status column and each names its 01-UAT.md test number and the 2026-09-01 confirmation; row 19 still reads `open`.
    - `.planning/REQUIREMENTS.md` line 19 begins `- [x] **MIG-04**`, and `git diff .planning/REQUIREMENTS.md` shows exactly one changed line.
    - `node scripts/scan-brand-residue.mjs` exits **0** and `bash scripts/verify-platform.sh --quick` exits **0** after the documentation edits.
  </acceptance_criteria>

  <done>The written record matches the tree: the defect that existed is recorded as a defect, its two scope decisions are recorded rather than assumed, and the three tracking artifacts the verifier flagged as stale are current.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| upstream ESR markup → shipped branding selectors | The DOM the stylesheet targets is owned by Mozilla and rewritten on every ESR rebase. Every selector in this repo is an untrusted assumption about that document. |
| About dialog → user | The only surface in this build that discharges the aggregated open-source licence disclosure. Suppressing content here is a legal-facing decision, not a cosmetic one. |
| `upstream/` filesystem read → checker process | The checker reads a file from a git-ignored multi-gigabyte clone it does not own. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-20-01 | Information Disclosure | `powerbrowser/branding/{dev,release}/content/aboutDialog.css` | medium | mitigate | The current defect: debranding removes the product's only in-UI route to the aggregated open-source licence text, so MPL and third-party licence obligations have no shipped surface. Fixed by the href-qualified selector in Task 1; regression-locked by assertion 4 of `about-dialog-suppression`, whose fault row (a) reproduces the pre-fix selector and must go red naming `about:license`. |
| T-01-20-02 | Tampering | `upstream/browser/base/content/aboutDialog.xhtml` (rebase-rewritten) | medium | mitigate | An ESR rebase that renames a container id turns every suppression selector into a silent no-op — the CSS still parses, nothing goes red, and mozilla.org links reappear inside a Power-Browser-branded dialog. Assertion 3 (every derived selector must match at least one element) fails by name; fault row (b) plants the rename. `scripts/rebase-upstream.sh` already runs the static gates on every rebase. |
| T-01-20-03 | Spoofing | About dialog bottom row | low | mitigate | Outbound mozilla.org Terms/Privacy links presented inside a Power-Browser-branded dialog imply a vendor relationship that does not exist. This is the original G-01-3 defect; the href-qualified selector retains its suppression rather than relaxing it, and assertion 3 keeps that suppression from decaying into a no-op. |
| T-01-20-04 | Denial of Service | `about-dialog-suppression` full-set row | low | accept | The checker reads one file from `upstream/` and never walks the tree, so it cannot repeat the 55 s / 463,930-file cost measured for `scan-brand-residue --extra-root` in 01-15. Accepted without mitigation. |
| T-01-20-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install task exists in this plan. `scripts/verify-about-dialog-suppression.mjs` imports only Node builtins (`node:fs`, `node:path`, `node:os`); no dependency is added to any manifest and no lockfile changes. RESEARCH.md's Package Legitimacy Audit is therefore not consulted and no legitimacy checkpoint is required. If the executor finds itself reaching for a package (a CSS parser, a DOM library), that is the signal to stop and re-plan — not to install. |
</threat_model>

<verification>
Run in order, from the repo root, outside any Nix dev shell (all three are `node`/`bash` only):

1. `node scripts/verify-about-dialog-suppression.mjs` — exits 0.
2. `node scripts/verify-about-dialog-suppression.mjs --self-test` — exits 0, four fault rows plus a green control.
3. `cmp powerbrowser/branding/dev/content/aboutDialog.css powerbrowser/branding/release/content/aboutDialog.css` — exits 0.
4. `bash scripts/verify-platform.sh --only about-dialog-suppression` — exits 0.
5. `bash scripts/verify-platform.sh --quick` — exits 0, 25 rows.
6. `node scripts/scan-brand-residue.mjs` — exits 0 with the new script staged.
7. `git -C upstream diff --stat` — empty.

Not run here, and deliberately: `scripts/verify-platform.sh --gate` reaches the incremental
`./mach build` path (~47-54 min on the reference host) and 01-VERIFICATION.md already records it as
skipped for cost. Nothing in this plan changes a build input.

The backstop truth — that a repackaged binary's About dialog shows `Licensing Information` as the
only bottom-row link — needs `./mach build faster` and a launched binary. It is a human-verification
item, not an automated one; if no explicit evidence is produced, the verifier must abstain
(`human_needed`), never record a silent pass.
</verification>

<success_criteria>
- 01-VERIFICATION.md's Truth 9 is closed: the internal `about:license` link survives debranding in both variants while both mozilla.org bottom links stay suppressed.
- All three of the gap's named `missing:` deliverables are landed — the target selector plus a corrected comment, a registered check with a planted-fault self-test, and a corrected `deferred-items.md` row 11.
- The check derives both sides at check time and goes red on four planted faults, so it is a gate rather than a green-by-construction row.
- `--quick` is green at 25 rows; `--only` reaches the upstream-reading row; no sibling driver was created.
- `upstream/` is unmodified and no branding value is generated.
</success_criteria>

<output>
Create `.planning/phases/01-platform-extraction-and-rename/01-20-SUMMARY.md` when done. It must
record verbatim: the pre-fix RED message from `verify-about-dialog-suppression.mjs`, the shipped
function names inside that script, the four self-test fault-row labels, and the `--quick` row count
before and after.
</output>
