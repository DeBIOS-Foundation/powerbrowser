# Deferred items — phase 01

Discovered during execution, out of scope for the plan that found them.

| # | Found in | Item | Route to |
|---|---|---|---|
| 1 | 01-01 Task 2 | The DeBIOS Foundation's GitHub organisation is not established. `sourcerer/endpoint-allowlist.json:82` and `theia/extensions/branding/src/browser/sourcerer-welcome-widget.tsx:13` carry `https://github.com/Deocracy/Sourcerer`, which the mechanical vendor rename turns into `https://github.com/DeBIOS/PowerBrowser`. D-12 establishes only the `powerbrowser.org` domain, so this URL is an assumption, not a decision. | Confirm before any release; plan 01-03 or 01-06 |
| 2 | 01-01 Task 1 | Plan 01-01's acceptance criterion `grep -c 'content/powerbrowser/' … → 5` returns 6, because the `%  content powerbrowser %content/powerbrowser/` package-declaration line matches too. Re-anchor on `^   content/powerbrowser/` if the criterion is reused. | Plan 01-02, if it copies the criterion |
| 3 | 01-01 Task 1 | `sourcerer/shell/jar.mn`'s column alignment drifted by three characters on the `TheiaService.sys.mjs` line, because `content/sourcerer/` → `content/powerbrowser/` is longer. `jar.mn` is whitespace-tolerant and D-06 forbids restructuring during a rename, so it was left alone. | Cosmetic; fix opportunistically or never |
| 4 | 01-03 Task 3 | `apply-patches-self-test` cannot run in this environment: it derives its fixture from `upstream/browser/moz.configure`, and `upstream/` is the 1.1 GB git-ignored clone `scripts/fetch-upstream.sh` materialises. Confirmed pre-existing and rename-independent (01-02 recorded the same). Re-tiered out of `--quick` into the full set in `verify-platform.sh`, since a check needing a 1.1 GB fetch does not belong in a no-build gate. | Runs whenever `upstream/` exists; no action needed |
| 5 | 01-03 Task 3 | `desktop-entry-quick` reads `objdir/config.status`, i.e. a built tree, but `verify-phase-03.sh` registered it **only** under `--quick` — so it was permanently red on a fresh checkout *and* unreachable in a full run. Re-tiered into the full set, where it also becomes reachable via `--only`. First green run requires plan 01-04's build. | Plan 01-04 (first build) |
| 6 | 01-03 Task 2 | The ten branding rasters are byte-identical between `dev/` and `release/`, because both derive from the one `mark.svg` and D-13's variant split lives in the display names and the title-bar pref, not the icon. If a variant ever needs a visually distinct icon, that is a second source SVG, not a post-process. | Only if a design decision requires it |
| 7 | 01-10 Task 1 | `powerbrowserRetry()` hides the error layer with a direct `style.display = "none"` without clearing `TheiaService._errorShown`, so a Retry whose restart path then fails again re-enters `_showError`, hits its `_errorShown` early return, and leaves the user on a blank screen with the error layer hidden and never repainted. Same defect class as this plan (an outcome nobody decides), but pre-existing since 05-02 and outside 01-10's named scope: fixing it changes the error layer's visible behaviour and needs its own check, which cannot be a chrome-side click on Linux (ledger 7). The terminal handler added here routes a *rejected* retry correctly; this is the *resolved-but-failed* path. | RESOLVED by plan 01-11: `retry()` now calls `_hideError()` and `powerbrowserRetry` no longer writes the element display. The analyzer `scripts/verify-shell-error-contract.mjs`, registered as `shell-error-contract`, performs exactly the drive this row asked for -- two consecutive failing retries against the shipped supervisor and the shipped chrome bootstrap -- and was observed red on the pre-fix tree. Its perceptual half stays on the WINDOWS.md human record (ledger 20's residual paragraph), because chrome-context Marionette is platform-blocked on Linux (ledger 7). |
| 8 | 01-13 (found by 01-VERIFICATION.md gap `2d`, re-scoped by 01-REVIEW.md CR-01) | The **classification route** of item 7's defect class, and the chain's last link — item 7's "RESOLVED by plan 01-11" disposition stays accurate for the **direct-DOM-write** route it names, and this row is a different route through the same error state, not a contradiction of it. `TheiaService.retry()` re-entered `_restart()` whatever the `recoverable` classification said, and called `_hideError()` on the way in, so the unrecoverable class was re-entered by the single control the error screen offered and every such click nulled `_failureDetails`. Closed by 01-13: `_errorRecoverable` guards `retry()` in the supervisor (the authority) and `errorRetryButton.hidden` mirrors it on the control (presentation), asserted by the registered scenario `unrecoverable-classification-refuses-the-retry-click`. **Deliberately NOT done:** the `POWERBROWSER_DECK_STATE` sentinel gained no key for the Retry control's visibility. That is the named place a future tier-3 launch-level check would observe this half from a real browser log; adding the key now would be scaffolding for a check nobody is writing, and the sentinel's shape is anchored by three separate assertions. | Only if a tier-3 launch-level check is written; the node harness covers the contract half today |
| 9 | 01-14 / 01-15 (found by 01-REVIEW.md CR-01/CR-02, independently re-confirmed by 01-VERIFICATION.md as CR-A/CR-B) | **CR-A** — `scripts/verify-shell-error-copy.mjs`'s rule (4) matched `/^(?:USER_MESSAGE\.[A-Za-z_$][\w$]*\|[A-Za-z_$][\w$]*\.message)$/`, whose second alternative admitted ANY identifier's `.message` including a caught exception's, so the one gate between an arbitrary string and `#powerbrowser-error-message` structurally could not fire on the shape it existed to catch. The shipped tree was clean on its merits — every live `_showError()` call site passed a table-derived value — but a gate that cannot go red for its stated cause is decorative, not verification. Closed by **01-14**: rule (4) now binds the argument to `messageBearingBindings(src, table)`, derived from the file under test at check time, and `catchParamNames(src)` selects which rejection copy prints; asserted by two `--self-test` fault rows, each proven GREEN under a scratch copy of the pre-fix checker and RED under the fixed one. **CR-B** — the residual-brand scan invoked by `scripts/rebase-upstream.sh` after the patch replay built its file set from `git ls-files` only, while `.gitignore:20` excludes `upstream/`. So the invocation whose entire stated purpose was re-checking the tree the replay had just rewritten scanned 109 tracked files and none of them under `upstream/`, and CLAUDE.md's stated guarantee — that an upstream rebase reintroducing a brand token fails there rather than in a release — did not hold for anything in the rebased tree. Closed by **01-15**: `--extra-root <dir>` walks a filesystem root outside the git index through `extraRootFiles()`, under the one `inScanScope()` predicate `scopeFiles()` also calls, never following a symlink (`upstream/powerbrowser` links back into this repo) and never descending `.git`; absent, non-directory and empty roots all FAIL loudly; `rebase-upstream.sh` passes `--extra-root "$UPSTREAM_DIR"` on both the real and the `--dry-run` paths; four `--self-test` rows (clean control, planted token, missing root, mistyped flag) prove it can go red, and the pre-fix script was measured exiting **0** with `PASS -- 109 scanned file(s)` on the very scenario row 2 plants. **Deliberately NOT done, with reasons:** (a) *no `verify-platform.sh` row scans the live `upstream/`* — it is a git-ignored multi-gigabyte clone absent on a fresh checkout, so a row that skipped when it is missing would be green by construction, which is precisely what row 4 above records this repo already hitting with `apply-patches-self-test`; the mode reaches `--quick` through hermetic `mktemp` scratch roots instead. (b) *No new registry rows at all* — the four existing rows (`scan-brand-residue`, `scan-brand-residue-self-test`, `shell-error-copy-no-internals`, `shell-error-copy-no-internals-self-test`) already invoke both checkers and both self-tests, so both plans' work rides rows that exist; adding a fifth would be a second driver by another name. (c) *`.github/workflows/rebase-upstream.yml` gained no functional change* — its step-1 scan is deliberately the PRE-rebase, tracked-tree-only check, run before any clone exists, and `--extra-root` on an absent directory is now a hard failure; the upstream-aware scan reaches CI through the `rebase-upstream.sh` step the workflow already runs. A comment in the YAML records that so the question is not silently reopened. (d) *Rule (4)'s `catch`-parameter derivation is file-scoped, not block-scoped* — an accepted ceiling, carrying a `ponytail:` comment naming its upgrade path; it only selects which rejection message prints and never widens the accept set, so it upgrades if and only if a real false positive appears. | **Largely retired by measurement.** The residual this row was written to carry — that the `--extra-root` walk had never executed over a real multi-gigabyte Gecko tree and its wall-clock cost was unknown — was measured during 01-15 against the live 5.6 GB `upstream/` checkout on the reference host: **55 s over 463,930 files, exit 0**, no false probe hits, and `git -C upstream diff --stat` empty afterwards. That is negligible next to the clone and patch replay it follows, so no budget concern remains. What stays open is narrower: that figure is one host and one ESR tag, and it was measured over a checkout that had NOT just been rebased. Any content-based exclusion a real rebase turns out to need belongs in `inventory/brand-tokens.json`'s `scope.exclude`, never as a literal in the walker. Re-measure opportunistically at the first real CI rebase; no action otherwise |
