# Deferred items — phase 01

Discovered during execution, out of scope for the plan that found them.

| # | Found in | Item | Route to |
|---|---|---|---|
| 1 | 01-01 Task 2 | The DeBIOS Foundation's GitHub organisation is not established. `sourcerer/endpoint-allowlist.json:82` and `theia/extensions/branding/src/browser/sourcerer-welcome-widget.tsx:13` carry `https://github.com/Deocracy/Sourcerer`, which the mechanical vendor rename turns into `https://github.com/DeBIOS/PowerBrowser`. D-12 establishes only the `powerbrowser.org` domain, so this URL is an assumption, not a decision. | Confirm before any release; plan 01-03 or 01-06 |
| 2 | 01-01 Task 1 | Plan 01-01's acceptance criterion `grep -c 'content/powerbrowser/' … → 5` returns 6, because the `%  content powerbrowser %content/powerbrowser/` package-declaration line matches too. Re-anchor on `^   content/powerbrowser/` if the criterion is reused. | Plan 01-02, if it copies the criterion |
| 3 | 01-01 Task 1 | `sourcerer/shell/jar.mn`'s column alignment drifted by three characters on the `TheiaService.sys.mjs` line, because `content/sourcerer/` → `content/powerbrowser/` is longer. `jar.mn` is whitespace-tolerant and D-06 forbids restructuring during a rename, so it was left alone. | Cosmetic; fix opportunistically or never |
| 4 | 01-03 Task 3 | `apply-patches-self-test` cannot run in this environment: it derives its fixture from `upstream/browser/moz.configure`, and `upstream/` is the 1.1 GB git-ignored clone `scripts/fetch-upstream.sh` materialises. Confirmed pre-existing and rename-independent (01-02 recorded the same). Re-tiered out of `--quick` into the full set in `verify-platform.sh`, since a check needing a 1.1 GB fetch does not belong in a no-build gate. | Runs whenever `upstream/` exists; no action needed |
| 5 | 01-03 Task 3 | `desktop-entry-quick` reads `objdir/config.status`, i.e. a built tree, but `verify-phase-03.sh` registered it **only** under `--quick` — so it was permanently red on a fresh checkout *and* unreachable in a full run. Re-tiered into the full set, where it also becomes reachable via `--only`. First green run requires plan 01-04's build. | Plan 01-04 (first build) |
| 6 | 01-03 Task 2 | The ten branding rasters are byte-identical between `dev/` and `release/`, because both derive from the one `mark.svg` and D-13's variant split lives in the display names and the title-bar pref, not the icon. If a variant ever needs a visually distinct icon, that is a second source SVG, not a post-process. | Only if a design decision requires it |
| 7 | 01-10 Task 1 | `powerbrowserRetry()` hides the error layer with a direct `style.display = "none"` without clearing `TheiaService._errorShown`, so a Retry whose restart path then fails again re-enters `_showError`, hits its `_errorShown` early return, and leaves the user on a blank screen with the error layer hidden and never repainted. Same defect class as this plan (an outcome nobody decides), but pre-existing since 05-02 and outside 01-10's named scope: fixing it changes the error layer's visible behaviour and needs its own check, which cannot be a chrome-side click on Linux (ledger 7). The terminal handler added here routes a *rejected* retry correctly; this is the *resolved-but-failed* path. | RESOLVED by plan 01-11: `retry()` now calls `_hideError()` and `powerbrowserRetry` no longer writes the element display. The analyzer `scripts/verify-shell-error-contract.mjs`, registered as `shell-error-contract`, performs exactly the drive this row asked for -- two consecutive failing retries against the shipped supervisor and the shipped chrome bootstrap -- and was observed red on the pre-fix tree. Its perceptual half stays on the WINDOWS.md human record (ledger 20's residual paragraph), because chrome-context Marionette is platform-blocked on Linux (ledger 7). |
| 8 | 01-13 (found by 01-VERIFICATION.md gap `2d`, re-scoped by 01-REVIEW.md CR-01) | The **classification route** of item 7's defect class, and the chain's last link — item 7's "RESOLVED by plan 01-11" disposition stays accurate for the **direct-DOM-write** route it names, and this row is a different route through the same error state, not a contradiction of it. `TheiaService.retry()` re-entered `_restart()` whatever the `recoverable` classification said, and called `_hideError()` on the way in, so the unrecoverable class was re-entered by the single control the error screen offered and every such click nulled `_failureDetails`. Closed by 01-13: `_errorRecoverable` guards `retry()` in the supervisor (the authority) and `errorRetryButton.hidden` mirrors it on the control (presentation), asserted by the registered scenario `unrecoverable-classification-refuses-the-retry-click`. **Deliberately NOT done:** the `POWERBROWSER_DECK_STATE` sentinel gained no key for the Retry control's visibility. That is the named place a future tier-3 launch-level check would observe this half from a real browser log; adding the key now would be scaffolding for a check nobody is writing, and the sentinel's shape is anchored by three separate assertions. | Only if a tier-3 launch-level check is written; the node harness covers the contract half today |
| 9 | 01-14 / 01-15 (found by 01-REVIEW.md CR-01/CR-02, independently re-confirmed by 01-VERIFICATION.md as CR-A/CR-B) | **CR-A** — `scripts/verify-shell-error-copy.mjs`'s rule (4) matched `/^(?:USER_MESSAGE\.[A-Za-z_$][\w$]*\|[A-Za-z_$][\w$]*\.message)$/`, whose second alternative admitted ANY identifier's `.message` including a caught exception's, so the one gate between an arbitrary string and `#powerbrowser-error-message` structurally could not fire on the shape it existed to catch. The shipped tree was clean on its merits — every live `_showError()` call site passed a table-derived value — but a gate that cannot go red for its stated cause is decorative, not verification. Closed by **01-14**: rule (4) now binds the argument to `messageBearingBindings(src, table)`, derived from the file under test at check time, and `catchParamNames(src)` selects which rejection copy prints; asserted by two `--self-test` fault rows, each proven GREEN under a scratch copy of the pre-fix checker and RED under the fixed one. **CR-B** — the residual-brand scan invoked by `scripts/rebase-upstream.sh` after the patch replay built its file set from `git ls-files` only, while `.gitignore:20` excludes `upstream/`. So the invocation whose entire stated purpose was re-checking the tree the replay had just rewritten scanned 109 tracked files and none of them under `upstream/`, and CLAUDE.md's stated guarantee — that an upstream rebase reintroducing a brand token fails there rather than in a release — did not hold for anything in the rebased tree. Closed by **01-15**: `--extra-root <dir>` walks a filesystem root outside the git index through `extraRootFiles()`, under the one `inScanScope()` predicate `scopeFiles()` also calls, never following a symlink (`upstream/powerbrowser` links back into this repo) and never descending `.git`; absent, non-directory and empty roots all FAIL loudly; `rebase-upstream.sh` passes `--extra-root "$UPSTREAM_DIR"` on both the real and the `--dry-run` paths; four `--self-test` rows (clean control, planted token, missing root, mistyped flag) prove it can go red, and the pre-fix script was measured exiting **0** with `PASS -- 109 scanned file(s)` on the very scenario row 2 plants. **Deliberately NOT done, with reasons:** (a) *no `verify-platform.sh` row scans the live `upstream/`* — it is a git-ignored multi-gigabyte clone absent on a fresh checkout, so a row that skipped when it is missing would be green by construction, which is precisely what row 4 above records this repo already hitting with `apply-patches-self-test`; the mode reaches `--quick` through hermetic `mktemp` scratch roots instead. (b) *No new registry rows at all* — the four existing rows (`scan-brand-residue`, `scan-brand-residue-self-test`, `shell-error-copy-no-internals`, `shell-error-copy-no-internals-self-test`) already invoke both checkers and both self-tests, so both plans' work rides rows that exist; adding a fifth would be a second driver by another name. (c) *`.github/workflows/rebase-upstream.yml` gained no functional change* — its step-1 scan is deliberately the PRE-rebase, tracked-tree-only check, run before any clone exists, and `--extra-root` on an absent directory is now a hard failure; the upstream-aware scan reaches CI through the `rebase-upstream.sh` step the workflow already runs. A comment in the YAML records that so the question is not silently reopened. (d) *Rule (4)'s `catch`-parameter derivation is file-scoped, not block-scoped* — an accepted ceiling, carrying a `ponytail:` comment naming its upgrade path; it only selects which rejection message prints and never widens the accept set, so it upgrades if and only if a real false positive appears. | **Largely retired by measurement.** The residual this row was written to carry — that the `--extra-root` walk had never executed over a real multi-gigabyte Gecko tree and its wall-clock cost was unknown — was measured during 01-15 against the live 5.6 GB `upstream/` checkout on the reference host: **55 s over 463,930 files, exit 0**, no false probe hits, and `git -C upstream diff --stat` empty afterwards. That is negligible next to the clone and patch replay it follows, so no budget concern remains. What stays open is narrower: that figure is one host and one ESR tag, and it was measured over a checkout that had NOT just been rebased. Any content-based exclusion a real rebase turns out to need belongs in `inventory/brand-tokens.json`'s `scope.exclude`, never as a literal in the walker. Re-measure opportunistically at the first real CI rebase; no action otherwise |
| 10 | 01-16 / 01-17 (gap-closure for 01-VERIFICATION.md's re-opened Truth 7 and Truth 8; findings from 01-REVIEW.md CR-01/CR-02/CR-03, independently re-reproduced by the verifier) | **CR-03** — `scripts/verify-shell-error-copy.mjs`'s rule (4) derived its accept set from the tree (01-14) but still ENUMERATED its call sites with a regex requiring a literal receiver and a trailing comma, so any `_showError()` call outside that shape was never examined rather than rejected, and the only completeness guard fired solely when every site disappeared at once. Closed by **01-16**: the call-site total is now derived independently of the matching regex and equality between the two counts is asserted, with fault rows planting the two shapes the regex structurally cannot read. **CR-01** — the `--extra-root` pass introduced by 01-15 called `scan()` with no `rows` argument, so it reused the full inventory including the three `coincidental` rows. `claimOccurrences` orders rows longest-token-first, so the 28-character row holding this checkout's own absolute source path won over the shorter `brand-identifier` row nested inside it, wrote the whole span into `taken[]`, classified the occurrence as a class `offensesOf` does not count, AND suppressed the independent residue probe at that same index. Over the tracked tree that is safe only because `reconcile()` condition 2 asserts the row's observed count is zero; the extra-root pass deliberately does not run `reconcile()`, so nothing constrained it there — and the likeliest residue in a replayed tree, an absolute source or objdir path baked into a mozconfig, a patch header or a generated build file, is exactly that shape. Closed by **01-17**: the extra-root `scan()` call filters the `coincidental` class out of its row set and only that class, with the reason stated at the call site, asserted by a fault row planting 01-REVIEW.md's literal reproduction. **CR-02** — `scan()`'s per-file read was wrapped in a bodyless `catch { continue; }` justified as "a path git tracks but this checkout does not materialise", which covers only the `git ls-files` provenance; every extra-root path came from a `readdirSync` that had just reported it as an existing regular file, so a throw there is EACCES, EISDIR or a read past the V8 string limit. The file was skipped, no failure was recorded, and it was still counted in the reported extra-root total, so the operator was told a file was scanned that was never opened. Closed by **01-17**: `scan()` collects every unreadable file as `{ file, reason }` on both of its return shapes, the extra-root caller fails the gate on any of them naming the path and the reason code, the tracked-tree caller fails on any reason other than `ENOENT`, and the reported count names files actually READ rather than files walked. **WR-01 and WR-02**, folded into 01-16 because they are the same silent-drop class in the same file: a `message:` key nested below depth 0 no longer makes a binding message-bearing, and `parseUserMessageTable` now asserts its own parse is total rather than returning whatever it happened to recognise. **IN-01, IN-02 and IN-04**, folded into 01-17: `--extra-root` now resolves a relative argument against the repo root, the same base `--report` uses, so the CLI's two path flags share one base; the root's symlink-following `statSync` against the walk's entry-level lstat semantics is documented as a deliberate asymmetry rather than changed, since making a symlinked root a hard failure would be a behaviour change smuggled in under a comment-level finding; and the `--self-test` header now records that every CLI-spawning row runs the full tracked-tree scan as a side effect, so a dirty working tree can turn one red and the clean control above them must be read first. **Deliberately NOT done, with reasons.** *WR-03, WR-04, WR-05 and WR-06* all live in `scripts/rebase-upstream.sh` or `.github/workflows/rebase-upstream.yml`, none appears in the verifier's `missing[]`, and none is the same edit as a Critical; taking them here would have widened this run into two more files of warning-tier hardening while the blocking class was still open for the third consecutive pass. Individually: **WR-04** is the one worth doing first, because a `--tag '*'` passes validation and then reaches the `rm -rf` of the upstream tree before failing inside `git clone` — the damage is a re-clonable git-ignored checkout rather than anything unrecoverable, which is why it is a warning and not a critical; **WR-03** is a narrow `pipefail`/early-exit `grep` race that misattributes a valid tag as missing; **WR-05** is a one-line quoting error that makes the dry-run rehearsal print a command that fails when pasted, annoying precisely because the dry run is the only locally exercised path; **WR-06** is CI hardening — the job carries no `permissions:`, no `timeout-minutes:` and no `persist-credentials: false`. *IN-03* — `FAULTS` in `verify-shell-error-copy.mjs` hand-keeps exact copy literals from the file under test. Not done because the current behaviour is fail-loud rather than silent: a reworded string makes `mutated === original` and the existing guard reports that the row proves nothing. That is drift friction, not a defeatable gate, and it is a different class from everything this run closed. *The tracked-tree `ENOENT` allowance is retained*, because a path git tracks which this checkout does not materialise is the legitimate documented case and turning it into a failure would make the gate red on a sparse or partial checkout. *No new `verify-platform.sh` registry rows and no sibling driver* — the four existing rows already invoke both checkers and both self-tests, and CLAUDE.md's one-driver-one-registry rule is the whole reason `verify-phase-0{2,3,4,5}.sh` were deleted in the commit that created it. *No row scans the live `upstream/`*, carried unchanged from row 9 and from row 4's precedent. *The four `human_verification` items in 01-VERIFICATION.md remain open* — the GUI-01 window walkthrough, the GUI-03 visible restyle, the two tier-3 regression re-confirmations, and the perceptual Retry-gate confirmation — because chrome-context Marionette is platform-blocked on Linux (WINDOWS.md ledger item 7) and the tier-3 items need a repackaged binary; they are human-record items, not plannable tasks, and this run did not touch them. *GUI-02 stays deferred to v2* at the D-22 gate: not scored, not orphaned, not reopened. | Two residuals. **(a)** The unreadable-file gate has only ever been exercised against a planted scratch fixture. A real ESR rebase over a live multi-gigabyte checkout is the first time it runs against a tree nobody in this repo wrote, where a clone artefact, a permissions oddity or a source file past the V8 string limit would turn it red for a reason unrelated to residual brand strings. No measurement exists. If that happens the answer is a content-based exclusion in `inventory/brand-tokens.json`'s `scope.exclude` — the derived source both file-set paths already read — never a literal in the walker and never a downgrade of the gate to a warning. **(b)** `--self-test` run as root will go RED on the unreadable-file row by design, because a mode-000 file is readable by root and the row fails rather than skips when it cannot establish its own precondition. This repo's CI runs on `ubuntu-latest` as a non-root user and local development runs as a non-root user, so no current caller is affected. If a root-running caller ever appears, the fix is a plant mechanism that is unreadable regardless of uid, never a silent skip — a skip is the exact failure class this run closed three times |
| 11 | 01-18 (gap-closure for UAT gap G-01-3) | Two deliberate not-dones from the About-dialog link suppression. **(a) The stock link rows are HIDDEN, not REMOVED from the DOM.** `powerbrowser/branding/{dev,release}/content/aboutDialog.css` now carries `display: none` for `#communityDesc`, `#contributeDesc` and `#bottomBox > hbox`, which is the whole of upstream's branding-side suppression channel: every one of those hrefs is a literal attribute in `upstream/browser/base/content/aboutDialog.xhtml`, no pref is consulted, and the branding stylesheet is the third and last entry of that file's linkset. Deleting the elements instead would require a new regenerated Gecko patch against `aboutDialog.xhtml`, which the debug session (`.planning/debug/about-dialog-stock-links.md`) ratified as the heavier path to take only if hidden-not-removed fails UAT. The rows are not reachable by keyboard or screen reader while hidden, so the practical difference is that the markup still exists in a chrome document nobody can see, and that a future ESR rebase changing the container ids would silently un-hide them — which the `--quick` packaging assertion added by this plan does NOT catch, because packaging is intact in that scenario and only the selectors have gone stale. **(b) `chrome://branding/content/about-wordmark.svg` is still unshipped.** That is why Task 1 REMOVED the wordmark-positioning block from both stylesheets rather than carrying it into a now-live file: the moment the stylesheet loaded, its 64px top padding would have reserved an empty band above the version text for an image that 404s. Upstream's own `aboutDialog.css` still sets the `background-image` for that box, so the box is present and empty by upstream's rules, not ours; only our padding is gone. | **(a)** A follow-up regenerated Gecko patch against `aboutDialog.xhtml`, ONLY if the human check rejects hidden-not-removed. If the selectors ever go stale against a newer ESR, the fix is a check that asserts the suppressed ids still exist in the upstream markup, not a wider selector. **(b)** Phase 3's icon pipeline, which ships the wordmark and restores the positioning block with it |
