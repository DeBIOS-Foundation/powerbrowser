---
phase: 01-platform-extraction-and-rename
plan: 10
type: execute
wave: 10
depends_on: ["01-09"]
gap_closure: true
files_modified:
  - powerbrowser/shell/TheiaService.sys.mjs
  - powerbrowser/shell/powerbrowser.js
  - scripts/verify-platform.sh
  - scripts/verify-start-path-recovery.mjs
  - .planning/WINDOWS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [MIG-04, GUI-01]

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A rejection escaping the supervisor's start path can no longer end as an unhandled promise rejection in chrome. Every fire-and-forget entry point onto the supervisor — the chrome bootstrap's start call, the Retry button's call, and the two long-lived loops the supervisor starts without awaiting — carries a terminal handler, and all four route to the ONE handler so there is a single place the outcome is decided."
    - "That handler paints a static, product-named sentence taken from the supervisor's existing USER_MESSAGE table, with the Retry and Details controls already on screen — never an invented string, never a raw exception message. It lives in `TheiaService.sys.mjs` specifically because that is the file `shell-error-copy-no-internals` derives the table from; copy authored in the chrome bootstrap would be outside every check that exists."
    - "The three unguarded throw sites the gap names each stop being an escape hatch. The settings-folder step and the credential-cookie step each produce the same `{ok, recoverable, message, details}` classified-result shape every other failure in `_spawnAndGate` returns, so they inherit the give-up budget, the retry path and the diagnostics rows rather than bypassing all three."
    - "The startup leftover-reap step is guarded too, but classified as non-fatal and logged rather than surfaced: it is best-effort cleanup of a PREVIOUS launch's process, and failing this launch because a stale pid could not be signalled would turn a cosmetic cleanup miss into a dead screen. Nothing is lost — the reap decision already lands in the ring buffer that the diagnostics layer renders."
    - "Every identifier the user-facing sentence deliberately drops reappears as a labelled diagnostics row for these new failure paths, exactly as it does for every pre-existing one, read through the single `getFailureDetails()` accessor so the rendered surface and the announced sentinel cannot disagree."
    - "A registered check drives a REAL throw out of the start path — not a simulated one — and requires the error layer to paint: the error sentinel appears exactly once, its reason names the product, and the deck-state line accompanying it does not report the error layer as hidden. The check was observed red before the terminal handler landed and green after."
    - "The static half of the recovery contract derives, from the supervisor source, the set of methods that return a promise, and requires every call to one of them in the chrome bootstrap to carry a terminal handler. Adding a new such method and calling it unhandled goes red; the set is never hand-kept in the checker."
    - "MIG-04 — the renamed tree's boot path has no state in which the user is looking at a screen that will never change with no way to act. Either the interface loads, or a product-named error with a working Retry and a populated Details layer is on screen."
    - "GUI-01 — the shell remains reachable and actionable after a start-path failure, so the browser-window toggle stays reachable rather than being stranded behind a dead loading layer."
    - "UI-SPEC E2 (error) — the painted copy is two-part plain language, what failed plus what to do next, with Retry and Details as on-screen affordances and no internal identifier in the string."
    - "UI-SPEC E2 (overflow / long-text) — the new failures reuse the existing error layer, so the `max-width: 40em` wrapping contract holds unchanged and no message is truncated or ellipsised."
    - "UI-SPEC E3 (partial) — rows render only for identifiers the failing path actually has, so a failure with no port contributes no empty-labelled port row. The new failure paths supply only the identifiers they hold."
    - statement: "The Retry control on the error layer re-enters the same restart path and is a real affordance for these new failure classes, not a decorative one. Whether a retry after a settings-folder permission failure actually succeeds depends on the host condition clearing, which no automated check on this host can arrange; the affordance's presence and wiring are checked, its eventual success is not."
      verification: backstop
    - statement: "The two long-lived supervisor loops now carry terminal handlers, but no registered check drives a rejection out of either loop — doing so would require injecting a fault into a mid-session code path that has no external control surface. Their handlers are asserted by the same source-derived coverage rule as the bootstrap's call sites, not by a runtime red."
      verification: backstop
  prohibitions:
    - statement: "MUST NOT author a new user-facing error sentence anywhere outside the supervisor's USER_MESSAGE table. Copy written in the chrome bootstrap, or an ad-hoc literal passed to the error layer, is outside `shell-error-copy-no-internals`'s derivation entirely — it would ship unchecked and re-open the exact leak class 01-07 closed."
      category: transparency
      status: resolved
      verification: judgment
      resolution: null
      reason: null
    - statement: "MUST NOT paint an error state for a backend that is in fact healthy. The terminal handler exists for rejections that escape, not as a timeout on the loading layer; a handler that fires on a slow but succeeding start would trade a stuck screen for a false alarm and destroy the user's trust in the message."
      category: values
      status: resolved
      verification: judgment
      resolution: null
      reason: null
    - statement: "MUST NOT put a raw exception message, pref key, sentinel name, port or timeout into the sentence the user reads. Those belong in the diagnostics rows, which is where the phase already decided they go; a next-step sentence a user cannot act on is worse than the stuck screen it replaced because it looks like it was addressed."
      category: transparency
      status: resolved
      verification: judgment
      resolution: null
      reason: null
    - statement: "MUST NOT close, downgrade, or automate WINDOWS.md ledger items 15 and 16. Both are perceptual verifications no human has performed; an automated proxy for either reports a green nobody earned, which is strictly worse than an open ledger row that still names the outstanding work."
      category: safety
      status: resolved
      verification: judgment
      resolution: null
      reason: null
  artifacts:
    - powerbrowser/shell/TheiaService.sys.mjs
    - powerbrowser/shell/powerbrowser.js
    - scripts/verify-platform.sh
    - scripts/verify-start-path-recovery.mjs
    - .planning/WINDOWS.md
  key_links:
    - "chrome bootstrap's start call → the supervisor's terminal-failure handler → the existing `_showError` path → the error layer and the diagnostics sentinel"
    - "the Retry button's supervisor call → the same terminal handler (a Retry that rejects must not be silent either)"
    - "the two fire-and-forget supervisor loops → the same terminal handler — one handler, four attachment points, no per-site logic"
    - "the settings-folder and credential-cookie throw sites → the classified-result shape → `_restart()`'s give-up budget → `_showError`'s message and detail rows"
    - "`scripts/verify-start-path-recovery.mjs`'s promise-returning-method derivation → every call to one of those methods in the chrome bootstrap (set-derived on both sides, never a hand-kept method list)"
---

<objective>
Close the error-affordance half of 01-VERIFICATION.md's one FAILED must-have. `powerbrowser.js:231`
calls `TheiaService.start(browserElement)` with no `await` and no `.catch()`, and three reachable
throw sites inside that start path are unguarded — so any of them becomes an unhandled promise
rejection in chrome with the identical user-visible outcome the state-gating defect produced: a dead
loading screen, no message, no Retry, no Details.

Plan 01-09 fixed the state-gating half and left the loading layer reachable again. This plan makes
the failure case survivable: a rejection anywhere in the start path paints a product-named sentence
from the existing copy table with the Retry and Details controls that are already on screen, and the
two throw sites that can carry useful classification produce the same result shape every other
failure in `_spawnAndGate` produces.

Scope boundary: WINDOWS.md ledger items 15 (GUI-01 perceptual walkthrough) and 16 (GUI-03 visible
restyle) are the two open human-verification halves. They are NOT in scope, and no task here may
substitute an automated proxy for either — the ledger rows are updated to reflect this phase's new
state, never closed.

Purpose: MIG-04's "boots" clause and GUI-01's reachability both require that a start-path failure
end in something a user can act on.
Output: one terminal handler with four attachment points, three guarded throw sites, one new runtime
check, one source-derived static rule, and a reconciled ledger.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md

@.planning/phases/01-platform-extraction-and-rename/01-VERIFICATION.md
@.planning/phases/01-platform-extraction-and-rename/01-REVIEW.md
@.planning/phases/01-platform-extraction-and-rename/01-UI-SPEC.md
@.planning/phases/01-platform-extraction-and-rename/01-09-SUMMARY.md
@powerbrowser/shell/TheiaService.sys.mjs
@powerbrowser/shell/powerbrowser.js
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces (plan 01-10)

**New symbol in `powerbrowser/shell/TheiaService.sys.mjs`**
- `reportUnexpectedFailure(err)` — the single public terminal-failure handler. Public because the
  chrome bootstrap attaches it; it is the only new member of the supervisor's public surface.

**New attachment points (no new symbols, four call sites)**
- `powerbrowser/shell/powerbrowser.js` — terminal handler on the supervisor's start call.
- `powerbrowser/shell/powerbrowser.js` — terminal handler on the Retry control's supervisor call.
- `powerbrowser/shell/TheiaService.sys.mjs` — terminal handler on the fire-and-forget health loop.
- `powerbrowser/shell/TheiaService.sys.mjs` — terminal handler on the fire-and-forget recovery probe loop.

**New shell function in `scripts/verify-platform.sh`**
- `check_start_failure_shows_error` — the real-throw runtime check.

**New registry label in `scripts/verify-platform.sh`** (appended row, D-21 — no sibling driver)
- `start-failure-shows-error` (full set — needs a built binary)

**Extended surface on `scripts/verify-start-path-recovery.mjs`** (created by plan 01-09)
- a source-derived terminal-handler coverage assertion in the default (static) mode;
- the corresponding planted fault in `--self-test`.

No new USER_MESSAGE table entry is minted — the existing `couldNotStart` value already satisfies the
copywriting contract for an unclassified start-path failure, and adding an unreferenced key would
itself go red under `shell-error-copy-no-internals`'s declared-versus-referenced equality.

No new `PowerBrowserAPI` method and no new Firefox internal touchpoint, so
`powerbrowser/INTERNAL-APIS.md` gains no row.
</artifacts_this_phase_produces>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Drive a real throw out of the start path, watch it disappear silently, then give the supervisor one terminal handler and attach it at all four fire-and-forget entry points</name>

  <precondition>`objdir/dist/bin/powerbrowser` exists and is executable, and `nix develop .#firefox` resolves — this task launches the built binary before and after the edit and repackages chrome JS with `./mach build faster` between the two runs. Plan 01-09's supervisor edit must already be packaged, so the red observed here is caused by the missing terminal handler and not by the state-gating defect 01-09 removed.</precondition>

  <reversibility rating="costly">`start-failure-shows-error` is a registry label, and labels are the contract `--only` and every later citation resolves against. Fix it now; renaming it later silently breaks citations.</reversibility>

  <files>powerbrowser/shell/TheiaService.sys.mjs, powerbrowser/shell/powerbrowser.js, scripts/verify-platform.sh</files>

  <read_first>
    - `powerbrowser/shell/TheiaService.sys.mjs` — the file being modified. Read `USER_MESSAGE` (around line 41) and its header comment, which states the contract the new handler must satisfy; `_showError` (around line 865) and the `_errorShown` guard; `_fatal` and `_pushLog` including the token-redaction comment; the fire-and-forget call to the health loop inside `_spawnAndGate`'s one-time block; and the fire-and-forget call to the recovery probe loop in `_startRecoveryProbe` (around line 839).
    - `powerbrowser/shell/powerbrowser.js` — the file being modified. Read the supervisor import and the un-awaited start call (around line 226-231), and the Retry global (around line 116-119). Note that the error layer's Retry and Details controls exist and are wired already — this task adds no UI.
    - `scripts/verify-shell-error-copy.mjs` — the gate the new handler must satisfy. Its rule 4 accepts a `this._showError(` whose first argument is a `USER_MESSAGE.<key>` reference or a result object's `.message`, and nothing else; its rule 3 requires declared and referenced key sets to be EQUAL, which is why no new table entry is minted here.
    - `.planning/phases/01-platform-extraction-and-rename/01-UI-SPEC.md` — `## Copywriting Contract` and the error-copy rewrite table, which fix what the painted sentence may and may not contain.
    - `scripts/verify-platform.sh` — `check_shell03_unrecoverable_immediate_error` (around line 889) is the closest existing check to the one being added: it plants an unusable pref, launches, and polls for the error sentinel. Read `start_shell` (around line 293) for how `VERIFY05_XDG_CONFIG_HOME` reaches the launched binary, and THE REGISTRY block (around line 3233).
    - `powerbrowser/shell/PowerBrowserAPI.sys.mjs` — the `ensureDirectory` wrapper (around line 78), to confirm which platform call the planted fault has to defeat.
  </read_first>

  <behavior>
    Fail-first, on the same binary, with only the chrome source differing between the two runs:

    - RED: with `check_start_failure_shows_error` written and the terminal handler NOT yet added,
      `scripts/verify-platform.sh --only start-failure-shows-error` MUST exit non-zero because no
      error sentinel ever appears — the rejection is swallowed. The launch log must show the shell
      alive and no error state, which is precisely the dead-screen symptom the gap describes.
      Record that output verbatim.
    - GREEN: after the handler and its four attachments land and the tree is repackaged, the
      identical command MUST exit 0, with exactly one error sentinel whose reason names the product.
    - If the RED run instead shows an error sentinel already appearing, STOP: the planted fault is
      being caught by something else and the check is not exercising the path it claims. Diagnose
      before proceeding; do not accept a green that was green before the fix.
  </behavior>

  <action>
Write the runtime check, observe the silent failure, then add one handler and attach it four times.

**Step 1 — `check_start_failure_shows_error` in `scripts/verify-platform.sh`.** Model it on
`check_shell03_unrecoverable_immediate_error`. The fault must make the supervisor's settings-folder
creation step fail while everything before it succeeds, so the rejection originates inside the start
path rather than at the earlier sidecar-resolution step (which already has a classified failure path
and would prove nothing).

Build two `mktemp -d` directories, both registered with `track_temp`:
  - a stub backend directory holding a trivial entry file that merely stays alive. It exists only so
    the sidecar-resolution step passes its existence check; it is never actually spawned, because
    the fault fires first.
  - a poisoned config home. Plant the fault by creating a regular FILE at the path the supervisor
    will try to create its settings folder at — that is the config home plus the product's own
    directory name, which `_resolveConfigDir` derives from the config-home environment variable.
    If that fault does not fire on this host (the platform call may tolerate it), fall back to
    making the config home itself unwritable with `chmod 500` and record in the SUMMARY which of the
    two faults was used and why.

Write a user.js pointing `powerbrowser.sidecar.backendMain` at the stub entry file, export it as
`VERIFY05_USER_JS_PROFILE` and export the poisoned directory as `VERIFY05_XDG_CONFIG_HOME` around
the `start_shell` call, then unset both immediately after, exactly as the neighbouring checks do.

Poll `$BROWSER_LOG` for the error sentinel with a bounded deadline and the same
`kill -0 "$BROWSER_SPAWN_PID"` early-exit guard the shell03 checks use. Then assert:
  - the error sentinel is present and appears exactly once (the `_errorShown` guard holds for this
    path too);
  - its JSON matches the established `{reason, recoverable}` shape and carries no additional key —
    reuse the anchored pattern `check_shell03_budget_exhausted_error` already applies, so a shape
    change is caught here as it is there;
  - the reason names the product in its display form, which is the copywriting contract's minimum
    and is cheap to assert positively;
  - the deck-state line whose `where` field is the error does not report the error layer as
    hidden — the same positive control the budget-exhausted check carries, so a sentinel that
    reported hidden unconditionally, or a computed-style read that does not work headless, cannot
    satisfy this check forever.
Call `stop_shell` on every exit path and dump the log to stderr on failure. Append the row
`start-failure-shows-error|check_start_failure_shows_error` to the registry's full set (it launches
a built binary) with the provenance comment convention the neighbouring NEW rows use.

**Step 2 — observe RED.** Sync the built tree first with
`nix develop .#firefox --command bash -c "cd upstream && MOZCONFIG=../.mozconfig ./mach build
faster"`, then run `scripts/verify-platform.sh --only start-failure-shows-error`. Require exit
non-zero for the reason `<behavior>` states, and record the output verbatim.

**Step 3 — the terminal handler.** Add one public method to `TheiaService`, named
`reportUnexpectedFailure`, taking the rejection value. It writes the full diagnostic text to the
log through the existing `_fatal` (that sink keeps everything the user-facing sentence drops, and
`_pushLog`'s token redaction already covers derived text), then calls the existing `_showError` with
`USER_MESSAGE.couldNotStart` as the message, a recoverable classification, and detail rows carrying
the failed-step label and the rejection's own message text. Pass `USER_MESSAGE.couldNotStart` as a
direct reference, not through a local variable or a parameter: `verify-shell-error-copy.mjs`'s rule
4 resolves the first argument syntactically, and an indirection would fail it.

Do NOT mint a new table entry. The existing `couldNotStart` value already satisfies the copywriting
contract for an unclassified start-path failure, and a new key would have to be referenced somewhere
to survive the declared-versus-referenced equality — which for a single generic backstop means
inventing a distinction a user cannot act on, exactly what the table's header comment says several
paths deliberately collapse to avoid.

The handler needs no defensive guard on the browser element: the element is assigned synchronously
in `start()` before the first await, so no rejection can reach here with it unset. Say so in a
comment rather than adding a guard whose true branch is unreachable.

**Step 4 — attach it at all four fire-and-forget entry points.** In the chrome bootstrap, attach a
terminal handler to the supervisor's start call and to the Retry control's supervisor call. In the
supervisor, attach the same handler to the fire-and-forget health-loop call inside `_spawnAndGate`'s
one-time block and to the fire-and-forget recovery-probe-loop call in `_startRecoveryProbe`. All
four route to the one handler with no per-site logic — four promise roots need four attachments
because there is no shared root to guard, but there is exactly one place the outcome is decided.

The two loop attachments are a strict superset of what the gap's `missing:` list names, deliberately
so: they are the same defect class (a promise root with no terminal handler) reachable through the
same supervisor, and leaving them while fixing the other two would leave the bug alive on the path
a mid-session outage takes.

Per D-08, keep every inherited decision-ID citation in the comments you touch (D-105, D-114, D-115,
D-121, SHELL-03) verbatim — amend the prose around a citation, never remove or renumber it.

**Step 5 — observe GREEN.** Repackage with the same tier-2 command and re-run the identical
`--only start-failure-shows-error` invocation; require exit 0. Then run
`scripts/verify-platform.sh --quick` and require it green — in particular
`shell-error-copy-no-internals` and its self-test, which are the gates the new handler's copy has to
survive. Then re-run `scripts/verify-platform.sh --only health-gate-recovery-swaps` and require it
still green, so this plan is proven not to have disturbed plan 01-09's fix.

Stage every touched file before trusting the residual-brand scan.
  </action>

  <verify>
    <automated>nix develop .#firefox --command bash -c "cd upstream &amp;&amp; MOZCONFIG=../.mozconfig ./mach build faster" &amp;&amp; scripts/verify-platform.sh --only start-failure-shows-error &amp;&amp; scripts/verify-platform.sh --only health-gate-recovery-swaps &amp;&amp; scripts/verify-platform.sh --quick</automated>
  </verify>

  <acceptance_criteria>
    - The SUMMARY records the RED observation verbatim: `scripts/verify-platform.sh --only start-failure-shows-error` exited non-zero on the pre-handler build with no error sentinel in the launch log, and the SUMMARY names which of the two planted faults was used.
    - After the handler and attachments land, `scripts/verify-platform.sh --only start-failure-shows-error` exits 0.
    - `grep -c 'reportUnexpectedFailure' powerbrowser/shell/powerbrowser.js` is exactly 2 — the start call and the Retry call.
    - `grep -c 'reportUnexpectedFailure' powerbrowser/shell/TheiaService.sys.mjs` is at least 3 — the declaration plus the health-loop and recovery-probe-loop attachments.
    - `node scripts/verify-shell-error-copy.mjs` exits 0 and `node scripts/verify-shell-error-copy.mjs --self-test` exits 0 — the new `_showError` call site resolves to a declared table entry and the declared/referenced key sets are still equal.
    - The USER_MESSAGE table gained no entry: `git diff powerbrowser/shell/TheiaService.sys.mjs` shows no added or removed line inside the `const USER_MESSAGE = {` block. State the declared-entry count before and after in the SUMMARY.
    - `scripts/verify-platform.sh --only health-gate-recovery-swaps` still exits 0 — plan 01-09's fix is intact.
    - `scripts/verify-platform.sh --quick` exits 0 with zero `: FAIL` lines.
    - `bash scripts/check-internals-boundary.sh` exits 0 and `git diff --stat powerbrowser/INTERNAL-APIS.md` is empty — no new internal touchpoint was opened.
    - `git -C upstream diff --quiet` succeeds — the pinned upstream checkout is untouched.
  </acceptance_criteria>

  <done>
    A real throw out of the supervisor's start path now paints a product-named sentence with Retry
    and Details on screen instead of vanishing into an unhandled rejection, proven by a registered
    check that was observed red before the handler existed. All four fire-and-forget entry points
    onto the supervisor route to that one handler.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Give the three named throw sites the classified-result shape, so each failure carries its own step and its own diagnostics rows instead of falling to the generic backstop</name>

  <reversibility rating="reversible">Three try/catch blocks and their result objects; revertible in isolation.</reversibility>

  <files>powerbrowser/shell/TheiaService.sys.mjs, scripts/verify-platform.sh</files>

  <read_first>
    - `powerbrowser/shell/TheiaService.sys.mjs` — the file being modified, as Task 1 left it. Read `_spawnAndGate`'s existing failure returns (the spawn failure around line 484, the stdin-handshake failure around line 519, the pinned-port and readiness failures around lines 549-594, the health-gate failure around line 624) — those are the shape to match exactly: `{ ok, recoverable, message, details }` with `message` a direct `USER_MESSAGE.<key>` reference and `details` an array of `[label, value]` pairs. Read `_resolveSidecar`'s returns for the `start()`-side variant of the same idea, and `_reapLeftover` in full (around line 343) including its own best-effort comments.
    - `powerbrowser/shell/PowerBrowserAPI.sys.mjs` — `ensureDirectory`, `setSessionCookie`, and `signalBarePid`. Note that `signalBarePid` opens libc OUTSIDE its own try block, which is the reachable throw in the reap path; the catch it already has does not cover that line.
    - `scripts/verify-shell-error-copy.mjs` — rules 2 and 4. Every `message:` property in the supervisor source must be spelled as a literal `USER_MESSAGE.<key>` reference; property shorthand and a message passed through a parameter both slip past the derivation, which is a hole this task must not widen.
    - `.planning/phases/01-platform-extraction-and-rename/01-UI-SPEC.md` — the error-copy rewrite table, which already maps a spawn-side failure and a credential-handshake failure onto existing sentences.
    - `scripts/verify-platform.sh` — `check_start_failure_shows_error` as Task 1 left it, and `check_shell_diagnostics_rows_populated` (around line 1285) for how a diagnostics-row assertion is expressed against a launch log.
  </read_first>

  <behavior>
    Each of the three sites gets a different, deliberate classification, and the difference is the
    point of this task rather than an inconsistency:

    - Settings-folder creation fails → recoverable classified result → the error layer, with a
      failed-step row naming the settings-folder step and a row carrying the platform error text.
      A permissions or disk condition can clear, so Retry is a real affordance.
    - Session-cookie minting fails → recoverable classified result returned FROM `_spawnAndGate`,
      before the navigation, so the launch is left un-completed and `_restart()`'s next attempt
      re-runs the whole one-time block rather than skipping it. This is the state-gating fix from
      plan 01-09 paying off: the completion field is still false, so the retry is not poisoned.
    - Leftover-reap fails → logged and execution continues to the spawn. This is a REASONED
      DEVIATION from the gap's `missing:` wording, which asks for a classified result at all three
      sites. Reap is best-effort cleanup of a PREVIOUS launch's process; failing this launch over
      it converts a cosmetic cleanup miss into the dead screen this whole gap is about. The
      escape-hatch defect is closed either way — the site is guarded and the outcome is decided —
      and nothing is lost, because the reap decision already lands in the ring buffer the
      diagnostics layer renders. Record the deviation explicitly in the SUMMARY.
  </behavior>

  <action>
Guard the three sites named in 01-VERIFICATION.md's `missing:` list.

**Site 1 — settings-folder creation, in `start()`.** Wrap the `PowerBrowserAPI.ensureDirectory` call
in a try/catch. On catch, build the same `{ ok, recoverable, message, details }` object the
`_spawnAndGate` failures build, with `message` spelled as a direct `USER_MESSAGE.couldNotStart`
reference and details rows carrying a plain-language failed-step label for the settings-folder step
plus the platform error text. Route it to `_showError` the way `start()` already routes
`_resolveSidecar`'s failure result, then return. Classify it recoverable — unlike the resolve step's
two failure classes, a directory that cannot be created today can be creatable after the user fixes
a permission or frees space, which is exactly what Retry is for.

**Site 2 — the leftover reap, in `start()`.** Wrap the `_reapLeftover()` call in a try/catch whose
catch writes the failure through the supervisor's existing dual-channel reap log helper (ring buffer
plus stdout, so the decision is independently observable from both the diagnostics layer and the
launch log) and then falls through to the spawn. Comment the classification decision at the site,
naming why this one is non-fatal while site 1 is fatal-to-this-attempt, so the asymmetry reads as
deliberate rather than as an oversight.

**Site 3 — session-cookie minting, in `_spawnAndGate`'s one-time block.** Wrap the
`PowerBrowserAPI.setSessionCookie` call in a try/catch. On catch, mark the supervisor not-healthy,
write the diagnostic through `_fatal`, and RETURN the classified failure result — recoverable, with
`message` spelled as a direct `USER_MESSAGE.couldNotStart` reference and rows naming the credential
step and the error text. Returning rather than continuing is load-bearing: the navigation must not
run for a launch whose cookie was never set, because the backend would refuse every request and the
user would land on a blank frame instead of an actionable error. Leaving the return before the
navigation also leaves the completion field false, so `_restart()`'s next attempt re-runs the whole
block — which is only true because plan 01-09 re-keyed that block; do not reorder the cookie and the
navigation.

Per D-08, keep the inherited decision-ID citations in the comments you touch (D-98, D-104, D-110,
D-111, D-113, SIDE-04) verbatim.

**Step 4 — extend the runtime check with a rows assertion.** In `check_start_failure_shows_error`,
add an assertion on the diagnostics sentinel the chrome bootstrap writes alongside the error
sentinel: parse its rows array and require a row whose label is the failed-step label and whose
value names the settings-folder step. This is the criterion that distinguishes this task's work from
Task 1's — Task 1 proved an error paints at all, this proves the failure carries its own
classification rather than the generic backstop's. Keep both assertions; neither subsumes the other.

Stage everything before running the residual-brand scan.
  </action>

  <verify>
    <automated>nix develop .#firefox --command bash -c "cd upstream &amp;&amp; MOZCONFIG=../.mozconfig ./mach build faster" &amp;&amp; scripts/verify-platform.sh --only start-failure-shows-error &amp;&amp; scripts/verify-platform.sh --only health-gate-recovery-swaps &amp;&amp; scripts/verify-platform.sh --quick</automated>
  </verify>

  <acceptance_criteria>
    - `scripts/verify-platform.sh --only start-failure-shows-error` exits 0 AND its assertions now include the diagnostics-row check; the SUMMARY quotes the parsed rows array from the passing run showing the settings-folder step row.
    - In `powerbrowser/shell/TheiaService.sys.mjs`, each of the three named sites is lexically inside a `try` block: reading the file confirms a `try {` opens before the settings-folder call, before the leftover-reap call, and before the session-cookie call, with a matching `catch` in each case. State the three line numbers in the SUMMARY.
    - `node scripts/verify-shell-error-copy.mjs` exits 0 — every new `message:` property is spelled as a literal `USER_MESSAGE.<key>` reference, so the derivation still resolves all of them, and no shorthand property was introduced.
    - `node scripts/verify-shell-error-copy.mjs --self-test` exits 0 — all six of its planted faults still go red naming the drift against the edited file.
    - The session-cookie catch returns before the navigation: in `_spawnAndGate`, the `return` inside that catch appears on an earlier line than the navigation helper call in the same block.
    - The reap deviation is recorded in the SUMMARY with its reasoning, naming it as a deviation from the gap's `missing:` item 3 wording and stating why the escape hatch is nonetheless closed.
    - `scripts/verify-platform.sh --only health-gate-recovery-swaps` still exits 0.
    - `scripts/verify-platform.sh --quick` exits 0 with zero `: FAIL` lines.
  </acceptance_criteria>

  <done>
    Each of the three named throw sites has a decided outcome. Two produce the same classified
    result shape every other `_spawnAndGate` failure produces and reach the user as a named step in
    the diagnostics layer; the third is guarded, logged, and deliberately non-fatal, with the
    reasoning recorded.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Derive the terminal-handler coverage rule from the tree, plant its fault, and reconcile the ledger and roadmap</name>

  <reversibility rating="reversible">One additional derivation in an existing script plus documentation rows.</reversibility>

  <files>scripts/verify-start-path-recovery.mjs, .planning/WINDOWS.md, .planning/ROADMAP.md</files>

  <read_first>
    - `scripts/verify-start-path-recovery.mjs` — the file being extended, as plan 01-09 left it. Its static mode, its `--file`/`--shell-file` overrides and its `--self-test` fault array are all already in place; this task adds one derivation and one fault.
    - `powerbrowser/shell/TheiaService.sys.mjs` and `powerbrowser/shell/powerbrowser.js` — the two derivation sources, as Tasks 1 and 2 left them.
    - `scripts/verify-shell-error-copy.mjs` — the `FAULTS`/`runSelfTest` shape to extend consistently.
    - `.planning/WINDOWS.md` — the ledger. Read rows 15 and 16 (the two open perceptual verifications) and row 17 (a worked example of how a `fixed` resolution is written: what changed, proven how, and what residual it explicitly does NOT close).
    - `.planning/ROADMAP.md` — Phase 1's `**Plans:**` line and plan list, which needs the two new plans appended.
    - `.planning/phases/01-platform-extraction-and-rename/01-VERIFICATION.md` — the `missing:` list, to reconcile item by item in the SUMMARY.
  </read_first>

  <behavior>
    The coverage rule is a set derivation on both sides, not a hand-kept method list:

    - Derivation A: from the supervisor source, the set of method names declared with the `async`
      keyword — every one of them returns a promise.
    - Derivation B: from the chrome bootstrap source, every call expression on the supervisor object
      whose method name is in set A.
    - Assertion: each call in B carries a terminal handler on the same statement. Adding a new
      promise-returning supervisor method and calling it unhandled from the bootstrap goes red;
      renaming an existing one keeps the check green because both sides move together.
    - Vacuity guard: if B is empty, fail. A rule that finds no call sites asserts nothing, which is
      how this class of check silently stops working.

    Planted faults, each required to go red naming the drift, against the clean control already in
    the self-test:
    - a terminal handler stripped from one of the bootstrap's supervisor calls;
    - every such call removed from the bootstrap copy, so the vacuity guard fires.
  </behavior>

  <action>
**Step 1 — the coverage derivation.** Add the two derivations described in `<behavior>` to the
static mode of `scripts/verify-start-path-recovery.mjs`. Strip comments before deriving, exactly as
`verify-shell-error-copy.mjs` does and for the same reason: a commented-out example call must not
count as a real one. Name, in each failure message, which call site lacks a handler, so the output
says what drifted rather than that something did.

**Step 2 — plant the faults.** Add the two faults to the existing `--self-test` array, mutating a
temporary copy of the chrome bootstrap and re-invoking through the existing `--shell-file` override.
Follow the reference implementation's convention that a fault whose mutation did not apply is
reported as a failure of that row.

**Step 3 — prove the discrimination once by hand.** Temporarily strip the terminal handler from the
bootstrap's start call in the working tree, run `scripts/verify-platform.sh --only
start-path-recovery`, confirm it goes red naming that call site, then restore. Record both outputs.

**Step 4 — reconcile the ledger.** Append a WINDOWS.md row for this gap in the established shape: a
`deviation` (or the kind the file's own vocabulary uses for a shipped defect found in verification)
naming the supervisor's state-keying conflation and the unguarded start path, with a resolution that
states what changed, how it was proven — the two registered checks and the red each was observed to
produce before its fix — and what residual it explicitly does NOT close. The residual to name
honestly: no registered check drives a rejection out of the two long-lived supervisor loops, so
those two terminal handlers rest on the source-derived coverage rule rather than on a runtime red.

Leave rows 15 and 16 OPEN. They are the GUI-01 and GUI-03 perceptual verifications, and nothing in
either gap-closure plan performs or substitutes for them. If either row's text is now inaccurate
because of what these two plans changed, amend the text; do not change the status.

**Step 5 — reconcile the roadmap.** Update Phase 1's `**Plans:**` count and append the two new plan
entries to its plan list, matching the existing entries' format. Do not edit any other phase's
section, and do not touch REQUIREMENTS.md's status table — requirement status is the verifier's
call on evidence, not the planner's or the executor's.

**Step 6 — the full static gate.** Run `scripts/verify-platform.sh --quick` and require it green,
then run the two new runtime rows by label and require both green. Stage everything and run the
residual-brand scan in both modes.
  </action>

  <verify>
    <automated>node scripts/verify-start-path-recovery.mjs --self-test &amp;&amp; scripts/verify-platform.sh --quick &amp;&amp; scripts/verify-platform.sh --only start-failure-shows-error &amp;&amp; scripts/verify-platform.sh --only health-gate-recovery-swaps</automated>
  </verify>

  <acceptance_criteria>
    - `node scripts/verify-start-path-recovery.mjs --self-test` exits 0 and reports at least 10 planted faults (plan 01-09's set plus this task's two) plus a green clean control.
    - `node scripts/verify-start-path-recovery.mjs` exits 0 against the working tree.
    - The hand-proven red is recorded in the SUMMARY: with one terminal handler stripped, `scripts/verify-platform.sh --only start-path-recovery` exited non-zero and its output named the specific call site that lacked a handler.
    - `scripts/verify-platform.sh --quick` exits 0 with zero `: FAIL` lines and its summary table contains `start-path-recovery: PASS` and `start-path-recovery-self-test: PASS`.
    - `scripts/verify-platform.sh --only start-failure-shows-error` and `--only health-gate-recovery-swaps` both exit 0.
    - `.planning/WINDOWS.md` contains a new row for this gap whose resolution names both registered checks and states the loop-handler residual; `grep -c '| open |' .planning/WINDOWS.md` still counts rows 15 and 16 among the open items (state the before and after open-row counts in the SUMMARY).
    - `.planning/ROADMAP.md`'s Phase 1 section lists `01-09-PLAN.md` and `01-10-PLAN.md` and its plan count matches the number of entries in its own list.
    - `git diff --stat .planning/REQUIREMENTS.md` is empty — requirement status was not self-certified.
    - `node scripts/scan-brand-residue.mjs` and `node scripts/scan-brand-residue.mjs --reconcile` both exit 0 with every touched file staged.
    - `ls scripts/verify-phase-*.sh` finds nothing — D-21's consolidation is intact and no sibling driver was created by either gap-closure plan.
  </acceptance_criteria>

  <done>
    The terminal-handler contract is a tree-derived rule that goes red on a stripped handler and on
    a vacuous derivation, proven by hand and by plant. The ledger records the defect, its proof and
    its named residual, and the two perceptual verifications remain open and un-substituted.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rejection value → user-facing surface | An arbitrary exception's `message` now flows into a diagnostics detail row and, via `_fatal`, into the ring buffer the diagnostics layer renders. |
| chrome supervisor → backend HTTP origin | The session cookie's minting step gains a failure branch that must not leave the launch half-initialised. |
| verification fixture → host filesystem | The new check plants a poisoned config home and may `chmod` a directory. |

## STRIDE Threat Register (OWASP ASVS L1; block on `high`)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-06 | Information Disclosure | `reportUnexpectedFailure` detail rows and `_fatal` output | high | mitigate | The rejection's text reaches the diagnostics layer through the existing `_showError` detail mapping, which already applies the per-launch token redaction to every value, and the ring buffer through `_pushLog`, which applies the same redaction. No new sink is introduced. `shell04-log-redacts-token` is required green in every task's `--quick` criterion. |
| T-01-07 | Information Disclosure | the painted sentence | high | mitigate | The sentence is a direct reference to an existing USER_MESSAGE entry, never derived text, so no pref key, sentinel, port, timeout or exception text can reach it. `shell-error-copy-no-internals` and its self-test are required green in Tasks 1 and 2, and its rule 4 resolves the `_showError` first argument syntactically — an indirection would fail it rather than slip through. |
| T-01-08 | Spoofing | session-cookie failure branch | high | mitigate | The cookie catch returns before the navigation, so a launch whose credential was never minted never navigates the content browser to the backend origin. The alternative — continuing past a failed cookie — would present an unauthenticated browser to a fail-closed backend and paint a blank frame rather than an actionable error. |
| T-01-09 | Denial of Service | terminal handler on the recovery-probe loop | medium | accept | A rejection escaping the recovery loop now paints the error state and stops that loop rather than being swallowed. The user retains the Retry control, which re-enters the same restart path with a fresh budget, so the failure remains user-recoverable. |
| T-01-10 | Tampering | `check_start_failure_shows_error` fixture | low | mitigate | Both planted directories are `mktemp -d` paths registered with `track_temp`, never a persisted repo file and never a path outside the temp root. The `chmod` fallback applies only to the fixture's own temp directory, and the fixture is torn down on every exit path. |
| T-01-11 | Repudiation | the leftover-reap non-fatal branch | low | accept | A failed reap is now logged rather than silent, on the dual channel (ring buffer plus stdout) the reap path already uses, so a cleanup miss is attributable after the fact instead of invisible. |
| T-01-SC | Tampering | npm/pip/cargo installs | high | mitigate | This plan adds no package-manager install task and no dependency; the extended script uses only `node:` builtins, as every existing `scripts/verify-*.mjs` does. The package-legitimacy gate is not triggered, no `[ASSUMED]`/`[SUS]` package enters the tree, and no human-verify checkpoint is owed. Reaching for a dependency here is a signal to stop and re-scope. |
</threat_model>

<verification>
Run in this order — every static gate before anything that costs a build:

1. `node scripts/verify-start-path-recovery.mjs --self-test` — exit 0.
2. `node scripts/verify-shell-error-copy.mjs` and `--self-test` — both exit 0.
3. `scripts/verify-platform.sh --quick` — exit 0, zero `: FAIL` rows.
4. `bash scripts/check-internals-boundary.sh` and `--catalogue` — exit 0, with `INTERNAL-APIS.md` unchanged.
5. `node scripts/scan-brand-residue.mjs` and `--reconcile` — both exit 0, with everything staged.
6. `nix develop .#firefox --command bash -c "cd upstream && MOZCONFIG=../.mozconfig ./mach build faster"` — tier 2.
7. `scripts/verify-platform.sh --only start-failure-shows-error` — exit 0.
8. `scripts/verify-platform.sh --only health-gate-recovery-swaps` — exit 0 (plan 01-09's fix intact).
9. `git -C upstream diff --quiet` — the pinned upstream checkout is untouched.

Human verification: none is added by this plan. WINDOWS.md ledger items 15 and 16 remain the two
outstanding perceptual verifications and are deliberately not automated here.
</verification>

<success_criteria>
- A real throw out of the supervisor's start path paints a product-named sentence with Retry and Details on screen, proven by a registered check observed red before the handler existed.
- All four fire-and-forget entry points onto the supervisor route to one terminal handler.
- The settings-folder and credential-cookie failures each carry their own named step into the diagnostics rows; the leftover-reap failure is guarded, logged and deliberately non-fatal with the deviation recorded.
- The terminal-handler coverage rule derives both of its sets from the tree and goes red on a stripped handler and on a vacuous derivation.
- Every one of 01-VERIFICATION.md's four `missing:` items is reconciled item by item in the SUMMARY, naming which plan and task closed it.
- WINDOWS.md ledger items 15 and 16 are still open, un-substituted, and their text is accurate.
</success_criteria>

<output>
Create `.planning/phases/01-platform-extraction-and-rename/01-10-SUMMARY.md` when done.

The SUMMARY must include:
- an item-by-item reconciliation table against 01-VERIFICATION.md's four `missing:` entries, naming the plan and task that closed each;
- the RED and GREEN outputs of `--only start-failure-shows-error`, verbatim;
- the hand-planted RED of `--only start-path-recovery` and the restored green;
- the reap-site deviation and its reasoning;
- which of the two planted config-home faults the runtime check ended up using, and why.
</output>
