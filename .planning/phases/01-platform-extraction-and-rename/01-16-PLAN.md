---
phase: 01-platform-extraction-and-rename
plan: 16
type: execute
wave: 1
depends_on: []
gap_closure: true
autonomous: true
requirements: [MIG-01, MIG-02, MIG-03, MIG-04, GUI-01, GUI-03, GUI-04, SEC-01]
files_modified:
  - scripts/verify-shell-error-copy.mjs

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "scripts/verify-shell-error-copy.mjs derives the TOTAL number of `_showError(` call sites present in the file under test independently of the argument-parsing regex — every textual `_showError(` in the comment-stripped source minus its one definition — and requires the enumeration regex's `callSites` to equal that total. This is the same derive-and-compare set-equality discipline checks (2) and (3) in this file already use, applied to the step that decides what gets checked at all."
    - "When the two counts disagree the check fails loudly and NAMES the gap: how many call sites are present, how many were parsed, and that a call site this check cannot read is a call site it is not checking. It does not report a count silently and it does not pass."
    - "A `--self-test` row plants a `_showError(` call site with no trailing comma and requires the check to go red for a call site it cannot read. Before this plan that exact input exits 0 — `01-REVIEW.md` CR-03 reproduced it and `01-VERIFICATION.md` re-reproduced it independently."
    - "A second `--self-test` row plants a `_showError(` call site with an optional-chaining receiver (`this?._showError`) and requires the same red. Two shapes, because the pre-existing regex requires two independent things (a literal `this.` receiver AND a trailing comma) and one row can only prove one of them."
    - "`messageBearingBindings` shape (a) tests for a `message:` property only at depth 0 of an object-literal initializer. A `message:` nested inside a sub-object or an array element no longer marks the outer binding as message-bearing, because `<binding>.message` is `undefined` at runtime in that case — the file's own doc comment already states that a binding whose `.message` would paint nothing must not be waved through (WR-01, reproduced by `01-REVIEW.md` as exiting 0)."
    - "`parseUserMessageTable` asserts its own parse is total: the number of depth-1 `key:` lines declared inside the `USER_MESSAGE` block equals the number of entries the entry regex produced, and a shortfall fails loudly naming the count. A declared-but-unparsed entry is otherwise invisible to check (1) (never leak-scanned) and to check (3) (which iterates the parsed entries), so it is a silent drop of exactly the kind CR-03 is (WR-02)."
    - "Each of the four new fault rows is observed RED against the fixed checker and GREEN against a scratch copy of the pre-fix checker restored from git. A row that behaves identically under both proves nothing and is not evidence."
    - "`node scripts/verify-shell-error-copy.mjs` against the real `powerbrowser/shell/TheiaService.sys.mjs` still exits 0: the file's 5 call sites are all parsed, the derived total is also 5, and the tightened `messageBearingBindings` and `parseUserMessageTable` produce no new failure on the shipped tree."
    - "`node scripts/verify-shell-error-copy.mjs --self-test` exits 0 reporting 12 planted faults — the 8 pre-existing rows unchanged plus the 4 new ones — and every row goes red naming its own drift."
    - "`scripts/verify-platform.sh --quick` exits 0 with `shell-error-copy-no-internals` and `shell-error-copy-no-internals-self-test` both PASS. No fifth registry row is added: those two rows already invoke this checker and its self-test, and a sibling driver is forbidden by CLAUDE.md's one-driver-one-registry rule."
    - "01-UI-SPEC.md `## UI Considerations`, E2 error message, `long-text`, status ✅ covered: the painted message is always a fixed `USER_MESSAGE` literal, and a caught exception's `.message` is a diagnostics field row, never user-facing copy — enforced by `shell-error-copy-no-internals` rejecting a `this._showError()` call that passes one. This plan is the repair that makes that enforcement hold for call shapes the enumeration previously could not see."
  artifacts:
    - "scripts/verify-shell-error-copy.mjs — an independent call-site total derivation and an equality assertion against the enumeration regex's count, inside check (4)"
    - "scripts/verify-shell-error-copy.mjs — a depth-0 property filter used by `messageBearingBindings` shape (a)"
    - "scripts/verify-shell-error-copy.mjs — a totality assertion on `parseUserMessageTable`'s entry parse"
    - "scripts/verify-shell-error-copy.mjs — four new `FAULTS` rows (comma-less call site, optional-chained call site, nested `message:` binding, unparsable table entry), taking the self-test from 8 planted faults to 12"
  key_links:
    - "the textual `_showError(` total derived from the comment-stripped source -> the enumeration regex's `callSites` counter -> the equality assertion -> `fail()` (the link 01-VERIFICATION.md records as PARTIALLY WIRED: the accept side is sound, the enumeration feeding it is unproven)"
    - "the four new FAULTS rows -> the pre-existing `shell-error-copy-no-internals-self-test` registry row in scripts/verify-platform.sh (the rows reach --quick through a row that already exists)"
    - "`stripComments()` -> the derived total (the total must be counted over the SAME comment-stripped source the enumeration regex runs over, or the eight doc-comment mentions of `_showError` in TheiaService.sys.mjs would inflate it and the check would be permanently red)"
    - "`messageBearingBindings`'s depth-0 filter -> `braceBody()` (the existing brace scanner is the source of the body being filtered; the filter is a second pass over that same string, not a second scanner)"
  prohibitions:
    - statement: "MUST NOT satisfy the equality assertion by widening the enumeration regex until today's five call sites happen to match again. Widening the regex is another unproven expectation; the assertion is the deliverable and the regex may stay exactly as it is."
    - statement: "MUST NOT hand-keep a list of accepted call shapes, identifier names, or `USER_MESSAGE` keys anywhere in this file. CLAUDE.md: derive from the tree and compare. A hand-kept list can only ever agree with the tree it was copied from."
    - statement: "MUST NOT weaken, rename, or remove any of the 8 pre-existing FAULTS rows, and MUST NOT relax check (1), (2), (3) or the accept-side derivation 01-14 landed."
    - statement: "MUST NOT edit powerbrowser/shell/TheiaService.sys.mjs. The file under test is read-only for this plan; a fix that changes the code to fit the checker inverts the gate."
    - statement: "MUST NOT add a verify-platform.sh registry row or create a sibling verification driver. scripts/verify-platform.sh is the one registry and verify-phase-0{2,3,4,5}.sh were deleted in the commit that created it."
    - statement: "MUST NOT assert on the absence of a log line, and MUST NOT accept a self-test row whose planted fault fails to apply. The existing `mutated === original` guard already reports that a row proves nothing; it stays."
    - statement: "MUST NOT introduce a generator, template, or derive-at-build-time helper for any branding value. Phase 2's acceptance test is byte-identity against what Phase 1 wrote by hand."
---

<objective>
Close **CR-03** from `01-VERIFICATION.md`'s first `gaps` entry (Truth 7 — "no raw internal
identifier, pref key, or exception message can reach the user-facing error layer, enforced by a check
that cannot be defeated"), and fold in the two Warning-tier findings that sit in the same file and are
the same defect class.

01-14 correctly closed CR-A by deriving rule (4)'s ACCEPT set from the file under test. It left the
step BEFORE that untouched: the regex `/this\._showError\(\s*([^,]+?)\s*,/g` that decides which call
sites are examined at all. That regex requires a literal `this.` receiver AND a comma after the first
argument. A call site matching neither is not rejected — it is never seen. The only completeness
guard is `callSites === 0`, which fires only when EVERY call site disappears, so 5 sites parsed out of
6 present is indistinguishable from a clean run.

Today's file parses 5 of 5, so there is no live leak. That is luck about comma placement, not an
invariant. `01-REVIEW.md` reproduced the defeat and `01-VERIFICATION.md` re-reproduced it
independently: appending a comma-less `this._showError(err.message)` to a scratch copy of
`TheiaService.sys.mjs` exits **0**.

**Scope fence.** This does not touch the accept-side derivation 01-14 landed, does not touch checks
(1), (2) or (3), and does not touch `TheiaService.sys.mjs`. The gap is scoped to the enumeration step
and to two sibling silent-drop paths in the same file.

Purpose: CLAUDE.md's verification doctrine is "derive from the tree and compare; do not hand-keep an
expectation list", and gives the reason — a hand-kept list can only ever agree with the tree it was
copied from. An enumeration regex whose completeness nothing asserts is that same failure in the
shape of a pattern rather than a list. This is the third consecutive verification pass to find this
class in this codebase, and each time in the code the previous pass shipped.

Output: an independently derived call-site total compared for equality against the enumeration's
count, failing loudly and naming the gap; a depth-0 filter on `messageBearingBindings` shape (a); a
totality assertion on `parseUserMessageTable`; and four new `FAULTS` rows, each observed red against
the fixed checker and green against the pre-fix one.

**Requirement mapping for this plan:** CR-03 maps to **MIG-04** — `01-VERIFICATION.md` scores MIG-04
NOT SATISFIED specifically because "a browser whose error-copy safety gate can be silently defeated
is not proven to work as an actual web browser under CLAUDE.md's own bar" — and to **SEC-01**, whose
fail-closed backend is the source of the diagnostics identifiers this gate keeps out of user-facing
copy. The remaining IDs on the `requirements` field are carried per the phase contract established by
plans 01-14 and 01-15; MIG-01 / MIG-02 / MIG-03 are addressed by plan 01-17 and GUI-01 / GUI-03 /
GUI-04 are unaffected by this plan.
</objective>

<planning_dispositions>
Decisions this plan makes explicitly rather than leaving open.

- **Spec-less probe fallback: SKIPPED, recorded.** Phase 01 has no plain `01-SPEC.md` (only
  `01-UI-SPEC.md`), so `EDGE_ABSENT=1` and `PROHIB_ABSENT=1` and the default-ON probe fallback would
  normally fire over the phase's eight requirement IDs. It is skipped for this gap-closure run, on
  the same recorded grounds plan 01-15 used: this is a replan of an already-executed phase, and
  `01-VERIFICATION.md`'s two `missing[]` lists are the authoritative, verifier-authored predicate
  source for the work in scope. Probing MIG-01…MIG-04 / GUI-01 / GUI-03 / GUI-04 / SEC-01 would
  re-derive edges for functionality already delivered and verified across plans 01-01…01-13 — noise
  against this plan's scope rather than coverage of it. `must_haves.truths` above is derived directly
  from CR-03's two `missing[]` predicates.
- **`## UI Considerations` lift: one row bears on this plan, and it is lifted.**
  `01-UI-SPEC.md`'s `## UI Considerations` table has an E2 error-message `long-text` row at status
  ✅ covered whose resolution text names this exact checker: the painted message is always a fixed
  `USER_MESSAGE` literal, a caught exception's `.message` is a diagnostics field row, and
  `shell-error-copy-no-internals` rejects a `this._showError()` call that passes one. That row is
  lifted verbatim in substance as the last `must_haves.truths` entry, because this plan is what makes
  the enforcement it asserts hold for call shapes the enumeration could not previously see. The three
  🧪 backstop rows in that table (E5 window count, E6 URL-less web tab, E6 frame-refused site) are
  perceptual GUI checks with no bearing on a static Node checker, so no backstop marker is carried
  into this plan's `must_haves`. No row is dropped silently.
- **WR-01 and WR-02: FOLDED IN, deliberately.** Neither appears in `01-VERIFICATION.md`'s
  `missing[]`, so neither is required work. Both are folded because they meet the stated fold-in
  test — same file, same defect class, cheap — and because the class here is specifically "a
  derivation that silently drops input". WR-02's fix is literally the same edit shape as CR-03's
  (count the input independently, require equality, fail naming the shortfall); doing one and not the
  other in the same file in the same pass would be arbitrary. WR-01 is a reproduced bypass of the
  same must-have — `01-REVIEW.md` gives a working input that exits 0 — and it contradicts the doc
  comment directly above the code it fixes.
- **IN-03: DEFERRED, with reason.** `FAULTS` hand-keeps exact copy literals from the file under test
  ("Power Browser's interface didn't finish starting.", `nodeMissing`, `couldNotStart`). Deriving the
  substitution targets from `parseUserMessageTable` is real work in the self-test harness, and the
  current behaviour is already FAIL-LOUD, not silent: a reworded string makes `mutated === original`
  and the existing guard prints that the row proves nothing. A fail-loud drift cost is a different
  class from the silent drops this plan closes, so it is drift friction rather than a defeatable
  gate. Recorded in ledger row 10 (plan 01-17, Task 3) with its route-to.
- **No new `verify-platform.sh` registry row.** `shell-error-copy-no-internals` and
  `shell-error-copy-no-internals-self-test` already invoke this checker and its `--self-test`, so all
  four new fault rows reach `--quick` through rows that already exist. Adding a fifth would be a
  second driver by another name, which CLAUDE.md's Verification section forbids by the same reasoning
  that deleted `verify-phase-0{2,3,4,5}.sh`.
- **The enumeration regex itself is NOT widened.** The deliverable is the assertion, not a broader
  pattern. Widening the regex to cover `this?.` and comma-less shapes would swap one unproven
  expectation for a slightly larger unproven expectation and would still not fail on the sixth shape
  nobody thought of. With the equality assertion in place, an unparsed call site is a hard, named
  failure that tells the developer to make the call shape parsable or teach the check the new shape —
  a decision a human makes, not one a regex guesses.
- **Wave 1, no dependencies.** This plan touches exactly one file, which plan 01-17 does not touch.
  It runs first because 01-17's Task 3 writes the ledger row recording BOTH plans' dispositions, and
  because both plans carry `scripts/verify-platform.sh --quick` exiting 0 as an acceptance criterion —
  two parallel executors would each observe that 24-check run against the other's half-landed tree.
  That is the same honest sequencing reason 01-15 recorded, and it still holds.
- **Reversibility: reversible.** Three additive assertions and four additive self-test rows inside one
  static checker; `git revert` restores the prior behaviour with no downstream migration and no state
  to unwind. No `checkpoint:decision` is earned.
- **API coverage:** no external API, SDK, or service is integrated — this plan modifies one local
  static Node script. No `COVERAGE.md` matrix is produced, and the absence is a decision rather than
  an undecided hole.
- **Assumption delta: `no-change`.** The detector fired on the word "alternative" inside 01-14's
  ROADMAP prose describing rule (4)'s old regex alternatives. That is a false positive on prose, not
  a singular→plural transition in a domain concept: nothing in this plan promotes a one-of to a
  many-of. The one genuine pluralization in this gap-closure run is `unreadable[]` in plan 01-17, and
  it is net-new collection state introduced by CR-02's own fix rather than a promotion of an existing
  singular. Primary noun unchanged; no data shape is generalised here.
- **Schema push:** no ORM or schema files exist in this tree. Not applicable, and no `[BLOCKING]`
  schema task is invented.
- **Package legitimacy:** no npm/pip/cargo install task is in scope. No legitimacy checkpoint is
  earned, and none may be silently skipped later on that basis.
</planning_dispositions>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Make the call-site enumeration prove its own completeness, and prove it can go red</name>

  <read_first>
    - `scripts/verify-shell-error-copy.mjs` — the file being modified. Read it in full. Specifically:
      `stripComments()` (lines 83-89) and the fact that `check()` runs every regex over the
      comment-stripped `src`, never over `raw` — the derived total must be counted over that same
      string or the doc-comment mentions of `_showError` in the file under test would inflate it and
      make the check permanently red; `check()`'s structure (lines 206-341) and its four numbered
      sections; rule (4) at lines 296-341, including the enumeration regex at line 303, the
      `callSites` counter, and the `callSites === 0` guard at 336-341 which is the only completeness
      assertion today; checks (2) and (3) at lines 250-292, which are the set-equality discipline this
      task copies; `messageBearingBindings` and `catchParamNames`, which rule (4)'s accept side uses
      and which this task does not change; the `fail()` helper and how a failure reaches the exit
      code; and the `FAULTS` array plus `runSelfTest()` at lines 348-460, whose `apply`/`expect` shape
      the new rows must follow and whose `mutated === original` guard already reports a row that
      proves nothing.
    - `powerbrowser/shell/TheiaService.sys.mjs` — the file under test, READ ONLY. `grep -n '_showError'`
      it before starting: there are 13 textual mentions, of which 5 are real call sites (lines 168,
      228, 929, 937, 1036), 1 is the method definition (line 1131), and 7 are inside doc comments that
      `stripComments()` removes. Confirm those numbers yourself against the live file — the derived
      total must equal 5 on the shipped tree or the check goes red on a clean file.
    - `.planning/phases/01-platform-extraction-and-rename/01-VERIFICATION.md` — the first `gaps`
      entry and its two `missing[]` predicates. That list is the acceptance source this task is scored
      against, verbatim.
    - `.planning/phases/01-platform-extraction-and-rename/01-REVIEW.md` §CR-03 — the three call shapes
      that exit 0 today and the reproduction command. Two of them become the new fault rows; the
      reproduction command becomes an acceptance criterion.
    - `.planning/phases/01-platform-extraction-and-rename/01-14-PLAN.md` — the plan that landed the
      accept-side derivation this task sits on top of, and the model for how this repo words a
      red-before/green-after discrimination control.
    - `scripts/verify-platform.sh` — the one registry. `grep -n 'shell-error-copy' scripts/verify-platform.sh`
      shows the two rows at lines 3544-3545 that already invoke this checker and its self-test. No
      third row is added by this task.
    - `CLAUDE.md` — the Verification section: one driver and one registry; never assert on the absence
      of a log line; derive from the tree and compare rather than hand-keeping an expectation list;
      every new check gets a `--self-test` that plants faults and requires each one to go red naming
      the drift. Also the User-facing copy section, which is the contract this checker enforces.
  </read_first>

  <files>scripts/verify-shell-error-copy.mjs</files>

  <behavior>
    Written as the failing expectations BEFORE the assertion exists. Each becomes a `FAULTS` row or an
    acceptance criterion below.

    - Given the real `powerbrowser/shell/TheiaService.sys.mjs`, the derived call-site total is 5, the
      enumeration parses 5, the counts agree, and the check exits 0 exactly as it does today.
    - Given a copy of that file with `this._showError(err.message)` appended — no comma after the
      first argument and no later comma anywhere in the file — the check exits non-zero and its output
      states that a call site is present which the check could not read, giving both counts.
    - Given a copy with `this?._showError(err.message, false, []);` appended, the check exits non-zero
      with the same class of message. The optional-chaining receiver defeats the other half of the
      regex's requirement, so it is a separate row.
    - Given a copy in which one call site is REMOVED entirely, both counts drop together and the check
      still exits 0 on that basis alone. The assertion is an equality between two derivations of the
      same tree, not a hard-coded expected count — a hard-coded 5 would be exactly the hand-kept
      expectation list CLAUDE.md forbids.
    - Given the pre-fix checker restored from git, both planted faults above exit 0. That is the
      discrimination control: a row that is red under both checkers proves nothing about this change.
  </behavior>

  <action>
Add an independent call-site total to rule (4) of `scripts/verify-shell-error-copy.mjs` and assert it
equals the enumeration regex's count.

**1. Derive the total, over the comment-stripped source.** Inside `check()`, in the rule (4) block
and before the `for (const m of src.matchAll(...))` loop, compute two counts from `src` (never from
`raw` — `check()` already assigns `src = stripComments(raw)` and every other regex in the function
runs over it):

  - `definitions` — the number of matches of a multiline-anchored pattern for a line whose first
    non-whitespace content is `_showError` followed by optional whitespace and an open paren. In the
    file under test this matches the single method declaration on line 1131 and matches no call site,
    because every call site is prefixed by a receiver.
  - `present` — the number of matches of a receiver-agnostic pattern for `_showError` followed by
    optional whitespace and an open paren, minus `definitions`.

Both patterns are deliberately dumber than the enumeration regex: no receiver, no argument capture,
no comma. That is what makes them an independent second derivation rather than the same expectation
restated, in the same way `rawCounts` in `scan-brand-residue.mjs` is a deliberately dumb recount that
lets its ground-truth arithmetic be a real cross-check rather than the scanner agreeing with itself.

**2. Assert equality after the loop, and keep the existing zero-guard.** After the enumeration loop,
compare `callSites` against `present`. On a mismatch, call `fail()` with a message that carries BOTH
numbers and states the consequence: a call site this check cannot read is a call site it is not
checking, and the remedy is either to write the call as `this._showError(<arg>, ...)` or to teach this
check the new shape. Keep the existing `callSites === 0` failure exactly as it is — it names a
different condition (the error layer is unreachable, or the check has stopped matching the code
entirely) and its message is already correct for that.

Add a comment above the derivation stating why it exists in the terms CLAUDE.md uses: the accept set
is derived from the tree, but until now the SET OF SITES the accept rule was applied to was an
unproven regex, and a check that silently examines five of six call sites can go green on the one it
missed. Name CR-03 and plan 01-16 so the provenance is traceable.

**3. Do NOT widen the enumeration regex.** Leave `/this\._showError\(\s*([^,]+?)\s*,/g` exactly as it
is. The deliverable is the assertion. Widening the pattern would replace one unproven expectation
with a marginally larger unproven expectation and would still be silent on the next shape; with the
equality assertion in place an unparsable call site is a hard, named failure that a human resolves.
Say this in the comment so a later reader does not "fix" the failure by loosening the regex.

**4. Add two `FAULTS` rows.** Both follow the existing row shape (`name`, `apply`, `expect`) and both
APPEND rather than substitute, because the defect is about call shapes the file does not currently
contain. Two constraints the append must respect, both mechanical:

  - The comma-less row's appended text must be the LAST thing in the mutated source and must contain
    no comma anywhere after the `this._showError(` it plants. The enumeration regex's `[^,]+?` capture
    excludes commas but does match newlines, so a comma appearing later in the file would let the
    regex match across the plant and mask the fault. Appending at the very end with no trailing comma
    is what makes the row exercise CR-03 rather than something else.
  - Neither appended snippet may introduce a `message:` property site or a `USER_MESSAGE.<key>`
    reference, or it will trip checks (2) or (3) instead and the row would go red for the wrong
    reason. A bare `this._showError(err.message)` and a bare `this?._showError(err.message, false, []);`
    both satisfy this.

Row names, printed verbatim by the self-test so a future reader can cite them:

  - `_showError() call site with no trailing comma is never enumerated`
  - `_showError() call site with an optional-chaining receiver is never enumerated`

Both rows set `expect` to the distinctive phrase from the new failure message — the phrase about a
call site this check cannot read — so a row that goes red for an unrelated reason is reported as
failing to name the drift rather than counted as a pass. `runSelfTest()` already implements exactly
that distinction and needs no change.

Update the file's header comment block to state that rule (4) now checks two things: that every
enumerated call site takes a table-derived message (01-14) and that the enumeration itself is
complete (this plan).
  </action>

  <verify>
    <automated>node scripts/verify-shell-error-copy.mjs && node scripts/verify-shell-error-copy.mjs --self-test</automated>
  </verify>

  <acceptance_criteria>
    - `node scripts/verify-shell-error-copy.mjs` exits 0 against the real
      `powerbrowser/shell/TheiaService.sys.mjs`. Record in the SUMMARY the derived `present` value and
      the `callSites` value observed on the shipped tree; both must be **5**.
    - `node scripts/verify-shell-error-copy.mjs --self-test` exits 0 and its header line reports
      **10** planted faults at the end of this task (8 pre-existing plus these 2). Every one of the 8
      pre-existing rows still prints its own `ok` line with its original name.
    - The self-test output contains both new row names verbatim, each on an `ok` line:
      `_showError() call site with no trailing comma is never enumerated` and
      `_showError() call site with an optional-chaining receiver is never enumerated`.
    - **CR-03 reproduction, re-run and now failing loudly.** Reuse the literal reproduction from
      `01-REVIEW.md` §CR-03: copy `powerbrowser/shell/TheiaService.sys.mjs` to a scratch path, append
      a comma-less `this._showError(err.message)` call as the last content in the file, then run
      `node scripts/verify-shell-error-copy.mjs --file <scratch>`. It must exit **non-zero** and the
      output must contain both counts (6 present, 5 parsed) and the phrase about a call site this
      check cannot read. Record the exact output line verbatim in the SUMMARY.
    - Same reproduction with `this?._showError(err.message, false, []);` appended instead: exits
      non-zero with the same class of message. Record verbatim.
    - **Discrimination control (run explicitly, both halves recorded verbatim in the SUMMARY).**
      Restore the pre-task `scripts/verify-shell-error-copy.mjs` from git to a scratch path, then run
      BOTH scratch `TheiaService.sys.mjs` copies above against it. Each must exit **0** — that is the
      defect, reproduced against the shipped checker, and it is what proves the two new rows are not
      red for some pre-existing reason. Then confirm each exits non-zero against the fixed checker. A
      row that behaves identically under both checkers is not evidence and the task is not done.
    - **Equality, not a hard-coded count.** Copy `TheiaService.sys.mjs` to a scratch path, DELETE one
      complete `this._showError(...)` call statement, and confirm
      `node scripts/verify-shell-error-copy.mjs --file <scratch>` does not fail on the count
      comparison — both derivations drop to 4 together. A checker that fails here is asserting a
      hard-coded 5, which is the hand-kept expectation list CLAUDE.md forbids. (Any OTHER failure the
      deletion causes, such as an entry becoming declared-but-unreferenced, is expected and is not
      what this criterion tests; read the failure text before judging it.)
    - `node scripts/verify-shell-error-copy.mjs --file /nonexistent/path/for/this/check` exits
      non-zero naming that path, exactly as it does today — the pre-existing read-failure path is
      unchanged.
    - `git diff --stat powerbrowser/shell/TheiaService.sys.mjs` produces no output. The file under
      test was not edited.
    - `grep -n 'shell-error-copy' scripts/verify-platform.sh` returns exactly the two pre-existing
      rows and no third. No sibling driver was created.
    - `scripts/verify-platform.sh --only shell-error-copy-no-internals-self-test` exits 0.
  </acceptance_criteria>

  <done>
    Rule (4) derives the total number of `_showError(` call sites present in the file under test
    independently of the enumeration regex, requires the two to be equal, and fails naming both counts
    and the consequence when they are not. Two self-test rows plant the two shapes the regex cannot
    read, each proven green against the pre-fix checker and red against the fixed one.
  </done>

  <reversibility rating="reversible">Two additive derivations, one additive assertion, and two additive self-test rows inside one static checker; `git revert` restores the prior behaviour with no state to unwind.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Close the two sibling silent-drop paths in the same file — a nested message: and an unparsable table entry</name>

  <read_first>
    - `scripts/verify-shell-error-copy.mjs` as edited by Task 1 — the file being modified. Read
      `parseUserMessageTable()` (lines 91-104) and note precisely what `entryRe` requires: a
      double-quoted, single-line string terminated by a comma. Read `braceBody()` (lines 106-124) and
      its doc comment about why the source is comment-stripped before it arrives. Read
      `messageBearingBindings()` (lines 163-204) in full, and read the doc comment above it (lines
      130-162) — it states the intent this task restores: a binding whose `.message` would paint
      nothing must not be waved through. Read how `table.entries` flows into checks (1), (2), (3) and
      into `messageBearingBindings`'s `declared` set, so the blast radius of an unparsed entry is
      concrete before you change anything.
    - `powerbrowser/shell/TheiaService.sys.mjs` — READ ONLY. Read the `const USER_MESSAGE = { ... };`
      table and count its declared entries and their exact indentation; the depth-1 line count this
      task derives must equal the parsed entry count on the shipped tree or the check goes red on a
      clean file. Also read the object-literal initializers `messageBearingBindings` shape (a)
      currently matches, and confirm each one's `message:` property is at depth 0 of its own literal —
      if any real binding relies on a nested `message:`, that is a finding to record rather than a
      reason to skip the fix.
    - `.planning/phases/01-platform-extraction-and-rename/01-REVIEW.md` §WR-01 and §WR-02 — each
      carries a concrete reproduction and a suggested minimal shape. The reproductions become the two
      new fault rows.
    - `.planning/phases/01-platform-extraction-and-rename/01-UI-SPEC.md` `## Copywriting Contract` and
      the `## UI Considerations` E2 error-message `long-text` row — the design contract this checker
      enforces, and the row this plan lifts into `must_haves.truths`.
    - `CLAUDE.md` — the User-facing copy section (no internal identifier may appear in user-facing
      text; nothing is dropped, every identifier a message does not carry appears as a labelled
      diagnostics row) and the Verification section's derive-and-compare rule.
  </read_first>

  <files>scripts/verify-shell-error-copy.mjs</files>

  <behavior>
    - Given a copy of `TheiaService.sys.mjs` with an object-literal binding whose only `message:`
      property is nested inside an array element or a sub-object, and a `this._showError(<binding>.message, ...)`
      call using it, the check exits non-zero stating that binding is not message-bearing. Before this
      task that input exits 0 (`01-REVIEW.md` §WR-01 reproduction).
    - Given a copy with one `USER_MESSAGE` entry rewritten in a form `entryRe` cannot parse — a
      template literal in place of a double-quoted string — the check exits non-zero naming how many
      declared entries could not be parsed.
    - Given the real file, both new assertions are silent: the declared depth-1 line count equals the
      parsed entry count, and every real message-bearing binding still qualifies under the depth-0
      rule, so `node scripts/verify-shell-error-copy.mjs` still exits 0.
  </behavior>

  <action>
Two additive assertions in `scripts/verify-shell-error-copy.mjs`, both closing a path where a
derivation silently drops its input — the same class as Task 1's, in two other functions.

**1. WR-01 — restrict `messageBearingBindings` shape (a) to depth-0 properties.** Shape (a) currently
tests `/(?:^|[^\w.$])message:/` against the ENTIRE brace body returned by `braceBody()`, at any
nesting depth, so a `message:` buried in a sub-object or an array element marks the outer binding as
message-bearing even though `<binding>.message` is `undefined` at runtime. Add a small helper beside
`braceBody()` that returns the given body with every span nested inside a `{` or a `[` removed —
walk the string once tracking a single depth counter, incrementing on `{` and `[`, decrementing on
`}` and `]`, and emitting only characters seen at depth 0. Apply shape (a)'s existing test to that
filtered string instead of the raw body. Do not change shape (b): it already resolves through a
method's `return` object literals and validates the property VALUE against a declared
`USER_MESSAGE.<key>`, which is a stricter path than shape (a) and is unaffected.

Add a comment naming WR-01 and stating what the depth-0 rule defends, in the doc comment's own terms:
a binding whose `.message` is `undefined` paints nothing, which is a different defect and must not be
waved through here — the doc comment already says exactly that about the `message: null` case, and
the nested case is the same defect arriving by another route.

**2. WR-02 — make `parseUserMessageTable` assert its own parse is total.** `entryRe` matches only a
double-quoted, single-line, comma-terminated string. An entry written as a template literal, with
single quotes, as a concatenation, or wrapped across two lines is dropped with no error, and the
consequences split badly: a REFERENCED unparsed key goes red at check (2) (fail-safe), but a
DECLARED-AND-UNREFERENCED unparsed key is invisible to check (1), which never leak-scans it, and to
check (3), which iterates the parsed entries. That is a silent drop of a user-facing string from the
one gate that scans user-facing strings.

Derive a second, independent count of the entries the table DECLARES: the number of lines inside the
captured table body whose indentation is the table's own depth-1 indentation followed by an
identifier and a colon. Return it alongside `entries` and `raw`. In `check()`, immediately after the
existing empty-table guard, compare it against `table.entries.size` and `fail()` on a shortfall with a
message naming both numbers and stating the consequence: a declared entry this parser cannot read is
a user-facing string that is never leak-scanned. Return from `check()` after that failure the same way
the no-table and empty-table guards already return — with an unparsed table, checks (1) through (4)
are all reasoning about an incomplete set and their output would be misleading noise.

**3. Add two `FAULTS` rows**, following the existing row shape and the existing `mutated === original`
guard. Row names, printed verbatim:

  - `nested message: makes a binding falsely message-bearing` — append a `const` object-literal
    binding whose only `message:` property sits inside an array element, plus a
    `this._showError(<binding>.message, false, []);` call that uses it. Set `expect` to the phrase from
    rule (4)'s existing not-message-bearing failure. Note the interaction with Task 1: the appended
    call site has a trailing comma after its first argument, so it is enumerated and both of Task 1's
    counts rise together — this row must go red on WR-01's rule, not on Task 1's equality assertion.
    Confirm which message the row actually produces before accepting it.
  - `USER_MESSAGE entry the parser cannot read is dropped silently` — insert one additional entry at
    the top of the `USER_MESSAGE` table written as a template literal rather than a double-quoted
    string, with a key that is not referenced anywhere in the file. That combination is the clean
    discrimination: an unreferenced key cannot trip check (2) or check (3), so the row can only go red
    on the new totality assertion. Set `expect` to the distinctive phrase from that assertion's
    message.

**4. IN-04 has no counterpart here and IN-03 is deliberately not done in this task** — see
`<planning_dispositions>`; ledger row 10 in plan 01-17 Task 3 records IN-03's deferral and its
route-to. Do not start it here.
  </action>

  <verify>
    <automated>node scripts/verify-shell-error-copy.mjs && node scripts/verify-shell-error-copy.mjs --self-test && scripts/verify-platform.sh --only shell-error-copy-no-internals</automated>
  </verify>

  <acceptance_criteria>
    - `node scripts/verify-shell-error-copy.mjs` exits 0 against the real
      `powerbrowser/shell/TheiaService.sys.mjs`. Record in the SUMMARY the declared depth-1 line count
      and the parsed entry count observed on the shipped tree; they must be equal, and record the
      number. Record also that every binding rule (4) accepts today still qualifies under the depth-0
      rule — if any does not, stop and report it rather than relaxing the rule.
    - `node scripts/verify-shell-error-copy.mjs --self-test` exits 0 and its header line reports
      **12** planted faults. All 8 original rows and both of Task 1's rows still print `ok` lines with
      their original names.
    - The self-test output contains both new row names verbatim on `ok` lines:
      `nested message: makes a binding falsely message-bearing` and
      `USER_MESSAGE entry the parser cannot read is dropped silently`.
    - **WR-01 reproduction, re-run and now failing loudly.** Reuse the literal reproduction from
      `01-REVIEW.md` §WR-01: a scratch copy of `TheiaService.sys.mjs` with a binding whose `message:`
      is nested one level inside an array element and a `this._showError(<binding>.message, false, []);`
      call. `node scripts/verify-shell-error-copy.mjs --file <scratch>` must exit non-zero and the
      output must state that the binding is not message-bearing. Record the exact line verbatim.
    - **Discrimination control (both halves recorded verbatim in the SUMMARY).** Restore the pre-task
      `scripts/verify-shell-error-copy.mjs` from git to a scratch path and run BOTH new scratch inputs
      against it. The WR-01 input must exit **0** — that is `01-REVIEW.md`'s reproduction, confirmed
      against the shipped checker. The WR-02 input must exit **0** as well, and this half must be
      checked carefully rather than assumed: the row uses an UNREFERENCED key precisely so that no
      pre-existing check can fire on it. If either exits non-zero under the pre-fix checker, the row
      is red for a pre-existing reason and is not evidence for this task — rewrite it until the
      control holds.
    - **Both new rows are red for their own stated cause, not for Task 1's assertion.** For each of
      the two new scratch inputs, confirm the failure text names the WR-01 or WR-02 condition
      respectively and does NOT report a call-site count mismatch. Record both failure lines verbatim.
    - `git diff --stat powerbrowser/shell/TheiaService.sys.mjs` produces no output.
    - `grep -n 'shell-error-copy' scripts/verify-platform.sh` returns exactly the two pre-existing
      rows.
    - `scripts/verify-platform.sh --quick` exits 0 and its summary lists both
      `shell-error-copy-no-internals` and `shell-error-copy-no-internals-self-test` as PASS. Record the
      total pass count (24/24 at plan time) in the SUMMARY.
  </acceptance_criteria>

  <done>
    A `message:` nested below depth 0 no longer makes a binding message-bearing, an unparsable
    `USER_MESSAGE` entry is a named failure instead of a silent drop, and both are proven by a fault
    row that is green against the pre-fix checker and red against the fixed one for its own stated
    cause.
  </done>

  <reversibility rating="reversible">One helper function, one narrowed test, one additive count assertion, two additive self-test rows; all inside a single static checker with no state.</reversibility>
</task>

</tasks>

<artifacts_this_phase_produces>
Every symbol, flag, row label and path this plan creates. `.planning/intel/API-SURFACE.md` extracted
**0 symbols** on this run and its extractor does not read this tree's `.mjs` shapes, so nothing below
may be assumed already catalogued there and its silence is not evidence about anything.

**New internal derivations in `scripts/verify-shell-error-copy.mjs` (module-local, not exported):**
- A `definitions` count — lines whose first non-whitespace content is the `_showError` method
  declaration, computed over the comment-stripped source.
- A `present` count — every textual `_showError(` in the comment-stripped source minus `definitions`;
  the independent total the enumeration regex's `callSites` is compared against.
- A depth-0 property filter used by `messageBearingBindings` shape (a), which returns an
  object-literal body with every span nested inside `{` or `[` removed.
- A declared-entry line count returned by `parseUserMessageTable` alongside its existing `entries` and
  `raw`, used to assert the entry parse is total.

**New failure messages (each is the `expect` target of a self-test row and must be greppable):**
- A rule (4) failure naming both the present and parsed call-site counts and stating that a call site
  this check cannot read is a call site it is not checking.
- A `parseUserMessageTable` totality failure naming how many declared entries could not be parsed.

**New `FAULTS` row labels (printed verbatim by `--self-test`):**
- `_showError() call site with no trailing comma is never enumerated`
- `_showError() call site with an optional-chaining receiver is never enumerated`
- `nested message: makes a binding falsely message-bearing`
- `USER_MESSAGE entry the parser cannot read is dropped silently`

**Planted-fault count change:** `--self-test` reports 8 planted faults before this plan, 10 after
Task 1, 12 after Task 2.

**New `scripts/verify-platform.sh` registry rows:** none. The pre-existing
`shell-error-copy-no-internals` and `shell-error-copy-no-internals-self-test` rows carry this work.

**New CLI flags:** none. `--file` and `--self-test` are unchanged.

**New file paths:** none. One existing file is modified.
</artifacts_this_phase_produces>

<threat_model>
OWASP ASVS level 1; blocking threshold `high`. Scope is one local static Node checker that reads one
source file and exits with a status. It opens no socket, takes no untrusted input at runtime, and
writes nothing. The asset being protected is not the script — it is the guarantee the script makes
about what can reach a user's screen.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `powerbrowser/shell/TheiaService.sys.mjs` (developer-authored, changes every phase) → `verify-shell-error-copy.mjs` | The checker's only input. A future edit to the file under test is the untrusted change this gate exists to catch, which is why the expectation must be derived from that file rather than hand-kept beside it. |
| the runtime's exception text (paths, ports, errno strings, pref keys) → the user-facing error layer | The boundary the gate defends. CLAUDE.md: no internal identifier may appear in user-facing text; nothing is dropped, every identifier a message does not carry appears as a labelled diagnostics row. |
| `scripts/verify-platform.sh --quick` → this checker | The commit gate. Whatever this checker fails to see, the commit gate also fails to see. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-16-01 | Information Disclosure | rule (4)'s call-site enumeration in `verify-shell-error-copy.mjs` | high | mitigate | A raw exception string — carrying filesystem paths, the sidecar port, errno text and pref keys — reaches the user-facing error layer through a call site the enumeration regex cannot read, and the gate that exists to stop it stays green. Reproduced twice independently (`01-REVIEW.md` CR-03, `01-VERIFICATION.md`). Mitigated by Task 1's independent total and equality assertion, and proven by two fault rows observed green against the pre-fix checker and red against the fixed one. Rated high because CLAUDE.md states this guarantee as holding today and it does not. |
| T-01-16-02 | Information Disclosure | `messageBearingBindings` shape (a) | medium | mitigate | A binding whose `message:` is nested below depth 0 is accepted as message-bearing, so `<binding>.message` — `undefined` at runtime — is admitted by the accept side. Reproduced by `01-REVIEW.md` §WR-01 as exiting 0. Mitigated by Task 2's depth-0 filter and its fault row. Rated medium rather than high because the runtime outcome is an empty message rather than a leaked identifier, but it is the accept side of the same gate. |
| T-01-16-03 | Information Disclosure | `parseUserMessageTable` | medium | mitigate | A declared-and-unreferenced entry the parser cannot read is never leak-scanned by check (1), so a user-facing string containing an all-caps sentinel or a dotted pref key could be declared and shipped without the gate ever inspecting it. Mitigated by Task 2's totality assertion and its fault row. |
| T-01-16-04 | Repudiation | the four new self-test rows as evidence | high | mitigate | A fault row that goes red for a pre-existing reason looks like proof of this change and is none — the precise failure this phase has now shipped three times. Mitigated by requiring, for every one of the four rows, an explicit discrimination control: green against the pre-fix checker restored from git, red against the fixed one, with both halves recorded verbatim in the SUMMARY, plus a per-row check that the failure text names the row's own stated cause. Rated high because a false evidence trail is what let two prior passes close a gap that had not closed. |
| T-01-16-05 | Denial of Service | the equality assertion turning `--quick` permanently red | medium | mitigate | An assertion computed over the wrong string (`raw` instead of the comment-stripped `src`) would count seven doc-comment mentions as call sites and fail on a clean tree, and a gate that is red for an unrelated reason gets switched off. Mitigated by counting over `src`, by the acceptance criterion requiring both counts to be observed as 5 on the shipped tree, and by the deletion criterion proving the assertion is an equality between two derivations rather than a hard-coded 5. |
| T-01-16-06 | Tampering | resolving a future failure by widening the enumeration regex | medium | mitigate | The cheapest way to silence a count mismatch is to loosen the regex until it matches again, which restores exactly the unproven expectation this plan removes. Mitigated by a prohibition in `must_haves`, by leaving the regex byte-identical, and by a comment at the assertion stating that the remedy is to make the call shape parsable or teach the check the new shape. |
| T-01-16-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package-manager install task exists in this plan. One existing file is edited in place with the repo's existing Node. No `[ASSUMED]`/`[SUS]` package is introduced, so no legitimacy checkpoint is earned — and none may later be skipped on the basis of this row. |

`T-01-16-01` and `T-01-16-04` are rated `high`, meeting the configured `security_block_on = high`
threshold. Both carry disposition `mitigate`, and both mitigations are tasks in this plan rather than
future work, so nothing at or above the threshold is left unmitigated.
</threat_model>

<verification>
Run from the repo root. `node` and `scripts/verify-platform.sh` both work outside a Nix dev shell, and
none of these need a build, a display, or `upstream/`.

1. `node scripts/verify-shell-error-copy.mjs` — exits 0; derived total and parsed count both 5.
2. `node scripts/verify-shell-error-copy.mjs --self-test` — exits 0, 12 planted faults, every row red
   naming its own drift.
3. The four scratch-input reproductions from the two tasks' acceptance criteria (comma-less call,
   optional-chained call, nested `message:` binding, unparsable table entry) — each exits non-zero
   against the fixed checker with a message naming its own cause.
4. The discrimination control for all four — each scratch input exits 0 against the pre-fix checker
   restored from git. Both halves recorded verbatim.
5. The deleted-call-site scenario — the count comparison does not fire when both derivations drop
   together, proving the assertion is an equality rather than a hard-coded number.
6. `git diff --stat powerbrowser/shell/TheiaService.sys.mjs` — empty. The file under test was not
   edited to fit the checker.
7. `scripts/verify-platform.sh --quick` — exits 0, no regression in any other row.
</verification>

<success_criteria>
- CR-03's first `missing[]` predicate is satisfied: the total number of `_showError(` call sites is
  derived independently of the enumeration regex and the two counts are required to be equal, with a
  loud failure naming the gap when they disagree.
- CR-03's second `missing[]` predicate is satisfied: a `--self-test` row plants a comma-less call site
  and a second plants an optional-chained one, and each requires a red for a call site the check
  cannot read.
- Both `missing[]` predicates are proven by a discrimination control, not by a green run: every new
  row is green against the pre-fix checker and red against the fixed one.
- The enumeration regex is unchanged. The fix is an assertion, not a wider pattern.
- WR-01 and WR-02 are closed in the same file with their own fault rows, each red for its own stated
  cause.
- The real `TheiaService.sys.mjs` still passes: 5 present, 5 parsed, table parse total, every real
  binding still message-bearing under the depth-0 rule.
- `scripts/verify-platform.sh --quick` exits 0. No new registry row, no sibling driver, no edit to the
  file under test.
</success_criteria>

<output>
Create `.planning/phases/01-platform-extraction-and-rename/01-16-SUMMARY.md` when done. Record
verbatim: the derived-versus-parsed counts observed on the shipped tree; the full `--self-test` output
showing all 12 rows; each of the four reproductions' failure lines; and BOTH halves of the
discrimination control for all four rows. A SUMMARY that reports only the green run reproduces exactly
the failure mode CR-03 named — a gate that has never been seen red for the input it exists to catch.
</output>
