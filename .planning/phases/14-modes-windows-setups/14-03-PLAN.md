---
phase: 14-modes-windows-setups
plan: 03
type: execute
wave: 3
depends_on: [14-02]
files_modified: [theia/extensions/modes/src/browser/setups-service.ts, theia/extensions/modes/src/browser/setups-commands.ts, theia/extensions/modes/src/browser/dependent-windows.ts, theia/extensions/modes/src/browser/modes-frontend-module.ts, powerbrowser/shell/PowerBrowserAPI.sys.mjs, powerbrowser/INTERNAL-APIS.md, scripts/verify-setup-roundtrip.mjs, scripts/verify-dependent-window-content.mjs, scripts/verify-platform.sh]
autonomous: true
requirements: [GUI-09]
must_haves:
  truths:
    - "User saves the current windows, tabs, and mode under a typed name and restores it later with geometry, placement, and mode applied"
    - "Restoring with missing tabs still completes on geometry plus mode with the contracted explanation, leaving nothing half-applied unexplained"
    - "A dependent window hosts exactly one tab view with no IDE frame, and closing the core window ends the session with relaunch restoring the last setup"
    - "A dependent whose tab closed elsewhere shows the contracted closed state with a window-only close"
    - "Deleting a setup is the only destructive action with the contracted confirmation and no undo"
  artifacts:
    - path: "theia/extensions/modes/src/browser/setups-service.ts"
      provides: "Named setups snapshot, list, restore, delete, and last-session applicator"
      contains: "setups.json"
    - path: "theia/extensions/modes/src/browser/setups-commands.ts"
      provides: "Setup and dependent command ids with contracted labels"
      contains: "powerbrowser.setups.save-setup"
    - path: "theia/extensions/modes/src/browser/dependent-windows.ts"
      provides: "Dependent hosting with membership gate and closed state"
      contains: "moveWidgetToSecondaryWindow"
    - path: "scripts/verify-setup-roundtrip.mjs"
      provides: "Setup schema roundtrip gate with self-test"
      min_lines: 60
    - path: "scripts/verify-dependent-window-content.mjs"
      provides: "Dependent-content gate with static half and live half"
      min_lines: 60
    - path: "scripts/verify-platform.sh"
      provides: "Setup and dependent gate entries plus self-test rows"
      contains: "gui09-setup-roundtrip"
  key_links:
    - from: "theia/extensions/modes/src/browser/setups-service.ts"
      to: "theia/extensions/modes/src/browser/setups-commands.ts"
      via: "save, restore, and delete invoke the service through the registry by imported id consts"
      pattern: "SETUPS.*COMMAND_ID"
    - from: "theia/extensions/modes/src/browser/setups-service.ts"
      to: "theia/extensions/modes/src/browser/mode-service.ts"
      via: "snapshots record the active mode id and restores switch to it"
      pattern: "switchPerspective"
    - from: "theia/extensions/modes/src/browser/dependent-windows.ts"
      to: "theia/extensions/modes/src/browser/setups-service.ts"
      via: "setup snapshots record dependent rects and hosted tab URIs for verbatim restore"
      pattern: "setups.json"
    - from: "scripts/verify-platform.sh"
      to: "scripts/verify-setup-roundtrip.mjs"
      via: "registry row invokes the roundtrip gate; self-test row invokes it with --self-test"
      pattern: "gui09-setup-roundtrip"
---

<objective>
Ship dependents and setups (GUI-09): named setup save and restore with last-session relaunch, dependent windows hosting single tab views per the probe verdict, and the two GUI-09 gates green.

Purpose: this closes the phase on the plan 14-01 tracer and the plan 14-02 modes foundation. Snapshots reference opaque tab URIs so the bridge stays landable, dependents never grow an IDE frame by membership construction, and core-close kills the session through the stock shutdown cascade with relaunch restoring automatically.
Output: setups service with applicator, setup and dependent commands, dependent hosting contribution, roundtrip and dependent gates with self-tests, quick green.

Schema call (resolves RESEARCH Open Question 2, setups half): user-storage setups.json holds an object with a numeric version, a setups array whose rows carry a name, a mode id, a windows array of verbatim rects with tab-URI lists and an active tab, plus a saved timestamp, and a last-session pointer naming the setup to auto-restore or null; validation is hand-rolled total parsing with corrupt data degrading to the contracted empty state and restore dropping unresolvable URIs with the contracted explanation.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-modes-windows-setups/14-CONTEXT.md
@.planning/phases/14-modes-windows-setups/14-RESEARCH.md
@.planning/phases/14-modes-windows-setups/14-PATTERNS.md
@.planning/phases/14-modes-windows-setups/14-UI-SPEC.md
@.planning/phases/14-modes-windows-setups/14-01-PLAN.md
@.planning/phases/14-modes-windows-setups/14-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Named setups with restore applicator plus setup commands</name>
  <reversibility rating="costly">The setups schema and command ids are asserted by gates and read by relaunch; renaming touches service, commands, and gate, but migration is a local file rewrite.</reversibility>
  <read_first>
    - theia/extensions/customize/src/browser/customize-css-contribution.ts lines 55-145 (never-create default, catch fallback, keep-last-good, file-change reload with debounce)
    - .planning/phases/14-modes-windows-setups/14-RESEARCH.md (Pattern 2 idiom, Pitfall 5 verbatim geometry with reachability clamp, restore via contribution open path never WidgetOpenHandler, ready-ordered applicator)
    - .planning/phases/14-modes-windows-setups/14-PATTERNS.md (sections 3 validation discipline with corrupt and gone-tabs semantics, 1 command-per-action with exported consts and destructive rules, 2 constrain-without-rebind)
    - theia/extensions/modes/src/browser/mode-service.ts (plan 14-02 customs idiom and active-id re-application to mirror for the last-session pointer)
    - theia/extensions/tab-uris/src/browser/view-open-handler.ts lines 11-77 (bare OpenHandler delegating to the contribution open path so placement comes from default view options)
    - .planning/phases/14-modes-windows-setups/14-UI-SPEC.md (named setups section with save, list, restore, delete, row meta format, and every contracted string)
  </read_first>
  <files>theia/extensions/modes/src/browser/setups-service.ts, theia/extensions/modes/src/browser/setups-commands.ts, theia/extensions/modes/src/browser/modes-frontend-module.ts</files>
  <action>Build named setups as user-storage data owned entirely Theia-side: snapshot window rects verbatim with tab URIs from the registry plus the active tab and the current mode id, persist through the file service with atomic writes, and list rows with the contracted meta format carrying the current marker while zero setups render the contracted empty copy and the list scrolls internally past its cap. Save through a dialog with one name field enforcing the contracted cap with overflow cut before commit, the contracted empty and duplicate errors writing nothing, no overwrite offer, and the contracted saved confirmation marking the new row current. Restore geometry verbatim with reachability clamping recorded as backstop behavior, place tabs through the contribution open path with unresolvable URIs dropped, switch to the stored mode, complete on geometry plus mode with the contracted variant explanation when tabs are gone, and on failure leave current windows and tabs untouched with the contracted restore-failure error. Delete as the only destructive action with the contracted confirmation and no undo, clearing the current marker while changing nothing on screen. Auto-save the last-session pointer on shutdown and re-apply it through a ready-ordered applicator after core layout restore with no confirmation on core close. Register four commands with scope-prefixed exported id consts: save and delete carrying their contracted labels verbatim, restore and dependent-open carrying no palette label, all invoked by const import. Bind service and commands statically at module load beside the existing binds. Stage the three files before any scan, compile in the Nix theia shell, and commit as feat(14-03): named setups with restore applicator and commands. Finish building with the quick gate green.</action>
  <verify>
    <automated>git add theia/extensions/modes/src/browser/setups-service.ts theia/extensions/modes/src/browser/setups-commands.ts theia/extensions/modes/src/browser/modes-frontend-module.ts && nix develop .#theia --command bash -c "cd theia/extensions/modes && yarn build" && grep -qc 'powerbrowser.setups.save-setup' theia/extensions/modes/src/browser/setups-commands.ts && grep -qc 'Save Setup' theia/extensions/modes/src/browser/setups-commands.ts && scripts/verify-platform.sh --quick</automated>
    <fails_when>extension fails to build, any setup command id missing or either contracted label reworded, or the quick gate exits non-zero</fails_when>
  </verify>
  <acceptance_criteria>
    - Snapshot, list, restore, and delete follow the contracted semantics with verbatim geometry and URI-keyed placement
    - Gone tabs drop with the contracted explanation and failures leave the session untouched
    - Last-session pointer auto-saves and re-applies after core restore with no core-close dialog
    - Four exported command ids with contracted labels on save and delete and none on restore and dependent-open
  </acceptance_criteria>
  <done>Setups remember geometry, placement, and mode with relaunch restoring automatically</done>
</task>

<task type="auto">
  <name>Dependent windows per the probe verdict with membership gate</name>
  <reversibility rating="costly">The extractable membership set and any fallback opener shape later window work; changing membership touches the gate EXPECTED set in the same commit.</reversibility>
  <read_first>
    - .planning/phases/14-modes-windows-setups/14-PROBE-DEPENDENT-WINDOWS.md (verdict line plus routing it selects — primary stock path on GREEN, chrome-side opener fallback on RED)
    - theia/extensions/tab-uris/src/browser/tab-uris-frontend-module.ts lines 93-132 (widget-create subscription plus mutable-field set with pre-existing fix, chat non-extractable precedent to mirror and invert)
    - theia/extensions/tab-uris/src/browser/browser-window-command.ts lines 56-85 (blocked-popup throw path with palette and activation guidance, never a new dialog)
    - powerbrowser/shell/PowerBrowserAPI.sys.mjs lines 510-572 (shell window shape with windowtype lookup, null args, stock flags to mirror ONLY on RED verdict)
    - powerbrowser/INTERNAL-APIS.md browser-window rows (catalogue voice for any RED-verdict amendment)
    - .planning/phases/14-modes-windows-setups/14-UI-SPEC.md (dependent windows section with single-view content, closed-state copy, focus return, motion rule)
  </read_first>
  <files>theia/extensions/modes/src/browser/dependent-windows.ts, theia/extensions/modes/src/browser/modes-frontend-module.ts, powerbrowser/shell/PowerBrowserAPI.sys.mjs, powerbrowser/INTERNAL-APIS.md</files>
  <action>Host dependents exactly as the probe verdict routes: on GREEN drive the stock secondary-window handler to move one tab-content widget into the bare dock host, on RED add a chrome-side opener method inside the existing boundary file reusing its already-imported window service to load the same secondary asset in a minimal chrome window with no new patch and no new import, cataloguing the touchpoint in the boundary catalogue in the existing voice. Pin membership at creation so exactly the tab-content widget type is extractable while the chrome bar, toggle, side panels, status bar, and tab strip stay non-extractable with pre-existing instances fixed, making a second IDE frame structurally unreachable. Apply dependent adjustments on window loaded never on opened while the document is still blank, return focus to the core window on close destroying nothing, keep the tab a core-model tab so the invariant holds across windows, render the contracted closed state with its window-only close button when the hosted tab closes elsewhere, and reuse the existing blocked path with no new dialog. Run no startup opens and author no custom window animation. Leave the two chrome-side files byte-identical on GREEN with an explicit note in the summary; touch them only on RED. Stage touched files before any scan, compile the touched extension in the Nix theia shell, run the boundary catalogue check on any RED-verdict edit, and commit as feat(14-03): dependent windows with membership gate. Finish building with the quick gate green.</action>
  <verify>
    <automated>git add theia/extensions/modes/src/browser/dependent-windows.ts theia/extensions/modes/src/browser/modes-frontend-module.ts powerbrowser/shell/PowerBrowserAPI.sys.mjs powerbrowser/INTERNAL-APIS.md && nix develop .#theia --command bash -c "cd theia/extensions/modes && yarn build" && grep -qc 'moveWidgetToSecondaryWindow' theia/extensions/modes/src/browser/dependent-windows.ts && grep -qc 'This tab is closed' theia/extensions/modes/src/browser/dependent-windows.ts && bash scripts/check-internals-boundary.sh && scripts/verify-platform.sh --quick</automated>
    <fails_when>extension fails to build, the stock move call or the contracted closed-state string missing, the boundary check exits non-zero, or the quick gate exits non-zero</fails_when>
  </verify>
  <acceptance_criteria>
    - Implementation follows the probe verdict with GREEN leaving chrome-side files byte-identical
    - Membership pins exactly one extractable widget type with pre-existing instances fixed
    - Closed-elsewhere renders the contracted state, focus returns on close, core-close cascade stays stock
    - Boundary check green with any RED-verdict touchpoint catalogued and no new patch
  </acceptance_criteria>
  <done>Dependents host single tab views with no second IDE frame reachable</done>
</task>

<task type="auto">
  <name>Setup roundtrip and dependent-content gates as registry rows</name>
  <reversibility rating="reversible">Two gate scripts plus test scaffolding; tightening either EXPECTED set edits one const.</reversibility>
  <read_first>
    - .planning/phases/14-modes-windows-setups/14-PATTERNS.md (section 5 derive-and-compare idiom with one EXPECTED const, set equality both directions, non-vacuity, self-test plants, honestly-quick rules, two-rows-per-check registration)
    - theia/extensions/modes/src/browser/setups-service.ts (Task 1 schema source the roundtrip gate derives its field set from)
    - theia/extensions/modes/src/browser/setups-commands.ts (the four setup and dependent command ids the gate derives)
    - theia/extensions/modes/src/browser/dependent-windows.ts (Task 2 membership set and closed-state strings the content gate derives)
    - theia/applications/browser/lib/frontend/secondary-window.html (built asset the static half asserts)
    - scripts/verify-platform.sh lines 3723-3724 (gui07 verdict row shape plus honesty comment to sit beside)
  </read_first>
  <files>scripts/verify-setup-roundtrip.mjs, scripts/verify-dependent-window-content.mjs, scripts/verify-platform.sh</files>
  <action>Write the roundtrip gate honestly quick: derive the setups field set from the service schema source at check time and run a node JSON roundtrip over save, list, restore, and delete covering the name cap, the contracted empty and duplicate errors, corrupt data degrading to the contracted empty state, and gone tabs dropping with the contracted explanation, plus derive the four command ids from the commands source with set equality and the contracted save and delete labels verbatim with the two labelless commands asserted labelless. Write the content gate in two halves: a static half deriving the built secondary asset presence plus the extractable membership set from the dependents source as set equality against one EXPECTED const with the contracted closed-state strings asserted verbatim, and a live half running extraction, geometry, and close-matrix observation through the probe harness reserved for the full suite. Give each a self-test asserting green first, then planted faults each going red naming the drift — a dropped schema field, a removed command id, a label drift for roundtrip; a widened membership set, a removed closed-state string, a missing asset for content — with plant-landed assertions. Append four rows beside the gui07 verdict pair with honesty comments marking the static halves quick and the live half full-suite, creating no sibling driver. Stage the three files before any scan and commit as feat(14-03): setup roundtrip and dependent-content gates. Finish with both scripts green, both self-tests green, single-gate sampling green across the new rows plus the guard rows, and the quick gate green.</action>
  <verify>
    <automated>git add scripts/verify-setup-roundtrip.mjs scripts/verify-dependent-window-content.mjs scripts/verify-platform.sh && node --check scripts/verify-setup-roundtrip.mjs && node --check scripts/verify-dependent-window-content.mjs && node scripts/verify-setup-roundtrip.mjs && node scripts/verify-setup-roundtrip.mjs --self-test && node scripts/verify-dependent-window-content.mjs && node scripts/verify-dependent-window-content.mjs --self-test && scripts/verify-platform.sh --only gui09-setup-roundtrip && scripts/verify-platform.sh --only gui09-setup-roundtrip-self-test && scripts/verify-platform.sh --only gui09-dependent-window-content && scripts/verify-platform.sh --only gui09-dependent-window-content-self-test && scripts/verify-platform.sh --only gui07-mode-toggle-commands && scripts/verify-platform.sh --only gui07-mode-switch-tabs-invariant && scripts/verify-platform.sh --only gui04-registry-shape && scripts/verify-platform.sh --only internals-catalogue && scripts/verify-platform.sh --quick</automated>
    <fails_when>either script exits non-zero unmodified, any self-test plant fails to go red naming the drift, any sampled row fails, or the quick gate exits non-zero</fails_when>
  </verify>
  <acceptance_criteria>
    - Roundtrip gate covers schema, cap, errors, corruption, gone-tabs, and command ids with green-first self-test on three plants
    - Content gate covers asset, membership, and closed-state copy statically with live behavior reserved full-suite and green-first self-test on three plants
    - Four new rows plus guard rows green through the single-gate runner with quick green
  </acceptance_criteria>
  <done>Setups and dependents are enforced mechanically with live behavior held for the full suite</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Setup file content to session state | Stored JSON becomes windows, tabs, and mode on restore. |
| Setup dialog text to storage | Typed setup names become persisted rows and list entries. |
| Secondary window to core model | Dependent hosts a core-owned tab view across windows. |
| Chrome boundary on RED verdict only | Fallback opener crosses content to chrome through the existing file. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-03-01 | Tampering | malformed setups file resurrecting state | high | mitigate | Total parsing with corrupt data degrading to the contracted empty state; geometry numbers validated before any move call; restore failures leave the session untouched. |
| T-14-03-02 | Information disclosure | restore forcing private-context tabs back | medium | mitigate | Placement re-opens through the existing opener and drops URIs that no longer resolve instead of forcing them. |
| T-14-03-03 | Spoofing | secondary-window message channel | medium | mitigate | No new message channel authored; stock same-origin markers ride untouched. |
| T-14-03-04 | Denial of Service | misrouted or blocked dependent leaking half-state | medium | mitigate | Null windows take the existing error path with plain language and a next step; never a second copy of the tab; membership keeps IDE chrome out structurally. |
| T-14-03-05 | Information disclosure | internal identifier in setup or dependent copy | medium | mitigate | Contracted strings verbatim from the UI-SPEC; roundtrip and content gates assert them; diagnostics carry what copy omits. |
| T-14-03-SC | Tampering | npm/pip/cargo installs | medium | mitigate | No new registry package; fallback reuses the already-imported window service with no new import. |
</threat_model>

<verification>
Setups save, restore, and delete per contract with last-session relaunch, dependents host single tab views per the probe verdict with membership enforced, roundtrip gate plus self-test green, content gate plus self-test green, sampled rows plus guard rows green, quick green, core diff clean, upstream diff empty, no new patch.
</verification>

<success_criteria>
The user works across core and dependent windows with named setups that survive relaunch, and every GUI-09 behavior is enforced mechanically.
</success_criteria>

<output>
Phase 14 complete: modes as data with shipped defaults and customs, dependents hosting tab content only, named setups with geometry plus placement plus mode, gates green; ready for Phase 15 organising canvas in the placeholder slot.
</output>
