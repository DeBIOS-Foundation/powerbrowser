---
phase: 09-extensions-crash-pipeline
plan: 04
type: execute
wave: 3
depends_on: ["09-01", "09-03"]
files_modified: [docs/BUILD.md, scripts/verify-downstream-fixture.mjs, scripts/verify-upstream-pins.mjs]
autonomous: true
requirements: [BLD-02, UPD-04]
estimate:
  tokens: 30000
  raw_tokens: 60000
  tasks: 3
  confidence: high
must_haves:
  truths:
    - "Per-fixture generate-level cells over the new extension source kinds pass with post-run tree hashes equal to the snapshot"
    - "A real pinned Open VSX entry plus synthetic npm and local-path fixtures travel the download hash build path on the reachable host"
    - "Unreachable packaging-host cells are recorded staged-unexecuted with exact errors and operator unblocks, never silent skips"
    - "The Theia pin agreement holds at the stated pin and the token-gate backend compiles with its suites green"
  artifacts:
    - path: "docs/BUILD.md"
      provides: "fixture matrix record plus re-pin runbook"
      contains: "pkg-win11"
    - path: "scripts/verify-downstream-fixture.mjs"
      provides: "generate-level fixture cells over new source kinds"
      contains: "fixtures-root"
    - path: "scripts/verify-upstream-pins.mjs"
      provides: "Theia re-pin agreement proof"
      contains: "theia_release"
  key_links:
    - from: "scripts/verify-downstream-fixture.mjs"
      to: "scripts/generate.mjs"
      via: "harness generates from staged fixture dirs in a child process, never inside the repo"
      pattern: "fixtures-root"
    - from: "docs/BUILD.md"
      to: ".mozbuild"
      via: "matrix and drill evidence logs referenced by path, gitignored like prior proof tooling"
      pattern: "mozbuild"
    - from: "scripts/verify-upstream-pins.mjs"
      to: "theia/package.json"
      via: "pin agreement over resolutions plus lockfile stanzas"
      pattern: "theia_release"
  prohibitions:
    - "No tier-3 green claimed without logs; unreachable cells stay staged-unexecuted with exact errors"
    - "No fixture drill residue left in the tree; post-run hashes must equal the snapshot"
    - "No Theia framework source edits under a re-pin; pins and lockfile only"
  assumptions:
    - "The 08-05 host record (nix-linux reachable, pkg-win11 and pkg-macos staged with operator unblocks) is re-read, not re-proven; only newly reachable hosts get live attempts"
    - "One real pinned Open VSX entry proves the live registry path; synthetic Acme fixtures prove npm and local-path shapes without depending on third-party package availability"
    - "Tier-3 full builds are wall-clock heavy and run unattended; static agreement gates stay the per-commit signal"
---

<objective>
Run the BLD-02 tier-3 fixture matrix over the new extension source kinds on real built artifacts and the UPD-04 Theia re-pin proof with the token-gate backend intact, reusing the 08-05 drill-then-restore and staged-unexecuted discipline.

Purpose: Prove the new kinds on real artifacts rather than schema alone, and prove the re-pin path keeps its token gate, without re-proving host reachability that 08-05 already recorded.
Output: Fixture matrix evidence, per-target download assertions, staged host cells, re-pin runbook with agreement proof.
</objective>

<execution_context>
<purpose>
Execute a phase prompt (PLAN.md) and create the outcome summary (SUMMARY.md).
</purpose>

<required_reading>
Read STATE.md before any operation to load project context.
Read config.json for planning behavior settings.
</required_reading>
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-extensions-crash-pipeline/09-RESEARCH.md
@.planning/phases/09-extensions-crash-pipeline/09-PATTERNS.md
@.planning/phases/09-extensions-crash-pipeline/09-01-SUMMARY.md
@.planning/phases/09-extensions-crash-pipeline/09-03-SUMMARY.md
@.planning/phases/08-installer-hardening-canonical-rename/08-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Generate-level fixture matrix over the new source kinds</name>
  <files>scripts/verify-downstream-fixture.mjs</files>
  <read_first>scripts/verify-downstream-fixture.mjs harness contract plus fixtures-root argv plus drill-then-restore hash snapshot region, scripts/verify-platform.sh downstream-fixture row region showing the root derivation, scripts/generate.mjs new-kind resolver branches from plan 09-01</read_first>
  <action>Extend the generate-level fixture coverage with one cell per new source kind using synthetic Acme entries plus one cell carrying a real pinned Open VSX entry, each cell staged through the harness contract that copies the source to a temp external dir for path independence, snapshots the platform generated tree hashes, and runs the generator in a child process with the staged config dir. Drive every committed cell through the harness all mode with the fixtures root taken from the registry row derivation rather than a spelled phase path, asserting post-run hashes equal the snapshot so the drill restores, and keep every fixture sweep-clean with Acme ids and example hosts.</action>
  <acceptance_criteria>All committed cells pass through the harness all mode; post-run generated tree hashes equal the pre-run snapshot; no cell leaves residue in the tree; synthetic entries stay sweep-clean</acceptance_criteria>
  <verify>
    <automated>node scripts/verify-downstream-fixture.mjs --self-test && scripts/verify-platform.sh --only verify-downstream-fixtures-self-test</automated>
    <fails_when>Either command exits nonzero, or any cell passes with a hash mismatch against its snapshot, or residue scan flags a fixture token</fails_when>
  </verify>
  <done>New-kind declarations generate correctly per fixture with drill-then-restore equality</done>
</task>

<task type="auto">
  <name>Tier-3 real-artifact cells plus staged host records</name>
  <files>docs/BUILD.md</files>
  <read_first>docs/BUILD.md host-capability record plus per-OS matrix plus staged-unexecuted row idiom, theia/applications/browser/package.json download plugins script, scripts/verify-extension-pins.mjs per-kind hash proof</read_first>
  <action>Run the reachable-host cells live inside the Theia dev shell: download plugins per target over the new-kind entries, assert per-target archives hash to their pins with target-qualified archive names, and build the sidecar over the downloaded set, recording exact commands with evidence log paths under the gitignored proof dir. For cells whose hosts stay unreachable, re-read the 08-05 capability record and append staged-unexecuted rows carrying the exact provisioning error plus the operator unblock command, never a silent skip and never a green claim without logs. Assert the downloader placeholder reaches per-target archives rather than one expanded platform literal, and extend the BUILD matrix with the per-target download evidence plus the hop-equivalent pin re-proof.</action>
  <acceptance_criteria>Reachable-host download plus hash plus build cells show green logs at referenced paths; every unreachable cell carries its exact error plus unblock command in BUILD.md; per-target archives are target-qualified with no generator-expanded platform literal; pin re-proof is green</acceptance_criteria>
  <verify>
    <automated>node scripts/verify-extension-pins.mjs --self-test && scripts/verify-platform.sh --quick</automated>
    <fails_when>Self-test exits nonzero, or --quick reports red, or a staged row lacks either the exact error or the unblock command</fails_when>
  </verify>
  <done>New kinds are proven on real artifacts where hosts exist and honestly staged where they do not</done>
</task>

<task type="auto">
  <name>Re-pin runbook plus agreement proof with token-gate intact</name>
  <files>docs/BUILD.md, scripts/verify-upstream-pins.mjs</files>
  <read_first>scripts/verify-upstream-pins.mjs Theia agreement steps plus monaco exception plus lockfile stanza check, theia/package.json resolutions block, docs/BUILD.md procedure idiom with timings naming tree host and toolchain</read_first>
  <action>Record the re-pin runbook as a BUILD procedure section following the research procedure verbatim: bump the manifest pin, set every framework pin across the root and member manifests keeping the single exact-name exception, re-resolve the lockfile so every tarball stanza carries the pin, then inside the Theia dev shell install, compile every extension including the token-gate backend modules, and run all plain-node suites. Prove the current tree first with the agreement gate self-test plus the full row, then compile plus suite the token-gate backend as the intactness artifact, and only attempt a live version bump when a newer upstream tag exists, otherwise recording the current-pin agreement green as the proof with the runbook staged for the next tag.</action>
  <acceptance_criteria>Agreement gate self-test and full row are green at the stated pin; token-gate backend compiles with its suites green; runbook section names every step with the exception called out; a live bump is either executed with rebuilt proofs or explicitly recorded as staged pending the next tag</acceptance_criteria>
  <verify>
    <automated>node scripts/verify-upstream-pins.mjs --self-test && scripts/verify-platform.sh --only upstream-pins && scripts/verify-platform.sh --quick</automated>
    <fails_when>Any command exits nonzero, or the token-gate compile emits errors, or a suite reports red</fails_when>
  </verify>
  <done>Re-pin path is proven with the token gate intact and the runbook recorded for the next tag</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| registry→build host | Live tarballs and vsix bytes land on tier-3 build hosts |
| re-pin→tree | New upstream framework bytes enter pins, lockfile, and compiled output |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-10 | Tampering | per-target downloads (tier-3 cells) | high | mitigate | Per-target archive hashes compared to pins before the build consumes them; placeholder expansion checked, never assumed |
| T-09-11 | Tampering | token-gate bypass via re-pin | high | mitigate | Agreement gate over resolutions plus lockfile plus token-gate compile and suite green at the pin; framework source edits forbidden by hard rule |
| T-09-12 | Repudiation | staged-unexecuted cells | medium | mitigate | Exact errors plus unblocks recorded in BUILD.md; no green without logs, so staged work cannot be mistaken for proven work |
| T-09-SC | Tampering | npm installs | low | accept | Tier-3 shells use the pinned toolchain; no new registry dependency is added by these drills |
</threat_model>

<verification>
Harness all mode plus self-test, pin-gate self-test, upstream-pins self-test plus full row, token-gate compile plus suites, and the commit gate.
</verification>

<success_criteria>
Fixture cells pass with hash-equal restore, real artifacts carry the new kinds on reachable hosts with honest staged records elsewhere, per-target resolution is asserted, and the re-pin agreement holds with the token gate green.
</success_criteria>

<artifacts_this_phase>
This plan produces: generate-level fixture cells over the new kinds; tier-3 download hash build evidence on reachable hosts; staged-unexecuted host rows with unblocks; the re-pin runbook with agreement and token-gate proofs.
</artifacts_this_phase>

<output>
Create `.planning/phases/09-extensions-crash-pipeline/09-04-SUMMARY.md` when done
</output>
