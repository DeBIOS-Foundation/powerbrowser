---
phase: 11-sql-store-design
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [.planning/phases/11-sql-store-design/authority/AUTHORITY.md, .planning/phases/11-sql-store-design/authority/SIGN-OFF.md]
autonomous: true
requirements: [SQL-02]
must_haves:
  truths:
    - "Authority/invariant table exists stating the single chrome-side writer, sessionstore restore authority, URI join key, backend-never-opens rule, own-file rule, and token-never-in-SQL"
    - "Recorded reviewer approval for the authority table exists with a per-item checklist"
    - "Static gates stay green with the new docs staged"
  artifacts:
    - path: ".planning/phases/11-sql-store-design/authority/AUTHORITY.md"
      provides: "Six-row invariant table plus Phase 12 enforcement pointers"
      min_lines: 40
    - path: ".planning/phases/11-sql-store-design/authority/SIGN-OFF.md"
      provides: "Recorded approval with checklist covering SQL-02"
      min_lines: 25
  key_links:
    - from: ".planning/phases/11-sql-store-design/authority/AUTHORITY.md"
      to: "Phase 12 negative scan"
      via: "each writer invariant names its enforcement check"
      pattern: "powerbrowser/shell/"
    - from: ".planning/phases/11-sql-store-design/authority/SIGN-OFF.md"
      to: ".planning/phases/11-sql-store-design/authority/AUTHORITY.md"
      via: "reviewed-files table names the doc with a verdict"
      pattern: "AUTHORITY\\.md"
---

<objective>
Write the SQL-02 authority/invariant table and record its review approval before any schema work exists.

Purpose: SQL-02 requires the authority rules reviewed and signed before schema work; this plan produces that signed record and proves the doc-to-gate path the schema docs reuse.
Output: authority/AUTHORITY.md plus authority/SIGN-OFF.md with a recorded approval, static gates green.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-sql-store-design/11-CONTEXT.md
@.planning/phases/11-sql-store-design/11-RESEARCH.md
@.planning/phases/11-sql-store-design/11-PATTERNS.md
@powerbrowser/INTERNAL-APIS.md
@brand/HUMAN-REVIEW.md
</context>

<tasks>

<task type="tracer">
  <name>End-to-end authority slice — invariant table plus sign-off draft, staged and gate-green</name>
  <reversibility rating="one-way">Single-writer plus restore-authority invariants bind Phase 12; reversing them after the writer ships requires re-architecture plus data handling.</reversibility>
  <read_first>
    - .planning/phases/11-sql-store-design/11-RESEARCH.md (Recommended Plan Decomposition item 1, Pattern 1, Security Domain)
    - .planning/phases/11-sql-store-design/11-PATTERNS.md (authority/AUTHORITY.md, SIGN-OFF.md, boundary-write shape)
    - powerbrowser/INTERNAL-APIS.md (catalogue-doc shape: invariant-row table, deliberately-not-touched voice, consistency section)
    - brand/HUMAN-REVIEW.md (STATUS header, procedure, reviewed-files table, sign-off record shape)
  </read_first>
  <files>.planning/phases/11-sql-store-design/authority/AUTHORITY.md, .planning/phases/11-sql-store-design/authority/SIGN-OFF.md</files>
  <action>Write AUTHORITY.md following the catalogue-doc shape with table columns number, invariant, enforced-where in Phase 12, and why plus threat-if-violated, carrying exactly six rows: single chrome-side writer with lazy-getter placement inside the sole boundary file and zero forbidden-pattern change; sessionstore authoritative for restore with the store as projection and quarantine-rebuilds-from-sessionstore pointer; registry URIs as opaque TEXT join key with chrome-binds-never-parses and zero registry-shape change; backend never opens profile database files with dedicated-file-only readonly access and places reads staying chrome-side; own-file rule with the fixed filename in the profile directory and never tables in the upstream-owned places file; token never in SQL with diagnostics-separation restatement. Close with a consistency section naming each row's Phase 12 enforcement, an engine paragraph stating SQLite only per the verdict research, a platform-content paragraph stating filename and schema version are fixed and never manifest keys, and a no-GUI-surface sentence. Write SIGN-OFF.md following the sign-off ritual shape with STATUS draft, a procedure checklist covering enforcement naming per row, brand-token absence, engine exclusivity, manifest-key absence, GUI absence, and the six assumed items recorded as pinned-or-deferred with procedure, a reviewed-files table row for AUTHORITY.md, an evidence-basis section, and an empty signature block. Stage both files before any scan. Run the quick gate. This tracer proves the doc-to-gate path the schema docs reuse.</action>
  <verify>
    <automated>git add .planning/phases/11-sql-store-design/authority/AUTHORITY.md .planning/phases/11-sql-store-design/authority/SIGN-OFF.md && test -f .planning/phases/11-sql-store-design/authority/AUTHORITY.md && test -f .planning/phases/11-sql-store-design/authority/SIGN-OFF.md && scripts/verify-platform.sh --quick</automated>
    <fails_when>quick gate exits non-zero or any registry row reports FAIL, including the residue scan over the staged docs</fails_when>
  </verify>
  <acceptance_criteria>
    - AUTHORITY.md exists with six invariant rows each naming its Phase 12 enforcement (grep for the shell directory path returns at least one enforcement pointer)
    - SIGN-OFF.md exists with STATUS draft, a procedure checklist, and a reviewed-files row naming AUTHORITY.md
    - Both files are tracked (git ls-files names them) and the quick gate exits 0
  </acceptance_criteria>
  <done>Authority slice exists staged with the gate green; schema work has a signed foundation to build on</done>
</task>

<task type="auto">
  <name>Reviewer pass and recorded approval for the authority table</name>
  <reversibility rating="reversible">Approval record edit; re-review rewrites one file.</reversibility>
  <read_first>
    - .planning/phases/11-sql-store-design/authority/AUTHORITY.md
    - .planning/phases/11-sql-store-design/authority/SIGN-OFF.md
    - brand/HUMAN-REVIEW.md (sign-off record section)
  </read_first>
  <files>.planning/phases/11-sql-store-design/authority/SIGN-OFF.md, .planning/phases/11-sql-store-design/authority/AUTHORITY.md</files>
  <action>Review each invariant row against the checklist: each names its Phase 12 enforcement; prose uses upstream and originating-product wording with no brand token; engine is SQLite only with no rejected-engine surface; no manifest keys; no GUI surface; no internal identifier in user-facing prose. Fix defects in AUTHORITY.md directly. Record the approval in SIGN-OFF.md by flipping STATUS to a recorded approval with reviewer name, date, per-item checklist results, and the assumed-items disposition. Stage both files and run the quick gate.</action>
  <verify>
    <automated>git add .planning/phases/11-sql-store-design/authority/AUTHORITY.md .planning/phases/11-sql-store-design/authority/SIGN-OFF.md && scripts/verify-platform.sh --quick && grep -q 'RECORDED' .planning/phases/11-sql-store-design/authority/SIGN-OFF.md</automated>
    <fails_when>quick gate exits non-zero, or no recorded-approval marker is present in SIGN-OFF.md</fails_when>
    <human-check>Reviewer reads AUTHORITY.md end to end and confirms the six invariants match SQL-02 with each enforcement pointer correct</human-check>
  </verify>
  <acceptance_criteria>
    - SIGN-OFF.md carries a recorded approval with name, date, and per-item checklist results
    - Every checklist item passes including brand-token absence via the green residue row inside the quick gate
    - Quick gate exits 0 on the final tree
  </acceptance_criteria>
  <done>SQL-02 authority table is reviewed and signed with the record on disk</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chrome-side writer to profile database file | Untrusted tab content (URI, title strings) crosses into SQL through the single writer |
| Theia backend to dedicated database file | Backend holds a readonly handle on the dedicated file only, never profile databases |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-01 | Tampering | chrome-side writer (Phase 12) | high | mitigate | Single-writer invariant stated; Phase 12 negative scan specified as enforcement |
| T-11-02 | Information disclosure | private-tab exclusion rule | high | mitigate | Exclusion-at-writer invariant stated; emitter-exercising absence test specified for Phase 12 |
| T-11-03 | Information disclosure | tab row contents | medium | mitigate | Token-never-in-SQL invariant stated in the table |
| T-11-04 | Tampering | Theia backend database opens | high | mitigate | Backend-never-opens-profile-databases invariant plus own-file rule stated |
| T-11-05 | Denial of service | corruption recovery path | medium | mitigate | Quarantine-not-delete referenced; full procedure owned by plan 11-02 |
| T-11-06 | Elevation of privilege | manifest parameterization of the store | medium | mitigate | Filename and schema version stated as fixed platform content, never manifest keys |

No package installs occur in this plan; the reader package pre-clear lives in RESEARCH and its install belongs to Phase 12.
</threat_model>

<verification>
scripts/verify-platform.sh --quick green on the final tree with both authority files staged; SIGN-OFF.md recorded approval present; AUTHORITY.md six rows each with an enforcement pointer.
</verification>

<success_criteria>
SQL-02 authority table exists, is reviewed and signed with a recorded checklist approval, and the static gates are green with no store code, no manifest change, and no GUI surface.
</success_criteria>

<output>
Create nothing outside authority/AUTHORITY.md and authority/SIGN-OFF.md; the 11-02 schema plan consumes AUTHORITY.md as its governing constraint.
</output>
