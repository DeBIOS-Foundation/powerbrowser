---
phase: 11-sql-store-design
plan: 03
type: execute
wave: 3
depends_on: [11-02]
files_modified: [.planning/phases/11-sql-store-design/11-REVIEW.md]
autonomous: true
requirements: [SQL-02, SQL-03]
must_haves:
  truths:
    - "Both recorded approvals exist and are named in one gate record with a requirements trace"
    - "Static gates are green on the final tree including the registry-shape and catalogue checks"
    - "Absence of rejected-engine surface, manifest parameterization, GUI surface, and new touchpoint rows is proven and recorded"
    - "Phase 12 handoff lists the specified registry rows, the reader install, and the rebase drill"
  artifacts:
    - path: ".planning/phases/11-sql-store-design/11-REVIEW.md"
      provides: "Review-sign gate record with evidence, trace, and Phase 12 handoff"
      min_lines: 40
  key_links:
    - from: ".planning/phases/11-sql-store-design/11-REVIEW.md"
      to: ".planning/phases/11-sql-store-design/authority/SIGN-OFF.md"
      via: "gate record cites the authority approval status"
      pattern: "authority/SIGN-OFF\\.md"
    - from: ".planning/phases/11-sql-store-design/11-REVIEW.md"
      to: ".planning/phases/11-sql-store-design/schema/SIGN-OFF.md"
      via: "gate record cites the schema approval status"
      pattern: "schema/SIGN-OFF\\.md"
---

<objective>
Assemble the review-sign gate proving both design documents are signed, the static gates are green, and Phase 12 has a specified handoff.

Purpose: The phase goal requires authority rules plus schema written down and reviewed before any store code exists; this plan records that both halves hold on the final tree and closes SQL-02 with SQL-03.
Output: 11-REVIEW.md gate record with evidence, requirements trace, and Phase 12 handoff, static gates green.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-sql-store-design/11-VALIDATION.md
@.planning/phases/11-sql-store-design/authority/SIGN-OFF.md
@.planning/phases/11-sql-store-design/schema/SIGN-OFF.md
@.planning/research/duckdb-vs-sqlite/VERDICT.md
@scripts/verify-registry-shape.mjs
@scripts/check-internals-boundary.sh
</context>

<tasks>

<!-- planner-discipline-allow: duckdb -->
<!-- planner-discipline-allow: configuration.toml -->

<task type="auto">
  <name>Assemble the gate record with evidence and requirements trace</name>
  <reversibility rating="reversible">Gate record edit; re-verification rewrites one file.</reversibility>
  <read_first>
    - .planning/phases/11-sql-store-design/authority/SIGN-OFF.md
    - .planning/phases/11-sql-store-design/schema/SIGN-OFF.md
    - .planning/phases/11-sql-store-design/11-VALIDATION.md (manual-only boxes, sampling map)
    - .planning/phases/11-sql-store-design/11-RESEARCH.md (Recommended Plan Decomposition item 3, Validation Architecture)
  </read_first>
  <files>.planning/phases/11-sql-store-design/11-REVIEW.md</files>
  <action>Write 11-REVIEW.md recording the gate evidence: presence plus recorded-approval status of both sign-off files with reviewer names and dates; quick-gate result; registry-shape check result proving the frozen contract untouched; internals-catalogue check result proving zero new touchpoint rows by construction; negative-assertion results for the rejected engine surface across the new doc directories, for manifest parameterization keys across the new doc directories, and for GUI surface proven by a changed-file listing confined to the phase directory pasted into the record; a requirements-trace table mapping the authority requirement to the authority documents and the schema requirement to the schema documents plus fixture and script; and a Phase 12 handoff list naming the four specified registry rows with self-test obligations, the pre-cleared reader install, and the live rebase drill over the new touchpoints. Run every check command while assembling so the record holds observed results, not recollections. Leave the validation-strategy file untouched for the verifier.</action>
  <verify>
    <automated>test -f .planning/phases/11-sql-store-design/11-REVIEW.md && grep -q 'authority/SIGN-OFF.md' .planning/phases/11-sql-store-design/11-REVIEW.md && grep -q 'schema/SIGN-OFF.md' .planning/phases/11-sql-store-design/11-REVIEW.md && scripts/verify-platform.sh --quick</automated>
    <fails_when>gate record absent, either sign-off citation missing from it, or the quick gate exits non-zero</fails_when>
  </verify>
  <acceptance_criteria>
    - 11-REVIEW.md cites both sign-off files with approval status plus the shape and catalogue check results
    - 11-REVIEW.md holds the negative-assertion results and the changed-file listing confined to the phase directory
    - Requirements trace maps both phase requirements to their documents and the Phase 12 handoff is complete
  </acceptance_criteria>
  <done>Gate record exists with observed evidence, trace, and handoff</done>
</task>

<task type="auto">
  <name>Final sweep and phase closeout</name>
  <reversibility rating="reversible">Closeout-line edit on the gate record.</reversibility>
  <read_first>
    - .planning/phases/11-sql-store-design/11-REVIEW.md
    - .planning/phases/11-sql-store-design/authority/SIGN-OFF.md
    - .planning/phases/11-sql-store-design/schema/SIGN-OFF.md
  </read_first>
  <files>.planning/phases/11-sql-store-design/11-REVIEW.md</files>
  <action>Re-run the quick gate plus the registry-shape check plus the catalogue check on the final tree, confirm both sign-off files still carry recorded approvals, confirm the rejected-engine and manifest-key negative checks still report absence over the new doc directories, append the closeout line to 11-REVIEW.md with the final gate result and the design-only confirmation naming zero new files outside the planning record, stage the record, and run the quick gate once more.</action>
  <verify>
    <automated>git add .planning/phases/11-sql-store-design/11-REVIEW.md && scripts/verify-platform.sh --quick && node scripts/verify-registry-shape.mjs && bash scripts/check-internals-boundary.sh --catalogue && grep -q 'RECORDED' .planning/phases/11-sql-store-design/authority/SIGN-OFF.md && grep -q 'RECORDED' .planning/phases/11-sql-store-design/schema/SIGN-OFF.md && ! grep -rEi 'duckdb' .planning/phases/11-sql-store-design/authority .planning/phases/11-sql-store-design/schema .planning/phases/11-sql-store-design/fixtures && ! grep -r 'configuration.toml' .planning/phases/11-sql-store-design/authority .planning/phases/11-sql-store-design/schema</automated>
    <fails_when>any check exits non-zero, either recorded approval is missing, or either absence check finds a match in the new doc directories</fails_when>
    <human-check>Reviewer reads 11-REVIEW.md and confirms both requirement boxes plus the Phase 12 handoff before verify-work runs</human-check>
  </verify>
  <acceptance_criteria>
    - Quick gate, registry-shape check, and catalogue check all exit 0 on the final tree
    - Both sign-off files carry recorded approvals and both absence checks report no match
    - 11-REVIEW.md closeout line records the final result with the design-only confirmation
  </acceptance_criteria>
  <done>Phase 11 gate is green and recorded; Phase 12 can start from the signed design</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gate record to evidence | 11-REVIEW.md must hold observed check results, never recollected claims |
| design phase to build phase | Nothing executable crosses into Phase 12 except the specified handoff list |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-13 | Tampering | exercise-log credibility | medium | mitigate | Non-vacuity controls required in the log; this gate confirms the firing drives are present |
| T-11-14 | Tampering | hand-kept expectations in specified gates | medium | mitigate | Handoff requires derive-at-run-time plus self-test on every specified Phase 12 row |
| T-11-15 | Tampering | unsigned document advancing | medium | mitigate | Both recorded approvals are re-confirmed on the final tree before closeout |
| T-11-16 | Information disclosure | brand token in new docs | low | mitigate | Residue row inside the green quick gate covers the staged record |
| T-11-17 | Denial of service | store code landing in a design phase | low | mitigate | Changed-file listing confined to the phase directory plus zero new catalogue rows proven |

No package installs occur in this plan; no GUI surface is introduced.
</threat_model>

<verification>
Quick gate plus registry-shape plus catalogue checks green on the final tree; both sign-off files recorded; absence checks clean over the new doc directories; 11-REVIEW.md holds evidence, trace, handoff, and closeout.
</verification>

<success_criteria>
SQL-02 and SQL-03 close with both recorded approvals present, every gate check green, absence of rejected-engine surface and manifest parameterization proven, zero new touchpoint rows, and a Phase 12 handoff specifying the registry rows, reader install, and rebase drill.
</success_criteria>

<output>
Phase 12 consumes 11-REVIEW.md as its entry contract; the validation-strategy file stays for the verifier untouched by this plan.
</output>
