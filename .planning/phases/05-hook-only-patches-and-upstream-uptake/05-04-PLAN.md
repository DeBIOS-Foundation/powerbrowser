---
phase: 05-hook-only-patches-and-upstream-uptake
plan: "04"
type: execute
wave: 4
depends_on:
  - 05-03
files_modified:
  - scripts/apply-patches.sh
  - scripts/fetch-upstream.sh
  - scripts/rebase-upstream.sh
  - scripts/verify-platform.sh
  - .github/workflows/rebase-upstream.yml
autonomous: true
requirements:
  - UPD-01
must_haves:
  truths:
    - "Upstream drift that breaks a patch fails loudly by patch name instead of proceeding"
    - "A silently-adopted patch (exit 0, nothing changed) is rejected by name on replay"
    - "The full uptake path is drillable short of the clone, and the live steps are exact commands"
  artifacts:
    - path: "scripts/apply-patches.sh"
      provides: "Replay with non-vacuity assertion (existing, fixture-proven here)"
    - path: "scripts/rebase-upstream.sh"
      provides: "Clone/replay/verify composition with dry-run proof"
    - path: ".github/workflows/rebase-upstream.yml"
      provides: "CI dispatch path for the live rebase drill"
  key_links:
    - from: "scripts/rebase-upstream.sh"
      to: "scripts/apply-patches.sh"
      via: "rebase composes replay; replay failure aborts before surface check"
      pattern: "apply-patches\.sh"
    - from: "scripts/rebase-upstream.sh"
      to: "scripts/check-patch-surface.sh"
      via: "surface check gates the replayed stack before success is reported"
      pattern: "check-patch-surface\.sh"
---

<objective>
Prove the uptake path fails loudly on every drift class hermetically, drill everything short of the clone, and stage the live rebase plus branded build as exact commands.

Purpose: UPD-01 — an ESR point release is adopted by bumping one pin, with tooling that fails loudly on drift. No fetch, clone, or build runs inside this plan's tasks.
Output: Hermetic drift fixtures proving loud failure, dry-run proof, CI dispatch readiness, staged live-drill commands.
</objective>

<execution_context>
<purpose>Execute this PLAN.md exactly as written and produce its SUMMARY.md.</purpose>
<required_reading>
Read .planning/STATE.md before any operation to load project context.
Read .planning/config.json for planning behavior settings.
</required_reading>
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/05-hook-only-patches-and-upstream-uptake/05-CONTEXT.md
@.planning/phases/05-hook-only-patches-and-upstream-uptake/05-03-SUMMARY.md
@scripts/apply-patches.sh
@scripts/rebase-upstream.sh
</context>

<tasks>

<task type="auto">
  <name>Prove loud failure on drift with hermetic fixtures</name>
  <files>scripts/apply-patches.sh, scripts/fetch-upstream.sh</files>
  <action>Without touching upstream/ or any network, build mktemp-dir fixtures that exercise the shipped gate logic against the three silent drift classes: (1) real conflict — a patch whose context no longer matches the target file is rejected naming the patch (drive apply_patch_with_assertion from apply-patches.sh against a throwaway repo, or extend its --self-test if the shape does not fit the existing fixture); (2) silent adoption — already covered by the existing second-apply self-test, re-run it and cite the output, do not duplicate it; (3) unaccounted dirt — already covered by fetch-upstream.sh --self-test cases 3 and 4, re-run and cite. Where an existing self-test already proves a class, citing its passing output in the SUMMARY is the deliverable — do not add redundant fixtures. Add a new fixture ONLY for a class with no red-capable proof (expected gap, if any: a 3-way-mergeable context shift that applies cleanly — document explicitly that git apply --3way resolving clean drift is correct behavior, and that the backstop for that class is the post-replay chain: check-patch-surface plus scan-brand-residue --extra-root plus fetch re-check, all already composed in rebase-upstream.sh). NEVER rm -rf upstream/, never clone, never cd into upstream/ for mutation.</action>
  <verify><automated>bash scripts/apply-patches.sh --self-test && bash scripts/fetch-upstream.sh --self-test && bash scripts/check-patch-surface.sh --self-test</automated></verify>
  <done>Every drift class maps to a red-capable proof (new or cited); the 3-way-clean case is documented as correct-apply with named backstops</done>
</task>

<task type="auto">
  <name>Drill the uptake path short of the clone and stage the live steps</name>
  <files>scripts/rebase-upstream.sh, .github/workflows/rebase-upstream.yml, scripts/verify-platform.sh</files>
  <action>Run the deepest drill possible without clone or build: rebase --dry-run against the manifest pin from 05-02 (this performs the cheap ls-remote tag-existence check only), the full static gate set from plans 05-01..05-03, and a read-through of .github/workflows/rebase-upstream.yml confirming the workflow consumes the same pin mirror the agreement check covers and runs generate/check/byte-identity before the replay plus byte-identity after (fail-fast ordering per its own comments). Fix any prose or ordering defect found; do not restructure the workflow. Then record in the SUMMARY the staged live drill as exact commands for CI dispatch or operator run (NOT executed here): dispatch the Rebase upstream workflow with tag = next ESR pin, or locally TAG=&lt;new-pin&gt; bash scripts/rebase-upstream.sh --tag &lt;new-pin&gt; followed by the toolchain-baseline diff under nix develop .#firefox and the tier-3 branded build plus built-artifact branding proof per the 03-04 precedent (dev variant, ~47-54 min reference timing). State the prerequisite this plan deliberately leaves open: the live drill needs a real next ESR tag and CI minutes, neither of which planning spends.</action>
  <verify><automated>bash scripts/rebase-upstream.sh --tag "$(grep -E '^FIREFOX_ESR_TAG=' generated/upstream-pins.env | cut -d= -f2)" --dry-run && node scripts/verify-upstream-pins.mjs</automated></verify>
  <done>Dry-run green against manifest pin; workflow read-through clean; live drill staged as exact unexecuted commands</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| replayed tree → build | A silently mis-patched Gecko tree would ship wrong branding or telemetry defaults |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-07 | Tampering | Silent patch drop on rebase | high | mitigate | Non-vacuity blob-hash assertion per patch plus D-76 dirt classifier plus post-replay surface and brand scans — four independent gates, each fixture-proven red-capable |
| T-05-08 | DenialOfService | Rebase blocked by drift | low | accept | A conflict that --3way cannot resolve aborts loudly by patch name; operator regenerates from the patched tree. Loud blockage is the designed behavior, documented as remediation output |
</threat_model>

<verification>
Static only — the live rebase and tier-3 build are staged, not run:
1. All three --self-test suites green (apply, fetch classifier, patch surface) plus pin agreement check
2. `rebase --dry-run` against the manifest pin exits 0
3. SUMMARY contains the exact staged live-drill commands (workflow dispatch + local rebase + toolchain diff + tier-3 build) marked UNEXECUTED with their prerequisites
4. `scripts/verify-platform.sh --quick` exits 0
</verification>

<success_criteria>
- UPD-01 proven as far as statically provable: loud failure on every drift class, drill green short of clone
- Live rebase + branded build staged as exact commands with named prerequisites; nothing fetched or built by planning
- Phase goal met in full when the staged drill runs green (recorded as the phase's known-open verification)
</success_criteria>

<output>
Create `.planning/phases/05-hook-only-patches-and-upstream-uptake/05-04-SUMMARY.md` when done
</output>
