---
phase: 05-hook-only-patches-and-upstream-uptake
plan: "03"
type: execute
wave: 3
depends_on:
  - 05-02
files_modified:
  - configuration.toml
  - scripts/lib/config-schema.json
  - scripts/generate.mjs
  - scripts/verify-upstream-pins.mjs
  - scripts/verify-platform.sh
  - docs/BUILD.md
autonomous: true
requirements:
  - CFG-06
  - UPD-02
must_haves:
  truths:
    - "Theia release is declared once in configuration.toml and every @theia/* pin agrees with it or the check fails naming the file"
    - "A documented one-pin re-pin procedure exists that never edits Theia core"
    - "diff-theia-core still proves zero changes inside @theia/* after the wiring"
  artifacts:
    - path: "scripts/verify-upstream-pins.mjs"
      provides: "Theia pin agreement coverage over all member package.json files, resolutions, and lockfile"
    - path: "configuration.toml"
      provides: "[upstreams] theia_release single declaration"
    - path: "docs/BUILD.md"
      provides: "One-pin Theia re-pin procedure"
  key_links:
    - from: "configuration.toml"
      to: "theia/package.json"
      via: "agreement check compares manifest pin against resolutions block and member pins"
      pattern: "@theia.*1\."
    - from: "scripts/verify-upstream-pins.mjs"
      to: "theia/yarn.lock"
      via: "lockfile resolved @theia tarballs must match the manifest pin"
      pattern: "@theia"
---

<objective>
Single-source the Theia pin in configuration.toml, prove agreement across every package.json and the lockfile, and document the re-pin procedure that keeps core unforked.

Purpose: CFG-06 (Theia half) and UPD-02 — a Theia release is adopted by re-pinning, with core neither forked nor patched.
Output: Manifest theia_release key, extended agreement check, re-pin procedure, registry rows.
</objective>

<execution_context>
<purpose>Execute this PLAN.md exactly as written and produce its SUMMARY.md.</purpose>
<required_reading>
Read .planning/STATE.md before any operation to load project context.
Read .planning/config.json for planning behavior settings.
</required_reading>
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/05-hook-only-patches-and-upstream-uptake/05-CONTEXT.md
@.planning/phases/05-hook-only-patches-and-upstream-uptake/05-02-SUMMARY.md
@scripts/diff-theia-core.sh
@theia/package.json
</context>

<tasks>

<task type="auto">
  <name>Declare theia_release and extend the agreement check over the Theia tree</name>
  <files>configuration.toml, scripts/lib/config-schema.json, scripts/generate.mjs, scripts/verify-upstream-pins.mjs</files>
  <action>Add theia_release to the [upstreams] section created in 05-02, carrying the currently pinned version (derive it from theia/package.json resolutions @theia/core — measured 1.74.1 across all member files; do not bump). Schema key plus generate.mjs validation: required key hard-failing by name when unset, strict numeric-triple shape. The generator must NOT rewrite theia/package.json (GEN-04: generator output lands only under gitignored generated/ — package.json files are committed build input, so rewriting them from the generator would break that boundary). Instead extend scripts/verify-upstream-pins.mjs to derive the expected version from the manifest at check time and assert set-equality: every @theia/* pin in theia/package.json resolutions, every member applications/*/package.json and extensions/*/package.json, and the resolved @theia tarball entries in theia/yarn.lock all equal the manifest pin — failing by file name on any drift, in either direction (a member pin the manifest does not know, or a manifest pin no member carries). Note the known exception inside the derivation: @theia/monaco-editor-core carries its own upstream-tracked version (measured 1.108.201) and is excluded by exact package name, documented in the check comment. Add --self-test rows: one member pin bumped by a patch version goes red naming the file; clean tree green.</action>
  <verify><automated>node scripts/generate.mjs --check && node scripts/verify-upstream-pins.mjs && node scripts/verify-upstream-pins.mjs --self-test</automated></verify>
  <done>Manifest declares theia_release once; agreement check covers resolutions, members, and lockfile with red-capable self-test</done>
</task>

<task type="auto">
  <name>Document the one-pin re-pin procedure and prove core untouched</name>
  <files>docs/BUILD.md, scripts/verify-platform.sh</files>
  <action>In docs/BUILD.md (read its Theia section first; append alongside existing timings rather than restructuring) document the re-pin procedure as exact commands: edit theia_release in configuration.toml, run the stock yarn upgrade sequence inside nix develop .#theia that moves all @theia/* pins plus resolutions plus yarn.lock to the new version, run node scripts/generate.mjs, then prove with node scripts/verify-upstream-pins.mjs and the diff-theia-core rows. State explicitly what the procedure never does: no edit under theia/node_modules/@theia, no patch file touching Theia sources, no vendored monorepo. Do NOT perform a real re-pin (no yarn install, no lockfile rewrite, no network) — the procedure is proven structurally here and exercised live only if an actual Theia release demands it. Then run the diff-theia-core proof against the current tree: --quick always; full stage 2 only if theia/node_modules/@theia already exists locally (it requires a prior yarn install — if absent, record as open verification, do NOT install as part of this task since yarn needs the nix shell and network). Register any new rows (pin check theia half is the same script's rows from 05-02 — add distinct --only labels only if the script exposes them).</action>
  <verify><automated>node scripts/verify-upstream-pins.mjs && bash scripts/diff-theia-core.sh --quick || echo "OPEN: node_modules absent, full diff deferred"</automated></verify>
  <done>Re-pin procedure documented as exact commands; core-untouched proof green where locally provable, open items named</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Open VSX / npm registry → theia/node_modules | Third-party tarballs resolved by yarn |
| manifest → package pins | Pin value drives which upstream code ships |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-05 | Tampering | Theia tarball supply chain | high | mitigate | Committed yarn.lock with integrity hashes plus yarn --frozen-lockfile installs; diff-theia-core stage 1 (integrity) and stage 2 (fresh-resolve diff) prove zero in-tree changes |
| T-05-06 | Tampering | Pin drift across member files | medium | mitigate | Manifest-derived set-equality check over resolutions, members, and lockfile fails naming the file |
</threat_model>

<verification>
Static only — no yarn install, no re-pin, no build in this plan:
1. `node scripts/verify-upstream-pins.mjs` plus `--self-test` green (both ESR and Theia halves)
2. `bash scripts/diff-theia-core.sh --quick` green where node_modules exists, else OPEN and named
3. Single-source grep-proof recorded in SUMMARY: the Theia version string appears only in configuration.toml, the derived/generated consumers, package pins, lockfile, and check fixtures — nowhere else as an independent declaration
4. `scripts/verify-platform.sh --quick` exits 0
</verification>

<success_criteria>
- Theia pin declared once with full-tree agreement check and red-capable self-test
- One-pin re-pin procedure documented; core-untouched posture preserved and proven
- CFG-06 complete (both pins); UPD-02 complete structurally
</success_criteria>

<output>
Create `.planning/phases/05-hook-only-patches-and-upstream-uptake/05-03-SUMMARY.md` when done
</output>
