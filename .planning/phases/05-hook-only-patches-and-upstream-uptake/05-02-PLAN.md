---
phase: 05-hook-only-patches-and-upstream-uptake
plan: "02"
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - configuration.toml
  - scripts/lib/config-schema.json
  - scripts/generate.mjs
  - scripts/fetch-upstream.sh
  - scripts/rebase-upstream.sh
  - scripts/verify-upstream-pins.mjs
  - scripts/verify-platform.sh
  - .github/workflows/rebase-upstream.yml
autonomous: true
requirements:
  - CFG-06
  - UPD-01
must_haves:
  truths:
    - "Firefox ESR tag is declared once in configuration.toml and every consumer reads that declaration"
    - "A pin/checkout mismatch fails loudly naming the expected tag before any clone or replay runs"
    - "No second tracked file carries its own copy of the tag string"
  artifacts:
    - path: "scripts/verify-upstream-pins.mjs"
      provides: "ESR pin agreement check with --self-test"
      exports: ["pin derivation from manifest", "multi-consumer agreement"]
    - path: "generated/upstream-pins.env"
      provides: "Shell-consumable pin fragment emitted by the generator"
    - path: "scripts/fetch-upstream.sh"
      provides: "TAG default sourced from the single pin, never a hardcoded literal"
  key_links:
    - from: "configuration.toml"
      to: "scripts/generate.mjs"
      via: "new [upstreams] schema keys validated at generate time"
      pattern: "upstreams.*firefox_esr_tag"
    - from: "generated/upstream-pins.env"
      to: "scripts/fetch-upstream.sh"
      via: "shell sources the generated fragment for its TAG default"
      pattern: "upstream-pins"
    - from: "scripts/verify-upstream-pins.mjs"
      to: ".github/workflows/rebase-upstream.yml"
      via: "check asserts the workflow input default equals the manifest pin"
      pattern: "FIREFOX_"
---

<objective>
Single-source the Firefox ESR pin in configuration.toml and rewire every consumer to read it, with an agreement check that fails loudly on drift.

Purpose: CFG-06 (ESR half) plus the UPD-01 wiring — adopting a release must start with editing one pin.
Output: [upstreams] manifest section, schema + validation, generated shell fragment, rewired fetch/rebase/workflow, new registered check.
</objective>

<execution_context>
<purpose>Execute this PLAN.md exactly as written and produce its SUMMARY.md.</purpose>
<required_reading>
Read .planning/STATE.md before any operation to load project context.
Read .planning/config.json for planning behavior settings.
</required_reading>
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/05-hook-only-patches-and-upstream-uptake/05-CONTEXT.md
@.planning/phases/05-hook-only-patches-and-upstream-uptake/05-01-SUMMARY.md
@scripts/fetch-upstream.sh
@scripts/rebase-upstream.sh
</context>

<tasks>

<task type="auto">
  <name>Declare [upstreams] firefox_esr_tag in manifest, schema, and generator</name>
  <files>configuration.toml, scripts/lib/config-schema.json, scripts/generate.mjs</files>
  <action>Add an [upstreams] section to configuration.toml carrying firefox_esr_tag = the currently pinned tag (read the live default out of scripts/fetch-upstream.sh line 7 — do not invent or bump the tag; a bump is plan 05-04's explicitly scoped drill, not this task). Add matching keys to scripts/lib/config-schema.json following the existing key-spec shape (study two neighboring entries first). Validation in scripts/generate.mjs: required key (hard-fail naming upstreams.firefox_esr_tag when unset, per the CFG-02 pattern), plain-tag-name shape matching rebase-upstream.sh's existing character class so a glob can never be a valid pin. Emit generated/upstream-pins.env (KEY=value shell fragment, header comment with the do-not-edit + rerun line per emitter convention) carrying the tag. Check whether scripts/verify-generated-identity.mjs needs the new fragment in its comparand set and wire it the same way the other fragments are wired.</action>
  <verify><automated>node scripts/generate.mjs && node scripts/generate.mjs --check && grep -q 'FIREFOX_' generated/upstream-pins.env</automated></verify>
  <done>Manifest declares the tag once; generate emits the fragment; --check green; unset pin hard-fails naming the key</done>
</task>

<task type="auto">
  <name>Rewire fetch, rebase, and workflow to consume the single pin</name>
  <files>scripts/fetch-upstream.sh, scripts/rebase-upstream.sh, .github/workflows/rebase-upstream.yml</files>
  <action>Change scripts/fetch-upstream.sh so TAG defaults to the value in generated/upstream-pins.env (source the fragment; explicit TAG= env export still overrides, preserving the rebase flow and the existing HEAD-vs-tag hash comparison, which stays byte-identical logic). If generated/ is absent (fresh clone), fail with a message naming the missing fragment and the rerun command — never fall back to a hardcoded tag literal, and remove the hardcoded default literal from the script. Update scripts/rebase-upstream.sh --dry-run text and comments that name the old default mechanism so no prose claims a hardcoded default still exists. For .github/workflows/rebase-upstream.yml: the workflow_dispatch input default cannot read TOML, so keep a literal default there but make it check-covered (next task), and extend the surrounding comment to state that the manifest is authoritative and the literal is a mirror. NEVER trigger a real clone, fetch, or rm -rf of upstream/ in this task — aiming every command at --dry-run and --self-test paths only.</action>
  <verify><automated>bash scripts/fetch-upstream.sh --self-test && bash scripts/rebase-upstream.sh --tag "$(grep -E '^FIREFOX_ESR_TAG=' generated/upstream-pins.env | cut -d= -f2)" --dry-run</automated></verify>
  <done>No hardcoded tag literal remains in fetch-upstream.sh; dry-run against the manifest pin exits 0 without cloning</done>
</task>

<task type="auto">
  <name>Add the pin-agreement check and register it</name>
  <files>scripts/verify-upstream-pins.mjs, scripts/verify-platform.sh</files>
  <action>Create scripts/verify-upstream-pins.mjs following the repo's checker conventions (named FAIL lines, --self-test with planted faults proven red-under-fixed plus a clean control). It derives the expected tag from configuration.toml at check time via scripts/lib/toml.cjs and asserts set-equality in both directions: manifest pin equals generated/upstream-pins.env value equals workflow input default equals fetch-upstream.sh's effective default (resolved without cloning — parse, do not execute the clone path), and no other in-scope tracked file contains a FIREFOX_ tag literal. Scope the literal sweep to build-relevant tree (scripts/, theia/, .github/, root toml/nix/mk files, generated/ excluded as derived output); explicitly exclude .planning/ and inventory history from the sweep and document why (historical record, not build input). --self-test rows: mirrored default edited to a neighbor tag goes red naming the file; planted second literal in scripts/ goes red; clean tree green. Register both rows in scripts/verify-platform.sh next to the fetch-upstream rows.</action>
  <verify><automated>node scripts/verify-upstream-pins.mjs && node scripts/verify-upstream-pins.mjs --self-test && scripts/verify-platform.sh --only fetch-upstream-self-test</automated></verify>
  <done>Agreement check green on shipped tree, red on both planted faults, registered in the driver</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| manifest → fetch/clone | Pin value drives which upstream commit is cloned |
| remote → local | Cloned Gecko tree from github.com |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-03 | Tampering | Pin consumption chain | high | mitigate | Manifest is local trusted input; agreement check fails loudly on mirror drift; fetch keeps HEAD-vs-tag hash comparison |
| T-05-04 | Spoofing | Upstream remote | medium | transfer | GitHub TLS + tag-existence gate; toolchain-baseline operator diff after rebase stays the human backstop (plan 05-04) |
</threat_model>

<verification>
Static only — no clone, no fetch, no build in this plan:
1. `node scripts/generate.mjs --check` and `generated-byte-identity` rows green
2. `node scripts/verify-upstream-pins.mjs` plus `--self-test` green
3. `bash scripts/rebase-upstream.sh --tag <manifest-pin> --dry-run` exits 0 (only network is the cheap ls-remote existence check)
4. Single-source grep-proof recorded in SUMMARY: the tag literal appears only in configuration.toml, the generated fragment, the workflow mirror, and the checker's own self-test fixtures
5. `scripts/verify-platform.sh --quick` exits 0
</verification>

<success_criteria>
- ESR pin declared once, consumed by fetch/rebase/workflow, agreement-checked with red-capable self-test
- CFG-06 ESR half complete; Theia half is plan 05-03; live bump drill is plan 05-04
</success_criteria>

<output>
Create `.planning/phases/05-hook-only-patches-and-upstream-uptake/05-02-SUMMARY.md` when done
</output>
