---
phase: 03-firefox-branding-emitter-and-icon-pipeline
plan: 04
type: execute
wave: 4
depends_on: [03-03]
files_modified: [scripts/generate.mjs, patches/010-powerbrowser-identity.patch, scripts/verify-platform.sh, .mozconfig, docs/BUILD.md]
autonomous: true
requirements: [GEN-01, GEN-02, GEN-03]
estimate:
  tokens: 40000
  raw_tokens: 80000
  tasks: 3
  confidence: high
assumption_delta_decision:
  signal: "pluralization 'second' in 'no second file edited' (success criterion 4)"
  decision: "add-alongside-nothing — covered by this plan's task 2"
  rationale: "The flagged noun is criterion-4 phrasing for single-edit propagation, not a new artifact proposal. Task 2 proves it directly: one manifest edit plus regenerate changes every Gecko-side surface with the byte-identity gate confirming the rest of the tree untouched."
must_haves:
  truths:
    - "MOZ_APP_VENDOR and MOZ_APP_UA_NAME reach the build from generated output, not from hard-coded patch lines, proven by an autoconf.mk diff after a forced reconfigure"
    - "Changing identity.display_name once and regenerating changes every Gecko-side branding surface with no other file edited"
    - "One full Linux build from generated output produces the correctly branded application with the downstream icon, verified against the built artifact"
  artifacts:
    - path: "patches/010-powerbrowser-identity.patch"
      provides: "De-configured identity patch with vendor lines moved to generated output"
      contains: "MOZ_APP_VENDOR"
    - path: "scripts/generate.mjs"
      provides: "Identity carrier emitter (mozconfig exports or identity.configure include per spike)"
      contains: "MOZ_APP_VENDOR"
  key_links:
    - from: "scripts/generate.mjs"
      to: "objdir/config/autoconf.mk"
      via: "Generated carrier values resolve through ./mach configure into MOZ_APP_VENDOR and MOZ_APP_DISPLAYNAME"
      pattern: "MOZ_APP_VENDOR"
    - from: "patches/010-powerbrowser-identity.patch"
      to: "scripts/check-patch-surface.sh"
      via: "Regenerated patch must keep the surface green with the non-vacuity assertion still firing"
      pattern: "check-patch-surface"
  prohibitions:
    - requirement_id: "GEN-01"
      category: "transparency"
      status: "resolved"
      verification: "test"
      resolution: null
      reason: null
      statement: "MUST NOT let a downstream build ship under Power Browser display strings, mark, or vendor identity by omission — identity and legal keys hard-fail with the offending key named, and cosmetic defaults always echo visibly, so no brand value is ever inherited silently"
---

<objective>
Close the phase: move the last hard-coded identity values out of the patch stack into generated output, prove single-edit propagation across every surface, and build the branded Linux application once from the generated tree.

Purpose: Criteria 1 through 4 converge here — the branding directory exists (03-01), the icons exist (03-02), the installer fields exist (03-03), and this plan proves the build consumes them, the patch carries no brand values for them, and one manifest edit moves them all.
Output: Identity carrier in generated output, regenerated patch 010, propagation proof, tier-3 Linux build green on the built artifact.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-firefox-branding-emitter-and-icon-pipeline/03-RESEARCH.md
@.planning/phases/03-firefox-branding-emitter-and-icon-pipeline/03-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Identity carrier plus patch 010 de-configuration</name>
  <files>scripts/generate.mjs, patches/010-powerbrowser-identity.patch, .mozconfig</files>
  <read_first>scripts/generate.mjs (emitMozconfig emitter around lines 687-760, four manifest-derived values), patches/010-powerbrowser-identity.patch (all 24 lines, the vendor lines 19-22), scripts/check-patch-surface.sh (surface rule lines 46-56), scripts/apply-patches.sh (--self-test mechanism), .planning/phases/03-firefox-branding-emitter-and-icon-pipeline/03-01-SUMMARY.md (spike answers: exports versus include carrier)</read_first>
  <reversibility rating="costly">A wrong carrier misbrands or breaks every variant build; undo needs a second patch regen plus a forced reconfigure, coordinated across the patch stack and the build tree.</reversibility>
  <action>Implement the spike-winning carrier exactly. When exports won: extend emitMozconfig with export MOZ_APP_VENDOR carrying product.vendor_machine and export MOZ_APP_UA_NAME carrying the literal Firefox, both values through assertEmittable, and update the tracked .mozconfig in the same commit per the 02-06 D-03 precedent. When the include route won: add emitIdentityConfigure producing generated/identity.configure with the two imply_option lines carrying the same two values plus a hash-comment banner naming configuration.toml and the re-run command, add its frozen TARGETS row with no tracked comparand, and leave .mozconfig exports out. Either way regenerate patches/010-powerbrowser-identity.patch from a patched tree using the repo's own regeneration flow (never hand-edit the .patch file): drop the MOZ_APP_VENDOR and MOZ_APP_UA_NAME hard-codes, keep the MOZ_APP_ID imply line and the healthreport plus normandy False lines byte-identical, and for the include route add the single include hook line in their place. Then force ./mach configure under the firefox nix shell (never an incremental build) and diff the MOZ_APP_VENDOR, MOZ_APP_DISPLAYNAME and MOZ_BRANDING_DIRECTORY lines of objdir/config/autoconf.mk against the pre-switch build, requiring identical values. Keep scripts/check-patch-surface.sh green unchanged and require the apply-patches non-vacuity assertion to still fire on a silent no-op.</action>
  <verify>
    <automated>cd /home/chris/coding/Power-Browser && node scripts/generate.mjs --self-test && scripts/verify-platform.sh --only generate-self-test && bash scripts/check-patch-surface.sh && grep -c 'MOZ_APP_VENDOR", "DeBIOS"' patches/010-powerbrowser-identity.patch; test $? -eq 1</automated>
    <fails_when>non-zero exit from --self-test, the generate-self-test row, or the surface check; or the vendor hard-code grep finding the removed line (grep must exit 1)</fails_when>
    <automated>cd /home/chris/coding/Power-Browser && grep '^MOZ_APP_VENDOR\|^MOZ_APP_DISPLAYNAME\|^MOZ_BRANDING_DIRECTORY' objdir/config/autoconf.mk</automated>
    <fails_when>non-zero exit, or any of the three lines absent or carrying a pre-switch value</fails_when>
  </verify>
  <acceptance_criteria>Patch 010 carries no vendor or UA-name brand value; autoconf.mk values identical pre- and post-switch after a forced configure; surface check green; non-vacuity assertion proven still firing.</acceptance_criteria>
  <done>Identity values flow from generated output; patch de-configured without breaking the merge chain.</done>
</task>

<task type="auto">
  <name>Single-edit propagation proof across every surface</name>
  <files>configuration.toml (temporarily edited and restored in-task), generated/ Gecko surfaces (read-only assertions)</files>
  <read_first>scripts/generate.mjs (frozen TARGETS table, the full row list after plans 01-03), configuration.toml (identity.display_name line 38)</read_first>
  <reversibility rating="reversible">Temporary manifest edit restored in the same task; the tree ends byte-identical.</reversibility>
  <action>Set identity.display_name to Zebra Browser in configuration.toml and run node scripts/generate.mjs, then assert the string Zebra Browser appears in every emitted Gecko-side surface: generated .mozconfig, both branding configure.sh files, all four locale files, all eight installer fragments, and both desktop entries, while the strings Power Browser and Power Browser Dev appear in none of them. Assert node scripts/generate.mjs --check reports stale naming those files. Then restore display_name to Power Browser, regenerate, and require generate --check PASS plus verify-generated-identity.mjs PASS, proving the tree is back to byte-identical and the only files the edit ever touched were generated ones — git status must show no tracked modification from the excursion. Record the surface list and both outcomes in the commit message.</action>
  <verify>
    <automated>cd /home/chris/coding/Power-Browser && node scripts/generate.mjs --check && node scripts/verify-generated-identity.mjs && git status --porcelain -- . ':!.planning' | grep -v '^??' ; test $? -eq 1</automated>
    <fails_when>non-zero exit from --check or the identity gate, or the status grep finding any tracked modification outside .planning (grep must exit 1)</fails_when>
  </verify>
  <acceptance_criteria>Zebra excursion touched every listed surface and nothing else; restored tree is check-clean, identity-clean, and shows no tracked drift.</acceptance_criteria>
  <done>Success criterion 4 proven: one manifest edit moves every Gecko-side surface.</done>
</task>

<task type="auto">
  <name>Tier-3 Linux build plus built-artifact branding proof</name>
  <files>objdir/dist/bin/powerbrowser (built artifact, gitignored — read-only evidence, never committed)</files>
  <read_first>docs/BUILD.md (full-build invocation and timing notes), scripts/verify-branding-identity.mjs (the six identity surfaces plus the runtime-identity positive control)</read_first>
  <reversibility rating="reversible">A build consumes the tree without changing it; the proof is read-only evidence.</reversibility>
  <action>Run node scripts/generate.mjs fresh, then the full ./mach build from inside upstream/ under the firefox nix shell with MOZCONFIG pointing at the generated mozconfig (roughly 47 to 54 minutes on the reference host; schedule once, do not iterate here). Against the built artifact run scripts/verify-branding-identity.mjs including its runtime-identity positive control, requiring all six surfaces green, and confirm the desktop entry Icon line resolves to an installed icon rasterized in 03-02. This task carries the phase's human-check under end-of-phase mode: launch the built application and judge that the window shows the downstream icon and the display name with suffix, and that the launcher and desktop entry show the mark — record the judgments as human-check lines in the summary rather than as a mid-flight checkpoint.</action>
  <verify>
    <automated>cd /home/chris/coding/Power-Browser && scripts/verify-platform.sh --only branding-identity 2>/dev/null || node scripts/verify-branding-identity.mjs --help 2>/dev/null; node scripts/verify-branding-identity.mjs</automated>
    <fails_when>non-zero exit from the identity verifier, any of the six surfaces red, or the positive control failing to discriminate</fails_when>
    <human-check>Launch objdir/dist/bin/powerbrowser: the window, launcher, and desktop entry show the downstream mark at correct density, and the title carries Power Browser Dev. Type approved or describe the visual defect.</human-check>
  </verify>
  <acceptance_criteria>Full build exits 0; all six identity surfaces green on the built artifact with the positive control green; human visual judgments recorded.</acceptance_criteria>
  <done>GEN-01, GEN-02 and GEN-03 Linux outputs build-verified on the real application.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| patch stack → upstream tree | Regenerated patch content crosses into the Gecko configure path |
| generated/ → compiler and packager | Every emitted surface crosses into the tier-3 build |
| scratch manifest edit → real tree | The Zebra excursion crosses the same writer as production runs |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-11 | Tampering | Wrong identity carrier (exports versus include semantics, config.status caching) | high | mitigate | Spike-proven carrier only; forced reconfigure never incremental; autoconf.mk three-line diff required identical |
| T-03-12 | Tampering | Patch regen degrading into a silent no-op | high | mitigate | Regenerate-from-tree flow only, never hand-edit; check-patch-surface green; non-vacuity assertion proven firing |
| T-03-13 | Spoofing | Downstream shipping under Power Browser identity by omission | high | mitigate | CFG-02 hard-fail unchanged; propagation proof shows the excursion reaches every surface so no surface can silently keep the old mark |
| T-03-SC | Tampering | npm/pip/cargo installs | high | mitigate | Zero new packages across the whole plan |
</threat_model>

<verification>
node scripts/generate.mjs --self-test plus scripts/verify-platform.sh --quick after the task 1 and task 2 commits; full scripts/verify-platform.sh plus the tier-3 build and built-artifact verifier for task 3. No new registry rows in this plan.
</verification>

<success_criteria>
Patch 010 de-configured with merge chain intact; single-edit propagation proven with a clean restore; Linux build green with all six identity surfaces passing on the built artifact; human visual judgments recorded.
</success_criteria>

<output>
Create `.planning/phases/03-firefox-branding-emitter-and-icon-pipeline/03-04-SUMMARY.md` when done
</output>

## Artifacts this phase produces

New symbols this plan creates (the plan-review-convergence source-grounding pass must exclude these from drift verification):

- Identity carrier: either export MOZ_APP_VENDOR plus export MOZ_APP_UA_NAME lines in emitMozconfig and the tracked .mozconfig, or emitIdentityConfigure plus the generated/identity.configure TARGETS row (spike decides; exactly one lands)
- Regenerated patches/010-powerbrowser-identity.patch minus the two vendor hard-codes (plus the include hook line only on the include route)
- Propagation proof procedure (Zebra excursion) recorded in the 03-04 summary for reuse by Phase 6 verification
- No new registry labels in this plan

## Flagged assumptions (no silent drops)

1. Tier-3 cost: the full build runs once at roughly 47 to 54 minutes on the reference host inside nix develop .#firefox; iteration before it uses ./mach configure plus autoconf.mk diffs only. If the reference host is unavailable, the build moves to whatever host BUILD.md names with its timing recorded in the summary.
2. MOZ_APP_ID stays patch-carried: the Firefox application ID brace string is a fixed platform constant shared with upstream, not a downstream brand value, so its imply_option line remains in patch 010 untouched by the de-configuration.
