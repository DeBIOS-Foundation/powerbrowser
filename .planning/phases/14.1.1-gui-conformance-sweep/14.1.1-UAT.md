---
status: diagnosed
phase: 14.1.1-gui-conformance-sweep
source: [.planning/GUI-DEFECTS.md, 14.1-UAT.md, 14.1-VERIFICATION.md, 14-UAT.md, 13-UI-SPEC.md, 14-UI-SPEC.md, 14.1-UI-SPEC.md, REQUIREMENTS.md, ROADMAP.md, docs/URI-SCHEMES.md, commit 2857353]
started: 2026-09-07T18:30:00Z
updated: 2026-09-07T18:30:00Z
---

## Current Test

number: 1
name: Content tabs survive every mode switch, including a mode's second visit
expected: |
  Switching Coding / Browsing / Organising never unmounts, closes, moves windows, or detaches a main-area tab — on a mode's first visit and on every visit after it. The status-bar chip holds the same count across every switch.
awaiting: live confirmation in Chris's window

## Tests

This is the living GUI defect list for the conformance sweep. It was built read-only on
2026-09-07 by checking every contract line in 13-UI-SPEC, 14-UI-SPEC and 14.1-UI-SPEC, every
GUI requirement line, and the Phase 13/14/14.1/15 success criteria against the code that
claims to satisfy them, then folding in every `.planning/GUI-DEFECTS.md` item not yet
CONFIRMED, every Phase 14 UAT gap still `in_tree`/`open`, the ten pending human tests from
14.1-UAT.md (verbatim, tests 33–42), and the six live-GUI fixes of commit `2857353` that no
one has confirmed on Chris's screen.

Every test carries the same lines: `contract` (the line it is measured against), `evidence`
(code path, or "unverified live"), `severity`, `fixable` (`yes` = a live check or static
gate can prove the fix; `needs-chris` = only his eyes can; `decision` = the docs and his
stated intent disagree, so someone has to choose), `result`, `gap`, `reason`. Tests with a
known defect have a gap of the same number (`G-14.1.1-N`); pending tests get a gap only
when Chris fails them. Order: critical first, then major, then minor; within a band,
`fixable: yes` → `decision` → `needs-chris`; the ten verbatim 14.1 tests close the list.

Owner statements locked on 2026-09-06/07 (the intent every entry below is measured
against): "+" opens a web tab inside the Theia shell's tab strip (delivered, GUI-DEFECTS
item 1 CONFIRMED); that tab moves between modes (test 11 — a decision, see below); the URL
must work and every tab has a URL — web = page URL, editor = file path, views = `view:`,
terminal = `terminal:` (tests 28, 29); Organising / Panorama shows his real tabs (test 14).

### 1. Content tabs survive every mode switch, including a mode's second visit
contract: 14-UI-SPEC.md:232-233 (never unmount, close, move windows, or detach), :223 (only side panels and the main-area view change); ROADMAP.md:112 (14.1 SC2)
expected: No main-area tab (web tab, editor, view) disappears on any switch, first visit or second; the chip count is unchanged after every hop.
evidence: Fix in tree — `theia/extensions/modes/src/browser/main-area-exemption.ts` strips the main area from the per-mode snapshot at the descriptor deactivate seam (GUI-DEFECTS.md:29, 14-UAT.md:112-143 G-14-1). `gui07-mode-switch-tabs-live` and `gui02-web-tab-live` (five-hop walk) pass on the working tree. Unconfirmed in Chris's window; the exemption file is uncommitted (other session).
severity: critical
fixable: needs-chris
result: issue
gap: G-14.1.1-1
reason: "Code landed and the live checks pass, but nothing on the defect list counts as done until seen in the real window (GUI-DEFECTS.md:10-12). This is the invariant every other GUI test depends on, so it is confirmed first."

### 2. Mode toggle applies the contracted per-mode shell map
contract: 14-UI-SPEC.md:225-229 (per-mode shell map); ROADMAP.md:87 (Phase 14 SC1)
expected: Coding shows the Explorer in a visible left panel and a visible bottom panel; Browsing and Organising hide all three side panels; Organising shows the Panorama surface in the main area.
evidence: Fix in tree — `chrome-bar-widget.tsx:530-549` routes the three shipped segments through `MODES_ACTIVATE_COMMAND_ID`; `mode-service.ts:240-277` applies `visibilityFor()` (:365-373) after the perspective switch (GUI-DEFECTS.md:30, 14-UAT.md:145-162 G-14-2). Commit `2857353` reports all three modes reshaping per the map in the running sidecar. Unconfirmed in Chris's window.
severity: critical
fixable: needs-chris
result: issue
gap: G-14.1.1-2
reason: "The whole contracted panel map was dead code for the three shipped modes until the reroute; measured live by the sidecar loop, never by Chris."

### 3. Chrome-to-content replies and state pushes reach the frontend
contract: 14.1-UI-SPEC.md:296-300 (pill follows every state push), :322-325 (Back/Forward enablement from pushes); REQUIREMENTS.md:25-26 (chrome sends URL, title, loading, back/forward)
expected: Every request from the Theia frame is acknowledged, every navigation pushes URL/title/loading/back-forward to the pill and nav buttons, and no group write waits out its 5 s timeout.
evidence: Regression of GUI-DEFECTS item 10 defect 5, fixed in `powerbrowser/shell/GroupActorChild.sys.mjs` (content window's own `JSON.parse` via `wrappedJSObject`) — GUI-DEFECTS.md:35 item 17, status IN TREE. Item 1's confirmation on 2026-09-07 (pill followed link clicks in Chris's window) implies pushes are delivered there, but item 17 has never been flipped and the note "needs the startup-cache purge in Chris's window" still stands.
severity: critical
fixable: needs-chris
result: issue
gap: G-14.1.1-3
reason: "The channel had never worked once before 2026-09-06 and regressed once after being confirmed; a chrome-side change is only proven after a cache-purged launch on Chris's machine."

### 4. A lost overlay renders the lost-view line without a Reload click
contract: 14.1-UI-SPEC.md:338 (overlay lost — chrome-side restart, unknown tab reported to a geometry/reload/back request — renders the lost-view state), :285-287 (Reload re-issues the open and the page returns), :242 (copy)
expected: When chrome no longer holds the tab's overlay, the tab body shows "Power Browser can't show this page right now. Choose Reload to try again, or close this tab." immediately, and one Reload click brings the page back.
evidence: `web-tab.ts:395-418` publishes geometry with `channel.send` (fire-and-forget), so chrome's `unknown-tab` outcome for a geometry message (`PowerBrowserAPI.sys.mjs:2306-2309`) never reaches `settle()` (:455-458). Only a navigate/back/forward/reload reply folds into `lostView` (:324-326). Consequence: after the overlay is dropped the body stays blank (`hasPage` true → empty text, :309-311); the first Reload click sends `webTabReload`, gets `unknown-tab`, and only then shows the line; a second Reload click re-opens (:443-450). Two clicks where the contract says one.
severity: major
fixable: yes
result: issue
gap: G-14.1.1-4
reason: "The geometry request is the one message chrome answers on every layout change and the only one sent while the user is idle; it is also the one whose reply the widget ignores."

### 5. A fresh frontend starts with no web tabs, and "+" never collides with a restored tab id
contract: 14.1-UI-SPEC.md:258-261 (each "+" adds one tab); docs/URI-SCHEMES.md:37-40 (non-user-chosen discriminators stay in construction options); 14.1-VERIFICATION.md:61-64 (no web tabs at startup before any click)
expected: A launch shows no web tabs unless a setup restores them (GUI-DEFECTS item 24); two "+" clicks give two distinct tabs each bound to its own overlay.
evidence: `theia/node_modules/@theia/core/lib/browser/shell/shell-layout-restorer.js:244-257` describes EVERY widget that has a `WidgetManager` description — `StatefulWidget` or not — and :351 recreates each with `getOrCreateWidget(factoryId, options)`. A web tab's options are `{ id: 'wt1', url }` (`web-tab.ts:513-515`), so a persisted layout re-creates last session's web tabs as `wt1`, `wt2` whenever the frontend origin repeats: an in-session reload after a backend respawn keeps the pinned port (`TheiaService.sys.mjs:595, :602`), and an ephemeral-port repeat across launches does the same. `web-tab.ts:22-23` assumes the origin always changes. `nextTabSeq` restarts at 0 (:483), so the next "+" mints `wt1` again: the channel registers two widgets under one tabId and chrome answers `already-open` (`PowerBrowserAPI.sys.mjs:2137-2138`) — two tabs driving one overlay. Hypothesis; item 24 was seen once and not reproduced.
severity: major
fixable: yes
result: issue
gap: G-14.1.1-5
reason: "Item 24 (`wt1`, `wt2` at startup after the automated runs) matches exactly what the stock layout restorer produces from a persisted layout; the id collision is the latent half nobody has hit yet."

### 6. In-shell web tabs get a Panorama thumbnail
contract: ROADMAP.md:155 (Phase 15 SC4: tab thumbnails render as PNG last-view snapshots); REQUIREMENTS.md:54 (GUI-08 thumbnails are PNG last-view snapshots); ROADMAP.md:114 (14.1 SC4: Organising sees real tabs)
expected: A web tab's card on the Organising canvas shows a snapshot of the page after it settles.
evidence: The only capture path, `PowerBrowserAPI.sys.mjs:1251 scheduleSettleCapture` → `:1285 captureTabThumbnail`, locates its target with `findStockTabBrowser(uri)` and is scheduled only from the stock-window tab listeners (:2017, :2029). The web-tab overlay's progress listener (:2205-2220) writes the row and never schedules a capture (zero `captureTabThumbnail` calls in :2120-2420). `organising-widget.ts:414-420` renders `tab.thumbnail` when present, so every in-shell web tab card falls to the no-thumbnail state. Since 14.1 replaced the stock-window escape, every tab Chris opens is an in-shell web tab.
severity: major
fixable: yes
result: issue
gap: G-14.1.1-6
reason: "Phase 15 shipped thumbnails for stock-window tabs; Phase 14.1 moved every tab out of stock windows and did not carry the capture across."

### 7. Suggestion dropdown does not re-open over the page after Enter
contract: 13-UI-SPEC.md:195-197 (Enter commits; Esc dismisses); 14.1-UI-SPEC.md:350-354 (occlusion restores the page within one frame of the layer detaching)
expected: Typing quickly and pressing Enter commits without the dropdown re-appearing (and hiding the page) after the in-flight query completes.
evidence: Fix in tree — `chrome-bar-widget.tsx:204-209` (`closeDropdown` bumps `querySeq` and disarms), :218 (a query may only open the dropdown while `dropdownArmed`), :256-262 (typing re-arms). GUI-DEFECTS.md:38 item 20, IN TREE. Not exercised by `gui02-web-tab-live`; unconfirmed in Chris's window.
severity: major
fixable: yes
result: issue
gap: G-14.1.1-7
reason: "A debounced query outstanding at Enter completed after the commit and re-opened the dropdown; the fix is present, and a live check (type, Enter within 150 ms, wait, assert no dropdown and overlay visible) can prove it."

### 8. The backend tab-row reader serves real rows while the browser runs
contract: ROADMAP.md:114 (14.1 SC4 — Organising and setups see real tabs); 13-UI-SPEC.md:192 (typing filters history suggestions)
expected: Suggestions and the Organising surface read the rows the chrome side writes, with the browser open.
evidence: Three stacked in-tree fixes — GUI-DEFECTS.md:33 item 9 (`TabQueryService` reads `POWERBROWSER_ENV`, not the scrubbed `process.env`; `tab-query-service.ts:26`), :36 item 18 (`openNotExclusive: true` so the readonly handle no longer gets `SQLITE_BUSY`), :37 item 19 (`better-sqlite3` marked external in `theia/applications/browser/esbuild.mjs`). `gui02-web-tab-live` reads the row back through the backend reader after navigation and sees `[]` after close (14.1-VERIFICATION.md:145). `tab-query-service.ts` is modified and uncommitted (other session).
severity: major
fixable: yes
result: issue
gap: G-14.1.1-8
reason: "Fixed and proven by the live check; listed because the three GUI-DEFECTS rows are still IN TREE and close together with test 14's confirmation."

### 9. A clean build succeeds from an empty `theia/**/lib`
contract: CLAUDE.md `## Environment` (builds run inside the Nix dev shells); docs/BUILD.md
expected: `yarn build` in `theia/` succeeds on a fresh clone with no `lib/` directories present.
evidence: `theia/package.json:69` orders `build:extensions` as branding → tab-uris → customize → telemetry → token-gate → …, but `tab-uris/src/node/tab-query-service.ts:26` imports `@powerbrowser/token-gate/lib/node/powerbrowser-env`, and `.gitignore:13` ignores `theia/**/lib/`. Works locally only because `token-gate/lib` already exists. GUI-DEFECTS.md:46 item 11. Sibling class: commit `2857353` #5 (`./modes.css` import never emitted into lib).
severity: major
fixable: yes
result: issue
gap: G-14.1.1-9
reason: "Hand-ordered `&&` chain with a dependency that points backwards; a clean-checkout build in CI would fail on the first run."

### 10. A gate covers the group write channel
contract: CLAUDE.md `## Verification` rule 2 (derive from the tree and compare; give a new check a `--self-test` that plants faults); GUI-DEFECTS.md:58-92 (the five channel defects)
expected: A `--quick` row goes red if `wantUntrusted: true`, the `resource://` child module path, the `document`-dispatch + `bubbles: true` pair, or the reply cloning regresses.
evidence: No registry row in `scripts/verify-platform.sh` names the channel; GUI-DEFECTS.md:48 item 13 records that `wantUntrusted` can silently regress with every other assertion green. `gui02-web-tab-live` exercises the channel end to end at full-check cost, not at commit-gate cost.
severity: major
fixable: yes
result: issue
gap: G-14.1.1-10
reason: "Five independent faults masked each other for a whole phase; the class needs a cheap static row with planted faults, not another end-to-end check."

### 11. Tabs move between modes (Variant B) — or the strip stays top (Variant-A fallback)
contract: REQUIREMENTS.md:38-45 (GUI-07: the tab strip relocates per mode, Variant B — top in browsing, IDE-docked in coding; if the spike is red the strip stays top); ROADMAP.md:88 (Phase 14 SC2); 14-UI-SPEC.md:22-28 (binding RED verdict — strip stays top in all modes); 13-UI-SPEC.md:219-225 (spike verdict rules); owner statement 2026-09-06/07: "that tab moves between modes"
expected: Decision required. Chris's words are that the web tab moves between modes. The 14-UI-SPEC contract, ratified on the 13-01 RED verdict, says the strip never moves and only side panels change. The RED verdict was ruled only because `diff-theia-core.sh` was red on pre-existing install drift; that instrument now passes, and the 13-01 spike proved the mechanics live (6/6 moves, identity preserved, reversible).
evidence: GUI-DEFECTS.md:21 item 2, OPEN. 14-UAT.md:38-41 test 3 skipped on the fallback. Today the strip stays top in every mode (`mode-service.ts:240-277` changes panel visibility only) and the web tab follows via the main-area exemption (test 1). No code relocates the strip.
severity: major
fixable: decision
result: issue
gap: G-14.1.1-11
reason: "The docs say strip-stays-top; the owner says the tab moves between modes. Either reading is consistent with what ships today (the tab persists and is visible in every mode), so this must be decided, not silently picked: re-probe Variant B (re-open GUI-07 relocation work) or ratify that 'moves between modes' means 'persists and follows the layout' and close item 2."

### 12. The fixes on this list are committed
contract: CLAUDE.md `## Verification` (`--quick` is the commit gate); memory `concurrent-sessions-on-main` (explicit-path commits; leave the other session's files alone)
expected: Every fix the list depends on is in a commit; nothing on this list rides only in the working tree.
evidence: GUI-DEFECTS.md:51 item 16. `git status` on 2026-09-07: modified and uncommitted `theia/extensions/modes/src/browser/main-area-exemption.ts` (test 1's fix), `mode-service.ts`, `modes-frontend-module.ts`, `group-actor-client.ts`, `theia/extensions/tab-uris/src/browser/browser-window-command.ts`, `theia/extensions/tab-uris/src/node/tab-query-service.ts` (test 8's fix), `powerbrowser/shell/jar.mn`, `scripts/verify-mode-switch-tabs-live.mjs` (staged), `scripts/verify-mode-switch-tabs-invariant.mjs`, `scripts/verify-mode-toggle-commands.mjs`, plus `.planning/GUI-DEFECTS.md`, 14-04/14-05 plans and 14-UAT.md. 14.1-VERIFICATION.md:300-301 records the same exposure.
severity: major
fixable: decision
result: issue
gap: G-14.1.1-12
reason: "These are another session's in-flight files; this sweep must not commit them. Chris decides when that session lands them. Until then a `git checkout` or a discarded worktree removes test 1's and test 8's fixes."

### 13. Coding docks the Explorer in a visible left panel on every activation
contract: 14-UI-SPEC.md:227 (Coding: left panel visible, Explorer)
expected: Every switch into Coding — first, second, after Organising — shows the Explorer docked and expanded on the left.
evidence: Fix in tree — `mode-service.ts:257-258` calls `ensureInArea('explorer-view-container', 'left')` (:297-) before `leftPanelHandler.expand` (GUI-DEFECTS.md:31 item 7; 14-UAT.md:164-180 G-14-3). No check asserts the Explorer's area after a second Coding visit; `verify-mode-switch-tabs-live.mjs` asserts main/bottom survival only.
severity: major
fixable: needs-chris
result: issue
gap: G-14.1.1-13
reason: "`leftPanelHandler.expand` is a lookup that no-ops on a miss; the dock call was added and measured once by the sidecar loop, never in Chris's window."

### 14. Organising / Panorama shows Chris's real tabs
contract: REQUIREMENTS.md:49-57 (GUI-08); ROADMAP.md:114 (14.1 SC4: Organising and named setups see real tabs), :152-154 (Phase 15 SC1-3); owner statement: Organising/Panorama should show his real tabs
expected: Opening Organising renders every open web tab as a card (ungrouped tray or its group), with drag, corner resize, auto-box on drop, zoom, and the tree flip over the same data — or the contracted empty state when there are none.
evidence: GUI-DEFECTS.md:23 item 4, OPEN. Writer and reader both fixed (tests 3, 8); rows flow in `gui02-web-tab-live`; at the 14.1-03 checkpoint Organising showed the open web tabs and Chris did not report otherwise. Commit `2857353` #4 made the surface reachable at all (three silent causes). Never confirmed complete on Chris's screen; 14-UAT.md:199-222 G-14-5 still `in_tree`.
severity: major
fixable: needs-chris
result: issue
gap: G-14.1.1-14
reason: "Two blind halves and an unreachable surface were fixed in three separate sessions; nobody has yet looked at the canvas with real rows and said it is complete."

### 15. Commit 2857353's six live-GUI fixes hold in Chris's window
contract: 13-UI-SPEC.md:174-180 (bar full-bleed, 40 px, order menubar → chrome bar → tab strip), :195 (Ctrl+L and Cmd+L); 14-UI-SPEC.md:226 (Browsing is the launch default), :229 (Organising main area); 14.1-UI-SPEC.md:316-318 (Ctrl+L / Cmd+L)
expected: The chrome bar spans the full window width at 40 px on its own row under the menubar and above the strip, does not drift; a cold start opens in Browsing with the toggle showing Browsing; clicking Coding from launch enters Coding; Organising renders the Panorama surface; Ctrl+L focuses the pill.
evidence: Commit `2857353` (2026-09-07) fixed: (1) bar beside the menubar at 712/1920 px, clipped; (2) perfect-scrollbar drift to y=-4; (3) no mode activated on launch and Coding unreachable from the false initial label; (4) Organising rendered nothing — no contribution `onStart`, no `WidgetFactory`, constructor read injected fields; (5) `./modes.css` import never emitted, so nothing Phase 15 shipped after `3d6c1a6` was in the bundle; (6) `cmd+l` rejected at startup, replaced by one `ctrlcmd+l` binding (`chrome-bar-keybindings.ts:24`). Measured in the standalone sidecar, not in the built browser or Chris's window.
severity: major
fixable: needs-chris
result: issue
gap: G-14.1.1-15
reason: "Six fixes landed in one commit; per the one-fix-at-a-time rule each needs a look in his window, and (3)/(4) overlap tests 2 and 14."

### 16. Custom mode saves and appears beside the shipped defaults
contract: 14-UI-SPEC.md:194 ("Save as Mode"), :205 (Mode "{name}" saved.), :204 (corrupt-mode fallback notice), :235-237 (custom modes are data; a custom never alters a shipped default), :308-310 (custom rows append, ellipsis at 96 px, menu scrolls); ROADMAP.md:87
expected: Saving the current panel layout as a named custom mode adds a toggle row beside Coding / Browsing / Organising with the active one in accent ink + 600; unreadable custom data falls back to Browsing with the contracted notice, never a blank shell.
evidence: `mode-service.ts:318-353` (save), :461-494 (corrupt store fallback + notice), `chrome-bar-widget.tsx:731-746` (custom rows), `chrome-bar.css:201-231`. 14-UAT.md:49-52 test 5 pending: never exercised live.
severity: major
fixable: needs-chris
result: pending
reason: "Never driven; the one Phase 14 path that already routed through the mode command, so least likely broken — which is not an observation."

### 17. Dependent window hosts one tab with no second IDE frame
contract: 14-UI-SPEC.md:249-265 (one tab's content, no chrome bar/toggle/panels/status bar/strip; closed-elsewhere state "This tab is closed" + "Close Window"; core close ends the session with no confirmation; next launch restores); REQUIREMENTS.md:61-65 (GUI-09); 14.1-UI-SPEC.md:383-392 (a web tab is not offered for the move)
expected: A dependent window shows exactly one tab view; the tab context menu offers no move for a web tab; a dependent whose tab closed elsewhere shows the contracted closed state and its close closes only that window; closing the core window closes dependents; the next launch restores the setup.
evidence: `dependent-windows.ts:44-46` (only the chrome bar pinned non-extractable there), `tab-uris-frontend-module.ts:126-136` (web tabs non-extractable), `dependent-windows.ts:148-181` (closed state); `gui09-dependent-window-content` green. 14-UAT.md:59-62 test 7 pending: the live half never ran.
severity: major
fixable: needs-chris
result: pending
reason: "GUI-09 is marked complete in REQUIREMENTS.md:137 on the strength of a static gate; no one has opened a dependent window in the built browser."

### 18. Named setups save and restore geometry, tab placement, and mode
contract: 14-UI-SPEC.md:267-291 (save/list/restore/delete contract), :193-214 (setup copy table); REQUIREMENTS.md:61-65; 14.1-UI-SPEC.md:396-399 (setups restore saved web tabs as in-shell tabs)
expected: A setup saved under a typed name restores windows, tab placement (web tabs come back as in-shell tabs), and mode; a restore with missing tabs completes with the "some tabs no longer exist" explanation; delete is the only destructive action and carries its confirmation.
evidence: `setups-service.ts:342-374` (save), :384-430 (restore), :433-468 (delete); `gui09-setup-roundtrip` green on the schema roundtrip only. 14-UAT.md:64-67 test 8 pending.
severity: major
fixable: needs-chris
result: pending
reason: "A schema roundtrip is not a restored window; never exercised live."

### 19. Overlay creation logs no `getTabForBrowser` TypeError
contract: CLAUDE.md `## Verification` rule 1 (a log line must be attributable to the code under test); 14.1-VERIFICATION.md:297 (warning: masks real errors in the same log)
expected: Opening a web tab produces no `TypeError: this.documentGlobal.gBrowser.getTabForBrowser is not a function` from browser-custom-element.mjs / LinkHandlerParent / ContextMenuParent.
evidence: `powerbrowser/shell/powerbrowser.js:64` — `window.gBrowser = { tabs: [...] }` has no `getTabForBrowser`. GUI-DEFECTS.md:52 item 23. One-line fix: `getTabForBrowser() { return null; }` on the stand-in. Provable by a launch-log grep after a cache-purged launch.
severity: minor
fixable: yes
result: issue
gap: G-14.1.1-19
reason: "Harmless to the page, noisy on every overlay, and it hides the next real chrome-side error."

### 20. The live check no longer dispatches a synthetic focus event on the placeholder
contract: CLAUDE.md `## Verification` rule 2 (a check must go red on the drift it exists to catch); 14.1-VERIFICATION.md:74-76 (coincidental reliance: fixture-only)
expected: `scripts/verify-web-tab-live.mjs` asserts the pill follows the strip's selected tab without helping a focus-keyed binding look correct.
evidence: `scripts/verify-web-tab-live.mjs:420` still dispatches `new FocusEvent('focus')` on the placeholder; this is what masked GUI-DEFECTS item 21. GUI-DEFECTS.md:54 item 25, OPEN.
severity: minor
fixable: yes
result: issue
gap: G-14.1.1-20
reason: "With the strip-selection binding (`chrome-bar-widget.tsx:816-817`) the accommodation is unnecessary, and while it stays a regression to focus-keyed binding cannot go red."

### 21. A failed setup save or delete shows copy for a save or delete, not for a restore
contract: 14-UI-SPEC.md:202 (restore-failure error is the restore surface); CLAUDE.md `## User-facing copy` (states the problem in plain language, ends with a real next step)
expected: If writing the setups store fails during Save Setup or Delete Setup, the flash says the setup could not be saved (or deleted) and that nothing changed.
evidence: `setups-service.ts:368` (save write failure) and :459 (delete write failure) both flash `SETUP_RESTORE_FAILURE` ("Power Browser couldn't restore this setup. Your current windows and tabs are unchanged — try again, or delete the setup and save a new one.") — wrong problem, and the delete case tells the user to delete. Modes has its own correct string at `mode-service.ts:342`.
severity: minor
fixable: yes
result: issue
gap: G-14.1.1-21
reason: "Copy reuse across three different failures; a copy gate over `setups-service.ts` can pin the two missing strings."

### 22. The Delete Setup confirm button is in destructive ink
contract: 14-UI-SPEC.md:136 (destructive colour: Delete Setup confirmation only — confirm button + dialog warning marker), :213 (Delete in destructive ink)
expected: The "Delete" button of the Delete Setup dialog renders in `--color-danger`; nothing else in the phase does.
evidence: `setups-service.ts:442-447` opens a stock `ConfirmDialog` with no class added; the only danger rule is `modes.css:586-588`, scoped to `.pb-org-close-confirm` (Panorama's close confirm). The Delete button is stock accent.
severity: minor
fixable: yes
result: issue
gap: G-14.1.1-22
reason: "Phase 15 added the scoped danger rule for its own confirm and Phase 14's dialog never received one."

### 23. Setup and mode names are cut at 60 characters at the input, before commit
contract: 14-UI-SPEC.md:269 (name field cap 60), :313 (pasted overflow is cut at the cap before commit)
expected: The name input refuses or trims past 60 characters while typing/pasting; the user never sees a longer name accepted and silently shortened.
evidence: `setups-service.ts:347` and `mode-service.ts:323` apply `.slice(0, MAX)` after the quick-input resolves; the input itself has no cap or `validateInput`.
severity: minor
fixable: yes
result: issue
gap: G-14.1.1-23
reason: "The cap exists but at the wrong end of the gesture; a `validateInput` on the quick input (or a truncation message) closes it."

### 24. A gate covers backend reads of `process.env.POWERBROWSER_*`
contract: CLAUDE.md `## Verification` rule 2 (derive from the tree and compare as set equality)
expected: A `--quick` row derives the set of backend files that read `process.env.POWERBROWSER_*` and requires it to equal exactly `{ powerbrowser-env.ts }`.
evidence: No such row in `scripts/verify-platform.sh`; GUI-DEFECTS.md:47 item 12, OPEN. `tab-query-service.ts:87-94` documents the trap in a comment only.
severity: minor
fixable: yes
result: issue
gap: G-14.1.1-24
reason: "The scrub runs 31 modules before tab-uris loads; the next backend module to read the env directly will read '' and fail silently, exactly as item 9 did."

### 25. `powerbrowser/INTERNAL-APIS.md` line references are current
contract: CLAUDE.md hard rule 2 (every touchpoint catalogued; `check-internals-boundary.sh --catalogue` enforces it); 14-UAT.md:261-270 G-14-8
expected: Every catalogue row points at the line it describes and the gate is green.
evidence: GUI-DEFECTS.md:49 item 14 records `PowerBrowserAPI.sys.mjs:817` → actually `:1643`. On 2026-09-07 `scripts/check-internals-boundary.sh --catalogue` returns PASS and neither `:817` nor `:1643` appears in the file, so the row has since been corrected. Item 14 and G-14-8 are closable after one confirming `--only internals-catalogue` run.
severity: minor
fixable: yes
result: issue
gap: G-14.1.1-25
reason: "Record drift, not a code defect: two documents still say the gate is red."

### 26. 15-RESEARCH and 15-REVIEW no longer assume the group write channel worked
contract: GUI-DEFECTS.md:58-95 (the channel never worked before 2026-09-06); 14-UAT.md:222
expected: Both documents carry a correction note dated 2026-09-06.
evidence: GUI-DEFECTS.md:50 item 15, OPEN; no correction present.
severity: minor
fixable: yes
result: issue
gap: G-14.1.1-26
reason: "A future reader of 15-REVIEW would trust a channel that had never delivered an event."

### 27. Phase 14 records match the tree
contract: 14-UAT.md:182-197 (G-14-4 `in_tree`), :224-247 (G-14-6 `in_tree`), :249-259 (G-14-7 `deferred`), :261-270 (G-14-8 `open`); 14-UI-SPEC.md:207-209 and :240-247 (Organising placeholder copy and slot)
expected: G-14-4 reads confirmed (GUI-DEFECTS item 8 CONFIRMED); G-14-6 reads closed by Phase 14.1 (items 1 and 3 CONFIRMED); G-14-7 reads superseded (GUI-02 delivered in 14.1); G-14-8 reads closed (catalogue PASS); the 14-UI-SPEC placeholder rows are marked superseded by the Phase 15 canvas (14-UAT.md:54-57 test 6 already says so).
evidence: All four gap statuses and the placeholder rows are stale against GUI-DEFECTS.md:20, :22, :32 and the catalogue run in test 25. "Organising arrives next" / "Back to Browsing" appear in no source file; the real surface is `organising-widget.ts`.
severity: minor
fixable: yes
result: issue
gap: G-14.1.1-27
reason: "Three sessions closed these without updating the record that a later planner will read first."

### 28. The Organising tab has an address
contract: docs/URI-SCHEMES.md:76-77 ("no tab without a URI"), :37-40; owner statement: every tab has a URL (views = `view:`); 14.1-UI-SPEC.md:296-302 and A6 (:468) accept an empty pill when a tab has no registry URI
expected: Decision required. With Organising current, the pill shows an address for the Organising tab (for example `view:organising` following the `view:` table, or a scheme of Chris's choosing) — or Chris confirms the empty pill A6 allows.
evidence: `chrome-bar-widget.tsx:412-425` `addressOf` → `tab-uri-registry.ts:124-184` has no row for factory `powerbrowser.modes.organising` (`organising-widget.ts:62`; not in `view-factory-table.ts:33-55`), and the widget is not Navigatable, so the pill is empty in Organising. Every other tab kind checked resolves: web (page URL), editors (`NavigatableWidget.getUri`, item 22), `settings:`, `terminal:`, `output:`, `webview:`, `vscode-extension:`, plugin view containers, and the 21 `view:` rows including `view:welcome`.
severity: minor
fixable: decision
result: issue
gap: G-14.1.1-28
reason: "Two documents say every tab has an address and one spec assumption allows the exception; the Organising slot is the one main-area tab kind with none."

### 29. The address placeholder promises what the pill does
contract: 13-UI-SPEC.md:159 (placeholder "Search or enter address"), :198-199 (a non-URL string is treated as search-or-address per the tab-URI rules); 14.1-UI-SPEC.md:315 (non-address text keeps the in-bar failure row; no search engine is minted); owner statement: the URL must work
expected: Decision required. Either the placeholder stops promising search (for example "Enter address") or a search route is contracted. Today typing `power browser` and pressing Enter shows "Power Browser couldn't open that address. Press Enter to try again." — a retry that can never succeed.
evidence: `chrome-bar-widget.tsx:48-56` `typedAddressTargetOf` returns undefined for text with a space or no dot; :352-354 shows the commit-failure row; :701 renders the placeholder. The failure copy's next step (Enter re-commits the same text) is not a real affordance for this input.
severity: minor
fixable: decision
result: issue
gap: G-14.1.1-29
reason: "13-UI-SPEC and 14.1-UI-SPEC disagree on what a non-address commit does, and the copy on screen sides with the one that is not implemented."

### 30. The setups list renders the contracted row
contract: 14-UI-SPEC.md:273-278 (name at 14 px over the meta line at 12 px muted; current setup carries the accent marker + 600 name; zero setups render the empty copy; the host may be any widget but the row contract binds it), :199-200 (empty setups copy)
expected: Decision required. Either the quick-pick host is ratified with its rendering (inline `$(check)` marker, meta as inline description, empty state delivered as a status-bar flash) or a list surface that can render the contracted row replaces it.
evidence: `setups-service.ts:709` flashes `${SETUPS_EMPTY_HEADING} — ${SETUPS_EMPTY_BODY}` in the status bar instead of rendering an empty list; :712-720 uses `showQuickPick` with `label: $(check) name` and `description: meta` — a quick pick cannot render 600 on a row name, accent ink on the marker, or name-over-meta. `setupRowMeta` (:260-264) matches the singular/plural contract.
severity: minor
fixable: decision
result: issue
gap: G-14.1.1-30
reason: "The planner chose a host that cannot meet the row contract it was told binds any host."

### 31. Every custom-mode and setup string is in the copy table
contract: 14-UI-SPEC.md:191-214 (copy table); CLAUDE.md `## User-facing copy`
expected: Decision required. The strings below are either added to the 14-UI-SPEC copy table or changed to contracted ones: `mode-service.ts:319` placeholder "Mode name"; :325 "Give the mode a name — type a name and choose Save as Mode."; :329 "A mode with this name already exists. Choose a different name."; :342 "Power Browser could not save this mode. Your panels are unchanged — try again."; `setups-service.ts:385` pick title "Restore Setup"; :120 "Power Browser restored this setup, but some tabs no longer exist. Geometry and mode are applied." (the contract at :279-283 names the wording only as a variant of the restore-failure error).
evidence: None of these appears in 14-UI-SPEC.md; all pass the no-internals rule (`shell-error-copy-no-internals` is scoped to the shell, not to `@powerbrowser/modes`).
severity: minor
fixable: decision
result: issue
gap: G-14.1.1-31
reason: "Uncontracted copy is not wrong copy, but the table is the only place the wording is reviewed."

### 32. Side panels slide on a mode switch
contract: 14-UI-SPEC.md:172-174 (panels slide per mode switch, ≤200 ms, same easing; `prefers-reduced-motion` renders the switch instantly), :231-232, :320
expected: Switching modes animates the side panels open/closed within 200 ms; with reduced motion the change is instant; content tabs show no loading state.
evidence: `modes.css` authors transitions only on custom rows and the dependent-closed button (:53, :107, :152) and its reduced-motion block (:116-121) covers those two selectors only; no rule targets the Theia side-panel containers. Stock `dockpanel.css:71-73` animates the sash (150 ms), not panel width. Whether the stock collapse reads as a slide is a visual judgement.
severity: minor
fixable: needs-chris
result: issue
gap: G-14.1.1-32
reason: "No slide is authored; only Chris can say whether the stock collapse satisfies the contract or the slide must be written."

### 33. Embedder wall rejects an in-overlay sender
expected: A page loaded inside a web tab cannot issue webTab* requests; only the primary Theia frame is honoured. An attempt from inside an overlay is refused and logged, never acted on.
contract: 14.1-UAT.md:19-21; 14.1-VERIFICATION.md:37-40; `PowerBrowserAPI.sys.mjs:149` (primary embedder predicate), :1491 (validation reason)
evidence: unverified live — the wall's presence is asserted by `gui02-web-tab-bridge`; no check sends a request from inside an overlay
severity: critical if failed
fixable: yes (a BiDi script can evaluate inside the overlay's browsing context, dispatch the request event, and assert the overlay survives)
result: pending

### 34. Two web tabs on one URL
expected: Opening the same address in two web tabs gives two overlays, both rendering, and one store row (documented ceiling A18); closing one leaves the other intact.
contract: 14.1-UAT.md:23-25; 14.1-UI-SPEC.md:410-411, A18 (:480)
evidence: unverified live — the live check opens one tab
severity: major if failed
fixable: yes
result: pending

### 35. Continuous panel slide and splitter drag
expected: Dragging a panel splitter or watching the ≤200ms mode-switch slide keeps the page attached to the tab body with no persisting misalignment and no white flash.
contract: 14.1-UAT.md:27-29; 14.1-UI-SPEC.md:367-371, A17 (:479)
evidence: unverified live — `web-tab.ts:395-418` coalesces to one rAF per widget and skips byte-identical rects; only a real window shows the frames
severity: major if failed
fixable: needs-chris
result: pending

### 36. Chrome-side idempotency
expected: Re-issuing an open for an already-open tab reports already-open without a second overlay; a request for an unknown tab reports unknown-tab and the widget shows the lost-view state.
contract: 14.1-UAT.md:31-33; `PowerBrowserAPI.sys.mjs:2127-2138`, :2333-2336
evidence: unverified live — the outcome words are present in the host; no check drives duplicate calls
severity: major if failed
fixable: yes
result: pending

### 37. Lost-view rendering and in-flight navigation across a mode switch
expected: A tab whose overlay is lost shows "Power Browser can't show this page right now. Choose Reload to try again, or close this tab." and Reload recovers it; switching mode while a page is still loading finishes the load in place.
contract: 14.1-UAT.md:35-37; 14.1-UI-SPEC.md:242, :285-287, :338
evidence: unverified live; the first half is expected to fail today for the reason in test 4
severity: major if failed
fixable: yes for the lost-view half (test 4's check); needs-chris for the in-flight-navigation half
result: pending

### 38. Typed address while a non-web tab is current
expected: With an editor or view tab active, typing an address and pressing Enter opens a new web tab for it rather than navigating nothing.
contract: 14.1-UAT.md:39-41; 14.1-UI-SPEC.md:312-314; ROADMAP.md:113
evidence: unverified live — `chrome-bar-widget.tsx:290-302` falls through to the opener when no web tab is current; `web-tab.ts:505-507` claims http(s) at priority 1000; the live check commits only while a web tab is current
severity: major if failed
fixable: yes
result: pending

### 39. Two New Tab clicks and the startup strip
expected: Clicking "+" twice gives two "New Tab" tabs, each with its own empty state; a fresh launch starts with no web tabs unless a setup restores them (defect item 24).
contract: 14.1-UAT.md:43-45; 14.1-UI-SPEC.md:258-261, :280-284
evidence: unverified live — see test 5 for the startup half
severity: major if failed
fixable: yes
result: pending

### 40. Pill typing guard against a mid-edit state push
expected: While an address is being edited in the pill, a state push from the page (title or URL change) does not overwrite the edit; Esc restores the committed address.
contract: 14.1-UAT.md:47-49; 14.1-UI-SPEC.md:303-305
evidence: unverified live — guard at `chrome-bar-widget.tsx:435-439`; two-stage Esc at :387-397
severity: minor if failed
fixable: yes
result: pending

### 41. Occlusion by palette, dialog, and menu
expected: Opening the command palette, a Theia dialog, or a menu hides the page under it within a frame and restores it on close; the dropdown case is already confirmed.
contract: 14.1-UAT.md:51-53; 14.1-UI-SPEC.md:342-360, A8 (:470)
evidence: unverified live — `web-tab.ts:58` selects `.pb-chrome-bar-dropdown, .quick-input-widget, .lm-Widget.dialogOverlay, .lm-Menu`; :76-91 one body observer; only the dropdown was confirmed by Chris
severity: major if failed
fixable: yes
result: pending

### 42. Many web tabs overflow
expected: Opening enough web tabs to overflow the strip gives stock strip scrolling; only the current tab's page paints, every other overlay is hidden.
contract: 14.1-UAT.md:55-57; 14.1-UI-SPEC.md:515-516, :520; 13-UI-SPEC.md:76
evidence: unverified live — `PowerBrowserAPI.sys.mjs:2324` sets `docShellIsActive = !!visible` per geometry message; no check opens more than one web tab
severity: minor if failed
fixable: yes for the only-current-paints and chip-count halves; needs-chris for the strip-scrolling look
result: pending

## Summary

total: 42
passed: 0
issues: 29
pending: 13
skipped: 0
blocked: 0

Gaps by severity: critical 3 (G-1, G-2, G-3) · major 12 (G-4 … G-15) · minor 14 (G-19 … G-32).
Gaps by fixability: yes 16 (G-4, 5, 6, 7, 8, 9, 10, 19, 20, 21, 22, 23, 24, 25, 26, 27) ·
decision 6 (G-11, 12, 28, 29, 30, 31) · needs-chris 7 (G-1, 2, 3, 13, 14, 15, 32).
Pending tests automatable: yes 8 (33, 34, 36, 38, 39, 40, 41, and the lost-view half of 37) ·
needs-chris 5 (16, 17, 18, 35, and the in-flight half of 37; test 42's strip look).

Note on the zero: no test in this file has been driven to a pass. Every CONFIRMED item in
GUI-DEFECTS.md (1, 3, 8, 21, 22) stays there and is not repeated here.

## Gaps

- gap_id: G-14.1.1-1
  truth: "Content tabs never unmount, close, move windows, or detach across a mode switch, including a mode's second visit"
  contract: "14-UI-SPEC.md:232-233 and :223; ROADMAP.md:112"
  status: in_tree
  severity: critical
  test: 1
  reason: "Live testing on 2026-09-06 showed main-area content tabs disappearing on a mode's second visit."
  root_cause: "Stock `doSwitchPerspective` restores the target mode's stale saved layout and Lumino unparents every widget absent from it (14-UAT.md:112-124). Fixed by `main-area-exemption.ts` at the descriptor deactivate seam; the file is uncommitted."
  artifacts:
    - theia/extensions/modes/src/browser/main-area-exemption.ts
    - scripts/verify-mode-switch-tabs-live.mjs
    - .planning/GUI-DEFECTS.md
    - .planning/milestones/v1.3-phases/14-modes-windows-setups/14-UAT.md
  fixable: needs-chris

- gap_id: G-14.1.1-2
  truth: "The chrome-bar mode toggle applies the contracted per-mode shell map for the three shipped modes"
  contract: "14-UI-SPEC.md:225-229"
  status: in_tree
  severity: critical
  test: 2
  reason: "Coding showed no Theia view; the panel map was unreachable."
  root_cause: "`chrome-bar-widget.tsx` called `switchPerspective` directly for the shipped segments, bypassing `ModeService.activateMode` where the map lives (14-UAT.md:145-162). Rerouted through `MODES_ACTIVATE_COMMAND_ID` (`chrome-bar-widget.tsx:538`)."
  artifacts:
    - theia/extensions/chrome-bar/src/browser/chrome-bar-widget.tsx
    - theia/extensions/modes/src/browser/mode-service.ts
  fixable: needs-chris

- gap_id: G-14.1.1-3
  truth: "Every chrome-to-content reply and state push is delivered to the Theia frame"
  contract: "14.1-UI-SPEC.md:296-300, :322-325; REQUIREMENTS.md:25-26"
  status: in_tree
  severity: critical
  test: 3
  reason: "Every awaited request timed out and no state push reached the pill (14.1-03 live check, before the fix)."
  root_cause: "Through the actor child's Xray on the content window `win.JSON.parse` is not callable; `sendResponse` and `receiveMessage` threw on every call. Fixed in `GroupActorChild.sys.mjs` with the content window's own `JSON.parse` via `wrappedJSObject` (GUI-DEFECTS.md:35). Chrome-side: needs a cache-purged launch to be seen."
  artifacts:
    - powerbrowser/shell/GroupActorChild.sys.mjs
    - .planning/GUI-DEFECTS.md
  fixable: needs-chris

- gap_id: G-14.1.1-4
  truth: "A tab whose overlay chrome no longer holds renders the lost-view line at once, and one Reload brings the page back"
  contract: "14.1-UI-SPEC.md:338, :285-287, :242"
  status: open
  severity: major
  test: 4
  reason: "The widget ignores the `unknown-tab` outcome of geometry messages, so the body stays blank until a navigation-class request fails; recovery then costs a second Reload."
  root_cause: "`web-tab.ts:395-418` sends geometry with `channel.send` (no reply); only `navigate/back/forward/reload` replies pass through `settle()` (:455-458). Fix candidates: request geometry replies and fold `unknown-tab` into `setLostView`, or have `reload()` re-open in the same click when the reply is `unknown-tab`."
  artifacts:
    - theia/extensions/tab-uris/src/browser/web-tab.ts
    - powerbrowser/shell/PowerBrowserAPI.sys.mjs
  fixable: yes

- gap_id: G-14.1.1-5
  truth: "A fresh frontend has no web tabs, and a new web tab's id never collides with one recreated from a persisted layout"
  contract: "14.1-UI-SPEC.md:258-261; docs/URI-SCHEMES.md:37-40; 14.1-VERIFICATION.md:61-64"
  status: open
  severity: major
  test: 5
  reason: "GUI-DEFECTS item 24: two web tabs `wt1`/`wt2` present at startup once, source unidentified."
  root_cause: "Hypothesis — stock `ShellLayoutRestorer` (`shell-layout-restorer.js:244-257, :351`) recreates every described widget from the persisted layout, `StatefulWidget` or not, so web tabs come back with their saved `{ id: 'wt1', url }` options whenever the frontend origin repeats (in-session reload after a backend respawn on the pinned port, `TheiaService.sys.mjs:595`; or an ephemeral-port repeat). `nextTabSeq` restarts at 0 (`web-tab.ts:483`), so the next '+' reuses `wt1` and chrome answers `already-open` (`PowerBrowserAPI.sys.mjs:2137-2138`). Reproduce first: open a web tab, reload the frontend page on the same origin, read the strip."
  artifacts:
    - theia/extensions/tab-uris/src/browser/web-tab.ts
    - theia/node_modules/@theia/core/lib/browser/shell/shell-layout-restorer.js
    - powerbrowser/shell/TheiaService.sys.mjs
  fixable: yes

- gap_id: G-14.1.1-6
  truth: "Every in-shell web tab writes a PNG last-view thumbnail that its Panorama card shows"
  contract: "ROADMAP.md:155 (Phase 15 SC4); REQUIREMENTS.md:54; ROADMAP.md:114"
  status: open
  severity: major
  test: 6
  reason: "Panorama cards for in-shell web tabs have no thumbnail; since 14.1 every tab is an in-shell web tab."
  root_cause: "`scheduleSettleCapture` (`PowerBrowserAPI.sys.mjs:1251`) is scheduled only from the stock-window tab listeners (:2017, :2029) and `captureTabThumbnail` (:1285) resolves its target with `findStockTabBrowser`. The web-tab overlay's progress listener (:2205-2220) never schedules a capture and the capture cannot find an overlay `<browser>`."
  artifacts:
    - powerbrowser/shell/PowerBrowserAPI.sys.mjs
    - theia/extensions/modes/src/browser/organising-widget.ts
  fixable: yes

- gap_id: G-14.1.1-7
  truth: "After Enter, an outstanding suggestion query never re-opens the dropdown over the page"
  contract: "13-UI-SPEC.md:195-197; 14.1-UI-SPEC.md:350-354"
  status: in_tree
  severity: major
  test: 7
  reason: "A query debounced by the last keystroke and still outstanding at Enter completed after the commit and re-opened the dropdown; the occlusion term then hid the page."
  root_cause: "Fixed in `chrome-bar-widget.tsx:204-209, :218, :256-262` (`dropdownArmed`). No check exercises the timing; add the type-then-Enter-within-150ms case to `verify-web-tab-live.mjs` or `verify-chrome-bar-suggestions.mjs`."
  artifacts:
    - theia/extensions/chrome-bar/src/browser/chrome-bar-widget.tsx
    - scripts/verify-web-tab-live.mjs
  fixable: yes

- gap_id: G-14.1.1-8
  truth: "The backend tab-row reader serves the rows the chrome side writes while the browser runs"
  contract: "ROADMAP.md:114; 13-UI-SPEC.md:192"
  status: in_tree
  severity: major
  test: 8
  reason: "Suggestions and Panorama read `[]` on every launch since 13-02 for three independent reasons."
  root_cause: "(a) `TabQueryService` read `process.env.POWERBROWSER_PROFILE_DIR` after token-gate had scrubbed it (item 9); (b) mozStorage's EXCLUSIVE locking made the readonly handle `SQLITE_BUSY` (item 18, `openNotExclusive: true`); (c) esbuild bundling broke `better-sqlite3`'s native-binding resolution (item 19, external in `esbuild.mjs`). All three fixed; proven by `gui02-web-tab-live` reading the row back. Close with test 14's confirmation."
  artifacts:
    - theia/extensions/tab-uris/src/node/tab-query-service.ts
    - theia/applications/browser/esbuild.mjs
    - scripts/verify-web-tab-live.mjs
  fixable: yes

- gap_id: G-14.1.1-9
  truth: "`yarn build` in theia/ succeeds from a clean checkout"
  contract: "CLAUDE.md ## Environment; docs/BUILD.md"
  status: open
  severity: major
  test: 9
  reason: "The extension build order has tab-uris before token-gate while tab-uris imports token-gate's lib; `theia/**/lib/` is gitignored."
  root_cause: "`theia/package.json:69` hand-ordered `&&` chain; `tab-query-service.ts:26` import. Fix: reorder (token-gate before tab-uris) or build via workspace topological order; prove with a check that removes `theia/extensions/*/lib` and runs `yarn build:extensions`."
  artifacts:
    - theia/package.json
    - theia/extensions/tab-uris/src/node/tab-query-service.ts
  fixable: yes

- gap_id: G-14.1.1-10
  truth: "A --quick row goes red when any of the five group-channel preconditions regresses"
  contract: "CLAUDE.md ## Verification rule 2; GUI-DEFECTS.md:58-92"
  status: open
  severity: major
  test: 10
  reason: "`wantUntrusted`, the `resource://` child path, `document`-dispatch with `bubbles: true`, and reply cloning can each regress with every other assertion green."
  root_cause: "No registry row covers the channel; the only end-to-end proof is the full-cost live check. Add a static row over `PowerBrowserAPI.sys.mjs` (actor registration) and `group-actor-client.ts` (dispatch shape) with a `--self-test` that plants each of the five faults."
  artifacts:
    - scripts/verify-platform.sh
    - powerbrowser/shell/PowerBrowserAPI.sys.mjs
    - theia/extensions/modes/src/browser/group-actor-client.ts
  fixable: yes

- gap_id: G-14.1.1-11
  truth: "The tab strip's per-mode behaviour matches one ratified reading: Variant B relocation, or strip-stays-top with tabs following the layout"
  contract: "REQUIREMENTS.md:38-45; ROADMAP.md:88; 14-UI-SPEC.md:22-28; 13-UI-SPEC.md:219-225; owner statement 2026-09-06/07"
  status: open
  severity: major
  test: 11
  reason: "Chris says the web tab moves between modes; the ratified 14-UI-SPEC says the strip never moves. Item 2 is OPEN pending a Variant-B re-probe now that `diff-theia-core.sh` passes."
  root_cause: "Decision, not a code defect. Options: (a) re-run the 13-01 spike as the instrument now passes, record GREEN, and open Variant-B strip work under GUI-07; (b) ratify that 'moves between modes' is satisfied by persistence + following each mode's layout (what ships) and close item 2 and GUI-07's relocation clause."
  artifacts:
    - .planning/GUI-DEFECTS.md
    - .planning/REQUIREMENTS.md
    - .planning/milestones/v1.3-phases/14-modes-windows-setups/14-UI-SPEC.md
  fixable: decision

- gap_id: G-14.1.1-12
  truth: "Every fix this list depends on is committed"
  contract: "CLAUDE.md ## Verification; memory concurrent-sessions-on-main"
  status: open
  severity: major
  test: 12
  reason: "Test 1's and test 8's fixes, the Phase 14 gap-closure plans and 14-UAT.md are uncommitted in another session's working tree."
  root_cause: "Another session owns those files; this sweep must not commit them. Chris decides when that session lands them (explicit-path commits, `--quick` green)."
  artifacts:
    - theia/extensions/modes/src/browser/main-area-exemption.ts
    - theia/extensions/tab-uris/src/node/tab-query-service.ts
    - .planning/milestones/v1.3-phases/14-modes-windows-setups/14-UAT.md
  fixable: decision

- gap_id: G-14.1.1-13
  truth: "Coding docks the Explorer in a visible left panel on every activation"
  contract: "14-UI-SPEC.md:227"
  status: in_tree
  severity: major
  test: 13
  reason: "Coding showed no Theia view even once the switch was routed correctly."
  root_cause: "`leftPanelHandler.expand(id)` is a lookup that no-ops on a miss; only stock `applyViewPlacements` adds the view, and only on first activation. `ensureInArea` (`mode-service.ts:257, :297-`) docks it first."
  artifacts:
    - theia/extensions/modes/src/browser/mode-service.ts
  fixable: needs-chris

- gap_id: G-14.1.1-14
  truth: "The Organising surface shows the groups and tabs that exist — Chris's real tabs — or the contracted empty state"
  contract: "REQUIREMENTS.md:49-57; ROADMAP.md:114, :152-154; owner statement"
  status: open
  severity: major
  test: 14
  reason: "Panorama was empty (reader and writer both blind) and then unreachable; rows now flow in the live check and the surface opens, but the canvas has not been confirmed complete on Chris's screen."
  root_cause: "Three fixed causes (G-3, G-8, commit 2857353 #4). Remaining work is confirmation; if the canvas is incomplete, the new observation becomes a new gap."
  artifacts:
    - theia/extensions/modes/src/browser/organising-widget.ts
    - theia/extensions/tab-uris/src/node/tab-query-service.ts
    - .planning/GUI-DEFECTS.md
  fixable: needs-chris

- gap_id: G-14.1.1-15
  truth: "The chrome bar is full-bleed on its own row, a cold start opens in Browsing, Coding is reachable from launch, Organising renders, and Ctrl+L binds on every platform"
  contract: "13-UI-SPEC.md:174-180, :195; 14-UI-SPEC.md:226, :229; 14.1-UI-SPEC.md:316-318"
  status: in_tree
  severity: major
  test: 15
  reason: "Six defects found by the sidecar loop on 2026-09-07 (commit 2857353): bar beside the menubar at 37% width, scrollbar drift, no launch mode, Organising rendered nothing, modes.css never bundled, cmd+l rejected."
  root_cause: "Fixed in commit 2857353 (`chrome-bar-widget.tsx`, `chrome-bar.css`, `chrome-bar-keybindings.ts`, `mode-service.ts`, `modes-frontend-module.ts`, `organising-widget.ts`). Measured in the standalone sidecar only."
  artifacts:
    - theia/extensions/chrome-bar/src/browser/chrome-bar.css
    - theia/extensions/chrome-bar/src/browser/chrome-bar-keybindings.ts
    - theia/extensions/modes/src/browser/modes-frontend-module.ts
  fixable: needs-chris

- gap_id: G-14.1.1-19
  truth: "Overlay creation logs no TypeError from the gBrowser stand-in"
  contract: "CLAUDE.md ## Verification rule 1; 14.1-VERIFICATION.md:297"
  status: open
  severity: minor
  test: 19
  reason: "Every overlay creation logs `TypeError: this.documentGlobal.gBrowser.getTabForBrowser is not a function`."
  root_cause: "`powerbrowser/shell/powerbrowser.js:64` — the stand-in has only `tabs`. Add `getTabForBrowser() { return null; }`; prove with a launch-log grep after a cache-purged launch."
  artifacts:
    - powerbrowser/shell/powerbrowser.js
  fixable: yes

- gap_id: G-14.1.1-20
  truth: "verify-web-tab-live.mjs goes red on a regression to a focus-keyed pill binding"
  contract: "CLAUDE.md ## Verification rule 2; 14.1-VERIFICATION.md:74-76"
  status: open
  severity: minor
  test: 20
  reason: "The synthetic focus event hid GUI-DEFECTS item 21 for a whole checkpoint."
  root_cause: "`scripts/verify-web-tab-live.mjs:420` dispatches `new FocusEvent('focus')` on the placeholder. Remove it; the pill assertion must pass on the strip-selection binding alone, and the self-test should plant a focus-keyed binding and require red."
  artifacts:
    - scripts/verify-web-tab-live.mjs
  fixable: yes

- gap_id: G-14.1.1-21
  truth: "A failed setup save or delete tells the user the save or delete failed and that nothing changed"
  contract: "14-UI-SPEC.md:202; CLAUDE.md ## User-facing copy"
  status: open
  severity: minor
  test: 21
  reason: "`setups-service.ts:368` and `:459` flash the restore-failure string on save and delete write failures."
  root_cause: "One constant reused for three failures. Add two strings (save failed, delete failed) to the copy table and the service."
  artifacts:
    - theia/extensions/modes/src/browser/setups-service.ts
    - .planning/milestones/v1.3-phases/14-modes-windows-setups/14-UI-SPEC.md
  fixable: yes

- gap_id: G-14.1.1-22
  truth: "The Delete Setup confirm button renders in the destructive colour"
  contract: "14-UI-SPEC.md:136, :213"
  status: open
  severity: minor
  test: 22
  reason: "The dialog is a stock ConfirmDialog with accent 'Delete'."
  root_cause: "`setups-service.ts:442-447` adds no class; `modes.css:586-588` scopes `--color-danger` to `.pb-org-close-confirm` only. Add a class on the setups dialog and a matching scoped rule (same pattern)."
  artifacts:
    - theia/extensions/modes/src/browser/setups-service.ts
    - theia/extensions/modes/src/browser/modes.css
  fixable: yes

- gap_id: G-14.1.1-23
  truth: "Setup and mode names are capped at 60 characters at the input, before commit"
  contract: "14-UI-SPEC.md:269, :313"
  status: open
  severity: minor
  test: 23
  reason: "Names are sliced after the quick input resolves; the user sees the full text accepted."
  root_cause: "`setups-service.ts:347`, `mode-service.ts:323`. Use `validateInput` on the quick input to refuse past 60."
  artifacts:
    - theia/extensions/modes/src/browser/setups-service.ts
    - theia/extensions/modes/src/browser/mode-service.ts
  fixable: yes

- gap_id: G-14.1.1-24
  truth: "Exactly one backend file reads process.env.POWERBROWSER_*, and a --quick row proves it"
  contract: "CLAUDE.md ## Verification rule 2"
  status: open
  severity: minor
  test: 24
  reason: "GUI-DEFECTS item 12: the class of bug behind item 9 has no gate."
  root_cause: "No registry row. Derive the set of `theia/extensions/*/src/node/**` files matching `process.env.POWERBROWSER_` and compare as set equality to `{ powerbrowser-env.ts }`, with a self-test plant."
  artifacts:
    - scripts/verify-platform.sh
    - theia/extensions/token-gate/src/node/powerbrowser-env.ts
  fixable: yes

- gap_id: G-14.1.1-25
  truth: "INTERNAL-APIS.md line references are current and the catalogue gate is green"
  contract: "CLAUDE.md hard rule 2; 14-UAT.md:261-270 G-14-8"
  status: open
  severity: minor
  test: 25
  reason: "GUI-DEFECTS item 14 and G-14-8 record a stale row / red gate; the gate now passes."
  root_cause: "Record drift. Run `scripts/verify-platform.sh --only internals-catalogue`, then close item 14 and G-14-8."
  artifacts:
    - powerbrowser/INTERNAL-APIS.md
    - .planning/GUI-DEFECTS.md
  fixable: yes

- gap_id: G-14.1.1-26
  truth: "15-RESEARCH.md and 15-REVIEW.md record that the GUI-08 write channel never worked before 2026-09-06"
  contract: "GUI-DEFECTS.md:58-95; 14-UAT.md:222"
  status: open
  severity: minor
  test: 26
  reason: "Both documents assume a working channel."
  root_cause: "Never corrected. Add a dated correction note to each."
  artifacts:
    - .planning/milestones/v1.3-phases/15-panorama-organising/15-RESEARCH.md
    - .planning/milestones/v1.3-phases/15-panorama-organising/15-REVIEW.md
  fixable: yes

- gap_id: G-14.1.1-27
  truth: "14-UAT.md gap statuses and the 14-UI-SPEC placeholder rows describe the tree"
  contract: "14-UAT.md:182-270; 14-UI-SPEC.md:207-209, :240-247"
  status: open
  severity: minor
  test: 27
  reason: "G-14-4 (confirmed as item 8), G-14-6 (closed by 14.1), G-14-7 (superseded by 14.1), G-14-8 (catalogue PASS) still read in_tree/open/deferred; the placeholder copy is no longer shipped."
  root_cause: "Closed in other sessions without updating the record. Flip the four statuses with a pointer to GUI-DEFECTS.md, and mark the placeholder rows superseded by Phase 15 (as 14-UAT.md test 6 already does)."
  artifacts:
    - .planning/milestones/v1.3-phases/14-modes-windows-setups/14-UAT.md
    - .planning/milestones/v1.3-phases/14-modes-windows-setups/14-UI-SPEC.md
  fixable: yes

- gap_id: G-14.1.1-28
  truth: "The Organising tab has an address the pill can show"
  contract: "docs/URI-SCHEMES.md:76-77, :37-40; owner statement (every tab has a URL); 14.1-UI-SPEC.md:296-302, A6"
  status: open
  severity: minor
  test: 28
  reason: "With Organising current the pill is empty; every other main-area tab kind resolves an address."
  root_cause: "`tab-uri-registry.ts:124-184` has no row for `powerbrowser.modes.organising`. Decision: assign `view:organising` (add the factory to the view table so `verify-uri-roundtrip` derives it) or ratify the empty pill."
  artifacts:
    - theia/extensions/tab-uris/src/browser/tab-uri-registry.ts
    - theia/extensions/tab-uris/src/browser/view-factory-table.ts
    - docs/URI-SCHEMES.md
  fixable: decision

- gap_id: G-14.1.1-29
  truth: "The address placeholder promises only what a commit does"
  contract: "13-UI-SPEC.md:159, :198-199; 14.1-UI-SPEC.md:315; owner statement (the URL must work)"
  status: open
  severity: minor
  test: 29
  reason: "The placeholder says 'Search or enter address'; a non-address commit shows a failure row whose next step (Enter again) cannot succeed."
  root_cause: "`chrome-bar-widget.tsx:48-56, :352-354, :701`. Decision: change the placeholder (and the failure copy for the non-address case) or contract a search route. No search engine host may be minted without a decision (CLAUDE.md network-egress rule; 14.1-VERIFICATION prohibition)."
  artifacts:
    - theia/extensions/chrome-bar/src/browser/chrome-bar-widget.tsx
    - .planning/milestones/v1.3-phases/13-chrome-bar-strip-relocation-spike/13-UI-SPEC.md
  fixable: decision

- gap_id: G-14.1.1-30
  truth: "The setups list renders name over meta, the current row with accent marker + 600, and an in-list empty state"
  contract: "14-UI-SPEC.md:273-278, :199-200"
  status: open
  severity: minor
  test: 30
  reason: "The quick-pick host renders `$(check) name` with the meta inline and flashes the empty copy in the status bar."
  root_cause: "`setups-service.ts:709-720`. Decision: ratify the quick-pick rendering in 14-UI-SPEC, or replace the host with a list that can meet the row contract."
  artifacts:
    - theia/extensions/modes/src/browser/setups-service.ts
    - .planning/milestones/v1.3-phases/14-modes-windows-setups/14-UI-SPEC.md
  fixable: decision

- gap_id: G-14.1.1-31
  truth: "Every custom-mode and setup string on screen is in the 14-UI-SPEC copy table"
  contract: "14-UI-SPEC.md:191-214; CLAUDE.md ## User-facing copy"
  status: open
  severity: minor
  test: 31
  reason: "Six strings ship uncontracted (listed in test 31)."
  root_cause: "Written during 14-02/14-03 without a copy-table update. Decision: add them to the table verbatim or replace them."
  artifacts:
    - theia/extensions/modes/src/browser/mode-service.ts
    - theia/extensions/modes/src/browser/setups-service.ts
    - .planning/milestones/v1.3-phases/14-modes-windows-setups/14-UI-SPEC.md
  fixable: decision

- gap_id: G-14.1.1-32
  truth: "Side panels slide on a mode switch within 200 ms, instantly under reduced motion"
  contract: "14-UI-SPEC.md:172-174, :231-232, :320"
  status: open
  severity: minor
  test: 32
  reason: "No slide is authored; the stock collapse is what shows."
  root_cause: "`modes.css` transitions cover custom rows and the dependent-closed button only (:53, :107, :152, :116-121). Chris judges whether the stock collapse passes; if not, author a width transition on the side-panel containers with the reduced-motion override."
  artifacts:
    - theia/extensions/modes/src/browser/modes.css
  fixable: needs-chris
