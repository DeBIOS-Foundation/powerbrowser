# Internal APIs Catalogue

D-96/D-97: `powerbrowser/shell/PowerBrowserAPI.sys.mjs` is the **only** file in
`powerbrowser/` permitted to reach a Firefox internal. Every row below is one
distinct occurrence of a forbidden pattern (`Services.*`, `Cc`/`Ci`/`Cr`/`Cu`,
XPCOM constructors, `AppConstants`, an unqualified `ChromeUtils.import*`) as
enumerated by `scripts/check-internals-boundary.sh`'s own
`FORBIDDEN_PATTERNS` array, run in `--catalogue` mode directly against
`PowerBrowserAPI.sys.mjs` (the one file the default `--quick`/full scan
excludes). The rule is written once in that script; this document is derived
from it, not maintained beside it — `check-internals-boundary.sh --catalogue`
fails on any occurrence with no matching row here, naming the uncatalogued
`file:line`.

One row = one touchpoint + why, the same discipline
`powerbrowser/endpoint-allowlist.json` already uses for network hosts.

## Touchpoints

| Internal | File:Line | `PowerBrowserAPI` Method | Purpose | Threat Notes |
|---|---|---|---|---|
| `resource://gre/modules/Subprocess.sys.mjs` + `resource://gre/modules/ctypes.sys.mjs` (`ChromeUtils.defineESModuleGetters`) | `PowerBrowserAPI.sys.mjs:15` | *(module-level lazy import, backs `spawnProcess`/`pathSearch` and, since Phase 5, `signalBarePid`)* | Lazy-imports Firefox's `Subprocess` module (`spawnProcess`/`pathSearch`, launches and locates the Theia backend) and, added by 05-01-PLAN.md Task 2, `ctypes` (`signalBarePid`, opens `libc.so.6` to call `kill(2)` on a pid this process did not itself spawn — the SIDE-04 leftover-reap case, where there is no `Process` object / `proc.kill()` to use). | **Process-spawn row.** The spawned backend runs unprivileged but with the user's own file access. Phase 2's **D-29** already records that `@theia/ai-terminal`'s backend `ShellExecutionServer` calls `spawn(command, [], { shell: true })` with **no permission logic** — all approval machinery is frontend-only — so Theia's AI terminal is an arbitrary-execution surface reachable the moment the page loads. The sidecar's per-launch token gate (see the cookie row below) is the sole guard against reaching that surface from off-host. **Bare-pid-kill row (D-111):** `signalBarePid` is called by `TheiaService._reapLeftover()` only after `readProcessStartTicks` has confirmed the recorded pid's `/proc/<pid>/stat` start-time ticks match the state file's recorded value **exactly** (string equality, no tolerance). A mismatch is never signalled — the operating system has recycled that pid onto an unrelated process, and `_reapLeftover()` returns without calling `signalBarePid` at all. No port-, process-name-, or process-tree-based selection is used anywhere in the reap path; the state file's own recorded pid plus its exact start-time is the only identity this project ever trusts before sending a real signal to a process it did not spawn. |
| `resource://gre/modules/AppConstants.sys.mjs` (`ChromeUtils.defineESModuleGetters`) | `PowerBrowserAPI.sys.mjs:23` | *(module-level lazy import, backs `getAppIdentity`)* | Added by 05-03-PLAN.md Task 1: lazy-imports `AppConstants` so `getAppIdentity()` can read `MOZ_APP_VERSION_DISPLAY`, a build-time preprocessor substitution (`upstream/browser/config/version_display.txt`), never a runtime-mutable value. `AppConstants` is on `FORBIDDEN_PATTERNS` in full, so this reaches it from the one lazy-getter block rather than importing it anywhere else. | **Identity row.** `getAppIdentity()` (this line plus `PowerBrowserAPI.sys.mjs:245`/`:102` below) renders state and identity **only** — brand name, vendor, and a build-time version string — and carries no credential, no token, and no cookie value. It backs both the diagnostics layer (SHELL-04) and the D-120-repointed branding-identity check; neither surface nor sentinel it feeds ever includes `this._token` or any other secret. |
| `resource://gre/modules/Sqlite.sys.mjs` (`ChromeUtils.defineESModuleGetters`) | `PowerBrowserAPI.sys.mjs:27` | *(module-level lazy import, backs `openTabStore`/`writeTabRow`/`removeTabRow`/`readTabRow`/`listTabRows`/`pruneClosedTabRows`/`checkTabStoreIntegrity`/`quarantineAndRebuildTabStore`/`ensureTabStore`)* | Added by 12-01-PLAN.md Task 1 (SQL-01): lazy-imports the in-tree `Sqlite` module so the single chrome-side writer can open profile-relative `tabs.sqlite`, run the forward migration chain, and upsert tab rows with bound parameters. Reached from the one lazy-getter block like every other Gecko module. | **Writer row (T-12-01, T-12-02).** The sole read-write opener of `tabs.sqlite` — no other process, test helper, or migration path opens the file read-write. Every statement binds URI/URL/title strings as parameters, never interpolated (the quote-plus-unicode title row in the roundtrip proof exercises this path). |
| `resource://gre/modules/PrivateBrowsingUtils.sys.mjs` (`ChromeUtils.defineESModuleGetters`) | `PowerBrowserAPI.sys.mjs:28` | *(module-level lazy import, backs `writeTabRow`)* | Added by 12-01-PLAN.md Task 1 (SQL-01): lazy-imports `PrivateBrowsingUtils` so `writeTabRow` can run the `isWindowPrivate` check on the tab's chrome window BEFORE the upsert — a private tab never reaches SQL, and no private column exists to select on. | **Private-filter row (T-12-03).** Exclusion is total at the single writer; the emitter-exercising absence gate lands in plan 12-02. |
| `resource:///modules/sessionstore/SessionStore.sys.mjs` (`ChromeUtils.defineESModuleGetters`) | `PowerBrowserAPI.sys.mjs:29` | *(module-level lazy import, backs `parseSessionStoreTabRows`)* | Added by 12-01-PLAN.md Task 1 (SQL-01): lazy-imports `SessionStore` so `parseSessionStoreTabRows` can read `getBrowserState()` (a JSON string) as the quarantine-rebuild source and the sweep input. Sessionstore stays authoritative for restore throughout — the store rebuilds FROM it, never the reverse. | **Rebuild-source row (T-12-04).** Read-only projection of live tabs; carries tab address/title fields only, never credentials (T-12-06). |
| `resource://gre/modules/PageThumbs.sys.mjs` (`ChromeUtils.defineESModuleGetters`) | `PowerBrowserAPI.sys.mjs:32` | *(module-level lazy import, backs `captureTabThumbnail`)* | Added by 15-02-PLAN.md Task 3 (GUI-08): lazy-imports `PageThumbs` so `captureTabThumbnail` can draw a stock tab browser into a canvas at card width (160px target). Reached from the one lazy-getter block like every other Gecko module; the name matches no `FORBIDDEN_PATTERNS` entry, so the guard needs no new pattern for this touchpoint. | **Thumbnail-capture row.** Capture stays chrome-side (`captureToCanvas` + `toDataURL`, 100KB capture cap, private-window skip at schedule and capture time); the frontend keeps rendering bytes-or-text with no privileged surface. |
| `resource://gre/modules/PlacesUtils.sys.mjs` (`ChromeUtils.defineESModuleGetters`) | `PowerBrowserAPI.sys.mjs:37` | *(module-level lazy import, backs `readHistoryEntry`/`readBookmarkByUrl`/`listBookmarkFolder`)* | Added by 12-02-PLAN.md Task 2 (SQL-04): lazy-imports `PlacesUtils` so history, bookmark, and folder-listing reads go through the platform `fetch`/`getFolderContents` APIs. Reached from the one lazy-getter block like every other Gecko module. | **Places-read row (T-12-11).** Reads use `History.fetch`, `Bookmarks.fetch`, and `getFolderContents` only; no new code names the places file directly, and the projected fields carry tab address/title data only, never credentials. |
| `SessionStore.getBrowserState` (via the `PowerBrowserAPI.sys.mjs:29` import, no new import) | `PowerBrowserAPI.sys.mjs:817` | `projectSessionStoreTabs` | Added by 12-02-PLAN.md Task 2 (SQL-04): the sessionstore read projection for consumers. Delegates to the 12-01 `parseSessionStoreTabRows` parser -- the single JSON-string parse -- so the rebuild source, the sweep input, and this read surface can never disagree on shape. | Same rebuild-source note as `PowerBrowserAPI.sys.mjs:29` above (T-12-04): tab address/title fields only, never credentials (T-12-06). |
| `Services.wm.getEnumerator` | `PowerBrowserAPI.sys.mjs:1849` | `startTabStoreTriggers` | Added by 12-01-PLAN.md Task 2 (SQL-01): enumerates stock `navigator:browser` windows so the writer can attach the TabOpen/TabClose/TabSelect/TabAttrModified family on each window's tab container. The shell window carries no tab browser, so it never matches. | **Trigger row.** Enumeration only; each event calls the write/remove wrapper keyed by the opaque browser-tab rule. No Theia-to-chrome channel, no actor registration. |
| `Services.obs.addObserver` | `PowerBrowserAPI.sys.mjs:1881` | `startTabStoreTriggers` | Added by the 12-CODE-REVIEW.md CR-04 fix: observes `browser-delayed-startup-finished` (pinned `upstream/browser/base/content/browser-init.js:792`, window as subject) so stock windows opened AFTER the startup enumeration above attach the same tab-event family through the one `attachToWindow` path. Without it the TabClose removal half covers only windows that existed at startup while the sweep's 7-day retention prune leaves later windows' closed rows stale for a week. | Same trigger row as `PowerBrowserAPI.sys.mjs:1849` above. Late-window attachment only; no new channel, no actor registration. |
| `Services.obs.addObserver` | `PowerBrowserAPI.sys.mjs:1889` | `startTabStoreTriggers` | Added by 12-01-PLAN.md Task 2 (SQL-01): observes `sessionstore-state-write-complete` to run the bounded reconciliation sweep (upsert missing rows, prune closed ones). | Same trigger row as `PowerBrowserAPI.sys.mjs:1849` above. Sessionstore stays the restore authority; the sweep only aligns the projection. |
| `Services.obs.removeObserver` | `PowerBrowserAPI.sys.mjs:1899` | `startTabStoreTriggers` | Added by 12-01-PLAN.md Task 2 (SQL-01): unregisters the sweep observer via the stop function `startTabStoreTriggers` returns, alongside per-window listener removal. | Same trigger row as `PowerBrowserAPI.sys.mjs:1849` above. |
| `Services.obs.removeObserver` | `PowerBrowserAPI.sys.mjs:1904` | `startTabStoreTriggers` | Added by the 12-CODE-REVIEW.md CR-04 fix: unregisters the late-window observer via the same stop function. | Same trigger row as `PowerBrowserAPI.sys.mjs:1849` above. |
| `Services.prefs.getStringPref` | `PowerBrowserAPI.sys.mjs:171` | `getStringPref` | Reads a string pref (sidecar config: `nodePath`, `backendMain`), never throwing — returns the caller's fallback on any error. | None — local config read, no externally-controlled input. |
| `Services.prefs.getIntPref` | `PowerBrowserAPI.sys.mjs:183` | `getIntPref` | Reads an int pref (sidecar timeouts/intervals/log-buffer size), never throwing. | None — local config read, no externally-controlled input. |
| `Services.env.get` | `PowerBrowserAPI.sys.mjs:191` | `getEnv` | Reads an environment variable used to help resolve sidecar paths. | None — host-controlled, feeds only `spawnProcess`'s environment, no network input. |
| `Services.dirsvc.get` / `Ci.nsIFile` | `PowerBrowserAPI.sys.mjs:204` | `getProfileDir` | Added as part of the CR-01 fix (05-REVIEW.md): reads the absolute path of this launch's own Firefox profile directory (`ProfD`) off the platform's directory service, never throwing (resolves `""` on any failure). Backs `TheiaService._profileStateKey()`, which suffixes the sidecar state file's name with a sanitized form of this path. | **State-file-scoping row.** Before this fix, `TheiaService._resolveConfigDir()`/`_stateFilePath` were keyed only on `XDG_CONFIG_HOME`/`HOME`, with no profile component at all — every PowerBrowser instance under the same OS user read and wrote the identical `sidecar-state.json` regardless of `--profile`. A second instance's startup reap (`_reapLeftover`, D-111's verified-identity check) would then find a FIRST instance's live, healthy backend pid in that shared file, confirm its `/proc/<pid>/stat` start ticks match exactly (they do — it genuinely is that process), and SIGTERM it: the identity check itself was sound, only its scope was wrong. `getProfileDir()` carries no credential and is read-only local filesystem-path state; the only new capability it grants is disambiguating one instance's persisted state from another's. |
| `Services.appinfo` | `PowerBrowserAPI.sys.mjs:245` | `getAppIdentity` | Added by 05-03-PLAN.md Task 1: reads `name`/`vendor` off the platform's own app-info service (the same values `application.ini`'s `Name=`/`Vendor=` lines back) for the diagnostics layer's identity fields and the startup identity sentinel. | Same identity-row note as `PowerBrowserAPI.sys.mjs:23` above. |
| `AppConstants.MOZ_APP_VERSION_DISPLAY` | `PowerBrowserAPI.sys.mjs:251` | `getAppIdentity` | Added by 05-03-PLAN.md Task 1: reads the build's display-version constant — the same value `about:support`'s own version string uses — for the diagnostics layer's identity fields and the startup identity sentinel. | Same identity-row note as `PowerBrowserAPI.sys.mjs:23` above. |
| `Services.cookies.add` | `PowerBrowserAPI.sys.mjs:293` | `setSessionCookie` | Mints the per-launch `POWERBROWSER_TOKEN` session cookie that gates every plain-HTTP route on the backend (D-99: `secure=false` is deliberate — the sidecar is loopback HTTP, and a secure cookie would never be sent). | **Cookie row.** The token gates HTTP and the WebSocket upgrade, but Phase 2's **D-66** records that the WS origin validator returns `true` when the `Origin` header is absent entirely — so anything that can reach `127.0.0.1:PORT` and omit `Origin` bypasses that check, and the token is the only real barrier left. The token's secrecy is what stands between a co-resident process and the backend, which is why it is **not** carried in the spawn environment. It used to be, and `/proc/<pid>/environ` is the exec-time snapshot: readable by every same-uid process for the life of the backend, never rewritten by anything the backend deletes from its own `process.env`, and (with Yama `ptrace_scope=1`, this target's setting) readable when `/proc/<pid>/mem` is not. `writeStdinLine` hands it over the stdin pipe instead, on every spawn including respawns, so D-104's respawn-accepts-the-same-cookie property holds without ever putting the credential where a co-resident reader can find it. `verify-platform.sh`'s `side04-token-not-in-environment` asserts its absence from the backend's own environ and from every child's. |
| `Ci.nsICookie.SAMESITE_LAX` | `PowerBrowserAPI.sys.mjs:303` | `setSessionCookie` | `SameSite=Lax` constant applied to the token cookie. **Lax, not Strict:** the shell's one swap is a top-level navigation from `chrome://powerbrowser/...` to `http://127.0.0.1:PORT/`, which is cross-site, and Strict withholds the cookie on precisely that navigation — the backend answers the shell's own first request with 403 and Theia never loads. Lax still sends it on that top-level GET while withholding it from cross-site subresources and POSTs. | Same cookie-row note as `PowerBrowserAPI.sys.mjs:293` above (D-66) — this constant is part of the one cookie-mint call. |
| `Ci.nsICookie.SCHEME_HTTP` | `PowerBrowserAPI.sys.mjs:304` | `setSessionCookie` | Cookie-scheme constant permitting the token cookie over plain HTTP, matching D-99's loopback-HTTP rationale. | Same cookie-row note as `PowerBrowserAPI.sys.mjs:293` above (D-66). |
| `Ci.nsICookieValidation.eOK` | `PowerBrowserAPI.sys.mjs:311` | `setSessionCookie` | Compares `add()`'s returned `nsICookieValidation.result` against `eOK` and throws when the cookie was rejected. | Same cookie-row note as `PowerBrowserAPI.sys.mjs:293` above (D-66). `nsICookieManager.add()` reports a rejected cookie by **returning** a validation object rather than throwing, so discarding it turns an unusable token cookie into a silent 403 at the far end of the swap — which is precisely how an expired `aExpiry` went unnoticed through Phase 4. This check is what makes that failure loud. |
| `Cc["@mozilla.org/timer;1"]` / `Ci.nsITimer` | `PowerBrowserAPI.sys.mjs:383` | `sleep` | Platform one-shot timer backing the supervisor's health-loop delay — a `sys.mjs` module's global scope has no `setTimeout`, confirmed against multiple in-tree modules that either import `Timer.sys.mjs` or hand-roll it with `nsITimer`. | None — no externally-controlled input, purely an internal timing primitive. |
| `Ci.nsITimer.TYPE_ONE_SHOT` | `PowerBrowserAPI.sys.mjs:405` | `sleep` | Timer-type constant for the one-shot delay, part of the same `sleep` call. | None — see `PowerBrowserAPI.sys.mjs:383` above. |
| `Services.obs.addObserver` | `PowerBrowserAPI.sys.mjs:446` | `onQuitGranted` | Registers `TheiaService`'s shutdown hook on `quit-application-granted` — the final, non-cancellable quit topic. | **Quit-observer row.** If the browser is force-killed (`SIGKILL`, a crash, `kill -9`) rather than quit normally, this observer never fires and the spawned backend receives no signal at all, leaving an orphaned process. This gap is explicitly out of this phase's scope and is named here rather than discovered later: it is Phase 5's **SIDE-04** (zero orphaned processes after `SIGKILL`, process-group kill semantics plus startup cleanup of leftovers). |
| `Services.obs.removeObserver` | `PowerBrowserAPI.sys.mjs:447` | `onQuitGranted` | Unregisters the quit observer via the unsubscribe function `onQuitGranted` returns. | Same quit-observer note as `PowerBrowserAPI.sys.mjs:446` above (Phase 5 SIDE-04). |
| `Services.obs.notifyObservers` | `PowerBrowserAPI.sys.mjs:470` | `notifyStartupFinished` | Fires `browser-idle-startup-tasks-finished`, the topic Firefox's own chrome fires from `browser/base/content/browser-init.js` once a browser window finishes initializing. The PowerBrowser shell is deliberately **not** that chrome, so nothing else in the process ever fires it. | **Automation-reachability row.** WebDriver's `session.new` blocks on this topic (`remote/components/RemoteAgent.sys.mjs`; `remote/webdriver-bidi/WebDriverBiDi.sys.mjs` awaits `browserStartupFinished`). Without it no WebDriver session can ever be created against the shell, so *every* automated check that drives the real binary is unrunnable — the condition that made Phase 4's shell checks look environmentally blocked. Fired once, from the shell's own `DOMContentLoaded`, after the shell has painted; it announces window readiness only and grants no capability. |
| `Cu.getGlobalForObject` | `PowerBrowserAPI.sys.mjs:482` | `createPermanentKey` | Mints a `permanentKey` object in the system global for the shell's one `<browser>`, exactly as `browser/components/tabbrowser/content/tabbrowser.js:2743` does for every tab it creates. Created in the JSM global, not the window's, so the key stays valid if anything outlives the window. | **Automation-reachability row**, same class as `PowerBrowserAPI.sys.mjs:470`. `remote/shared/NavigableManager.sys.mjs:131` returns a **null** context id for any browser lacking a `permanentKey`, so without this the shell's browsing context is enumerable but unaddressable and no WebDriver command can target it. Mints a plain empty object used only as an identity token — it carries no data and grants no capability. |
| `fixupAndLoadURIString` | `PowerBrowserAPI.sys.mjs:499` | `loadURIInBrowser` | Navigates the shell's one content `<browser>` to the Theia backend URL — the single swap SHELL-05 sequences after both readiness sentinels. | **Navigation row.** This is the only navigation the shell ever performs and its target is always the loopback backend the supervisor just health-gated, never externally-supplied input. It previously lived in `powerbrowser.js` with a comment saying it stayed there to avoid this file — a real internals touch outside the boundary, uncatalogued, while `check-internals-boundary.sh` reported PASS because its pattern list did not name this API. Both this and `nodePrincipal` are in `FORBIDDEN_PATTERNS` now, so the same class of hole fails the guard instead of passing it. |
| `ownerDocument.nodePrincipal` | `PowerBrowserAPI.sys.mjs:500` | `loadURIInBrowser` | Supplies the triggering principal for that one navigation: the shell is loaded from `chrome://`, so its document principal is the system principal. | Same navigation row as `PowerBrowserAPI.sys.mjs:499` above. Using the system principal is what lets a chrome document initiate the load at all; the load target is fixed by the supervisor, so this does not widen what can be navigated to. |
| `Services.wm.getMostRecentWindow` | `PowerBrowserAPI.sys.mjs:611` | `findShellWindow` | Added by 05-04-PLAN.md Task 1: looks up the shell's own already-open window by the `windowtype` `powerbrowser.xhtml`'s root element declares (`"powerbrowser:main"`), for the single-instance command-line handler below. Returns `null`, never throwing, when none exists. | **Single-instance-handler row.** The handler never opens a window and never reads a command-line argument; it acts only when an existing window of this application's own window type is found, and does nothing at all on the first launch (no window yet) or a launch naming a different profile (the platform's `nsRemoteService` keys remoting on the profile path, `upstream/toolkit/xre/nsAppRunner.cpp:5342-5358`, so a different-profile launch is a wholly separate OS process that never reaches this handler). |
| `Services.ww.openWindow` | `PowerBrowserAPI.sys.mjs:628` | `openShellWindow` | Added by 01-05-PLAN.md Task 1 (D-20): opens the shell's own chrome document as a top-level window. This is **startup-window selection**, moved here out of `patches/020-powerbrowser-shell.patch`'s compiled `BROWSER_CHROME_URL` override so that define keeps its stock upstream value. Called by `PowerBrowserSingleInstanceHandler.handle()` on the process's own initial launch only. | **Startup-window row.** The URL is a compile-time literal `chrome://powerbrowser/content/...`, never derived from a command-line argument or any other externally-supplied input — the handler deliberately reads no argument at all. Opening the window is what lets the handler then set `cmdLine.preventDefault`, which is the documented gate the stock default handler (`upstream/browser/components/BrowserContentHandler.sys.mjs`) checks before opening a window of its own; the same gate's `else` branch closes the early `navigator:blank` window, producing the startup flicker `01-UI-SPEC.md` accepts and documents. |
| `Cc["@mozilla.org/supports-string;1"]` / `Ci.nsISupportsString` | `PowerBrowserAPI.sys.mjs:657` | `openBrowserWindow` | Added by 01-05-PLAN.md Task 1: wraps the optional URL argument the way upstream's own `openBrowserWindow` does, so a bare JS string does not go down `loadOneOrMoreURIs`'s `"\|"`-splitting path. | **Browser-window row.** See `PowerBrowserAPI.sys.mjs:659` below — this XPCOM construction carries only the caller's URL string and grants nothing on its own. |
| `Services.ww.openWindow` | `PowerBrowserAPI.sys.mjs:659` | `openBrowserWindow` | Added by 01-05-PLAN.md Task 1 (GUI-01): opens ONE stock upstream browser window — a real `chrome://browser/content/browser.xhtml` with its own address bar, tab strip, and in-window modal dialogs — optionally loading a caller-supplied URL. | **Browser-window row / T-05-01, T-05-03.** This is the one privileged operation GUI-01's entry affordance reaches. It accepts a URL and a request to open a window and **nothing else**: no chrome-document parameter, no feature string, no script, and no general chrome-eval surface, so a caller that can reach it can do no more than a user pressing Ctrl+N and typing an address. A stock browser window does expose navigation, devtools and `about:` surfaces the chrome-less shell deliberately did not (T-05-03, dispositioned *accept* — reachable stock chrome is exactly what GUI-01 asks for), which is why `scripts/verify-endpoints.sh` is re-run deliberately after this change. |
| `AppConstants.BROWSER_CHROME_URL` | `PowerBrowserAPI.sys.mjs:661` | `openBrowserWindow` | Added by 01-05-PLAN.md Task 1: reads the build's own browser-chrome-URL constant rather than hardcoding a chrome path, so this opens whatever upstream considers the main browser document. | **Browser-window row, and the one that records why the move matters.** Upstream compares `window.location.href` against this same constant in five places to decide "am I the main browser window" (`browser.js:1312/1335/4677`, `browser-commands.js:297/358`, `utilityOverlay.js:418`). While `patches/020` overrode the define to point at the shell, a real browser window failed every one of those tests: no address-bar focus from `openLocation()`, `gDialogBox` nulled so in-window modal dialogs were suppressed, and `loadOneOrMoreURIs` re-opening the shell. Reading the constant here is therefore also an assertion that the define is stock; if a future patch re-overrode it, this call would silently start opening the shell again instead of a browser window. |
| `ChromeUtils.registerWindowActor` | `PowerBrowserAPI.sys.mjs:1359` | `registerGroupActor` | Added by 15-01-PLAN.md Task 2 (GUI-08): registers the PowerBrowserGroup actor pair (parent in this file, child in `GroupActorChild.sys.mjs`), scoped by matches to the Theia local origin. The SOLE registration caller in this tree -- the guard fails any second one by design (WINDOWS.md #13 procedure: guard entry plus catalogue row in the same commit as first use). | **Group-actor row (W5).** Registration crosses the privilege boundary no matter which URL it names, so no exemption applies. The matches pin keeps the child out of stock-window web content; the parent re-checks the sender origin per message; chrome-side field validation is the wall behind that. |
| `Ci.nsICommandLine.STATE_INITIAL_LAUNCH` | `PowerBrowserAPI.sys.mjs:1918` | `isInitialLaunch` | Added by 01-05-PLAN.md Task 1: the command-line state constant `PowerBrowserSingleInstanceHandler` branches on to tell its own process's first launch from a remote handoff sent by a second launch of the same binary against the same profile. | Same single-instance-handler row as `PowerBrowserAPI.sys.mjs:611` above. The constant is read-only and the predicate returns a boolean; the handler still never reads a command-line *argument*. |
| `Ci.nsICommandLineHandler` | `PowerBrowserAPI.sys.mjs:1960` | *(the `PowerBrowserSingleInstanceHandler` class's own `QueryInterface`, registered via `powerbrowser/shell/components.conf`)* | Added by 05-04-PLAN.md Task 1: the interface constant `ChromeUtils.generateQI` needs to identify this class as a `command-line-handler` category component. | Same single-instance-handler row as `PowerBrowserAPI.sys.mjs:611` above. The handler's whole body is wrapped so a thrown error is logged (`PowerBrowserAPI.log`) and swallowed, never propagated into the platform's own handler enumeration, which would otherwise break every handler still due to run after it. |
| `Services.wm.getEnumerator` | `PowerBrowserAPI.sys.mjs:1237` | `findStockTabBrowser` (shared by `closeStockTabByUri` + `captureTabThumbnail`) | Added by 15-02-PLAN.md Task 3 (GUI-08): the ONE stock-window enumeration behind both the group-close path and the thumbnail capture path, so the two can never disagree on which live browser an opaque URI names. Same enumeration the tab-store triggers use; the shell window carries no tab browser, so it never matches. | **Group-close/thumbnail row.** Enumeration only; callers ask the matched tab to close through `gBrowser.removeTab` or draw it through `PageThumbs.captureToCanvas`. |
| `Services.io.newURI` | `PowerBrowserAPI.sys.mjs:119` | *(module-level `groupSenderSpecIsTheia`, backs `groupSenderIsTheia`/`handleGroupMutation`)* | Added by the 15-REVIEW.md CR-01 fix: parses the actor sender's documentURI spec through the platform IO service so the W5 second wall compares parsed host (`127.0.0.1`/`localhost`, any port) instead of an exact-or-slash-prefix string match against portless `http://127.0.0.1` — the string match nacked 100% of production traffic because Theia is always served with a port (`TheiaService._swap`). | **Origin-check row (W5).** `newURI` plus `schemeIs("http")` plus `host` comparison only; the parsed host is compared against the two loopback names, never interpolated into SQL or navigated to. A naive `startsWith` repair would admit `http://127.0.0.1.evil.com/`; the host comparison rejects it. |

## Deliberately not touched

| Internal | Reason |
|---|---|
| Firefox's session store (`sessionstore.jsonlz4`, `SessionStore.sys.mjs`) | **D-107:** Phase 4 introduces no persistence of its own — port, PID, token, and the supervisor's log buffer are all per-launch, in-memory state; nothing writes session files, and no dependence on Firefox's SessionStore is added. **D-108:** naming it here, un-touched, keeps the storage ground clean for the future unified SQL store (SEED-001, dormant this milestone), whose first real consumer (the chrome-owned TabModel) is post-4.0. **SQL-01 (12-01) amendment:** the tab-store writer now lazy-imports `SessionStore.sys.mjs` (`PowerBrowserAPI.sys.mjs:29`) and reads `getBrowserState()` in `parseSessionStoreTabRows` as the quarantine-rebuild source and sweep input — a read-only projection, catalogued above. Sessionstore stays authoritative for restore throughout: the store rebuilds FROM it and never replaces it, and nothing writes session files. |

## Consistency

Enforced by `scripts/check-internals-boundary.sh --catalogue`, run as the
`internals-catalogue` check in `verify-platform.sh`'s `--quick` set.
It re-derives the occurrence list above straight from
`PowerBrowserAPI.sys.mjs` using the guard script's own `FORBIDDEN_PATTERNS`
enumeration and fails on any occurrence whose `file:line` has no matching
row here — the two can never silently drift apart. `--self-test` proves the
check can go red on both a planted boundary violation and a planted removal
from this document.
